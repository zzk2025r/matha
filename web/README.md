# Matha AI Assistant — 小白友好型数学计算平台

> 🧮 零安装门槛 · 自然语言计算 · 全平台支持

## 快速开始

### Web 版（零安装，最推荐小白）

```bash
# 1. 确保已安装 Python 3.10+
# 2. 启动服务
python src/ai_assistant_server.py

# 3. 浏览器打开 http://localhost:8080
# 手机/平板在同一 WiFi 下访问本机 IP:8080
```

### 桌面版

```bash
python src/ai_assistant_desktop.py
```

### 演示模式

```bash
python main.py --demo
```

---

## 小白使用指南

### 你可以这样问：

| 你说 | 效果 |
|---|---|
| `计算 3 加 5` | 输出 8 |
| `求 100 的平方根` | 输出 10 |
| `找出 1 到 100 的素数` | 列出所有素数 |
| `求 [1,2,3,4,5] 的平均值` | 输出 3.0 |
| `计算 5 的阶乘` | 输出 120 |
| `自由落体 3 秒` | 计算下落距离 |
| `斜抛 30度，速度 20` | 计算射程 |
| `解释什么是素数` | 概念讲解 |
| `help` | 显示帮助 |

### 内置数学概念库

- 加法、乘法、除法
- 平方、开方
- 平均值、统计
- 素数、阶乘
- 三角函数
- 物理公式（自由落体、斜抛等）

---

## 架构说明

```
┌─────────────────────────────────────────────────────┐
│                   前端 (HTML/JS)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 聊天界面  │  │ 任务面板  │  │ 概念速查  │          │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘          │
│       │             │             │                 │
│       └─────────────┴─────────────┘                 │
│                    │                                │
│              POST /api/chat                         │
└────────────────────┼────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│            后端 API (ai_assistant_server.py)          │
│  ┌──────────────────────────────────────────┐       │
│  │  MathaAIAssistant.chat(text)             │       │
│  │    ├── FriendlyIntentParser.classify()   │       │
│  │    ├── decompose() → 步骤分解            │       │
│  │    ├── 生成 Matha 代码                    │       │
│  │    ├── Interpreter.run() 执行             │       │
│  │    └── explain_error() 错误翻译           │       │
│  └──────────────────────────────────────────┘       │
│                        │                            │
│                ┌───────▼────────┐                   │
│                │  Matha 解释器   │                   │
│                │  (src/interp.py)│                   │
│                └────────────────┘                   │
└─────────────────────────────────────────────────────┘
```

---

## 跨平台发布

### PWA（手机/平板安装）
在 Chrome/Safari 中打开网页 → 添加到主屏幕 → 像原生 App 一样使用

### Electron 桌面应用
```bash
# 构建
pip install electron-builder
electron-builder --win --mac --linux
```

### 打包为exe（Windows）
```bash
pip install pyinstaller
pyinstaller --onefile --windowed src/ai_assistant_desktop.py
```

---

## 测试

```bash
python tests/test_ai_assistant.py
```
