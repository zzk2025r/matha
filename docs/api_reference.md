# Matha API 参考文档

> 自动生成于 2026-08-31 | 版本 v3.0
> 共 5995 个文档条目

## 目录

### adapters.language_adapters

- [AdaptResult](`#class-adaptresult`)
- [LanguageAdapter](`#class-languageadapter`)
- [PythonAdapter](`#class-pythonadapter`)
- [RustAdapter](`#class-rustadapter`)
- [LanguageAdapterRegistry](`#class-languageadapterregistry`)

**方法**
- `def __init__(self, name: str, ext: str)`
- `def adapt(self, matha_ir: str) -> AdaptResult`
- `def translate(self, matha_ir: str) -> str`
- `def _cache_key(self, code: str) -> str`
- `def _get_cached(self, key: str) -> Optional[Any]`
- `def _save_cache(self, key: str, result: Any)`
- `def __init__(self)`
- `def adapt(self, matha_ir: str) -> AdaptResult`
- `def translate(self, matha_ir: str) -> str`
- `def __init__(self, optimize: bool)`
- `def adapt(self, matha_ir: str) -> AdaptResult`
- `def translate(self, matha_ir: str) -> str`
- `def _compile(self, rust_code: str, cache_key: str) -> Optional[str]`
- `def register(cls, name: str, adapter: LanguageAdapter)`
- `def get(cls, name: str) -> Optional[LanguageAdapter]`
- `def list_adapters(cls) -> List[str]`

### ai_assistant

- [IntentType](`#class-intenttype`)
- [Step](`#class-step`)
- [Task](`#class-task`)
- [ChatMessage](`#class-chatmessage`)
- [FriendlyIntentParser](`#class-friendlyintentparser`)
- [MathaAIAssistant](`#class-mathaaiassistant`)

**方法**
- `def __init__(self)`
- `def classify(self, text: str) -> tuple[IntentType, float]`
- `def _infer_from_numbers(self, text: str, nums: list[float]) -> Optional[IntentType]`
- `def learn(self, text: str, intent: IntentType) -> None`
- `def record_failure(self, text: str, error: str, intent: IntentType) -> None`
- `def _auto_learn_from_failure(self, text: str, error: str) -> None`
- `def record_correction(self, original_text: str, original_intent: str, corrected_intent: str) -> None`
- `def get_growth_stats(self) -> dict`
- `def extract_numbers(self, text: str) -> list[float]`
- `def extract_range(self, text: str) -> Optional[tuple[int, int]]`
- `def extract_variable(self, text: str) -> Optional[str]`
- `def decompose(self, text: str) -> list[Step]`
- `def _decompose_arithmetic(self, text: str, nums: list[float]) -> list[Step]`
- `def _decompose_math_func(self, text: str, nums: list[float]) -> list[Step]`
- `def _decompose_statistics(self, text: str, nums: list[float]) -> list[Step]`
- `def _decompose_string(self, text: str) -> list[Step]`
- `def _decompose_array(self, text: str, nums: list[float]) -> list[Step]`
- `def _decompose_physics(self, text: str, nums: list[float]) -> list[Step]`
- `def _decompose_trig(self, text: str, nums: list[float]) -> list[Step]`
- `def _decompose_number_theory(self, text: str, rng: Optional[tuple], nums: list[float]) -> list[Step]`
- `def _decompose_unit_convert(self, text: str, nums: list[float]) -> list[Step]`
- `def execute_steps(self, steps: list[Step], interp) -> list[dict]`
- `def explain_error(self, error_msg: str) -> dict`
- `def explain_intent(self, text: str) -> dict`
- `def explain_concept(self, topic: str) -> dict`
- `def create_task(self, title: str, steps: list[Step]) -> Task`
- `def get_task(self, task_id: str) -> Optional[Task]`
- `def list_tasks(self, status: Optional[str]) -> list[Task]`
- `def mark_done(self, task_id: str) -> bool`
- `def add_message(self, msg: ChatMessage)`
- `def get_history(self, limit: int) -> list[ChatMessage]`
- `def _decompose_commonsense(self, text: str, intent: IntentType) -> list[Step]`
- `def _try_alternative(self, text: str, intent: IntentType) -> list[Step]`
- `def __init__(self)`
- `def chat(self, text: str, interp) -> dict`
- `def get_growth_report(self) -> dict`
- `def help(self) -> str`

**函数**
- `def launch_desktop()` — 启动桌面应用（通过 webview 嵌入）。

### ai_assistant_server

- [APIHandler](`#class-apihandler`)

**函数**
- `def run(host: str, port: int)` — 启动 Web 服务。

**方法**
- `def log_message(self, format, *args)`
- `def _send_json(self, data: dict, status: int)`
- `def _read_body(self) -> str`
- `def do_OPTIONS(self)`
- `def do_GET(self)`
- `def do_POST(self)`
- `def _serve_file(self, path: str, content_type: str)`

### ast_nodes

- [Program](`#class-program`)
- [Annotation](`#class-annotation`)
- [CommandLiteral](`#class-commandliteral`)
- [SetUpItem](`#class-setupitem`)
- [SetUp](`#class-setup`)
- [LoopFraction](`#class-loopfraction`)
- [SegLoopSuffix](`#class-segloopsuffix`)
- [GlobalLoopSuffix](`#class-globalloopsuffix`)
- [OutputTrail](`#class-outputtrail`)
- [FileMarker](`#class-filemarker`)
- [GlobalIdStmt](`#class-globalidstmt`)
- [MechUnit](`#class-mechunit`)
- [Generate](`#class-generate`)
- [GenStmt](`#class-genstmt`)
- [Binding](`#class-binding`)
- [PathExpr](`#class-pathexpr`)
- [SetConstruct](`#class-setconstruct`)
- [DictLiteral](`#class-dictliteral`)
- [Iteration](`#class-iteration`)
- [TupleExpr](`#class-tupleexpr`)
- [ListLiteral](`#class-listliteral`)
- [IndexExpr](`#class-indexexpr`)
- [SliceExpr](`#class-sliceexpr`)
- [ChainStmt](`#class-chainstmt`)
- [IntegerLit](`#class-integerlit`)
- [FloatLit](`#class-floatlit`)
- [StringLit](`#class-stringlit`)
- [BoolLit](`#class-boollit`)
- [Variable](`#class-variable`)
- [BinaryOp](`#class-binaryop`)
- [UnaryOp](`#class-unaryop`)
- [AngleExpr](`#class-angleexpr`)
- [Belongs](`#class-belongs`)
- [Output](`#class-output`)
- [ReadBlock](`#class-readblock`)
- [NLBlock](`#class-nlblock`)
- [Lambda](`#class-lambda`)
- [CodeBlock](`#class-codeblock`)
- [FuncApp](`#class-funcapp`)
- [IfExpr](`#class-ifexpr`)
- [LetBinding](`#class-letbinding`)
- [LetTupleBinding](`#class-lettuplebinding`)
- [IfStmt](`#class-ifstmt`)
- [WhileStmt](`#class-whilestmt`)
- [ForStmt](`#class-forstmt`)
- [LoopStep](`#class-loopstep`)
- [LoopWhile](`#class-loopwhile`)
- [MatchStmt](`#class-matchstmt`)
- [BasicType](`#class-basictype`)
- [SetType](`#class-settype`)
- [FuncType](`#class-functype`)
- [TupleType](`#class-tupletype`)
- [AnnotatedType](`#class-annotatedtype`)
- [FuncDef](`#class-funcdef`)
- [StructDef](`#class-structdef`)
- [EnumDef](`#class-enumdef`)
- [AliasDef](`#class-aliasdef`)
- [ModuleDecl](`#class-moduledecl`)
- [ImportDecl](`#class-importdecl`)
- [GoStmt](`#class-gostmt`)
- [ChanExpr](`#class-chanexpr`)
- [SendExpr](`#class-sendexpr`)
- [SelectStmt](`#class-selectstmt`)
- [DefineOp](`#class-defineop`)

**函数**
- `def _node_to_dict(node: Any) -> Any` — 递归把 AST 节点（或普通值）转为可 JSON 序列化的结构。
- `def ast_to_dict(node: Any) -> dict | list | Any` — 把任意 AST 节点转为 dict/list/基础值。
- `def ast_to_json(node: Any, indent: int) -> str` — 把任意 AST 节点转为 JSON 字符串。
- `def program_to_dict(source: str) -> dict` — 把 Matha 源码解析为 dict 形式的 AST。
- `def program_to_json(source: str, indent: int) -> str` — 把 Matha 源码解析为 JSON 字符串形式的 AST。
- `def tokens_to_dict(source: str) -> list[dict]` — 把 Matha 源码的 Token 流导出为 dict 列表（供其它语言做词法分析）。
- `def tokens_to_json(source: str, indent: int) -> str` — 把 Token 流导出为 JSON。

### async_runtime

- [ThreadPool](`#class-threadpool`)
- [EventLoop](`#class-eventloop`)
- [Mutex](`#class-mutex`)
- [Semaphore](`#class-semaphore`)
- [Condition](`#class-condition`)
- [AsyncSupport](`#class-asyncsupport`)

**函数**
- `def get_thread_pool() -> ThreadPool` — 
- `def get_event_loop() -> EventLoop` — 

**方法**
- `def __init__(self, max_workers: int) -> None`
- `def _get_pool(self) -> concurrent.futures.ThreadPoolExecutor`
- `def submit(self, fn: Callable, *args, **kwargs) -> concurrent.futures.Future`
- `def map(self, fn: Callable, *iterables)`
- `def shutdown(self, wait: bool) -> None`
- `def max_workers(self) -> int`
- `def max_workers(self, value: int) -> None`
- `def __init__(self) -> None`
- `def _get_loop(self) -> asyncio.AbstractEventLoop`
- `def run(self, coro: Coroutine) -> Any`
- `def run_async(self, coro: Coroutine) -> None`
- `def close(self) -> None`
- `def __init__(self) -> None`
- `def acquire(self) -> bool`
- `def release(self) -> None`
- `def __enter__(self)`
- `def __exit__(self, *args)`
- `def __init__(self, value: int) -> None`
- `def acquire(self) -> bool`
- `def release(self) -> None`
- `def __init__(self, mutex: Optional[Mutex]) -> None`
- `def wait(self) -> None`
- `def notify(self) -> None`
- `def notify_all(self) -> None`
- `def __enter__(self)`
- `def __exit__(self, *args)`
- `def is_async_func(func: Callable) -> bool`
- `def await_func(func: Callable, *args) -> Any`
- `def parallel_await(coros: list[Coroutine]) -> list[Any]`

### async_runtime_v2

- [GState](`#class-gstate`)
- [Goroutine](`#class-goroutine`)
- [GoroutineScheduler](`#class-goroutinescheduler`)
- [Channel](`#class-channel`)
- [Actor](`#class-actor`)
- [AsyncSyntax](`#class-asyncsyntax`)
- [AsyncRuntime](`#class-asyncruntime`)

**函数**
- `def async_spawn(fn: Callable, *args, **kwargs) -> int` — 便捷函数：启动 goroutine。
- `def async_wait(gid: int, timeout: float) -> Any` — 便捷函数：等待 goroutine。
- `def new_channel(capacity: int) -> Channel` — 创建 channel。
- `def create_actor(name: str, handler: Callable) -> Actor` — 创建 actor。

**方法**
- `def __init__(self, num_workers: int) -> None`
- `def spawn(self, fn: Callable, *args, **kwargs) -> int`
- `def spawn_channel(self, fn: Callable, *args, **kwargs) -> 'Channel'`
- `def _channel_producer(self, ch: 'Channel', fn: Callable, *args, **kwargs) -> None`
- `def _wakeup(self) -> None`
- `def _run_loop(self) -> None`
- `def wait(self, gid: int, timeout: float) -> Any`
- `def wait_all(self, gids: list[int], timeout: float) -> list[Any]`
- `def shutdown(self, wait: bool) -> None`
- `def pending_count(self) -> int`
- `def __init__(self, capacity: int) -> None`
- `def send(self, value: Any) -> None`
- `def recv(self, timeout: float) -> Any`
- `def close(self) -> None`
- `def send_error(self, error: Exception) -> None`
- `def closed(self) -> bool`
- `def __iter__(self)`
- `def __next__(self) -> Any`
- `def drain(self) -> list`
- `def __init__(self, name: str, handler: Callable) -> None`
- `def start(self) -> int`
- `def tell(self, message: Any) -> None`
- `def ask(self, message: Any, timeout: float) -> Any`
- `def stop(self) -> None`
- `def is_alive(self) -> bool`
- `def is_coroutine(obj: Any) -> bool`
- `def await_func(func: Callable, *args) -> Any`
- `def async_map(func: Callable, items: list) -> list`
- `def async_filter(func: Callable, items: list) -> list`
- `def __init__(self, goroutines: int) -> None`
- `def spawn(self, fn: Callable, *args, **kwargs) -> int`
- `def spawn_channel(self, fn: Callable, *args, **kwargs) -> Channel`
- `def wait(self, gid: int, timeout: float) -> Any`
- `def wait_all(self, gids: list[int], timeout: float) -> list`
- `def create_actor(self, name: str, handler: Callable) -> Actor`
- `def get_actor(self, name: str) -> Optional[Actor]`
- `def shutdown(self) -> None`
- `def pending(self) -> int`

### auth.api

- [ChangeType](`#class-changetype`)
- [ChangeTarget](`#class-changetarget`)
- [PermissionChangeRequest](`#class-permissionchangerequest`)
- [PermissionChangeResult](`#class-permissionchangeresult`)
- [PermissionChangeAPI](`#class-permissionchangeapi`)

**函数**
- `def example_usage() -> None` — 权限变更 API 使用示例。

**方法**
- `def __init__(self, rbac: RBACMiddleware, mgr) -> None`
- `def add_role(self, username, roles, operator, reason)`
- `def remove_role(self, username, roles, operator, reason)`
- `def set_roles(self, username, roles, operator, reason)`
- `def update_users(self, usernames)`
- `def get_user_roles(self, username: str) -> list[str]`
- `def get_role_permissions(self, role: str) -> set[str]`
- `def list_roles(self) -> list[str]`
- `def audit_log(self) -> list[dict]`
- `def clear_audit_log(self) -> int`
- `def _check_admin_permission(self, operator: str) -> None`

### auth.exceptions

- [AuthError](`#class-autherror`)
- [AuthenticationError](`#class-authenticationerror`)
- [AuthorizationError](`#class-authorizationerror`)
- [TokenError](`#class-tokenerror`)
- [RegistrationError](`#class-registrationerror`)

**方法**
- `def __init__(self, message: str, code: str)`
- `def __init__(self, message: str)`
- `def __init__(self, message: str)`
- `def __init__(self, message: str)`
- `def __init__(self, message: str, code: str)`

**函数**
- `def _b64url_encode(data: bytes) -> str` — 
- `def _b64url_decode(s: str) -> bytes` — 解码 base64url 字符串（兼容 Python 3.14 严格模式）。
- `def encode_token(payload: dict, secret: str, exp_hours: float) -> str` — 签发 JWT access token。
- `def decode_token(token: str, secret: str) -> Optional[dict]` — 验证并解码 JWT。过期或签名不符返回 None。
- `def encode_refresh_token(payload: dict, secret: str, exp_days: float) -> str` — 签发 JWT refresh token（有效期更长）。
- `def decode_refresh_token(token: str, secret: str) -> Optional[dict]` — 验证 refresh token。
- `def get_token_expiry(token: str, secret: str) -> Optional[float]` — 获取 token 剩余有效时间（秒），无效时返回 None。

### auth.models

- [User](`#class-user`)
- [Session](`#class-session`)

**方法**
- `def to_dict(self) -> dict`
- `def is_expired(self) -> bool`
- `def to_dict(self) -> dict`

**函数**
- `def _b64url_encode(data: bytes) -> str` — 
- `def _b64url_decode(s: str) -> bytes` — 解码 base64url 字符串（兼容 Python 3.14 严格模式）。
- `def hash_password(password: str, rounds: int) -> str` — 对密码进行 PBKDF2 哈希。格式: base64url(salt) . base64url(dk)。
- `def verify_password(password: str, password_hash: str) -> bool` — 验证密码是否匹配哈希值。
- `def validate_password_strength(password: str) -> tuple[bool, str]` — 校验密码强度，返回 (是否通过, 错误信息)。

### auth.rbac

- [Permission](`#class-permission`)
- [RBACMiddleware](`#class-rbacmiddleware`)

**函数**
- `def get_rbac() -> RBACMiddleware` — 获取全局 RBAC 实例。
- `def reset_rbac() -> None` — 重置全局 RBAC 实例（用于测试）。

**方法**
- `def __init__(self, resource: str, action: str)`
- `def __eq__(self, other: object) -> bool`
- `def __hash__(self) -> int`
- `def __repr__(self) -> str`
- `def __init__(self) -> None`
- `def register_role(self, name: str, permissions: Collection[str]) -> None`
- `def remove_role(self, name: str) -> bool`
- `def get_role_permissions(self, role: str) -> frozenset`
- `def list_roles(self) -> list[str]`
- `def _match(self, pattern: str, target: str) -> bool`
- `def _user_has_permission(self, roles: list[str], permission: str) -> bool`
- `def _get_merged_permissions(self, roles: list[str]) -> frozenset`
- `def has_permission(self, roles: list[str], permission: str) -> bool`
- `def authorize(self, roles: list[str], permission: str, resource: str) -> None`
- `def get_effective_permissions(self, roles: list[str]) -> frozenset`
- `def require_permission(self, permission: str) -> Callable`
- `def require_any_permission(self, *permissions) -> Callable`

### auth.service

- [SessionManager](`#class-sessionmanager`)

**函数**
- `def _validate_password(password: str) -> tuple[bool, str]` — 

**方法**
- `def __init__(self) -> None`
- `def register(self, username: str, email: str, password: str, roles: list[str] | None) -> User`
- `def login(self, username: str, password: str) -> Session`
- `def refresh_token(self, refresh_token: str) -> tuple[str, str]`
- `def logout(self, session_id: str) -> bool`
- `def invalidate_all_sessions(self, username: str) -> int`
- `def get_user(self, username: str) -> Optional[User]`
- `def get_session(self, session_id: str) -> Optional[Session]`
- `def verify_access_token(self, token: str) -> Optional[dict]`
- `def get_active_session_count(self, username: str) -> int`
- `def get_all_usernames(self) -> list[str]`
- `def count_users(self) -> int`

### autonomous

- [KnowledgeDiscovery](`#class-knowledgediscovery`)
- [MathaCodeGenerator](`#class-mathacodegenerator`)
- [LanguageConverter](`#class-languageconverter`)
- [SelfEvolution](`#class-selfevolution`)
- [_Sample](`#class-_sample`)
- [_OptResult](`#class-_optresult`)
- [_GrowResult](`#class-_growresult`)
- [AutoDebugger](`#class-autodebugger`)
- [PerformanceOptimizer](`#class-performanceoptimizer`)
- [SelfGrower](`#class-selfgrower`)

**函数**
- `def auto_debug(interp, src, max_attempts)` — 便捷函数：自动调试。
- `def auto_optimize_memoize(interp, func_name)` — 便捷函数：记忆化优化。
- `def self_grow(interp, src, name)` — 便捷函数：自成长学习。
- `def _install_matha_builtins(interp)` — 安装 Matha 侧自升级内建函数。
- `def build_software(interp, requirement: str) -> dict` — 自主构建软件。

**方法**
- `def __init__(self, matha_root: str) -> None`
- `def discover(self) -> list[dict]`
- `def _scan_directory(self, path: str) -> None`
- `def _scan_matha_file(self, filepath: str) -> None`
- `def _detect_category(self, filepath: str) -> str`
- `def search(self, keyword: str) -> list[dict]`
- `def generate(cls, template_name: str, **kwargs) -> str`
- `def python_to_matha(code: str) -> str`
- `def js_to_matha(code: str) -> str`
- `def __init__(self) -> None`
- `def learn(self, source: str, category: str) -> dict`
- `def optimize(self, func_name: str, data: list, target: str) -> dict`
- `def evolve(self, source: str, iterations: int) -> dict`
- `def __init__(self, calls, avg_ms, times, args)`
- `def __init__(self, 成功, 变更)`
- `def __init__(self, 成功, 新能力, 名称)`
- `def __init__(self, interp)`
- `def debug(self, src, max_attempts)`
- `def __init__(self, interp)`
- `def profile(self, func_name, args, runs)`
- `def hotspot(self)`
- `def optimize_memoize(self, func_name)`
- `def __init__(self, interp)`
- `def learn(self, src, name)`
- `def learn_from_file(self, filepath)`
- `def specialize(self, func_name, args)`

**函数**
- `def setup_logging(verbose: bool)` — 设置日志级别。
- `def print_section(title: str)` — 打印章节标题。
- `def benchmark(func, iterations: int, warmup: int) -> dict` — 性能基准测试。
- `def benchmark_symbolic_derivative()` — 符号求导性能测试。
- `def benchmark_symbolic_integral()` — 符号积分性能测试。
- `def benchmark_definite_integral()` — 定积分性能测试。
- `def benchmark_matrix_multiply()` — 矩阵乘法性能测试。
- `def benchmark_matrix_determinant()` — 矩阵行列式性能测试。
- `def benchmark_matrix_inverse()` — 矩阵求逆性能测试。
- `def benchmark_matrix_eigenvalues()` — 特征值计算性能测试。
- `def benchmark_svd()` — SVD 分解性能测试。
- `def benchmark_combined()` — 综合性能测试（符号微积分 + 矩阵运算）。
- `def main()` — 主函数。

**函数**
- `def setup_logging(verbose: bool)` — 设置日志级别。默认 INFO，--verbose 时启用 DEBUG。
- `def print_section(title: str)` — 打印章节标题。
- `def benchmark(func, iterations: int, warmup: int) -> dict` — 性能基准测试。
- `def benchmark_inverse_python(sizes: list)` — 纯 Python 矩阵求逆性能测试。
- `def benchmark_inverse_numpy(sizes: list)` — NumPy 矩阵求逆性能测试。
- `def benchmark_inverse_cached(sizes: list)` — 缓存矩阵求逆性能测试。
- `def benchmark_svd_python(sizes: list)` — 纯 Python SVD 性能测试。
- `def benchmark_svd_numpy(sizes: list)` — NumPy SVD 性能测试。
- `def benchmark_svd_cached(sizes: list)` — 缓存 SVD 性能测试。
- `def benchmark_parallel_inverse(n: int, n_matrices: int, n_workers: int)` — 并行矩阵求逆性能测试。
- `def benchmark_parallel_svd(n: int, n_matrices: int, n_workers: int)` — 并行 SVD 性能测试。
- `def print_comparison_report(inverse_results: dict, svd_results: dict)` — 打印性能对比报告。
- `def main()` — 主函数。

### benchmarks.benchmark_svd_analysis

- [BenchmarkResult](`#class-benchmarkresult`)

**函数**
- `def setup_logging(verbose: bool)` — 设置日志级别。
- `def benchmark_svd_python(A_data: List[List[float]], iterations: int, warmup: int) -> float` — 基准测试纯 Python SVD。
- `def benchmark_svd_numpy(A_data: List[List[float]], iterations: int, warmup: int) -> Optional[float]` — 基准测试 NumPy SVD。
- `def generate_random_matrix(n: int, seed: int) -> List[List[float]]` — 生成随机矩阵。
- `def run_benchmark(sizes: List[int]) -> List[BenchmarkResult]` — 运行所有基准测试。
- `def print_performance_chart(results: List[BenchmarkResult], numpy_available: bool)` — 打印性能对比图表。
- `def print_analysis_report(results: List[BenchmarkResult], numpy_available: bool)` — 打印性能分析报告。
- `def generate_csv_report(results: List[BenchmarkResult], output_path: str)` — 生成 CSV 格式报告。
- `def main()` — 主函数。

**函数**
- `def main()` — 生成最终性能总结图表。

**函数**
- `def setup_logging(verbose: bool)` — 设置日志级别。
- `def generate_summary()` — 生成性能总结。

### benchmarks.svd_performance_prediction

- [SVDPerformancePredictor](`#class-svdperformancepredictor`)

**函数**
- `def setup_logging(verbose: bool)` — 设置日志级别。
- `def main()` — 主函数。

**方法**
- `def __init__(self)`
- `def fit_power_law(self, sizes: List[int], times: List[float]) -> Dict`
- `def predict_time(self, n: int, method: str, sparsity: float) -> float`
- `def predict_all(self, target_sizes: List[int]) -> Dict`
- `def generate_chart(self, results: Dict, output_path: Optional[str]) -> str`
- `def generate_csv_report(self, results: Dict, output_path: str)`

**函数**
- `def to_binary(n: int) -> str` — 整数转二进制字符串（无前缀）。

10 → "1010"，0 → "0"，-5 → "-101"
- `def from_binary(s: str) -> int` — 二进制字符串转整数。

"1010" → 10，支持空格分隔（"1010 0011" → 163）
- `def to_ternary(n: int) -> str` — 整数转三进制字符串（无前缀）。

21 → "210"，0 → "0"，-5 → "-12"
- `def from_ternary(s: str) -> int` — 三进制字符串转整数。

"210" → 21，支持空格分隔
- `def binary_to_ternary(s: str) -> str` — 二进制字符串转三进制字符串。

"1010"(=10) → "101"(=10)  即 10 的三进制表示
- `def ternary_to_binary(s: str) -> str` — 三进制字符串转二进制字符串。

"210"(=21) → "10101"(=21)
- `def to_matha_binary(n: int) -> str` — 整数转 Matha 专属二进制编码。

10 → "M2:1010"
- `def to_matha_ternary(n: int) -> str` — 整数转 Matha 专属三进制编码。

21 → "M3:210"
- `def from_matha_code(code: str) -> int` — Matha 专属编码转整数。

"M2:1010" → 10，"M3:210" → 21
无前缀时按十进制解析（兼容传统
- `def matha_binary_to_ternary(code: str) -> str` — Matha 二进制编码转三进制编码。

"M2:1010" → "M3:101"
- `def matha_ternary_to_binary(code: str) -> str` — Matha 三进制编码转二进制编码。

"M3:210" → "M2:10101"
- `def encode(n: int, radix: str) -> str` — 便捷编码入口。

radix: "matha-binary" / "matha-ternary" / "binary" 

**函数**
- `def bootstrap_compile(source: str, target: str, optimize: bool) -> str` — Bootstrap 编译 Matha 源码。
- `def compile_and_benchmark() -> dict` — 编译并基准测试。
- `def show_mir_example() -> None` — 展示 MIR 示例。

### codegen.backend

- [BackendGenerator](`#class-backendgenerator`)

**方法**
- `def generate(self) -> CodegenResult`
- `def _build_python(self) -> str`
- `def _build_routes(self) -> str`
- `def _handler_body(self, ep, body_var: str) -> str`
- `def _class_name(self) -> str`
- `def _escape_py(text: str) -> str`

### codegen.base

- [CodegenError](`#class-codegenerror`)
- [Element](`#class-element`)
- [Endpoint](`#class-endpoint`)
- [StyleRule](`#class-stylerule`)
- [AppSpec](`#class-appspec`)
- [CodegenResult](`#class-codegenresult`)
- [Generator](`#class-generator`)

**函数**
- `def parse_app_spec(spec: Any) -> AppSpec` — 把 Matha 列表规格树解析为 AppSpec。

输入格式：
    ["应用", 类型, 名称, [元素...]]
- `def _parse_item(item: Any, app: AppSpec) -> None` — 把单个规格项分类解析到 app 的对应列表。
- `def _parse_element(item: Any) -> Optional[Element]` — 递归解析子元素。
- `def codegen(spec: Any, out_dir: str) -> CodegenResult` — 按规格类型分发到对应生成器，写出文件。

Args:
    spec: Matha 列表规格树
    out_dir

**方法**
- `def is_void(self) -> bool`
- `def as_dict(self) -> dict`
- `def __init__(self, app: AppSpec, out_dir: str)`
- `def generate(self) -> CodegenResult`
- `def _write(self, filename: str, content: str) -> str`
- `def _serialize_attrs(attrs: list[tuple[str, str]]) -> str`
- `def _escape_html(text: str) -> str`
- `def _element_to_html(self, el: Element, indent: int) -> str`

### codegen.desktop

- [DesktopGenerator](`#class-desktopgenerator`)

**方法**
- `def generate(self) -> CodegenResult`
- `def _build_python(self) -> str`
- `def _widget_code(self, el: Element, idx: int, var_name: str) -> tuple[str, list[str], list[str]]`
- `def _build_layout(self) -> list[str]`
- `def _attrs_dict(self, el: Element) -> dict[str, str]`
- `def _extract_handler(onclick: str) -> str`
- `def _class_name(self) -> str`
- `def _escape_py(text: str) -> str`

### codegen.game

- [GameGenerator](`#class-gamegenerator`)

**方法**
- `def generate(self) -> CodegenResult`
- `def _build_html(self) -> str`
- `def _build_js(self, players, enemies, collects, walls, cw: str, ch: str) -> str`
- `def _attrs_dict(self, el: Element) -> dict[str, str]`
- `def _escape_html(text: str) -> str`

### codegen.kernel

- [KernelSpec](`#class-kernelspec`)
- [KernelGenerator](`#class-kernelgenerator`)

**函数**
- `def generate_kernel(spec, out_dir)` — 快捷函数：生成内核代码。

spec: Matha 列表规格（同 AppSpec 格式）
out_dir: 输出目录（默

**方法**
- `def __init__(self, spec: list)`
- `def _parse(self, spec: list) -> None`
- `def generate(self) -> CodegenResult`
- `def _gen_boot_asm(self, spec: KernelSpec) -> str`
- `def _gen_kernel_asm(self, spec: KernelSpec) -> str`
- `def _gen_makefile(self, spec: KernelSpec) -> str`
- `def _build_syscall_table(self, spec: KernelSpec) -> list`

### codegen.model3d

- [Model3DGenerator](`#class-model3dgenerator`)

**方法**
- `def generate(self) -> CodegenResult`
- `def _build_html(self) -> str`
- `def _build_meshes_js(self, meshes: list, default_mat: str, default_color: str) -> str`
- `def _geometry_js(self, tag: str, a: dict) -> str | None`
- `def _material_js(self, material: str, color: str) -> str`
- `def _build_lights_js(self, lights: list) -> str`
- `def _build_camera_js(self, camera_cfg: dict | None) -> str`
- `def _build_anim_js(self, animation: str, meshes: list) -> str`
- `def _attrs_dict(self, el: Element) -> dict[str, str]`
- `def _escape_html(text: str) -> str`

### codegen.system

- [SystemGenerator](`#class-systemgenerator`)

**方法**
- `def generate(self) -> CodegenResult`
- `def _build_sh(self) -> str`
- `def _build_bat(self) -> str`
- `def _safe_sh(self, text: str) -> str`
- `def _ep_to_sh(self, ep) -> str`
- `def _ep_to_bat(self, ep) -> str`

### codegen.web

- [WebGenerator](`#class-webgenerator`)

**方法**
- `def generate(self) -> CodegenResult`
- `def _build_html(self) -> str`
- `def _build_css(self) -> str`
- `def _build_js(self) -> str`

### collaboration

- [Collaborator](`#class-collaborator`)
- [Operation](`#class-operation`)
- [ChatMessage](`#class-chatmessage`)
- [Invite](`#class-invite`)
- [OperationTransformer](`#class-operationtransformer`)
- [CollaborationSession](`#class-collaborationsession`)

**函数**
- `def create_session(session_id: str, project_id: str) -> CollaborationSession` — 创建协作会话
- `def get_session(session_id: str) -> Optional[CollaborationSession]` — 获取协作会话
- `def destroy_session(session_id: str) -> bool` — 销毁协作会话
- `def list_sessions() -> List[dict]` — 列出所有会话

**方法**
- `def transform(self, op_a: Operation, op_b: Operation) -> tuple[Operation, Operation]`
- `def _adjust_after_delete(self, op: Operation, deleted: Operation) -> Operation`
- `def apply_operations(self, operations: List[Operation], state: dict) -> dict`
- `def _apply_single(self, op: Operation, state: dict) -> dict`
- `def __init__(self, session_id: str, project_id: str)`
- `def collaborator_count(self) -> int`
- `def collaborators(self) -> Dict[str, Collaborator]`
- `def join(self, user_id: str, username: str, color: str) -> Collaborator`
- `def leave(self, user_id: str) -> None`
- `def update_cursor(self, user_id: str, x: float, y: float) -> None`
- `def broadcast_operation(self, op: Operation) -> None`
- `def get_operation_history(self, limit: int) -> List[Operation]`
- `def send_message(self, user_id: str, username: str, content: str, is_system: bool) -> ChatMessage`
- `def get_chat_history(self, limit: int) -> List[ChatMessage]`
- `def create_invite(self, inviter_id: str, inviter_name: str, max_members: int) -> Invite`
- `def redeem_invite(self, code: str, user_id: str, username: str) -> Optional[Collaborator]`
- `def get_invite_info(self, code: str) -> Optional[dict]`
- `def on_operation(self, callback: Callable) -> None`
- `def on_chat(self, callback: Callable) -> None`
- `def on_invite(self, callback: Callable) -> None`
- `def on_member_join(self, callback: Callable) -> None`
- `def on_member_leave(self, callback: Callable) -> None`
- `def get_state(self) -> dict`
- `def to_dict(self) -> dict`

### compiler.aot

- [Trampoline](`#class-trampoline`)
- [MathaAOTCompiler](`#class-mathaaotcompiler`)
- [LoopUnroller](`#class-loopunroller`)
- [SIMDVectorizer](`#class-simdvectorizer`)
- [AOTProfiler](`#class-aotprofiler`)
- [CompilerCache](`#class-compilercache`)

**方法**
- `def run(fn: Callable) -> Any`
- `def yield_(fn: Callable) -> tuple`
- `def __init__(self) -> None`
- `def compile_program(self, program_ast) -> dict`
- `def _build_namespace(self) -> dict`
- `def _ast_to_python(self, program_ast) -> str`
- `def _compile_func(self, func_def) -> list[str]`
- `def _compile_stmt(self, stmt) -> list[str]`
- `def _compile_block(self, block) -> list[str]`
- `def _compile_expr(self, expr) -> str`
- `def compile_and_cache(self, source: str, program_ast) -> dict`
- `def unroll(code: str, threshold: int) -> str`
- `def vectorize(expr: str, array_name: str) -> str`
- `def is_vectorizable(code: str) -> bool`
- `def __init__(self) -> None`
- `def profile(self, name: str, source: str, iterations: int) -> dict`
- `def report(self) -> str`
- `def __init__(self, cache_dir: str) -> None`
- `def get(self, key: str) -> Optional[dict]`
- `def set(self, key: str, value: dict) -> None`
- `def invalidate(self, key: str) -> None`
- `def clear(self) -> None`
- `def stats(self) -> dict`

### compiler.ir

- [IRBackend](`#class-irbackend`)
- [CompileTarget](`#class-compiletarget`)
- [CompileResult](`#class-compileresult`)
- [IRCompiler](`#class-ircompiler`)
- [PythonCompiler](`#class-pythoncompiler`)
- [LLVMCompiler](`#class-llvmcompiler`)
- [CCompiler](`#class-ccompiler`)
- [CompilerFactory](`#class-compilerfactory`)

**函数**
- `def compile_mir(mir_code: str, backend: str, **kwargs) -> CompileResult` — 便捷函数：编译 Matha IR。
- `def execute_mir(mir_code: str, backend: str, **kwargs) -> Any` — 便捷函数：执行 Matha IR。

**方法**
- `def __post_init__(self)`
- `def is_ok(self) -> bool`
- `def unwrap(self) -> Any`
- `def to_dict(self) -> Dict`
- `def __init__(self, backend: IRBackend)`
- `def compile(self, mir_code: str, target: CompileTarget) -> CompileResult`
- `def _cache_key(self, code: str) -> str`
- `def _get_cached(self, key: str, target: CompileTarget) -> Optional[CompileResult]`
- `def _save_cache(self, key: str, result: CompileResult, target: CompileTarget)`
- `def __init__(self)`
- `def compile(self, mir_code: str, target: CompileTarget) -> CompileResult`
- `def _translate(self, mir_code: str) -> str`
- `def __init__(self)`
- `def compile(self, mir_code: str, target: CompileTarget) -> CompileResult`
- `def _mir_to_llvm(self, mir_code: str) -> str`
- `def _compile_llvm(self, llvm_ir: str, cache_key: str, target: CompileTarget) -> Optional[str]`
- `def _execute(self, exe_path: str) -> Optional[str]`
- `def __init__(self)`
- `def compile(self, mir_code: str, target: CompileTarget) -> CompileResult`
- `def _translate(self, mir_code: str) -> str`
- `def _compile_c(self, c_code: str, cache_key: str, target: CompileTarget) -> Optional[str]`
- `def _execute(self, exe_path: str) -> Optional[str]`
- `def get_compiler(cls, backend: IRBackend) -> IRCompiler`
- `def compile(cls, mir_code: str, target: Optional[CompileTarget]) -> CompileResult`

### compiler.jit

- [ConstantFolder](`#class-constantfolder`)
- [MathaJITCompiler](`#class-mathajitcompiler`)

**函数**
- `def jit_func(func: Callable, auto_memoize: bool, pattern: Optional[str]) -> Callable` — 装饰器：JIT 编译函数并自动 memoize 递归函数。

用法：
    @jit_func
    def fib
- `def get_jit_compiler(cache_dir: str) -> MathaJITCompiler` — 获取全局 JIT 编译器实例。
- `def jit_compile(expr: str, cache_dir: str) -> Callable` — 便捷函数：JIT 编译表达式。
- `def jit_stats() -> Dict` — 获取 JIT 编译器统计。

**方法**
- `def __init__(self) -> None`
- `def fold(self, expr_str: str, env: dict) -> Any`
- `def clear_cache(self) -> None`
- `def __init__(self, cache_dir: str)`
- `def compile_expr(self, expr_str: str) -> Callable`
- `def _matha_to_python(self, expr: str) -> str`
- `def _cache_key(self, expr: str) -> str`
- `def _cache_file(self, key: str) -> Path`
- `def _save_to_cache(self, expr: str, fn: Callable) -> None`
- `def _load_from_cache(self, expr: str) -> Optional[Callable]`
- `def clear_cache(self) -> None`
- `def get_stats(self) -> Dict`
- `def compile_func(self, func: Callable, auto_memoize: bool, pattern: Optional[str]) -> Callable`
- `def _extract_source(self, func: Callable) -> str`
- `def _detect_recursion(self, source: str, func_name: str) -> Optional[str]`
- `def _compile_python_func(self, func: Callable, source: str) -> Callable`
- `def get_func_stats(self, func_name: str) -> Dict`

### compiler.llvm_backend

- [LLVMType](`#class-llvmtype`)
- [LLVMValue](`#class-llvmvalue`)
- [MathaIRNode](`#class-mathairnode`)
- [IRConstant](`#class-irconstant`)
- [IRVariable](`#class-irvariable`)
- [IRArithOp](`#class-irarithop`)
- [IRCompareOp](`#class-ircompareop`)
- [IRLogicalOp](`#class-irlogicalop`)
- [IRFunctionCall](`#class-irfunctioncall`)
- [IRVariableAccess](`#class-irvariableaccess`)
- [IRAssignment](`#class-irassignment`)
- [IRIfExpr](`#class-irifexpr`)
- [IRLoop](`#class-irloop`)
- [IRFunctionDef](`#class-irfunctiondef`)
- [MathaToIRConverter](`#class-mathatoirconverter`)
- [LLVMIRGenerator](`#class-llvmirgenerator`)
- [LLVMCompiler](`#class-llvmcompiler`)
- [LLVMHotJIT](`#class-llvmhotjit`)

**函数**
- `def matha_to_llvm_ir(ast_node: Any, module_name: str) -> str` — 将 Matha AST 转换为 LLVM IR 文本。
- `def compile_llvm_ir(llvm_ir: str, func_name: str) -> Optional[Callable]` — 编译 LLVM IR 为原生函数。
- `def create_hot_jit(threshold: int) -> LLVMHotJIT` — 创建热点追踪 JIT 编译器。

**方法**
- `def __str__(self) -> str`
- `def __repr__(self) -> str`
- `def __init__(self) -> None`
- `def _fresh_var(self, lltype: str) -> str`
- `def convert(self, ast_node: Any) -> MathaIRNode`
- `def _convert_binary(self, node: Any) -> MathaIRNode`
- `def _convert_unary(self, node: Any) -> MathaIRNode`
- `def _convert_func_app(self, node: Any) -> MathaIRNode`
- `def _convert_lambda(self, node: Any) -> MathaIRNode`
- `def _convert_if_expr(self, node: Any) -> MathaIRNode`
- `def convert_program(self, program: Any) -> list[MathaIRNode]`
- `def __init__(self, module_name: str) -> None`
- `def _fresh_var(self) -> str`
- `def _llvm_type(self, matha_type: str) -> str`
- `def generate(self, ir_nodes: list[MathaIRNode]) -> str`
- `def _emit_node(self, node: MathaIRNode) -> list[str]`
- `def _emit_constant(self, node: IRConstant) -> list[str]`
- `def _emit_variable(self, node: IRVariableAccess) -> list[str]`
- `def _emit_arith(self, node: IRArithOp) -> list[str]`
- `def _emit_compare(self, node: IRCompareOp) -> list[str]`
- `def _emit_logical(self, node: IRLogicalOp) -> list[str]`
- `def _emit_func_call(self, node: IRFunctionCall) -> list[str]`
- `def _emit_assignment(self, node: IRAssignment) -> list[str]`
- `def _emit_if(self, node: IRIfExpr) -> list[str]`
- `def _emit_loop(self, node: IRLoop) -> list[str]`
- `def _emit_function_def(self, node: IRFunctionDef) -> list[str]`
- `def __init__(self, toolchain: str) -> None`
- `def compile(self, llvm_ir: str, func_name: str) -> Optional[Callable]`
- `def get_cached(self, cache_key: str) -> Optional[Callable]`
- `def cache_size(self) -> int`
- `def clear_cache(self) -> None`
- `def __init__(self, threshold: int) -> None`
- `def record(self, name: str) -> None`
- `def should_compile(self, name: str) -> bool`
- `def compile_and_cache(self, name: str, llvm_ir: str) -> Optional[Callable]`
- `def get_compiled(self, name: str) -> Optional[Callable]`
- `def stats(self) -> dict`

### compiler.llvm_hybrid

- [SimulatedLLVMBackend](`#class-simulatedllvmbackend`)
- [HybridLLVMBackend](`#class-hybridllvmbackend`)

**函数**
- `def benchmark_compiler() -> dict` — 编译性能基准测试。
- `def run_full_test() -> bool` — 运行完整编译测试。
- `def matha_to_llvm(source: str) -> str` — 将 Matha 源码转换为 LLVM IR 文本。

**方法**
- `def __init__(self) -> None`
- `def compile(self, llvm_ir: str, output_name: str) -> str`
- `def _validate_llvm_ir(self, llvm_ir: str) -> list[str]`
- `def _simulate_compilation(self, llvm_ir: str, output_name: str) -> dict`
- `def run(self, exe_path: str, args: list) -> dict`
- `def stats(self) -> dict`
- `def _check_llvm() -> bool`
- `def __init__(self) -> None`
- `def _check_real_llvm(self) -> bool`
- `def compile(self, llvm_ir: str, output_name: str) -> str`
- `def _compile_real(self, llvm_ir: str, output_name: str) -> str`
- `def run(self, exe_path: str, args: list) -> dict`
- `def stats(self) -> dict`

### compiler.matha_cc

- [MathaToken](`#class-mathatoken`)
- [MathaLexer](`#class-mathalexer`)
- [ASTNode](`#class-astnode`)
- [Program](`#class-program`)
- [Binding](`#class-binding`)
- [FuncDef](`#class-funcdef`)
- [BinaryOp](`#class-binaryop`)
- [UnaryOp](`#class-unaryop`)
- [FuncApp](`#class-funcapp`)
- [Lambda](`#class-lambda`)
- [IfExpr](`#class-ifexpr`)
- [WhileStmt](`#class-whilestmt`)
- [ForStmt](`#class-forstmt`)
- [Literal](`#class-literal`)
- [Variable](`#class-variable`)
- [ListLiteral](`#class-listliteral`)
- [DictLiteral](`#class-dictliteral`)
- [Output](`#class-output`)
- [MathaParser](`#class-mathaparser`)
- [MathaIR](`#class-mathair`)
- [MathaFrontend](`#class-mathafrontend`)
- [MathaLLVMGenerator](`#class-mathallvmgenerator`)
- [MathaBackend](`#class-mathabackend`)
- [MathaCompiler](`#class-mathacompiler`)

**函数**
- `def matha_compile(source: str, output_name: str, optimize: bool) -> str` — 
- `def matha_run(source: str, args: list) -> subprocess.CompletedProcess` — 
- `def matha_to_llvm(source: str) -> str` — 

**方法**
- `def __init__(self, type_: str, value: str, line: int, col: int)`
- `def __repr__(self)`
- `def __init__(self, source: str)`
- `def tokenize(self) -> list[MathaToken]`
- `def _skip_whitespace(self) -> None`
- `def _skip_line_comment(self) -> None`
- `def _skip_block_comment(self) -> None`
- `def _read_number(self) -> MathaToken`
- `def _read_string(self, quote: str) -> MathaToken`
- `def _read_identifier(self) -> MathaToken`
- `def _read_operator(self) -> MathaToken`
- `def _is_unicode_id_start(self, ch: str) -> bool`
- `def _advance(self) -> None`
- `def __init__(self, line: int, col: int)`
- `def __init__(self, decls, line, col)`
- `def __init__(self, name, value, ann_type, line, col)`
- `def __init__(self, name, params, return_type, body, line, col)`
- `def __init__(self, op, left, right, line, col)`
- `def __init__(self, op, operand, line, col)`
- `def __init__(self, func, arg, line, col)`
- `def __init__(self, params, body, line, col)`
- `def __init__(self, cond, then, else_, line, col)`
- `def __init__(self, cond, body, line, col)`
- `def __init__(self, target, iterable, body, line, col)`
- `def __init__(self, value, kind, line, col)`
- `def __init__(self, name, line, col)`
- `def __init__(self, items, line, col)`
- `def __init__(self, keys, values, line, col)`
- `def __init__(self, expr, line, col)`
- `def __init__(self, tokens: list[MathaToken])`
- `def parse(self) -> Program`
- `def _peek(self) -> MathaToken`
- `def _advance(self) -> MathaToken`
- `def _expect(self, type_: str) -> MathaToken`
- `def _parse_decl(self) -> ASTNode`
- `def _parse_func_def(self) -> FuncDef`
- `def _parse_param_list(self) -> list`
- `def _parse_binding(self) -> Binding`
- `def _parse_output(self) -> Output`
- `def _parse_expr(self) -> ASTNode`
- `def _parse_or(self) -> ASTNode`
- `def _parse_and(self) -> ASTNode`
- `def _parse_equality(self) -> ASTNode`
- `def _parse_comparison(self) -> ASTNode`
- `def _parse_additive(self) -> ASTNode`
- `def _parse_multiplicative(self) -> ASTNode`
- `def _parse_unary(self) -> ASTNode`
- `def _parse_primary(self) -> ASTNode`
- `def _parse_arg_list(self) -> list`
- `def _parse_list_literal(self) -> ListLiteral`
- `def _parse_dict_literal(self) -> DictLiteral`
- `def __init__(self) -> None`
- `def _new_temp(self) -> str`
- `def emit(self, instr: str) -> str`
- `def emit_label(self, name: str) -> None`
- `def emit_call(self, func: str, args: list, result: str) -> str`
- `def emit_binary(self, op: str, left: str, right: str, result: str) -> str`
- `def emit_load(self, name: str, result: str) -> str`
- `def emit_store(self, name: str, value: str) -> None`
- `def to_dict(self) -> dict`
- `def __init__(self) -> None`
- `def compile(self, program: Program) -> MathaIR`
- `def _compile_decl(self, decl: ASTNode) -> None`
- `def _compile_func(self, func: FuncDef) -> None`
- `def _compile_binding(self, binding: Binding) -> None`
- `def _compile_expr(self, expr: ASTNode) -> str`
- `def _emit_literal(self, lit: Literal) -> str`
- `def _compile_binary(self, expr: BinaryOp) -> str`
- `def _compile_unary(self, expr: UnaryOp) -> str`
- `def _compile_func_app(self, expr: FuncApp) -> str`
- `def _compile_if(self, expr: IfExpr) -> str`
- `def _compile_list(self, expr: ListLiteral) -> str`
- `def _compile_dict(self, expr: DictLiteral) -> str`
- `def __init__(self, module_name: str) -> None`
- `def generate(self, matha_ir: MathaIR) -> str`
- `def save(self, path: str) -> None`
- `def __init__(self, toolchain: str) -> None`
- `def compile(self, llvm_ir: str, output_name: str) -> str`
- `def run(self, exe_path: str, args: list) -> subprocess.CompletedProcess`
- `def cache_stats(self) -> dict`
- `def __init__(self, optimize: bool) -> None`
- `def compile_file(self, matha_file: str, output_name: str) -> str`
- `def compile_source(self, source: str, output_name: str) -> str`
- `def _optimize_llvm(self, llvm_ir: str) -> str`
- `def run(self, source: str, args: list) -> subprocess.CompletedProcess`
- `def stats(self) -> dict`

**函数**
- `def cmd_compile(args) -> int` — 编译命令。
- `def cmd_run(args) -> int` — 运行命令。
- `def cmd_llvm(args) -> int` — 生成 LLVM IR 命令。
- `def cmd_optimize(args) -> int` — 优化编译命令。
- `def cmd_info(args) -> int` — 显示工具链信息。
- `def cmd_test(args) -> int` — 运行编译器测试。
- `def main(argv) -> int` — CLI 入口。

### compiler.memoize

- [LRUCache](`#class-lrucache`)
- [RecursivePattern](`#class-recursivepattern`)
- [RecursivePatternDetector](`#class-recursivepatterndetector`)
- [MemoizeDecorator](`#class-memoizedecorator`)
- [MemoizeOptimizer](`#class-memoizeoptimizer`)

**函数**
- `def get_memoize_optimizer(max_cache_size: int) -> MemoizeOptimizer` — 获取全局 memoization 优化器实例。
- `def memoize(func: Callable, **kwargs) -> Callable` — 便捷装饰器：自动为函数添加 memoization。
- `def fib_raw(n: int) -> int` — 
- `def factorial_raw(n: int) -> int` — 

**方法**
- `def __init__(self, max_size: int)`
- `def get(self, key: str) -> Optional[Any]`
- `def put(self, key: str, value: Any) -> None`
- `def clear(self) -> None`
- `def size(self) -> int`
- `def stats(self) -> Dict`
- `def detect(cls, func_source: str, func_name: str) -> Optional[RecursivePattern]`
- `def __init__(self, max_size: int, key_func: Optional[Callable])`
- `def __call__(self, func: Callable) -> Callable`
- `def _make_key(self, func: Callable, args: Tuple, kwargs: Dict) -> str`
- `def _serialize(obj: Any) -> str`
- `def clear(self) -> None`
- `def stats(self) -> Dict`
- `def __init__(self, max_cache_size: int, enable_tail_recursion: bool)`
- `def memoize(self, func: Callable, max_args: Optional[int], pattern: Optional[str]) -> Callable`
- `def _try_tail_recursion(self, func: Callable, pattern: str) -> Optional[Callable]`
- `def optimize_fibonacci(self, n: int) -> int`
- `def benchmark(self, func: Callable, test_cases: List[Tuple], iterations: int) -> Dict`
- `def get_stats(self) -> Dict`
- `def clear_all(self) -> None`

### compiler.native

- [ProtocolInterpreter](`#class-protocolinterpreter`)
- [DriverBuilder](`#class-driverbuilder`)
- [NativeCompiler](`#class-nativecompiler`)

**函数**
- `def interpret_protocol(spec: ProtocolSpec) -> dict` — 解释协议规格。
- `def build_driver(spec: DriverSpec) -> dict` — 构建硬件驱动。
- `def native_compile(expr: str, arch: Architecture, func_name: str, target_lang: str) -> dict` — 原生编译表达式。
- `def get_native_stats() -> dict` — 获取原生编译层全部统计。

**方法**
- `def __init__(self, parser: ProtocolParser)`
- `def interpret(self, spec: ProtocolSpec) -> dict`
- `def get_code(self, name: str, lang: str) -> Optional[str]`
- `def list_protocols(self) -> List[str]`
- `def get_stats(self) -> dict`
- `def __init__(self, generator: DriverGenerator)`
- `def build(self, spec: DriverSpec) -> dict`
- `def build_sensor(self, name: str, math_expr: str, scale: float, offset: float, unit: str, protocol: Optional[ProtocolSpec], arch: Architecture) -> dict`
- `def build_actuator(self, name: str, min_val: float, max_val: float, step: float, protocol: Optional[ProtocolSpec], arch: Architecture) -> dict`
- `def build_math(self, name: str, math_expr: str, arch: Architecture) -> dict`
- `def list_built(self) -> List[str]`
- `def get_stats(self) -> dict`
- `def __init__(self, backend: NativeBackend, side_effect_engine: SafeSideEffectEngine, pointer_mgr: PointerManager)`
- `def compile(self, expr: str, arch: Architecture, func_name: str, target_lang: str, safety_level: str) -> dict`
- `def compile_to_c(self, expr: str, func_name: str, arch: Architecture) -> dict`
- `def compile_to_asm(self, expr: str, func_name: str, arch: Architecture) -> dict`
- `def compile_and_exec(self, expr: str, arch: Architecture, func_name: str) -> dict`
- `def get_stats(self) -> dict`

### concurrent_v2

- [ProcessPool](`#class-processpool`)
- [SelectMonitor](`#class-selectmonitor`)
- [WorkerState](`#class-workerstate`)
- [Worker](`#class-worker`)
- [Supervisor](`#class-supervisor`)
- [DistributedLock](`#class-distributedlock`)

**方法**
- `def __init__(self, max_workers: int) -> None`
- `def _get_pool(self) -> multiprocessing.Pool`
- `def map(self, fn: Callable, items: list, chunksize: int) -> list`
- `def apply(self, fn: Callable, args: tuple) -> Any`
- `def apply_async(self, fn: Callable, args: tuple) -> multiprocessing.pool.ApplyResult`
- `def shutdown(self, wait: bool) -> None`
- `def max_workers(self) -> int`
- `def __init__(self) -> None`
- `def add_reader(self, fd, callback: Callable) -> None`
- `def add_writer(self, fd, callback: Callable) -> None`
- `def remove(self, fd) -> None`
- `def poll(self, timeout: float) -> list[tuple]`
- `def _is_readable(self, fd) -> bool`
- `def _is_writable(self, fd) -> bool`
- `def __init__(self, name: str) -> None`
- `def supervise(self, name: str, max_restarts: int) -> Worker`
- `def start_worker(self, name: str, fn: Callable, *args) -> threading.Thread`
- `def _restart(self, name: str, fn: Callable, args: tuple) -> None`
- `def get_state(self, name: str) -> Optional[WorkerState]`
- `def get_all_states(self) -> dict[str, str]`
- `def __init__(self, lock_name: str, timeout: float) -> None`
- `def acquire(self) -> bool`
- `def release(self) -> None`
- `def __enter__(self)`
- `def __exit__(self, *args)`

### cross_language_verifier

- [LanguageResult](`#class-languageresult`)
- [CrossLanguageResult](`#class-crosslanguageresult`)
- [CrossLanguageVerifier](`#class-crosslanguageverifier`)

**方法**
- `def passed(self) -> bool`
- `def __init__(self, verbose: bool) -> None`
- `def verify(self, algorithm: str, sources: dict[str, str]) -> CrossLanguageResult`
- `def batch_verify(self, test_cases: list[dict]) -> dict`
- `def _verify_language(self, language: str, source: str) -> LanguageResult`
- `def _print_lang_result(self, lang: str, result: LanguageResult) -> None`
- `def _generate_summary(self, result: CrossLanguageResult) -> str`
- `def print_report(self, result: CrossLanguageResult) -> None`

### csp_os_thread

- [Channel](`#class-channel`)
- [Goroutine](`#class-goroutine`)
- [SelectCase](`#class-selectcase`)
- [CSPRuntime](`#class-cspruntime`)
- [ProcessPool](`#class-processpool`)

**函数**
- `def go(target: Callable, *args, **kwargs) -> Goroutine` — 快捷启动 goroutine。
- `def channel(capacity: int) -> Channel` — 快捷创建 channel。
- `def parallel_map(func: Callable, items: List[Any], workers: int) -> List[Any]` — 并行 map（绕过 GIL）。
- `def compute_square(x)` — 
- `def heavy_compute(x)` — 

**方法**
- `def __init__(self, capacity: int)`
- `def send(self, value: T) -> None`
- `def recv(self, timeout: float) -> T`
- `def close(self) -> None`
- `def is_closed(self) -> bool`
- `def stats(self) -> dict`
- `def __repr__(self) -> str`
- `def __init__(self, target: Callable, args: tuple, kwargs: dict)`
- `def start(self) -> 'Goroutine'`
- `def _run(self)`
- `def join(self, timeout: float) -> Optional[Any]`
- `def is_alive(self) -> bool`
- `def stats(self) -> dict`
- `def __init__(self, max_workers: int)`
- `def go(self, target: Callable, *args, **kwargs) -> Goroutine`
- `def new_channel(self, capacity: int) -> Channel`
- `def select(self, cases: List[SelectCase], timeout: float) -> tuple`
- `def wait_all(self, timeout: float) -> List[Any]`
- `def stats(self) -> dict`
- `def __init__(self, num_workers: int)`
- `def map(self, func: Callable, items: List[Any]) -> List[Any]`
- `def starmap(self, func: Callable, items: List[tuple]) -> List[Any]`
- `def stats(self) -> dict`

**函数**
- `def setup_logging(verbose: bool)` — 设置日志级别。默认 INFO，--verbose 时启用 DEBUG。
- `def print_section(title: str)` — 打印章节标题。
- `def print_result(label: str, value)` — 打印结果。
- `def parallel_map(func, items, max_workers: int)` — 并行映射。
- `def demo1_derivative_matrix(expr: str)` — 演示1：符号求导与矩阵运算结合。
- `def demo2_integral_verification(expr: str)` — 演示2：符号积分与数值验证。
- `def demo3_taylor_matrix()` — 演示3：泰勒展开与矩阵拟合。
- `def demo4_limit_convergence()` — 演示4：极限计算与收敛性分析。
- `def demo5_ode_matrix()` — 演示5：微分方程与矩阵求解。
- `def demo6_matrix_calculus(use_numpy: bool, parallel: bool, n_workers: int, use_sparse: bool)` — 演示6：矩阵微积分综合应用。
- `def main()` — 主函数。

### deps

- [LRUCache](`#class-lrucache`)
- [TTLCache](`#class-ttlcache`)
- [MathaLogger](`#class-mathalogger`)

**函数**
- `def get_platform() -> str` — 获取当前平台标识。
- `def get_arch() -> str` — 获取 CPU 架构。
- `def get_python_version() -> tuple[int, int, int]` — 获取 Python 版本。
- `def is_portable_env() -> bool` — 检测是否为无 pip 的便携式环境（如 pyarmor、Nuitka 静态编译）。
- `def resolve_matha_root() -> Path` — 解析 Matha 项目根目录。
- `def to_json(obj) -> str` — 对象序列化为 JSON 字符串（纯标准库）。
- `def from_json(text: str) -> object` — JSON 字符串反序列化为对象（纯标准库）。
- `def to_json_pretty(obj) -> str` — 对象序列化为格式化 JSON 字符串。
- `def _json_default(obj)` — JSON 默认序列化器。
- `def to_pickle(obj) -> bytes` — 对象序列化为 pickle 字节（纯标准库）。
- `def from_pickle(data: bytes) -> object` — pickle 字节反序列化为对象（纯标准库）。
- `def to_b64(obj) -> str` — 对象序列化为 Base64 字符串（JSON 兼容）。
- `def from_b64(text: str) -> object` — Base64 字符串反序列化为对象。
- `def hash_obj(obj) -> str` — 计算对象的 SHA-256 哈希（用于缓存键）。
- `def _try_import_optional()` — 尝试导入可选依赖，失败则保持 None。
- `def fast_json_dumps(obj) -> str` — 尝试使用最快的 JSON 实现。
- `def fast_json_loads(text: str) -> object` — 尝试使用最快的 JSON 实现。
- `def fast_serialize(obj) -> bytes` — 尝试使用最快的序列化。
- `def fast_deserialize(data: bytes) -> object` — 尝试使用最快的反序列化。
- `def compress_data(data: bytes) -> bytes` — 压缩数据（优先 lz4，降级 zlib）。
- `def decompress_data(data: bytes) -> bytes` — 解压缩数据。
- `def serialize_for_storage(obj, use_compression: bool) -> bytes` — 序列化为可存储格式（带压缩）。
- `def deserialize_from_storage(data: bytes) -> object` — 从存储格式反序列化。
- `def safe_read_text(path: str, encoding: str) -> str` — 安全读取文本文件。
- `def safe_write_text(path: str, content: str, encoding: str) -> None` — 安全写入文本文件（自动创建目录）。
- `def safe_append_text(path: str, content: str, encoding: str) -> None` — 安全追加文本文件（自动创建目录）。
- `def ensure_dir(path: str) -> Path` — 确保目录存在。
- `def list_files(path: str, pattern: str) -> list` — 列出目录下匹配的文件。
- `def file_age_seconds(path: str) -> float` — 文件距今秒数。
- `def atomic_write(path: str, content: str, encoding: str) -> None` — 原子写入文件（先写临时文件再重命名）。
- `def timing(func, *args, **kwargs) -> tuple` — 执行函数并返回 (结果, 耗时秒数)。
- `def benchmark(func, *args, **kwargs) -> dict` — 性能基准测试。
- `def get_dependency_status() -> dict` — 返回当前依赖状态报告。
- `def _try_import_yaml() -> bool` — 

**方法**
- `def __init__(self, maxsize: int)`
- `def get(self, key) -> object | None`
- `def put(self, key, value) -> None`
- `def invalidate(self, key) -> bool`
- `def clear(self) -> None`
- `def size(self) -> int`
- `def stats(self) -> dict`
- `def __init__(self, default_ttl: float, maxsize: int)`
- `def get(self, key, ttl: float) -> object | None`
- `def put(self, key, value, ttl: float) -> None`
- `def invalidate(self, key) -> bool`
- `def clear(self) -> None`
- `def _evict_if_needed(self) -> None`
- `def __init__(self, name: str, level: int)`
- `def info(self, msg: str, **kwargs) -> None`
- `def debug(self, msg: str, **kwargs) -> None`
- `def warning(self, msg: str, **kwargs) -> None`
- `def error(self, msg: str, **kwargs) -> None`
- `def set_level(self, level: int) -> None`
- `def is_debug(self) -> bool`

### diagnostics

- [DiagnosticSeverity](`#class-diagnosticseverity`)
- [Diagnostic](`#class-diagnostic`)
- [MathaErrorKind](`#class-mathaerrorkind`)
- [DiagnosticCollector](`#class-diagnosticcollector`)
- [SourceHighlighter](`#class-sourcehighlighter`)
- [LSPServer](`#class-lspserver`)

**方法**
- `def to_lsp(self) -> dict`
- `def to_json(self) -> str`
- `def __init__(self) -> None`
- `def add(self, diag: Diagnostic) -> None`
- `def add_error(self, message: str, line: int, col: int, code: str, fix: str) -> Diagnostic`
- `def add_warning(self, message: str, line: int, col: int, code: str, fix: str) -> Diagnostic`
- `def add_hint(self, message: str, line: int, col: int) -> Diagnostic`
- `def errors(self) -> list[Diagnostic]`
- `def warnings(self) -> list[Diagnostic]`
- `def has_errors(self) -> bool`
- `def clear(self) -> None`
- `def summary(self) -> str`
- `def to_json(self) -> str`
- `def highlight(cls, source: str, line: int, col: int, message: str, severity: DiagnosticSeverity) -> str`
- `def highlight_range(cls, source: str, start_line: int, start_col: int, end_line: int, end_col: int, message: str, severity: DiagnosticSeverity) -> str`
- `def print_diagnostics(cls, source: str, diagnostics: list[Diagnostic]) -> None`
- `def __init__(self) -> None`
- `def analyze(self, source: str, uri: str) -> list[dict]`
- `def _parse_diagnostics(self, source: str) -> None`
- `def get_completions(self, source: str, line: int, col: int) -> list[dict]`

### diagnostics_v2

- [Severity](`#class-severity`)
- [Diagnostic](`#class-diagnostic`)
- [ContextAnalyzer](`#class-contextanalyzer`)
- [ErrorHistory](`#class-errorhistory`)
- [EnhancedDiagnosticCollector](`#class-enhanceddiagnosticcollector`)
- [SourceHighlighter](`#class-sourcehighlighter`)

**方法**
- `def to_lsp(self) -> dict`
- `def to_json(self) -> str`
- `def __init__(self, source: str, line: int, col: int) -> None`
- `def get_context(self, radius: int) -> list[str]`
- `def find_similar_vars(self, name: str) -> list[str]`
- `def find_similar_funcs(self, name: str, known_funcs: list[str]) -> list[str]`
- `def _levenshtein(s1: str, s2: str) -> int`
- `def __init__(self, max_entries: int) -> None`
- `def record(self, diagnostic: Diagnostic) -> None`
- `def get_duplicates(self) -> list[dict]`
- `def get_recent(self, n: int) -> list[dict]`
- `def clear(self) -> None`
- `def __init__(self) -> None`
- `def add(self, diag: Diagnostic) -> None`
- `def add_error(self, message: str, line: int, col: int, code: str, fix: str, context: list[str]) -> Diagnostic`
- `def add_warning(self, message: str, line: int, col: int, code: str, fix: str) -> Diagnostic`
- `def analyze_source(self, source: str, errors: list[str]) -> list[Diagnostic]`
- `def _find_line(self, lines: list[str], message: str) -> int`
- `def errors(self) -> list[Diagnostic]`
- `def warnings(self) -> list[Diagnostic]`
- `def has_errors(self) -> bool`
- `def summary(self) -> str`
- `def to_json(self) -> str`
- `def clear(self) -> None`
- `def highlight(cls, source: str, line: int, col: int, message: str, severity: Severity) -> str`
- `def print_diagnostics(cls, source: str, diagnostics: list[Diagnostic]) -> None`

### doc_gen

- [DocEntry](`#class-docentry`)
- [DocExtractor](`#class-docextractor`)
- [DocGenerator](`#class-docgenerator`)

**方法**
- `def to_dict(self) -> Dict`
- `def __init__(self, source_file: str, module_name: str)`
- `def visit_Module(self, node: ast.Module) -> None`
- `def visit_ClassDef(self, node: ast.ClassDef) -> None`
- `def visit_FunctionDef(self, node: ast.FunctionDef) -> None`
- `def visit_Import(self, node: ast.Import) -> None`
- `def _decode_decorator(self, node: ast.expr) -> str`
- `def _ast_to_str(self, node: ast.AST) -> str`
- `def get_entries(self) -> List[DocEntry]`
- `def __init__(self, src_dir: str, output_dir: str, project_name: str, project_version: str)`
- `def generate_all(self) -> Dict[str, str]`
- `def _scan_and_extract(self) -> None`
- `def _get_module_name(self, path: Path) -> Optional[str]`
- `def _generate_markdown(self) -> str`
- `def _entry_to_markdown(self, entry: DocEntry) -> List[str]`
- `def _generate_html(self) -> str`
- `def _generate_json(self) -> str`

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _声_空气声速(T_C)` — 
- `def _声_波长(c, f)` — 
- `def _声_频率(c, wavelength)` — 
- `def _声_周期(f)` — 
- `def _声_角频率(f)` — 
- `def _声_波数(wavelength)` — 
- `def _声_介质声速(E, rho)` — 
- `def _声_横波声速(G, rho)` — 
- `def _强级_声强由声压(p, rho, c)` — 
- `def _强级_声强级(I)` — 
- `def _强级_声压级(p)` — 
- `def _强级_声功率级(W)` — 
- `def _强级_声强由功率(W, A)` — 
- `def _强级_声强由级(L_I)` — 
- `def _强级_声压由级(L_p)` — 
- `def _强级_分贝叠加(L_list)` — 
- `def _多普勒_观察者运动(f, c, v_o)` — 
- `def _多普勒_声源运动(f, c, v_s)` — 
- `def _多普勒_通用(f, c, v_o, v_s)` — 
- `def _多普勒_马赫数(v, c)` — 
- `def _多普勒_马赫角(v, c)` — 
- `def _现象_拍频(f1, f2)` — 
- `def _现象_驻波波长(L, n)` — 
- `def _现象_驻波频率(n, v, L)` — 
- `def _现象_反平方衰减(I1, r1, r2)` — 
- `def _现象_吸收衰减(I1, alpha, d)` — 
- `def _现象_声压衰减(p1, r1, r2)` — 
- `def _弦管_弦上波速(T, mu)` — 
- `def _弦管_弦频率(T, mu, L)` — 
- `def _弦管_弦泛音(T, mu, L, n)` — 
- `def _弦管_开管基频(c, L)` — 
- `def _弦管_闭管基频(c, L)` — 
- `def _弦管_开管谐波(c, L, n)` — 
- `def _弦管_闭管谐波(c, L, n)` — 
- `def _register_acoustics(builtins: dict) -> None` — 将声学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（em 之后）。
- `def _acoustics_symtab_names() -> list[str]` — 返回声学所有内建名（用于语义分析注册）。

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _轨道速度计算(轨道高度_km)` — 圆轨道速度 v = sqrt(GM/(R+h))。
- `def _推进剂消耗率(推力_N, 比冲_s)` — 推进剂质量流率 kg/s。
- `def _结构强度系数(材料强度_MPa, 载荷_MPa)` — 结构强度安全系数。
- `def _热防护质量(热流密度_W_m2, 面积_m2, 防护材料热容_J_kgK)` — 热防护系统质量估算。
- `def _再入角估算(轨道速度_ms, 目标高度_m)` — 再入角估算（度）。
- `def _比冲(推力_N, 质量流率_kg_s)` — 比冲 Isp = F/(ṁ·g₀)。
- `def _register_aerospace(builtins: dict) -> None` — 
- `def _aerospace_symtab_names() -> list[str]` — 

### domains.ai_data_science

- [AdamOptimizer](`#class-adamoptimizer`)

**函数**
- `def sigmoid(x: float) -> float` — Sigmoid 激活函数。
- `def sigmoid_deriv(x: float) -> float` — Sigmoid 导数。
- `def relu(x: float) -> float` — ReLU 激活函数。
- `def relu_deriv(x: float) -> float` — ReLU 导数。
- `def softmax(values: list[float]) -> list[float]` — Softmax 激活函数。
- `def tanh(x: float) -> float` — Tanh 激活函数。
- `def mse(y_true: list[float], y_pred: list[float]) -> float` — 均方误差。
- `def mae(y_true: list[float], y_pred: list[float]) -> float` — 平均绝对误差。
- `def cross_entropy(y_true: list[float], y_pred: list[float]) -> float` — 交叉熵损失。
- `def log_loss(y_true: list[float], y_pred: list[float]) -> float` — 对数损失（同交叉熵）。
- `def accuracy(y_true: list[float], y_pred: list[float]) -> float` — 准确率。
- `def dot_product(a: list[float], b: list[float]) -> float` — 点积。
- `def matrix_mult(a: list[list[float]], b: list[list[float]]) -> list[list[float]]` — 矩阵乘法。
- `def transpose(matrix: list[list[float]]) -> list[list[float]]` — 矩阵转置。
- `def matrix_add(a: list[list[float]], b: list[list[float]]) -> list[list[float]]` — 矩阵加法。
- `def gradient_descent(params: dict, grads: dict, lr: float) -> dict` — SGD 梯度下降。
- `def forward_prop(layers: list[list[float]], weights: list[list[list[float]]], biases: list[list[float]]) -> list[list[float]]` — 前向传播。
- `def backward_prop(layers: list[list[float]], weights: list[list[list[float]]], outputs: list[list[float]], y_true: list[float]) -> tuple[list[list[list[float]]], list[list[float]]]` — 反向传播（简化版）。
- `def mean(values: list[float]) -> float` — 平均值。
- `def variance(values: list[float]) -> float` — 方差。
- `def std(values: list[float]) -> float` — 标准差。
- `def correlation(x: list[float], y: list[float]) -> float` — 皮尔逊相关系数。
- `def _register_ai_data_science(builtins: dict) -> None` — 注册 AI 与数据科学内建到解释器。
- `def _ai_data_science_symtab_names() -> list[str]` — 

**方法**
- `def __init__(self, lr: float, beta1: float, beta2: float, eps: float)`
- `def step(self, params: dict, grads: dict) -> dict`

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _策略夏普比率(策略收益_list, 无风险利率)` — 策略夏普比率。
- `def _最大回撤估算(净值序列)` — 最大回撤（%）。
- `def _订单执行成本(订单金额, 买卖价差, 市场冲击)` — 订单执行成本。
- `def _滑点估算(订单量, 市场深度, 价格波动率)` — 滑点估算（基点）。
- `def _波动率预测(历史收益率_list, 预测期)` — 波动率预测（年化）。
- `def _相关性矩阵(资产收益1, 资产收益2)` — 计算两资产相关系数。
- `def _register_algo_trading(builtins: dict) -> None` — 
- `def _algo_trading_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _系统_心胸比(cardiac_TD_mm, thoracic_TD_mm)` — 心胸比 CTR = 心脏横径 / 胸廓横径。>0.5 提示心脏增大。
- `def _系统_股骨颈干角分类(angle_deg)` — 股骨颈干角分类：<110 内翻，110-140 正常，>140 外翻。
- `def _系统_股骨前倾角分类(angle_deg)` — 股骨前倾角分类（成人典型 12°）：<5 后倾，5-25 正常，>25 异常前倾。
- `def _系统_颈椎曲度指数(posterior_depth_mm, vertebral_length_mm)` — 颈椎曲度指数 = 后缘深度 / 椎体长度。0.1-0.3 正常，<0.1 变直。
- `def _系统_腰椎Cobb角正切(delta_y_mm, delta_x_mm)` — 由 Cobb 角端点垂足差估算正切角 = atan(Δy/Δx)（度）。
- `def _系统_骨盆入口指数(AP_diameter_mm, transverse_diameter_mm)` — 骨盆入口指数 = 入口前后径 / 横径。男<1 女>1（产科）。
- `def _系统_脑室指数(ventricle_width_mm, brain_width_mm)` — 脑室指数 = 侧脑室宽度 / 脑横径。>0.5 提示脑积水。
- `def _系统_主动脉Z值(aortic_root_mm, BSA)` — 主动脉根部 Z 值简化估算（基于 BSA 标化，非临床精确公式）：
预期 D = 14.4 + BSA*9.5，Z = 
- `def _系统_椎管矢状径判定(level, sagittal_mm)` — 按节段判定椎管矢状径是否狭窄（颈<13 / 腰<15 提示狭窄），返回 (是否狭窄, 参考值)。
- `def _系统_视神经鞘判定(ONSD_mm)` — 视神经鞘直径判定：>5.4mm 提示颅内压升高。
- `def _局部_甲状腺体积(a_mm, b_mm, c_mm)` — 甲状腺体积（mL）= π/6 · a · b · c（椭球公式，mm³ → mL）。
- `def _局部_前列腺体积(a_mm, b_mm, c_mm)` — 前列腺体积（mL）= π/6 · a · b · c。
- `def _局部_肝脏体积(L_mm, W_mm, H_mm, k)` — 肝脏体积（mL）= k · L · W · H / 1000，k ≈ 0.55。
- `def _局部_脾脏体积(a_mm, b_mm, c_mm)` — 脾脏体积（mL）= π/6 · a · b · c。
- `def _局部_肾脏体积(L_mm, W_mm, H_mm)` — 肾脏体积（mL）= π/6 · L · W · H。
- `def _局部_睾丸体积(L_mm, W_mm, H_mm)` — 睾丸体积（mL）= π/6 · L · W · H。
- `def _局部_左室质量(IVSd_mm, LVDd_mm, LVPWd_mm)` — 左室质量估测（Devereux 校正版）：LVM(g) = 0.8·[1.04·(IVSd+LVDd+LVPWd)³ -
- `def _局部_关节腔深度(depth_to_bone_mm, needle_advance_mm)` — 关节腔深度估算 = depth_to_bone - safety_margin（安全余量）。
- `def _表面_体表面积Mosteller(height_cm, weight_kg)` — Mosteller 公式：BSA(m²) = sqrt(H·W/3600)，H=cm, W=kg。
- `def _表面_体表面积DuBois(height_cm, weight_kg)` — DuBois 公式：BSA(m²) = 0.007184·W^0.425·H^0.725。
- `def _表面_烧伤九分法(body_part)` — 成人烧伤九分法（按部位查体表面积百分比）。
- `def _表面_椎体节段定位(distance_from_C2_mm, segment_length_mm)` — 椎体节段定位 = 距 C2 距离 / 平均节段长（向下取整）→ 节段序号。
- `def _表面_经皮进针深度(depth_to_target_mm, compression_mm)` — 经皮进针深度 = 目标深度 + 压缩补偿。
- `def _表面_体表标志间距(landmark1, landmark2)` — 按标志对查标准间距（cm）。未收录返回 0.0。
- `def _影解_椎管矢状径(level)` — 按椎体节段查椎管矢状径参考值（mm）。
- `def _影解_椎弓根间距(region)` — 按脊柱区段查椎弓根间距参考最大值（mm）。
- `def _影解_脊髓圆锥位置(vertebral_level)` — 脊髓圆锥位置判定：L1-L2 正常，L3 以下提示低位。
- `def _影解_脑沟宽度(age_years, sulcus_mm)` — 按年龄判定脑沟宽度：青壮年<5mm 正常；老年>6mm 异常。
- `def _影解_侧脑室前角宽度(age_years)` — 按年龄返回侧脑室前角宽度参考上限（mm）。
- `def _影解_垂体高度判定(height_mm)` — 垂体高度判定：>10mm 提示垂体增大。
- `def _影解_Evans指数(frontal_horn_mm, cranial_width_mm)` — Evans 指数 = 侧脑室前角宽/颅最大内径。>0.3 提示脑积水。
- `def _影解_肺结节倍增时间(D0_mm, D1_mm, days)` — 肺结节体积倍增时间（天）：V = π/6·D³；DT = ln2·Δt / ln(V1/V0)。
- `def _临解_心脏质量预测(body_weight_kg)` — 心脏质量预测 = body_weight × 0.0045（g）。
- `def _临解_肝脏质量预测(body_weight_kg)` — 肝脏质量预测 = body_weight × 0.0225（g）。
- `def _临解_脾脏质量预测(body_weight_kg)` — 脾脏质量预测 = body_weight × 0.0023（g）。
- `def _临解_肾脏质量预测(body_weight_kg)` — 双肾质量预测 = body_weight × 0.0021 × 2（g，含双侧）。
- `def _临解_脑质量预测(body_weight_kg)` — 脑质量预测 = body_weight × 0.021（g）。
- `def _临解_血容量(body_weight_kg, is_male)` — 血容量估算（mL）= body_weight × (70 if 男 else 65)。
- `def _临解_肺总量预测(height_cm, is_male)` — 肺总量估算（mL）：男 50·H - 4500；女 45·H - 4000（H=cm）。
- `def _临解_脏器体重比(organ_mass_g, body_weight_kg)` — 脏器质量/体重比 = organ_mass / (body_weight × 1000)。
- `def _register_anatomy(builtins: dict) -> None` — 将解剖学子领域内建注册到解释器 builtins。
- `def _anatomy_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _建物_热阻(d_m, lambda_val)` — 材料热阻 R = d/λ（m²·K/W）。
- `def _建物_传热系数(R_total)` — 传热系数 U = 1/ΣR（W/(m²·K)）。
- `def _建物_传热量(U, area_m2, delta_T)` — 传热量 Φ = U·A·ΔT（W）。
- `def _建物_太阳得热系数(Q_solar, Q_incident)` — SHGC = Q_solar / Q_incident。
- `def _建物_质量定律隔声(surface_density_kg_m2)` — 质量定律隔声 TL = 20·log₁₀(m) - 47（dB，m 面密度 kg/m²）。
- `def _建物_Sabine混响时间(V_m3, A_total)` — Sabine 公式 T = 0.161·V/A（s）。
- `def _建物_总吸声量(areas_m2, coeffs)` — 吸声量 A = Σ(α_i · S_i)。areas 与 coeffs 等长列表。
- `def _建物_换气次数(Q_m3h, V_room_m3)` — 空气渗透换气次数 ACH = Q/V（次/h）。
- `def _建物_采光系数(E_indoor, E_outdoor)` — 采光系数 DF = E_indoor/E_outdoor × 100（%）。
- `def _建物_露点判定(T_air_C, RH)` — 按室内温湿度估算露点温度（℃）并判定是否结露。
简化：Td = T - (100 - RH)/5（Lawrence 近似
- `def _建材_水灰比反算(fc_MPa, k, n)` — Abrams 公式简化反算：f'c = A / (w/c)^n，给定 f'c 求 w/c。
默认 A=22, n=0.6
- `def _建材_配筋率(As_mm2, b_mm, d_mm)` — 配筋率 ρ = As / (b·d)。
- `def _建材_砂浆体积比(cement, sand, water)` — 砂浆体积配合比归一化：返回 (c/Σ, s/Σ, w/Σ)。
- `def _建材_砖砌体抗压强度(brick_grade, mortar_grade)` — 按砖与砂浆等级（MU/M 后数值）查砌体抗压强度设计值（MPa）。
简化公式：f = 0.78·√(砖·砂浆)/4.5（
- `def _建材_木材强度分级(f_m_MPa)` — 按抗弯强度判定木材等级：TC17/TC15/TC13/TC11 或不合格。
- `def _建材_钢材屈服判定(fy_MPa)` — 按屈服强度判定钢材级别（HPB300/HRB400/HRB500）。
- `def _建材_混凝土材料用量(V_m3, water_kg, w_c, w_s)` — 按水灰比 w/c 与砂率 w_s 估算 V_m3 m³ 混凝土各材料总用量（kg）。
water_kg 为每 m³ 用水
- `def _建材_砌体材料用量(wall_area_m2, thickness_m, mortar_ratio)` — 估算砌体材料用量（按面积/厚度）：返回 (砌块体积, 砂浆体积) m³。
- `def _建设_黄金比例()` — 黄金比例 φ = (1+√5)/2 ≈ 1.6180339887。
- `def _建设_容积率(GFA_m2, site_area_m2)` — 容积率 FAR = 总建筑面积 / 用地面积。
- `def _建设_建筑密度(footprint_m2, site_area_m2)` — 建筑密度 = 建筑基底面积 / 用地面积（0~1）。
- `def _建设_绿地率(green_m2, site_area_m2)` — 绿地率 = 绿地面积 / 用地面积（0~1）。
- `def _建设_人均面积(total_area_m2, occupants)` — 人均使用面积 = 总面积 / 人数。
- `def _建设_模数协调(base_module_count)` — 按模数倍数返回尺寸：n × 1M = n × 100mm。
- `def _建设_建筑高度限值(aircraft_clear_m, solar_extra_m)` — 建筑高度限值 = 净空 - 余量（航空限高/日照叠加）。
- `def _建设_日照间距系数(building_height_m, sun_altitude_deg)` — 日照间距系数 = H / tan(α)（α 太阳高度角）。
返回 L = H/tan(α)（日照间距）。
- `def _建施_土方量棱柱法(L_m, A1_m2, A2_m2)` — 土方量棱柱法：V = L·(A1 + A2) / 2。
- `def _建施_混凝土体积(L_m, b_m, h_m)` — 混凝土体积 V = L·b·h（m³）。
- `def _建施_钢筋下料长度(L_design_mm, hook_count, d_mm)` — 钢筋下料长度 = 设计长 + 弯钩增加（每个 10d）。
- `def _建施_模板面积(L_m, h_m)` — 矩形截面模板面积：A = 2·(L + h)·h（侧模+底模近似）。
- `def _建施_脚手架立杆承载力(A_mm2, f_MPa, safety_factor)` — 单根立杆承载力 N = A·f / K（kN）。
- `def _建施_塔吊起重量(moment_kNm, radius_m)` — 塔吊起重量 Q = M / R（kN，力矩 M / 幅度 R）。
- `def _建施_放坡系数(h_m, b_m)` — 放坡系数 K = h / b（深度/放坡宽度）。
- `def _建施_抹灰面积(L_m, H_m, opening_m2)` — 墙面抹灰面积 = L·H - 洞口（m²）。
- `def _建规_疏散宽度(occupants, width_index_m_per_100)` — 疏散总宽度 W = N × 宽度指标 / 100（m）。
- `def _建规_最小楼梯宽度(building_type)` — 按建筑类型返回最小疏散楼梯宽度（m）。
- `def _建规_通风换气量(ACH, V_room_m3)` — 通风换气量 Q = ACH × V（m³/h）。
- `def _建规_卫生器具数(occupants, fixture_index)` — 卫生器具数 = 使用人数 × 配建指标（每 20 人 1 个 → 0.05）。
- `def _建规_停车位配建(GFA_m2, parking_index_per_100m2)` — 停车位配建数 = 总建筑面积 × 指标 / 100（取整）。
- `def _建规_疏散距离限值(building_type, sprinklered)` — 疏散距离限值（m，按建筑类型与是否设喷淋）。
- `def _建规_防火分区面积(building_type)` — 防火分区最大允许建筑面积（m²，一级耐火）。
- `def _建规_窗地比最小(room_type)` — 按房间类型返回最小窗地比（窗洞面积/房间面积）。
- `def _register_architecture(builtins: dict) -> None` — 将建筑学子领域内建注册到解释器 builtins。
- `def _architecture_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _音频采样率转换(源采样率, 目标采样率, 声道数)` — 采样率转换后数据量（字节/秒）。
- `def _视频码率估算(分辨率_w, 分辨率_h, 帧率, 压缩率)` — 视频码率估算（Mbps）。
- `def _流媒体延迟(缓冲秒数, 网络延迟_ms, 编码延迟_ms)` — 流媒体总延迟。
- `def _编解码压缩比(原始大小_MB, 压缩后大小_MB)` — 压缩比。
- `def _音频信噪比(信号功率, 噪声功率)` — 音频信噪比（dB）。
- `def _视频帧率稳定性(目标帧率, 实际帧率_list)` — 帧率稳定性指数。
- `def _register_audio_video(builtins: dict) -> None` — 
- `def _audio_video_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _PLC扫描周期估算(iO点数, 扫描指令数, 指令周期_ns)` — PLC扫描周期 T = I/O耗时 + 指令执行耗时。
- `def _传感器采样率(噪声密度, 带宽, 分辨率_bit)` — 根据噪声和带宽估算有效采样率。
- `def _执行器响应时间(类型, 负载_kg, 行程_mm)` — 执行器响应时间估算（ms）。
- `def _时序约束满足(周期_ms, 执行时间_ms, 开销_ms)` — 判断时序约束是否满足。
- `def _自动化流程执行效率(步骤数, 平均步时长_ms, 并行度)` — 自动化流程执行效率（吞吐率）。
- `def _异常检测率(误报率, 漏报率, 样本数)` — 异常检测综合率。
- `def _register_automation(builtins: dict) -> None` — 
- `def _automation_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _碰撞时间估算(距离_m, 相对速度_ms)` — 碰撞时间 TTC = d/v。
- `def _路径规划复杂度(网格数, 分支因子)` — A*路径规划最坏情况复杂度。
- `def _传感器融合误差(传感器误差_list)` — 多传感器融合后误差（协方差近似）。
- `def _决策响应时间(感知延迟_ms, 计算延迟_ms, 执行延迟_ms)` — 决策总响应时间。
- `def _定位精度(卫星数, PDOP)` — GNSS定位精度估算（米）。
- `def _油耗估算(车重_kg, 风阻系数, 速度_kmh, 路况)` — 油耗估算（L/100km）。
- `def _register_autonomous(builtins: dict) -> None` — 
- `def _autonomous_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _GC含量计算(序列)` — DNA序列GC含量（%）。
- `def _分子质量估算(氨基酸序列)` — 蛋白质分子质量估算（Da）。
- `def _蛋白折叠能量(序列长度, 疏水比例)` — 简化蛋白折叠自由能估算（kcal/mol）。
- `def _序列比对得分(序列1, 序列2, 匹配奖, 错配罚, 空位罚)` — 序列比对打分（简化版）。
- `def _系统稳定性(反馈强度, 延迟, 阻尼)` — 控制理论系统稳定性判据。
- `def _代谢通量(酶浓度, Km, Vmax, 底物浓度)` — Michaelis-Menten代谢通量。
- `def _register_bio_computing(builtins: dict) -> None` — 
- `def _bio_computing_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _分子_DNA互补链(seq)` — 返回 DNA 互补链（同方向 5'-3'：互补后反转）。
- `def _分子_DNA转录(seq)` — DNA 模板链 → mRNA（T→U，保留顺序，互补反义做了反转后再取 T→U）。
这里简单实现：输入编码链（5'-3'
- `def _分子_GC含量(seq)` — 返回 GC 碱基比例 (0~1)。
- `def _分子_Tm_Wallace(seq)` — Wallace 公式：Tm(°C) = 2(A+T) + 4(G+C)（≤20 bp 探针近似）。
- `def _分子_dsDNA质量(bp)` — 双链 DNA 质量 Da ≈ bp × 650 Da/bp。
- `def _分子_ssDNA质量(nt)` — 
- `def _分子_ssRNA质量(nt)` — 
- `def _分子_PCR扩增(n_cycle)` — 
- `def _分子_PCR扩增效率(n_cycle, eff)` — 实际扩增倍数：(1+eff)^n，eff∈[0,1]。
- `def _分子_质量转摩尔(mass_g, M_Da)` — 摩尔数 mol = mass_g / (M_Da × g/mol 当量)。Da 单位即 g/mol，直接除。
- `def _分子_密码子查找(codon)` — 传入 3 字母密码子（U 为 RNA 形式），返回单字母 AA 或 *。
- `def _分子_mRNA翻译(mRNA, from_start)` — 从 mRNA 翻译为蛋白质序列（单字母，终止符 *）。
from_start=True 则从第一个 AUG 开始按 3 
- `def _分子_蛋白分子量(seq)` — 按单字母序列计算残基分子量（Da，不含水和修饰）。
- `def _分子_等电点简化()` — 
- `def _细胞_球体体积(r)` — 
- `def _细胞_球体表面积(r)` — 
- `def _细胞_表面积体积比(r)` — 
- `def _细胞_生长速率(t_d)` — k = ln2 / t_d，t_d 与 k 时间单位一致（1/时间）。
- `def _细胞_倍增时间(k)` — 
- `def _细胞_指数生长(N0, k, t)` — 
- `def _细胞_倍增代数(N0, N)` — 从 N0 到 N 共经历几代倍增：n = log2(N/N0)。
- `def _细胞_特征扩散时间(x, D)` — x 特征距离（m），D 扩散系数 m²/s → t(s) ≈ x²/(2D)。
- `def _细胞_膜电容(A_cm2)` — 细胞膜总电容（μF）：典型 Cm=1 μF/cm² × 面积 cm²。
- `def _细胞_椭球体积(a, b, c)` — 
- `def _生化_米氏方程(Vmax, Km, S)` — v = Vmax·[S] / (Km + [S])。
- `def _生化_Lineweaver斜率(Vmax, Km)` — 
- `def _生化_Lineweaver截距(Vmax)` — 
- `def _生化_HH(pKa, ratio_Aminus_over_HA)` — Henderson-Hasselbalch：pH = pKa + log([A⁻]/[HA])。
- `def _生化_缓冲容量(C, Ka, pH)` — β = 2.303·C·Ka·[H⁺]/(Ka+[H⁺])²；[H⁺] = 10^-pH。
- `def _生化_能斯特(E0, z, ratio_Ox_Red, T_K)` — E = E° + (RT/zF)·ln(Ox/Red)。
- `def _生化_阿伦尼乌斯(A_pre, Ea_Jmol, T_K)` — k = A·exp(-Ea/(RT))。
- `def _生化_Hill方程(n_Hill, Kd, L)` — θ = [L]^n / (Kd^n + [L]^n)。
- `def _生化_竞争抑制Km(Km, I, Ki)` — Km_app = Km·(1 + [I]/Ki)。
- `def _生化_非竞争抑制Vmax(Vmax, I, Ki)` — Vmax_app = Vmax/(1 + [I]/Ki)。
- `def _生理_BMI(kg, m)` — 体重指数 kg/m²。
- `def _生理_心输出量(HR_bpm, SV_mL)` — CO = HR × SV（mL/min）。
- `def _生理_肺泡通气量(TV_mL, VD_mL, f_bpm)` — VA = (TV - VD) × f（mL/min）。
- `def _生理_血氧含量(Hb_g_dL, SaO2, PaO2_mmHg)` — CaO2 = 1.34·Hb·SaO2 + 0.003·PaO2（mL O2/100mL 血）。
- `def _生理_基础代谢(m_kg, h_cm, age, is_male)` — Mifflin-St Jeor：男 10m + 6.25h - 5a + 5；女 同 -161。单位 kcal/日。
- `def _种群_指数增长(N0, r, t)` — 
- `def _种群_倍增时间(r)` — 
- `def _种群_逻辑斯蒂(N0, r, K, t)` — N(t) = K / (1 + (K/N0 - 1)·exp(-rt))。
- `def _种群_内禀增长率(R0, T_generation)` — r ≈ ln(R0)/T（离散世代近似）。
- `def _种群_世代总数(r, t)` — 
- `def _微生_CFU(plate_count, dilution_factor, volume_mL)` — CFU/mL = plate_count × dilution_factor / volume_mL。
- `def _微生_对数减少(N0, N)` — LR = log10(N0/N)。
- `def _微生_杀菌存活率(LR)` — S = N/N0 = 10^(-LR)。
- `def _微生_D值存活(N0, t_min, D_min)` — N = N0·10^(-t/D)。
- `def _微生_Z值(logD_ratio, delta_T)` — 由 log(D1/D2) = (T2-T1)/z → z = ΔT / log(D1/D2)。
- `def _微生_世代时间(N0, N, t_h)` — g = t / log2(N/N0)。
- `def _微生_MOI(phage_count, host_count)` — 感染复数 = 噬菌体颗粒 / 宿主细菌数。
- `def _微生_OD600转细胞数(OD600)` — 经验：大肠杆菌 OD600=1 ≈ 8e8 CFU/mL。
- `def _免疫_效价稀释(initial_dilution, titer_positive)` — 简化：titer = 初始稀释倍数 × 终点阳性管几何平均。返回单一 positive 稀释下倒数。
- `def _register_biology(builtins: dict) -> None` — 将生物学子领域内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（fluid_
- `def _biology_symtab_names() -> list[str]` — 

**函数**
- `def block_create(prev_hash: str, transactions: list[dict], nonce: int) -> dict` — 创建区块。
- `def block_verify(block: dict, prev_block: Optional[dict]) -> bool` — 验证区块。
- `def chain_validate(chain: list[dict]) -> bool` — 验证区块链。
- `def hash_block(data: str) -> str` — 区块哈希。
- `def merkle_root(leaves: list[str]) -> str` — Merkle 根。
- `def sign_transaction(tx: dict, private_key: str) -> str` — 交易签名。
- `def verify_signature(tx: dict, signature: str, public_key: str) -> bool` — 验证签名。
- `def poW_mine(block: dict, difficulty: int) -> dict` — PoW 挖矿。
- `def poS_validate(proposer: str, stake: float, validators: list[dict]) -> bool` — PoS 验证（简化）。
- `def smart_contract_deploy(code: str, owner: str) -> str` — 部署智能合约。
- `def smart_contract_call(addr: str, func: str, args: list) -> dict` — 调用智能合约。
- `def token_transfer(from_addr: str, to_addr: str, amount: float) -> bool` — Token 转账。
- `def token_balance(addr: str) -> float` — 查询 Token 余额。
- `def token_mint(addr: str, amount: float) -> None` — 铸造 Token。
- `def _register_blockchain(builtins: dict) -> None` — 注册区块链内建到解释器。
- `def _blockchain_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _混凝_受弯承载力简化(fy, As, h0)` — 单筋矩形梁受弯承载力简化：Mu = 0.9·fy·As·h0（kN·m，按规范简化估算）。
- `def _混凝_相对受压区高度(fy, As, fc, b, h0)` — ξ = x/h0 = (fy·As)/(α1·fc·b·h0)，α1=1.0（C50 及以下）。
- `def _混凝_最小配筋率(ft, fy)` — ρmin = max(0.2%, 0.45·ft/fy)。返回比值（非百分比）。
- `def _混凝_最大配筋率(xi_b, fc, fy)` — ρmax = ξb·fc/fy（单筋矩形截面最大配筋率）。
- `def _混凝_界限相对受压区高度(rebar_grade)` — ξb 按钢筋级别查表（C50 及以下）。
- `def _混凝_偏心距增大系数(l0, h, zeta1, zeta2)` — η = 1 + ζ1·ζ2·(l0/h)² / 1400·e0/h（简化默认 e0=h/2，分母=1400·0.5=70
- `def _混凝_T形翼缘宽度(b, hf, l0)` — T 形截面翼缘有效宽度 bf' 简化：min(b + 12·hf, l0/3, b + S_n)。
返回 min(b +
- `def _混凝_受剪承载力(ft, b, h0, fyv, Asv, s)` — V = 0.7·ft·b·h0 + 1.25·fyv·Asv·h0/s（kN，GB 50010 简化）。
- `def _混凝_轴压承载力(fc, A, fy, As_prime, phi)` — N = 0.9·φ·(fc·A + fy·As')（kN）。
- `def _混凝_裂缝宽度(alpha_cr, psi, sigma_sk, Es, c, deq, rho_te)` — wmax = αcr·ψ·σsk/Es·(1.9c + 0.08·deq/ρte)（mm，GB 50007 公式）。
- `def _钢结_抗弯强度(M, gamma_x, Wn)` — σ = M/(γx·Wn) ≤ f（MPa）。M 单位 kN·m，Wn 单位 cm³ → 转换。
- `def _钢结_抗剪强度(V, S, I, tw)` — τ = V·S/(I·tw) ≤ fv（MPa）。V 单位 kN，S/I 单位 mm → 转换。
- `def _钢结_整体稳定系数(lambda_b)` — 梁整体稳定系数 φb 简化估算（λb 简化长细比）：
λb ≤ 0.6 → 1.0；> 0.6 → 用 1.07 - λ
- `def _钢结_轴压稳定系数(slenderness)` — 轴心受压稳定系数 φ 按长细比 λ 查 a 类截面近似公式：
λ ≤ 0.215 → φ = 1 - α·λ²；> 0.
- `def _钢结_宽厚比限值(level)` — 局部稳定宽厚比限值。level: 一般/严格。
- `def _钢结_焊缝计算长度(N, he, ffw)` — 角焊缝计算长度 lw = N/(he·ffw·0.7)（mm，正面角焊缝强度增大系数 βf=1.22 已忽略）。
- `def _钢结_螺栓抗剪承载力(ns, d, fv_b)` — 单个普通螺栓抗剪承载力 Nb = ns·πd²/4·fv_b（kN）。
- `def _钢结_螺栓抗拉承载力(d, ft_b)` — 单个普通螺栓抗拉承载力 Nt = πd²/4·ft_b（kN）。
- `def _钢结_高强螺栓摩擦型承载力(mu, nf, P)` — 单个高强螺栓摩擦型抗剪 Nv = μ·nf·P / 1.0（kN，承载力设计值）。
- `def _钢结_长细比(l0, i)` — λ = l0/i。
- `def _砌体_抗压承载力(phi, f, A)` — N ≤ φ·f·A（kN）。f MPa, A mm²。
- `def _砌体_局压承载力(gamma, f, Al)` — Nl ≤ γ·f·Al（kN）。
- `def _砌体_局压提高系数(Al, Aln)` — γ = 1 + 0.35·√(Al/Aln)，≤ 3。
- `def _砌体_高厚比(H0, h)` — β = H0/h（计算高厚比）。
- `def _砌体_允许高厚比(mortar_grade)` — [β] 按砂浆等级查表（无门窗洞口墙）。
- `def _砌体_稳定性判定(H0, h, mortar_grade)` — 墙柱稳定性判定：β ≤ [β]。返回 (是否稳定, β, [β])。
- `def _砌体_受剪承载力(fv, sigma0, A)` — V ≤ (fv + 0.18·σ0)·A（kN，GB 50003）。
- `def _砌体_网状配筋提高系数(rho_s, fyv)` — 网状配筋砌体抗压强度提高系数 Δf ≈ 2·ρ·fyv/100（简化估算，MPa）。
- `def _木结_顺纹抗压承载力(fc, An)` — N = fc·An（kN）。fc MPa, An mm²。
- `def _木结_抗弯承载力(fm, Wn)` — M = fm·Wn（kN·m）。fm MPa, Wn mm³。
- `def _木结_顺纹抗剪承载力(fv, b, I, S)` — V = fv·b·I/S（kN）。
- `def _木结_齿连接承压(fc_alpha, Ac)` — 齿连接承压承载力 N = fc_α·Ac（kN）。
- `def _木结_螺栓连接承载力(fv_bolt, a, d)` — 木结构螺栓单剪承载力 Nv = kv·d²·√a/10（简化估算，kN）。
fv_bolt 为螺栓钢材抗剪（MPa），a
- `def _木结_稳定系数(slenderness)` — 木构件稳定系数 φ（按长细比）：
λ ≤ 75 → φ = 1 - (λ/100)²；> 75 → φ = 3000/λ
- `def _木结_长细比(l0, i)` — λ = l0/i（与钢结构公式一致）。
- `def _基础_承载力修正(fak, eta_b, gamma, b, eta_d, gamma_m, d)` — 地基承载力修正 f = fak + ηb·γ·(b-3) + ηd·γm·(d-0.5)（kPa）。
b<3 取 3；b
- `def _基础_中心受压面积(N, f, gamma_m, d)` — 中心受压基础底面积 A ≥ N/(f - γm·d)（m²）。
- `def _基础_附加应力(p, gamma_m, d)` — 基底附加应力 p0 = p - γm·d（kPa）。
- `def _基础_Boussinesq应力(P, z, R)` — 集中力 P 在深度 z 处的附加应力 σz = 3P/(2π)·z³/R⁵（kPa）。
若未给 R（计算点到 P 的距离
- `def _基础_分层沉降(sigma_z, H, Es)` — 单层压缩量 s = σz·H/Es（mm）。sigma_z kPa, H m, Es MPa。
- `def _基础_单桩承载力(up, qsik_list, li_list, qpk, Ap)` — 单桩竖向承载力 Q = up·Σqsik·li + qpk·Ap（kN）。
up 周长 m，qsik_list 侧阻力 
- `def _基础_承台冲切承载力(ft, h0, um)` — 承台冲切承载力 F = 0.7·ft·h0·um（kN）。ft MPa, h0 mm, um mm。
- `def _抗震_水平地震作用(alpha, G)` — F = α·G（kN，底部剪力法等效单质点）。
- `def _抗震_地震影响系数(intensity, level)` — αmax 按烈度与水准查表（直接返回 αmax，Tg/α 需按场地与周期进一步计算）。
- `def _抗震_重力荷载代表值(dead_load, live_load, live_factor)` — G = 恒载 + ψ·活载（ψ 通常 0.5）。
- `def _抗震_楼层最小剪力(lambda_factor, G)` — Vmin = λ·G（kN）。
- `def _抗震_轴压比(N, fc, A)` — μ = N/(fc·A)（无量纲）。N kN, fc MPa, A mm²。
- `def _抗震_轴压比判定(N, fc, A, limit)` — 轴压比限值判定，返回 (是否满足, μ, 限值)。
- `def _抗震_剪跨比(M, V, h0)` — λ = M/(V·h0)（无量纲）。
- `def _抗震_抗震等级调整系数(structure_type, intensity)` — 按结构类型与烈度返回抗震等级调整系数（简化，1/2/3/4 级 → 1.0/0.8/0.75/0.7）。
- `def _抗震_弹性层间位移角限值(structure_type)` — 弹性层间位移角限值 [θe]（GB 50011）。
- `def _抗震_弹塑性层间位移角限值(structure_type)` — 弹塑性层间位移角限值 [θp]（GB 50011）。
- `def _register_building_struct(builtins: dict) -> None` — 将建筑结构工程子领域内建注册到解释器 builtins。
- `def _curry6(func)` — 
- `def _curry7(func)` — 
- `def _building_struct_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _引力_万有引力(m1, m2, r)` — 
- `def _引力_引力势能(m1, m2, r)` — 
- `def _引力_引力加速度(M, r)` — 
- `def _引力_环绕速度(M, r)` — 
- `def _引力_逃逸速度(M, r)` — 
- `def _引力_第三宇宙速度(M_planet, r_planet, M_sun, r_orbit)` — 
- `def _引力_圆轨道周期(M, r)` — 
- `def _引力_同步轨道半径(M, T)` — 
- `def _引力_轨道角速度(M, r)` — 
- `def _开普勒_周期(a, M)` — 
- `def _开普勒_半长轴(M, T)` — 
- `def _开普勒_椭圆面积(a, b)` — 
- `def _开普勒_偏心率(a, b)` — 
- `def _开普勒_近地点(a, e)` — 
- `def _开普勒_远地点(a, e)` — 
- `def _开普勒_椭圆周长(a, b)` — 
- `def _开普勒_平均运动(T)` — 
- `def _轨道_活力速度(M, r, a)` — 
- `def _轨道_角动量(M, m, a, e)` — 
- `def _轨道_总能量(M, m, a)` — 
- `def _轨道_近地点速度(M, a, e)` — 
- `def _轨道_远地点速度(M, a, e)` — 
- `def _轨道_霍曼转移(M, r1, r2)` — 从圆轨道 r1 转移到圆轨道 r2 的霍曼转移速度增量。
- `def _轨道_霍曼转移时间(M, r1, r2)` — 
- `def _轨道_逃逸能量(M, m, r)` — 将质量 m 从 r 处逃逸到无穷远所需能量。
- `def _潮汐_潮汐力(M, m, dr, r)` — 
- `def _潮汐_潮汐加速度(M, dr, r)` — 
- `def _潮汐_洛希极限刚体(R, rho_M, rho_m)` — 
- `def _潮汐_洛希极限流体(R, rho_M, rho_m)` — 
- `def _潮汐_引力势(M, r)` — 
- `def _潮汐_引力场强度(M, r)` — 
- `def _相对论_史瓦西半径(M)` — 
- `def _相对论_近日点进动(M, a, e)` — 
- `def _相对论_时间膨胀(t, M, r)` — 
- `def _相对论_引力红移(M, r)` — 
- `def _相对论_光线偏折(M, b)` — 
- `def _相对论_黑洞温度(M)` — 
- `def _register_celestial(builtins: dict) -> None` — 将天体力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（quantum 
- `def _celestial_symtab_names() -> list[str]` — 返回天体力学所有内建名（用于语义分析注册）。

**函数**
- `def lorenz_deriv(x: float, y: float, z: float, sigma: float, rho: float, beta: float) -> tuple[float, float, float]` — Lorenz 方程导数。
- `def lorenz_attractor(x0: float, y0: float, z0: float, dt: float, steps: int, sigma: float, rho: float, beta: float) -> list[tuple[float, float, float]]` — 计算 Lorenz 吸引子轨迹。
- `def henon_map(x: float, y: float, a: float, b: float) -> tuple[float, float]` — Hénon 映射。
- `def henon_attractor(x0: float, y0: float, a: float, b: float, steps: int) -> list[tuple[float, float]]` — Hénon 吸引子轨迹。
- `def logistic_map(x: float, r: float) -> float` — Logistic 映射：x_{n+1} = r * x_n * (1 - x_n)。
- `def logistic_orbit(x0: float, r: float, steps: int) -> list[float]` — Logistic 映射轨道。
- `def mandelbrot_iter(cx: float, cy: float, max_iter: int) -> int` — Mandelbrot 迭代。
- `def mandelbrot_set(width: int, height: int, x_min: float, x_max: float, y_min: float, y_max: float, max_iter: int) -> list[list[int]]` — 生成 Mandelbrot 集图像数据。
- `def julia_iter(cx: float, cy: float, px: float, py: float, max_iter: int) -> int` — Julia 集迭代。
- `def julia_set(width: int, height: int, cx: float, cy: float, max_iter: int) -> list[list[int]]` — 生成 Julia 集图像数据。
- `def box_counting_dim(points: list[tuple[float, float]], min_scale: float, max_scale: float) -> float` — 盒子计数法估算分形维数。
- `def lyapunov_exponent(logistic_r: float, x0: float, steps: int) -> float` — 计算 Logistic 映射的 Lyapunov 指数。
- `def _register_chaos_fractal(builtins: dict) -> None` — 注册混沌理论与分型内建到解释器。
- `def _chaos_fractal_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _摩尔质量(formula: str) -> float` — 计算化合物摩尔质量（g/mol）。支持简化公式如 H2O、C6H12O6。
- `def _理想气体方程(P, V, n, T)` — 理想气体状态方程 PV=nRT，已知三个求第四个。

参数：四个参数中一个为 None，其余三个为已知值。
P: Pa,
- `def _溶液浓度(mol_solute, vol_L)` — 摩尔浓度 c = n/V。
- `def _pH计算(H_conc)` — pH = -log10[H+]。
- `def _pOH计算(OH_conc)` — pOH = -log10[OH-]。
- `def _Henderson_方程(pKa, acid, base)` — Henderson-Hasselbalch 方程：pH = pKa + log10([base]/[acid])。
- `def _Gibbs自由能(ΔH, ΔS, T)` — ΔG = ΔH - TΔS。ΔH: J/mol, ΔS: J/(mol·K), T: K。
- `def _平衡常数(ΔG_std, T)` — K = exp(-ΔG°/RT)。ΔG_std: J/mol。
- `def _Arrhenius方程(A, Ea, T)` — k = A·exp(-Ea/RT)。A: 频率因子, Ea: 活化能 J/mol。
- `def _Nernst方程(E_std, n, Q)` — Nernst 方程：E = E° - (RT/nF)·lnQ，25°C 简化。
- `def _反应速率常溫比(k1, T1, Ea)` — 已知 k1 在 T1 下的速率常数，求 Ea 下的 k2（T2=298K）。
- `def _烷烃通式(n)` — CnH(2n+2)，返回分子式字符串。
- `def _烯烃通式(n)` — CnH2n，返回分子式字符串。
- `def _炔烃通式(n)` — CnH(2n-2)，返回分子式字符串。
- `def _同分异构体数(n)` — 烷烃 CnH(2n+2) 的同分异构体数目（n≤10 精确值）。
- `def _不饱和度(formula)` — 计算不饱和度（双键等价数）。公式格式如 C6H12O6。
- `def _register_chemistry(builtins: dict) -> None` — 将化学领域内建注册到解释器 builtins。
- `def _curry1(func)` — 一参柯里化。
- `def _register_chemistry_symtab_names() -> list[str]` — 返回化学领域所有内建名。

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _分子轨道能量(Z, n)` — 类氢原子轨道能量（eV）。E = -13.6 * Z² / n²。
- `def _反应活化能(温度, 指前因子, 速率常数)` — Arrhenius方程反算活化能（kJ/mol）。Ea = R*T*ln(A/k)。
- `def _键长计算(原子1半径, 原子2半径, 键级)` — 共价键长估算（pm）。
- `def _振动频率(力常数_Nm, 约化质量_kg)` — 简谐振动频率（Hz）。
- `def _热力学稳定性(生成焓_kJ, 熵_J_K, 温度_K)` — 吉布斯自由能判据。ΔG = ΔH - TΔS。
- `def _溶剂化能(电荷, 半径_A, 介电常数)` — Born溶剂化能（kJ/mol）。
- `def _register_comp_chem(builtins: dict) -> None` — 
- `def _comp_chem_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _大O比较(n, threshold)` — 根据 n 的大小估算可用算法复杂度等级。

n ≤ 10:      O(n!) 可行
n ≤ 20:      O(2
- `def _递归深度估算(T_n, n)` — 估算递归算法的调用次数（简化版）。

T(n) = T(n-1) + O(1)  →  O(n)
T(n) = 2·T(
- `def _香农熵(probabilities)` — 计算离散随机变量的香农熵 H = -Σ p·log2(p)。
- `def _信息量(probability)` — 单个事件的自信息 I = -log2(p)。
- `def _编码效率(熵, 平均码长)` — 编码效率 η = H/L，衡量编码接近理论极限的程度。
- `def _完全图边数(n)` — n 个顶点的完全图边数：n(n-1)/2。
- `def _树边数(n)` — n 个顶点的树边数：n-1。
- `def _dfs遍历数(n, branching)` — DFS 遍历节点数上界估算。
- `def _最短路径估算(n, m)` — Dijkstra 最短路径复杂度估算。
- `def _德摩根定律_not_and(a, b)` — ¬(a ∧ b) ≡ ¬a ∨ ¬b。
- `def _德摩根定律_not_or(a, b)` — ¬(a ∨ b) ≡ ¬a ∧ ¬b。
- `def _蕴含等价(p, q)` — p → q ≡ ¬p ∨ q。
- `def _双蕴含等价(p, q)` — p ↔ q ≡ (p → q) ∧ (q → p)。
- `def _真值表行数(n)` — n 个命题变元的真值表行数：2^n。
- `def _栈操作操作(操作序列)` — 模拟栈操作序列，返回最终栈状态和错误次数。

操作序列：["push", 1, "push", 2, "pop", ..
- `def _队列操作操作(操作序列)` — 模拟队列操作序列，返回最终队列状态和错误次数。
- `def _register_computer_science(builtins: dict) -> None` — 将计算机科学领域内建注册到解释器 builtins。
- `def _register_computer_science_symtab_names() -> list[str]` — 返回计算机科学领域所有内建名。

### domains.creative_coding

- [PerlinNoise](`#class-perlinnoise`)
- [FlowField](`#class-flowfield`)

**函数**
- `def simplex_noise_2d(x: float, y: float) -> float` — 简化 Simplex 噪声。
- `def particle_system(n: int, width: float, height: float, source_x: float, source_y: float) -> list[dict]` — 初始化粒子系统。
- `def particle_update(particles: list[dict], dt: float, gravity: float) -> list[dict]` — 更新粒子。
- `def fractal_barnsley_fern(n: int) -> list[tuple[float, float]]` — Barnsley 蕨类分形。
- `def fractal_sierpinski(n: int) -> list[tuple[float, float]]` — Sierpinski 三角形。
- `def color_hsl_to_rgb(h: float, s: float, l: float) -> tuple[float, float, float]` — HSL 转 RGB。
- `def color_lerp(c1: tuple[float, float, float], c2: tuple[float, float, float], t: float) -> tuple[float, float, float]` — 颜色插值。
- `def audio_reactive(freq_bands: list[float], energy: float, sensitivity: float) -> list[float]` — 音频反应式可视化。
- `def visual_midi(note: int, frequency: float, duration: float) -> dict` — MIDI 音符可视化。
- `def _register_creative_coding(builtins: dict) -> None` — 注册创意编程内建到解释器。
- `def _creative_coding_symtab_names() -> list[str]` — 

**方法**
- `def __init__(self, seed: int)`
- `def _shuffle(self, seed: int) -> list[int]`
- `def _fade(self, t: float) -> float`
- `def _lerp(self, a: float, b: float, t: float) -> float`
- `def _grad(self, hash_val: int, x: float, y: float) -> float`
- `def noise_2d(self, x: float, y: float) -> float`
- `def noise_3d(self, x: float, y: float, z: float) -> float`
- `def __init__(self, width: int, height: int, scale: float)`
- `def _generate(self) -> None`
- `def get_direction(self, x: int, y: int) -> tuple[float, float]`

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _水印嵌入强度(载体大小_bytes, 水印比特数, 嵌入密度)` — 水印嵌入强度评估。
- `def _版权保护指数(存证数, 验证通过率, 篡改检测数)` — 版权保护综合指数（0-100）。
- `def _访问控制粒度(角色数, 权限数, 策略数)` — 访问控制粒度评估。
- `def _哈希碰撞概率(哈希位数, 数据量)` — 生日攻击碰撞概率估算。P ≈ n² / 2^（b+1）。
- `def _密钥轮换周期(密钥长度_bit, 计算能力_次秒)` — 密钥安全轮换周期（天）。
- `def _数字指纹(数据)` — 生成数据的SHA-256指纹（十六进制前16位）。
- `def _register_digital_rights(builtins: dict) -> None` — 
- `def _digital_rights_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _牛顿_力(m, a)` — 
- `def _牛顿_加速度(F, m)` — 
- `def _牛顿_质量(F, a)` — 
- `def _摩擦_滑动摩擦力(mu, N)` — 
- `def _摩擦_最大静摩擦(mu_s, N)` — 
- `def _合力_同向(F1, F2)` — 
- `def _合力_垂直(F1, F2)` — 
- `def _动量_动量(m, v)` — 
- `def _动量_冲量(F, t)` — 
- `def _动量_末动量(p1, F, t)` — 
- `def _动量_碰后速度1(m1, v1, m2, v2, v2_after)` — 
- `def _弹性碰撞_速度1(m1, v1, m2, v2)` — 
- `def _弹性碰撞_速度2(m1, v1, m2, v2)` — 
- `def _碰撞_恢复系数(v1, v2, v1_after, v2_after)` — 
- `def _功_功(F, s, theta_rad)` — 
- `def _功_功率(W, t)` — 
- `def _功_功率力速(F, v)` — 
- `def _能量_动能(m, v)` — 
- `def _能量_重力势能(m, h, g_val)` — 
- `def _能量_弹性势能(k, x)` — 
- `def _能量_动能定理(m, v1, v2)` — 
- `def _能量_守恒末速度(v1, h1, h2, g_val)` — 
- `def _转动_力矩(r, F, theta_rad)` — 
- `def _转动_角加速度(M, I)` — 
- `def _转动惯量_质点(m, r)` — 
- `def _转动惯量_细杆端点(m, L)` — 
- `def _转动惯量_细杆中心(m, L)` — 
- `def _转动惯量_圆盘(m, r)` — 
- `def _转动惯量_圆环(m, r)` — 
- `def _转动惯量_实心球(m, r)` — 
- `def _转动惯量_空心球(m, r)` — 
- `def _转动惯量_圆柱(m, r)` — 
- `def _转动惯量_平行轴(I_cm, m, d)` — 
- `def _转动_角动量(I, omega)` — 
- `def _转动_角动量守恒(I1, omega1, I2)` — 
- `def _转动_转动动能(I, omega)` — 
- `def _振动_周期(m, k)` — 
- `def _振动_频率(T)` — 
- `def _振动_角频率(T)` — 
- `def _振动_位移(A, omega, t)` — 
- `def _振动_速度(A, omega, t)` — 
- `def _振动_总能量(k, A)` — 
- `def _振动_单摆周期(L, g_val)` — 
- `def _振动_复摆周期(I, m, g_val, d)` — 
- `def _振动_阻尼角频率(omega0, beta)` — 
- `def _register_dynamics(builtins: dict) -> None` — 将动力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（mechanics
- `def _dynamics_symtab_names() -> list[str]` — 返回动力学所有内建名（用于语义分析注册，避免报未定义）。

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _复利终值(P, r, n)` — 复利终值 FV = P(1+r)^n。P: 本金, r: 年利率, n: 年数。
- `def _复利现值(FV, r, n)` — 复利现值 PV = FV/(1+r)^n。
- `def _年金终值(PMT, r, n)` — 普通年金终值 FV = PMT · [(1+r)^n - 1] / r。
- `def _年金现值(PMT, r, n)` — 普通年金现值 PV = PMT · [1 - (1+r)^(-n)] / r。
- `def _NPV(cash_flows, rate)` — 净现值 NPV = Σ CFt/(1+r)^t。cash_flows: [CF0, CF1, ...]。
- `def _IRR估算(cash_flows, guess)` — IRR 估算（牛顿法，最多 100 次迭代）。
- `def _单利终值(P, r, t)` — 单利终值 FV = P(1 + r·t)。
- `def _实际利率(名义利率, 通胀率)` — 费雪方程：r_real ≈ r_nominal - inflation。
- `def _需求价格弹性(价格, 需求量, dQ_dP)` — 点弹性 Ed = (dQ/dP) · (P/Q)。
- `def _供给价格弹性(价格, 供给量, dQ_dP)` — 供给点弹性 Es = (dQ/dP) · (P/Q)。
- `def _消费者剩余(需求函数系数, 均衡价格)` — 线性需求 Q = a - bP，消费者剩余 CS = (a - b·Pe)²/(2b)。
- `def _生产者剩余(供给函数系数, 均衡价格)` — 线性供给 Q = c + dP，生产者剩余 PS = (Pe - c/d)²·d/2。
- `def _边际成本(Q, C_fixed, C_variable_per_unit)` — 边际成本 MC = dC/dQ = C_variable（线性成本）。
- `def _总成本(Q, C_fixed, C_variable_per_unit)` — 总成本 TC = FC + VC·Q。
- `def _GDP支出法(C, I, G, X, M)` — GDP = C + I + G + (X - M)。消费+投资+政府+净出口。
- `def _GDP收入法(W, R, I_prof, Pi)` — GDP = 工资 + 租金 + 利息 + 利润（简化版）。
- `def _乘数效应(边际消费倾向)` — 投资乘数 k = 1/(1-MPC)。
- `def _物价指数(基期价格, 报告期价格)` — CPI = (报告期价格/基期价格) × 100。
- `def _通货膨胀率(基期CPI, 本期CPI)` — 通胀率 = (CPI_t - CPI_0) / CPI_0 × 100%。
- `def _人均GDP(GDP, 人口)` — 人均 GDP = GDP / 人口。
- `def _register_economics(builtins: dict) -> None` — 将经济学领域内建注册到解释器 builtins。
- `def _register_economics_symtab_names() -> list[str]` — 返回经济学领域所有内建名。

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _欧姆定律(V, R)` — 欧姆定律：I = V/R。V: V, R: Ω, 返回 I: A。
- `def _电功率(V, I)` — 电功率 P = V·I。
- `def _电功率_R(V, R)` — P = V²/R。
- `def _分压公式(V_in, R1, R2)` — 串联分压：V_out = V_in · R2/(R1+R2)。
- `def _分流公式(I_total, R1, R2)` — 并联分流：I1 = I_total · R2/(R1+R2)。
- `def _串联电阻(*resistors)` — 串联总电阻 R = R1 + R2 + ...
- `def _并联电阻(*resistors)` — 并联总电阻 1/R = 1/R1 + 1/R2 + ...
- `def _感抗(f, L)` — 感抗 X_L = 2πfL。f: Hz, L: H。
- `def _容抗(f, C)` — 容抗 X_C = 1/(2πfC)。f: Hz, C: F。
- `def _RLC阻抗(R, L, C, f)` — RLC 串联阻抗 Z = sqrt(R² + (XL - XC)²)。
- `def _谐振频率(L, C)` — RLC 谐振频率 f₀ = 1/(2π√(LC))。
- `def _功率因数(Z, R)` — 功率因数 cosφ = R/Z。
- `def _库仑力(q1, q2, r)` — 库仑定律 F = k·q1·q2/r²。k = 1/(4πε₀)。
- `def _电场强度(Q, r)` — 点电荷电场 E = Q/(4πε₀r²)。
- `def _长直导线磁场(I, r)` — 无限长直导线磁场 B = μ₀I/(2πr)。
- `def _通电导线受力(B, I, L, angle)` — 安培力 F = BIL·sinθ。
- `def _RC截止频率(R, C)` — RC 低通滤波器截止频率 fc = 1/(2πRC)。
- `def _RL截止频率(R, L)` — RL 低通滤波器截止频率 fc = R/(2πL)。
- `def _傅里叶频率分辨率(sample_rate, N)` — DFT 频率分辨率 Δf = fs/N。
- `def _采样定理_min_rate(f_max)` — 奈奎斯特最低采样率 fs ≥ 2·f_max。
- `def _register_electrical(builtins: dict) -> None` — 将电气工程领域内建注册到解释器 builtins。
- `def _register_electrical_symtab_names() -> list[str]` — 返回电气工程领域所有内建名。

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _电_库仑力(q1, q2, r)` — 
- `def _电_电场(Q, r)` — 
- `def _电_电势(Q, r)` — 
- `def _电_电势能(q1, q2, r)` — 
- `def _电_平行板电容(S, d)` — 
- `def _电_电容储能(C, V)` — 
- `def _电_电容电荷(C, V)` — 
- `def _电_高斯通量(Q)` — 
- `def _电_电偶极矩(q, d)` — 
- `def _电路_电压(I, R)` — 
- `def _电路_电流(V, R)` — 
- `def _电路_电阻(V, I)` — 
- `def _电路_串联电阻(R_list)` — 
- `def _电路_并联电阻(R_list)` — 
- `def _电路_功率(V, I)` — 
- `def _电路_功率热(I, R)` — 
- `def _电路_功率压(V, R)` — 
- `def _电路_焦耳热(I, R, t)` — 
- `def _磁_洛伦兹力(q, v, B, theta_rad)` — 
- `def _磁_安培力(B, I, L, theta_rad)` — 
- `def _磁_直导线磁场(I, r)` — 
- `def _磁_圆线圈中心磁场(I, R)` — 
- `def _磁_螺线管磁场(n, I)` — 
- `def _磁_磁通量(B, A, theta_rad)` — 
- `def _磁_回旋半径(m, v, q, B)` — 
- `def _磁_回旋频率(q, B, m)` — 
- `def _感应_法拉第电动势(N, dPhi, dt)` — 
- `def _感应_动生电动势(B, L, v)` — 
- `def _感应_自感电动势(L, dI, dt)` — 
- `def _感应_磁场能量(L, I)` — 
- `def _感应_互感(N2, Phi21, I1)` — 
- `def _感应_RL时间常数(L, R)` — 
- `def _交流_感抗(f, L)` — 
- `def _交流_容抗(f, C)` — 
- `def _交流_阻抗(R, XL, XC)` — 
- `def _交流_谐振频率(L, C)` — 
- `def _交流_有效值(V_max)` — 
- `def _交流_峰值(V_rms)` — 
- `def _交流_功率因数(R, Z)` — 
- `def _交流_有功功率(V, I, cos_phi)` — 
- `def _交流_视在功率(V, I)` — 
- `def _交流_无功功率(V, I, cos_phi)` — 
- `def _交流_品质因数(R, L, C)` — 
- `def _register_em(builtins: dict) -> None` — 将电磁学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（thermo 之后
- `def _em_symtab_names() -> list[str]` — 返回电磁学所有内建名（用于语义分析注册）。

**函数**
- `def _adc_value(voltage: float, ref_voltage: float, bits: int) -> int` — ADC 电压 → 数字值。
- `def _adc_voltage(digital: int, ref_voltage: float, bits: int) -> float` — ADC 数字值 → 电压。
- `def _dac_value(voltage: float, ref_voltage: float, bits: int) -> int` — DAC 电压 → 数字值（同 ADC）。
- `def _pwm_duty_cycle(on_time: float, period: float) -> float` — 计算占空比（%）。
- `def _pwm_period(freq: float, duty: float) -> float` — 计算周期（秒），给定频率。
- `def _pwm_on_time(period: float, duty: float) -> float` — 计算高电平时间（秒）。
- `def _thermistor_temperature(R: float, R0: float, T0: float, B: float) -> float` — NTC 热敏电阻温度计算（K）。

R: 当前电阻 (Ω)
R0: 标称电阻 (Ω)，通常 25°C 时
T0: 标称温
- `def _natural_log(x: float) -> float` — 自然对数。
- `def _thermistor_temperature(R: float, R0: float, T0: float, B: float) -> float` — NTC 热敏电阻温度计算（K）。
- `def _thermistor_temperature_celsius(R: float, R0: float, T0: float, B: float) -> float` — NTC 热敏电阻温度计算（°C）。
- `def _photocell_light(R_dark: float, R_light: float, R_sensor: float) -> float` — 光敏电阻光照估算（相对百分比）。
- `def _ultrasonic_distance(time_us: float, speed_of_sound: float) -> float` — 超声波测距（米）。time_us: 回波时间（微秒）。
- `def _accel_angle(ax: float, ay: float, az: float) -> tuple[float, float]` — 加速度计计算俯仰角和横滚角（度）。
- `def _stepper_steps_to_degrees(steps: int, steps_per_rev: int, reduction: float) -> float` — 步进数 → 角度（度）。
- `def _stepper_degrees_to_steps(degrees: float, steps_per_rev: int, reduction: float) -> int` — 角度 → 步进数。
- `def _servo_angle(pulse_us: float, min_pulse: float, max_pulse: float) -> float` — 脉冲宽度 → 舵机角度（度）。
- `def _servo_pulse(angle: float, min_pulse: float, max_pulse: float) -> float` — 舵机角度 → 脉冲宽度（微秒）。
- `def _uart_baud_error(desired: float, crystal: float, divider: int) -> float` — UART 波特率误差计算（%）。
- `def _i2c_pullup_resistor(vcc: float, i2c_current: float) -> float` — 计算 I2C 上拉电阻（Ω）。
- `def _spi_clock_divider(clock: float, sys_clock: float) -> int` — SPI 时钟分频器计算。
- `def _dc_motor_rpm(voltage: float, k_v: float) -> float` — 直流电机转速（RPM）。k_v: RPM/V。
- `def _dc_motor_current(torque: float, k_t: float) -> float` — 直流电机堵转电流（A）。k_t: Nm/A。
- `def _stepper_torque(freq: float, voltage: float, resistance: float) -> float` — 步进电机转矩估算（简化）。
- `def _battery_voltage(cells: int, cell_type: str) -> float` — 电池组电压估算。
- `def _battery_capacity_wh(ah: float, voltage: float) -> float` — 电池容量（Wh）。
- `def _power_budget(items: list) -> float` — 功耗预算计算（W）。items: [(power_w, duty_cycle), ...]。
- `def _register_embedded(builtins: dict) -> None` — 将嵌入式领域内建注册到解释器。
- `def _register_embedded_symtab_names() -> list[str]` — 
- `def _curry2(fn)` — 两参 → 柯里化 f(a)(b)。
- `def _curry3(fn)` — 三参 → 柯里化 f(a)(b)(c)。
- `def _curry4(fn)` — 四参 → 柯里化 f(a)(b)(c)(d)。
- `def _curry2(fn)` — 

**函数**
- `def _梁弯曲正应力(M: float, y: float, I: float) -> float` — 梁弯曲正应力 σ = My/I。
- `def _梁弯曲挠度(载荷: float, L: float, E: float, I: float, 支座: str) -> float` — 梁弯曲挠度计算。
- `def _柱临界载荷(Paschen公式) -> float` — 柱临界载荷（欧拉公式）。
- `def _轴应力(扭矩: float, d: float) -> float` — 圆轴扭转剪应力 τ = 16T/(πd³)。
- `def _register_structural(builtins: dict) -> None` — 
- `def _管道沿程损失(Q: float, L: float, D: float, f: float) -> float` — 达西-魏斯巴赫公式：hf = f·L/D·v²/2g。
- `def _雷诺数(v: float, D: float, nu: float) -> float` — 雷诺数 Re = vD/ν。
- `def _伯努利方程(z1: float, p1: float, v1: float, z2: float, rho: float) -> float` — 伯努利方程求 p2。
- `def _register_fluid(builtins: dict) -> None` — 
- `def _热传导(Q: float, A: float, dT: float, L: float, k: float) -> float` — 傅里叶热传导：Q = kA·dT/L → 求 k。
- `def _对流换热(h: float, A: float, Tw: float, Tf: float) -> float` — 牛顿冷却定律：Q = hA(Tw - Tf)。
- `def _辐射换热(ε: float, A: float, T1: float, T2: float, σ: float) -> float` — 斯蒂芬-玻尔兹曼辐射定律。
- `def _register_thermal(builtins: dict) -> None` — 
- `def _三相功率线电压(VL: float, IL: float, pf: float) -> float` — 三相功率 P = √3·VL·IL·pf。
- `def _变压器变比(N1: int, N2: int) -> float` — 变压器变比 k = N1/N2。
- `def _电机扭矩(T: float, n: float, P: float) -> float` — 电机扭矩 T = 9550·P/n。
- `def _register_power(builtins: dict) -> None` — 
- `def _PID输出(Kp: float, Ki: float, Kd: float, e: float, e_integral: float, e_derivative: float) -> float` — PID 控制器输出。
- `def _一阶惯性环节(T: float, t: float) -> float` — 一阶惯性环节响应：1 - e^(-t/T)。
- `def _二阶系统参数(ζ: float, ωn: float) -> dict` — 二阶系统性能指标。
- `def _register_control(builtins: dict) -> None` — 
- `def _胡克定律(σ: float, E: float) -> float` — 胡克定律：ε = σ/E。
- `def _疲劳寿命(S: float, Sn: float, N: float) -> int` — Miner 疲劳寿命估算（简化）。
- `def _注册材料科学(builtins: dict) -> None` — 
- `def _register_extended_modeling(builtins: dict) -> None` — 注册所有扩展建模函数。
- `def _register_extended_modeling_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _BlackScholes期权定价(S, K, T, r, sigma)` — Black-Scholes欧式看涨期权定价。
- `def _VaR风险价值(组合价值, 日收益率标准差, 置信水平, 持有期天)` — VaR风险价值估算。
- `def _夏普比率(超额收益, 波动率)` — 夏普比率 = (R-Rf)/σ。
- `def _信用评分(负债率, 收入稳定性, 逾期次数, 信用历史年限)` — 简化信用评分（0-100）。
- `def _流动性覆盖率(优质流动资产, 净现金流出)` — LCR = HQLA / 净现金流出。
- `def _杠杆率(总资产, 权益资本)` — 杠杆率 = 权益资本 / 总资产。
- `def _register_fintech(builtins: dict) -> None` — 
- `def _fintech_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _静水_压强(rho, h, g_val)` — 
- `def _静水_深度(p, rho, g_val)` — 
- `def _帕斯卡_液压力(F1, A1, A2)` — 
- `def _浮力_浮力(rho, V, g_val)` — 
- `def _浮力_漂浮判断(rho_obj, rho_fluid)` — 
- `def _静水_壁面总压力(rho, h, b, g_val)` — 
- `def _管道_圆管面积(d)` — 
- `def _管道_矩形面积(w, h)` — 
- `def _流量_体积流量(A, v)` — 
- `def _流量_质量流量(rho, A, v)` — 
- `def _流量_连续性速度(A1, v1, A2)` — 
- `def _流量_流速(Q, A)` — 
- `def _伯努利_压强2(p1, rho, v1, h1, v2, h2, g_val)` — 
- `def _伯努利_小孔流速(h, g_val)` — 
- `def _伯努利_文丘里流速(dp, rho, A1, A2)` — 
- `def _伯努利_皮托管(dp, rho)` — 
- `def _流_雷诺数(rho, v, D, mu)` — 
- `def _流_雷诺数运动(v, D, nu)` — 
- `def _流_流态判断(Re)` — 
- `def _粘_粘性切应力(mu, du, dy)` — 
- `def _粘_泊肃叶流量(r, dp, mu, L)` — 
- `def _粘_泊肃叶流速(r, dp, mu, L)` — 
- `def _粘_斯托克斯阻力(eta, r, v)` — 
- `def _粘_终端速度(r, g_val, rho_s, rho_f, eta)` — 
- `def _粘_达西水头损失(f, L, D, v, g_val)` — 
- `def _register_fluid(builtins: dict) -> None` — 将流体力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（dynamics
- `def _curry5(func)` — 五参 → 柯里化 f(a)(b)(c)(d)(e)。
- `def _fluid_symtab_names() -> list[str]` — 返回流体力学所有内建名（用于语义分析注册，避免报未定义）。

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _边界_局部雷诺数(U, x, nu)` — 
- `def _边界_层流厚度(U, x, nu)` — 
- `def _边界_层流位移厚度(U, x, nu)` — 
- `def _边界_层流动量厚度(U, x, nu)` — 
- `def _边界_层流壁面切应力(rho, U, x, nu)` — 
- `def _边界_层流平板阻力系数(Re_L)` — 
- `def _边界_湍流厚度(U, x, nu)` — 
- `def _边界_湍流平板阻力系数(Re_L)` — 
- `def _边界_混合平板阻力系数(Re_L)` — 
- `def _边界_形状因子(delta_star, theta)` — 
- `def _可压缩_声速(gamma, R_gas, T)` — 
- `def _可压缩_马赫数(v, c)` — 
- `def _可压缩_等熵温度比(gamma, Ma)` — 
- `def _可压缩_等熵压强比(gamma, Ma)` — 
- `def _可压缩_等熵密度比(gamma, Ma)` — 
- `def _可压缩_激波压强比(gamma, Ma1)` — 
- `def _可压缩_激波密度比(gamma, Ma1)` — 
- `def _可压缩_激波温度比(gamma, Ma1)` — 
- `def _可压缩_激波后马赫数(gamma, Ma1)` — 
- `def _可压缩_临界压强比(gamma)` — 
- `def _可压缩_临界温度比(gamma)` — 
- `def _可压缩_喷管面积比(gamma, Ma)` — 
- `def _可压缩_普朗特迈耶角(gamma, Ma)` — 
- `def _明渠_谢才流速(C, R, S)` — 
- `def _明渠_曼宁流速_SI(n, R, S)` — 
- `def _明渠_曼宁流速_英制(n, R, S)` — 
- `def _明渠_矩形水力半径(b, h)` — 
- `def _明渠_梯形水力半径(b, h, m)` — 
- `def _明渠_弗劳德数(v, h, g_val)` — 
- `def _明渠_流态判断(Fr)` — 
- `def _明渠_临界水深(q, g_val)` — 
- `def _明渠_临界流速(h_c, g_val)` — 
- `def _明渠_水跃共轭水深(y1, Fr1)` — 
- `def _明渠_水跃能量损失(y1, y2)` — 
- `def _明渠_比能(h, v, g_val)` — 
- `def _泵_扬程(p1, p2, rho, v1, v2, dz, g_val)` — 
- `def _泵_有效功率(rho, g_val, Q, H)` — 
- `def _泵_轴功率(rho, g_val, Q, H, eta)` — 
- `def _泵_效率(P_w, P_shaft)` — 
- `def _泵_风机全压功率(p_t, Q)` — 
- `def _泵_比转速(n_rpm, Q, H)` — 
- `def _泵_相似律流量(n1, n2, D1, D2, Q1)` — 
- `def _泵_相似律扬程(n1, n2, D1, D2, H1)` — 
- `def _泵_相似律功率(n1, n2, D1, D2, P1)` — 
- `def _泵_风机静压升(p_t, rho, v)` — 
- `def _管损_局部水头损失(zeta, v, g_val)` — 
- `def _管损_当量长度(zeta, D, f)` — 
- `def _管损_总水头损失(f, L, D, v, sum_zeta, g_val)` — 
- `def _管损_突扩阻力系数(A1, A2)` — 
- `def _管损_突缩阻力系数(A1, A2)` — 
- `def _管损_比阻(f, L, D, g_val)` — 
- `def _管损_串联比阻(S1, S2)` — 
- `def _管损_并联比阻(S1, S2)` — 
- `def _管损_锐缘入口系数()` — 
- `def _管损_出口损失系数()` — 
- `def _管损_阻力系数查询(name_key)` — 
- `def _register_fluid_exp(builtins: dict) -> None` — 
- `def _fluid_exp_symtab_names() -> list[str]` — 

### domains.game_dev

- [GameConfig](`#class-gameconfig`)
- [Sprite](`#class-sprite`)
- [Particle](`#class-particle`)

**函数**
- `def game_loop(dt: float, update_fn, render_fn) -> None` — 游戏主循环（抽象接口）。
- `def sprite_create(x: float, y: float, w: float, h: float) -> dict` — 创建精灵。
- `def sprite_move(sprite: dict, dx: float, dy: float) -> dict` — 移动精灵。
- `def sprite_apply_force(sprite: dict, fx: float, fy: float, mass: float) -> dict` — 对精灵施加力。
- `def sprite_collide(a: dict, b: dict) -> bool` — AABB 碰撞检测。
- `def sprite_bounce(sprite: dict, width: float, height: float) -> dict` — 边界反弹。
- `def particle_emitter(x: float, y: float, count: int, speed: float) -> list[dict]` — 粒子发射器。
- `def particle_update(particles: list[dict], dt: float, gravity: float) -> list[dict]` — 更新粒子。
- `def physics_gravity(obj: dict, g: float, dt: float) -> dict` — 应用重力。
- `def physics_apply_force(obj: dict, fx: float, fy: float, mass: float, dt: float) -> dict` — 施加力。
- `def physics_solve_collision(a: dict, b: dict) -> tuple[dict, dict]` — 弹性碰撞求解。
- `def audio_play(freq: float, duration: float, volume: float) -> dict` — 播放音频（模拟）。
- `def audio_stop() -> None` — 停止音频（模拟）。
- `def audio_volume(vol: float) -> None` — 设置音量。
- `def input_key(key: str) -> bool` — 检测按键（模拟）。
- `def input_mouse() -> tuple[float, float]` — 获取鼠标位置（模拟）。
- `def input_gamepad(axis: str) -> float` — 获取游戏杆输入（模拟）。
- `def render_2d(sprite: dict, screen_x: float, screen_y: float) -> dict` — 2D 渲染变换。
- `def render_3d(point: tuple, camera_pos: tuple, target: tuple) -> tuple[float, float]` — 3D 到 2D 投影。
- `def camera_look_at(eye: tuple, center: tuple, up: tuple) -> list[list[float]]` — 生成视图矩阵。
- `def _register_game_dev(builtins: dict) -> None` — 注册游戏开发内建到解释器。
- `def _game_dev_symtab_names() -> list[str]` — 

**函数**
- `def ga_evolve(population: list[list[int]], fitness_fn: Callable, pop_size: int, max_gen: int, mutation_rate: float, crossover_rate: float) -> dict` — 遗传算法主循环。
- `def _roulette_select(population: list, probs: list) -> list[int]` — 轮盘赌选择。
- `def _crossover(parent1: list[int], parent2: list[int]) -> list[int]` — 单点交叉。
- `def _mutate(individual: list[int], rate: float) -> list[int]` — 位翻转变异。
- `def elitism_preserve(population: list[list[int]], fitness_fn: Callable, elite_count: int) -> list[list[int]]` — 精英保留。
- `def neuro_evolve(genome_length: int, fitness_fn: Callable, pop_size: int, max_gen: int) -> dict` — 简化版神经进化（NEAT风格）。
- `def nesma_estimate(lines_of_code: int) -> float` — NESMA 功能点估算。
- `def code_generation(template: str, params: dict) -> str` — 模板代码生成。
- `def code_optimizer(code: str) -> str` — 简单代码优化（字符串层面）。
- `def hyperparameter_search(search_space: dict, fitness_fn: Callable, n_iterations: int) -> dict` — 随机超参数搜索。
- `def _register_genetic_algo(builtins: dict) -> None` — 注册遗传算法内建到解释器。
- `def _genetic_algo_symtab_names() -> list[str]` — 

### domains.graph

- [Graph](`#class-graph`)

**函数**
- `def bfs(graph: Graph, start: int) -> Dict[int, int]` — BFS 遍历，返回各顶点到起点的距离

Args:
    graph: 图对象
    start: 起始顶点

Re
- `def bfs_path(graph: Graph, start: int, end: int) -> Optional[List[int]]` — BFS 最短路径

Args:
    graph: 图对象
    start: 起始顶点
    end: 目标顶点
- `def dfs(graph: Graph, start: int) -> List[int]` — DFS 遍历

Args:
    graph: 图对象
    start: 起始顶点

Returns:
    遍
- `def dfs_cycles(graph: Graph) -> List[List[int]]` — DFS 检测环（有向图）

Args:
    graph: 有向图对象

Returns:
    环列表
- `def dijkstra(graph: Graph, start: int) -> Tuple[List[float], List[Optional[int]]]` — Dijkstra 最短路径

Args:
    graph: 图对象（权重必须非负）
    start: 起始顶点

- `def dijkstra_path(graph: Graph, start: int, end: int) -> Optional[List[int]]` — Dijkstra 最短路径（单点对）

Args:
    graph: 图对象
    start: 起始顶点
   
- `def prim(graph: Graph, start: int) -> List[Tuple[int, int, float]]` — Prim 算法最小生成树

Args:
    graph: 无向连通图
    start: 起始顶点

Return
- `def kruskal(graph: Graph) -> List[Tuple[int, int, float]]` — Kruskal 算法最小生成树

Args:
    graph: 无向连通图

Returns:
    最小生成树的
- `def topological_sort(graph: Graph) -> Optional[List[int]]` — 拓扑排序（Kahn 算法）

Args:
    graph: 有向无环图 (DAG)

Returns:
    拓扑
- `def connected_components(graph: Graph) -> List[List[int]]` — 连通分量分析

Args:
    graph: 无向图

Returns:
    连通分量列表
- `def _strongly_connected_components(graph: Graph) -> List[List[int]]` — 有向图的强连通分量（简化版，使用 Kosaraju 算法）
- `def is_connected(graph: Graph) -> bool` — 检查图是否连通

Args:
    graph: 无向图

Returns:
    是否连通
- `def random_graph(n: int, m: int, directed: bool, weight_range: Tuple[float, float]) -> Graph` — 生成随机图

Args:
    n: 顶点数
    m: 边数
    directed: 是否为有向图
    w
- `def complete_graph(n: int, weight: float) -> Graph` — 生成完全图
- `def path_graph(n: int, weight: float) -> Graph` — 生成路径图
- `def cycle_graph(n: int, weight: float) -> Graph` — 生成环图
- `def _register_graph(builtins: dict) -> None` — 注册图算法内建到解释器。
- `def graph_symtab_names() -> list[str]` — 返回图算法模块的符号表名列表。

**方法**
- `def __post_init__(self)`
- `def add_edge(self, u: int, v: int, weight: float) -> None`
- `def remove_edge(self, u: int, v: int) -> None`
- `def has_edge(self, u: int, v: int) -> bool`
- `def neighbors(self, u: int) -> List[int]`
- `def degree(self, u: int) -> int`
- `def edges(self) -> List[Tuple[int, int, float]]`
- `def density(self) -> float`
- `def __repr__(self) -> str`

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _齐次变换矩阵(旋转角, 平移_x, 平移_y, 缩放)` — 生成2D齐次变换矩阵。
- `def _投影变换(视锥_near, 视锥_far, fov_deg)` — 透视投影参数。
- `def _裁剪区域(边界_x_min, 边界_y_min, 边界_x_max, 边界_y_max)` — 裁剪区域面积。
- `def _光栅化点数(宽度_px, 高度_px, 抗锯齿等级)` — 光栅化后像素点数。
- `def _颜色空间转换(R, G, B)` — RGB转HSV。
- `def _抗锯齿系数(采样数)` — 抗锯齿质量系数。
- `def _register_graphics(builtins: dict) -> None` — 
- `def _graphics_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _碳足迹估算(能源_kWh, 排放因子)` — 碳足迹（kg CO2）。
- `def _能源效率(输出能量, 输入能量)` — 能源效率（%）。
- `def _太阳能转化率(入射功率_W, 面积_m2, 效率)` — 太阳能转换功率（W）。
- `def _风力发电系数(风速_ms, 叶片半径_m, 空气密度)` — 风力发电功率（W）。P = 0.5·ρ·A·v³·Cp。
- `def _电池循环寿命(容量衰减率_per_cycle, 目标容量保持率)` — 电池循环寿命估算。
- `def _减排量计算(替代能源_kWh, 传统排放因子, 替代排放因子)` — 减排量（kg CO2）。
- `def _register_green_tech(builtins: dict) -> None` — 
- `def _green_tech_symtab_names() -> list[str]` — 

**函数**
- `def _cpu_count() -> int` — 返回逻辑 CPU 核心数。
- `def _memory_info() -> dict` — 返回内存信息（bytes）。
- `def _platform_info() -> str` — 返回当前平台标识。
- `def _arch_info() -> str` — 返回架构信息。
- `def _exec_cmd(cmd: str, timeout: int) -> dict` — 执行系统命令，返回 {code, stdout, stderr}。
- `def _ps() -> list[dict]` — 返回当前进程列表。
- `def _kill(pid: int, sig: int) -> bool` — 向进程发送信号。
- `def _wait(pid: int, timeout: int) -> bool` — 等待进程结束。
- `def _socket_send(host: str, port: int, data: str, timeout: int) -> dict` — 发送 TCP 数据，返回 {sent, response}。
- `def _socket_recv(host: str, port: int, timeout: int) -> str` — 连接并接收数据（HTTP GET 场景）。
- `def _http_get(url: str, timeout: int) -> dict` — HTTP GET 请求，返回 {status, body}。
- `def _dns_resolve(hostname: str) -> list[str]` — DNS 解析，返回 IP 列表。
- `def _ping(host: str, count: int) -> dict` — Ping 测试。
- `def _gpio_init(pin: int, mode: str) -> None` — 初始化 GPIO 引脚（仿真）。mode: 'in' | 'out' | 'pwm'。
- `def _gpio_set(pin: int, value: int) -> None` — 设置 GPIO 引脚输出。
- `def _gpio_get(pin: int) -> int` — 读取 GPIO 引脚输入。
- `def _gpio_pwm(pin: int, duty: float, freq: int) -> None` — 设置 PWM 输出（仿真）。duty: 0-100。
- `def _gpio_cleanup(pin: int) -> None` — 清理 GPIO 引脚。
- `def _list_dir(path: str) -> list[str]` — 列出目录内容。
- `def _mkdir(path: str, recursive: bool) -> bool` — 创建目录。
- `def _file_exists(path: str) -> bool` — 检查文件是否存在。
- `def _file_size(path: str) -> int` — 返回文件大小（字节）。
- `def _env_get(name: str, default: str) -> str` — 读取环境变量。
- `def _env_set(name: str, value: str) -> None` — 设置环境变量。
- `def _register_hardware(builtins: dict) -> None` — 将硬件控制内建注册到解释器。
- `def _register_hardware_symtab_names() -> list[str]` — 返回硬件域所有内建名。
- `def _curry2(fn)` — 两参 Python 函数 → 柯里化 f(a)(b)。
- `def _curry3(fn)` — 三参 → 柯里化 f(a)(b)(c)。

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _信号频率分析(采样率, 窗口长度_ms)` — FFT频率分辨率（Hz）。
- `def _协议解析率(总消息数, 成功解析数)` — 协议消息解析成功率。
- `def _固件完整性校验(固件大小_MB, 校验和匹配)` — 固件完整性评估。
- `def _逆向复杂度(代码行数, 函数数, 调用图复杂度)` — 逆向工程复杂度估算。
- `def _时钟频率估算(周期_ns)` — 从周期估算时钟频率（MHz）。
- `def _功耗分析(电压_V, 电流_mA, 占空比)` — 平均功耗（mW）。
- `def _register_hardware_reverse(builtins: dict) -> None` — 
- `def _hardware_reverse_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _Amdahl加速比(串行比例, 核心数)` — Amdahl定律加速比 S = 1/(s + p/n)。
- `def _并行效率(加速比, 核心数)` — 并行效率 E = S/n。
- `def _通信延迟估算(消息数, 消息大小_bytes, 带宽_Gbps)` — 通信总延迟（ms）。
- `def _负载均衡度(任务列表)` — 负载均衡度（标准差/均值）。越低越均衡。
- `def _内存带宽利用率(访问次数, 数据量_GB, 时间_s)` — 内存带宽利用率（%）。
- `def _浮点运算峰值(核心数, 频率_GHz, FLOPS_per_cycle)` — 峰值浮点运算能力（TFLOPS）。
- `def _register_hpc(builtins: dict) -> None` — 
- `def _hpc_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _MQTT消息大小估算(主题长度, 载荷字节, QoS等级)` — MQTT消息总大小（字节）。
- `def _传感器覆盖半径(发射功率_dBm, 灵敏度_dBm, 频率_GHz)` — 自由空间路径损耗模型估算覆盖半径（米）。
- `def _边缘延迟计算(任务数, 每任务计算_ms, 网络延迟_ms)` — 边缘计算总延迟。
- `def _设备在线率(总设备数, 离线设备数, 监控周期_h)` — 设备在线率估算。
- `def _数据聚合效率(源数据量_MB, 聚合后量_MB, 压缩算法)` — 数据聚合效率。
- `def _功耗预算(组件列表)` — 功耗预算计算（W）。组件列表: [(功耗_W, 占空比), ...]。
- `def _register_iot_hardware(builtins: dict) -> None` — 
- `def _iot_hardware_symtab_names() -> list[str]` — 

**函数**
- `def syscall_num(syscall_name: str) -> int` — 返回系统调用号（基于 Linux x86 32-bit 约定）。

常用 syscall 号：
    write=4,
- `def syscall_entry_size(syscall_count: int) -> int` — 系统调用表入口总大小（字节）。

每项 8 字节（32 位系统）：跳转目标(4B) + padding(4B)
- `def syscall_latency_ns(kernel_mode: bool) -> float` — 系统调用开销（纳秒估算）。

kernel_mode=True: 纯内核路径（无用户态切换）
kernel_mode=F
- `def pcb_size(num_registers: int, stack_size: int) -> int` — 计算 PCB 结构体大小（字节）。

典型 x86 32-bit PCB 组成：
    - 进程状态 (4B)
   
- `def pcb_alloc_cost(num_tasks: int, pcb_size: int) -> int` — 分配 num_tasks 个 PCB 所需的总内存（字节）。
- `def pcb_context_switch_cycles(num_registers: int) -> int` — 上下文切换开销（时钟周期估算）。

包括：保存当前寄存器 → 切换页表 → 加载新寄存器
- `def page_table_entries(page_size: int, memory_size: int, page_table_entries_per_table: int) -> int` — 计算需要的页表项总数（含多级页表）。

x86 32-bit 使用两级页表：
    - PML4 不存在（32-bit
- `def page_table_overhead_bytes(page_size: int, memory_size: int) -> int` — 页表结构本身占用的内存（字节）。

每级页表固定 4KB（1024 项 × 4B）。
- `def linear_address(page_table: int, page_offset: int, page_size: int) -> int` — 物理地址 = page_table * page_size + page_offset。
- `def virtual_to_physical(vaddr: int, page_size: int) -> tuple[int, int]` — 虚拟地址 → (页号, 页内偏移)。
- `def interrupt_latency_us(irq_number: int, pic_master: bool) -> float` — 中断响应延迟估算（微秒）。

IRQ0-7: 主 PIC（8259A）
IRQ8-15: 从 PIC（级联）
- `def exception_vector(vector: int) -> str` — 返回异常向量名称。
- `def scheduler_tick_latency_us(tsc_mhz: int) -> float` — 调度器时钟滴答开销（微秒）。

TSC (Time Stamp Counter) 频率 MHz。
- `def context_switch_overhead_us(tsc_mhz: int, num_regs: int) -> float` — 上下文切换总开销（微秒）。

包括：TLB flush + 寄存器保存/加载 + 栈切换 + 页表切换
- `def throughput_max_tasks(num_tasks: int, switch_us: float) -> float` — 最大吞吐任务数估算（基于上下文切换开销）。
- `def kernel_mem_layout(text_size: int, data_size: int, bss_size: int, stack_size: int, page_size: int) -> dict` — 计算内核内存布局各段地址（x86 32-bit 传统布局）。

返回 dict: {段名: (起始地址, 大小)}
- `def total_kernel_mem(layout: dict) -> int` — 计算内核总内存占用（字节）。
- `def _register_kernel_builtins(builtins: dict) -> None` — 将 kernel math 函数注册到解释器 builtins。
- `def kernel_symtab_names() -> list[str]` — 返回 kernel math 模块的符号表名列表。

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _轴设_扭转切应力(T, d)` — τ = T / Wt = 16T / (πd³)，T 单位 N·mm，d mm，返回 MPa。
- `def _轴设_弯曲正应力(M, d)` — σ = M / W = 32M / (πd³)，M 单位 N·mm，d mm，返回 MPa。
- `def _轴设_弯扭当量弯矩(M, T, alpha)` — Me = √(M² + (α·T)²)，α 为折合系数（不变 0.3，脉动 0.6）。
- `def _轴设_按扭转初估直径(P, n, tau_allow)` — 按扭转强度初估轴径：d ≥ ∛(9.55e6·P / (0.2·τ·n))。
P kW，n r/min，tau_allo
- `def _轴设_平键挤压强度(T, d, h, L)` — σp = 4T / (d·h·L)，T N·mm，d,h,L mm → 返回 MPa。
- `def _轴设_平键剪切强度(T, d, b, L)` — τ = 2T / (d·b·L)，T N·mm，d,b,L mm → 返回 MPa。
- `def _轴设_查平键规格(d)` — 按轴径 d（mm）查平键规格 b×h（GB/T 1095 简化）。
返回 (b, h, t1, t2)，未匹配时返回 (
- `def _轴设_花键挤压强度(T, z, h, L, Dm)` — 矩形花键挤压强度（简化）：σp = 8T / (z·h·L·Dm)，T N·mm，其他 mm，返回 MPa。
- `def _轴设_过盈配合扭矩(p, d, L, mu)` — 过盈配合传递摩擦扭矩 T = π·p·d·L·μ·d/2。
p 配合压强 MPa，d 配合直径 mm，L 配合长度 mm
- `def _轴承_额定寿命_转(C, P, epsilon)` — L10 = (C/P)^ε × 10⁶ 转，C P 同单位（kN 或 N），ε=3球/10/3滚子。
- `def _轴承_额定寿命_小时(C, P, n, epsilon)` — Lh = (10⁶ / (60n)) × (C/P)^ε（小时），n r/min。
- `def _轴承_当量动载荷(Fr, Fa, X, Y, fp)` — P = fp·(X·Fr + Y·Fa)，fp 载荷系数（平稳1/中冲击1.2/重冲击1.5）。
- `def _轴承_静载校核(P0, C0, S0)` — 静强度判定：P0 ≤ C0/S0 时返回 True 满足，否则 False。
- `def _轴承_附加轴向力(Fr, e)` — 角接触轴承附加轴向力简化 Fa = e·Fr（e 查简化值，7000C 取 0.68）。
- `def _轴承_寿命系数(n, epsilon)` — fn = (33.3 / n)^(1/ε)，n r/min，对应 C 单位 kN 时寿命速度系数。
- `def _轴承_查深沟球_62xx(model)` — 查 62xx 深沟球轴承参数 (C, C0, d, D, B)，不存在返回 (0,0,0,0,0)。
- `def _轴承_查深沟球_63xx(model)` — 查 63xx 深沟球轴承参数 (C, C0, d, D, B)。
- `def _齿轮_分度圆直径(m, z)` — d = m·z，模数 m mm，齿数 z → 直径 mm。
- `def _齿轮_中心距(m, z1, z2)` — a = m(z1+z2)/2。
- `def _齿轮_传动比_齿数(z1, z2)` — i = z2/z1（n1/n2）。
- `def _齿轮_齿宽(phi_d, d1)` — b = φd·d1，φd 齿宽系数（0.3~1.2 常用），d1 小轮分度圆 mm → b mm。
- `def _齿轮_圆周速度(d1_mm, n1)` — v = π·d1·n1 / (60×1000)，d1 mm，n1 r/min → v m/s。
- `def _齿轮_转矩_Pn(P_kW, n1)` — T1 = 9.55e6·P/n1，P kW，n r/min → T1 N·mm。
- `def _齿轮_弯曲应力(T1, phi_d, m, z1, YFa, YSa, K)` — 直齿圆柱齿轮弯曲疲劳 σF = 2KT1·YFa·YSa / (φd·b·m²·z1)
简化：b = φd·d1 = φ
- `def _齿轮_许用接触应力(Hlim, Zn, S_Hmin)` — σHP = Hlim·Zn/S_Hmin，Zn 寿命系数。返回 MPa。
- `def _齿轮_许用弯曲应力(Flim, Yn, S_Fmin, Yx)` — σFP = Flim·Yn·Yx / S_Fmin，Yn 寿命系数、Yx 尺寸系数。返回 MPa。
- `def _弹簧_旋绕比(D, d)` — C = D/d（旋绕比/弹簧指数），推荐 4~16。
- `def _弹簧_曲度系数(C)` — K = (4C-1)/(4C-4) + 0.615/C（Wahl 系数）。
- `def _弹簧_切应力(F, D, d, K)` — τ = 8·K·D·F / (π·d³)。F N, D mm, d mm, K 曲度系数 → τ MPa。
- `def _弹簧_变形量(F, D, d, n, G)` — λ = 8·F·D³·n / (G·d⁴)，n 有效圈数，G 切变模量 MPa。返回 mm。
- `def _弹簧_刚度(D, d, n, G)` — k = G·d⁴ / (8·D³·n) N/mm。
- `def _弹簧_总圈数(n, end_type)` — n1 = n + 2（YI型/Y型两端各磨平一圈），n1=n+1.5（YII型）。
- `def _弹簧_自由高度(n, p, d, end_type)` — H0 = n·p + 2·d（Y 型端部不磨）；
H0 = n·p + 1.5·d（YI 型两端磨平）。
- `def _弹簧_螺旋角(p, D)` — α = arctan(p / (π·D))，返回弧度。
- `def _联接_受拉螺栓强度(F_pre, A_s, sigma_s, S)` — 受拉预紧螺栓：σ = 1.3·F_pre / A_s ≤ σs/S。
F_pre N，A_s 应力面积 mm²，σs M
- `def _联接_受剪螺栓强度(F, m, d0)` — τ = 4F / (m·π·d0²)，m 剪切面数，d0 螺栓杆 mm，F N → τ MPa。
- `def _联接_螺栓挤压强度(F, d0, sum_t)` — σp = F / (d0·Σt)，Σt 同方向被连接件最小厚度和 mm → σp MPa。
- `def _联接_螺栓总拉力(F_work, F_double_prime)` — F' = F_work + F''，轴向工作载荷 F_work + 残余预紧 F'' → 总拉力 N。
- `def _联接_残余预紧力(F_work, chi)` — F'' = χ·F_work，χ 变载 0.6~1.0 / 静载 0.2~0.6。
- `def _联接_查螺栓强度(grade)` — 查螺栓强度等级：(σs, σb) MPa。
- `def _联接_查螺栓规格(spec)` — 查螺栓规格 (d, d1, d0, p) mm，spec 如 "M12"。
- `def _联接_销剪切强度(F, d, z)` — τ = 4F / (π·d²·z)，d mm, z 剪切面数 → τ MPa。
- `def _公差_IT值(grade_str, D_mm)` — IT 标准公差值（μm）：i = 0.45·∛D + 0.001·D，IT = a·i，
a 为等级系数；D_mm 为公
- `def _公差_基孔制配合(IT_grade, fit_type, D_mm)` — 基孔制配合（简化）：返回 (ES, EI, es, ei) μm。
fit_type:
  "H7h6" → 间隙（常用
- `def _公差_尺寸链封闭环(plus_rings, minus_rings)` — 极值法：A0 = Σ(增环) - Σ(减环)。
plus_rings/minus_rings 为 list 或单值。
- `def _公差_尺寸链封闭环公差(tolerances)` — 极值法：T0 = ΣTi。tolerances 为组成环公差 list。
- `def _公差_查表面粗糙度(method)` — 按加工方法查典型 Ra（μm）。
- `def _公差_威布尔可靠度(t, eta, beta)` — R(t) = exp(-(t/η)^β)，η 特征寿命，β 形状参数。
- `def _register_mech_design(builtins: dict) -> None` — 将机械设计内建注册到解释器 builtins。
- `def _curry6(func)` — 
- `def _curry7(func)` — 
- `def _mech_design_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 两参 Python 函数 → 柯里化 f(a)(b)。
- `def _curry3(func)` — 三参 → 柯里化 f(a)(b)(c)。
- `def _curry4(func)` — 四参 → 柯里化 f(a)(b)(c)(d)。
- `def _匀速_位移(v, t)` — 
- `def _匀速_速度(s, t)` — 
- `def _匀速_时间(s, v)` — 
- `def _匀变速_位移(v0, a, t)` — 
- `def _匀变速_末速度(v0, a, t)` — 
- `def _匀变速_位移由速度(v0, v, a)` — 
- `def _自由落体_位移(t, g_val)` — 
- `def _自由落体_末速度(t, g_val)` — 
- `def _平抛_落地时间(h, g_val)` — 
- `def _平抛_射程(v0, h, g_val)` — 
- `def _斜抛_射程(v0, theta_rad, g_val)` — 
- `def _斜抛_最大高度(v0, theta_rad, g_val)` — 
- `def _斜抛_飞行时间(v0, theta_rad, g_val)` — 
- `def _圆周_线速度(omega, r)` — 
- `def _圆周_角速度(T)` — 
- `def _圆周_向心加速度_v(v, r)` — 
- `def _圆周_向心加速度_w(omega, r)` — 
- `def _圆周_周期(v, r)` — 
- `def _拉压_应力(F, A)` — 
- `def _拉压_应变(dL, L)` — 
- `def _拉压_胡克(E, epsilon)` — 
- `def _拉压_变形(F, L, E, A)` — 
- `def _安全系数(sigma_s, sigma)` — 
- `def _剪切_剪应力(F, A)` — 
- `def _剪切_剪应变(delta, L)` — 
- `def _剪切_胡克(G, gamma)` — 
- `def _截面_矩形惯性矩(b, h)` — 
- `def _截面_圆形惯性矩(d)` — 
- `def _截面_圆环惯性矩(D, d)` — 
- `def _弯曲_正应力(M, y, I)` — 
- `def _弯曲_最大正应力(M, y_max, I)` — 
- `def _扭转_极惯性矩(d)` — 
- `def _扭转_空心极惯性矩(D, d)` — 
- `def _扭转_剪应力(T, r, Ip)` — 
- `def _扭转_最大剪应力(T, d, Ip)` — 
- `def _扭转_扭转角(T, L, G, Ip)` — 
- `def _抗弯截面模量_圆形(d)` — 
- `def _抗弯截面模量_矩形(b, h)` — 
- `def _材料_属性(mat_name: str, prop_key: str) -> float` — 读取材料属性。未命中抛 KeyError 转 MathaRuntimeError（注册侧处理）。
- `def _register_mechanics(builtins: dict) -> None` — 将机械领域内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（mathlib 
- `def _mechanics_symtab_names() -> list[str]` — 返回机械领域所有内建名（用于语义分析注册，避免报未定义）。

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _药代_消除速率常数(t_half)` — ke = ln2 / t½（t½ 单位须与 ke 一致，通常 h⁻¹）。
- `def _药代_半衰期(ke)` — t½ = ln2 / ke。
- `def _药代_一室浓度(C0, ke, t)` — 一室静注模型 C(t) = C0·exp(-ke·t)。
- `def _药代_表观分布容积(Dose, C0)` — Vd = Dose / C0（Dose 单位与 C0 对应）。
- `def _药代_清除率(ke, Vd)` — CL = ke·Vd。
- `def _药代_稳态浓度(Dose, CL, tau)` — 多次给药稳态平均浓度 Css = Dose / (CL·τ)。
- `def _药代_生物利用度(AUC_po, AUC_iv)` — 绝对生物利用度 F = AUC_po / AUC_iv。
- `def _药代_AUC梯形(t1, c1, t2, c2)` — 单区间梯形法 AUC = (c1+c2)/2 · (t2-t1)。
- `def _药代_峰浓度(Dose, Vd, F)` — Cmax = Dose·F / Vd（血管外给药达峰近似）。
- `def _药代_谷浓度(Cmax, ke, tau)` — Cmin = Cmax·exp(-ke·τ)（稳态谷浓度）。
- `def _药代_负荷剂量(Css_target, Vd, F)` — 负荷剂量 LD = Css·Vd / F，使血药浓度迅速达到稳态。
- `def _药效_Emax模型(Emax, EC50, C)` — Emax 模型 E = Emax·C / (EC50 + C)。
- `def _药效_Sigmoid_Emax(Emax, EC50, C, n)` — Sigmoid Emax（Hill）E = Emax·C^n / (EC50^n + C^n)。
- `def _药效_治疗指数(TD50, ED50)` — 治疗指数 TI = TD50 / ED50。
- `def _药效_安全范围(TD1, ED99)` — 安全范围 SR = TD1 / ED99。
- `def _药效_竞争拮抗浓度(dr, Ki)` — 竞争性拮抗所需拮抗剂浓度 [I] = (dr - 1)·Ki。dr 为剂量比。
- `def _药效_部分激动剂活性(Emax_part, Emax_full)` — 部分激动剂内在活性 α = Emax_part / Emax_full。
- `def _药效_量反应斜率(ED16, ED84)` — 由 ED16/ED84 估算 Hill 系数 n = ln9 / ln(ED84/ED16)。
- `def _检验_肌酐清除率(age, weight, SCr, is_female)` — Cockcroft-Gault：CCr = (140-age)·wt / (72·SCr) ·(0.85 if 女)。单
- `def _检验_eGFR_MDRD(SCr, age, is_female, is_black)` — MDRD 简化公式 eGFR = 175·SCr^-1.154·age^-0.203·(0.742 if 女)·(1.2
- `def _检验_阴离子间隙(Na, Cl, HCO3)` — AG = Na - (Cl + HCO3)。单位 mmol/L。
- `def _检验_渗透压(Na, Glu, BUN)` — 血浆渗透压 Osm = 2·Na + Glu/18 + BUN/2.8。Glu/BUN 单位 mg/dL。
- `def _检验_MCV(Hct_pct, RBC_TperL)` — 平均红细胞体积 MCV = Hct(%)·10 / RBC(×10¹²/L)。单位 fL。
- `def _检验_MCH(Hb_gL, RBC_TperL)` — 平均红细胞血红蛋白 MCH = Hb(g/L) / RBC(×10¹²/L)。单位 pg。
- `def _检验_MCHC(Hb_gL, Hct_LperL)` — 平均红细胞血红蛋白浓度 MCHC = Hb(g/L) / Hct(L/L)。单位 g/L。
- `def _检验_校正钙(Ca, Alb)` — 白蛋白校正血钙 Ca_corr = Ca + 0.8·(4 - Alb)。单位 mg/dL。
- `def _检验_白蛋白校正AG(AG, Alb)` — 白蛋白校正阴离子间隙 AG_corr = AG + 2.5·(4 - Alb)。
- `def _检验_Na校正血糖(Na, Glu)` — 高血糖校正血钠 Na_corr = Na + 1.6·(Glu - 100)/100。Glu 单位 mg/dL。
- `def _检验_游离水清除率(V_urine, Osm_u, Osm_p)` — 游离水清除率 CH2O = V·(1 - Osm_u/Osm_p)。V 单位 mL/h。
- `def _检验_校正沉降率(ESR, age, is_female)` — 校正红细胞沉降率（简化年龄法）ESR_corr = ESR - (age - 50)/2（女性再加 5）。
- `def _影像_当量剂量(D_Gy, wR)` — 当量剂量 H = D·wR。单位 Sv。
- `def _影像_有效剂量(H_Sv, wT)` — 有效剂量 E = H·wT。单位 Sv。
- `def _影像_半值层(mu)` — 半值层 HVL = ln2 / μ。
- `def _影像_放射性活度(A0, half_life, t)` — A(t) = A0·exp(-λ·t)，λ = ln2/t½。
- `def _影像_CT值(mu, mu_water)` — CT 值（Hounsfield）HU = 1000·(μ - μ水)/μ水。
- `def _影像_RBE(LET)` — 相对生物效应 RBE（LET≤100 线性近似 1+LET/100，>100 饱和至 2.0）。LET 单位 keV/μ
- `def _影像_BED(n, d, alpha_beta)` — 生物等效剂量 BED = n·d·(1 + d/(α/β))。n 分次数，d 单次剂量 Gy。
- `def _影像_EQD2(total_dose, d, alpha_beta)` — 2Gy 等效剂量 EQD2 = total·(d + α/β)/(2 + α/β)。
- `def _影像_平方反比(I0, d1, d2)` — 平方反比律 I2 = I1·(d1/d2)²。
- `def _影像_半价层衰减(n)` — 经 n 个半值层后剩余强度分数 = (1/2)^n。
- `def _理疗_HRmax(age)` — 最大心率 HRmax = 220 - age。
- `def _理疗_心率储备(age, HRrest, intensity)` — Karvonen 公式 THR = (HRmax - HRrest)·intensity + HRrest。
- `def _理疗_METs_VO2(METs)` — METs → VO2 = 3.5·METs（mL/kg/min）。
- `def _理疗_Borg_VO2(borg)` — Borg 6-20 量表 → VO2max 估算（简化）VO2max ≈ (Borg - 4)·3.5 + 3.5。
- `def _理疗_代谢热量(METs, weight_kg, min)` — 运动热量消耗 kcal = METs·3.5·wt(kg)·min / 200。
- `def _理疗_超声衰减(I0, alpha, depth)` — 超声强度衰减 I(d) = I0·exp(-α·d)。α 单位 1/(cm·MHz)，d 单位 cm·MHz。
- `def _理疗_等长力矩(F, r)` — 等长收缩力矩 τ = F·r。F 单位 N，r 单位 m，结果 N·m。
- `def _理疗_步态速度(stride_length, cadence)` — 步态速度 v = 步长·步频。步长 m，步频 步/min → m/min。
- `def _理疗_关节活动度(current_ROM, normal_ROM)` — 关节活动度百分比 ROM% = 当前/正常·100。
- `def _理疗_FIM分级(score)` — FIM 总分分级（18-126）→ 独立性等级字符串。
- `def _register_medical(builtins: dict) -> None` — 将医疗与医药理疗子领域内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（bi
- `def _medical_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _中医_脉率分类(bpm)` — 按脉率（次/分）分类：<60 迟脉，<=90 平脉，<=120 数脉，>120 疾脉。
- `def _中医_骨度分寸(landmark)` — 按体表标志查骨度分寸（同身寸），未收录返回 0.0。
- `def _中医_针刺深度(depth_total, ratio)` — 按总深度与比例计算进针深度 = depth_total * ratio。
- `def _中医_艾灸温度(T_source, d, d_ref)` — 艾灸温度按距离平方反比估算 T = T_source * (d_ref/d)^2，d<=0 时返回源温。
- `def _中医_拔罐负压(P_atm, r_inner_cm)` — 拔罐负压吸力：delta_P=P_atm*0.4, A=pi*(r/100)^2, F=delta_P*1000*A。
- `def _中医_子午流注(hour)` — 按时辰（24h）映射子午流注经脉，返回 'X时·Y经'。
- `def _中医_体质指数(BMI, bias_score)` — 校正体质指数 = BMI * (1 + bias_score/100)。
- `def _中医_经穴间距(landmark, frac1, frac2)` — 按骨度分寸与两穴比例计算间距 = |frac1-frac2| * total。
- `def _中医_九针选用(depth_mm)` — 按针刺深度选用九针：<3 鍉针，<10 毫针，<20 长针，否则大针。
- `def _中医_耳穴分区(zone_code)` — 按耳穴分区代码查区域名称。
- `def _中药_古今剂量(liang, dynasty)` — 按朝代折算两→克 = liang * factor。
- `def _中药_煎煮浓缩(herb_g, final_mL)` — 煎煮后浓度 = herb_g / final_mL。
- `def _中药_君臣佐使(jun, chen, zuo, shi)` — 君药占比 = jun / (jun+chen+zuo+shi)。
- `def _中药_儿童剂量Clark(adult_dose, child_kg, adult_kg)` — Clark 公式：儿童剂量 = adult_dose * child_kg / adult_kg。
- `def _中药_儿童剂量Young(adult_dose, age)` — Young 公式：儿童剂量 = adult_dose * age / (age + 12)。
- `def _中药_煎药水量(herb_g, ratio)` — 煎药加水体积 = herb_g * ratio。
- `def _中药_浸泡时间(herb_type)` — 按药材类型查浸泡时间（分钟）。
- `def _中药_毒性限量(herb, dose_g)` — 判定毒性药材是否超限，返回 (是否超限, 极量)。
- `def _中药_折干率(fresh_g, dry_ratio)` — 折干重 = fresh_g * dry_ratio。
- `def _中药_配伍七情(herb1_type, herb2_type)` — 按两药配伍类型查七情关系，未匹配返回单行。
- `def _中药_分次服用(total_mL, times_per_day)` — 单次服用量 = total_mL / times_per_day。
- `def _手术_缝合线张力(usp)` — 按 USP 号查缝合线抗拉张力（N）。
- `def _手术_缝合线直径(usp)` — 按 USP 号查缝合线直径（mm）。
- `def _手术_缝合针弯矩(F, r_needle_mm)` — 缝合针弯矩 = F * r_needle_mm。
- `def _手术_止血带压力(limb_circumference_cm, SBP_mmHg)` — 止血带压力 = SBP + 50 + (周径/10)*10。
- `def _手术_螺钉拔出力(d_mm, bone_density_gcm3)` — 螺钉拔出力 = 50 * d * 骨密度。
- `def _手术_克氏针选择(bone_type)` — 按骨类型选克氏针直径（mm）。
- `def _手术_放大镜视野(magnification, FOV_at_1x_mm)` — 放大镜视野 = FOV_at_1x / magnification。
- `def _手术_牵引重量(body_kg, fraction)` — 牵引重量 = body_kg * fraction。
- `def _手术_钳夹力(F_handle, lever_ratio)` — 钳夹力 = F_handle * lever_ratio。
- `def _手术_电刀功率(tissue_type)` — 按组织类型查电刀功率（W）。
- `def _设备_输液滴速(mL_per_h, drop_factor)` — 输液滴速（滴/分）= mL_per_h * drop_factor / 60。
- `def _设备_注射泵流速(dose_mg, conc_mg_per_mL, duration_h)` — 注射泵流速（mL/h）= dose / (conc * duration)。
- `def _设备_分钟通气量(VT_mL, RR)` — 分钟通气量（mL/min）= VT * RR。
- `def _设备_IE比校验(I_time, E_time)` — I:E 比化简：对吸呼时间各 ×10 取整后用 gcd 化简。
- `def _设备_除颤能量(body_kg, J_per_kg)` — 除颤能量（J）= body_kg * J_per_kg。
- `def _设备_超声穿透深度(freq_MHz)` — 超声穿透深度（cm）≈ 40 / freq_MHz。
- `def _设备_血透超滤率(target_mL, duration_h)` — 血透超滤率（mL/h）= target / duration。
- `def _设备_心电采样率(diagnostic_type)` — 按诊断类型查心电采样率（Hz）。
- `def _设备_输液泵压力限(tube_ID_mm)` — 输液泵压力限 = 100 + tube_ID * 30。
- `def _设备_心率报警限(age)` — 按年龄查心率报警上下限，返回 (下限, 上限)。
- `def _康复_接受腔压力(weight_N, contact_area_cm2)` — 接受腔压力（kPa）= weight / (area*1e-4) / 1000。
- `def _康复_轮椅推进力(weight_kg, slope_deg)` — 轮椅推进力（N）= weight * g * sin(slope)。
- `def _康复_弹簧常数(load_N, deformation_mm)` — 弹簧常数（N/mm）= load / deformation。
- `def _康复_外骨骼力矩(torque_needed, assist_ratio)` — 外骨骼力矩 = torque_needed * assist_ratio。
- `def _康复_关节接触应力(load_N, contact_area_mm2)` — 关节接触应力（MPa）= load / area。
- `def _康复_肌腱张力(area_mm2, UTS_MPa, safety)` — 肌腱张力（N）= area * UTS * safety。
- `def _康复_假体磨损(load_N, cycles, wear_coeff)` — 假体磨损量 = load * cycles * wear_coeff。
- `def _康复_辅助器高度(height_cm, ratio)` — 辅助器高度 = height * ratio。
- `def _康复_压缩袜压力(ankle_pressure, gradient)` — 压缩袜梯度压力 = ankle_pressure * gradient。
- `def _康复_CPM角度增量(target_ROM, days)` — CPM 每日角度增量 = target_ROM / days。
- `def _register_medtools(builtins: dict) -> None` — 将中医/西医器械与设备子领域内建注册到解释器 builtins。
- `def _medtools_symtab_names() -> list[str]` — 

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _渲染帧率估算(三角形数, 像素数, 每三角形耗时_ns)` — 渲染帧率估算（FPS）。
- `def _物理模拟步长(物体数, 约束数, 可用算力_MFlops)` — 物理模拟最大稳定步长（ms）。
- `def _碰撞检测复杂度(物体数)` — O(n²)碰撞检测复杂度估算。
- `def _用户并发数(服务器带宽_Gbps, 每用户带宽_Mbps)` — 最大并发用户数估算。
- `def _资产加载延迟(资产大小_MB, 网络带宽_Mbps, 压缩率)` — 资产加载延迟（ms）。
- `def _网络同步延迟(往返时间_ms, 插值窗口_ms)` — 网络同步后延迟（ms）。
- `def _register_metaverse_arch(builtins: dict) -> None` — 
- `def _metaverse_arch_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _核_质量亏损(Z, N, M_nucleus)` — 
- `def _核_结合能J(delta_m)` — 
- `def _核_结合能MeV(delta_m)` — 
- `def _核_比结合能(delta_m, A)` — 
- `def _核_结合能由U(delta_m_u)` — 
- `def _核_半经验结合能(A, Z)` — Weizsäcker 半经验质量公式，返回结合能（MeV）。
- `def _核_半经验比结合能(A, Z)` — 
- `def _核_α衰变Q(M_parent, M_daughter, M_alpha)` — 
- `def _核_β衰变Q(M_parent, M_daughter)` — 
- `def _衰变_剩余核数(N0, lam, t)` — 
- `def _衰变_活度(lam, N)` — 
- `def _衰变_半衰期(lam)` — 
- `def _衰变_衰变常数(T_half)` — 
- `def _衰变_平均寿命(lam)` — 
- `def _衰变_已衰变数(N0, lam, t)` — 
- `def _衰变_双衰变剩余(N0, lam1, lam2, t)` — 
- `def _衰变_碳14测年(N0, N_now)` — 碳-14年代测定，半衰期 5730 年。返回年代（年）。
- `def _衰变_Ci转Bq(Ci)` — 
- `def _衰变_Bq转Ci(Bq)` — 
- `def _反应_Q值(m_initial, m_final)` — 
- `def _反应_Q值U(m_initial_u, m_final_u)` — 
- `def _反应_阈能(Q_MeV, m_a, m_A)` — Q < 0 时的反应阈能（MeV）。
- `def _反应_裂变能(n, E_per_fission_MeV)` — n 次裂变释放的能量（MeV）。
- `def _反应_聚变能(n, E_per_fusion_MeV)` — n 次聚变释放的能量（MeV）。
- `def _反应_裂变功率(rate, E_per_fission_MeV)` — rate = 裂变率（次/秒），返回功率（W）。
- `def _反应_临界质量估算(rho, sigma_f, nu, v_th)` — 简化的临界质量估算（kg），rho密度, sigma_f裂变截面, nu每次裂变中子数, v_th热中子速度。
- `def _粒子_洛伦兹因子(v)` — 
- `def _粒子_相对论总能量(m, v)` — 
- `def _粒子_相对论动能(m, v)` — 
- `def _粒子_相对论动量(m, v)` — 
- `def _粒子_质能等价(m)` — 
- `def _粒子_质能等价MeV(m)` — 
- `def _粒子_德布罗意波长相对论(m, v)` — 
- `def _粒子_速度由动能(K, m)` — 
- `def _粒子_动量由能量(E_total, m)` — 
- `def _反应堆_有效增殖系数(k_inf, L2, B2)` — 
- `def _反应堆_临界判断(k_eff)` — 
- `def _反应堆_反应性(k_eff)` — 
- `def _反应堆_周期(l_neutron, rho)` — 
- `def _反应堆_慢化比(A)` — 
- `def _反应堆_慢化碰撞数(E0, E_th, A)` — 
- `def _反应堆_吸收剂量(energy, mass)` — 
- `def _反应堆_当量剂量(D, w_R)` — 
- `def _反应堆_半减弱层(mu)` — 
- `def _反应堆_屏蔽强度(I0, mu, x)` — 
- `def _register_nuclear(builtins: dict) -> None` — 将核物理内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（celestial
- `def _nuclear_symtab_names() -> list[str]` — 返回核物理所有内建名（用于语义分析注册）。

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _几何_折射角(n1, theta1, n2)` — 
- `def _几何_全反射角(n1, n2)` — 
- `def _几何_球面镜焦距(R)` — 
- `def _几何_像距(u, f)` — 
- `def _几何_放大率(v, u)` — 
- `def _几何_透镜焦距(n, R1, R2)` — 
- `def _几何_介质光速(n)` — 
- `def _几何_折射率(v)` — 
- `def _几何_透镜组合(f1, f2)` — 
- `def _波动_双缝条纹间距(wavelength, D, d)` — 
- `def _波动_薄膜光程差(n, d)` — 
- `def _波动_单缝暗纹角(wavelength, a, m)` — 
- `def _波动_光栅衍射角(wavelength, d_grating, m)` — 
- `def _波动_光栅分辨本领(m, N)` — 
- `def _波动_马吕斯定律(I0, theta)` — 
- `def _波动_布儒斯特角(n1, n2)` — 
- `def _波动_双缝光程差(d, theta)` — 
- `def _光度_光通量(I)` — 
- `def _光度_照度(I, r)` — 
- `def _光度_斜照度(I, r, theta)` — 
- `def _光度_亮度(I, A)` — 
- `def _光度_光视效能(Phi, P)` — 
- `def _光度_发光强度(Phi, Omega)` — 
- `def _仪器_放大镜(f)` — 
- `def _仪器_显微镜(L, f_o, f_e)` — 
- `def _仪器_望远镜(f_o, f_e)` — 
- `def _仪器_数值孔径(n, alpha)` — 
- `def _仪器_最小分辨角(wavelength, D)` — 
- `def _仪器_分辨极限(wavelength, NA)` — 
- `def _仪器_人眼分辨角()` — 
- `def _色散_柯西折射率(A, B, wavelength)` — 
- `def _色散_色散率(B, wavelength)` — 
- `def _色散_光子能量频率(f)` — 
- `def _色散_光子能量波长(wavelength)` — 
- `def _色散_光子动量(wavelength)` — 
- `def _色散_红移(lam_obs, lam_emit)` — 
- `def _色散_红移速度(z)` — 
- `def _register_optics(builtins: dict) -> None` — 将光学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（acoustics 
- `def _optics_symtab_names() -> list[str]` — 返回光学所有内建名（用于语义分析注册）。

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _进程调度等待时间(进程数, 时间片_ms, 优先级数)` — FIFO/RR调度等待时间估算。
- `def _内存页表开销(内存大小_MB, 页面大小_kb)` — 页表内存开销（KB）。
- `def _文件碎片率(已用块数, 总块数, 连续块数)` — 文件系统碎片率。
- `def _TCP重传率(丢包率, 重传阈值)` — TCP重传率估算。
- `def _DNS查询延迟(服务器距离_km, 解析跳数)` — DNS查询延迟（ms）。光速传输+每跳处理延迟。
- `def _带宽利用率(吞吐量_Mbps, 链路容量_Mbps)` — 带宽利用率（%）。
- `def _register_os_network(builtins: dict) -> None` — 
- `def _os_network_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _波_德布罗意波长(m, v)` — 
- `def _波_德布罗意波长动量(p)` — 
- `def _波_概率密度(psi)` — 
- `def _波_能量由频率(omega)` — 
- `def _波_自由粒子能量(k, m)` — 
- `def _波_动量由波数(k)` — 
- `def _波_波数由动量(p)` — 
- `def _波_动能期望(p, m)` — 
- `def _波_角频率由能量(E)` — 
- `def _不确定_动量不确定度(dx)` — 
- `def _不确定_位置不确定度(dp)` — 
- `def _不确定_能量不确定度(dt)` — 
- `def _不确定_时间不确定度(dE)` — 
- `def _不确定_下限()` — 
- `def _不确定_验证(dx, dp)` — 
- `def _角动_轨道模(l)` — 
- `def _角动_轨道z(ml)` — 
- `def _角动_自旋模(s)` — 
- `def _角动_自旋z(ms)` — 
- `def _角动_总模(j)` — 
- `def _角动_总z(mj)` — 
- `def _角动_磁矩(g, m)` — 
- `def _角动_朗德g(J, L, S)` — 
- `def _势阱_无限深能级(n, m, L)` — 
- `def _势阱_氢原子能级J(n)` — 
- `def _势阱_氢原子能级eV(n)` — 
- `def _势阱_谐振子能级(n, omega)` — 
- `def _势阱_谐振子特征长度(m, omega)` — 
- `def _势阱_谐振子振幅(E, m, omega)` — 
- `def _势阱_玻尔半径()` — 
- `def _势阱_氢原子半径(n)` — 
- `def _势阱_氢原子电离能()` — 
- `def _隧穿_衰减常数(m, V0, E)` — 
- `def _隧穿_WKB概率(kappa, a)` — 
- `def _隧穿_WKB概率直接(m, V0, E, a)` — 
- `def _隧穿_方势垒透射(E, V0, m, a)` — 
- `def _隧穿_光电效应(f, phi)` — 
- `def _隧穿_截止频率(phi)` — 
- `def _隧穿_康普顿波长(m)` — 
- `def _隧穿_康普顿偏移(theta)` — 
- `def _register_quantum(builtins: dict) -> None` — 将量子力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（structur
- `def _quantum_symtab_names() -> list[str]` — 返回量子力学所有内建名（用于语义分析注册）。

**函数**
- `def hadamard()` — Hadamard 门。
- `def pauli_x()` — Pauli-X 门（量子非门）。
- `def pauli_y()` — Pauli-Y 门。
- `def pauli_z()` — Pauli-Z 门。
- `def cnot()` — CNOT 门。
- `def swap_gate()` — SWAP 门。
- `def toffoli()` — Toffoli（CCNOT）门（8x8矩阵）。
- `def bell_state()` — Bell 态 |Phi+> = (|00> + |11>) / sqrt(2)。
- `def ghz_state(n_qubits: int)` — GHZ 态 |0...0> + |1...1>。
- `def qubit_state(theta: float, phi: float)` — 单量子比特状态 |psi> = cos(theta/2)|0> + e^(i*phi)*sin(theta/2)|1>。
- `def grover_iterate(state, oracle: Callable, n_iterations: int)` — Grover 迭代（简化版）。
- `def shor_period_finding(a: int, N: int, max_period: int)` — Shor 算法周期寻找（简化版经典部分）。
- `def quantum_fourier_transform(state)` — 量子傅里叶变换。
- `def circuit_depth(gates: list, n_qubits: int) -> int` — 计算电路深度（简化）。
- `def gate_decompose(cnot_count: int) -> list` — 门分解（CNOT → 基础门）。
- `def _register_quantum_compute(builtins: dict) -> None` — 注册量子计算内建到解释器。
- `def _quantum_compute_symtab_names() -> list[str]` — 

### domains.real_hardware

- [GPIOHardware](`#class-gpiohardware`)
- [SerialDriver](`#class-serialdriver`)
- [I2CDriver](`#class-i2cdriver`)
- [ADCDriver](`#class-adcdriver`)
- [TemperatureSensor](`#class-temperaturesensor`)
- [DistanceSensor](`#class-distancesensor`)
- [StepperMotor](`#class-steppermotor`)
- [ServoMotor](`#class-servomotor`)
- [OLEDDisplay](`#class-oleddisplay`)
- [HardwareDriverRegistry](`#class-hardwaredriverregistry`)

**函数**
- `def gpio(pin: int, mode: str) -> None` — 便捷 GPIO 初始化。
- `def gpio_write(pin: int, value: int) -> None` — 便捷 GPIO 写入。
- `def gpio_read(pin: int) -> int` — 便捷 GPIO 读取。
- `def temperature() -> dict` — 读取温度传感器。
- `def distance() -> float` — 读取距离传感器。
- `def motor_step(steps: int, speed: float) -> None` — 步进电机控制。
- `def servo_angle(angle: float) -> None` — 舵机角度控制。
- `def _register_real_hardware(builtins: dict) -> None` — 将真实硬件驱动注册到解释器。

**方法**
- `def __new__(cls) -> 'GPIOHardware'`
- `def __init__(self) -> None`
- `def setup(self, pin: int, mode: str) -> None`
- `def output(self, pin: int, value: int) -> None`
- `def input(self, pin: int) -> int`
- `def PWM(self, pin: int, frequency: float, duty_cycle: float) -> None`
- `def cleanup(self) -> None`
- `def __init__(self, port: str, baudrate: int) -> None`
- `def write(self, data: bytes) -> int`
- `def read(self, size: int) -> bytes`
- `def readline(self) -> str`
- `def close(self) -> None`
- `def __init__(self, bus: int) -> None`
- `def write_byte(self, addr: int, value: int) -> None`
- `def write_bytes(self, addr: int, data: list[int]) -> None`
- `def read_byte(self, addr: int) -> int`
- `def read_bytes(self, addr: int, length: int) -> list[int]`
- `def scan(self) -> list[int]`
- `def __init__(self, channel: int, ref_voltage: float, bits: int) -> None`
- `def read(self) -> int`
- `def read_voltage(self) -> float`
- `def __init__(self, sensor_type: str) -> None`
- `def read(self) -> dict`
- `def __init__(self, trigger_pin: int, echo_pin: int) -> None`
- `def read(self) -> float`
- `def __init__(self, step_pin: int, dir_pin: int, steps_per_rev: int) -> None`
- `def step(self, steps: int, speed: float) -> None`
- `def rotate_degrees(self, degrees: float, speed: float) -> None`
- `def __init__(self, pin: int, min_pulse: float, max_pulse: float) -> None`
- `def set_angle(self, angle: float) -> None`
- `def __init__(self, width: int, height: int) -> None`
- `def clear(self) -> None`
- `def text(self, x: int, y: int, string: str, size: int) -> None`
- `def pixel(self, x: int, y: int, on: bool) -> None`
- `def register(cls, name: str, driver_class: type) -> None`
- `def create(cls, name: str, **kwargs) -> Any`
- `def list_drivers(cls) -> list[str]`

### domains.registry

- [DomainMeta](`#class-domainmeta`)
- [DomainRegistry](`#class-domainregistry`)

**函数**
- `def get_registry() -> DomainRegistry` — 获取全局领域注册表。
- `def domain_compile(source: str, target: str, domain: Optional[str]) -> str` — 使用指定领域编译源码。

**方法**
- `def load_module(self) -> Any`
- `def get_builtins(self) -> dict`
- `def __init__(self) -> None`
- `def register(self, domain_key: str, meta: Optional[DomainMeta]) -> bool`
- `def unregister(self, domain_key: str) -> bool`
- `def get(self, domain_key: str) -> Optional[DomainMeta]`
- `def load(self, domain_key: str) -> Optional[Any]`
- `def get_builtins(self, domain_key: str) -> dict`
- `def list_domains(self, category: Optional[str]) -> list[dict]`
- `def add_callback(self, event: str, callback: Callable) -> None`
- `def fire(self, event: str, **kwargs) -> None`
- `def get_stats(self) -> dict`

### domains.software_app

- [HTTPClient](`#class-httpclient`)
- [SQLiteDatabase](`#class-sqlitedatabase`)

**函数**
- `def http_get(url: str, headers: Optional[dict]) -> dict` — 
- `def http_post(url: str, body: dict, headers: Optional[dict]) -> dict` — 
- `def http_put(url: str, body: dict, headers: Optional[dict]) -> dict` — 
- `def http_delete(url: str, headers: Optional[dict]) -> dict` — 
- `def _get_db(name: str, path: str) -> SQLiteDatabase` — 
- `def db_query(table: str, conditions: Optional[dict], columns: Optional[list[str]], limit: int) -> list[dict]` — 查询数据库表。
- `def db_insert(table: str, data: dict) -> int` — 插入数据。
- `def db_update(table: str, row_id: int, updates: dict) -> bool` — 更新数据。
- `def db_delete(table: str, row_id: int) -> bool` — 删除数据。
- `def _b64url_encode(data: bytes) -> str` — 
- `def _b64url_decode(s: str) -> bytes` — 
- `def jwt_encode(payload: dict, secret: str, exp_hours: float) -> str` — 编码 JWT。
- `def jwt_decode(token: str, secret: str) -> Optional[dict]` — 解码并验证 JWT。
- `def bcrypt_hash(password: str, rounds: int) -> str` — PBKDF2 密码哈希。
- `def bcrypt_verify(password: str, hash_value: str) -> bool` — 验证密码。
- `def cache_get(key: str) -> Optional[Any]` — 
- `def cache_set(key: str, value: Any, ttl: float) -> None` — 
- `def cache_invalidate(key: str) -> bool` — 
- `def cache_size() -> int` — 
- `def queue_enqueue(item: Any) -> int` — 
- `def queue_dequeue() -> Optional[Any]` — 
- `def queue_size() -> int` — 
- `def _register_software_app(builtins: dict) -> None` — 
- `def _software_app_symtab_names() -> list[str]` — 

**方法**
- `def __init__(self)`
- `def register_mock(self, url: str, response: dict) -> None`
- `def request(self, method: str, url: str, **kwargs) -> dict`
- `def __init__(self, db_path: str)`
- `def execute(self, sql: str, params: tuple) -> list[dict]`
- `def close(self) -> None`

**函数**
- `def _curry1(func)` — 
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _空间索引效率(要素数, 索引深度)` — 空间索引查询效率估算。
- `def _元数据查询延迟(元数据量_MB, 索引大小_MB)` — 元数据查询延迟（ms）。
- `def _地理编码精度(坐标系统, 分辨率_m)` — 地理编码精度（米）。
- `def _坐标变换误差(源精度_m, 目标精度_m, 变换次数)` — 累积坐标变换误差。
- `def _边界框交集(bbox1_xmin, bbox1_ymin, bbox1_xmax, bbox1_ymax, bbox2_xmin, bbox2_ymin, bbox2_xmax, bbox2_ymax)` — 两个边界框是否相交。
- `def _缓冲区分析(中心_x, 中心_y, 半径_m, 目标点_x, 目标点_y)` — 目标点是否在缓冲区内。
- `def _curry8(func)` — 八参数柯里化。
- `def _register_spatial_meta(builtins: dict) -> None` — 
- `def _spatial_meta_symtab_names() -> list[str]` — 

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _速率_最概然速率(m, T)` — 
- `def _速率_平均速率(m, T)` — 
- `def _速率_方均根速率(m, T)` — 
- `def _速率_分布函数(m, T, v)` — 
- `def _速率_理想气体压强(n, T)` — 
- `def _速率_理想气体压力(N, T, V)` — 
- `def _速率_平均自由程(n, sigma)` — 
- `def _速率_碰撞频率(n, sigma, v_bar)` — 
- `def _配分_玻尔兹曼因子(E, T)` — 
- `def _配分_离散配分函数(energies, T)` — energies: 能级列表 [ε1, ε2, ...]
- `def _配分_简并配分函数(levels, T)` — levels: [(ε1, g1), (ε2, g2), ...]
- `def _配分_谐振子配分函数(omega, T)` — 
- `def _配分_转动配分函数(I, T, sigma)` — 
- `def _配分_内能(Z_func, T)` — Z_func: 温度的函数（Python callable），返回 Z。
- `def _配分_熵(Z_func, T)` — Z_func: 温度的函数（Python callable），返回 Z。
- `def _配分_自由能(Z, T)` — 
- `def _配分_自由能压力(F_func, V)` — F_func: 体积的函数（Python callable），返回 F。
- `def _熵_玻尔兹曼熵(W)` — 
- `def _熵_玻尔兹曼熵2(W)` — 
- `def _熵_吉布斯熵(probs)` — probs: 概率列表 [p1, p2, ...]
- `def _熵_萨克尔泰特罗德(N, V, m, U)` — 单原子理想气体熵。
- `def _熵_等温熵变(N, V1, V2)` — 
- `def _熵_理想气体熵变(N, V1, V2, T1, T2)` — 
- `def _熵_焓(U, P, V)` — 
- `def _熵_吉布斯自由能(H, T, S)` — 
- `def _熵_等容热容(N)` — 
- `def _熵_等压热容(N)` — 
- `def _熵_热容比(Cp, Cv)` — 
- `def _统计_费米狄拉克(E, mu, T)` — 
- `def _统计_玻色爱因斯坦(E, mu, T)` — 
- `def _统计_费米能级(m, n)` — 
- `def _统计_费米温度(EF)` — 
- `def _统计_费米速度(EF, m)` — 
- `def _统计_德拜频率(v_s, n)` — 
- `def _统计_德拜温度(omega_D)` — 
- `def _统计_德拜热容低温(N, T, theta_D)` — 
- `def _统计_维恩位移(T)` — 
- `def _统计_黑体辐射功率(T)` — 
- `def _统计_光子气体内能密度(T)` — 
- `def _涨落_能量涨落(T, Cv)` — 
- `def _涨落_粒子数涨落(T, dN_dmu)` — 
- `def _涨落_相对能量涨落(T, Cv, E_avg)` — 
- `def _涨落_布朗位移(D, t)` — 
- `def _涨落_爱因斯坦扩散系数(T, eta, r)` — 
- `def _涨落_密度涨落(T, kappa_T, V)` — 
- `def _涨落_体积涨落(T, V, kappa_T)` — 
- `def _涨落_温度涨落(T, Cv)` — 
- `def _curry5(func)` — 
- `def _register_statmech(builtins: dict) -> None` — 将统计力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（nuclear 
- `def _curry5(func)` — 
- `def _statmech_symtab_names() -> list[str]` — 返回统计力学所有内建名（用于语义分析注册）。

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _应力_主应力1(sigma_x, sigma_y, tau_xy)` — 
- `def _应力_主应力2(sigma_x, sigma_y, tau_xy)` — 
- `def _应力_最大剪应力(sigma_x, sigma_y, tau_xy)` — 
- `def _应力_主应力方向(sigma_x, sigma_y, tau_xy)` — 
- `def _应力_截面正应力(sigma_x, sigma_y, tau_xy, theta)` — 
- `def _应力_截面剪应力(sigma_x, sigma_y, tau_xy, theta)` — 
- `def _应力_广义胡克应变(sigma_x, sigma_y, sigma_z, E, nu, axis)` — axis: 'x'=0, 'y'=1, 'z'=2
- `def _应力_vonMises(s1, s2, s3)` — 
- `def _应力_Tresca(s1, s2, s3)` — 
- `def _应力_体积应变(sigma_x, sigma_y, sigma_z, E, nu)` — 
- `def _梁_简支均布弯矩(q, L)` — 
- `def _梁_简支均布挠度(q, L, E, I)` — 
- `def _梁_简支均布转角(q, L, E, I)` — 
- `def _梁_简支集中弯矩(P, L)` — 
- `def _梁_简支集中挠度(P, L, E, I)` — 
- `def _梁_简支集中转角(P, L, E, I)` — 
- `def _梁_悬臂均布弯矩(q, L)` — 
- `def _梁_悬臂均布挠度(q, L, E, I)` — 
- `def _梁_悬臂集中弯矩(P, L)` — 
- `def _梁_悬臂集中挠度(P, L, E, I)` — 
- `def _梁_悬臂集中转角(P, L, E, I)` — 
- `def _梁_弯曲正应力(M, y, I)` — 
- `def _梁_矩形剪应力(V, A)` — 
- `def _压杆_欧拉临界力(E, I, mu, L)` — 
- `def _压杆_欧拉临界应力(E, lam)` — 
- `def _压杆_长细比(mu, L, r)` — 
- `def _压杆_回转半径(I, A)` — 
- `def _压杆_临界长细比(E, sigma_p)` — 
- `def _压杆_安全压力(P_cr, n)` — 
- `def _压杆_长细比几何(mu, L, I, A)` — 
- `def _桁架_简支均布反力(q, L)` — 
- `def _桁架_简支集中反力(P)` — 
- `def _桁架_悬臂均布反力(q, L)` — 
- `def _桁架_悬臂集中反力(P)` — 
- `def _桁架_杆件内力(F, theta)` — 
- `def _桁架_超静定次数(m, r, j)` — 
- `def _桁架_合力(Fx_list, Fy_list)` — 
- `def _能量_轴向应变能(N, L, E, A)` — 
- `def _能量_弯曲应变能(M, L, E, I)` — 
- `def _能量_剪切应变能(V, L, G, A)` — 
- `def _能量_动荷系数(h, delta_st)` — 
- `def _能量_应变能密度(sigma, E)` — 
- `def _能量_扭转应变能(T, L, G, Ip)` — 
- `def _能量_总应变能(U_axial, U_bending, U_shear)` — 
- `def _register_structural(builtins: dict) -> None` — 将结构力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（optics 之
- `def _structural_symtab_names() -> list[str]` — 返回结构力学所有内建名（用于语义分析注册）。

**函数**
- `def _curry2(func)` — 
- `def _curry3(func)` — 
- `def _curry4(func)` — 
- `def _curry5(func)` — 
- `def _热_c2k(T_C)` — 
- `def _热_k2c(T_K)` — 
- `def _热_气压(n, T, V)` — 
- `def _热_气体体积(n, T, p)` — 
- `def _热_摩尔数(p, V, T)` — 
- `def _热_质量转摩尔(m, M)` — 
- `def _热_玻意耳压强(p1, V1, V2)` — 
- `def _热_查理体积(V1, T1, T2)` — 
- `def _热_盖吕萨克压强(p1, T1, T2)` — 
- `def _热_道尔顿分压(p_list)` — 
- `def _过程_等温功(n, T, V1, V2)` — 
- `def _过程_等压功(p, V1, V2)` — 
- `def _过程_等容功(*_)` — 
- `def _过程_绝热功(p1, V1, p2, V2, gamma)` — 
- `def _过程_绝热压强(p1, V1, V2, gamma)` — 
- `def _过程_绝热温度(T1, V1, V2, gamma)` — 
- `def _过程_第一定律(Q, W)` — 
- `def _过程_内能变化(n, Cv, dT)` — 
- `def _传热_热传导(k, A, T2, T1, d)` — 
- `def _传热_热对流(h, A, T_s, T_f)` — 
- `def _传热_热辐射(epsilon, A, T_hot, T_cold)` — 
- `def _传热_热阻(d, k, A)` — 
- `def _传热_串联传热(dT, R_list)` — 
- `def _传热_串联热阻(R_list)` — 
- `def _传热_并联热阻(R_list)` — 
- `def _热机_卡诺效率(T_h, T_c)` — 
- `def _热机_奥托效率(r, gamma)` — 
- `def _热机_效率(Q_h, Q_c)` — 
- `def _热机_制冷系数(T_c, T_h)` — 
- `def _热机_热泵系数(T_h, T_c)` — 
- `def _热机_卡诺功(Q_h, T_h, T_c)` — 
- `def _相变_显热(m, c, dT)` — 
- `def _相变_潜热(m, L)` — 
- `def _相变_线膨胀(alpha, L0, dT)` — 
- `def _相变_体膨胀(beta, V0, dT)` — 
- `def _相变_热容(m, c)` — 
- `def _相变_比热容比(cp, cv)` — 
- `def _相变_理想气体内能(f, n, T)` — 
- `def _相变_迈耶关系(cp, cv)` — 
- `def _register_thermo(builtins: dict) -> None` — 将热力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（fluid 之后）
- `def _thermo_symtab_names() -> list[str]` — 返回热力学所有内建名（用于语义分析注册）。

### driver_matrix

- [DriverCategory](`#class-drivercategory`)
- [DriverSubType](`#class-driversubtype`)
- [DriverSpec](`#class-driverspec`)
- [MathaDriverMatrix](`#class-mathadrivermatrix`)

**方法**
- `def to_dict(self) -> dict`
- `def __init__(self)`
- `def _init_all_drivers(self)`
- `def _register(self, spec: DriverSpec)`
- `def _update_stats(self)`
- `def list_all(self) -> List[dict]`
- `def list_by_category(self, category: DriverCategory) -> List[dict]`
- `def list_by_architecture(self, arch: Architecture) -> List[dict]`
- `def get_core_drivers(self) -> List[dict]`
- `def get(self, category: DriverCategory, sub_type: DriverSubType) -> Optional[dict]`
- `def generate_code(self, category: DriverCategory, sub_type: DriverSubType, target_arch: Architecture, target_lang: str) -> dict`
- `def _generate_driver_code(self, spec: DriverSpec, arch: Architecture, lang: str) -> str`
- `def _gen_python_driver(self, spec: DriverSpec) -> str`
- `def _gen_c_driver(self, spec: DriverSpec, arch: Architecture) -> str`
- `def _gen_matha_driver(self, spec: DriverSpec) -> str`
- `def generate_all_python(self) -> List[dict]`
- `def generate_all_c(self) -> List[dict]`
- `def get_stats(self) -> dict`
- `def print_matrix(self)`

### enhanced_intent

- [EnhancedIntentParser](`#class-enhancedintentparser`)
- [ErrorAggregator](`#class-erroraggregator`)
- [IntentParseContext](`#class-intentparsecontext`)

**函数**
- `def parse_intent_safe(text: str, target_lang: str) -> Result` — 安全解析：返回 Result[Intent, MathaError]。
- `def explain_intent_safe(text: str, target_lang: str) -> str` — 安全解释：返回自然语言报告（含错误信息）。
- `def execute_intent(text: str, target_lang: str) -> Result` — 解析 + 执行 + 返回结果。

**方法**
- `def parse(self, text: str, target_lang: str) -> Result`
- `def _classify_with_recovery(self, text: str, intent: Intent) -> Result`
- `def _extract_params_with_recovery(self, text: str, intent: Intent) -> Result`
- `def _generate_code(self, intent: Intent) -> Result`
- `def _get_candidates(self, text: str) -> list[str]`
- `def execute_and_verify(self, intent: Intent) -> Result`
- `def explain_with_errors(self, text: str) -> str`
- `def _format_error_report(self, error: MathaError, original_text: str) -> str`
- `def __init__(self)`
- `def add(self, error: MathaError) -> 'ErrorAggregator'`
- `def add_result(self, result: Result) -> 'ErrorAggregator'`
- `def aggregate(self) -> CompositeError`
- `def report(self) -> str`
- `def __init__(self, text: str)`
- `def parse(self) -> Result`
- `def recover(self) -> Optional[MathaError]`
- `def report(self) -> str`
- `def __enter__(self)`
- `def __exit__(self, exc_type, exc_val, exc_tb)`

### errors

- [ErrorStage](`#class-errorstage`)
- [ErrorSeverity](`#class-errorseverity`)
- [MathaError](`#class-mathaerror`)
- [ParseError](`#class-parseerror`)
- [ClassifyError](`#class-classifyerror`)
- [ParamExtractError](`#class-paramextracterror`)
- [CodeGenError](`#class-codegenerror`)
- [ExecError](`#class-execerror`)
- [CompositeError](`#class-compositeerror`)
- [RecoveryStrategy](`#class-recoverystrategy`)

**函数**
- `def ok_with_context(value, **ctx) -> Result` — 创建带上下文的 Ok。
- `def err_with_stage(message: str, stage: ErrorStage, **kwargs) -> Result` — 创建带阶段的 Err。
- `def map_errors(fn, *args, **kwargs) -> Result` — 执行函数并映射错误到结构化错误。
- `def _recover_classify(error: MathaError) -> Optional[MathaError]` — 意图分类失败的恢复策略。
- `def _recover_params(error: MathaError) -> Optional[MathaError]` — 参数提取失败的恢复策略。
- `def _recover_codegen(error: MathaError) -> Optional[MathaError]` — 代码生成失败的恢复策略。
- `def _recover_exec(error: MathaError) -> Optional[MathaError]` — 执行失败的恢复策略。
- `def classify_error(msg: str, candidates: list) -> MathaError` — 
- `def parse_error(msg: str, line: int, col: int, expected: str)` — 
- `def param_error(msg: str, expected: str, actual: str)` — 
- `def codegen_error(msg: str, lang: str)` — 
- `def exec_error(msg: str, exc: Exception)` — 
- `def composite_error(msg: str, errors: list)` — 

**方法**
- `def __lt__(self, other: 'ErrorSeverity') -> bool`
- `def __gt__(self, other: 'ErrorSeverity') -> bool`
- `def __init__(self, message: str, stage: ErrorStage, severity: ErrorSeverity, code: str, suggestions: list, context: dict, cause: 'MathaError', stack: list, children: list)`
- `def with_cause(self, cause: 'MathaError') -> 'MathaError'`
- `def add_child(self, child: 'MathaError') -> 'MathaError'`
- `def add_suggestion(self, suggestion: str) -> 'MathaError'`
- `def report(self, indent: int) -> str`
- `def suggestions_text(self) -> str`
- `def to_result(self) -> Err`
- `def _format_trace(self) -> str`
- `def __init__(self, message: str, line: int, col: int, expected: str)`
- `def add_suggestion(self, suggestion: str) -> 'ParseError'`
- `def __init__(self, message: str, candidates: list[str])`
- `def __init__(self, message: str, expected_type: str, actual_type: str)`
- `def __init__(self, message: str, target_lang: str)`
- `def __init__(self, message: str, exception: Exception)`
- `def __init__(self, message: str, errors: list[MathaError])`
- `def recover(self) -> Optional[MathaError]`
- `def register(cls, stage: ErrorStage)`
- `def try_recover(cls, error: MathaError) -> Optional[MathaError]`
- `def get_strategy_count(cls, stage: ErrorStage) -> int`
- `def _mark_builtin(cls, fn) -> None`
- `def clear(cls) -> None`

### exceptions

- [MathaError](`#class-mathaerror`)
- [MatrixError](`#class-matrixerror`)
- [DimensionMismatchError](`#class-dimensionmismatcherror`)
- [SingularMatrixError](`#class-singularmatrixerror`)
- [SymbolicError](`#class-symbolicerror`)
- [ImportError](`#class-importerror`)
- [ConfigurationError](`#class-configurationerror`)

**函数**
- `def safe_execute(func: Callable, *args, **kwargs) -> Any` — 安全执行函数，捕获并记录异常。

Args:
    func: 要执行的函数
    *args: 位置参数
    
- `def with_error_handling(func: Callable) -> Callable` — 装饰器：为函数添加错误处理。

用法:
    @with_error_handling
    def my_func
- `def validate_matrix_shape(rows: int, cols: int) -> None` — 验证矩阵维度。

Args:
    rows: 行数
    cols: 列数

Raises:
    ValueE
- `def validate_numeric(value: Any, name: str) -> None` — 验证数值类型。

Args:
    value: 要验证的值
    name: 参数名称（用于错误消息）

Rais
- `def check_dependency(module_name: str, feature: str) -> bool` — 检查依赖是否可用。

Args:
    module_name: 模块名称
    feature: 功能描述

Re

### ffi

- [FFIImport](`#class-ffiimport`)
- [FFICall](`#class-fficall`)
- [MathaFFIBridge](`#class-mathaffibridge`)

**函数**
- `def get_ffi() -> MathaFFIBridge` — 获取 FFI 桥接器单例。
- `def ffi_register(name: str, func: Callable, **kwargs)` — 便捷注册函数。
- `def ffi_call(name: str, *args) -> Any` — 便捷调用函数。
- `def my_add(a, b)` — 

**方法**
- `def __init__(self)`
- `def _register_math_builtins(self)`
- `def register(self, name: str, func: Callable, params: List[str], doc: str)`
- `def unregister(self, name: str)`
- `def import_module(self, module_name: str, alias: str, exclude: List[str], include: List[str])`
- `def import_numpy(self)`
- `def call(self, name: str, *args) -> Any`
- `def _resolve(self, name: str) -> Optional[Callable]`
- `def get_function(self, name: str) -> Optional[Callable]`
- `def is_registered(self, name: str) -> bool`
- `def list_functions(self, source: str) -> List[dict]`
- `def get_stats(self) -> dict`
- `def clear_log(self)`

### firewall

- [MathaFirewallException](`#class-mathafirewallexception`)
- [FirewallLevel](`#class-firewalllevel`)
- [FirewallRule](`#class-firewallrule`)
- [FirewallPolicy](`#class-firewallpolicy`)
- [MathaFirewall](`#class-mathafirewall`)

**函数**
- `def _install_firewall_builtins(interp: Interpreter) -> None` — 将防火墙能力注册为 Matha 内建函数，使 Matha 代码可以：
  - 检查当前防火墙级别
  - 查询被拦截的内
- `def configure_logging(level: int) -> None` — 配置防火墙日志输出到 stderr。

用法：
    from src.firewall import configu
- `def sandbox_interpret(source: str) -> tuple[list, list[str]]` — 沙箱模式执行 Matha 源码。
- `def restricted_interpret(source: str) -> tuple[list, list[str]]` — 受限模式执行 Matha 源码。
- `def full_interpret(source: str) -> tuple[list, list[str]]` — 全功能模式执行 Matha 源码。

**方法**
- `def __init__(self, builtin_name: str, level: str, reason: str)`
- `def applies_to(self, level: str) -> bool`
- `def __init__(self, level: FirewallLevel)`
- `def _load_defaults(self, level: FirewallLevel) -> None`
- `def deny(self, builtin: str, reason: str, tags: Optional[list[str]]) -> 'FirewallPolicy'`
- `def allow(self, builtin: str, reason: str, tags: Optional[list[str]]) -> 'FirewallPolicy'`
- `def is_blocked(self, builtin_name: str) -> tuple[bool, str]`
- `def list_blocked(self) -> list[str]`
- `def to_matha_rule_text(self) -> str`
- `def __init__(self, level: FirewallLevel, policy: Optional[FirewallPolicy])`
- `def _wrap_interp_call(self) -> None`
- `def run(self, program: ast.Program) -> tuple[list, list[str]]`
- `def interpret(self, source: str) -> tuple[list, list[str]]`
- `def call(self, name: str, *args) -> object`
- `def run_interactive(self, code: str) -> dict`
- `def set_level(self, level: FirewallLevel) -> None`
- `def add_rule(self, rule: FirewallRule) -> None`
- `def get_status(self) -> dict`
- `def reset(self) -> None`

### foreign

- [ForeignRunner](`#class-foreignrunner`)
- [CompareResult](`#class-compareresult`)
- [DualComparator](`#class-dualcomparator`)

**函数**
- `def _values_equal(a: Any, b: Any) -> bool` — 对比 Matha 值与外部值是否相等（容忍类型差异）。

- 数值：直接 == （int/float 互通）
- 字符串
- `def compare_upgrade(parent: Interpreter, matha_src: str, matha_func: str, foreign_lang: str, foreign_code: str, test_cases: list, extra_verify) -> UpgradeResult` — 双实现对比驱动的自我升级。

流程：
  1. 创建沙箱（克隆本体）
  2. 在沙箱中加载 Matha 源码（试运行）

**方法**
- `def eval(self, lang: str, code: str, inputs: Optional[dict]) -> Any`
- `def call(self, lang: str, code: str, func_name: str, args: list) -> Any`
- `def _eval_python(self, code: str, inputs: Optional[dict]) -> Any`
- `def _call_python(self, code: str, func_name: str, args: list) -> Any`
- `def _eval_subprocess(self, lang: str, code: str, inputs: Optional[dict]) -> Any`
- `def _call_subprocess(self, lang: str, code: str, func_name: str, args: list) -> Any`
- `def _build_call_wrapper(self, lang: str, code: str, func_name: str, args: list) -> str`
- `def _ext(lang: str) -> str`
- `def _parse_output(out: str) -> Any`
- `def as_dict(self) -> dict`
- `def __init__(self, interp: Interpreter, matha_func: str, foreign_lang: str, foreign_code: str)`
- `def compare(self, test_cases: list) -> CompareResult`
- `def _foreign_func_name(self) -> str`

### growth

- [Extension](`#class-extension`)
- [ExtensionRegistry](`#class-extensionregistry`)
- [DiagnosticResult](`#class-diagnosticresult`)
- [SelfDiagnostic](`#class-selfdiagnostic`)
- [Modification](`#class-modification`)
- [SelfModifier](`#class-selfmodifier`)
- [GrowthState](`#class-growthstate`)
- [GrowthLoop](`#class-growthloop`)

**函数**
- `def create_growth_system() -> GrowthLoop` — 创建成长系统。
- `def grow(source: str, verbose: bool) -> dict` — 快捷函数：运行成长循环。

**方法**
- `def load(self) -> Any`
- `def __init__(self) -> None`
- `def register(self, ext: Extension) -> None`
- `def unregister(self, name: str) -> bool`
- `def get(self, name: str) -> Optional[Extension]`
- `def get_instance(self, name: str) -> Any`
- `def list_extensions(self, kind: Optional[str]) -> list[dict]`
- `def add_callback(self, event: str, callback: Callable) -> None`
- `def fire(self, event: str, **kwargs) -> None`
- `def __init__(self, registry: ExtensionRegistry) -> None`
- `def diagnose(self, source: str) -> list[DiagnosticResult]`
- `def _check_syntax(self, source: str) -> list[DiagnosticResult]`
- `def _check_performance(self, source: str) -> list[DiagnosticResult]`
- `def _check_quality(self, source: str) -> list[DiagnosticResult]`
- `def _check_extensions(self) -> list[DiagnosticResult]`
- `def get_summary(self) -> dict`
- `def __init__(self, registry: ExtensionRegistry, diagnostic: SelfDiagnostic) -> None`
- `def propose_fix(self, diagnostic: DiagnosticResult) -> Optional[Modification]`
- `def _propose_enable_pass(self, message: str) -> Optional[Modification]`
- `def apply(self, modification: Modification) -> bool`
- `def rollback(self, modification: Modification) -> None`
- `def _sandbox_validate(self, modification: Modification) -> bool`
- `def _execute_modification(self, modification: Modification) -> None`
- `def get_stats(self) -> dict`
- `def __init__(self, registry: ExtensionRegistry) -> None`
- `def run(self, source: str, max_iterations: Optional[int]) -> dict`
- `def enable_pass(self, pass_name: str) -> bool`
- `def add_pass(self, module: str, class_name: str, description: str) -> bool`
- `def get_state(self) -> dict`

### growth_engine

- [Severity](`#class-severity`)
- [DefectCategory](`#class-defectcategory`)
- [AuditPhase](`#class-auditphase`)
- [RemediationAction](`#class-remediationaction`)
- [Defect](`#class-defect`)
- [ResourceEntry](`#class-resourceentry`)
- [GrowthReport](`#class-growthreport`)
- [GrowthEngine](`#class-growthengine`)

**函数**
- `def create_growth_engine(assistant) -> GrowthEngine` — 创建成长引擎实例。
- `def run_growth_cycle(assistant, max_iterations: int) -> dict` — 快捷入口：运行一次成长循环。

**方法**
- `def __init__(self, assistant)`
- `def _build_resource_spec(self) -> list[dict]`
- `def _check_keywords(self, kwargs: dict) -> tuple[bool, str]`
- `def _check_variations(self, kwargs: dict) -> tuple[bool, str]`
- `def _check_rules(self, kwargs: dict) -> tuple[bool, str]`
- `def _check_concepts(self, kwargs: dict) -> tuple[bool, str]`
- `def identify_concept_gaps(self) -> list[str]`
- `def _check_growth(self, kwargs: dict) -> tuple[bool, str]`
- `def _check_security(self, kwargs: dict) -> tuple[bool, str]`
- `def _check_firewall(self, kwargs: dict) -> tuple[bool, str]`
- `def audit_resources(self) -> list[ResourceEntry]`
- `def _add_defect(self, category: DefectCategory, severity: Severity, message: str, source: str) -> Defect`
- `def _resolve_defect(self, defect_id: str, resolved_by: str, patch_code: str, action: RemediationAction) -> None`
- `def get_defects(self, status: str, severity: Severity) -> list[Defect]`
- `def get_defect_stats(self) -> dict`
- `def link_defects(self, id1: str, id2: str) -> bool`
- `def auto_link_related(self, defect: Defect, all_defects: list[Defect]) -> list[str]`
- `def balance_defects(self, defect: Defect) -> list[str]`
- `def self_diagnose(self) -> list[Defect]`
- `def diagnose_intent_parser(self) -> None`
- `def diagnose_interpreter(self) -> None`
- `def diagnose_firewall(self) -> None`
- `def diagnose_security(self) -> None`
- `def diagnose_growth(self) -> None`
- `def diagnose_cross_function(self) -> None`
- `def web_search(self, query: str) -> Optional[str]`
- `def search_and_patch(self, defect: Defect) -> Optional[str]`
- `def _generate_patch_from_search(self, search_result: str, defect: Defect) -> str`
- `def generate_patch(self, defect: Defect) -> Optional[str]`
- `def auto_expand_concepts(self, max_new: int) -> int`
- `def auto_expand_keywords(self) -> int`
- `def run_upgrade_pipeline(self, patch_code: str, verify_fn) -> bool`
- `def _sandbox_verify(self, patch_code: str) -> bool`
- `def _apply_patch(self, patch_code: str) -> bool`
- `def _rollback_patch(self) -> None`
- `def _verify_after_patch(self) -> bool`
- `def auto_remediate(self, defect: Defect) -> bool`
- `def auto_remediate_by_message(self, message: str, category: str) -> bool`
- `def run_growth_cycle(self, max_iterations: int) -> GrowthReport`
- `def get_growth_stats(self) -> dict`
- `def trigger_growth(self) -> dict`

### hal

- [Platform](`#class-platform`)
- [GPIOInterface](`#class-gpiointerface`)
- [WindowsGPIO](`#class-windowsgpio`)
- [LinuxARMGPIO](`#class-linuxarmgpio`)
- [SerialDriver](`#class-serialdriver`)
- [I2CDriver](`#class-i2cdriver`)
- [ADCDriver](`#class-adcdriver`)
- [TemperatureSensor](`#class-temperaturesensor`)
- [DistanceSensor](`#class-distancesensor`)
- [GPUSupport](`#class-gpusupport`)
- [HALConfig](`#class-halconfig`)
- [HardwareAbstractionLayer](`#class-hardwareabstractionlayer`)

**函数**
- `def detect_platform() -> Platform` — 
- `def hal() -> HardwareAbstractionLayer` — 获取 HAL 单例。
- `def register_hardware_builtins(builtins: dict) -> None` — 将 HAL 函数注册为 Matha 内建。

**方法**
- `def setup(self, pin: int, mode: str) -> None`
- `def output(self, pin: int, value: int) -> None`
- `def input(self, pin: int) -> int`
- `def PWM(self, pin: int, frequency: float, duty: float) -> None`
- `def cleanup(self) -> None`
- `def __init__(self) -> None`
- `def setup(self, pin: int, mode: str) -> None`
- `def output(self, pin: int, value: int) -> None`
- `def input(self, pin: int) -> int`
- `def PWM(self, pin: int, frequency: float, duty: float) -> None`
- `def cleanup(self) -> None`
- `def __init__(self) -> None`
- `def _try_import(self) -> None`
- `def setup(self, pin: int, mode: str) -> None`
- `def output(self, pin: int, value: int) -> None`
- `def input(self, pin: int) -> int`
- `def PWM(self, pin: int, frequency: float, duty: float) -> None`
- `def cleanup(self) -> None`
- `def __init__(self, port: str, baudrate: int) -> None`
- `def _try_open(self) -> None`
- `def write(self, data: bytes) -> int`
- `def read(self, size: int) -> bytes`
- `def readline(self) -> str`
- `def close(self) -> None`
- `def __init__(self, bus: int) -> None`
- `def write_byte(self, addr: int, value: int) -> None`
- `def read_byte(self, addr: int) -> int`
- `def scan(self) -> list[int]`
- `def close(self) -> None`
- `def __init__(self, channel: int, ref_voltage: float, bits: int) -> None`
- `def read(self) -> int`
- `def read_voltage(self) -> float`
- `def __init__(self, sensor_type: str) -> None`
- `def read(self) -> dict`
- `def __init__(self, trigger_pin: int, echo_pin: int) -> None`
- `def read(self) -> float`
- `def __init__(self) -> None`
- `def _try_detect(self) -> None`
- `def available(self) -> bool`
- `def devices(self) -> list[dict]`
- `def to_gpu(self, array: list) -> Any`
- `def to_cpu(self, gpu_array: Any) -> list`
- `def __init__(self, config: Optional[HALConfig]) -> None`
- `def _init_drivers(self) -> None`
- `def gpio_setup(self, pin: int, mode: str) -> None`
- `def gpio_write(self, pin: int, value: int) -> None`
- `def gpio_read(self, pin: int) -> int`
- `def gpio_pwm(self, pin: int, freq: float, duty: float) -> None`
- `def gpio_cleanup(self) -> None`
- `def serial_write(self, data: bytes) -> int`
- `def serial_read(self, size: int) -> bytes`
- `def serial_close(self) -> None`
- `def i2c_write(self, addr: int, value: int) -> None`
- `def i2c_read(self, addr: int) -> int`
- `def i2c_scan(self) -> list[int]`
- `def i2c_close(self) -> None`
- `def adc_read(self) -> int`
- `def adc_voltage(self) -> float`
- `def temperature(self) -> dict`
- `def distance(self) -> float`
- `def gpu_available(self) -> bool`
- `def gpu_devices(self) -> list[dict]`
- `def to_gpu(self, data: list) -> Any`
- `def to_cpu(self, gpu_data: Any) -> list`
- `def platform_info(self) -> dict`
- `def cleanup(self) -> None`

**函数**
- `def benchmark_with_logging()` — 基准测试：带日志的高频操作。
- `def benchmark_without_logging()` — 基准测试：不带日志的高频操作。
- `def benchmark_batch_write()` — 批量写入测试。
- `def analyze_bottlenecks()` — 分析性能瓶颈。

### hardware.hal

- [AsyncHALLogger](`#class-asynchallogger`)
- [DeviceType](`#class-devicetype`)
- [DeviceState](`#class-devicestate`)
- [DeviceConfig](`#class-deviceconfig`)
- [IODevice](`#class-iodevice`)
- [ScreenDevice](`#class-screendevice`)
- [KeyboardDevice](`#class-keyboarddevice`)
- [FileDevice](`#class-filedevice`)
- [NetworkDevice](`#class-networkdevice`)
- [SerialDevice](`#class-serialdevice`)
- [GPIODevice](`#class-gpiodevice`)
- [I2CDevice](`#class-i2cdevice`)
- [HardwareAbstractionLayer](`#class-hardwareabstractionlayer`)
- [MathaHardwareOps](`#class-mathahardwareops`)

**函数**
- `def _gpio_writer_worker(worker_id: int, pin: int, iterations: int, result_queue: mp.Queue)` — GPIO 写入 Worker（进程级，模块顶层定义以支持序列化）。
每个进程拥有独立 HAL 实例，无 GIL 竞争。
- `def _gpio_batch_writer_worker(worker_id: int, pins: List[int], iterations: int, result_queue: mp.Queue)` — 批量 GPIO 写入 Worker（进程级）。
- `def run_multiprocess_stress_test(num_workers: int, pin: int, iterations_per_worker: int, target_frequency: int, use_batch: bool, batch_pins: Optional[List[int]]) -> Dict[str, Any]` — 运行 multiprocessing 压力测试（绕过 GIL）。

Args:
    num_workers: Wor

**方法**
- `def __init__(self, name: str, maxsize: int)`
- `def start(self)`
- `def stop(self)`
- `def _flush_remaining(self)`
- `def _flush_batch(self)`
- `def _write_batch(self, batch: list)`
- `def _adjust_batch_size(self, actual_batch_size: int)`
- `def _should_drop(self) -> bool`
- `def info(self, msg: str, *args)`
- `def debug(self, msg: str, *args)`
- `def warning(self, msg: str, *args)`
- `def error(self, msg: str, *args)`
- `def get_stats(self) -> dict`
- `def __init__(self, config: DeviceConfig)`
- `def read(self) -> Any`
- `def write(self, value: Any) -> bool`
- `def configure(self, **kwargs) -> bool`
- `def online(self) -> bool`
- `def offline(self) -> bool`
- `def reset(self) -> bool`
- `def name(self) -> str`
- `def address(self) -> str`
- `def device_type(self) -> DeviceType`
- `def access_count(self) -> int`
- `def __init__(self)`
- `def _print_to_screen(self, value: Any)`
- `def __init__(self)`
- `def _read_from_keyboard(self) -> str`
- `def __init__(self, base_path: str)`
- `def read(self, path: str) -> Optional[str]`
- `def write(self, content: str, path: str) -> bool`
- `def __init__(self)`
- `def http_get(self, url: str) -> Optional[str]`
- `def http_post(self, url: str, data: dict) -> Optional[str]`
- `def __init__(self, port: str, baudrate: int)`
- `def _open(self)`
- `def _close(self)`
- `def read(self) -> Optional[str]`
- `def write(self, value: str) -> bool`
- `def __del__(self)`
- `def __init__(self, pin: int)`
- `def read(self) -> Optional[bool]`
- `def write(self, value: bool) -> bool`
- `def pwm_write(self, value: float)`
- `def __init__(self, address: int)`
- `def read(self) -> Optional[Dict[str, Any]]`
- `def write(self, value: bytes) -> bool`
- `def __init__(self)`
- `def _register_builtin_devices(self)`
- `def register(self, device: IODevice)`
- `def unregister(self, name: str)`
- `def get(self, name: str) -> Optional[IODevice]`
- `def get_by_address(self, address: str) -> Optional[IODevice]`
- `def list_devices(self) -> List[Dict]`
- `def read(self, device_name: str) -> Optional[Any]`
- `def write(self, device_name: str, value: Any, **kwargs) -> bool`
- `def batch_write(self, operations: List[tuple]) -> List[bool]`
- `def __init__(self, hal: HardwareAbstractionLayer)`
- `def 读取(self, device_name: str, path: str) -> Optional[Any]`
- `def 写入(self, device_name: str, value: Any, path: str) -> bool`
- `def 批量写入(self, operations: List[tuple]) -> List[bool]`
- `def 打开(self, device_name: str) -> bool`
- `def 关闭(self, device_name: str) -> bool`
- `def 列出设备(self) -> List[Dict]`

### hardware.hal_v2

- [SideEffectType](`#class-sideeffecttype`)
- [SideEffect](`#class-sideeffect`)
- [SafeSideEffectEngine](`#class-safesideeffectengine`)
- [MemoryPage](`#class-memorypage`)
- [Pointer](`#class-pointer`)
- [PointerManager](`#class-pointermanager`)
- [Architecture](`#class-architecture`)
- [BareMetalTarget](`#class-baremetaltarget`)
- [ProtocolType](`#class-protocoltype`)
- [ProtocolSpec](`#class-protocolspec`)
- [ProtocolParser](`#class-protocolparser`)
- [DriverKind](`#class-driverkind`)
- [DriverSpec](`#class-driverspec`)
- [DriverGenerator](`#class-drivergenerator`)
- [NativeBackend](`#class-nativebackend`)

**函数**
- `def get_side_effect_engine(mode: str) -> SafeSideEffectEngine` — 
- `def get_pointer_manager(page_count: int) -> PointerManager` — 
- `def get_protocol_parser() -> ProtocolParser` — 
- `def get_driver_generator() -> DriverGenerator` — 
- `def get_native_backend() -> NativeBackend` — 
- `def get_hardware_stats() -> dict` — 获取硬件层全部统计信息。

**方法**
- `def __str__(self)`
- `def __init__(self, mode: str)`
- `def register_func(self, name: str, effect_type: SideEffectType, permission: str) -> None`
- `def check_permission(self, func_name: str, required_perm: str) -> bool`
- `def execute_with_check(self, func: Callable, *args, **kwargs) -> Any`
- `def get_stats(self) -> dict`
- `def _count_by_type(self) -> Dict[str, int]`
- `def clear_audit(self) -> None`
- `def __init__(self, page_id: int, base_addr: int)`
- `def read(self, offset: int, size: int) -> Any`
- `def write(self, offset: int, value: Any, size: int) -> None`
- `def __repr__(self)`
- `def __init__(self, manager: 'PointerManager', addr: int, name: str)`
- `def addr(self) -> int`
- `def name(self) -> str`
- `def get(self, offset: int, size: int) -> Any`
- `def set(self, value: Any, offset: int, size: int) -> None`
- `def plus(self, offset: int) -> 'Pointer'`
- `def deref(self) -> Any`
- `def __repr__(self)`
- `def __eq__(self, other)`
- `def __hash__(self)`
- `def __init__(self, page_count: int)`
- `def alloc(self, size: int, name: str) -> Pointer`
- `def free(self, ptr: Pointer) -> bool`
- `def read(self, addr: int, size: int) -> Any`
- `def write(self, addr: int, value: Any, size: int) -> None`
- `def get_stats(self) -> dict`
- `def __post_init__(self)`
- `def _default_linker_script(self) -> str`
- `def __init__(self)`
- `def parse(self, spec: ProtocolSpec) -> dict`
- `def _parse_uart(self, spec: ProtocolSpec) -> dict`
- `def _parse_spi(self, spec: ProtocolSpec) -> dict`
- `def _parse_i2c(self, spec: ProtocolSpec) -> dict`
- `def _parse_can(self, spec: ProtocolSpec) -> dict`
- `def __init__(self, protocol_parser: ProtocolParser)`
- `def generate(self, spec: DriverSpec) -> dict`
- `def _gen_sensor_driver(self, spec: DriverSpec) -> str`
- `def _gen_actuator_driver(self, spec: DriverSpec) -> str`
- `def _gen_math_driver(self, spec: DriverSpec) -> str`
- `def _gen_comm_driver(self, spec: DriverSpec) -> str`
- `def _gen_generic_driver(self, spec: DriverSpec) -> str`
- `def list_generated(self) -> List[dict]`
- `def __init__(self)`
- `def register_target(self, target: BareMetalTarget) -> None`
- `def compile(self, expr: str, arch: Architecture, func_name: str, target_lang: str) -> str`
- `def _compile_to_c(self, expr: str, func_name: str, target: BareMetalTarget) -> str`
- `def _compile_to_python(self, expr: str, func_name: str) -> str`
- `def _compile_to_asm(self, expr: str, func_name: str, arch: Architecture) -> str`
- `def get_targets(self) -> List[str]`
- `def get_stats(self) -> dict`

### inner_loop

- [LoopState](`#class-loopstate`)
- [InteractionRecord](`#class-interactionrecord`)
- [MathaInnerLoop](`#class-mathainnerloop`)

**函数**
- `def get_inner_loop() -> MathaInnerLoop` — 获取或创建内循环单例。
- `def trigger_inner_loop(verbose: bool) -> dict` — 触发一次完整内循环（便捷入口）。
- `def start_inner_loop(interval: float)` — 启动持续内循环（便捷入口）。
- `def stop_inner_loop()` — 停止持续内循环（便捷入口）。
- `def get_loop_status() -> dict` — 获取内循环状态（便捷入口）。

**方法**
- `def __init__(self)`
- `def init_modules(self)`
- `def on_interaction(self, text: str, result: dict) -> InteractionRecord`
- `def collect_resource_snapshot(self) -> dict`
- `def collect_interaction_summary(self) -> dict`
- `def cognitive_diagnose(self) -> dict`
- `def execute_remediation(self, diagnosis: dict) -> dict`
- `def _cross_module_collaboration(self) -> int`
- `def _handle_error_buffer(self) -> int`
- `def _parse_intent_from_text(self, text: str)`
- `def self_extend_concepts(self) -> int`
- `def self_extend_intents(self) -> int`
- `def self_upgrade_check(self) -> dict`
- `def self_upgrade_apply(self, target_version: str) -> dict`
- `def self_upgrade_rollback(self) -> bool`
- `def self_optimize_performance(self) -> dict`
- `def verify_fixes(self, pre_diagnosis: dict) -> dict`
- `def save_state(self, path: str) -> str`
- `def load_state(self, path: str) -> bool`
- `def run_cycle(self, verbose: bool) -> dict`
- `def trigger_once(self, verbose: bool) -> dict`
- `def start_loop(self, interval: float, verbose: bool)`
- `def _loop_worker(self)`
- `def stop_loop(self)`
- `def get_status(self) -> dict`
- `def get_state(self) -> dict`

### intent.intent_decomposer

- [IntentNodeType](`#class-intentnodetype`)
- [IntentNode](`#class-intentnode`)
- [IntentDecomposer](`#class-intentdecomposer`)

**函数**
- `def _extract_arithmetic(text: str) -> str` — 提取算术表达式。
- `def _extract_math_func(text: str) -> str` — 提取数学函数。
- `def _extract_comparison(text: str) -> str` — 提取比较表达式。
- `def _extract_array_op(text: str) -> str` — 提取数组操作。
- `def _extract_string_op(text: str) -> str` — 提取字符串操作。
- `def _extract_prime_search(text: str) -> str` — 提取素数搜索。
- `def _extract_range(text: str) -> str` — 提取范围。
- `def _extract_conditional(text: str) -> str` — 提取条件表达式。

**方法**
- `def is_complete(self) -> bool`
- `def to_math_code(self) -> str`
- `def to_dict(self) -> Dict`
- `def from_dict(cls, data: Dict) -> 'IntentNode'`
- `def __init__(self, use_llm: bool)`
- `def decompose(self, text: str) -> IntentNode`
- `def _decompose_short(self, text: str) -> IntentNode`
- `def _decompose_medium(self, text: str) -> IntentNode`
- `def _decompose_long(self, text: str) -> IntentNode`
- `def _llm_decompose(self, text: str) -> IntentNode`
- `def _heuristic_long_decompose(self, text: str) -> IntentNode`
- `def _split_by_punctuation(self, text: str) -> List[str]`
- `def _simulate_llm_decomposition(self, text: str) -> IntentNode`
- `def learn(self, input_text: str, math_expr: str, success: bool)`
- `def _load_templates(self) -> Dict`
- `def _extract_pattern(self, text: str) -> str`

### intent.llm_parser

- [IntentType](`#class-intenttype`)
- [Intent](`#class-intent`)
- [LLMIntentParser](`#class-llmintentparser`)

**函数**
- `def parse_intent(text: str, model: str, **kwargs) -> Intent` — 便捷函数：解析自然语言为意图。
- `def explain_intent(text: str, model: str, **kwargs) -> str` — 便捷函数：解释意图。

**方法**
- `def is_valid(self) -> bool`
- `def to_dict(self) -> Dict`
- `def __init__(self, api_key: Optional[str], model: str, base_url: Optional[str], timeout: float, cache_dir: str)`
- `def _get_client(self)`
- `def parse(self, text: str, strict: bool) -> Intent`
- `def _call_llm(self, text: str) -> Intent`
- `def _call_gemini(self, user_prompt: str) -> str`
- `def _parse_llm_response(self, raw_json: str, original_text: str) -> Intent`
- `def _fallback_regex_parse(self, text: str) -> Intent`
- `def _cache_key(self, text: str) -> str`
- `def clear_cache(self)`
- `def get_supported_models(self) -> List[str]`
- `def _get_gemini_client(self)`
- `def _get_chatglm_client(self)`

### intent.mir_generator

- [MIRNodeKind](`#class-mirnodekind`)
- [MIRNode](`#class-mirnode`)
- [MIRGenerator](`#class-mirgenerator`)

**函数**
- `def generate_mir(text: str, intent: Optional[Intent]) -> str` — 便捷函数：生成 MIR 代码。
- `def explain_mir(mir_code: str) -> str` — 解释 MIR 代码的数学含义。

**方法**
- `def to_math_code(self, indent: int) -> str`
- `def __init__(self)`
- `def generate(self, intent: Intent) -> MIRNode`
- `def _generate_from_intent(self, intent: Intent) -> MIRNode`
- `def generate_from_text(self, text: str, intent: Optional[Intent]) -> str`
- `def _cache_key(self, intent: Intent) -> str`
- `def _parse_mir_ast(self, mir_code: str) -> MIRNode`
- `def clear_cache(self)`
- `def get_stats(self) -> Dict`

### intent_parser

- [IntentType](`#class-intenttype`)
- [Intent](`#class-intent`)
- [IntentClassifier](`#class-intentclassifier`)
- [ParamExtractor](`#class-paramextractor`)
- [CodeGenerator](`#class-codegenerator`)
- [IntentParser](`#class-intentparser`)

**函数**
- `def parse_intent(text: str, target_lang: str) -> Intent` — 便捷函数：解析自然语言为计算意图。
- `def explain_intent(text: str, target_lang: str) -> str` — 便捷函数：解析并解释自然语言意图。

**方法**
- `def is_valid(self) -> bool`
- `def classify(self, text: str) -> IntentType`
- `def extract_numbers(self, text: str) -> list[float]`
- `def extract_variables(self, text: str) -> list[str]`
- `def extract_range(self, text: str) -> Optional[tuple[int, int]]`
- `def generate_python(self, intent: Intent) -> str`
- `def _gen_arithmetic(self, intent: Intent, params: dict) -> str`
- `def _gen_string_op(self, intent: Intent, params: dict) -> str`
- `def _gen_array_op(self, intent: Intent, params: dict) -> str`
- `def _gen_math_func(self, intent: Intent, params: dict) -> str`
- `def _gen_conditional(self, intent: Intent, params: dict) -> str`
- `def _gen_loop(self, intent: Intent, params: dict) -> str`
- `def _gen_conversion(self, intent: Intent, params: dict) -> str`
- `def _gen_generic(self, intent: Intent, params: dict) -> str`
- `def generate(self, intent: Intent, target_lang: str) -> str`
- `def _gen_rust(self, intent: Intent) -> str`
- `def _gen_go(self, intent: Intent) -> str`
- `def __init__(self)`
- `def parse(self, text: str, target_lang: str) -> Intent`
- `def _describe_intent(self, itype: IntentType, text: str) -> str`
- `def _calc_confidence(self, intent: Intent, text: str) -> float`
- `def explain(self, intent: Intent) -> str`

### interop

- [PythonInterop](`#class-pythoninterop`)
- [JSInterop](`#class-jsinterop`)
- [CInterop](`#class-cinterop`)
- [IPC](`#class-ipc`)
- [MathaInterop](`#class-mathainterop`)

**函数**
- `def get_interop() -> MathaInterop` — 获取互操作实例。

**方法**
- `def __init__(self, interpreter) -> None`
- `def register_py_func(self, matha_name: str, py_func: Callable) -> None`
- `def call_py_func(self, name: str, *args) -> Any`
- `def register_matha_func(self, name: str, matha_code: str) -> None`
- `def call_matha_func(self, name: str, *args) -> Any`
- `def __init__(self) -> None`
- `def register_js_func(self, name: str, js_code: str) -> None`
- `def call_js_func(self, name: str, *args) -> Any`
- `def call_js_module(self, module: str, func: str, *args) -> Any`
- `def __init__(self) -> None`
- `def load_library(self, name: str, path: str) -> None`
- `def register_func(self, lib_name: str, func_name: str, argtypes: list[type], restype: type) -> None`
- `def call_func(self, name: str, *args) -> Any`
- `def __init__(self) -> None`
- `def send(self, data: Any) -> None`
- `def receive(self, timeout: float) -> Optional[Any]`
- `def set_shared(self, key: str, value: Any) -> None`
- `def get_shared(self, key: str) -> Any`
- `def __init__(self, interpreter) -> None`
- `def register_function(self, name: str, lang: str, func: Callable) -> None`
- `def call_function(self, name: str, lang: str, *args) -> Any`

### interp

- [MathaRuntimeError](`#class-matharuntimeerror`)
- [Interpreter](`#class-interpreter`)

**函数**
- `def _curry_module(n: int, func)` — 模块级柯里化（供 _build_domain_builtins 使用，对应 Interpreter._curry 的静态
- `def _build_domain_builtins() -> dict` — 构建包含所有领域内建符号的 dict（模块级缓存，仅调用一次）。
- `def configure_debug_logging(stream, level: int) -> None` — 装一个 stderr（或指定 stream）handler，开启 matha.interp 详细日志。

幂等：重复调用
- `def _env_debug_flag() -> bool` — 读取 MATHA_DEBUG 环境变量。1/true/yes/on（不区分大小写）为真。
- `def builtin_ord(c: str) -> int` — 
- `def builtin_chr(n: int) -> str` — 
- `def builtin_len(seq) -> int` — 
- `def builtin_get(seq)` — get(seq)(index) → seq[index]
- `def builtin_slice(seq)` — slice(seq)(start)(end) → seq[start:end]
- `def builtin_append(lst)` — append(lst)(elem) → 新列表 lst + [elem]
- `def builtin_list(*args)` — list() → []；list(x) → [x]
- `def builtin_token(ttype)` — token(类型)(文本)(行)(列) -> Token 字典（柯里化四参构造子）。

供函数式词法器组装 Token；
- `def builtin_parse_json(text: str)` — 解析_JSON(json字符串) → 嵌套列表/字典。

把 JSON 数组 [a, b, c] 解析为 Python 
- `def builtin_read_file(path: str) -> str` — 读文件(path) → 文件内容字符串。
- `def builtin_write_file(path: str, content: str) -> None` — 写文件(path, 内容) → 无返回值。
- `def builtin_append_file(path: str, content: str) -> None` — 追加文件(path, 内容) → 无返回值。
- `def builtin_parse_json_file(path: str)` — 读JSON文件(path) → 嵌套列表/字典。
- `def builtin_write_json_file(path: str, data) -> None` — 写JSON文件(path, 数据) → 无返回值。
- `def builtin_字符串截取(s: str, start: int, end: int) -> str` — 截取(文本, 起始[, 结束]) → 子串。
- `def builtin_字符串替换(s: str, old: str, new: str) -> str` — 替换(文本, 旧, 新) → 新字符串。
- `def builtin_字符串查找(s: str, sub: str) -> int` — 查找(文本, 子串) → 索引（未找到返回 -1）。
- `def builtin_字符串分割(s: str, sep: str) -> list` — 分割(文本[, 分隔符]) → 列表。
- `def builtin_字符串拼接(sep: str, *parts) -> str` — 拼接(分隔符, 部分1, 部分2, ...) → 字符串。
- `def builtin_字符串去空白(s: str) -> str` — 去空白(文本) → 去除首尾空白。
- `def builtin_字符串小写(s: str) -> str` — 小写(文本) → 全小写。
- `def builtin_字符串大写(s: str) -> str` — 大写(文本) → 全大写。
- `def builtin_列表映射(lst, fn)` — 映射(列表, 函数) → 新列表。
- `def builtin_列表过滤(lst, fn)` — 过滤(列表, 函数) → 新列表。
- `def builtin_列表反转(lst) -> list` — 反转(列表) → 新列表。
- `def builtin_列表排序(lst, reverse: bool) -> list` — 排序(列表[, 降序]) → 新列表。
- `def builtin_列表展平(nested) -> list` — 展平(嵌套列表) → 一维列表。
- `def builtin_列表求和(lst) -> float` — 求和(列表) → 数值和。
- `def builtin_列表去重(lst) -> list` — 去重(列表) → 去重后列表。
- `def _curry_callable(fn, arity)` — 给一个 Python callable 做柯里化包装，使其匹配 Matha 的单参应用。

对 n 参函数 f(a, b
- `def interpret(source: str, debug: bool | None) -> tuple[list, list[str]]` — 解析并执行 Matha 源码，返回 (outputs, trace)。

debug=None 服从 MATHA_DEB
- `def lexer_bootstrap_interpret(source: str, debug: bool | None) -> list[dict]` — 用 Matha 自举 lexer 的函数式扫描器 tokenize 一段源码字符串。

matha/lexer.math

**方法**
- `def __init__(self, debug: bool | None)`
- `def _log(self, level: int, msg: str) -> None`
- `def _log_enter(self, tag: str, detail: str) -> None`
- `def _log_exit(self, tag: str, result: object) -> None`
- `def _fmt(v: object) -> str`
- `def run(self, program: ast.Program) -> tuple[list, list[str]]`
- `def call(self, name: str, *args) -> object`
- `def _register(self, decl) -> None`
- `def _exec_decl(self, decl) -> None`
- `def _exec_set_up(self, node: ast.SetUp) -> None`
- `def _exec_if_stmt(self, stmt: ast.IfStmt) -> None`
- `def _exec_while_stmt(self, stmt: ast.WhileStmt) -> None`
- `def _exec_for_stmt(self, stmt: ast.ForStmt) -> None`
- `def _exec_match_stmt(self, stmt: ast.MatchStmt) -> None`
- `def _eval_match(self, stmt: ast.MatchStmt) -> object`
- `def _match_pattern(self, pattern, value) -> bool`
- `def _exec_codeblock_or_stmt(self, node) -> None`
- `def _exec_stmt(self, stmt) -> None`
- `def _exec_stmt_or_expr(self, node) -> None`
- `def _exec_gen_stmt(self, stmt: ast.GenStmt) -> None`
- `def _eval(self, expr) -> object`
- `def _eval_variable(self, node: ast.Variable) -> object`
- `def _eval_binary(self, node: ast.BinaryOp) -> object`
- `def _eval_func_app(self, node: ast.FuncApp) -> object`
- `def _apply(self, func, arg) -> object`
- `def _call_func(self, fdef: ast.FuncDef, args: list) -> object`
- `def _call_lambda(self, lam: ast.Lambda, captured: dict, args: list) -> object`
- `def _target_name(self, target) -> str`
- `def _param_name(self, param) -> str`
- `def _curry(n: int, func)`
- `def foreign_eval(self, lang: str, code: str, inputs)`
- `def _b_foreign_eval(self, lang: str, code: str)`
- `def _b_compare_impl(self, matha_func: str, foreign_lang: str, foreign_code: str, test_cases: list) -> bool`
- `def _b_compare_upgrade(self, matha_src: str, matha_func: str, foreign_lang: str, foreign_code: str, test_cases: list) -> list`
- `def probe(self)`
- `def sandbox(self)`
- `def upgrade(self, source: str, verify)`
- `def _install_self_builtins(self) -> None`
- `def _b_probe_state(self, _) -> dict`
- `def _b_func_names(self, _) -> list`
- `def _b_has(self, name: str) -> bool`
- `def _b_dry_run(self, source: str) -> bool`
- `def _b_upgrade(self, source: str) -> list`
- `def _b_auto_debug(self, source: str, max_attempts: int) -> dict`
- `def _b_auto_optimize(self, func_name: str, strategy: str) -> dict`
- `def _b_self_grow(self, source: str, desc: str) -> dict`
- `def _b_library_list(self, _) -> list`
- `def _b_library_read(self, path: str, _) -> str`
- `def _b_library_load(self, path: str, _) -> dict`
- `def _b_library_grow(self, requirement: str, discipline: str, name: str) -> dict`
- `def _b_gen_web(self, spec, out_dir: str) -> dict`
- `def _b_gen_desktop(self, spec, out_dir: str) -> dict`
- `def _b_gen_service(self, spec, out_dir: str) -> dict`
- `def _b_gen_system(self, spec, out_dir: str) -> dict`
- `def _b_gen_game(self, spec, out_dir: str) -> dict`
- `def _b_gen_model3d(self, spec, out_dir: str) -> dict`
- `def _normalize_spec(self, spec)`
- `def _b_build_software(self, requirement: str) -> dict`
- `def _b_export_ast(self, source: str) -> dict`
- `def _b_export_tokens(self, source: str) -> list`
- `def _b_transpile_python(self, source: str) -> str`
- `def _b_transpile_js(self, source: str) -> str`
- `def _b_export_symtab(self, fmt: str) -> str`
- `def _b_net_scan(self, source_ips: list) -> list`
- `def _b_net_detect_virus(self, behavior: str) -> list`
- `def _b_net_quarantine(self, threat_id: str) -> dict`
- `def _b_net_eliminate(self, threat_id: str) -> dict`
- `def _b_net_eliminate_all(self, _) -> dict`
- `def _b_net_fw_rules(self, _) -> list`
- `def _b_net_risk_assess(self, ip: str) -> dict`
- `def _b_net_risk_report(self, _) -> dict`
- `def _b_net_threat_report(self, threat_id: str) -> dict`
- `def _b_net_add_sig(self, name: str, pattern: str, level: str, category: str, description: str) -> bool`
- `def _b_net_list_sigs(self, _) -> list`
- `def _b_net_threat_count(self, _) -> int`
- `def _b_net_eliminated_count(self, _) -> int`
- `def _b_net_fw_list(self, _) -> list`
- `def _b_net_threat_status(self, threat_id: str) -> dict`
- `def _b_net_create_virus(self, name: str, category: str, level: str, behavior: str, payload: str) -> dict`
- `def _b_net_create_virus_batch(self, count: int, categories: list) -> list`
- `def _b_net_analyze_virus(self, virus_id: str) -> dict`
- `def _b_net_patch_vuln(self, vuln_id: str, severity: str) -> dict`
- `def _b_net_simulate_spread(self, virus_id: str, network_size: int) -> dict`
- `def _b_net_neutralize(self, threat_id: str, method: str) -> dict`
- `def _b_net_quarantine_all(self, _) -> dict`
- `def _b_net_virus_library(self, _) -> list`
- `def _b_net_create_scenario(self, name: str, virus_count: int, network_size: int) -> dict`
- `def _b_net_quarantine_log(self, _) -> list`
- `def _b_net_elimination_log(self, _) -> list`

### jupyter.matha_magic

- [MathaMagics](`#class-mathamagics`)

**函数**
- `def load_ipython_extension(ipython)` — IPython 扩展加载入口。
- `def unload_ipython_extension(ipython)` — IPython 扩展卸载入口。

**方法**
- `def __init__(self, shell)`
- `def _init_magics(self)`

### lang_features

- [MathaContextManager](`#class-mathacontextmanager`)
- [MathaGenerator](`#class-mathagenerator`)
- [MathaAsyncFunction](`#class-mathaasyncfunction`)
- [MathaProperty](`#class-mathaproperty`)

**函数**
- `def with_statement(resource_fn: Callable, body_fn: Callable) -> Any` — Matha with 语句实现。
- `def decorator(decorator_fn: Callable) -> Callable` — Matha 装饰器语法糖。
- `def static_method(fn: Callable) -> staticmethod` — Matha @staticmethod 等价物。
- `def class_method(fn: Callable) -> classmethod` — Matha @classmethod 等价物。
- `def property_getter(fn: Callable) -> property` — Matha @property 等价物。
- `def generator(gen_fn: Callable) -> MathaGenerator` — 创建 Matha 生成器。
- `def yield_value(value: Any) -> Any` — Matha yield 语句等价物（在生成器函数内调用）。
- `def async_function(fn: Callable) -> MathaAsyncFunction` — Matha async 函数装饰器。
- `def await_expression(coro: Any) -> Any` — Matha await 表达式等价物。
- `def matha_property(fget, fset, fdel, doc)` — Matha @property 语法。
- `def matha_metaclass(name: str, bases: tuple, namespace: dict) -> type` — Matha 元类工厂。
- `def gc_set_threshold(memcounts: tuple) -> None` — 设置 GC 阈值。
- `def gc_get_threshold() -> tuple` — 
- `def gc_collect() -> int` — 
- `def register_language_features(builtins: dict) -> None` — 将语言特性注册为 Matha 内建。

**方法**
- `def __init__(self, enter_fn: Callable, exit_fn: Callable) -> None`
- `def __enter__(self) -> Any`
- `def __exit__(self, exc_type, exc_val, exc_tb) -> bool`
- `def __init__(self, gen_fn: Callable) -> None`
- `def __iter__(self)`
- `def __next__(self) -> Any`
- `def send(self, value: Any) -> Any`
- `def close(self) -> None`
- `def __init__(self, async_fn: Callable) -> None`
- `def __await__(self)`
- `def __init__(self, fget, fset, fdel, doc)`
- `def __get__(self, obj, objtype)`
- `def __set__(self, obj, value)`
- `def __delete__(self, obj)`
- `def getter(self, fget)`
- `def setter(self, fset)`
- `def deleter(self, fdel)`

### lexer

- [LexerError](`#class-lexererror`)
- [Lexer](`#class-lexer`)

**函数**
- `def is_cjk(ch: str) -> bool` — 判断字符是否为 CJK 表意文字（可用作标识符 / 单位）。
- `def is_greek_letter(ch: str) -> bool` — 判断字符是否为希腊字母（可用作标识符）。
- `def is_ascii_letter(ch: str) -> bool` — 
- `def is_digit(ch: str) -> bool` — 
- `def is_unicode_letter(ch: str) -> bool` — 判断字符是否为 Unicode 字母（覆盖所有语言：ASCII、CJK、日文、阿拉伯、希腊等）。

使用 str.isa
- `def is_unicode_id_continue(ch: str) -> bool` — 判断字符是否可作为标识符续字符（字母/数字/下划线）。
- `def _starts_multi_char_op(source: str, pos: int) -> bool` — 检查 pos 位置是否是多字符运算符的起始。

**方法**
- `def __init__(self, msg: str, line: int, col: int)`
- `def __init__(self, source: str)`
- `def _peek(self, offset: int) -> str`
- `def _advance(self) -> str`
- `def _match(self, text: str) -> bool`
- `def _skip_whitespace_inline(self) -> None`
- `def tokenize(self)`
- `def _skip_comment(self) -> None`
- `def _single_char(self, ch: str) -> Token | None`
- `def _number(self) -> Token`
- `def _radix_literal(self, prefix: str, start_line: int, start_col: int) -> Token`
- `def _string(self) -> Token`
- `def _identifier(self) -> Token`

### library

- [LibraryEntry](`#class-libraryentry`)
- [GrowResult](`#class-growresult`)
- [Library](`#class-library`)

**函数**
- `def get_library() -> Library` — 获取全局资源库单例。

**方法**
- `def as_dict(self) -> dict`
- `def as_dict(self) -> dict`
- `def __init__(self, root: str)`
- `def _scan(self) -> None`
- `def list(self) -> list[dict]`
- `def list_by_discipline(self, discipline: str) -> list[dict]`
- `def has(self, path: str) -> bool`
- `def read(self, path: str) -> Optional[str]`
- `def get_entry(self, path: str) -> Optional[LibraryEntry]`
- `def load(self, path: str, interp: Interpreter) -> dict`
- `def load_discipline(self, discipline: str, interp: Interpreter) -> dict`
- `def grow(self, interp: Interpreter, requirement: str, discipline: str, name: str) -> GrowResult`
- `def _generate_candidate(self, requirement: str, discipline: str, name: str) -> Optional[str]`
- `def is_protected(self, path: str) -> bool`
- `def disciplines(self) -> list[str]`

**函数**
- `def log_matrix_operation(func)` — 装饰器：为矩阵运算添加日志。

用法:
    @log_matrix_operation
    def matrix
- `def log_svd_operation(func)` — 装饰器：为 SVD 分解添加日志。

用法:
    @log_svd_operation
    def svd_de
- `def log_inverse_operation(func)` — 装饰器：为矩阵求逆添加日志。

用法:
    @log_inverse_operation
    def matri
- `def log_cache_operation(cache_name: str, cache: dict, key, result)` — 记录缓存操作日志。

Args:
    cache_name: 缓存名称
    cache: 缓存字典
    ke

### lsp

- [LSPKind](`#class-lspkind`)
- [LSPPosition](`#class-lspposition`)
- [LSPRange](`#class-lsprange`)
- [CompletionItem](`#class-completionitem`)
- [Diagnostic](`#class-diagnostic`)
- [SymbolRegistry](`#class-symbolregistry`)
- [MathaLSP](`#class-mathalsp`)

**方法**
- `def to_dict(self) -> Dict`
- `def to_dict(self) -> Dict`
- `def to_dict(self) -> Dict`
- `def to_dict(self) -> Dict`
- `def __init__(self)`
- `def _register_builtins(self)`
- `def _register_matha_keywords(self)`
- `def register(self, name: str, kind: LSPKind, detail: str, documentation: str)`
- `def get(self, name: str) -> Optional[Dict]`
- `def get_all(self) -> Dict[str, Dict]`
- `def __init__(self)`
- `def complete(self, source: str, position: Tuple[int, int], context: Optional[Dict]) -> List[CompletionItem]`
- `def hover(self, source: str, position: Tuple[int, int]) -> Optional[Dict]`
- `def diagnostics(self, source: str, filepath: str) -> List[Diagnostic]`
- `def find_definition(self, source: str, position: Tuple[int, int]) -> Optional[Dict]`
- `def generate_readme(self, source: str) -> str`

### math_driver

- [HardwareType](`#class-hardwaretype`)
- [HardwareSpec](`#class-hardwarespec`)
- [MathDriver](`#class-mathdriver`)
- [LinearAlgebraDriver](`#class-linearalgebradriver`)
- [CalculusDriver](`#class-calculusdriver`)
- [SignalDriver](`#class-signaldriver`)
- [GeometryDriver](`#class-geometrydriver`)
- [OptimizationDriver](`#class-optimizationdriver`)
- [MathDriverManager](`#class-mathdrivermanager`)

**函数**
- `def get_driver_manager() -> MathDriverManager` — 

**方法**
- `def register_op(self, name: str, func: Callable)`
- `def execute(self, op_name: str, *args, **kwargs) -> Any`
- `def get_stats(self) -> dict`
- `def __init__(self)`
- `def _register_builtin_ops(self)`
- `def _dot(a: list, b: list) -> float`
- `def _cross(a: list, b: list) -> List[float]`
- `def _mat_mul(a: list, b: list) -> List[List[float]]`
- `def _mat_add(a: list, b: list) -> List[List[float]]`
- `def _mat_transpose(m: list) -> List[List[float]]`
- `def _mat_det(m: list) -> float`
- `def _mat_inv(m: list) -> Optional[List[List[float]]]`
- `def _eigenvalues(m: list) -> List[float]`
- `def _norm(v: list, p: int) -> float`
- `def _mat_power(m: list, n: int) -> List[List[float]]`
- `def __init__(self)`
- `def _register_builtin_ops(self)`
- `def _derivative(func: Callable, x: float, h: float) -> float`
- `def _integral(func: Callable, a: float, b: float, n: int) -> float`
- `def _taylor(func: Callable, x: float, x0: float, n: int) -> float`
- `def _limit(func: Callable, x: float, direction: str) -> float`
- `def _newton(func: Callable, x0: float, tol: float, max_iter: int) -> float`
- `def __init__(self)`
- `def _register_builtin_ops(self)`
- `def _fft(signal: List[complex]) -> List[complex]`
- `def _ifft(signal: List[complex]) -> List[complex]`
- `def _convolve(a: List[float], b: List[float]) -> List[float]`
- `def _lowpass(signal: List[float], cutoff: float) -> List[float]`
- `def _highpass(signal: List[float], cutoff: float) -> List[float]`
- `def _magnitude(signal: List[complex]) -> List[float]`
- `def __init__(self)`
- `def _register_builtin_ops(self)`
- `def _polygon_area(points: List[Tuple[float, float]]) -> float`
- `def __init__(self)`
- `def _register_builtin_ops(self)`
- `def _gradient_descent(func: Callable, x0: float, lr: float, tol: float, max_iter: int) -> float`
- `def _binary_search(func: Callable, lo: float, hi: float, tol: float) -> float`
- `def _golden_section(func: Callable, lo: float, hi: float, tol: float) -> float`
- `def __init__(self)`
- `def _init_drivers(self)`
- `def register_driver(self, driver: MathDriver)`
- `def execute(self, driver_name: str, op_name: str, *args, **kwargs) -> Any`
- `def list_drivers(self) -> List[dict]`
- `def get_stats(self) -> dict`
- `def consume(self, lang: str, functions: List[dict])`
- `def _wrap_function(self, lang: str, func_info: dict) -> Callable`

### matha_growth

- [GrowthReport](`#class-growthreport`)
- [MathaGrowthEngine](`#class-mathagrowthengine`)

**方法**
- `def __init__(self) -> None`
- `def grow(self, source: str) -> GrowthReport`
- `def _single_growth_iteration(self, source: str, iteration: int) -> GrowthReport`
- `def _diagnose(self, source: str) -> list[str]`
- `def _apply_optimizations(self, source: str) -> tuple[str, list[str]]`
- `def _verify(self, original: str, improved: str) -> bool`
- `def _apply_const_folding(self, source: str) -> tuple[str, int]`
- `def _apply_dead_code_elimination(self, source: str) -> tuple[str, int]`
- `def _apply_const_propagation(self, source: str) -> tuple[str, int]`
- `def _apply_function_inlining(self, source: str) -> tuple[str, int]`
- `def _apply_loop_unrolling(self, source: str) -> tuple[str, int]`
- `def _apply_memory_optimization(self, source: str) -> tuple[str, int]`
- `def _apply_trig_identities(self, source: str) -> tuple[str, int]`

**函数**
- `def _curry3(func)` — 将三参 Python 函数转为柯里化：f(a)(b)(c)。
- `def _register_math_builtins(builtins: dict) -> None` — 将数学函数与常量注册到解释器 builtins 字典。

在 Interpreter.__init__ 中调用：
   
- `def _curry2(func)` — 将两参 Python 函数转为柯里化：f(a)(b)。
- `def _max_variadic(*args)` — max 支持多参或单列表。
- `def _min_variadic(*args)` — min 支持多参或单列表。
- `def _sign(x)` — 符号函数：x>0 返回 1，x<0 返回 -1，x==0 返回 0。
- `def _register_unit_builtins(builtins: dict) -> None` — 注册单位换算内建。

每个换算注册为 换算名(值) -> 转换值 的单参函数。
温度换算因非线性，单独注册为 lambd
- `def _median(lst)` — 计算中位数。
- `def _variance(lst)` — 计算方差（总体方差）。
- `def _covariance(x, y)` — 计算协方差。
- `def _correlation(x, y)` — 计算皮尔逊相关系数。
- `def _power_set(s)` — 生成幂集（所有子集）。
- `def _pascal_triangle(n)` — 生成杨辉三角前 n 行。
- `def _is_prime(n)` — 素数判定。
- `def _sieve_of_eratosthenes(n)` — 埃氏筛，返回 n 以内的所有素数。
- `def _norm_pdf(x, mu, sigma)` — 正态分布概率密度函数。
- `def _curry2(func)` — 两参柯里化。
- `def _curry3(func)` — 三参柯里化。

### mir

- [MIRType](`#class-mirtype`)
- [MIRInstrType](`#class-mirinstrtype`)
- [MIRInstr](`#class-mirinstr`)
- [MIRArithInstr](`#class-mirarithinstr`)
- [MIRUnaryInstr](`#class-mirunaryinstr`)
- [MIRCallInstr](`#class-mircallinstr`)
- [MIRCImportInstr](`#class-mircimportinstr`)
- [MIRCompareInstr](`#class-mircompareinstr`)
- [MIRLogicalInstr](`#class-mirlogicalinstr`)
- [MIRBranchInstr](`#class-mirbranchinstr`)
- [MIRCondBranchInstr](`#class-mircondbranchinstr`)
- [MIRLabelInstr](`#class-mirlabelinstr`)
- [MIRReturnInstr](`#class-mirreturninstr`)
- [MIRStoreInstr](`#class-mirstoreinstr`)
- [MIRLoadInstr](`#class-mirloadinstr`)
- [MIRConstInstr](`#class-mirconstinstr`)
- [MIRFunction](`#class-mirfunction`)
- [MIRProgram](`#class-mirprogram`)
- [MIRGenerator](`#class-mirgenerator`)

**函数**
- `def generate_mir(ast: Any) -> MIRProgram` — 生成 MIR 程序。
- `def mir_to_dict(mir: MIRProgram) -> dict` — 将 MIR 转换为字典。

**方法**
- `def __str__(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def get_c_signature(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def format(self) -> str`
- `def __init__(self) -> None`
- `def _new_temp(self) -> str`
- `def _ensure_scope(self) -> dict`
- `def generate(self, ast: Any) -> MIRProgram`
- `def _compile_program(self, program: Any) -> None`
- `def _compile_decl(self, decl: Any) -> None`
- `def _compile_func_def(self, func_def: Any) -> None`
- `def _compile_binding(self, binding: Any) -> None`
- `def _compile_expr(self, expr: Any) -> str`
- `def _compile_literal(self, lit: Any) -> str`
- `def _compile_variable(self, var: Any) -> str`
- `def _compile_binary(self, op_expr: Any) -> str`
- `def _compile_unary(self, op_expr: Any) -> str`
- `def _compile_func_app(self, func_app: Any) -> str`
- `def _compile_lambda(self, lam: Any) -> str`
- `def _compile_if(self, if_expr: Any) -> str`
- `def _compile_list(self, lst: Any) -> str`
- `def _compile_dict(self, dct: Any) -> str`
- `def _compile_path(self, path: Any) -> str`
- `def _compile_tuple(self, tpl: Any) -> str`
- `def _compile_index(self, idx: Any) -> str`
- `def _compile_slice(self, slc: Any) -> str`
- `def _compile_set(self, set_node: Any) -> str`
- `def _add_required_imports(self) -> None`
- `def to_dict(self) -> dict`

### mir2_frontend

- [Effect](`#class-effect`)
- [TypeInfo](`#class-typeinfo`)
- [MIR2Expr](`#class-mir2expr`)
- [MIR2Function](`#class-mir2function`)
- [MIR2Program](`#class-mir2program`)
- [PythonFrontend](`#class-pythonfrontend`)
- [MultiLanguageFrontend](`#class-multilanguagefrontend`)

**方法**
- `def __repr__(self)`
- `def __init__(self)`
- `def compile(self, source: str) -> Any`
- `def _compile_function(self, node: ast.FunctionDef) -> MIR2Function`
- `def _compile_stmt(self, stmt: ast.stmt, func: MIR2Function) -> None`
- `def _compile_assignment(self, node: ast.Assign, context: Any) -> None`
- `def _compile_expr(self, node: ast.expr, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_constant(self, node: ast.Constant) -> MIR2Expr`
- `def _compile_name(self, node: ast.Name, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_binop(self, node: ast.BinOp, func: MIR2Function) -> MIR2Expr`
- `def _compile_unaryop(self, node: ast.UnaryOp, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_call(self, node: ast.Call, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_if(self, node: ast.If, func: MIR2Function) -> None`
- `def _compile_while(self, node: ast.While, func: Optional[MIR2Function]) -> None`
- `def _compile_for(self, node: ast.For, func: Optional[MIR2Function]) -> None`
- `def _compile_return(self, node: ast.Return, func: MIR2Function) -> None`
- `def _compile_import(self, node: ast.Import, program: MIR2Program) -> None`
- `def _compile_import_from(self, node: ast.ImportFrom, program: MIR2Program) -> None`
- `def _map_binop(self, op_name: str) -> str`
- `def _map_unaryop(self, op_name: str) -> str`
- `def _infer_effect(self, node: ast.FunctionDef) -> Effect`
- `def _get_name(self, node: ast.AST) -> str`
- `def _compile_boolop(self, node: ast.BoolOp, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_compare(self, node: ast.Compare, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_list(self, node: ast.List, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_dict(self, node: ast.Dict, func: Optional[MIR2Function]) -> MIR2Expr`
- `def _compile_subscript(self, node: ast.Subscript, func: MIR2Expr) -> MIR2Expr`
- `def _compile_attribute(self, node: ast.Attribute, func: MIR2Expr) -> MIR2Expr`
- `def _map_cmpop(self, op_name: str) -> str`
- `def __init__(self)`
- `def register(self, lang: str, frontend: Any) -> None`
- `def compile(self, source: str, language: str) -> MIR2Program`
- `def supported_languages(self) -> list[str]`

### mir_codegen

- [MIRToCGenerator](`#class-mirtocgenerator`)
- [MIRToPythonGenerator](`#class-mirtopythongenerator`)
- [MathaCodeGenerator](`#class-mathacodegenerator`)

**函数**
- `def generate_c_code(mir_program: any) -> str` — 生成 C 代码。
- `def generate_python_code(mir_program: any) -> str` — 生成 Python 代码。
- `def compile_to_c(ast_or_mir: any) -> str` — 编译 MIR 为 C 代码。
- `def compile_to_python(ast_or_mir: any) -> str` — 编译 MIR 为 Python 代码。

**方法**
- `def __init__(self, optimize: bool) -> None`
- `def generate(self, mir_program: any) -> str`
- `def _generate_function(self, name: str, func: any) -> list[str]`
- `def _generate_locals(self, func: any) -> tuple[list[str], list[str]]`
- `def _generate_instructions(self, instructions: list) -> list[str]`
- `def _generate_instr_code(self, instr: any) -> Optional[str]`
- `def _optimize(self, source: str) -> str`
- `def __init__(self, optimize: bool) -> None`
- `def generate(self, mir_program: any) -> str`
- `def _generate_function(self, name: str, func: any) -> list[str]`
- `def _generate_instr_code(self, instr: any) -> str`
- `def __init__(self, target: str, optimize: bool) -> None`
- `def generate(self, mir_program: any) -> str`
- `def compile_and_run(self, mir_program: any, args: list) -> tuple`

### mir_converter

- [MIRToMathaGenerator](`#class-mirtomathagenerator`)
- [MathaToMIRGenerator](`#class-mathatomirgenerator`)
- [MathaConverter](`#class-mathaconverter`)

**函数**
- `def matha_to_mir(matha_source: str) -> str` — 将 Matha 源码转换为 MIR 文本表示。
- `def convert(source: str, source_lang: str, target_lang: str) -> str` — 通用转换器。
- `def convert_all(source: str, source_lang: str) -> dict` — 转换为所有支持的语言。

**方法**
- `def __init__(self, indent: int) -> None`
- `def generate(self, mir_program: any) -> str`
- `def _generate_main(self, func: any) -> list[str]`
- `def _generate_function(self, name: str, func: any) -> list[str]`
- `def _generate_instr_code(self, instr: any) -> Optional[str]`
- `def _resolve_var(self, var: str) -> str`
- `def _new_var(self) -> str`
- `def __init__(self) -> None`
- `def _new_temp(self) -> str`
- `def generate_from_matha(self, matha_source: str) -> str`
- `def __init__(self) -> None`
- `def convert(self, source: str, source_lang: str, target_lang: str) -> str`
- `def _to_mir(self, source: str, lang: str) -> any`
- `def _from_mir(self, mir_program: any, lang: str) -> str`
- `def _c_to_mir(self, c_code: str) -> any`
- `def _python_to_mir(self, py_code: str) -> any`
- `def batch_convert(self, source: str, target_langs: list) -> dict`

### mir_opt

- [MathaConstFoldPass](`#class-mathaconstfoldpass`)
- [MathaSimplifyPass](`#class-mathasimplifypass`)
- [MathaDeadCodeElimPass](`#class-mathadeadcodeelimpass`)
- [MathaCommonSubexprElimPass](`#class-mathacommonsubexprelimpass`)
- [MathaCopyPropagationPass](`#class-mathacopypropagationpass`)
- [MathaStrengthReductionPass](`#class-mathastrengthreductionpass`)
- [MathaInlinePass](`#class-mathainlinepass`)
- [MathaPeepholeOptimizer](`#class-mathapeepholeoptimizer`)
- [MathaOptimizationPipeline](`#class-mathaoptimizationpipeline`)
- [MathaTailRecPass](`#class-mathatailrecpass`)
- [MathaLoopUnrollPass](`#class-mathaloopunrollpass`)
- [MathaSIMDPass](`#class-mathasimdpass`)
- [MathaCurryFlattenPass](`#class-mathacurryflattenpass`)
- [MathaOptimizationPipeline](`#class-mathaoptimizationpipeline`)

**函数**
- `def _get_var(name: str) -> str` — 提取变量名（去除 % 前缀）。
- `def _get_op(instr: Any) -> str` — 安全获取操作符。
- `def _is_const(val: Any) -> bool` — 检查值是否为常量。
- `def _parse_const(val: Any) -> float | None` — 解析常量值。
- `def _eval_const(left: float, right: float, op: str) -> float | None` — 评估常量运算。
- `def _simplify_expr(left: float, right: float, op: str) -> tuple[str, bool]` — 简化表达式，返回 (简化后的字符串, 是否可简化)。

**方法**
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def _eliminate_dead_code(self, func: Any) -> Any`
- `def _remove_unreachable(self, func: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self, aggressive: bool) -> None`
- `def run(self, program: Any) -> Any`
- `def get_summary(self) -> str`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self, threshold: int) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self) -> None`
- `def run(self, program: Any) -> Any`
- `def stats(self) -> dict`
- `def __init__(self, aggressive: bool) -> None`
- `def run(self, program: Any) -> Any`
- `def get_summary(self) -> str`

### mobile_compat

- [MobileCompatibility](`#class-mobilecompatibility`)

**函数**
- `def get_mobile_api()` — 获取移动端 API

Returns:
    SimplifiedAPI: 简化的 API 对象
- `def is_mobile_device() -> bool` — 检测是否为移动设备

Returns:
    bool: 是否运行在移动设备上

**方法**
- `def __new__(cls)`
- `def __init__(self)`
- `def _detect_dependencies(self)`
- `def is_mobile(self) -> bool`
- `def numpy_available(self) -> bool`
- `def get_optimized_matrix_ops(self)`
- `def create_simplified_api(self)`
- `def optimize_for_mobile(self)`

### mobile_full

- [MobileConfig](`#class-mobileconfig`)
- [MobileDeviceDetector](`#class-mobiledevicedetector`)
- [MobileAPI](`#class-mobileapi`)
- [FlutterShell](`#class-fluttershell`)

**函数**
- `def get_mobile_config() -> MobileConfig` — 获取移动端配置
- `def get_mobile_api() -> MobileAPI` — 获取移动端 API
- `def is_mobile() -> bool` — 检测是否运行在移动设备上
- `def get_mobile_state() -> dict` — 获取移动端状态

**方法**
- `def detect(cls) -> bool`
- `def __init__(self, config: Optional[MobileConfig])`
- `def _detect_backend(self) -> None`
- `def zeros(self, shape: tuple) -> Any`
- `def eye(self, n: int) -> Any`
- `def matmul(self, a: Any, b: Any) -> Any`
- `def svd(self, matrix: Any) -> tuple`
- `def clear_cache(self) -> int`
- `def create_init_message(app_name: str, version: str) -> dict`
- `def create_math_request(request_id: str, operation: str, params: list) -> dict`
- `def create_math_response(request_id: str, result: Any, error: Optional[str]) -> dict`
- `def create_storage_request(request_id: str, action: str, key: str, value: Any) -> dict`
- `def create_collab_request(request_id: str, action: str, data: dict) -> dict`

### multi_lang_codegen

- [SymbolCompat](`#class-symbolcompat`)
- [CppGenerator](`#class-cppgenerator`)
- [RustGenerator](`#class-rustgenerator`)
- [GoGenerator](`#class-gogenerator`)
- [JavaGenerator](`#class-javagenerator`)
- [CodeGenResult](`#class-codegenresult`)
- [MultiLangCodeGen](`#class-multilangcodegen`)

**函数**
- `def generate_cpp(func_name: str, params: List[tuple], expr: str, return_type: str) -> str` — 快捷生成 C++ 代码。
- `def generate_rust(func_name: str, params: List[tuple], expr: str, return_type: str) -> str` — 快捷生成 Rust 代码。
- `def generate_go(func_name: str, params: List[tuple], expr: str, return_type: str) -> str` — 快捷生成 Go 代码。
- `def generate_java(func_name: str, params: List[tuple], expr: str, return_type: str, class_name: str) -> str` — 快捷生成 Java 代码。

**方法**
- `def simplify(expr: str) -> str`
- `def to_pipe(expr: str) -> str`
- `def __init__(self, func_name: str, return_type: str, use_templates: bool)`
- `def emit(self, line: str)`
- `def block(self, fn)`
- `def get_code(self) -> str`
- `def generate_function(self, params: List[tuple], expr: str) -> str`
- `def generate_class(self, class_name: str, methods: List[dict]) -> str`
- `def generate_main(self, func_name: str, test_cases: List[tuple]) -> str`
- `def _expr_to_cpp(self, expr: str) -> str`
- `def __init__(self, func_name: str, return_type: str, generic: bool)`
- `def get_code(self) -> str`
- `def generate_function(self, params: List[tuple], expr: str) -> str`
- `def generate_trait(self, trait_name: str, methods: List[dict]) -> str`
- `def generate_main(self) -> str`
- `def _expr_to_rust(self, expr: str) -> str`
- `def _get_expected(self, params: List[tuple]) -> str`
- `def __init__(self, func_name: str, return_type: str)`
- `def get_code(self) -> str`
- `def generate_function(self, params: List[tuple], expr: str) -> str`
- `def generate_goroutine(self, func_name: str, args: List[tuple]) -> str`
- `def _expr_to_go(self, expr: str) -> str`
- `def __init__(self, func_name: str, return_type: str, class_name: str)`
- `def get_code(self) -> str`
- `def generate_class(self, params: List[tuple], expr: str) -> str`
- `def generate_interface(self, interface_name: str, methods: List[dict]) -> str`
- `def _expr_to_java(self, expr: str) -> str`
- `def __init__(self)`
- `def generate(self, language: str, func_name: str, params: List[tuple], expr: str, return_type: str) -> CodeGenResult`
- `def generate_all(self, func_name: str, params: List[tuple], expr: str, return_type: str) -> Dict[str, CodeGenResult]`
- `def get_result(self, language: str) -> Optional[CodeGenResult]`
- `def summary(self) -> str`

### multi_lang_frontend

- [IRKind](`#class-irkind`)
- [IRNode](`#class-irnode`)
- [CompileResult](`#class-compileresult`)
- [RustFrontend](`#class-rustfrontend`)
- [GoFrontend](`#class-gofrontend`)
- [JSFrontend](`#class-jsfrontend`)
- [CFrontend](`#class-cfrontend`)
- [MultiLanguageFrontend](`#class-multilanguagefrontend`)
- [_TS_RustAdapter](`#class-_ts_rustadapter`)
- [_TS_GoAdapter](`#class-_ts_goadapter`)
- [_TS_JSAdapter](`#class-_ts_jsadapter`)
- [_TS_CAdapter](`#class-_ts_cadapter`)

**函数**
- `def _ir_to_mir(node: IRNode) -> list[MIRInstr]` — 将 IR 节点转换为 MIR 指令。
- `def get_frontend() -> MultiLanguageFrontend` — 获取全局前端实例。

**方法**
- `def with_type(self, typ: Type) -> 'IRNode'`
- `def __repr__(self) -> str`
- `def success(self) -> bool`
- `def to_mir(self) -> MIRProgram`
- `def compile(self, source: str) -> CompileResult`
- `def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]`
- `def _resolve_type(self, type_str: str) -> Type`
- `def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _tokenize_expr(self, expr: str) -> list[str]`
- `def _has_io(self, body: str) -> bool`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`
- `def compile(self, source: str) -> CompileResult`
- `def _parse_py_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]`
- `def _parse_py_return(self, ret_expr: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_py_expr(self, expr_str: str, param_types: Optional[dict[str, Type]]) -> list[IRNode]`
- `def _parse_py_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]`
- `def _resolve_type(self, type_str: str) -> Type`
- `def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _tokenize_expr(self, expr: str) -> list[str]`
- `def _has_io(self, body: str) -> bool`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`
- `def compile(self, source: str) -> CompileResult`
- `def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]`
- `def _resolve_type(self, type_str: str) -> Type`
- `def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _tokenize_expr(self, expr: str) -> list[str]`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`
- `def compile(self, source: str) -> CompileResult`
- `def _parse_py_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]`
- `def _parse_py_return(self, ret_expr: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_py_expr(self, expr_str: str, param_types: Optional[dict[str, Type]]) -> list[IRNode]`
- `def _parse_py_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]`
- `def _resolve_type(self, type_str: str) -> Type`
- `def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]`
- `def _tokenize_expr(self, expr: str) -> list[str]`
- `def _has_io(self, body: str) -> bool`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`
- `def __init__(self)`
- `def register(self, language: str, frontend: Any) -> None`
- `def compile(self, source: str, language: str) -> CompileResult`
- `def infer_types(self, source: str, language: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str, language: str) -> dict[str, str]`
- `def supported_languages(self) -> list[str]`
- `def to_mir(self, result: CompileResult) -> MIRProgram`
- `def compile(self, source: str) -> CompileResult`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`
- `def compile(self, source: str) -> CompileResult`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`
- `def compile(self, source: str) -> CompileResult`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`
- `def compile(self, source: str) -> CompileResult`
- `def infer_types(self, source: str) -> dict[str, Type]`
- `def analyze_effects(self, source: str) -> dict[str, str]`

### multi_lang_verifier

- [CompareResult](`#class-compareresult`)
- [MultiLangVerification](`#class-multilangverification`)
- [MultiLangCodeGenerator](`#class-multilangcodegenerator`)
- [MultiLangCompiler](`#class-multilangcompiler`)
- [MultiLangVerifier](`#class-multilangverifier`)
- [PerformanceBenchmark](`#class-performancebenchmark`)

**方法**
- `def __str__(self) -> str`
- `def all_passed(self) -> bool`
- `def summary(self) -> str`
- `def gen_python(func_name: str, params: List[str], expr: str) -> str`
- `def gen_cpp(func_name: str, params: List[str], expr: str) -> str`
- `def gen_rust(func_name: str, params: List[str], expr: str) -> str`
- `def gen_go(func_name: str, params: List[str], expr: str) -> str`
- `def gen_java(func_name: str, params: List[str], expr: str) -> str`
- `def __init__(self, work_dir: str)`
- `def compile_cpp(self, code: str, func_name: str) -> str`
- `def compile_rust(self, code: str, func_name: str) -> str`
- `def compile_go(self, code: str, func_name: str) -> str`
- `def compile_java(self, code: str, func_name: str) -> str`
- `def run_executable(self, exe: str, input_args: List[float]) -> Tuple[float, str]`
- `def __init__(self, matha_impl)`
- `def verify(self, func_name: str, params: List[str], expr: str, test_cases: List[Tuple[List[Any], Any]], matha_func) -> MultiLangVerification`
- `def _close_enough(self, a: float, b: float, tolerance: float) -> bool`
- `def get_verification(self, func_name: str) -> Optional[MultiLangVerification]`
- `def generate_report(self) -> str`
- `def benchmark_func(func, args, iterations: int) -> dict`

### multi_paradigm

- [Paradigm](`#class-paradigm`)
- [ParadigmContext](`#class-paradigmcontext`)
- [FunctionalEngine](`#class-functionalengine`)
- [ImperativeEngine](`#class-imperativeengine`)
- [LogicEngine](`#class-logicengine`)
- [DataflowEngine](`#class-dataflowengine`)
- [MultiParadigmEngine](`#class-multiparadigmengine`)

**函数**
- `def get_paradigm_engine() -> MultiParadigmEngine` — 
- `def paradigm_compute(task: dict) -> dict` — 便捷入口。

**方法**
- `def __init__(self)`
- `def eval(self, expr: Any, env: Dict) -> Any`
- `def curry(self, func: Callable, n: int) -> Callable`
- `def compose(self, *funcs) -> Callable`
- `def fold(self, func: Callable, init: Any, lst: list) -> Any`
- `def map(self, func: Callable, lst: list) -> list`
- `def filter(self, func: Callable, lst: list) -> list`
- `def __init__(self)`
- `def set(self, name: str, value: Any)`
- `def get(self, name: str) -> Any`
- `def eval_stmt(self, stmt: dict) -> Any`
- `def _eval_expr(self, expr: Any) -> Any`
- `def get_state(self) -> Dict[str, Any]`
- `def __init__(self)`
- `def add_rule(self, name: str, body: Callable, conclusion: Callable)`
- `def add_fact(self, name: str, value: Any)`
- `def query(self, goal: str) -> Optional[Any]`
- `def unify(self, pattern: dict, facts: dict) -> Optional[dict]`
- `def __init__(self)`
- `def add_node(self, name: str, func: Callable)`
- `def add_edge(self, from_node: str, to_node: str)`
- `def run(self, inputs: Dict[str, Any]) -> Dict[str, Any]`
- `def __init__(self)`
- `def compute(self, task: dict) -> dict`
- `def _run_functional(self, task: dict) -> dict`
- `def _run_imperative(self, task: dict) -> dict`
- `def _run_symbolic(self, task: dict) -> dict`
- `def _run_logic(self, task: dict) -> dict`
- `def _run_dataflow(self, task: dict) -> dict`
- `def _run_mixed(self, task: dict) -> dict`
- `def switch_paradigm(self, paradigm: Paradigm)`
- `def get_paradigm_history(self) -> List[str]`

### net_security

- [ThreatLevel](`#class-threatlevel`)
- [ThreatSignature](`#class-threatsignature`)
- [NetworkThreat](`#class-networkthreat`)
- [NetSecurityEngine](`#class-netsecurityengine`)

**函数**
- `def _get_engine() -> NetSecurityEngine` — 
- `def builtin_威胁检测(source_ips: list) -> list` — 威胁检测(source_ips) → 威胁列表。
- `def builtin_检测病毒(behavior: str) -> list` — 检测病毒(行为模式) → 匹配签名列表。
- `def builtin_隔离威胁(threat_id: str) -> dict` — 隔离威胁(威胁ID) → 操作结果。
- `def builtin_清杀威胁(threat_id: str) -> dict` — 清杀威胁(威胁ID) → 操作结果。
- `def builtin_清杀全部(level: str) -> dict` — 清杀全部威胁(级别) → 批量操作结果。
- `def builtin_生成防火墙规则(threats: list) -> list` — 生成防火墙规则(威胁列表) → 规则列表。
- `def builtin_风险评估(ip: str) -> dict` — 风险评估(IP地址) → 风险评估结果。
- `def builtin_风险报告() -> dict` — 风险报告() → 整体风险报告。
- `def builtin_威胁报告(threat_id: str) -> dict` — 威胁报告(威胁ID) → 威胁详情。
- `def builtin_添加签名(name: str, pattern: str, level: str, category: str, description: str) -> bool` — 添加签名(名称, 模式, 等级, 分类, 描述) → 是否成功。
- `def builtin_列出签名() -> list` — 列出签名() → 所有威胁签名。
- `def builtin_威胁数量() -> int` — 威胁数量() → 当前威胁总数。
- `def builtin_已清杀数量() -> int` — 已清杀数量() → 已清杀威胁数。
- `def builtin_防火墙规则列表() -> list` — 防火墙规则列表() → 已生成规则。
- `def builtin_威胁状态(threat_id: str) -> dict` — 威胁状态(威胁ID) → 威胁当前状态。

**方法**
- `def to_dict(self) -> dict`
- `def __init__(self, signatures: Optional[list[ThreatSignature]])`
- `def scan_threats(self, source_ips: list[str], dest_ip: str, dest_port: int) -> list[dict]`
- `def detect_virus(self, behavior_pattern: str) -> list[dict]`
- `def quarantine(self, threat_id: str) -> dict`
- `def eliminate(self, threat_id: str) -> dict`
- `def eliminate_all(self, level: Optional[str]) -> dict`
- `def generate_firewall_rules(self, threats: Optional[list[dict]]) -> list[dict]`
- `def get_firewall_rules(self) -> list[dict]`
- `def assess_risk(self, ip: str) -> dict`
- `def get_risk_report(self) -> dict`
- `def get_threat_report(self, threat_id: str) -> dict`
- `def add_signature(self, name: str, pattern: str, level: str, category: str, description: str, mitigation: str) -> bool`
- `def remove_signature(self, name: str) -> bool`
- `def list_signatures(self) -> list[dict]`
- `def threat_count(self) -> int`
- `def eliminated_count(self) -> int`
- `def threat_status(self, threat_id: str) -> dict`
- `def _create_threat(self, source_ip: str, dest_ip: str, dest_port: int, name: str, level: ThreatLevel, category: str, description: str, matched_pattern: str, mitigation: str, risk_score: float) -> NetworkThreat`
- `def _calc_risk(self, level: ThreatLevel) -> float`
- `def _is_suspicious_port(self, port: int) -> bool`
- `def _check_ip_reputation(self, ip: str) -> dict`
- `def _is_valid_ip(self, ip: str) -> bool`
- `def _random_source_port() -> int`
- `def create_virus(self, name: str, category: str, level: str, behavior: str, payload: str, source_ip: str, target_ip: str) -> dict`
- `def create_virus_batch(self, count: int, categories: Optional[list]) -> list`
- `def analyze_virus(self, virus_id: str) -> dict`
- `def _analyze_behavior(self, behavior: str) -> dict`
- `def patch_vulnerability(self, vuln_id: str, severity: str) -> dict`
- `def simulate_spread(self, virus_id: str, network_size: int) -> dict`
- `def neutralize(self, threat_id: str, method: str) -> dict`
- `def quarantine_all(self) -> dict`
- `def get_virus_library(self) -> list[dict]`
- `def create_test_scenario(self, name: str, virus_count: int, network_size: int) -> dict`
- `def get_quarantine_log(self) -> list[dict]`
- `def get_elimination_log(self) -> list[dict]`

### numpy_compat

- [ndarray](`#class-ndarray`)

**函数**
- `def array(data: Union[List, Tuple], dtype: type) -> ndarray` — 创建数组
- `def zeros(shape: Tuple[int, ...], dtype: type) -> ndarray` — 创建零数组
- `def ones(shape: Tuple[int, ...], dtype: type) -> ndarray` — 创建全一数组
- `def eye(n: int, dtype: type) -> ndarray` — 单位矩阵
- `def arange(start: float, stop: Optional[float], step: float) -> ndarray` — 生成等差数列
- `def linspace(start: float, stop: float, num: int) -> ndarray` — 生成等间隔数列
- `def random(size: Union[int, Tuple[int, ...]]) -> ndarray` — 随机数组
- `def matrix_multiply(A: ndarray, B: ndarray) -> ndarray` — 矩阵乘法
- `def matrix_transpose(A: ndarray) -> ndarray` — 矩阵转置
- `def matrix_inverse(A: ndarray) -> Optional[ndarray]` — 矩阵求逆（高斯-约当消元法）
- `def matrix_determinant(A: ndarray) -> float` — 矩阵行列式（高斯消元法）
- `def svd_decompose(A: ndarray) -> Tuple[ndarray, ndarray, ndarray]` — SVD 分解：A = U Σ V^T

使用纯 Python 实现（QR 迭代法）
- `def _svd_from_eigenvalues(A: ndarray) -> Tuple[ndarray, ndarray, ndarray]` — 从特征值分解计算 SVD（对称矩阵）

对于对称矩阵 A，SVD 可以通过特征值分解得到：
- 奇异值 σ_i = |λ
- `def _svd_general(A: ndarray) -> Tuple[ndarray, ndarray, ndarray]` — 通用矩阵的 SVD 分解（幂迭代法）
- `def _eig_power_iteration(A: ndarray, max_iter: int, tol: float) -> list` — 使用幂迭代法计算特征值和特征向量
- `def _mat_vec_mul(A: ndarray, v: list) -> list` — 矩阵向量乘法
- `def _dot_product(v1: list, v2: list) -> float` — 向量点积
- `def _scale_vec(v: list, scalar: float) -> list` — 向量缩放
- `def _vec_sub(v1: list, v2: list) -> list` — 向量减法
- `def trace(A: ndarray) -> float` — 矩阵迹
- `def norm(A: ndarray, ord: Optional[str]) -> float` — 矩阵范数

**方法**
- `def __init__(self, data: Union[List, Tuple, 'ndarray'], dtype: type)`
- `def _convert_to_array(self, data: Union[List, Tuple, 'ndarray']) -> List`
- `def _compute_shape(self, data: List) -> Tuple[int, ...]`
- `def _cast(self, value) -> Union[int, float, complex]`
- `def shape(self) -> Tuple[int, ...]`
- `def size(self) -> int`
- `def ndim(self) -> int`
- `def reshape(self, *new_shape) -> 'ndarray'`
- `def flatten(self) -> List`
- `def _flatten_list(self, data: List) -> List`
- `def __getitem__(self, index)`
- `def _nested_get(self, data: List, indices: Tuple) -> any`
- `def __setitem__(self, index, value)`
- `def _nested_set(self, data: List, indices: Tuple, value)`
- `def __repr__(self) -> str`
- `def __str__(self) -> str`
- `def _format_array(self, data: List, depth: int) -> str`
- `def __add__(self, other: Union['ndarray', int, float]) -> 'ndarray'`
- `def __sub__(self, other: Union['ndarray', int, float]) -> 'ndarray'`
- `def __mul__(self, other: Union['ndarray', int, float]) -> 'ndarray'`
- `def __truediv__(self, other: Union['ndarray', int, float]) -> 'ndarray'`
- `def _broadcast_op(self, data, scalar, op)`
- `def _element_wise_op(self, data1, data2, op)`
- `def abs(self) -> 'ndarray'`
- `def sqrt(self) -> 'ndarray'`
- `def sin(self) -> 'ndarray'`
- `def cos(self) -> 'ndarray'`
- `def tan(self) -> 'ndarray'`
- `def exp(self) -> 'ndarray'`
- `def log(self) -> 'ndarray'`
- `def dot(self, other: 'ndarray') -> 'ndarray'`
- `def transpose(self) -> 'ndarray'`
- `def sum(self, axis: Optional[int]) -> Union['ndarray', float]`
- `def _sum_all(self, data) -> float`
- `def mean(self) -> float`
- `def std(self) -> float`

### offline.mobile_api_client

- [HTTPMethod](`#class-httpmethod`)
- [SyncStatus](`#class-syncstatus`)
- [APIResponse](`#class-apiresponse`)
- [SyncRecord](`#class-syncrecord`)
- [MathaAPIClient](`#class-mathaapiclient`)

**函数**
- `def get_api_client() -> MathaAPIClient` — 
- `def set_api_client(client: MathaAPIClient) -> None` — 

**方法**
- `def success(self) -> bool`
- `def is_conflict(self) -> bool`
- `def __init__(self, base_url: str, api_key: Optional[str], timeout: float, max_retries: int)`
- `def is_authenticated(self) -> bool`
- `def _build_headers(self) -> Dict[str, str]`
- `def on_conflict(self, callback: Callable[[SyncRecord], None]) -> None`
- `def on_sync(self, callback: Callable[[str, str], None]) -> None`
- `def _notify_conflict(self, record_type: str, record_id: str, local_data: Dict, remote_data: Dict) -> None`
- `def _notify_sync(self, action: str, record_id: str) -> None`
- `def get_base_url(self) -> str`
- `def set_base_url(self, url: str) -> None`
- `def get_token(self) -> Optional[str]`
- `def clear_token(self) -> None`
- `def __repr__(self) -> str`

### offline.sqlite_storage

- [HistoryRecord](`#class-historyrecord`)
- [PreferenceRecord](`#class-preferencerecord`)
- [SQLiteStorage](`#class-sqlitestorage`)

**函数**
- `def get_storage() -> SQLiteStorage` — 获取全局存储实例（单例）

**方法**
- `def __init__(self, db_path: Optional[str])`
- `def _get_connection(self) -> sqlite3.Connection`
- `def _init_database(self) -> None`
- `def save_history(self, code: str, output: str, is_success: bool, mode: str) -> int`
- `def get_history(self, limit: int, offset: int) -> List[HistoryRecord]`
- `def get_history_count(self) -> int`
- `def clear_history(self) -> int`
- `def delete_history(self, record_id: int) -> bool`
- `def save_preference(self, key: str, value: str) -> None`
- `def get_preference(self, key: str, default: str) -> str`
- `def get_all_preferences(self) -> Dict[str, str]`
- `def delete_preference(self, key: str) -> bool`
- `def cache_result(self, code: str, output: str, duration_ms: float) -> bool`
- `def get_cached_result(self, code: str) -> Optional[str]`
- `def get_cache_stats(self) -> Dict[str, int]`
- `def clear_cache(self) -> int`
- `def add_word(self, word: str, category: str) -> None`
- `def increment_word_usage(self, word: str) -> None`
- `def get_words(self, category: Optional[str], limit: int) -> List[str]`
- `def add_batch_words(self, words: List[str], category: str) -> int`
- `def enqueue_sync(self, action: str, record_type: str, record_id: str, data: Dict[str, Any], priority: int) -> int`
- `def get_pending_sync(self, limit: int) -> List[Dict[str, Any]]`
- `def mark_synced(self, queue_id: int) -> bool`
- `def mark_failed(self, queue_id: int, error: str) -> bool`
- `def get_queue_stats(self) -> Dict[str, int]`
- `def clear_sync_queue(self) -> int`
- `def get_db_size(self) -> float`
- `def backup(self, backup_path: str) -> bool`
- `def restore(self, backup_path: str) -> bool`
- `def close(self) -> None`
- `def __enter__(self)`
- `def __exit__(self, exc_type, exc_val, exc_tb)`
- `def __del__(self)`

### offline.storage

- [HistoryEntry](`#class-historyentry`)
- [UserPreference](`#class-userpreference`)
- [OfflineStorage](`#class-offlinestorage`)

**函数**
- `def get_storage() -> OfflineStorage` — 获取全局存储实例（单例）

**方法**
- `def __init__(self, db_path: Optional[str])`
- `def _get_connection(self) -> sqlite3.Connection`
- `def _init_database(self) -> None`
- `def save_history(self, code: str, output: str, is_success: bool) -> int`
- `def get_history(self, limit: int, offset: int) -> List[HistoryEntry]`
- `def clear_history(self) -> int`
- `def get_history_count(self) -> int`
- `def save_preference(self, key: str, value: str) -> None`
- `def get_preference(self, key: str, default: str) -> str`
- `def get_all_preferences(self) -> Dict[str, str]`
- `def cache_result(self, code: str, output: str) -> bool`
- `def get_cached_result(self, code: str) -> Optional[str]`
- `def get_cache_stats(self) -> Dict[str, int]`
- `def add_completion_word(self, word: str, category: str) -> None`
- `def increment_word_usage(self, word: str) -> None`
- `def get_completion_words(self, category: Optional[str], limit: int) -> List[str]`
- `def add_batch_words(self, words: List[str], category: str) -> int`
- `def get_db_size(self) -> float`
- `def backup(self, backup_path: str) -> bool`
- `def restore(self, backup_path: str) -> bool`
- `def close(self) -> None`
- `def __enter__(self)`
- `def __exit__(self, exc_type, exc_val, exc_tb)`
- `def __del__(self)`

### offline.sync

- [ConflictStrategy](`#class-conflictstrategy`)
- [SyncConflict](`#class-syncconflict`)
- [SyncLog](`#class-synclog`)
- [SyncConflictResolver](`#class-syncconflictresolver`)
- [SyncLogger](`#class-synclogger`)
- [OfflineSyncManager](`#class-offlinesyncmanager`)

**方法**
- `def __init__(self, default_strategy: ConflictStrategy)`
- `def register_callback(self, callback: Callable[[SyncConflict], None]) -> None`
- `def resolve(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]`
- `def _last_write_wins(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]`
- `def _first_write_wins(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]`
- `def _merge(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]`
- `def _deep_merge(self, local: Dict[str, Any], remote: Dict[str, Any]) -> Dict[str, Any]`
- `def _merge_lists(self, local: List[Any], remote: List[Any]) -> List[Any]`
- `def manual_resolve(self, conflict_id: str, resolution: str, resolved_data: Dict[str, Any]) -> None`
- `def _notify_callbacks(self, conflict: SyncConflict) -> None`
- `def get_conflict_history(self, limit: int) -> List[SyncConflict]`
- `def clear_history(self) -> None`
- `def _apply_resolution(self, conflict: SyncConflict, resolution: str) -> Optional[Dict[str, Any]]`
- `def __init__(self, log_file: Optional[str])`
- `def log(self, action: str, record_type: str, record_id: str, status: str, details: str) -> None`
- `def _write_to_file(self, log: SyncLog) -> None`
- `def get_logs(self, limit: int, action: Optional[str]) -> List[Dict[str, Any]]`
- `def clear_logs(self) -> None`
- `def __init__(self, resolver: Optional[SyncConflictResolver], sync_logger: Optional[SyncLogger], sync_interval: float)`
- `def register_callback(self, callback: Callable[[str, str, str], None]) -> None`
- `def enqueue(self, action: str, record_type: str, record_id: str, data: Dict[str, Any], priority: int) -> None`
- `def process_queue(self, remote_storage: Any) -> List[Dict[str, Any]]`
- `def _process_item(self, item: Dict[str, Any], remote_storage: Any) -> Dict[str, Any]`
- `def _push_item(self, item: Dict[str, Any], remote_storage: Any) -> Dict[str, Any]`
- `def _pull_item(self, item: Dict[str, Any], remote_storage: Any) -> Dict[str, Any]`
- `def _has_conflict(self, local: Dict[str, Any], remote: Dict[str, Any]) -> bool`
- `def _check_network(self) -> bool`
- `def get_queue_size(self) -> int`
- `def is_syncing(self) -> bool`
- `def get_last_sync_time(self) -> float`
- `def get_sync_logs(self, limit: int) -> List[Dict[str, Any]]`
- `def clear_queue(self) -> None`

### offline_store

- [ChangeEntry](`#class-changeentry`)
- [OfflineStore](`#class-offlinestore`)

**函数**
- `def get_offline_store() -> OfflineStore` — 获取离线存储单例

**方法**
- `def __init__(self, db_path: Optional[str])`
- `def _generate_client_id(self) -> str`
- `def _init_db(self) -> None`
- `def save_project(self, project_id: str, name: str, data: dict) -> bool`
- `def load_project(self, project_id: str) -> Optional[dict]`
- `def list_projects(self) -> List[dict]`
- `def delete_project(self, project_id: str) -> bool`
- `def _log_change(self, entity_type: str, entity_id: str, action: str, data: dict) -> None`
- `def get_pending_changes(self, limit: int) -> List[ChangeEntry]`
- `def mark_synced(self, change_id: str) -> bool`
- `def get_sync_stats(self) -> dict`
- `def resolve_conflict(self, local_data: dict, remote_data: dict, local_ts: float, remote_ts: float) -> dict`
- `def close(self) -> None`
- `def __enter__(self)`
- `def __exit__(self, *args)`

### optimization.sparse_svd

- [SVDResult](`#class-svdresult`)
- [PerformanceStats](`#class-performancestats`)
- [SparseSVDOptimizer](`#class-sparsesvdoptimizer`)

**函数**
- `def create_optimizer(threshold: float, max_iter: int, cache_enabled: bool) -> SparseSVDOptimizer` — 创建稀疏 SVD 优化器实例。

Args:
    threshold: 稀疏度阈值
    max_iter: 最大
- `def quick_svd(A: 'Matrix', threshold: float, max_iter: int) -> Tuple['Matrix', 'Matrix', 'Matrix']` — 快速 SVD 分解（便捷函数）。

Args:
    A: 输入矩阵
    threshold: 稀疏度阈值
   

**方法**
- `def __init__(self, threshold: float, max_iter: int, max_singular_values: int, cache_enabled: bool, cache_max_size: int)`
- `def is_sparse(self, A: 'Matrix') -> bool`
- `def _get_sparsity(self, A: 'Matrix') -> float`
- `def _make_cache_key(self, A: 'Matrix') -> tuple`
- `def _evict_cache(self)`
- `def svd(self, A: 'Matrix', use_numpy: bool, force_sparse: bool) -> SVDResult`
- `def compare_performance(self, A: 'Matrix', iterations: int) -> PerformanceStats`
- `def batch_svd(self, matrices: List['Matrix'], use_numpy: bool, parallel: bool, n_workers: int) -> List[SVDResult]`
- `def predict_large_matrix_time(self, base_size: int, base_time_ms: float, target_size: int, sparsity: float) -> Dict[str, float]`
- `def get_statistics(self) -> Dict`
- `def clear_cache(self)`

### parser

- [ParseError](`#class-parseerror`)
- [Parser](`#class-parser`)

**函数**
- `def parse(source: str) -> ast.Program` — 解析 Matha 源码，返回 AST。
- `def parse_file(path: str) -> ast.Program` — 从文件读取并解析。
- `def _parse_worker(source: str) -> ast.Program` — 工作线程执行的单条解析任务。
- `def parse_batch(sources: list[str], max_workers: int) -> list[ast.Program]` — 批量并行解析 Matha 源码（v2.5 线程池版本）。

参数:
    sources: 待解析的源码列表
    
- `def _get_or_create_pool(max_workers: int) -> ThreadPoolExecutor` — 获取或创建全局线程池（线程安全）。
- `def shutdown_parsers(wait: bool) -> None` — 关闭解析器线程池。

**方法**
- `def __init__(self, msg: str, token: Token)`
- `def __init__(self, source: str)`
- `def _peek(self, offset: int) -> Token`
- `def _current(self) -> Token`
- `def _advance(self) -> Token`
- `def _check(self, *types) -> bool`
- `def _match(self, *types) -> Token | None`
- `def _expect(self, ttype: TokenType, desc: str) -> Token`
- `def _skip_newlines(self) -> None`
- `def _skip_semicolon(self) -> None`
- `def _is_colon(self) -> bool`
- `def _is_comma(self) -> bool`
- `def _skip_comma(self) -> None`
- `def _is_read_open(self) -> bool`
- `def _is_read_close(self) -> bool`
- `def parse(self) -> ast.Program`
- `def _parse_top_level(self)`
- `def _parse_annotation(self) -> ast.Annotation`
- `def _parse_command_literal(self) -> ast.CommandLiteral`
- `def _parse_command_text(self) -> str`
- `def _parse_set_up(self, seg_id: int | None) -> ast.SetUp`
- `def _parse_set_up_item(self) -> ast.SetUpItem`
- `def _parse_variable_or_path(self)`
- `def _is_path_context(self) -> bool`
- `def _parse_loop_fraction(self) -> ast.LoopFraction`
- `def _parse_seg_loop_suffix(self) -> ast.SegLoopSuffix`
- `def _parse_global_loop_suffix(self) -> ast.GlobalLoopSuffix`
- `def _parse_output_trail(self) -> ast.OutputTrail`
- `def _parse_subfile_ref(self) -> list[str]`
- `def _parse_file_ref(self) -> str`
- `def _parse_global_id_stmt(self) -> ast.GlobalIdStmt`
- `def _parse_mech_unit(self) -> ast.MechUnit`
- `def _parse_gen_stmt_content(self, generate: ast.Generate)`
- `def _parse_generate(self) -> ast.Generate`
- `def _parse_code_block(self) -> ast.CodeBlock`
- `def _parse_block_body(self) -> ast.CodeBlock`
- `def _parse_mech_stmt(self)`
- `def _parse_define_op(self) -> ast.AST`
- `def _parse_gen_or_file_marker(self)`
- `def _parse_loop_line(self, generate: ast.Generate)`
- `def _parse_read_or_command(self)`
- `def _parse_brace_dispatch(self)`
- `def _parse_set_construct(self) -> ast.SetConstruct`
- `def _parse_dict_literal(self) -> ast.DictLiteral`
- `def _parse_expr_or_binding(self)`
- `def _make_binding_or_expr(self, expr) -> ast.AST`
- `def _has_conditional_suffix(self, expr) -> bool`
- `def _parse_conditional_binding(self, var: ast.Variable, rel_expr: ast.BinaryOp) -> ast.Binding`
- `def _is_chain_context(self) -> bool`
- `def _parse_chain(self, first_stmt) -> ast.ChainStmt`
- `def _parse_expr(self)`
- `def _parse_or_expr(self)`
- `def _parse_and_expr(self)`
- `def _parse_rel_expr(self)`
- `def _parse_add_expr(self)`
- `def _parse_mul_expr(self)`
- `def _parse_pow_expr(self)`
- `def _parse_unary(self)`
- `def _parse_postfix(self)`
- `def _is_primary_start(self) -> bool`
- `def _parse_primary(self)`
- `def _parse_paren_dispatch(self)`
- `def _parse_lambda_param(self)`
- `def _parse_output(self) -> ast.Output`
- `def _parse_list_literal(self) -> ast.ListLiteral`
- `def _parse_variable(self) -> ast.Variable`
- `def _make_number_lit(self, value: str, is_float: bool)`
- `def _parse_nl_block(self)`
- `def _slice_from(self, start_tok: Token) -> str`
- `def _parse_type_expr(self)`
- `def _parse_statement(self)`
- `def _parse_let(self) -> ast.AST`
- `def _parse_let_tuple(self, is_rec: bool) -> ast.AST`
- `def _parse_while(self) -> ast.WhileStmt`
- `def _parse_if(self) -> ast.AST`
- `def _parse_if_then_else_expr(self, cond) -> ast.IfExpr`
- `def _parse_if_then_else_expr_no_then(self, cond) -> ast.IfExpr`
- `def _parse_if_expr_impl(self) -> ast.IfExpr`
- `def _parse_if_then_else_expr(self, cond) -> ast.IfExpr`
- `def _parse_if_then_else_expr_no_then(self, cond) -> ast.IfExpr`
- `def _parse_semicolon_exprs(self) -> Any`
- `def _parse_if_expr(self) -> ast.IfExpr`
- `def _parse_for(self) -> ast.ForStmt`
- `def _parse_match_stmt(self) -> ast.MatchStmt`
- `def _parse_pattern(self)`
- `def _parse_decl(self)`
- `def _parse_func_def(self) -> ast.FuncDef`
- `def _parse_typed_param(self) -> tuple[str, Any]`
- `def _parse_type_def(self)`
- `def _parse_struct_def(self) -> ast.StructDef`
- `def _parse_enum_def(self) -> ast.EnumDef`
- `def _parse_alias_def(self) -> ast.AliasDef`
- `def _parse_optional_type_params(self) -> list[str]`
- `def _skip_to_rbrace(self) -> None`
- `def _parse_module_decl(self) -> ast.ModuleDecl`
- `def _parse_import_decl(self) -> ast.ImportDecl`
- `def _parse_module_name(self) -> str`
- `def _parse_command_unit(self)`

### parser_pool

- [SerializableResult](`#class-serializableresult`)
- [ProcessPoolIntentParser](`#class-processpoolintentparser`)
- [HybridIntentParser](`#class-hybridintentparser`)

**函数**
- `def _worker_init() -> None` — 每个工作进程启动时调用，完成一次性初始化。

关键：恢复策略和标准库必须在工作进程中重新注册，
因为子进程是全新的 Py
- `def _worker_parse(text: str, target_lang: str) -> SerializableResult` — 工作进程执行的单条解析任务。

每个工作进程持有独立的 EnhancedIntentParser 实例，
避免跨进程共享
- `def _worker_execute(text: str, target_lang: str) -> SerializableResult` — 工作进程执行的解析 + 执行任务。
- `def benchmark_pools()` — 对比单进程、线程池、进程池的性能。

**方法**
- `def to_result(self) -> Result`
- `def from_result(result: Result) -> 'SerializableResult'`
- `def __init__(self, max_workers: int, init_timeout: float)`
- `def _get_executor(self) -> ProcessPoolExecutor`
- `def parse(self, text: str, target_lang: str) -> Result`
- `def parse_batch(self, texts: list[str], target_lang: str) -> list[Result]`
- `def _reorder_results(self, futures: list, texts: list[str], target_lang: str) -> list[Result]`
- `def execute(self, text: str, target_lang: str) -> Result`
- `def execute_batch(self, texts: list[str], target_lang: str) -> list[Result]`
- `def shutdown(self, wait: bool) -> None`
- `def __init__(self, thread_workers: int, process_workers: int)`
- `def parse(self, text: str, target_lang: str) -> Result`
- `def execute(self, text: str, target_lang: str) -> Result`
- `def shutdown(self) -> None`

### perf_opt

- [HotFunctionTracker](`#class-hotfunctiontracker`)
- [LocalVariableEnv](`#class-localvariableenv`)
- [NumericPool](`#class-numericpool`)
- [ArithmeticInlineOptimizer](`#class-arithmeticinlineoptimizer`)
- [PyPyJITAdapter](`#class-pypyjitadapter`)
- [PerformanceProfiler](`#class-performanceprofiler`)
- [PersistentCache](`#class-persistentcache`)

**方法**
- `def __init__(self, threshold: int) -> None`
- `def record(self, name: str) -> None`
- `def should_compile(self, name: str) -> bool`
- `def compile_and_cache(self, name: str, func_def, env_builder: Callable) -> None`
- `def get_compiled(self, name: str) -> Optional[Callable]`
- `def stats(self) -> dict`
- `def __init__(self, capacity: int) -> None`
- `def _ensure_slot(self, name: str) -> int`
- `def set(self, name: str, value: Any) -> None`
- `def get(self, name: str) -> Any`
- `def contains(self, name: str) -> bool`
- `def to_dict(self) -> dict`
- `def clear(self) -> None`
- `def __init__(self, low: int, high: int) -> None`
- `def get(self, value: int) -> int`
- `def size(self) -> int`
- `def try_inline(expr_str: str) -> Optional[float]`
- `def optimize_addition(a: Any, b: Any) -> Any`
- `def optimize_multiplication(a: Any, b: Any) -> Any`
- `def is_pypy() -> bool`
- `def optimize_run(func: Callable) -> Callable`
- `def get_stats() -> dict`
- `def __init__(self) -> None`
- `def start(self, tag: str) -> float`
- `def stop(self, tag: str, start: float) -> float`
- `def report(self) -> str`
- `def clear(self) -> None`
- `def __init__(self, cache_dir: str) -> None`
- `def _key_path(self, key: str) -> str`
- `def get(self, key: str) -> Optional[dict]`
- `def put(self, key: str, value: dict) -> None`
- `def invalidate(self, key: str) -> None`
- `def clear(self) -> None`
- `def stats(self) -> dict`

### performance_benchmark

- [BenchmarkResult](`#class-benchmarkresult`)
- [PerformanceReport](`#class-performancereport`)
- [BenchmarkSuite](`#class-benchmarksuite`)
- [MultiLangBenchmark](`#class-multilangbenchmark`)

**方法**
- `def add_result(self, result: BenchmarkResult) -> None`
- `def compute_summary(self) -> Dict[str, Dict[str, float]]`
- `def generate_markdown(self) -> str`
- `def __init__(self)`
- `def run_benchmark(self, name: str, func, args: tuple, iterations: int, language: str) -> BenchmarkResult`
- `def add_result(self, result: BenchmarkResult) -> None`
- `def generate_report(self) -> PerformanceReport`
- `def __init__(self, work_dir: str)`
- `def benchmark_matrix_svd(self, size: int, iterations: int) -> None`
- `def benchmark_sort(self, n: int, iterations: int) -> None`
- `def benchmark_parallel_compute(self, n: int, iterations: int) -> None`
- `def _compile_cpp(self, code: str, name: str) -> Optional[str]`
- `def run_all(self) -> PerformanceReport`
- `def generate_report(self, output_file: str) -> str`

### pkg_manager

- [Version](`#class-version`)
- [PackageMeta](`#class-packagemeta`)
- [DependencyResolver](`#class-dependencyresolver`)
- [PackageNotFoundError](`#class-packagenotfounderror`)
- [VersionConflictError](`#class-versionconflicterror`)
- [MathaPackage](`#class-mathapackage`)

**方法**
- `def __init__(self, major: int, minor: int, patch: int)`
- `def parse(cls, version_str: str) -> 'Version'`
- `def __str__(self) -> str`
- `def __repr__(self) -> str`
- `def __eq__(self, other) -> bool`
- `def __lt__(self, other) -> bool`
- `def __le__(self, other) -> bool`
- `def __gt__(self, other) -> bool`
- `def __ge__(self, other) -> bool`
- `def __hash__(self) -> int`
- `def to_dict(self) -> dict`
- `def from_dict(cls, data: dict) -> 'PackageMeta'`
- `def __init__(self)`
- `def check_constraint(self, version: Version, constraint: str) -> bool`
- `def check_constraint(self, version: Version, constraint: str) -> bool`
- `def _check_single(self, version: Version, constraint: str) -> bool`
- `def _tilde_match(version: Version, spec: str) -> bool`
- `def _caret_match(version: Version, spec: str) -> bool`
- `def resolve(self, package: PackageMeta, registry: Dict[str, PackageMeta]) -> List[str]`
- `def _resolve_conflict(self, pkg_name: str, constraint: str, registry: Dict[str, PackageMeta]) -> Optional[PackageMeta]`
- `def clear_cache(self)`
- `def __init__(self, root_dir: str)`
- `def _load_registry(self)`
- `def _scan_stdlib(self, stdlib_dir: Path)`
- `def _scan_core_modules(self, src_dir: Path)`
- `def install(self, spec: str, dev: bool) -> List[str]`
- `def list_packages(self) -> List[Tuple[str, Version]]`
- `def search(self, query: str) -> List[PackageMeta]`
- `def show(self, name: str)`

### pkg_manager_v2

- [LockEntry](`#class-lockentry`)
- [Lockfile](`#class-lockfile`)
- [RemotePackageClient](`#class-remotepackageclient`)
- [PackageDownloadError](`#class-packagedownloaderror`)
- [MathaPackageManager](`#class-mathapackagemanager`)

**方法**
- `def to_dict(self) -> Dict`
- `def from_dict(cls, data: Dict) -> 'LockEntry'`
- `def __init__(self, lockfile_path: str)`
- `def load(self) -> None`
- `def save(self) -> None`
- `def add(self, entry: LockEntry) -> None`
- `def get(self, name: str) -> Optional[LockEntry]`
- `def remove(self, name: str) -> None`
- `def update(self, name: str, version: Version) -> bool`
- `def packages(self) -> Dict[str, LockEntry]`
- `def __init__(self, base_url: str)`
- `def search(self, query: str) -> List[Dict]`
- `def get_metadata(self, name: str, version: Optional[str]) -> Optional[Dict]`
- `def download(self, name: str, version: str, dest_dir: Path) -> Path`
- `def __init__(self, root_dir: str, remote_url: str)`
- `def lock(self) -> str`
- `def _compute_checksum(self, pkg: PackageMeta) -> str`
- `def check_lockfile(self) -> Dict`
- `def install_remote(self, spec: str) -> List[str]`
- `def search_remote(self, query: str) -> List[Dict]`
- `def pack(self, pkg_name: str, version: Optional[str]) -> str`
- `def publish(self, pkg_name: str, version: Optional[str]) -> bool`
- `def create_env(self, env_name: str) -> Path`
- `def list_envs(self) -> List[Dict]`
- `def show_tree(self, pkg_name: str, depth: int) -> str`
- `def install(self, spec: str, dev: bool) -> List[str]`
- `def list_packages(self) -> List[Tuple[str, Version]]`

### profiler

- [CallStats](`#class-callstats`)
- [MathaProfiler](`#class-mathaprofiler`)

**函数**
- `def profile(func: Optional[Callable]) -> Callable` — 函数级 Profiler 装饰器。

用法:
    @profile
    def fib(n):
        
- `def get_profile_report(func_name: str) -> str` — 获取指定函数的性能报告。
- `def all_reports() -> Dict[str, str]` — 获取所有已 profiled 函数的报告。
- `def fib(n: int) -> int` — 
- `def fib_raw(n: int) -> int` — 

**方法**
- `def to_dict(self) -> Dict`
- `def __init__(self, sort_by: str, top_n: int)`
- `def __enter__(self) -> 'MathaProfiler'`
- `def __exit__(self, *args) -> None`
- `def start(self) -> None`
- `def stop(self) -> None`
- `def _parse_stats(self) -> None`
- `def report(self, format: str) -> str`
- `def _report_markdown(self, total_time: float) -> str`
- `def _report_json(self, total_time: float) -> str`
- `def save_flamegraph(self, filepath: str) -> str`
- `def get_call_tree(self) -> Dict[str, CallStats]`
- `def reset(self) -> None`

### recovery_strategy_cow

- [CopyOnWriteRecoveryStrategy](`#class-copyonwriterecoverystrategy`)

**函数**
- `def benchmark_cow_vs_rwlock()` — 对比 Copy-on-Write（v2.6 按需深拷贝）和读写锁的性能。
- `def benchmark_register_cost()` — 测量 v2.6 按需深拷贝 vs 旧版全量深拷贝的注册开销。

**方法**
- `def register(cls, stage: ErrorStage)`
- `def try_recover(cls, error: MathaError) -> Optional[MathaError]`
- `def get_strategy_count(cls, stage: ErrorStage) -> int`
- `def clear(cls) -> None`
- `def get_snapshot_size(cls) -> int`

### recovery_strategy_v24

- [RWRecoveryStrategy](`#class-rwrecoverystrategy`)

**函数**
- `def benchmark_rwlock_vs_rlock()` — 对比单一 RLock 和读写锁分离的性能。

**方法**
- `def register(cls, stage: ErrorStage)`
- `def try_recover(cls, error: MathaError) -> Optional[MathaError]`
- `def get_registered_stages(cls) -> list[ErrorStage]`
- `def get_strategy_count(cls, stage: ErrorStage) -> int`
- `def clear(cls) -> None`

### repl

- [REPLState](`#class-replstate`)
- [MathaREPL](`#class-matharepl`)

**函数**
- `def run_repl(debug: bool) -> None` — 启动 REPL。
- `def main(argv) -> int` — CLI 入口。支持：matha（REPL）、matha eval <expr>、matha run <file>。
- `def _cmd_eval(args) -> int` — 计算单行表达式。
- `def _cmd_run(args) -> int` — 运行 .matha 文件。

**方法**
- `def __init__(self, debug: bool)`
- `def _setup_readline(self) -> None`
- `def run(self) -> None`
- `def _print_banner(self) -> None`
- `def _read_input(self) -> str`
- `def _needs_continuation(self, text: str) -> bool`
- `def _process_line(self, line: str) -> None`
- `def _process_matha_expr(self, line: str) -> None`
- `def _process_natural_language(self, line: str) -> None`
- `def _parse_intent(self, line: str) -> None`
- `def _explain_intent(self, line: str) -> None`
- `def _set_mode(self, mode: str) -> None`
- `def _store_result(self, outputs: list) -> None`
- `def _print_help(self) -> None`
- `def _print_history(self) -> None`
- `def _print_summary(self) -> None`

### repl_completion

- [REPLCompleter](`#class-replcompleter`)
- [REPLHistoryManager](`#class-replhistorymanager`)
- [TerminalColors](`#class-terminalcolors`)

**函数**
- `def setup_readline_completion(repl_state, use_colors: bool)` — 设置 readline 补全

Args:
    repl_state: REPLState 对象
    use_c
- `def demo_completion()` — 演示自动补全功能

**方法**
- `def __init__(self, state)`
- `def get_all_completions(self) -> List[str]`
- `def complete(self, text: str, state: int) -> Optional[str]`
- `def _get_completions(self, text: str) -> List[str]`
- `def get_hint(self, text: str) -> str`
- `def get_context_hint(self, text: str) -> str`
- `def _get_function_hints(self, func_name: str) -> List[str]`
- `def _get_attribute_hints(self, prefix: str, suffix: str) -> List[str]`
- `def __init__(self, max_history: int)`
- `def add(self, entry: str) -> None`
- `def previous(self) -> Optional[str]`
- `def next(self) -> Optional[str]`
- `def current(self) -> Optional[str]`
- `def __len__(self) -> int`
- `def __iter__(self)`
- `def colored(cls, text: str, color: str) -> str`
- `def keyword(cls, text: str) -> str`
- `def builtin(cls, text: str) -> str`
- `def variable(cls, text: str) -> str`
- `def command(cls, text: str) -> str`
- `def hint(cls, text: str) -> str`

### repl_highlight

- [SyntaxHighlighter](`#class-syntaxhighlighter`)

**函数**
- `def get_highlighter() -> SyntaxHighlighter` — 获取全局语法高亮器实例
- `def highlight_syntax(text: str, enabled: bool) -> str` — 快速高亮函数

Args:
    text: 原始文本
    enabled: 是否启用高亮

Returns:
 
- `def demo_highlight()` — 演示语法高亮

**方法**
- `def __init__(self, enabled: bool)`
- `def _check_color_support(self) -> bool`
- `def highlight(self, text: str) -> str`
- `def _highlight_comments(self, text: str) -> str`
- `def _highlight_strings(self, text: str) -> str`
- `def _highlight_numbers(self, text: str) -> str`
- `def _highlight_keywords(self, text: str) -> str`
- `def _highlight_functions(self, text: str) -> str`
- `def _highlight_operators(self, text: str) -> str`

**函数**
- `def check_repl_integration()` — 检查 REPL 集成状态

### repl_v23

- [REPLState](`#class-replstate`)
- [MathaREPL](`#class-matharepl`)

**函数**
- `def run_repl(debug: bool) -> None` — 

**方法**
- `def append_error(self, error: MathaError) -> None`
- `def get_error_log(self) -> list[MathaError]`
- `def __init__(self, debug: bool)`
- `def run(self) -> None`
- `def _print_banner(self) -> None`
- `def _read_input(self) -> str`
- `def _process_line(self, line: str) -> None`
- `def _process_matha_expr(self, line: str) -> None`
- `def _process_natural_language(self, line: str) -> None`
- `def _parse_intent(self, line: str) -> None`
- `def _explain_intent(self, line: str) -> None`
- `def _recover_last_error(self) -> None`
- `def _handle_matha_error(self, error: MathaError, source: str) -> None`
- `def _set_mode(self, mode: str) -> None`
- `def _store_result(self, outputs: list) -> None`
- `def _print_help(self) -> None`
- `def _print_history(self) -> None`
- `def _print_errors(self) -> None`
- `def _print_summary(self) -> None`

### result

- [ResultState](`#class-resultstate`)
- [Ok](`#class-ok`)
- [Err](`#class-err`)
- [Some](`#class-some`)
- [None_](`#class-none_`)
- [MathaResultError](`#class-matharesulterror`)
- [MathaOptionError](`#class-mathaoptionerror`)
- [MathaTypeError](`#class-mathatypeerror`)

**函数**
- `def ok(value: T) -> Result` — 创建成功结果。
- `def err(error: E) -> Result` — 创建错误结果。
- `def some(value: T) -> Option` — 创建 Some。
- `def none() -> Option` — 创建 None。
- `def result(fn, *args, **kwargs) -> Result` — 执行函数并捕获异常，返回 Result。
- `def try_unwrap(result: Result) -> Any` — 安全解包 Result。
- `def try_unwrap_or(result: Result, default: Any) -> Any` — 安全解包 Result，失败时返回默认值。
- `def register_result_builtins(builtins: dict) -> None` — 注册 Result/Option 类型到 Matha 内建函数表。

**方法**
- `def is_ok(self) -> bool`
- `def is_err(self) -> bool`
- `def unwrap(self) -> T`
- `def expect(self, msg: str) -> T`
- `def unwrap_or(self, default: T) -> T`
- `def map(self, fn) -> 'Result'`
- `def map_err(self, _fn) -> 'Result'`
- `def and_then(self, fn) -> 'Result'`
- `def or_else(self, _fn) -> 'Result'`
- `def unwrap_or_else(self, fn) -> T`
- `def err(self) -> None`
- `def ok(self) -> Optional[T]`
- `def as_ref(self) -> 'Ok'`
- `def iter(self) -> Any`
- `def label(self) -> str`
- `def is_ok(self) -> bool`
- `def is_err(self) -> bool`
- `def unwrap(self) -> T`
- `def expect(self, msg: str) -> T`
- `def unwrap_or(self, default: T) -> T`
- `def map(self, _fn) -> 'Result'`
- `def map_err(self, fn) -> 'Result'`
- `def and_then(self, _fn) -> 'Result'`
- `def or_else(self, fn) -> 'Result'`
- `def unwrap_or_else(self, fn) -> T`
- `def err(self) -> Optional[E]`
- `def ok(self) -> None`
- `def as_ref(self) -> 'Err'`
- `def iter(self) -> Any`
- `def label(self) -> str`
- `def context(self, msg: str) -> 'Err'`
- `def is_some(self) -> bool`
- `def is_none(self) -> bool`
- `def unwrap(self) -> T`
- `def unwrap_or(self, default: T) -> T`
- `def unwrap_or_else(self, fn) -> T`
- `def map(self, fn) -> 'Option'`
- `def and_then(self, fn) -> 'Option'`
- `def filter(self, pred) -> 'Option'`
- `def ok_or(self, err: E) -> 'Result'`
- `def ok_or_else(self, err_fn) -> 'Result'`
- `def iter(self) -> Any`
- `def label(self) -> str`
- `def is_some(self) -> bool`
- `def is_none(self) -> bool`
- `def unwrap(self) -> T`
- `def expect(self, msg: str) -> T`
- `def unwrap_or(self, default: T) -> T`
- `def unwrap_or_else(self, fn) -> T`
- `def map(self, _fn) -> 'Option'`
- `def and_then(self, _fn) -> 'Option'`
- `def filter(self, _pred) -> 'Option'`
- `def ok_or(self, err: E) -> 'Result'`
- `def ok_or_else(self, err_fn) -> 'Result'`
- `def iter(self) -> Any`
- `def label(self) -> str`
- `def __init__(self, message: str, trace: str)`

### selfupgrade

- [Probe](`#class-probe`)
- [Sandbox](`#class-sandbox`)
- [UpgradeResult](`#class-upgraderesult`)

**函数**
- `def upgrade(parent: Interpreter, source: str, verify: Optional[Callable[[Sandbox], bool]]) -> UpgradeResult` — 加载 Matha 源码到沙箱试运行，通过后合并到本体解释器。

流程：
  1. 创建沙箱（克隆本体状态）
  2. 在

**方法**
- `def __init__(self, interp: Interpreter)`
- `def state(self) -> dict`
- `def env(self) -> dict`
- `def func_names(self) -> list`
- `def builtin_names(self) -> list`
- `def has(self, name: str) -> bool`
- `def func_info(self, name: str) -> Optional[dict]`
- `def __init__(self, parent: Interpreter)`
- `def run(self, source: str) -> tuple[list, list[str], Optional[str]]`
- `def call(self, name: str, *args)`
- `def diff(self) -> dict`
- `def commit(self) -> dict`
- `def rollback(self) -> None`
- `def _check(self) -> None`
- `def as_dict(self) -> dict`

### semantic

- [SemanticError](`#class-semanticerror`)
- [SemanticAnalyzer](`#class-semanticanalyzer`)

**函数**
- `def _build_builtin_symtab() -> 'SymbolTable'` — 构建包含所有内建符号的符号表（模块级缓存，仅调用一次）。
- `def analyze_ast(program: ast.Program, verbose: bool) -> list[SemanticError]` — 分析已解析的 AST，返回语义错误/警告列表。

适用于已调用 parse() 得到 program 的场景，避免重复解
- `def analyze(program: ast.Program, verbose: bool) -> list[SemanticError]` — 分析 AST，返回语义错误/警告列表。

verbose=True 时输出详细调试日志（子文件引用、文件路径、循环后缀等
- `def analyze_source(source: str, verbose: bool) -> tuple[ast.Program, list[SemanticError]]` — 从源码分析，返回 (AST, 错误列表)。

verbose=True 时输出详细调试日志，便于排查子文件引用等潜在问题

**方法**
- `def __init__(self, msg: str, severity: str, line: int, node: Any)`
- `def __repr__(self) -> str`
- `def __init__(self, verbose: bool)`
- `def _enable_logging(self) -> None`
- `def _log(self, msg: str) -> None`
- `def analyze(self, program: ast.Program) -> list[SemanticError]`
- `def _visit(self, node: Any) -> Any`
- `def _visit_generic(self, node: Any) -> Any`
- `def _error(self, msg: str, node: Any, severity: str) -> None`
- `def _visit_Program(self, node: ast.Program) -> None`
- `def _visit_ModuleDecl(self, node: ast.ModuleDecl) -> None`
- `def _visit_ImportDecl(self, node: ast.ImportDecl) -> None`
- `def _visit_StructDef(self, node: ast.StructDef) -> None`
- `def _visit_EnumDef(self, node: ast.EnumDef) -> None`
- `def _visit_AliasDef(self, node: ast.AliasDef) -> None`
- `def _visit_FuncDef(self, node: ast.FuncDef) -> None`
- `def _visit_MechUnit(self, node: ast.MechUnit) -> None`
- `def _visit_CodeBlock(self, node: ast.CodeBlock) -> None`
- `def _visit_GenStmt(self, node: ast.GenStmt) -> None`
- `def _classify_gen_stmt_content(self, content: Any) -> Optional[str]`
- `def _expr_contains_placeholder(self, expr: Any) -> bool`
- `def _check_formula_layering(self, tracker: SegmentTracker, letter_formula: Any) -> None`
- `def _extract_op_structure(self, expr: Any) -> list[str]`
- `def _collect_ops(self, expr: Any, ops: list[str]) -> None`
- `def _visit_SetUp(self, node: ast.SetUp) -> None`
- `def _visit_set_up_item(self, item: ast.SetUpItem) -> None`
- `def _visit_Binding(self, node: ast.Binding) -> None`
- `def _visit_Variable(self, node: ast.Variable) -> None`
- `def _visit_ChainStmt(self, node: ast.ChainStmt) -> None`
- `def _check_chain_trigger_condition(self, node: ast.ChainStmt) -> None`
- `def _is_simple_command(self, stmt: Any) -> bool`
- `def _has_chain_data_dependency(self, node: ast.ChainStmt) -> bool`
- `def _classify_chain_link(self, stmt: Any) -> Optional[str]`
- `def _unwrap_chain_link_content(self, stmt: Any) -> Any`
- `def _check_chain_compatibility(self, prev: str, curr: str) -> bool`
- `def _extract_output_vars(self, stmt: Any) -> list[str]`
- `def _collect_var_names(self, expr: Any, names: list[str]) -> None`
- `def _visit_OutputTrail(self, node: ast.OutputTrail) -> None`
- `def _check_seg_loop(self, seg_loop: ast.SegLoopSuffix) -> None`
- `def _check_global_loop(self, global_loop: ast.GlobalLoopSuffix) -> None`
- `def _check_subfiles(self, subfiles: list[str]) -> None`
- `def _check_file_ref(self, file_ref: str) -> None`
- `def _check_resource_reading(self, node: ast.OutputTrail) -> None`
- `def _extract_text(self, expr: Any) -> Optional[str]`
- `def _visit_CommandLiteral(self, node: ast.CommandLiteral) -> None`
- `def _visit_ReadBlock(self, node: ast.ReadBlock) -> None`
- `def _visit_NLBlock(self, node: ast.NLBlock) -> None`
- `def _visit_FileMarker(self, node: ast.FileMarker) -> None`
- `def _visit_GlobalIdStmt(self, node: ast.GlobalIdStmt) -> None`
- `def _visit_DictLiteral(self, node: ast.DictLiteral) -> None`
- `def _visit_BinaryOp(self, node: ast.BinaryOp) -> None`
- `def _visit_UnaryOp(self, node: ast.UnaryOp) -> None`
- `def _visit_Output(self, node: ast.Output) -> None`
- `def _visit_AngleExpr(self, node: ast.AngleExpr) -> None`
- `def _visit_FuncApp(self, node: ast.FuncApp) -> None`
- `def _visit_Lambda(self, node: ast.Lambda) -> None`
- `def _visit_LetBinding(self, node: ast.LetBinding) -> None`
- `def _visit_LetTupleBinding(self, node: ast.LetTupleBinding) -> None`
- `def _visit_IfExpr(self, node: ast.IfExpr) -> None`
- `def _visit_PathExpr(self, node: ast.PathExpr) -> None`
- `def _visit_IfStmt(self, node: ast.IfStmt) -> None`
- `def _visit_MatchStmt(self, node: ast.MatchStmt) -> None`
- `def _visit_LoopStep(self, node: ast.LoopStep) -> None`
- `def _visit_GoStmt(self, node: ast.GoStmt) -> None`
- `def _visit_IntegerLit(self, node: ast.IntegerLit) -> None`
- `def _visit_FloatLit(self, node: ast.FloatLit) -> None`
- `def _visit_StringLit(self, node: ast.StringLit) -> None`
- `def _visit_BoolLit(self, node: ast.BoolLit) -> None`
- `def _visit_Annotation(self, node: ast.Annotation) -> None`

### stdlib.algebra

- [Polynomial](`#class-polynomial`)

**函数**
- `def solve_linear(a: float, b: float) -> Optional[float]` — 求解一元一次方程：ax + b = 0

数学定义：
  solve_linear: ℝ × ℝ → ℝ ∪ {None
- `def solve_quadratic(a: float, b: float, c: float) -> List[complex]` — 求解一元二次方程：ax² + bx + c = 0

数学定义：
  x = (-b ± √(b²-4ac)) / (2
- `def solve_system_linear(eqs: List[Tuple[float, float, float]]) -> Optional[Tuple[float, float]]` — 求解二元一次方程组：
  a₁x + b₁y = c₁
  a₂x + b₂y = c₂

Args:
    eqs:
- `def factor_integer(n: int) -> Dict[int, int]` — 整数因式分解：n = p1^a1 × p2^a2 × ... × pk^ak

返回 {素数: 指数} 字典。

Exa
- `def factor_polynomial_simple(coeffs: List[float]) -> str` — 简单多项式因式分解（仅处理二次多项式）。

对于 ax² + bx + c，尝试因式分解为 (px + q)(rx + 
- `def simplify_expression(expr: str) -> str` — 简化表达式（基础版）。

支持：
- 合并同类项
- 消除零项
- 简化常数和
- `def expand_expression(expr: str) -> str` — 展开表达式（基础版）。

支持 (a+b)(c+d) 形式展开。
- `def poly_add(p1: List[float], p2: List[float]) -> List[float]` — 多项式加法。
- `def poly_mul(p1: List[float], p2: List[float]) -> List[float]` — 多项式乘法。
- `def poly_div(p1: List[float], p2: List[float]) -> Tuple[List[float], List[float]]` — 多项式除法：p1 = p2 × q + r

Returns:
    (quotient, remainder)
- `def poly_derivative(p: List[float]) -> List[float]` — 多项式求导。
- `def poly_integral(p: List[float], c: float) -> List[float]` — 多项式积分（不定积分）。

**方法**
- `def degree(self) -> int`
- `def evaluate(self, x: float) -> float`
- `def __str__(self) -> str`
- `def __add__(self, other: 'Polynomial') -> 'Polynomial'`
- `def __mul__(self, other: 'Polynomial') -> 'Polynomial'`

### stdlib.arithmetic

- [MathConstants](`#class-mathconstants`)

**函数**
- `def add(a: Union[int, float], b: Union[int, float]) -> Union[int, float]` — 加法运算：a + b

数学定义：
  add: ℝ × ℝ → ℝ
  add(a, b) = a + b

Args
- `def subtract(a: Union[int, float], b: Union[int, float]) -> Union[int, float]` — 减法运算：a - b

数学定义：
  subtract: ℝ × ℝ → ℝ
  subtract(a, b) = a
- `def multiply(a: Union[int, float], b: Union[int, float]) -> Union[int, float]` — 乘法运算：a × b

数学定义：
  multiply: ℝ × ℝ → ℝ
  multiply(a, b) = a
- `def divide(a: Union[int, float], b: Union[int, float]) -> Optional[float]` — 除法运算：a ÷ b

数学定义：
  divide: ℝ × (ℝ \ {0}) → ℝ
  divide(a, b)
- `def power(base: Union[int, float], exp: Union[int, float]) -> Union[int, float]` — 幂运算：base^exp

数学定义：
  power: ℝ × ℤ → ℝ
  power(base, exp) = 
- `def sqrt(x: Union[int, float]) -> Optional[float]` — 平方根运算：√x

数学定义：
  sqrt: ℝ⁺ → ℝ
  sqrt(x) = x^(1/2)

Args:
  
- `def abs_value(x: Union[int, float]) -> Union[int, float]` — 绝对值运算：|x|

数学定义：
  abs: ℝ → ℝ⁺
  abs(x) = |x|

Args:
    x: 
- `def floor(x: Union[int, float]) -> int` — 向下取整：⌊x⌋

数学定义：
  floor: ℝ → ℤ
  floor(x) = max{n ∈ ℤ | n ≤ 
- `def ceil(x: Union[int, float]) -> int` — 向上取整：⌈x⌉

数学定义：
  ceil: ℝ → ℤ
  ceil(x) = min{n ∈ ℤ | n ≥ x}
- `def round_value(x: Union[int, float], ndigits: int) -> Union[int, float]` — 四舍五入：round(x, ndigits)

数学定义：
  round: ℝ × ℕ → ℝ
  round(x, 
- `def trunc(x: Union[int, float]) -> Union[int, float]` — 截断取整：向零取整

数学定义：
  trunc: ℝ → ℤ
  trunc(x) = sign(x) × ⌊|x|⌋
- `def gcd(a: int, b: int) -> int` — 最大公约数：gcd(a, b)

数学定义：
  gcd: ℤ × ℤ → ℕ
  gcd(a, b) = max{n 
- `def lcm(a: int, b: int) -> int` — 最小公倍数：lcm(a, b)

数学定义：
  lcm: ℤ × ℤ → ℕ
  lcm(a, b) = min{n 
- `def factorial(n: int) -> int` — 阶乘：n!

数学定义：
  factorial: ℕ → ℕ
  factorial(n) = n! = ∏_{i=1
- `def is_prime(n: int) -> bool` — 素数判断：is_prime(n)

数学定义：
  is_prime: ℕ → {True, False}
  is_p
- `def prime_factors(n: int) -> List[int]` — 素因数分解：n = p1^a1 × p2^a2 × ... × pk^ak

数学定义：
  prime_factors
- `def sieve_of_eratosthenes(limit: int) -> List[int]` — 埃拉托斯特尼筛法：找出 2 到 limit 之间的所有素数

数学定义：
  sieve: ℕ → ℤ*
  sieve
- `def sin(rad: float) -> float` — 正弦函数：sin(x)
- `def cos(rad: float) -> float` — 余弦函数：cos(x)
- `def tan(rad: float) -> float` — 正切函数：tan(x)
- `def asin(x: float) -> Optional[float]` — 反正弦函数：arcsin(x)，x ∈ [-1, 1]
- `def acos(x: float) -> Optional[float]` — 反余弦函数：arccos(x)，x ∈ [-1, 1]
- `def atan(x: float) -> float` — 反正切函数：arctan(x)
- `def atan2(y: float, x: float) -> float` — 二参数反正切：arctan(y/x)
- `def log(x: float, base: float) -> Optional[float]` — 对数函数：log_base(x)
- `def log2(x: float) -> Optional[float]` — 以 2 为底的对数：log2(x)
- `def log10(x: float) -> Optional[float]` — 以 10 为底的对数：log10(x)
- `def combination(n: int, k: int) -> Optional[int]` — 组合数：C(n, k) = n! / (k! × (n-k)!)

数学定义：
  C: ℕ × ℕ → ℕ
  C(n
- `def permutation(n: int, k: int) -> Optional[int]` — 排列数：P(n, k) = n! / (n-k)!

数学定义：
  P: ℕ × ℕ → ℕ
  P(n, k) = 

**方法**
- `def list_all(cls) -> dict`

**函数**
- `def derivative(f: Callable[[float], float], x: float, h: float) -> float` — 数值求导：f'(x) ≈ [f(x+h) - f(x-h)] / (2h)

数学定义：
  f'(x) = lim(h
- `def second_derivative(f: Callable[[float], float], x: float, h: float) -> float` — 数值求二阶导数：f''(x) ≈ [f(x+h) - 2f(x) + f(x-h)] / h²

Examples:
 
- `def integral(f: Callable[[float], float], a: float, b: float, n: int) -> float` — 数值积分（辛普森法则）：∫[a,b] f(x)dx

数学定义：
  ∫[a,b] f(x)dx ≈ (h/3)[f(x
- `def numerical_integral_trapezoid(f: Callable[[float], float], a: float, b: float, n: int) -> float` — 数值积分（梯形法则）：∫[a,b] f(x)dx

更简单但精度略低。
- `def limit_forward(f: Callable[[float], float], x: float, direction: float) -> float` — 前向极限：lim(h→0+) f(x+h)

Args:
    f: 函数
    x: 趋近点
    direct
- `def limit_two_sided(f: Callable[[float], float], x: float, h: float) -> float` — 双侧极限：lim(x→x₀) f(x)
- `def taylor_series(f: Callable[[float], float], x0: float, x: float, n: int) -> float` — 泰勒级数展开：f(x) ≈ Σ f^(k)(x₀) / k! × (x-x₀)^k

Args:
    f: 函数
 
- `def _numerical_derivative(f: Callable[[float], float], x: float, n: int, h: float) -> float` — 数值计算 n 阶导数。
- `def infinite_sum(term_fn: Callable[[int], float], max_terms: int, tolerance: float) -> Tuple[float, int]` — 无穷级数求和：S = Σ term(n) for n in [0, ∞)

当连续项变化小于 tolerance 时停止
- `def newton_method(f: Callable[[float], float], x0: float, tolerance: float, max_iter: int) -> Tuple[float, int]` — 牛顿法求根：f(x) = 0

迭代公式：x_{n+1} = x_n - f(x_n) / f'(x_n)

Args:
- `def gamma(n: float) -> float` — Gamma 函数：Γ(n) = (n-1)!

使用 Lanczos 近似。
- `def beta(a: float, b: float) -> float` — Beta 函数：B(a,b) = Γ(a)Γ(b) / Γ(a+b)

### stdlib.calculus_symbolic

- [_DummySymbol](`#class-_dummysymbol`)
- [SymbolicConstants](`#class-symbolicconstants`)

**函数**
- `def symbolic_derivative(expr: str, var: str) -> str` — 符号求导：f'(x) = d/dx(f(x))

数学定义：
  f'(x) = lim(h→0) [f(x+h) - 
- `def symbolic_partial_derivative(expr: str, var: str) -> str` — 符号偏导数：∂f/∂x

Examples:
    >>> symbolic_partial_derivative("
- `def symbolic_integral(expr: str, var: str) -> str` — 符号积分：∫f(x)dx

数学定义：
  ∫f(x)dx = F(x) + C，其中 F'(x) = f(x)

Ar
- `def definite_integral(expr: str, var: str, lower: float, upper: float) -> float` — 定积分：∫[lower, upper] f(x)dx

数学定义：
  ∫[a,b] f(x)dx = F(b) - F
- `def taylor_series(expr: str, var: str, point: float, order: int) -> str` — 泰勒展开：f(x) ≈ Σ f^(n)(a)/n! * (x-a)^n

数学定义：
  f(x) = f(a) + f
- `def limit(expr: str, var: str, point: float, direction: str) -> float` — 极限计算：lim(x→point) f(x)

数学定义：
  lim(x→a) f(x) = L，当 x 趋近于 a 
- `def infinite_sum(expr: str, var: str, lower: int, upper: Optional[int]) -> Union[str, float]` — 级数求和：Σ f(n)

数学定义：
  Σ[n=lower, upper] f(n)

Args:
    expr:
- `def solve_ode(ode: str, func_name: str, var: str) -> str` — 常微分方程求解

数学定义：
  求解 dy/dx = f(x, y) 的通解或特解

Args:
    ode: 微
- `def sympy_parse(expr: str, namespace: Optional[dict]) -> any` — 解析数学表达式为 SymPy 对象

支持的运算符：
  +  -  *  /  **  (优先级与 Python 一致
- `def latex_format(expr: str) -> str` — 将表达式转换为 LaTeX 格式。

Examples:
    >>> latex_format("x**2 + 2*
- `def pretty_format(expr: str) -> str` — 将表达式转换为美观的文本格式。

Examples:
    >>> pretty_format("x**2 + 2*x
- `def batch_derivative(expressions: List[str], var: str) -> List[str]` — 批量求导

Args:
    expressions: 表达式列表
    var: 变量名

Returns:
  
- `def batch_integral(expressions: List[str], var: str) -> List[str]` — 批量积分

Args:
    expressions: 表达式列表
    var: 变量名

Returns:
  
- `def batch_taylor(expressions: List[str], var: str, point: float, order: int) -> List[str]` — 批量泰勒展开

Args:
    expressions: 表达式列表
    var: 变量名
    point:

**方法**
- `def list_all(cls) -> dict`

### stdlib.core

- [MathaType](`#class-mathatype`)

**函数**
- `def register_core_builtins(builtins: dict) -> None` — 将 Core 标准库注册到 Matha 内建函数表。

参数:
    builtins: 内建函数 dict，由 _b
- `def _int_to_roman(n: int) -> str` — 整数转罗马数字。
- `def _roman_to_int(s: str) -> int` — 罗马数字转整数。
- `def _int_factors(n: int) -> list` — 返回 n 的所有正因数。
- `def _int_is_prime(n: int) -> bool` — 判断 n 是否为素数。
- `def _str_pad_left(s: Any, width: int, char: str) -> str` — 左填充字符串。
- `def _str_pad_right(s: Any, width: int, char: str) -> str` — 右填充字符串。
- `def _str_to_bool(s: str) -> bool` — 字符串转布尔。
- `def _array_reduce(arr: list, fn, initial: Any) -> Any` — 数组归约。
- `def _array_flatten(arr: list, depth: int) -> list` — 扁平化嵌套数组。

**方法**
- `def to_int(x: Any) -> int`
- `def to_float(x: Any) -> float`
- `def to_str(x: Any) -> str`
- `def to_bool(x: Any) -> bool`
- `def to_array(x: Any) -> list`
- `def coalesce(*args) -> Any`

### stdlib.linear_algebra

- [Matrix](`#class-matrix`)

**函数**
- `def _make_cache_key(A: 'Matrix') -> tuple` — 创建缓存键。
- `def _evict_cache(cache: dict, max_size: int)` — 缓存淘汰：当缓存过大时，移除最早插入的条目。
- `def matrix_multiply(A: Matrix, B: Matrix) -> Matrix` — 矩阵乘法：C = A × B

数学定义：
  c_ij = Σ_k a_ik * b_kj

Args:
    A:
- `def matrix_transpose(A: Matrix) -> Matrix` — 矩阵转置：A^T

数学定义：
  (A^T)_ij = A_ji

Args:
    A: m×n 矩阵

Retu
- `def matrix_scale(A: Matrix, scalar: float) -> Matrix` — 矩阵数乘：c × A

Args:
    A: 矩阵
    scalar: 标量

Returns:
    c ×
- `def matrix_add(A: Matrix, B: Matrix) -> Matrix` — 矩阵加法：A + B

Args:
    A: m×n 矩阵
    B: m×n 矩阵

Returns:
    
- `def matrix_subtract(A: Matrix, B: Matrix) -> Matrix` — 矩阵减法：A - B

Args:
    A: m×n 矩阵
    B: m×n 矩阵

Returns:
    
- `def matrix_determinant(A: Matrix) -> float` — 矩阵行列式：det(A)

数学定义：
  det(A) = Σ (-1)^(i+j) * a_ij * M_ij

A
- `def matrix_trace(A: Matrix) -> float` — 矩阵迹：tr(A) = Σ a_ii

Args:
    A: n×n 方阵

Returns:
    迹
- `def matrix_rank(A: Matrix) -> int` — 矩阵秩：rank(A)

数学定义：
  rank(A) = 最大线性无关行（列）数

Args:
    A: m×n
- `def matrix_norm(A: Matrix, ord: Optional[str]) -> float` — 矩阵范数

支持的范数：
  - 'fro': Frobenius 范数
  - 'inf': 无穷范数（行和最大值）

- `def matrix_inverse(A: Matrix) -> Optional[Matrix]` — 矩阵求逆：A^(-1)

数学定义：
  A × A^(-1) = I

Args:
    A: n×n 可逆方阵


- `def matrix_eigenvalues(A: Matrix) -> List[complex]` — 矩阵特征值：det(A - λI) = 0

数学定义：
  A × v = λ × v

Args:
    A: n
- `def _qr_eigenvalues(A: Matrix) -> List[complex]` — QR 算法求特征值（简化版）。

使用 Iterated QR 分解逼近特征值。
- `def _qr_decomposition(A: List[List[float]]) -> Tuple[List[List[float]], List[List[float]]]` — QR 分解：A = QR

使用 Gram-Schmidt 正交化。
- `def svd_decompose(A: Matrix) -> Tuple[Matrix, Matrix, Matrix]` — SVD 分解：A = U Σ V^T

数学定义：
  A = U × Σ × V^T

Args:
    A: m×
- `def _is_sparse_matrix(A: Matrix, threshold: float) -> bool` — 检测矩阵是否为稀疏矩阵。

Args:
    A: m×n 矩阵
    threshold: 稀疏度阈值（默认 0.
- `def svd_decompose_sparse(A: Matrix, max_iter: int) -> Tuple[Matrix, Matrix, Matrix]` — 稀疏矩阵 SVD 分解（基于迭代法）。

使用 Lanczos 迭代或幂迭代法，适用于稀疏矩阵。

Args:
    
- `def _svd_from_eigenvalues(A: Matrix) -> Tuple[Matrix, Matrix, Matrix]` — 从特征值分解推导 SVD（仅适用于对称矩阵）。

对于对称矩阵 A：
- 奇异值 σ_i = |λ_i|（特征值的绝对值
- `def _eigenvectors_with_values(A: Matrix) -> list` — 使用幂迭代法计算特征值和特征向量。

Returns:
    list of (eigenvalue, eigenve
- `def _svd_power_iteration(A: Matrix) -> Tuple[Matrix, Matrix, Matrix]` — 使用幂迭代法近似 SVD（简化版）。
- `def lu_decompose(A: Matrix) -> Tuple[Matrix, Matrix]` — LU 分解：A = L × U

Args:
    A: n×n 方阵

Returns:
    L: 下三角矩阵

- `def cholesky_decompose(A: Matrix) -> Optional[Matrix]` — Cholesky 分解：A = L × L^T

Args:
    A: n×n 对称正定矩阵

Returns:
 
- `def solve_linear_system(A: Matrix, b: List[float]) -> Optional[List[float]]` — 求解线性方程组 Ax = b

使用高斯消元法。

Args:
    A: n×n 系数矩阵
    b: n×1 常

**方法**
- `def __post_init__(self)`
- `def zeros(cls, rows: int, cols: int) -> 'Matrix'`
- `def ones(cls, rows: int, cols: int) -> 'Matrix'`
- `def identity(cls, n: int) -> 'Matrix'`
- `def random(cls, rows: int, cols: int, scale: float) -> 'Matrix'`
- `def rows(self) -> int`
- `def cols(self) -> int`
- `def shape(self) -> Tuple[int, int]`
- `def __repr__(self) -> str`
- `def __str__(self) -> str`
- `def __add__(self, other: 'Matrix') -> 'Matrix'`
- `def __sub__(self, other: 'Matrix') -> 'Matrix'`
- `def __mul__(self, other: Union['Matrix', float]) -> 'Matrix'`
- `def __rmul__(self, other: float) -> 'Matrix'`
- `def __eq__(self, other: object) -> bool`

### stdlib.logic

- [Predicate](`#class-predicate`)
- [InferenceEngine](`#class-inferenceengine`)

**函数**
- `def AND(p: bool, q: bool) -> bool` — 逻辑与：p ∧ q
- `def OR(p: bool, q: bool) -> bool` — 逻辑或：p ∨ q
- `def NOT(p: bool) -> bool` — 逻辑非：¬p
- `def IMPLIES(p: bool, q: bool) -> bool` — 逻辑蕴含：p → q（等价于 ¬p ∨ q）
- `def IFF(p: bool, q: bool) -> bool` — 逻辑等价：p ↔ q
- `def XOR(p: bool, q: bool) -> bool` — 异或：p ⊕ q
- `def NAND(p: bool, q: bool) -> bool` — 与非：¬(p ∧ q)
- `def NOR(p: bool, q: bool) -> bool` — 或非：¬(p ∨ q)
- `def truth_table(num_vars: int, expr_fn: callable) -> List[dict]` — 生成真值表。

Args:
    num_vars: 变量数量
    expr_fn: 接受 bool 列表，返回 
- `def print_truth_table(num_vars: int, expr_fn: callable, var_names: List[str])` — 打印可读的真值表。
- `def set_union(a: Set, b: Set) -> Set` — 集合并：A ∪ B
- `def set_intersection(a: Set, b: Set) -> Set` — 集合交：A ∩ B
- `def set_difference(a: Set, b: Set) -> Set` — 集合差：A \ B
- `def set_complement(a: Set, universe: Set) -> Set` — 集合补：A' = U \ A
- `def set_symmetric_difference(a: Set, b: Set) -> Set` — 对称差：A △ B = (A \ B) ∪ (B \ A)
- `def is_subset(a: Set, b: Set) -> bool` — 子集判断：A ⊆ B
- `def is_proper_subset(a: Set, b: Set) -> bool` — 真子集判断：A ⊂ B
- `def prove_by_contradiction(assumption_fn: callable, contradiction_fn: callable) -> bool` — 归谬法证明辅助。

如果假设 P 导致矛盾，则 P 为假。

Args:
    assumption_fn: 假设函数
- `def check_tautology(num_vars: int, expr_fn: callable) -> bool` — 检查是否为永真式（重言式）。

Args:
    num_vars: 变量数
    expr_fn: 表达式函数


- `def check_contradiction(num_vars: int, expr_fn: callable) -> bool` — 检查是否为矛盾式。

Returns:
    True 如果所有赋值下表达式均为 False

**方法**
- `def evaluate(self, element) -> bool`
- `def exists(self) -> bool`
- `def forall(self) -> bool`
- `def count(self) -> int`
- `def modus_ponens(p_implies_q: bool, p: bool) -> Optional[bool]`
- `def modus_tollens(p_implies_q: bool, not_q: bool) -> Optional[bool]`
- `def hypothetical_syllogism(p_implies_q: bool, q_implies_r: bool) -> Optional[bool]`
- `def disjunctive_syllogism(p_or_q: bool, not_p: bool) -> Optional[bool]`

### stdlib.probability_stats

- [Distribution](`#class-distribution`)
- [NormalDistribution](`#class-normaldistribution`)
- [BinomialDistribution](`#class-binomialdistribution`)
- [PoissonDistribution](`#class-poissondistribution`)
- [ExponentialDistribution](`#class-exponentialdistribution`)
- [UniformDistribution](`#class-uniformdistribution`)
- [RegressionResult](`#class-regressionresult`)

**函数**
- `def mean(data: List[float]) -> float` — 样本均值：x̄ = (1/n)Σx_i

Args:
    data: 样本数据

Returns:
    均值
- `def variance(data: List[float], ddof: int) -> float` — 样本方差：s² = Σ(x_i - x̄)² / (n - ddof)

Args:
    data: 样本数据
  
- `def std(data: List[float], ddof: int) -> float` — 样本标准差：s = √s²

Args:
    data: 样本数据
    ddof: 自由度修正

Returns
- `def correlation(x: List[float], y: List[float]) -> float` — 皮尔逊相关系数：r = cov(X,Y) / (σ_X × σ_Y)

数学定义：
  r = Σ(x_i - x̄)(
- `def z_test(sample: List[float], population_mean: float, population_std: Optional[float], sample_std: Optional[float]) -> Tuple[float, float]` — Z 检验：检验样本均值与总体均值的差异

数学定义：
  Z = (x̄ - μ) / (σ/√n)

当总体标准差已知
- `def t_test(sample: List[float], population_mean: float) -> Tuple[float, float]` — t 检验：检验样本均值与假设均值的差异

数学定义：
  t = (x̄ - μ) / (s/√n)

当总体标准差未知
- `def chi_square_test(observed: List[int], expected: Optional[List[float]]) -> Tuple[float, float]` — 卡方检验：检验观测频数与期望频数的差异

数学定义：
  χ² = Σ(O_i - E_i)² / E_i

Args:
- `def _chi_square_cdf(x: float, k: int) -> float` — 卡方分布累积分布函数（近似）。
- `def _lower_incomplete_gamma(a: float, x: float) -> float` — 下不完全伽马函数（连分数展开）。
- `def _upper_incomplete_gamma(a: float, x: float) -> float` — 上不完全伽马函数（连分数展开）。
- `def linear_regression(x: List[float], y: List[float]) -> RegressionResult` — 一元线性回归：y = β₀ + β₁x + ε

数学定义：
  β₁ = Σ(x_i - x̄)(y_i - ȳ) /
- `def polynomial_regression(x: List[float], y: List[float], degree: int) -> RegressionResult` — 多项式回归：y = β₀ + β₁x + β₂x² + ... + β_dx^d

数学定义：
  通过最小二乘法求解回
- `def _matrix_transpose_multiply(A: List[List[float]]) -> List[List[float]]` — 矩阵转置乘矩阵。
- `def _matrix_vector_multiply_transpose(A: List[List[float]], v: List[float]) -> List[float]` — 矩阵转置乘向量。
- `def _solve_linear_system(A: List[List[float]], b: List[float]) -> List[float]` — 高斯消元求解线性方程组。
- `def _matrix_vector_multiply(A: List[List[float]], v: List[float], row_idx: int) -> float` — 矩阵乘向量（指定行）。

**方法**
- `def __post_init__(self)`
- `def pdf(self, x: float) -> float`
- `def cdf(self, x: float) -> float`
- `def ppf(self, p: float) -> float`
- `def mean(self) -> float`
- `def variance(self) -> float`
- `def std(self) -> float`
- `def __post_init__(self)`
- `def pmf(self, k: int) -> float`
- `def cdf(self, k: int) -> float`
- `def mean(self) -> float`
- `def variance(self) -> float`
- `def __post_init__(self)`
- `def pmf(self, k: int) -> float`
- `def cdf(self, k: int) -> float`
- `def mean(self) -> float`
- `def variance(self) -> float`
- `def std(self) -> float`
- `def __post_init__(self)`
- `def pdf(self, x: float) -> float`
- `def cdf(self, x: float) -> float`
- `def ppf(self, p: float) -> float`
- `def mean(self) -> float`
- `def variance(self) -> float`
- `def std(self) -> float`
- `def __post_init__(self)`
- `def pdf(self, x: float) -> float`
- `def cdf(self, x: float) -> float`
- `def ppf(self, p: float) -> float`
- `def mean(self) -> float`
- `def variance(self) -> float`
- `def std(self) -> float`
- `def sample(self, n: int) -> List[float]`

**函数**
- `def _register_standard_library(builtins: dict) -> None` — 将标准库函数注册为 Matha 内建。
- `def register_all(builtins: dict) -> None` — 

### symbol_codegen

- [CodeGenerator](`#class-codegenerator`)
- [PythonGenerator](`#class-pythongenerator`)
- [JavaScriptGenerator](`#class-javascriptgenerator`)
- [CGenerator](`#class-cgenerator`)
- [MathaGenerator](`#class-mathagenerator`)
- [DriverSpec](`#class-driverspec`)
- [DriverGenerator](`#class-drivergenerator`)
- [MathaCodeGen](`#class-mathacodegen`)

**函数**
- `def get_codegen() -> MathaCodeGen` — 
- `def codegen_python(expr: str, **kwargs) -> str` — 
- `def codegen_javascript(expr: str, **kwargs) -> str` — 
- `def codegen_c(expr: str, **kwargs) -> str` — 

**方法**
- `def __init__(self, lang: str, indent: str)`
- `def emit(self, line: str)`
- `def block(self, body_fn)`
- `def get_code(self) -> str`
- `def __init__(self, func_name: str, imports: List[str])`
- `def _emit_header(self)`
- `def _emit_body(self)`
- `def add_expression(self, expr_str: str, var_name: str)`
- `def _expr_to_python(self, expr: str) -> str`
- `def __init__(self, func_name: str, as_module: bool)`
- `def _emit_header(self)`
- `def _emit_body(self)`
- `def add_expression(self, expr_str: str, var_name: str)`
- `def _expr_to_javascript(self, expr: str) -> str`
- `def __init__(self, func_name: str, return_type: str)`
- `def _emit_header(self)`
- `def _emit_body(self)`
- `def add_expression(self, expr_str: str, var_name: str)`
- `def _expr_to_c(self, expr: str) -> str`
- `def __init__(self, func_name: str)`
- `def _emit_header(self)`
- `def _emit_body(self)`
- `def add_expression(self, expr_str: str, var_name: str)`
- `def __init__(self)`
- `def generate(self, spec: DriverSpec) -> str`
- `def generate_library(self, functions: List[Dict[str, str]], lang: str) -> str`
- `def __init__(self)`
- `def python(self, expr: str, func_name: str, imports: List[str]) -> str`
- `def javascript(self, expr: str, func_name: str, as_module: bool) -> str`
- `def c(self, expr: str, func_name: str, return_type: str) -> str`
- `def matha(self, expr: str, func_name: str) -> str`
- `def generate_driver(self, spec: DriverSpec) -> str`
- `def generate_library(self, functions: List[Dict[str, str]], lang: str) -> str`

### symbolic

- [ASTNodeType](`#class-astnodetype`)
- [ASTNode](`#class-astnode`)
- [Expr](`#class-expr`)
- [Num](`#class-num`)
- [Var](`#class-var`)
- [Neg](`#class-neg`)
- [Add](`#class-add`)
- [Sub](`#class-sub`)
- [Mul](`#class-mul`)
- [Div](`#class-div`)
- [Pow](`#class-pow`)
- [Log](`#class-log`)
- [FuncCall](`#class-funccall`)
- [SymbolicParser](`#class-symbolicparser`)
- [LISPExpr](`#class-lispexpr`)

**函数**
- `def to_expr(obj) -> Expr` — 将 Python 值转换为 Expr 对象。
- `def symbolic(var: str) -> Var` — 快捷创建符号变量。
- `def symbol_expr(text: str) -> Expr` — 解析符号表达式字符串。
- `def simplify_expr(expr: Expr) -> Expr` — 简化表达式。
- `def diff_expr(expr: Expr, var: str) -> Expr` — 对变量求导。
- `def eval_expr(expr: Expr, **bindings) -> float` — 数值求值。
- `def ast_to_dict(expr: Expr) -> dict` — 表达式转 AST 字典（用于代码生成）。
- `def _expr_to_ast(expr: Expr) -> ASTNode` — Expr → ASTNode 转换。
- `def symbolic_calc(expression: str, **bindings) -> dict` — 统一符号计算接口。

**方法**
- `def to_dict(self) -> dict`
- `def __str__(self) -> str`
- `def __repr__(self)`
- `def __add__(self, other)`
- `def __radd__(self, other)`
- `def __sub__(self, other)`
- `def __rsub__(self, other)`
- `def __mul__(self, other)`
- `def __rmul__(self, other)`
- `def __truediv__(self, other)`
- `def __rtruediv__(self, other)`
- `def __pow__(self, other)`
- `def __rpow__(self, other)`
- `def __neg__(self)`
- `def simplify(self) -> 'Expr'`
- `def diff(self, var: str) -> 'Expr'`
- `def evaluate(self, bindings: Dict[str, float]) -> float`
- `def substitute(self, var: str, value: 'Expr') -> 'Expr'`
- `def __str__(self)`
- `def __repr__(self)`
- `def simplify(self)`
- `def evaluate(self, _bindings)`
- `def substitute(self, *a)`
- `def diff(self, var)`
- `def __str__(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def __init__(self, expr)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def __init__(self, name, args)`
- `def simplify(self)`
- `def evaluate(self, bindings)`
- `def substitute(self, var, value)`
- `def diff(self, var)`
- `def __str__(self)`
- `def parse(self, text: str) -> Expr`
- `def _parse_expr(self, text: str) -> Expr`
- `def _parse_term(self, text: str) -> Expr`
- `def _parse_power(self, text: str) -> Expr`
- `def _parse_primary(self, text: str) -> Expr`
- `def _split_top_level(self, text: str, separators: List[str]) -> List[str]`
- `def _split_with_ops(self, text: str, separators: List[str]) -> List[tuple]`
- `def parse(s: str)`
- `def _tokenize(s: str) -> List[str]`
- `def _parse_tokens(tokens, pos)`
- `def eval(expr, env: dict) -> Any`
- `def __init__(self, *args)`
- `def __call__(self, *args)`
- `def __str__(self)`

### symbols

- [Symbol](`#class-symbol`)
- [Scope](`#class-scope`)
- [SymbolTable](`#class-symboltable`)
- [SegmentTracker](`#class-segmenttracker`)

**函数**
- `def detect_resource_type(text: str) -> str` — 识别命令/输出内容中的资源类型（M3.2 一等能力）。

判断规则（按优先级）：
    1. 包含 :// → URL

**方法**
- `def define(self, sym: Symbol) -> bool`
- `def resolve(self, name: str) -> Optional[Symbol]`
- `def resolve_local(self, name: str) -> Optional[Symbol]`
- `def child(self, name: str) -> 'Scope'`
- `def __init__(self)`
- `def push(self, name: str) -> Scope`
- `def pop(self) -> Scope`
- `def define(self, name: str, kind: str, decl: Any, line: int, type_info: Any, is_placeholder: bool) -> bool`
- `def resolve(self, name: str) -> Optional[Symbol]`
- `def resolve_local(self, name: str) -> Optional[Symbol]`
- `def record_step(self, step: str, node: Any) -> Optional[str]`
- `def get_step(self, step: str) -> Optional[Any]`

**函数**
- `def _get_arity(fn) -> int` — 推断函数的参数数。
- `def _classify_function(name: str) -> tuple[str, str]` — 根据函数名推断所属领域和分类。
- `def export_symtab(interp: Interpreter) -> list[dict]` — 导出 Matha 的完整符号表。
- `def export_symtab_json(interp: Interpreter, indent: int) -> str` — 导出符号表为 JSON。
- `def export_symtab_d_ts(interp: Interpreter) -> str` — 导出符号表为 TypeScript .d.ts 类型声明。
- `def export_symtab_markdown(interp: Interpreter) -> str` — 导出符号表为 Markdown 表格。
- `def export_symtab_python(interp: Interpreter) -> str` — 导出符号表为 Python 可导入模块字符串。

**函数**
- `def old_to_new(src: str, title: str, code_id: str) -> str` — 旧模板 → 新模板

将 #N：{ ... } 形式的多段代码合并为 #：{ ... } 单代码块。
段号被去除，语句被
- `def new_to_old(src: str) -> str` — 新模板 → 旧模板

将 [？|N]#：{ ... } 单代码块拆分为 #：{ } #2：{ } ... 多段代码。
绑
- `def convert(src: str, direction: str) -> Tuple[str, str]` — 自动检测并转换模板格式

direction: "auto" | "old_to_new" | "new_to_old"

### tokens

- [TokenType](`#class-tokentype`)
- [Token](`#class-token`)

**方法**
- `def __init__(self, type: TokenType, value: str, line: int, col: int)`
- `def __repr__(self) -> str`
- `def __eq__(self, other) -> bool`

### tools

- [FormatConfig](`#class-formatconfig`)
- [MathaFormatter](`#class-mathaformatter`)
- [LintIssue](`#class-lintissue`)
- [MathaLinter](`#class-mathalinter`)
- [MathaTestCase](`#class-mathatestcase`)
- [MathaProfiler](`#class-mathaprofiler`)
- [MathaREPL](`#class-matharepl`)

**函数**
- `def test(name: str, fn: callable) -> MathaTestCase` — 创建并运行测试用例。
- `def run_tests(test_cases: list[MathaTestCase]) -> dict` — 运行所有测试用例。

**方法**
- `def __init__(self, config: FormatConfig) -> None`
- `def format(self, source: str) -> str`
- `def check_format(self, source: str) -> list[str]`
- `def __init__(self) -> None`
- `def lint(self, source: str) -> list[LintIssue]`
- `def get_summary(self, issues: list[LintIssue]) -> str`
- `def __init__(self, name: str) -> None`
- `def assert_equal(self, actual: Any, expected: Any, msg: str) -> bool`
- `def assert_true(self, value: Any, msg: str) -> bool`
- `def assert_raises(self, exc_type: type, fn: callable, *args, **kwargs) -> bool`
- `def run_all(self) -> dict`
- `def __init__(self) -> None`
- `def enable(self) -> None`
- `def disable(self) -> None`
- `def profile_call(self, name: str, fn: callable, *args, **kwargs) -> Any`
- `def report(self) -> str`
- `def __init__(self) -> None`
- `def run(self, code: str) -> Any`
- `def history(self, n: int) -> list[str]`
- `def clear(self) -> None`

### tools.doc_generator

- [FunctionDoc](`#class-functiondoc`)
- [ClassDoc](`#class-classdoc`)
- [ModuleDoc](`#class-moduledoc`)
- [MathaDocGenerator](`#class-mathadocgenerator`)

**方法**
- `def __init__(self, source_dirs: List[str], exclude_patterns: Optional[List[str]])`
- `def discover_modules(self) -> List[Path]`
- `def extract_docstring(self, obj: Any) -> str`
- `def parse_docstring(self, docstring: str) -> Dict[str, Any]`
- `def inspect_function(self, func) -> Optional[FunctionDoc]`
- `def _format_type_annotation(self, annotation) -> str`
- `def inspect_class(self, cls) -> Optional[ClassDoc]`
- `def process_module(self, module_path: Path) -> Optional[ModuleDoc]`
- `def generate(self) -> Dict[str, ModuleDoc]`
- `def generate_markdown(self, output_path: str) -> str`
- `def generate_html(self, output_path: str) -> str`
- `def generate_json(self, output_path: str) -> str`

### tools.flame_graph

- [CallFrame](`#class-callframe`)
- [FlameGraphData](`#class-flamegraphdata`)
- [FlameGraphBuilder](`#class-flamegraphbuilder`)
- [FlameGraphRenderer](`#class-flamegraphrenderer`)

**函数**
- `def _get_color(depth: int, palette: str) -> str` — 根据深度获取颜色。
- `def build_flame_data_from_profiler(profiler_data: dict) -> FlameGraphData` — 从 MathaProfiler 的统计数据结构构建火焰图数据。
- `def build_flame_data_from_trace(trace: List[dict]) -> FlameGraphData` — 从原始调用跟踪列表构建火焰图数据。

trace 格式：
  [{"name": "func_a", "start": 
- `def gen_call_stack(depth, remaining)` — 

**方法**
- `def to_dict(self) -> dict`
- `def __init__(self, palette: str, max_depth: int)`
- `def build(self, call_stack: List[Tuple[str, float]], total_duration: float) -> FlameGraphData`
- `def _layout(self, frame: CallFrame, start: float, end: float)`
- `def _flatten(self, frame: CallFrame) -> List[CallFrame]`
- `def render(self, data: FlameGraphData, output_path: str) -> str`
- `def render_to_string(self, data: FlameGraphData) -> str`
- `def _build_html(title: str, total_ms: float, frame_count: int, gen_time: str, data_json: str, palette_json: str) -> str`

### tools.perf_profiler

- [ProfileResult](`#class-profileresult`)
- [PerformanceReport](`#class-performancereport`)
- [MathaProfiler](`#class-mathaprofiler`)

**函数**
- `def profile(func: Callable) -> Callable` — 简化的 profile 装饰器
- `def benchmark(name: str, func: Callable, *args, **kwargs) -> Dict[str, float]` — 简化的 benchmark 函数
- `def sample_function(n: int) -> int` — 

**方法**
- `def __init__(self, enable_memory: bool, enable_jit: bool)`
- `def start(self) -> None`
- `def stop(self) -> None`
- `def profile(self, func: Callable) -> Callable`
- `def benchmark(self, name: str, func: Callable, *args, **kwargs) -> Dict[str, float]`
- `def compare_jit(self, name: str, func: Callable, *args, **kwargs) -> Dict[str, float]`
- `def get_memory_usage(self) -> Dict[str, float]`
- `def generate_html_report(self, output_path: str) -> str`
- `def generate_json_report(self, output_path: str) -> str`
- `def _generate_summary(self) -> Dict[str, Any]`
- `def _generate_results_html(self) -> str`
- `def _generate_benchmarks_html(self) -> str`
- `def _generate_jit_html(self) -> str`
- `def run_performance_test(self, test_name: str, func: Callable, *args, **kwargs) -> bool`
- `def clear_results(self) -> None`
- `def print_summary(self) -> None`

### transpiler

- [TranspileError](`#class-transpileerror`)
- [PythonTranspiler](`#class-pythontranspiler`)
- [JavaScriptTranspiler](`#class-javascripttranspiler`)

**函数**
- `def json_repr(s: str) -> str` — 把字符串转为 JSON 字符串表示（JS 兼容）。
- `def transpile(source: str, target: str) -> str` — 把 Matha 源码转译为目标语言源码。

Args:
    source: Matha 源码
    target:

**方法**
- `def __init__(self)`
- `def transpile(self, source: str) -> str`
- `def _pad(self) -> str`
- `def _transpile_decl(self, decl) -> None`
- `def _transpile_func(self, fn: ast.FuncDef) -> None`
- `def _transpile_stmt(self, stmt) -> None`
- `def _transpile_expr(self, expr) -> str`
- `def _map_op(op: str) -> str`
- `def __init__(self)`
- `def transpile(self, source: str) -> str`
- `def _pad(self) -> str`
- `def _transpile_decl(self, decl) -> None`
- `def _transpile_func(self, fn: ast.FuncDef) -> None`
- `def _transpile_stmt(self, stmt) -> None`
- `def _transpile_expr(self, expr) -> str`
- `def _map_op(op: str) -> str`

### transpiler_ts

- [TranspileError](`#class-transpileerror`)
- [TypeScriptTranspiler](`#class-typescripttranspiler`)

**函数**
- `def transpile(source: str, target: str) -> str` — 把 Matha 源码转译为目标语言源码。

Args:
    source: Matha 源码
    target:

**方法**
- `def __init__(self, add_types: bool)`
- `def transpile(self, source: str) -> str`
- `def _pad(self) -> str`
- `def _transpile_decl(self, decl) -> None`
- `def _transpile_func(self, fn: ast.FuncDef) -> None`
- `def _transpile_stmt(self, stmt) -> None`
- `def _transpile_expr(self, expr) -> str`
- `def _infer_type(self, expr) -> str | None`
- `def _infer_param_type(self, param) -> str`
- `def _map_op(op: str) -> str`

### tree_sitter_backends

- [ASTNode](`#class-astnode`)
- [RustParser](`#class-rustparser`)
- [GoParser](`#class-goparser`)
- [JSParser](`#class-jsparser`)
- [CParser](`#class-cparser`)

**函数**
- `def get_parser(language: str) -> Any` — 根据语言返回对应的解析器实例
- `def is_cext_available() -> bool` — 检查 C 扩展是否可用（当前始终为 False，使用内联解析器）
- `def parse_source(language: str, source: str) -> ASTNode` — 便捷函数：解析指定语言的源码

**方法**
- `def to_dict(self) -> Dict[str, Any]`
- `def __len__(self) -> int`
- `def __bool__(self) -> bool`
- `def parse(self, source: str) -> ASTNode`
- `def parse(self, source: str) -> ASTNode`
- `def parse(self, source: str) -> ASTNode`
- `def parse(self, source: str) -> ASTNode`

### tree_sitter_cext

- [CST_RustParser](`#class-cst_rustparser`)
- [CST_GoParser](`#class-cst_goparser`)
- [CST_JSParser](`#class-cst_jsparser`)
- [CST_CParser](`#class-cst_cparser`)
- [CSTNode](`#class-cstnode`)

**函数**
- `def _try_load_tree_sitter() -> bool` — 尝试加载 tree-sitter C 扩展。
- `def _cst_from_tree(tree) -> 'CSTNode'` — 将 tree-sitter Tree 转换为统一 CSTNode。
- `def _extract_fields(node) -> dict` — 从 tree-sitter 节点提取命名字段。
- `def get_cst_parser(language: str)` — 根据语言返回对应的 CST 解析器。

**方法**
- `def __init__(self)`
- `def parse(self, source: str) -> 'CSTNode'`
- `def __init__(self)`
- `def parse(self, source: str) -> 'CSTNode'`
- `def __init__(self)`
- `def parse(self, source: str) -> 'CSTNode'`
- `def __init__(self)`
- `def parse(self, source: str) -> 'CSTNode'`
- `def __init__(self, type: str, value: str, children: list, fields: dict)`
- `def child(self, type_filter: Optional[str]) -> Optional['CSTNode']`
- `def children_by(self, type_filter: str) -> list['CSTNode']`
- `def leaf_text(self) -> str`
- `def __repr__(self) -> str`

### type_system

- [TypeKind](`#class-typekind`)
- [Type](`#class-type`)
- [TypeEnv](`#class-typeenv`)
- [TypeError](`#class-typeerror`)
- [TypeInferencer](`#class-typeinferencer`)
- [TypeChecker](`#class-typechecker`)
- [GenericType](`#class-generictype`)

**方法**
- `def __str__(self) -> str`
- `def __repr__(self) -> str`
- `def list_of(element_type: 'Type') -> 'Type'`
- `def dict_of(key_type: 'Type', value_type: 'Type') -> 'Type'`
- `def tuple_of(*types) -> 'Type'`
- `def function(params: tuple['Type', ...], return_type: 'Type') -> 'Type'`
- `def option(element_type: 'Type') -> 'Type'`
- `def is_numeric(self) -> bool`
- `def is_container(self) -> bool`
- `def element_type(self) -> Optional['Type']`
- `def value_type(self) -> Optional['Type']`
- `def define_var(self, name: str, typ: Type) -> None`
- `def define_func(self, name: str, typ: Type) -> None`
- `def get_var(self, name: str) -> Optional[Type]`
- `def get_func(self, name: str) -> Optional[Type]`
- `def merge(self, other: 'TypeEnv') -> None`
- `def copy(self) -> 'TypeEnv'`
- `def __str__(self) -> str`
- `def __init__(self) -> None`
- `def _init_builtin_types(self) -> None`
- `def infer(self, program: Any) -> list[TypeError]`
- `def _infer_decl(self, decl: Any) -> None`
- `def _infer_binding(self, binding: Any) -> None`
- `def _infer_func_def(self, func_def: Any) -> None`
- `def _infer_expr(self, expr: Any) -> Type`
- `def _infer_binary_op(self, expr: Any) -> Type`
- `def _infer_func_app(self, expr: Any) -> Type`
- `def _infer_stmt(self, stmt: Any) -> None`
- `def _get_var_name(self, target: Any) -> Optional[str]`
- `def _type_compatible(self, source: Type, target: Type) -> bool`
- `def _infer_type_expr(self, type_expr: Any) -> Type`
- `def check(cls, value: Any, expected_type_name: str, var_name: str) -> bool`
- `def check_numeric(cls, value: Any, var_name: str) -> bool`
- `def coerce(cls, value: Any, target_type: str) -> Any`
- `def bind(cls, type_var: str, concrete_type: Type) -> None`
- `def resolve(cls, type_expr: Type) -> Type`
- `def infer_generic(cls, func_name: str, arg_types: tuple[Type, ...]) -> dict[str, Type]`

### type_system_v2

- [TypeKind](`#class-typekind`)
- [Type](`#class-type`)
- [SubtypeRegistry](`#class-subtyperegistry`)
- [RefinementChecker](`#class-refinementchecker`)
- [TypeConstraint](`#class-typeconstraint`)
- [TypeError](`#class-typeerror`)
- [EnhancedTypeInferencer](`#class-enhancedtypeinferencer`)
- [TypeChecker](`#class-typechecker`)

**方法**
- `def __str__(self) -> str`
- `def __repr__(self) -> str`
- `def list_of(element_type: 'Type') -> 'Type'`
- `def dict_of(key_type: 'Type', value_type: 'Type') -> 'Type'`
- `def tuple_of(*types) -> 'Type'`
- `def function(params: Tuple['Type', ...], return_type: 'Type') -> 'Type'`
- `def option(element_type: 'Type') -> 'Type'`
- `def union(*types) -> 'Type'`
- `def refinement(name: str, predicate: str) -> 'Type'`
- `def dependent(param_name: str, param_type: 'Type', body_type: 'Type') -> 'Type'`
- `def subtype(name: str, supertype: 'Type') -> 'Type'`
- `def enum(name: str, members: List[str]) -> 'Type'`
- `def alias(name: str, underlying: 'Type') -> 'Type'`
- `def __init__(self)`
- `def add_subtype(self, subtype: str, supertype: str) -> None`
- `def is_subtype_of(self, subtype: str, supertype: str) -> bool`
- `def get_supertypes(self, subtype: str) -> Set[str]`
- `def get_hierarchy(self, type_name: str) -> List[str]`
- `def check(self, value: Any, predicate: str) -> bool`
- `def __str__(self) -> str`
- `def __init__(self)`
- `def _init_builtin_types(self) -> None`
- `def define_alias(self, name: str, type_expr: Type) -> None`
- `def define_enum(self, name: str, members: List[str]) -> None`
- `def add_subtype(self, subtype: str, supertype: str) -> None`
- `def infer(self, expr: str, context: Dict[str, Type]) -> Type`
- `def check_subtype(self, subtype: Type, supertype: Type) -> bool`
- `def check_refinement(self, value: Any, predicate: str) -> bool`
- `def infer_program(self, program: str) -> List[Type]`
- `def __init__(self, inferencer: EnhancedTypeInferencer)`
- `def check(self, expr: str, expected: Type, context: Dict[str, Type]) -> bool`
- `def check_call(self, func_name: str, args: List[Any], expected_sig: Type) -> bool`

### typesystem_v2

- [TypeKind](`#class-typekind`)
- [Type](`#class-type`)
- [Constraint](`#class-constraint`)
- [ConstraintSolver](`#class-constraintsolver`)
- [PatternMatchInferencer](`#class-patternmatchinferencer`)
- [TypeConstraint](`#class-typeconstraint`)
- [StrictTypeChecker](`#class-stricttypechecker`)
- [TypeEnv](`#class-typeenv`)
- [EnhancedTypeInferencer](`#class-enhancedtypeinferencer`)

**方法**
- `def __str__(self) -> str`
- `def int_type() -> 'Type'`
- `def float_type() -> 'Type'`
- `def string_type() -> 'Type'`
- `def bool_type() -> 'Type'`
- `def void_type() -> 'Type'`
- `def any_type() -> 'Type'`
- `def unknown_type() -> 'Type'`
- `def list_of(element: 'Type') -> 'Type'`
- `def dict_of(key: 'Type', value: 'Type') -> 'Type'`
- `def tuple_of(*types) -> 'Type'`
- `def option_of(element: 'Type') -> 'Type'`
- `def function(params: tuple['Type', ...], ret: 'Type') -> 'Type'`
- `def with_constraints(self, *constraints) -> 'Type'`
- `def is_numeric(self) -> bool`
- `def is_container(self) -> bool`
- `def element_type(self) -> Optional['Type']`
- `def has_constraint(self, constraint: str) -> bool`
- `def __eq__(self, other: object) -> bool`
- `def solve(self) -> bool`
- `def _is_subtype(self, sub: Type, super_type: Type) -> bool`
- `def __init__(self) -> None`
- `def add(self, constraint: Constraint) -> None`
- `def solve(self) -> list[str]`
- `def infer_from_call(self, func_type: Type, arg_types: list[Type]) -> dict[str, Type]`
- `def infer(self, scrutinee_type: Type, patterns: list[tuple[Type, Type]]) -> list[Type]`
- `def _compatible(self, t1: Type, t2: Type) -> bool`
- `def _common_type(self, types: list[Type]) -> Type`
- `def satisfies(cls, type_name: str, constraint: str) -> bool`
- `def __init__(self, strict: bool) -> None`
- `def check(self, value: Any, expected: Type, var_name: str) -> bool`
- `def get_errors(self) -> list[str]`
- `def clear(self) -> None`
- `def define_var(self, name: str, typ: Type) -> None`
- `def define_func(self, name: str, typ: Type) -> None`
- `def set_constraint(self, name: str, constraints: list[str]) -> None`
- `def get_var(self, name: str) -> Optional[Type]`
- `def get_func(self, name: str) -> Optional[Type]`
- `def resolve_generic(self, typ: Type) -> Type`
- `def copy(self) -> 'TypeEnv'`
- `def __init__(self) -> None`
- `def infer(self, program: Any) -> list[str]`
- `def _init_builtins(self) -> None`
- `def _infer_decl(self, decl: Any) -> None`
- `def _infer_binding(self, binding: Any) -> None`
- `def _infer_func_def(self, func_def: Any) -> None`
- `def _infer_expr(self, expr: Any) -> Type`
- `def _infer_binary(self, expr: Any) -> Type`
- `def _infer_func_app(self, expr: Any) -> Type`
- `def _infer_stmt(self, stmt: Any) -> None`
- `def _infer_type_expr(self, type_expr: Any) -> Type`
- `def _compatible(self, source: Type, target: Type) -> bool`
- `def check_runtime(self, value: Any, expected: Type, var_name: str) -> bool`
- `def get_errors(self) -> list[str]`

### typesystem_v2_fixed

- [Type](`#class-type`)
- [Constraint](`#class-constraint`)
- [ConstraintSolver](`#class-constraintsolver`)

**方法**
- `def int_type(cls) -> 'Type'`
- `def float_type(cls) -> 'Type'`
- `def string_type(cls) -> 'Type'`
- `def bool_type(cls) -> 'Type'`
- `def void_type(cls) -> 'Type'`
- `def any_type(cls) -> 'Type'`
- `def unknown_type(cls) -> 'Type'`
- `def list_type(cls, elem: 'Type') -> 'Type'`
- `def dict_type(cls, key: 'Type', value: 'Type') -> 'Type'`
- `def func_type(cls, params: list['Type'], return_type: 'Type') -> 'Type'`
- `def with_constraints(self, *constraints) -> 'Type'`
- `def is_numeric(self) -> bool`
- `def is_container(self) -> bool`
- `def element_type(self) -> Optional['Type']`
- `def has_constraint(self, constraint: str) -> bool`
- `def __eq__(self, other: object) -> bool`
- `def __hash__(self) -> int`
- `def __repr__(self) -> str`
- `def solve(self) -> bool`
- `def _is_subtype(self, sub: Type, super_type: Type) -> bool`
- `def __repr__(self) -> str`
- `def __init__(self) -> None`
- `def add(self, constraint: Constraint) -> None`
- `def add_eq(self, left: Type, right: Type) -> None`
- `def add_subtype(self, sub: Type, super_type: Type) -> None`
- `def solve(self) -> list[str]`
- `def solve_and_unify(self) -> dict[str, Type]`
- `def check_type(self, expr_type: Type, expected_type: Type) -> bool`

### visual_editor.node_executor

- [ExecutionStatus](`#class-executionstatus`)
- [ExecutionError](`#class-executionerror`)
- [NodeExecutionResult](`#class-nodeexecutionresult`)
- [GraphMetrics](`#class-graphmetrics`)
- [NodeExecutor](`#class-nodeexecutor`)

**函数**
- `def get_executor() -> NodeExecutor` — 获取全局执行器实例

**方法**
- `def __init__(self, message: str, node_id: Optional[str], port_name: Optional[str])`
- `def __init__(self, node_id: str, node_type: str, status: ExecutionStatus, output: Optional[Dict[str, Any]], error: Optional[str], duration_ms: float)`
- `def to_dict(self) -> Dict[str, Any]`
- `def __init__(self)`
- `def to_dict(self) -> Dict[str, Any]`
- `def __init__(self)`
- `def add_node(self, node_id: str, node_data: Dict[str, Any]) -> None`
- `def add_connection(self, from_node: str, from_port: str, to_node: str, to_port: str) -> None`
- `def remove_node(self, node_id: str) -> bool`
- `def remove_connection(self, from_node: str, from_port: str, to_node: str, to_port: str) -> bool`
- `def clear(self) -> None`
- `def validate_graph(self) -> Tuple[bool, Optional[str]]`
- `def _detect_cycle(self) -> Tuple[bool, List[str]]`
- `def compute_execution_order(self) -> List[str]`
- `def execute(self, node_executor: Optional[Dict[str, Any]], context: Optional[Dict[str, Any]]) -> Dict[str, Any]`
- `def execute_incremental(self, changed_node_ids: Set[str], node_executor: Optional[Dict[str, Any]], context: Optional[Dict[str, Any]]) -> Dict[str, Any]`
- `def _get_affected_nodes(self, changed_node_ids: Set[str]) -> Set[str]`
- `def _collect_inputs(self, node_id: str, node_outputs: Dict[str, Dict]) -> Dict[str, Any]`
- `def _default_execute(self, node_data: Dict, inputs: Dict, context: Dict) -> Dict`
- `def _execute_math_node(self, node_type: str, inputs: Dict) -> Dict`
- `def _execute_logic_node(self, node_type: str, inputs: Dict) -> Dict`
- `def _execute_stats_node(self, node_type: str, inputs: Dict) -> Dict`
- `def _execute_matrix_node(self, node_type: str, inputs: Dict) -> Dict`
- `def _execute_special_node(self, node_type: str, node_data: Dict, inputs: Dict) -> Dict`
- `def on_node_execute(self, callback) -> None`
- `def on_execution_complete(self, callback) -> None`
- `def on_error(self, callback) -> None`
- `def nodes(self) -> Dict[str, Any]`
- `def connections(self) -> List[Dict[str, str]]`
- `def execution_order(self) -> List[str]`
- `def metrics(self) -> GraphMetrics`
- `def execution_history(self) -> List[NodeExecutionResult]`
- `def to_dict(self) -> Dict[str, Any]`
- `def from_dict(cls, data: Dict[str, Any]) -> 'NodeExecutor'`

### visual_editor.node_types

- [NodeType](`#class-nodetype`)
- [PortDefinition](`#class-portdefinition`)
- [NodeDefinition](`#class-nodedefinition`)
- [NodeRegistry](`#class-noderegistry`)
- [Node](`#class-node`)

**函数**
- `def _create_math_node(node_type: NodeType, label: str, category: str, **kwargs) -> NodeDefinition` — 创建数学运算节点
- `def _create_logic_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition` — 创建逻辑运算节点
- `def _create_constant_node(node_type: NodeType, label: str, value: Any, **kwargs) -> NodeDefinition` — 创建常量节点
- `def _create_variable_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition` — 创建变量节点
- `def _create_input_node(**kwargs) -> NodeDefinition` — 创建输入节点
- `def _create_output_node(**kwargs) -> NodeDefinition` — 创建输出节点
- `def _create_if_node(**kwargs) -> NodeDefinition` — 创建条件分支节点
- `def _create_sequence_node(**kwargs) -> NodeDefinition` — 创建序列节点
- `def _create_stats_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition` — 创建统计节点
- `def _create_matrix_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition` — 创建矩阵运算节点
- `def register_all_nodes() -> None` — 注册所有节点

**方法**
- `def input_count(self) -> int`
- `def output_count(self) -> int`
- `def register(cls, definition: NodeDefinition) -> None`
- `def get(cls, node_type: str) -> Optional[NodeDefinition]`
- `def get_all(cls) -> Dict[str, NodeDefinition]`
- `def get_by_category(cls, category: str) -> List[NodeDefinition]`
- `def search(cls, keyword: str) -> List[NodeDefinition]`
- `def __init__(self, node_type: NodeType, position: tuple, config: Optional[Dict[str, Any]])`
- `def _init_ports(self) -> None`
- `def set_input(self, port_name: str, value: Any) -> None`
- `def get_input(self, port_name: str) -> Any`
- `def set_output(self, port_name: str, value: Any) -> None`
- `def get_output(self, port_name: str) -> Any`
- `def execute(self) -> Dict[str, Any]`
- `def to_dict(self) -> Dict[str, Any]`
- `def from_dict(cls, data: Dict[str, Any]) -> 'Node'`

### vm

- [VMResult](`#class-vmresult`)
- [VMFrame](`#class-vmframe`)
- [VMState](`#class-vmstate`)
- [MathaVM](`#class-mathavm`)
- [CrossVerifier](`#class-crossverifier`)

**函数**
- `def configure_vm_logging(level: int) -> None` — 配置 VM 日志。
- `def benchmark(interpreter_source: str, iterations: int) -> dict` — 性能基准测试。

**方法**
- `def __init__(self, max_iterations: int, debug: bool) -> None`
- `def run(self, mir_program: Any) -> tuple[list, list[str]]`
- `def run_func(self, func_name: str, args: Optional[list[float]]) -> float`
- `def _execute_frame(self, frame: VMFrame) -> None`
- `def _execute_instruction(self, frame: VMFrame, instr: Any, pc: int) -> VMResult`
- `def _execute_arith(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult`
- `def _execute_compare(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult`
- `def _execute_logical(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult`
- `def _execute_call(self, frame: VMFrame, instr: Any, operands: list, result_var: str, metadata: dict) -> VMResult`
- `def _execute_function_call(self, caller_frame: VMFrame, func_name: str, func: Any, args: list[float], result_var: str) -> VMResult`
- `def _execute_store(self, frame: VMFrame, instr: Any, operands: list) -> VMResult`
- `def _execute_load(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult`
- `def _resolve_operand(self, frame: VMFrame, op: Any) -> float`
- `def _eval_expr(self, expr: Any, frame: VMFrame) -> float`
- `def __init__(self, verbose: bool) -> None`
- `def verify(self, source: str) -> dict`
- `def batch_verify(self, sources: list[str]) -> dict`
- `def get_summary(self) -> str`

---

## 详细文档

# `adapters.language_adapters`

```matha

```

**描述**: Matha v4.0 — 语言适配器层

设计原则：
  1. Matha 代码 → 目标语言代码（翻译）
  2. 目标语言代码 → 可执行（编译/解释）
  3. 执行结果 → 返回给 Matha 运行时

架构：
  Matha IR → LanguageAdapter → 目标语言 → 执行 → 结果

Source: `src\adapters\language_adapters.py:1`

---

## `AdaptResult`

```matha
class AdaptResult
```

**描述**: 适配结果。

**装饰器**: dataclass

Source: `src\adapters\language_adapters.py:30`

---

## `LanguageAdapter`

```matha
class LanguageAdapter(abc.ABC)
```

**描述**: 语言适配器基类。

Source: `src\adapters\language_adapters.py:41`

---

### `__init__`

```matha
def __init__(self, name: str, ext: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `ext` | `str` | |

Source: `src\adapters\language_adapters.py:44`

---

### `adapt`

```matha
def adapt(self, matha_ir: str) -> AdaptResult
```

**描述**: 将 Matha IR 适配为目标语言并执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_ir` | `str` | |

**返回**: `AdaptResult`

**装饰器**: abc.abstractmethod

Source: `src\adapters\language_adapters.py:52`

---

### `translate`

```matha
def translate(self, matha_ir: str) -> str
```

**描述**: Matha IR → 目标语言代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_ir` | `str` | |

**返回**: `str`

**装饰器**: abc.abstractmethod

Source: `src\adapters\language_adapters.py:57`

---

### `_cache_key`

```matha
def _cache_key(self, code: str) -> str
```

**描述**: 生成缓存键。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |

**返回**: `str`

Source: `src\adapters\language_adapters.py:61`

---

### `_get_cached`

```matha
def _get_cached(self, key: str) -> Optional[Any]
```

**描述**: 获取缓存的执行结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `Optional[Any]`

Source: `src\adapters\language_adapters.py:65`

---

### `_save_cache`

```matha
def _save_cache(self, key: str, result: Any)
```

**描述**: 保存执行结果到缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `result` | `Any` | |

Source: `src\adapters\language_adapters.py:73`

---

## `PythonAdapter`

```matha
class PythonAdapter(LanguageAdapter)
```

**描述**: Python 适配器。

Matha IR → Python 代码 → exec() 执行

Source: `src\adapters\language_adapters.py:84`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\adapters\language_adapters.py:91`

---

### `adapt`

```matha
def adapt(self, matha_ir: str) -> AdaptResult
```

**描述**: 适配并执行 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_ir` | `str` | |

**返回**: `AdaptResult`

Source: `src\adapters\language_adapters.py:94`

---

### `translate`

```matha
def translate(self, matha_ir: str) -> str
```

**描述**: Matha IR → Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_ir` | `str` | |

**返回**: `str`

Source: `src\adapters\language_adapters.py:137`

---

## `RustAdapter`

```matha
class RustAdapter(LanguageAdapter)
```

**描述**: Rust 适配器。

Matha IR → Rust 代码 → rustc 编译 → 可执行文件

Source: `src\adapters\language_adapters.py:173`

---

### `__init__`

```matha
def __init__(self, optimize: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `optimize` | `bool` | |

Source: `src\adapters\language_adapters.py:180`

---

### `adapt`

```matha
def adapt(self, matha_ir: str) -> AdaptResult
```

**描述**: 适配并执行 Rust 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_ir` | `str` | |

**返回**: `AdaptResult`

Source: `src\adapters\language_adapters.py:184`

---

### `translate`

```matha
def translate(self, matha_ir: str) -> str
```

**描述**: Matha IR → Rust 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_ir` | `str` | |

**返回**: `str`

Source: `src\adapters\language_adapters.py:241`

---

### `_compile`

```matha
def _compile(self, rust_code: str, cache_key: str) -> Optional[str]
```

**描述**: 编译 Rust 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `rust_code` | `str` | |
| `cache_key` | `str` | |

**返回**: `Optional[str]`

Source: `src\adapters\language_adapters.py:268`

---

## `LanguageAdapterRegistry`

```matha
class LanguageAdapterRegistry
```

**描述**: 语言适配器注册表。

Source: `src\adapters\language_adapters.py:295`

---

### `register`

```matha
def register(cls, name: str, adapter: LanguageAdapter)
```

**描述**: 注册语言适配器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `name` | `str` | |
| `adapter` | `LanguageAdapter` | |

**装饰器**: classmethod

Source: `src\adapters\language_adapters.py:301`

---

### `get`

```matha
def get(cls, name: str) -> Optional[LanguageAdapter]
```

**描述**: 获取语言适配器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `name` | `str` | |

**返回**: `Optional[LanguageAdapter]`

**装饰器**: classmethod

Source: `src\adapters\language_adapters.py:306`

---

### `list_adapters`

```matha
def list_adapters(cls) -> List[str]
```

**描述**: 列出所有已注册的适配器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `List[str]`

**装饰器**: classmethod

Source: `src\adapters\language_adapters.py:311`

---

# `ai_assistant`

```matha

```

**描述**: Matha AI Assistant — 小白友好的数学计算助手

功能：
  1. 自然语言 → Matha 代码自动生成
  2. 多步骤任务分解（AI 辅助规划）
  3. 小白友好错误解释（非技术语言）
  4. 数学概念讲解 + 例题
  5. 待办事项管理
  6. 跨平台（Web/PWA/Electron）

Source: `src\ai_assistant.py:1`

---

## `IntentType`

```matha
class IntentType(str, Enum)
```

Source: `src\ai_assistant.py:42`

---

## `Step`

```matha
class Step
```

**描述**: 任务分解后的单步。

**装饰器**: dataclass

Source: `src\ai_assistant.py:71`

---

## `Task`

```matha
class Task
```

**描述**: 待办事项。

**装饰器**: dataclass

Source: `src\ai_assistant.py:79`

---

## `ChatMessage`

```matha
class ChatMessage
```

**描述**: 对话消息。

**装饰器**: dataclass

Source: `src\ai_assistant.py:91`

---

## `FriendlyIntentParser`

```matha
class FriendlyIntentParser
```

**描述**: 将自然语言分解为步骤，生成 Matha 代码，并用小白语言解释。

工作流程：
  用户输入 → 意图分类 → 参数提取 → 步骤分解 → 代码生成 → 执行 → 结果解释

Source: `src\ai_assistant.py:103`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ai_assistant.py:430`

---

### `classify`

```matha
def classify(self, text: str) -> tuple[IntentType, float]
```

**描述**: 根据关键词分类，返回 (类型, 置信度)。

增强策略：
  1. 精确关键词匹配（原有逻辑）
  2. 变体表达匹配（VARIATION_MAP）
  3. 常识推理匹配（COMMONSENSE_RULES）
  4. 数字特征推断（fallback）
  5. 用户学习记忆（_learned_patterns）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `tuple[IntentType, float]`

Source: `src\ai_assistant.py:442`

---

### `_infer_from_numbers`

```matha
def _infer_from_numbers(self, text: str, nums: list[float]) -> Optional[IntentType]
```

**描述**: 从数字特征推断意图（兜底策略）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `Optional[IntentType]`

Source: `src\ai_assistant.py:509`

---

### `learn`

```matha
def learn(self, text: str, intent: IntentType) -> None
```

**描述**: 学习用户的表达方式，提升未来分类准确度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `intent` | `IntentType` | |

**返回**: `None`

Source: `src\ai_assistant.py:534`

---

### `record_failure`

```matha
def record_failure(self, text: str, error: str, intent: IntentType) -> None
```

**描述**: 记录执行失败，用于后续自动学习修复。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `error` | `str` | |
| `intent` | `IntentType` | |

**返回**: `None`

Source: `src\ai_assistant.py:550`

---

### `_auto_learn_from_failure`

```matha
def _auto_learn_from_failure(self, text: str, error: str) -> None
```

**描述**: 从失败中自动学习，生成修复建议。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `error` | `str` | |

**返回**: `None`

Source: `src\ai_assistant.py:558`

---

### `record_correction`

```matha
def record_correction(self, original_text: str, original_intent: str, corrected_intent: str) -> None
```

**描述**: 记录用户纠正，用于提升分类准确度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `original_text` | `str` | |
| `original_intent` | `str` | |
| `corrected_intent` | `str` | |

**返回**: `None`

Source: `src\ai_assistant.py:575`

---

### `get_growth_stats`

```matha
def get_growth_stats(self) -> dict
```

**描述**: 返回成长统计数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\ai_assistant.py:594`

---

### `extract_numbers`

```matha
def extract_numbers(self, text: str) -> list[float]
```

**描述**: 从文本中提取所有数字。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `list[float]`

Source: `src\ai_assistant.py:607`

---

### `extract_range`

```matha
def extract_range(self, text: str) -> Optional[tuple[int, int]]
```

**描述**: 提取范围，如 '1到100' → (1, 100)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Optional[tuple[int, int]]`

Source: `src\ai_assistant.py:611`

---

### `extract_variable`

```matha
def extract_variable(self, text: str) -> Optional[str]
```

**描述**: 提取变量名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Optional[str]`

Source: `src\ai_assistant.py:621`

---

### `decompose`

```matha
def decompose(self, text: str) -> list[Step]
```

**描述**: 将自然语言分解为可执行的步骤。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:629`

---

### `_decompose_arithmetic`

```matha
def _decompose_arithmetic(self, text: str, nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:669`

---

### `_decompose_math_func`

```matha
def _decompose_math_func(self, text: str, nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:696`

---

### `_decompose_statistics`

```matha
def _decompose_statistics(self, text: str, nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:737`

---

### `_decompose_string`

```matha
def _decompose_string(self, text: str) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:762`

---

### `_decompose_array`

```matha
def _decompose_array(self, text: str, nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:767`

---

### `_decompose_physics`

```matha
def _decompose_physics(self, text: str, nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:791`

---

### `_decompose_trig`

```matha
def _decompose_trig(self, text: str, nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:815`

---

### `_decompose_number_theory`

```matha
def _decompose_number_theory(self, text: str, rng: Optional[tuple], nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `rng` | `Optional[tuple]` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:830`

---

### `_decompose_unit_convert`

```matha
def _decompose_unit_convert(self, text: str, nums: list[float]) -> list[Step]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `nums` | `list[float]` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:859`

---

### `execute_steps`

```matha
def execute_steps(self, steps: list[Step], interp) -> list[dict]
```

**描述**: 执行步骤列表，返回结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `steps` | `list[Step]` | |
| `interp` | `` | |

**返回**: `list[dict]`

Source: `src\ai_assistant.py:869`

---

### `explain_error`

```matha
def explain_error(self, error_msg: str) -> dict
```

**描述**: 将技术错误翻译为小白语言。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `error_msg` | `str` | |

**返回**: `dict`

Source: `src\ai_assistant.py:895`

---

### `explain_intent`

```matha
def explain_intent(self, text: str) -> dict
```

**描述**: 生成完整意图解释。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `dict`

Source: `src\ai_assistant.py:911`

---

### `explain_concept`

```matha
def explain_concept(self, topic: str) -> dict
```

**描述**: 讲解数学概念。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `topic` | `str` | |

**返回**: `dict`

Source: `src\ai_assistant.py:1227`

---

### `create_task`

```matha
def create_task(self, title: str, steps: list[Step]) -> Task
```

**描述**: 创建待办事项。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `title` | `str` | |
| `steps` | `list[Step]` | |

**返回**: `Task`

Source: `src\ai_assistant.py:1240`

---

### `get_task`

```matha
def get_task(self, task_id: str) -> Optional[Task]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task_id` | `str` | |

**返回**: `Optional[Task]`

Source: `src\ai_assistant.py:1247`

---

### `list_tasks`

```matha
def list_tasks(self, status: Optional[str]) -> list[Task]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `status` | `Optional[str]` | |

**返回**: `list[Task]`

Source: `src\ai_assistant.py:1250`

---

### `mark_done`

```matha
def mark_done(self, task_id: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task_id` | `str` | |

**返回**: `bool`

Source: `src\ai_assistant.py:1255`

---

### `add_message`

```matha
def add_message(self, msg: ChatMessage)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `ChatMessage` | |

Source: `src\ai_assistant.py:1263`

---

### `get_history`

```matha
def get_history(self, limit: int) -> list[ChatMessage]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |

**返回**: `list[ChatMessage]`

Source: `src\ai_assistant.py:1266`

---

### `_decompose_commonsense`

```matha
def _decompose_commonsense(self, text: str, intent: IntentType) -> list[Step]
```

**描述**: 当标准分解失败时，用常识推断生成步骤。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `intent` | `IntentType` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:1271`

---

### `_try_alternative`

```matha
def _try_alternative(self, text: str, intent: IntentType) -> list[Step]
```

**描述**: 当首选方案失败时，尝试替代方案。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `intent` | `IntentType` | |

**返回**: `list[Step]`

Source: `src\ai_assistant.py:1368`

---

## `MathaAIAssistant`

```matha
class MathaAIAssistant
```

**描述**: Matha AI 助手 — 小白友好型数学计算平台。

Source: `src\ai_assistant.py:1413`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ai_assistant.py:1416`

---

### `chat`

```matha
def chat(self, text: str, interp) -> dict
```

**描述**: 处理用户输入，返回结构化响应。

返回:
  {
    "reply": str,          # 给用户的回复
    "code": str,           # 生成的 Matha 代码
    "steps": list,         # 分解步骤
    "result": Any,         # 执行结果
    "explanation": str,    # 结果解释
    "type": str            # text / error / task
  }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `interp` | `` | |

**返回**: `dict`

Source: `src\ai_assistant.py:1436`

---

### `get_growth_report`

```matha
def get_growth_report(self) -> dict
```

**描述**: 返回成长分析报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\ai_assistant.py:1565`

---

### `help`

```matha
def help(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\ai_assistant.py:1569`

---

# `ai_assistant_desktop`

```matha

```

**描述**: Electron 主进程 — Matha AI Assistant 桌面应用

打包命令：
  pip install electron-builder
  electron-builder --mac --win --linux

Source: `src\ai_assistant_desktop.py:1`

---

## `launch_desktop`

```matha
def launch_desktop()
```

**描述**: 启动桌面应用（通过 webview 嵌入）。

Source: `src\ai_assistant_desktop.py:20`

---

# `ai_assistant_server`

```matha

```

**描述**: Matha AI Assistant — Web 后端 API 服务
提供 RESTful API 供前端调用，零依赖轻量服务

Source: `src\ai_assistant_server.py:1`

---

## `APIHandler`

```matha
class APIHandler(BaseHTTPRequestHandler)
```

**描述**: HTTP API 处理器。

Source: `src\ai_assistant_server.py:28`

---

### `log_message`

```matha
def log_message(self, format, *args)
```

**描述**: 静默日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `format` | `` | |

Source: `src\ai_assistant_server.py:34`

---

### `_send_json`

```matha
def _send_json(self, data: dict, status: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `dict` | |
| `status` | `int` | |

Source: `src\ai_assistant_server.py:38`

---

### `_read_body`

```matha
def _read_body(self) -> str
```

**描述**: 读取请求体。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\ai_assistant_server.py:45`

---

### `do_OPTIONS`

```matha
def do_OPTIONS(self)
```

**描述**: 处理 CORS 预检。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ai_assistant_server.py:52`

---

### `do_GET`

```matha
def do_GET(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ai_assistant_server.py:60`

---

### `do_POST`

```matha
def do_POST(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ai_assistant_server.py:245`

---

### `_serve_file`

```matha
def _serve_file(self, path: str, content_type: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |
| `content_type` | `str` | |

Source: `src\ai_assistant_server.py:356`

---

## `run`

```matha
def run(host: str, port: int)
```

**描述**: 启动 Web 服务。

| 参数 | 类型 | 说明 |
|------|------|------|
| `host` | `str` | |
| `port` | `int` | |

Source: `src\ai_assistant_server.py:373`

---

# `ast_nodes`

```matha

```

**描述**: Matha 抽象语法树节点定义。

对应 EBNF 各章产生式，每个 AST 节点用 dataclass 表示。
节点命名与 EBNF 非终结符对应（驼峰命名）。

Source: `src\ast_nodes.py:1`

---

## `Program`

```matha
class Program
```

**描述**: <program> = { <top_level> }

**装饰器**: dataclass

Source: `src\ast_nodes.py:17`

---

## `Annotation`

```matha
class Annotation
```

**描述**: <annotation> = "*/" , <annot_text> , [ "*" , <expr> ] , "/*" 

**装饰器**: dataclass

Source: `src\ast_nodes.py:27`

---

## `CommandLiteral`

```matha
class CommandLiteral
```

**描述**: <command_literal> = 《文字》 | 【文字】（两种写法等价，M3.1）

**装饰器**: dataclass

Source: `src\ast_nodes.py:34`

---

## `SetUpItem`

```matha
class SetUpItem
```

**描述**: <set_up_item> = ( <variable> | <path_expr> ) , [ <annotation> ] , [ "=" , <expr> ]

**装饰器**: dataclass

Source: `src\ast_nodes.py:41`

---

## `SetUp`

```matha
class SetUp
```

**描述**: <set_up> = @(...) | @:xxx，yyy（双形式）

**装饰器**: dataclass

Source: `src\ast_nodes.py:49`

---

## `LoopFraction`

```matha
class LoopFraction
```

**描述**: <loop_fraction> = (x/y) 或 （x/y），半/全角等价

**装饰器**: dataclass

Source: `src\ast_nodes.py:56`

---

## `SegLoopSuffix`

```matha
class SegLoopSuffix
```

**描述**: <seg_loop_suffix> = … , [ <seg_id> ] , <loop_fraction>（单省略号，段级）

**装饰器**: dataclass

Source: `src\ast_nodes.py:63`

---

## `GlobalLoopSuffix`

```matha
class GlobalLoopSuffix
```

**描述**: <global_loop_suffix> = …… , <loop_fraction>（双省略号，全局）

**装饰器**: dataclass

Source: `src\ast_nodes.py:70`

---

## `OutputTrail`

```matha
class OutputTrail
```

**描述**: <output_trail> = <output>
, [ <seg_loop_suffix>
  , [ <subfile_ref> ]            (* 段循环后：子文件引用，下位文件补充/扩充，| 分隔多个 *)
  , [ <global_code_id> ]
  , [ <global_loop_suffix>
    , [ <file_ref> ]             (* 全局循环后：文件路径，当前文件被分割成多文件 *)
    ]
  ]

**装饰器**: dataclass

Source: `src\ast_nodes.py:76`

---

## `FileMarker`

```matha
class FileMarker
```

**描述**: <file_marker> = #：【文件】 或 #：【路径】

**装饰器**: dataclass

Source: `src\ast_nodes.py:94`

---

## `GlobalIdStmt`

```matha
class GlobalIdStmt
```

**描述**: <global_id_stmt> = <global_code_id>（跨文件绑定，独立一行或 #： 前缀）
is_placeholder=True 表示 ？ 模板占位符。

**装饰器**: dataclass

Source: `src\ast_nodes.py:101`

---

## `MechUnit`

```matha
class MechUnit
```

**描述**: <mech_unit> = ( <generate> | <generate_seg> ) , ( <code_block> | <mech_stmt> | <mech_body> )

**装饰器**: dataclass

Source: `src\ast_nodes.py:113`

---

## `Generate`

```matha
class Generate
```

**描述**: <generate> = #：

**装饰器**: dataclass

Source: `src\ast_nodes.py:120`

---

## `GenStmt`

```matha
class GenStmt
```

**描述**: <gen_stmt> / <gen_stmt_seg> = #(N)：( <command_literal> | <expr> | <output_trail> )

**装饰器**: dataclass

Source: `src\ast_nodes.py:126`

---

## `Binding`

```matha
class Binding
```

**描述**: <binding> = ( <variable> | <path_expr> ) , [ <annotation> ] , "=" , <expr>

**装饰器**: dataclass

Source: `src\ast_nodes.py:133`

---

## `PathExpr`

```matha
class PathExpr
```

**描述**: <path_expr> = <variable> >> <variable>（路径/距离）

**装饰器**: dataclass

Source: `src\ast_nodes.py:141`

---

## `SetConstruct`

```matha
class SetConstruct
```

**描述**: <set_construct> = { <var_list> | <cond_list> } 或 { <literal_list> }

**装饰器**: dataclass

Source: `src\ast_nodes.py:148`

---

## `DictLiteral`

```matha
class DictLiteral
```

**描述**: <dict_literal> = { <key> : <value> , { , <key> : <value> } }

**装饰器**: dataclass

Source: `src\ast_nodes.py:157`

---

## `Iteration`

```matha
class Iteration
```

**描述**: <iteration> = (？<variable> | <variable>) >> <expr> <block>

**装饰器**: dataclass

Source: `src\ast_nodes.py:164`

---

## `TupleExpr`

```matha
class TupleExpr
```

**描述**: <tuple_expr> = ( <expr> , { , <expr> } )

**装饰器**: dataclass

Source: `src\ast_nodes.py:173`

---

## `ListLiteral`

```matha
class ListLiteral
```

**描述**: <list_literal> = [ <expr> , { , <expr> } ]

**装饰器**: dataclass

Source: `src\ast_nodes.py:179`

---

## `IndexExpr`

```matha
class IndexExpr
```

**描述**: <index_expr> = <expr> [ <expr> ]（列表/字符串索引）

**装饰器**: dataclass

Source: `src\ast_nodes.py:185`

---

## `SliceExpr`

```matha
class SliceExpr
```

**描述**: <slice_expr> = <expr> [ <start> : <end> ]（列表/字符串切片）

**装饰器**: dataclass

Source: `src\ast_nodes.py:192`

---

## `ChainStmt`

```matha
class ChainStmt
```

**描述**: <chain_stmt> = <mech_stmt> , { >> , <mech_stmt> }（通用链式，M3.1）
M3.2 触发条件：单条命令/单条输出不足以完成任务时才启用。

**装饰器**: dataclass

Source: `src\ast_nodes.py:200`

---

## `IntegerLit`

```matha
class IntegerLit
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:211`

---

## `FloatLit`

```matha
class FloatLit
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:217`

---

## `StringLit`

```matha
class StringLit
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:223`

---

## `BoolLit`

```matha
class BoolLit
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:228`

---

## `Variable`

```matha
class Variable
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:233`

---

## `BinaryOp`

```matha
class BinaryOp
```

**描述**: 二元运算：<expr> <op> <expr>

**装饰器**: dataclass

Source: `src\ast_nodes.py:239`

---

## `UnaryOp`

```matha
class UnaryOp
```

**描述**: 一元运算：- <expr> 或 ^ <expr>（前缀开方）

**装饰器**: dataclass

Source: `src\ast_nodes.py:247`

---

## `AngleExpr`

```matha
class AngleExpr
```

**描述**: <angle_expr> = << <expr>

**装饰器**: dataclass

Source: `src\ast_nodes.py:254`

---

## `Belongs`

```matha
class Belongs
```

**描述**: <belongs> = <expr> >> <expr>（属于判断）

**装饰器**: dataclass

Source: `src\ast_nodes.py:260`

---

## `Output`

```matha
class Output
```

**描述**: <output> = [ , [ <expr> ] , ]

**装饰器**: dataclass

Source: `src\ast_nodes.py:267`

---

## `ReadBlock`

```matha
class ReadBlock
```

**描述**: <read_block> = 【 <read_content> 】 或 〔 ... 〕

**装饰器**: dataclass

Source: `src\ast_nodes.py:274`

---

## `NLBlock`

```matha
class NLBlock
```

**描述**: <nl_block> = 【*/标注/*】<natural_lang>（自然语言意图块）

标注确定意图类别，natural_lang 为意图描述正文（纯文本，不做变量检查）。

**装饰器**: dataclass

Source: `src\ast_nodes.py:280`

---

## `Lambda`

```matha
class Lambda
```

**描述**: <lambda> = ( <params> ) => <expr>

**装饰器**: dataclass

Source: `src\ast_nodes.py:290`

---

## `CodeBlock`

```matha
class CodeBlock
```

**描述**: <code_block> = { <newline> , { <mech_stmt> , <newline> } , }

**装饰器**: dataclass

Source: `src\ast_nodes.py:297`

---

## `FuncApp`

```matha
class FuncApp
```

**描述**: 函数应用（后缀）：<primary> <primary>

**装饰器**: dataclass

Source: `src\ast_nodes.py:303`

---

## `IfExpr`

```matha
class IfExpr
```

**描述**: <if_expr> = <expr> ? <expr> : <expr>（三元）

**装饰器**: dataclass

Source: `src\ast_nodes.py:314`

---

## `LetBinding`

```matha
class LetBinding
```

**描述**: let 局部绑定节点。

**装饰器**: dataclass

Source: `src\ast_nodes.py:322`

---

## `LetTupleBinding`

```matha
class LetTupleBinding
```

**描述**: let (a, b, ...) = expr in body — 元组解构绑定。

**装饰器**: dataclass

Source: `src\ast_nodes.py:332`

---

## `IfStmt`

```matha
class IfStmt
```

**描述**: <if_stmt> = <expr> <block> [ 否则 ... ]（草案）

**装饰器**: dataclass

Source: `src\ast_nodes.py:340`

---

## `WhileStmt`

```matha
class WhileStmt
```

**描述**: <while_stmt> = while <expr> { <block> }

**装饰器**: dataclass

Source: `src\ast_nodes.py:348`

---

## `ForStmt`

```matha
class ForStmt
```

**描述**: <for_stmt> = for <var> in <expr> { <block> }

**装饰器**: dataclass

Source: `src\ast_nodes.py:355`

---

## `LoopStep`

```matha
class LoopStep
```

**描述**: <loop_step> = (？<var> | <var>) >> <expr> <block>（步进循环）

**装饰器**: dataclass

Source: `src\ast_nodes.py:363`

---

## `LoopWhile`

```matha
class LoopWhile
```

**描述**: <loop_while> = <expr> ? <block>（条件循环，草案）

**装饰器**: dataclass

Source: `src\ast_nodes.py:372`

---

## `MatchStmt`

```matha
class MatchStmt
```

**描述**: <match_stmt> = match <expr> { | <pattern> => <expr> }

**装饰器**: dataclass

Source: `src\ast_nodes.py:379`

---

## `BasicType`

```matha
class BasicType
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:390`

---

## `SetType`

```matha
class SetType
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:395`

---

## `FuncType`

```matha
class FuncType
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:400`

---

## `TupleType`

```matha
class TupleType
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:406`

---

## `AnnotatedType`

```matha
class AnnotatedType
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:411`

---

## `FuncDef`

```matha
class FuncDef
```

**描述**: <func_def> = <identifier> [ <annotation> ] : <func_type> = <lambda>

**装饰器**: dataclass

Source: `src\ast_nodes.py:421`

---

## `StructDef`

```matha
class StructDef
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:430`

---

## `EnumDef`

```matha
class EnumDef
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:438`

---

## `AliasDef`

```matha
class AliasDef
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:446`

---

## `ModuleDecl`

```matha
class ModuleDecl
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:453`

---

## `ImportDecl`

```matha
class ImportDecl
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:460`

---

## `GoStmt`

```matha
class GoStmt
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:467`

---

## `ChanExpr`

```matha
class ChanExpr
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:472`

---

## `SendExpr`

```matha
class SendExpr
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:478`

---

## `SelectStmt`

```matha
class SelectStmt
```

**装饰器**: dataclass

Source: `src\ast_nodes.py:484`

---

## `DefineOp`

```matha
class DefineOp
```

**描述**: <define_op> = @define_op: <symbol> = <precedence> | <assoc>

用户自定义运算符定义节点。

**装饰器**: dataclass

Source: `src\ast_nodes.py:489`

---

# `ast_serializer`

```matha

```

**描述**: Matha AST 序列化器：把 Matha 程序的 AST 导出为 JSON。

让任何编程语言（Python/JavaScript/Rust/Go/Java...）都能识别解读 Matha 程序结构。

设计：
  - ast_to_dict(node)   → 把任意 AST 节点转为 dict
  - ast_to_json(node)   → 把任意 AST 节点转为 JSON 字符串
  - program_to_dict(src) → 把 Matha 源码解析为 dict 形式的 AST
  - program_to_json(src) → 把 Matha 源码解析为 JSON 字符串

JSON 格式规范：
  每个节点 = {"node": <类型名>, <字段名>: <字段值>, ...}
  列表字段保持列表，嵌套节点递归序列化。
  字面量值直接存储（int/float/str/bool/None）。

示例：
  Matha 源码  #：{ x = 3 + 4  #：[x] }
  → JSON:
  {
    "node": "Program",
    "decls": [{
      "node": "MechUnit",
      "generate": {"node": "Generate", "seg_id": 1, "form": "seg"},
      "body": {"node": "CodeBlock", "stmts": [
        {"node": "Binding",
         "target": {"node": "Variable", "name": "x"},
         "value": {"node": "BinaryOp", "op": "+",
                   "left": {"node": "IntegerLit", "value": 3},
                   "right": {"node": "IntegerLit", "value": 4}}
        },
        {"node": "Output", "expr": {"node": "Variable", "name": "x"}}
      ]}
    }]
  }

Source: `src\ast_serializer.py:1`

---

## `_node_to_dict`

```matha
def _node_to_dict(node: Any) -> Any
```

**描述**: 递归把 AST 节点（或普通值）转为可 JSON 序列化的结构。

| 参数 | 类型 | 说明 |
|------|------|------|
| `node` | `Any` | |

**返回**: `Any`

Source: `src\ast_serializer.py:47`

---

## `ast_to_dict`

```matha
def ast_to_dict(node: Any) -> dict | list | Any
```

**描述**: 把任意 AST 节点转为 dict/list/基础值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `node` | `Any` | |

**返回**: `dict | list | Any`

Source: `src\ast_serializer.py:73`

---

## `ast_to_json`

```matha
def ast_to_json(node: Any, indent: int) -> str
```

**描述**: 把任意 AST 节点转为 JSON 字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `node` | `Any` | |
| `indent` | `int` | |

**返回**: `str`

Source: `src\ast_serializer.py:78`

---

## `program_to_dict`

```matha
def program_to_dict(source: str) -> dict
```

**描述**: 把 Matha 源码解析为 dict 形式的 AST。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `dict`

Source: `src\ast_serializer.py:83`

---

## `program_to_json`

```matha
def program_to_json(source: str, indent: int) -> str
```

**描述**: 把 Matha 源码解析为 JSON 字符串形式的 AST。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `indent` | `int` | |

**返回**: `str`

Source: `src\ast_serializer.py:89`

---

## `tokens_to_dict`

```matha
def tokens_to_dict(source: str) -> list[dict]
```

**描述**: 把 Matha 源码的 Token 流导出为 dict 列表（供其它语言做词法分析）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `list[dict]`

Source: `src\ast_serializer.py:94`

---

## `tokens_to_json`

```matha
def tokens_to_json(source: str, indent: int) -> str
```

**描述**: 把 Token 流导出为 JSON。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `indent` | `int` | |

**返回**: `str`

Source: `src\ast_serializer.py:108`

---

# `async_runtime`

```matha

```

**描述**: Matha 异步运行时与线程池。

支持：
  1. async/await 语法（通过 _parse_expr 中的 async 关键字）
  2. 线程池：用于并行计算密集型任务
  3. 事件循环：用于 I/O 密集型任务
  4. 并发原语：Mutex, Semaphore, Condition

Source: `src\async_runtime.py:1`

---

## `ThreadPool`

```matha
class ThreadPool
```

**描述**: Matha 线程池。

用于并行执行计算密集型任务。

Source: `src\async_runtime.py:23`

---

### `__init__`

```matha
def __init__(self, max_workers: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_workers` | `int` | |

**返回**: `None`

Source: `src\async_runtime.py:29`

---

### `_get_pool`

```matha
def _get_pool(self) -> concurrent.futures.ThreadPoolExecutor
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `concurrent.futures.ThreadPoolExecutor`

Source: `src\async_runtime.py:34`

---

### `submit`

```matha
def submit(self, fn: Callable, *args, **kwargs) -> concurrent.futures.Future
```

**描述**: 提交任务到线程池。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |

**返回**: `concurrent.futures.Future`

Source: `src\async_runtime.py:42`

---

### `map`

```matha
def map(self, fn: Callable, *iterables)
```

**描述**: 并行映射。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |

Source: `src\async_runtime.py:46`

---

### `shutdown`

```matha
def shutdown(self, wait: bool) -> None
```

**描述**: 关闭线程池。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `wait` | `bool` | |

**返回**: `None`

Source: `src\async_runtime.py:50`

---

### `max_workers`

```matha
def max_workers(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\async_runtime.py:58`

---

### `max_workers`

```matha
def max_workers(self, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `int` | |

**返回**: `None`

**装饰器**: max_workers.setter

Source: `src\async_runtime.py:62`

---

## `EventLoop`

```matha
class EventLoop
```

**描述**: Matha 事件循环封装。

Source: `src\async_runtime.py:71`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:74`

---

### `_get_loop`

```matha
def _get_loop(self) -> asyncio.AbstractEventLoop
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `asyncio.AbstractEventLoop`

Source: `src\async_runtime.py:77`

---

### `run`

```matha
def run(self, coro: Coroutine) -> Any
```

**描述**: 运行协程。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `coro` | `Coroutine` | |

**返回**: `Any`

Source: `src\async_runtime.py:83`

---

### `run_async`

```matha
def run_async(self, coro: Coroutine) -> None
```

**描述**: 异步运行协程（不阻塞）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `coro` | `Coroutine` | |

**返回**: `None`

Source: `src\async_runtime.py:87`

---

### `close`

```matha
def close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:94`

---

## `Mutex`

```matha
class Mutex
```

**描述**: 互斥锁。

Source: `src\async_runtime.py:103`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:106`

---

### `acquire`

```matha
def acquire(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\async_runtime.py:109`

---

### `release`

```matha
def release(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:112`

---

### `__enter__`

```matha
def __enter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\async_runtime.py:115`

---

### `__exit__`

```matha
def __exit__(self, *args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\async_runtime.py:119`

---

## `Semaphore`

```matha
class Semaphore
```

**描述**: 信号量。

Source: `src\async_runtime.py:123`

---

### `__init__`

```matha
def __init__(self, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\async_runtime.py:126`

---

### `acquire`

```matha
def acquire(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\async_runtime.py:129`

---

### `release`

```matha
def release(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:132`

---

## `Condition`

```matha
class Condition
```

**描述**: 条件变量。

Source: `src\async_runtime.py:136`

---

### `__init__`

```matha
def __init__(self, mutex: Optional[Mutex]) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mutex` | `Optional[Mutex]` | |

**返回**: `None`

Source: `src\async_runtime.py:139`

---

### `wait`

```matha
def wait(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:142`

---

### `notify`

```matha
def notify(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:145`

---

### `notify_all`

```matha
def notify_all(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime.py:148`

---

### `__enter__`

```matha
def __enter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\async_runtime.py:151`

---

### `__exit__`

```matha
def __exit__(self, *args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\async_runtime.py:155`

---

## `AsyncSupport`

```matha
class AsyncSupport
```

**描述**: Matha async/await 语法支持。

Source: `src\async_runtime.py:163`

---

### `is_async_func`

```matha
def is_async_func(func: Callable) -> bool
```

**描述**: 检查函数是否为 async。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\async_runtime.py:167`

---

### `await_func`

```matha
def await_func(func: Callable, *args) -> Any
```

**描述**: 等待 async 函数完成。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\async_runtime.py:172`

---

### `parallel_await`

```matha
def parallel_await(coros: list[Coroutine]) -> list[Any]
```

**描述**: 并行等待多个协程。

| 参数 | 类型 | 说明 |
|------|------|------|
| `coros` | `list[Coroutine]` | |

**返回**: `list[Any]`

**装饰器**: staticmethod

Source: `src\async_runtime.py:180`

---

## `get_thread_pool`

```matha
def get_thread_pool() -> ThreadPool
```

**返回**: `ThreadPool`

Source: `src\async_runtime.py:197`

---

## `get_event_loop`

```matha
def get_event_loop() -> EventLoop
```

**返回**: `EventLoop`

Source: `src\async_runtime.py:201`

---

# `async_runtime_v2`

```matha

```

**描述**: Matha 增强异步运行时：async/await + Channel + Actor + goroutine 调度器。

Source: `src\async_runtime_v2.py:1`

---

## `GState`

```matha
class GState(Enum)
```

Source: `src\async_runtime_v2.py:19`

---

## `Goroutine`

```matha
class Goroutine
```

**描述**: 类 goroutine 协程。

**装饰器**: dataclass

Source: `src\async_runtime_v2.py:28`

---

## `GoroutineScheduler`

```matha
class GoroutineScheduler
```

**描述**: M:N goroutine 调度器。

Source: `src\async_runtime_v2.py:39`

---

### `__init__`

```matha
def __init__(self, num_workers: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `num_workers` | `int` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:42`

---

### `spawn`

```matha
def spawn(self, fn: Callable, *args, **kwargs) -> int
```

**描述**: 启动一个 goroutine。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |

**返回**: `int`

Source: `src\async_runtime_v2.py:52`

---

### `spawn_channel`

```matha
def spawn_channel(self, fn: Callable, *args, **kwargs) -> 'Channel'
```

**描述**: 启动 goroutine 并返回结果 channel。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |

**返回**: `'Channel'`

Source: `src\async_runtime_v2.py:62`

---

### `_channel_producer`

```matha
def _channel_producer(self, ch: 'Channel', fn: Callable, *args, **kwargs) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ch` | `'Channel'` | |
| `fn` | `Callable` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:68`

---

### `_wakeup`

```matha
def _wakeup(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:75`

---

### `_run_loop`

```matha
def _run_loop(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:83`

---

### `wait`

```matha
def wait(self, gid: int, timeout: float) -> Any
```

**描述**: 等待 goroutine 完成。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `gid` | `int` | |
| `timeout` | `float` | |

**返回**: `Any`

Source: `src\async_runtime_v2.py:108`

---

### `wait_all`

```matha
def wait_all(self, gids: list[int], timeout: float) -> list[Any]
```

**描述**: 等待所有 goroutine。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `gids` | `list[int]` | |
| `timeout` | `float` | |

**返回**: `list[Any]`

Source: `src\async_runtime_v2.py:126`

---

### `shutdown`

```matha
def shutdown(self, wait: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `wait` | `bool` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:133`

---

### `pending_count`

```matha
def pending_count(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\async_runtime_v2.py:140`

---

## `Channel`

```matha
class Channel
```

**描述**: 类 Go channel 的同步通道。

Source: `src\async_runtime_v2.py:149`

---

### `__init__`

```matha
def __init__(self, capacity: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `capacity` | `int` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:152`

---

### `send`

```matha
def send(self, value: Any) -> None
```

**描述**: 发送值。阻塞如果通道满。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:160`

---

### `recv`

```matha
def recv(self, timeout: float) -> Any
```

**描述**: 接收值。阻塞如果通道空。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `timeout` | `float` | |

**返回**: `Any`

Source: `src\async_runtime_v2.py:171`

---

### `close`

```matha
def close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:186`

---

### `send_error`

```matha
def send_error(self, error: Exception) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `error` | `Exception` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:191`

---

### `closed`

```matha
def closed(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\async_runtime_v2.py:195`

---

### `__iter__`

```matha
def __iter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\async_runtime_v2.py:198`

---

### `__next__`

```matha
def __next__(self) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\async_runtime_v2.py:201`

---

### `drain`

```matha
def drain(self) -> list
```

**描述**: drain all values。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list`

Source: `src\async_runtime_v2.py:207`

---

## `Actor`

```matha
class Actor
```

**描述**: 类 Erlang actor 的 Actor。

Source: `src\async_runtime_v2.py:220`

---

### `__init__`

```matha
def __init__(self, name: str, handler: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `handler` | `Callable` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:223`

---

### `start`

```matha
def start(self) -> int
```

**描述**: 启动 actor。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\async_runtime_v2.py:232`

---

### `tell`

```matha
def tell(self, message: Any) -> None
```

**描述**: 异步发送消息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `Any` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:252`

---

### `ask`

```matha
def ask(self, message: Any, timeout: float) -> Any
```

**描述**: 发送消息并等待响应。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `Any` | |
| `timeout` | `float` | |

**返回**: `Any`

Source: `src\async_runtime_v2.py:258`

---

### `stop`

```matha
def stop(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:267`

---

### `is_alive`

```matha
def is_alive(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\async_runtime_v2.py:273`

---

## `AsyncSyntax`

```matha
class AsyncSyntax
```

**描述**: Matha async/await 语法支持。

Source: `src\async_runtime_v2.py:281`

---

### `is_coroutine`

```matha
def is_coroutine(obj: Any) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `Any` | |

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\async_runtime_v2.py:285`

---

### `await_func`

```matha
def await_func(func: Callable, *args) -> Any
```

**描述**: 等待 async 函数完成。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\async_runtime_v2.py:289`

---

### `async_map`

```matha
def async_map(func: Callable, items: list) -> list
```

**描述**: 并行 map。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `items` | `list` | |

**返回**: `list`

**装饰器**: staticmethod

Source: `src\async_runtime_v2.py:308`

---

### `async_filter`

```matha
def async_filter(func: Callable, items: list) -> list
```

**描述**: 并行 filter。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `items` | `list` | |

**返回**: `list`

**装饰器**: staticmethod

Source: `src\async_runtime_v2.py:315`

---

## `AsyncRuntime`

```matha
class AsyncRuntime
```

**描述**: 统一异步运行时。

Source: `src\async_runtime_v2.py:331`

---

### `__init__`

```matha
def __init__(self, goroutines: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `goroutines` | `int` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:334`

---

### `spawn`

```matha
def spawn(self, fn: Callable, *args, **kwargs) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |

**返回**: `int`

Source: `src\async_runtime_v2.py:339`

---

### `spawn_channel`

```matha
def spawn_channel(self, fn: Callable, *args, **kwargs) -> Channel
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |

**返回**: `Channel`

Source: `src\async_runtime_v2.py:342`

---

### `wait`

```matha
def wait(self, gid: int, timeout: float) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `gid` | `int` | |
| `timeout` | `float` | |

**返回**: `Any`

Source: `src\async_runtime_v2.py:345`

---

### `wait_all`

```matha
def wait_all(self, gids: list[int], timeout: float) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `gids` | `list[int]` | |
| `timeout` | `float` | |

**返回**: `list`

Source: `src\async_runtime_v2.py:348`

---

### `create_actor`

```matha
def create_actor(self, name: str, handler: Callable) -> Actor
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `handler` | `Callable` | |

**返回**: `Actor`

Source: `src\async_runtime_v2.py:351`

---

### `get_actor`

```matha
def get_actor(self, name: str) -> Optional[Actor]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Actor]`

Source: `src\async_runtime_v2.py:357`

---

### `shutdown`

```matha
def shutdown(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\async_runtime_v2.py:360`

---

### `pending`

```matha
def pending(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\async_runtime_v2.py:366`

---

## `async_spawn`

```matha
def async_spawn(fn: Callable, *args, **kwargs) -> int
```

**描述**: 便捷函数：启动 goroutine。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `Callable` | |

**返回**: `int`

Source: `src\async_runtime_v2.py:374`

---

## `async_wait`

```matha
def async_wait(gid: int, timeout: float) -> Any
```

**描述**: 便捷函数：等待 goroutine。

| 参数 | 类型 | 说明 |
|------|------|------|
| `gid` | `int` | |
| `timeout` | `float` | |

**返回**: `Any`

Source: `src\async_runtime_v2.py:379`

---

## `new_channel`

```matha
def new_channel(capacity: int) -> Channel
```

**描述**: 创建 channel。

| 参数 | 类型 | 说明 |
|------|------|------|
| `capacity` | `int` | |

**返回**: `Channel`

Source: `src\async_runtime_v2.py:384`

---

## `create_actor`

```matha
def create_actor(name: str, handler: Callable) -> Actor
```

**描述**: 创建 actor。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `handler` | `Callable` | |

**返回**: `Actor`

Source: `src\async_runtime_v2.py:389`

---

# `auth.api`

```matha

```

**描述**: 用户权限变更 API 接口设计。

提供管理端接口，用于运行时动态变更用户角色和权限。

设计原则:
  - 所有变更需管理员权限（user:manage）
  - 变更记录审计日志
  - 支持批量操作
  - 变更即时生效（影响后续请求）

Source: `src\auth\api.py:1`

---

## `ChangeType`

```matha
class ChangeType(str, Enum)
```

**描述**: 权限变更类型。

Source: `src\auth\api.py:30`

---

## `ChangeTarget`

```matha
class ChangeTarget(str, Enum)
```

**描述**: 变更作用目标。

Source: `src\auth\api.py:38`

---

## `PermissionChangeRequest`

```matha
class PermissionChangeRequest
```

**描述**: 权限变更请求。

**装饰器**: dataclass

Source: `src\auth\api.py:46`

---

## `PermissionChangeResult`

```matha
class PermissionChangeResult
```

**描述**: 权限变更结果。

**装饰器**: dataclass

Source: `src\auth\api.py:58`

---

## `PermissionChangeAPI`

```matha
class PermissionChangeAPI
```

**描述**: 用户权限变更 API。

用法:
    api = PermissionChangeAPI(session_mgr, rbac)

    # 给用户添加角色
    result = api.add_role("admin_user", ["editor"], operator="super_admin")

    # 修改用户角色列表
    result = api.set_roles("viewer_user", ["editor"], operator="super_admin")

    # 批量启用/禁用用户
    result = api.update_users(["u1", "u2"], is_active=False, operator="admin")

    # 审计日志
    for entry in api.audit_log:
        print(entry)

Source: `src\auth\api.py:72`

---

### `__init__`

```matha
def __init__(self, rbac: RBACMiddleware, mgr) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `rbac` | `RBACMiddleware` | |
| `mgr` | `` | |

**返回**: `None`

Source: `src\auth\api.py:92`

---

### `add_role`

```matha
def add_role(self, username, roles, operator, reason)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `` | |
| `roles` | `` | |
| `operator` | `` | |
| `reason` | `` | |

Source: `src\auth\api.py:101`

---

### `remove_role`

```matha
def remove_role(self, username, roles, operator, reason)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `` | |
| `roles` | `` | |
| `operator` | `` | |
| `reason` | `` | |

Source: `src\auth\api.py:119`

---

### `set_roles`

```matha
def set_roles(self, username, roles, operator, reason)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `` | |
| `roles` | `` | |
| `operator` | `` | |
| `reason` | `` | |

Source: `src\auth\api.py:137`

---

### `update_users`

```matha
def update_users(self, usernames)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `usernames` | `` | |

Source: `src\auth\api.py:153`

---

### `get_user_roles`

```matha
def get_user_roles(self, username: str) -> list[str]
```

**描述**: 查询用户当前角色列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `str` | |

**返回**: `list[str]`

Source: `src\auth\api.py:176`

---

### `get_role_permissions`

```matha
def get_role_permissions(self, role: str) -> set[str]
```

**描述**: 查询角色权限集合。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `role` | `str` | |

**返回**: `set[str]`

Source: `src\auth\api.py:183`

---

### `list_roles`

```matha
def list_roles(self) -> list[str]
```

**描述**: 列出所有可用角色。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\auth\api.py:187`

---

### `audit_log`

```matha
def audit_log(self) -> list[dict]
```

**描述**: 审计日志（只读）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

**装饰器**: property

Source: `src\auth\api.py:192`

---

### `clear_audit_log`

```matha
def clear_audit_log(self) -> int
```

**描述**: 清空审计日志，返回清理条数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\auth\api.py:196`

---

### `_check_admin_permission`

```matha
def _check_admin_permission(self, operator: str) -> None
```

**描述**: 检查操作者是否拥有 user:manage 权限。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `operator` | `str` | |

**返回**: `None`

Source: `src\auth\api.py:206`

---

## `example_usage`

```matha
def example_usage() -> None
```

**描述**: 权限变更 API 使用示例。

**返回**: `None`

Source: `src\auth\api.py:230`

---

# `auth.exceptions`

```matha

```

**描述**: 认证异常定义。

Source: `src\auth\exceptions.py:1`

---

## `AuthError`

```matha
class AuthError(Exception)
```

**描述**: 认证基础异常。

Source: `src\auth\exceptions.py:5`

---

### `__init__`

```matha
def __init__(self, message: str, code: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `code` | `str` | |

Source: `src\auth\exceptions.py:8`

---

## `AuthenticationError`

```matha
class AuthenticationError(AuthError)
```

**描述**: 登录失败 — 用户名/密码错误。

Source: `src\auth\exceptions.py:14`

---

### `__init__`

```matha
def __init__(self, message: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |

Source: `src\auth\exceptions.py:17`

---

## `AuthorizationError`

```matha
class AuthorizationError(AuthError)
```

**描述**: 权限不足。

Source: `src\auth\exceptions.py:21`

---

### `__init__`

```matha
def __init__(self, message: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |

Source: `src\auth\exceptions.py:24`

---

## `TokenError`

```matha
class TokenError(AuthError)
```

**描述**: 令牌相关错误。

Source: `src\auth\exceptions.py:28`

---

### `__init__`

```matha
def __init__(self, message: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |

Source: `src\auth\exceptions.py:31`

---

## `RegistrationError`

```matha
class RegistrationError(AuthError)
```

**描述**: 注册失败。

Source: `src\auth\exceptions.py:35`

---

### `__init__`

```matha
def __init__(self, message: str, code: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `code` | `str` | |

Source: `src\auth\exceptions.py:38`

---

# `auth.jwt`

```matha

```

**描述**: JWT 令牌管理。

Source: `src\auth\jwt.py:1`

---

## `_b64url_encode`

```matha
def _b64url_encode(data: bytes) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `str`

Source: `src\auth\jwt.py:16`

---

## `_b64url_decode`

```matha
def _b64url_decode(s: str) -> bytes
```

**描述**: 解码 base64url 字符串（兼容 Python 3.14 严格模式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `bytes`

Source: `src\auth\jwt.py:20`

---

## `encode_token`

```matha
def encode_token(payload: dict, secret: str, exp_hours: float) -> str
```

**描述**: 签发 JWT access token。

| 参数 | 类型 | 说明 |
|------|------|------|
| `payload` | `dict` | |
| `secret` | `str` | |
| `exp_hours` | `float` | |

**返回**: `str`

Source: `src\auth\jwt.py:29`

---

## `decode_token`

```matha
def decode_token(token: str, secret: str) -> Optional[dict]
```

**描述**: 验证并解码 JWT。过期或签名不符返回 None。

| 参数 | 类型 | 说明 |
|------|------|------|
| `token` | `str` | |
| `secret` | `str` | |

**返回**: `Optional[dict]`

Source: `src\auth\jwt.py:46`

---

## `encode_refresh_token`

```matha
def encode_refresh_token(payload: dict, secret: str, exp_days: float) -> str
```

**描述**: 签发 JWT refresh token（有效期更长）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `payload` | `dict` | |
| `secret` | `str` | |
| `exp_days` | `float` | |

**返回**: `str`

Source: `src\auth\jwt.py:66`

---

## `decode_refresh_token`

```matha
def decode_refresh_token(token: str, secret: str) -> Optional[dict]
```

**描述**: 验证 refresh token。

| 参数 | 类型 | 说明 |
|------|------|------|
| `token` | `str` | |
| `secret` | `str` | |

**返回**: `Optional[dict]`

Source: `src\auth\jwt.py:71`

---

## `get_token_expiry`

```matha
def get_token_expiry(token: str, secret: str) -> Optional[float]
```

**描述**: 获取 token 剩余有效时间（秒），无效时返回 None。

| 参数 | 类型 | 说明 |
|------|------|------|
| `token` | `str` | |
| `secret` | `str` | |

**返回**: `Optional[float]`

Source: `src\auth\jwt.py:76`

---

# `auth.models`

```matha

```

**描述**: 用户与会话数据模型。

Source: `src\auth\models.py:1`

---

## `User`

```matha
class User
```

**描述**: 用户数据模型。

**装饰器**: dataclass

Source: `src\auth\models.py:10`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\auth\models.py:20`

---

## `Session`

```matha
class Session
```

**描述**: 会话数据模型。

**装饰器**: dataclass

Source: `src\auth\models.py:32`

---

### `is_expired`

```matha
def is_expired(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\auth\models.py:42`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\auth\models.py:45`

---

# `auth.password`

```matha

```

**描述**: 密码哈希工具 — 基于 PBKDF2-HMAC-SHA256。

Source: `src\auth\password.py:1`

---

## `_b64url_encode`

```matha
def _b64url_encode(data: bytes) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `str`

Source: `src\auth\password.py:10`

---

## `_b64url_decode`

```matha
def _b64url_decode(s: str) -> bytes
```

**描述**: 解码 base64url 字符串（兼容 Python 3.14 严格模式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `bytes`

Source: `src\auth\password.py:14`

---

## `hash_password`

```matha
def hash_password(password: str, rounds: int) -> str
```

**描述**: 对密码进行 PBKDF2 哈希。格式: base64url(salt) . base64url(dk)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `password` | `str` | |
| `rounds` | `int` | |

**返回**: `str`

Source: `src\auth\password.py:23`

---

## `verify_password`

```matha
def verify_password(password: str, password_hash: str) -> bool
```

**描述**: 验证密码是否匹配哈希值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `password` | `str` | |
| `password_hash` | `str` | |

**返回**: `bool`

Source: `src\auth\password.py:30`

---

## `validate_password_strength`

```matha
def validate_password_strength(password: str) -> tuple[bool, str]
```

**描述**: 校验密码强度，返回 (是否通过, 错误信息)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `password` | `str` | |

**返回**: `tuple[bool, str]`

Source: `src\auth\password.py:42`

---

# `auth.rbac`

```matha

```

**描述**: 基于角色的访问控制（RBAC）中间件。

设计:
  - 角色（Role）：admin, editor, viewer, guest
  - 权限（Permission）：资源操作的最小粒度，如 "doc:read", "user:write"
  - 角色包含权限集合，用户继承角色
  - 支持通配符权限匹配： "doc:*" 匹配所有 doc 操作

用法:
    from src.auth.rbac import RBACMiddleware, Permission

    rbac = RBACMiddleware()
    rbac.register_role("admin", {"doc:read", "doc:write", "user:manage"})
    rbac.register_role("viewer", {"doc:read"})

    # 授权检查
    rbac.authorize(user.roles, "doc:read")       # OK
    rbac.authorize(user.roles, "doc:write")      # raises AuthorizationError
    rbac.authorize(user.roles, "doc:read", resource="my_doc")  # OK

    # 装饰器用法
    @rbac.require_permission("doc:write")
    def create_doc(user, name): ...

    # 检查用户是否拥有某权限
    rbac.has_permission(user.roles, "doc:read")  # True/False

Source: `src\auth\rbac.py:1`

---

## `Permission`

```matha
class Permission
```

**描述**: 权限标识符。格式: '资源:操作'。

Source: `src\auth\rbac.py:40`

---

### `__init__`

```matha
def __init__(self, resource: str, action: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `resource` | `str` | |
| `action` | `str` | |

Source: `src\auth\rbac.py:43`

---

### `__eq__`

```matha
def __eq__(self, other: object) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `object` | |

**返回**: `bool`

Source: `src\auth\rbac.py:48`

---

### `__hash__`

```matha
def __hash__(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\auth\rbac.py:55`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\auth\rbac.py:58`

---

## `RBACMiddleware`

```matha
class RBACMiddleware
```

**描述**: 基于角色的访问控制中间件。支持权限缓存加速多角色合并。

Source: `src\auth\rbac.py:89`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\auth\rbac.py:92`

---

### `register_role`

```matha
def register_role(self, name: str, permissions: Collection[str]) -> None
```

**描述**: 注册或更新角色及其权限集合。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `permissions` | `Collection[str]` | |

**返回**: `None`

Source: `src\auth\rbac.py:101`

---

### `remove_role`

```matha
def remove_role(self, name: str) -> bool
```

**描述**: 删除角色。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\auth\rbac.py:106`

---

### `get_role_permissions`

```matha
def get_role_permissions(self, role: str) -> frozenset
```

**描述**: 获取角色的权限集合。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `role` | `str` | |

**返回**: `frozenset`

Source: `src\auth\rbac.py:115`

---

### `list_roles`

```matha
def list_roles(self) -> list[str]
```

**描述**: 列出所有角色。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\auth\rbac.py:119`

---

### `_match`

```matha
def _match(self, pattern: str, target: str) -> bool
```

**描述**: 通配符匹配：'doc:*' 匹配 'doc:read' / 'doc:*' 匹配 'doc:*'。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pattern` | `str` | |
| `target` | `str` | |

**返回**: `bool`

Source: `src\auth\rbac.py:127`

---

### `_user_has_permission`

```matha
def _user_has_permission(self, roles: list[str], permission: str) -> bool
```

**描述**: 检查用户角色是否拥有指定权限（使用缓存加速多角色合并）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `roles` | `list[str]` | |
| `permission` | `str` | |

**返回**: `bool`

Source: `src\auth\rbac.py:136`

---

### `_get_merged_permissions`

```matha
def _get_merged_permissions(self, roles: list[str]) -> frozenset
```

**描述**: 获取角色合并后的权限集合，使用缓存加速。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `roles` | `list[str]` | |

**返回**: `frozenset`

Source: `src\auth\rbac.py:151`

---

### `has_permission`

```matha
def has_permission(self, roles: list[str], permission: str) -> bool
```

**描述**: 检查用户是否拥有指定权限。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `roles` | `list[str]` | |
| `permission` | `str` | |

**返回**: `bool`

Source: `src\auth\rbac.py:167`

---

### `authorize`

```matha
def authorize(self, roles: list[str], permission: str, resource: str) -> None
```

**描述**: 授权检查，无权限时抛出 AuthorizationError。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `roles` | `list[str]` | |
| `permission` | `str` | |
| `resource` | `str` | |

**返回**: `None`

Source: `src\auth\rbac.py:177`

---

### `get_effective_permissions`

```matha
def get_effective_permissions(self, roles: list[str]) -> frozenset
```

**描述**: 获取用户所有角色合并后的权限集合（使用缓存）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `roles` | `list[str]` | |

**返回**: `frozenset`

Source: `src\auth\rbac.py:194`

---

### `require_permission`

```matha
def require_permission(self, permission: str) -> Callable
```

**描述**: 权限检查装饰器。

用法:
    @rbac.require_permission("doc:write")
    def create_doc(user, name):
        ...

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `permission` | `str` | |

**返回**: `Callable`

Source: `src\auth\rbac.py:202`

---

### `require_any_permission`

```matha
def require_any_permission(self, *permissions) -> Callable
```

**描述**: 多权限 OR 检查装饰器（满足其一即可）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Callable`

Source: `src\auth\rbac.py:220`

---

## `get_rbac`

```matha
def get_rbac() -> RBACMiddleware
```

**描述**: 获取全局 RBAC 实例。

**返回**: `RBACMiddleware`

Source: `src\auth\rbac.py:243`

---

## `reset_rbac`

```matha
def reset_rbac() -> None
```

**描述**: 重置全局 RBAC 实例（用于测试）。

**返回**: `None`

Source: `src\auth\rbac.py:251`

---

# `auth.service`

```matha

```

**描述**: 会话管理器 — 内存存储用户与会话。

Source: `src\auth\service.py:1`

---

## `SessionManager`

```matha
class SessionManager
```

**描述**: 内存会话管理器。支持多用户注册、登录、登出、令牌刷新。

Source: `src\auth\service.py:22`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\auth\service.py:25`

---

### `register`

```matha
def register(self, username: str, email: str, password: str, roles: list[str] | None) -> User
```

**描述**: 注册新用户。重复用户名抛出 RegistrationError。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `str` | |
| `email` | `str` | |
| `password` | `str` | |
| `roles` | `list[str] | None` | |

**返回**: `User`

Source: `src\auth\service.py:37`

---

### `login`

```matha
def login(self, username: str, password: str) -> Session
```

**描述**: 验证凭据并创建新会话。失败抛出 AuthenticationError。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `str` | |
| `password` | `str` | |

**返回**: `Session`

Source: `src\auth\service.py:78`

---

### `refresh_token`

```matha
def refresh_token(self, refresh_token: str) -> tuple[str, str]
```

**描述**: 用 refresh token 换取新 access + refresh token 对。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `refresh_token` | `str` | |

**返回**: `tuple[str, str]`

Source: `src\auth\service.py:123`

---

### `logout`

```matha
def logout(self, session_id: str) -> bool
```

**描述**: 登出指定会话。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `session_id` | `str` | |

**返回**: `bool`

Source: `src\auth\service.py:207`

---

### `invalidate_all_sessions`

```matha
def invalidate_all_sessions(self, username: str) -> int
```

**描述**: 踢出用户所有会话（密码修改等场景）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `str` | |

**返回**: `int`

Source: `src\auth\service.py:236`

---

### `get_user`

```matha
def get_user(self, username: str) -> Optional[User]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `str` | |

**返回**: `Optional[User]`

Source: `src\auth\service.py:257`

---

### `get_session`

```matha
def get_session(self, session_id: str) -> Optional[Session]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `session_id` | `str` | |

**返回**: `Optional[Session]`

Source: `src\auth\service.py:262`

---

### `verify_access_token`

```matha
def verify_access_token(self, token: str) -> Optional[dict]
```

**描述**: 验证 access token 并返回 payload。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `token` | `str` | |

**返回**: `Optional[dict]`

Source: `src\auth\service.py:271`

---

### `get_active_session_count`

```matha
def get_active_session_count(self, username: str) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `username` | `str` | |

**返回**: `int`

Source: `src\auth\service.py:294`

---

### `get_all_usernames`

```matha
def get_all_usernames(self) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\auth\service.py:303`

---

### `count_users`

```matha
def count_users(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\auth\service.py:306`

---

## `_validate_password`

```matha
def _validate_password(password: str) -> tuple[bool, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `password` | `str` | |

**返回**: `tuple[bool, str]`

Source: `src\auth\service.py:314`

---

# `autonomous`

```matha

```

**描述**: Matha 自主成长扩展：自动发现、学习、进化 + 自升级子系统。

功能：
  1. 知识发现：自动扫描 matha/ 目录，发现新函数和公式
  2. 代码生成：根据需求生成 Matha 代码
  3. 自我改进：分析运行结果，自动优化参数
  4. 跨语言学习：读取 Python/JS 代码，转换为 Matha
  5. 知识库自动扩充
  6. 自升级：AutoDebugger / PerformanceOptimizer / SelfGrower

Source: `src\autonomous.py:1`

---

## `KnowledgeDiscovery`

```matha
class KnowledgeDiscovery
```

**描述**: 自动发现 Matha 知识库中的新函数和公式。

Source: `src\autonomous.py:29`

---

### `__init__`

```matha
def __init__(self, matha_root: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_root` | `str` | |

**返回**: `None`

Source: `src\autonomous.py:32`

---

### `discover`

```matha
def discover(self) -> list[dict]
```

**描述**: 扫描 matha/ 目录，发现所有函数定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\autonomous.py:36`

---

### `_scan_directory`

```matha
def _scan_directory(self, path: str) -> None
```

**描述**: 递归扫描目录。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `None`

Source: `src\autonomous.py:42`

---

### `_scan_matha_file`

```matha
def _scan_matha_file(self, filepath: str) -> None
```

**描述**: 解析 .matha 文件，提取函数和公式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `filepath` | `str` | |

**返回**: `None`

Source: `src\autonomous.py:53`

---

### `_detect_category`

```matha
def _detect_category(self, filepath: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `filepath` | `str` | |

**返回**: `str`

Source: `src\autonomous.py:73`

---

### `search`

```matha
def search(self, keyword: str) -> list[dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `keyword` | `str` | |

**返回**: `list[dict]`

Source: `src\autonomous.py:85`

---

## `MathaCodeGenerator`

```matha
class MathaCodeGenerator
```

**描述**: 根据需求自动生成 Matha 代码。

Source: `src\autonomous.py:96`

---

### `generate`

```matha
def generate(cls, template_name: str, **kwargs) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `template_name` | `str` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\autonomous.py:113`

---

## `LanguageConverter`

```matha
class LanguageConverter
```

**描述**: 将其他语言代码转换为 Matha。

Source: `src\autonomous.py:126`

---

### `python_to_matha`

```matha
def python_to_matha(code: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\autonomous.py:130`

---

### `js_to_matha`

```matha
def js_to_matha(code: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\autonomous.py:147`

---

## `SelfEvolution`

```matha
class SelfEvolution
```

**描述**: Matha 自我进化系统。

Source: `src\autonomous.py:168`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\autonomous.py:171`

---

### `learn`

```matha
def learn(self, source: str, category: str) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `category` | `str` | |

**返回**: `dict`

Source: `src\autonomous.py:175`

---

### `optimize`

```matha
def optimize(self, func_name: str, data: list, target: str) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `data` | `list` | |
| `target` | `str` | |

**返回**: `dict`

Source: `src\autonomous.py:189`

---

### `evolve`

```matha
def evolve(self, source: str, iterations: int) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `iterations` | `int` | |

**返回**: `dict`

Source: `src\autonomous.py:201`

---

## `_Sample`

```matha
class _Sample
```

**描述**: 性能采样结果。

Source: `src\autonomous.py:214`

---

### `__init__`

```matha
def __init__(self, calls, avg_ms, times, args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `calls` | `` | |
| `avg_ms` | `` | |
| `times` | `` | |
| `args` | `` | |

Source: `src\autonomous.py:216`

---

## `_OptResult`

```matha
class _OptResult
```

**描述**: 优化结果。

Source: `src\autonomous.py:223`

---

### `__init__`

```matha
def __init__(self, 成功, 变更)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `成功` | `` | |
| `变更` | `` | |

Source: `src\autonomous.py:225`

---

## `_GrowResult`

```matha
class _GrowResult
```

**描述**: 成长结果。

Source: `src\autonomous.py:230`

---

### `__init__`

```matha
def __init__(self, 成功, 新能力, 名称)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `成功` | `` | |
| `新能力` | `` | |
| `名称` | `` | |

Source: `src\autonomous.py:232`

---

## `AutoDebugger`

```matha
class AutoDebugger
```

**描述**: 自动调试器：检测并修复未定义变量和函数。

Source: `src\autonomous.py:240`

---

### `__init__`

```matha
def __init__(self, interp)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interp` | `` | |

Source: `src\autonomous.py:243`

---

### `debug`

```matha
def debug(self, src, max_attempts)
```

**描述**: 调试并修复源码。返回 {成功, 修复方案, 错误类型}。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `src` | `` | |
| `max_attempts` | `` | |

Source: `src\autonomous.py:246`

---

## `auto_debug`

```matha
def auto_debug(interp, src, max_attempts)
```

**描述**: 便捷函数：自动调试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `` | |
| `src` | `` | |
| `max_attempts` | `` | |

Source: `src\autonomous.py:292`

---

## `PerformanceOptimizer`

```matha
class PerformanceOptimizer
```

**描述**: 性能优化器：采样、热点识别、记忆化特化。

Source: `src\autonomous.py:300`

---

### `__init__`

```matha
def __init__(self, interp)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interp` | `` | |

Source: `src\autonomous.py:303`

---

### `profile`

```matha
def profile(self, func_name, args, runs)
```

**描述**: 性能采样。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `` | |
| `args` | `` | |
| `runs` | `` | |

Source: `src\autonomous.py:307`

---

### `hotspot`

```matha
def hotspot(self)
```

**描述**: 识别热点函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\autonomous.py:324`

---

### `optimize_memoize`

```matha
def optimize_memoize(self, func_name)
```

**描述**: 为函数生成记忆化特化版本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `` | |

Source: `src\autonomous.py:330`

---

## `auto_optimize_memoize`

```matha
def auto_optimize_memoize(interp, func_name)
```

**描述**: 便捷函数：记忆化优化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `` | |
| `func_name` | `` | |

Source: `src\autonomous.py:353`

---

## `SelfGrower`

```matha
class SelfGrower
```

**描述**: 自成长学习器：从源码/文件学习，参数特化。

Source: `src\autonomous.py:361`

---

### `__init__`

```matha
def __init__(self, interp)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interp` | `` | |

Source: `src\autonomous.py:364`

---

### `learn`

```matha
def learn(self, src, name)
```

**描述**: 从源码学习新函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `src` | `` | |
| `name` | `` | |

Source: `src\autonomous.py:368`

---

### `learn_from_file`

```matha
def learn_from_file(self, filepath)
```

**描述**: 从文件学习。优先 utf-8，失败时 fallback 到 gb18030。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `filepath` | `` | |

Source: `src\autonomous.py:379`

---

### `specialize`

```matha
def specialize(self, func_name, args)
```

**描述**: 为常用参数特化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `` | |
| `args` | `` | |

Source: `src\autonomous.py:393`

---

## `self_grow`

```matha
def self_grow(interp, src, name)
```

**描述**: 便捷函数：自成长学习。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `` | |
| `src` | `` | |
| `name` | `` | |

Source: `src\autonomous.py:407`

---

## `_install_matha_builtins`

```matha
def _install_matha_builtins(interp)
```

**描述**: 安装 Matha 侧自升级内建函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `` | |

Source: `src\autonomous.py:417`

---

## `build_software`

```matha
def build_software(interp, requirement: str) -> dict
```

**描述**: 自主构建软件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `` | |
| `requirement` | `str` | |

**返回**: `dict`

Source: `src\autonomous.py:437`

---

# `benchmarks.benchmark_calculus_matrix`

```matha

```

**描述**: Matha v4.4 — 符号微积分与矩阵运算性能基准测试

本脚本测试符号微积分和矩阵运算模块的性能。

功能：
  1. 符号求导性能基准
  2. 符号积分性能基准
  3. 矩阵乘法性能基准
  4. 矩阵分解性能基准
  5. 综合性能报告

用法：
  python src/benchmarks/benchmark_calculus_matrix.py
  python src/benchmarks/benchmark_calculus_matrix.py --verbose

Source: `src\benchmarks\benchmark_calculus_matrix.py:1`

---

## `setup_logging`

```matha
def setup_logging(verbose: bool)
```

**描述**: 设置日志级别。

| 参数 | 类型 | 说明 |
|------|------|------|
| `verbose` | `bool` | |

Source: `src\benchmarks\benchmark_calculus_matrix.py:33`

---

## `print_section`

```matha
def print_section(title: str)
```

**描述**: 打印章节标题。

| 参数 | 类型 | 说明 |
|------|------|------|
| `title` | `str` | |

Source: `src\benchmarks\benchmark_calculus_matrix.py:43`

---

## `benchmark`

```matha
def benchmark(func, iterations: int, warmup: int) -> dict
```

**描述**: 性能基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |
| `iterations` | `int` | |
| `warmup` | `int` | |

**返回**: `dict`

Source: `src\benchmarks\benchmark_calculus_matrix.py:50`

---

## `benchmark_symbolic_derivative`

```matha
def benchmark_symbolic_derivative()
```

**描述**: 符号求导性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:79`

---

## `benchmark_symbolic_integral`

```matha
def benchmark_symbolic_integral()
```

**描述**: 符号积分性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:104`

---

## `benchmark_definite_integral`

```matha
def benchmark_definite_integral()
```

**描述**: 定积分性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:127`

---

## `benchmark_matrix_multiply`

```matha
def benchmark_matrix_multiply()
```

**描述**: 矩阵乘法性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:151`

---

## `benchmark_matrix_determinant`

```matha
def benchmark_matrix_determinant()
```

**描述**: 矩阵行列式性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:169`

---

## `benchmark_matrix_inverse`

```matha
def benchmark_matrix_inverse()
```

**描述**: 矩阵求逆性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:186`

---

## `benchmark_matrix_eigenvalues`

```matha
def benchmark_matrix_eigenvalues()
```

**描述**: 特征值计算性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:205`

---

## `benchmark_svd`

```matha
def benchmark_svd()
```

**描述**: SVD 分解性能测试。

Source: `src\benchmarks\benchmark_calculus_matrix.py:228`

---

## `benchmark_combined`

```matha
def benchmark_combined()
```

**描述**: 综合性能测试（符号微积分 + 矩阵运算）。

Source: `src\benchmarks\benchmark_calculus_matrix.py:249`

---

## `main`

```matha
def main()
```

**描述**: 主函数。

Source: `src\benchmarks\benchmark_calculus_matrix.py:272`

---

# `benchmarks.benchmark_inverse_svd`

```matha

```

**描述**: Matha v4.4 — 矩阵求逆与 SVD 性能对比测试

本脚本对比测试矩阵求逆和 SVD 分解在不同场景下的性能：
  1. 纯 Python 实现 vs NumPy 实现
  2. 缓存命中 vs 缓存未命中
  3. 串行计算 vs 并行计算
  4. 不同矩阵规模的性能对比

用法：
  python src/benchmarks/benchmark_inverse_svd.py
  python src/benchmarks/benchmark_inverse_svd.py --verbose
  python src/benchmarks/benchmark_inverse_svd.py --sizes 10 50 100
  python src/benchmarks/benchmark_inverse_svd.py --parallel --workers 4

Source: `src\benchmarks\benchmark_inverse_svd.py:1`

---

## `setup_logging`

```matha
def setup_logging(verbose: bool)
```

**描述**: 设置日志级别。默认 INFO，--verbose 时启用 DEBUG。

| 参数 | 类型 | 说明 |
|------|------|------|
| `verbose` | `bool` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:31`

---

## `print_section`

```matha
def print_section(title: str)
```

**描述**: 打印章节标题。

| 参数 | 类型 | 说明 |
|------|------|------|
| `title` | `str` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:41`

---

## `benchmark`

```matha
def benchmark(func, iterations: int, warmup: int) -> dict
```

**描述**: 性能基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |
| `iterations` | `int` | |
| `warmup` | `int` | |

**返回**: `dict`

Source: `src\benchmarks\benchmark_inverse_svd.py:48`

---

## `benchmark_inverse_python`

```matha
def benchmark_inverse_python(sizes: list)
```

**描述**: 纯 Python 矩阵求逆性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sizes` | `list` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:77`

---

## `benchmark_inverse_numpy`

```matha
def benchmark_inverse_numpy(sizes: list)
```

**描述**: NumPy 矩阵求逆性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sizes` | `list` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:97`

---

## `benchmark_inverse_cached`

```matha
def benchmark_inverse_cached(sizes: list)
```

**描述**: 缓存矩阵求逆性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sizes` | `list` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:122`

---

## `benchmark_svd_python`

```matha
def benchmark_svd_python(sizes: list)
```

**描述**: 纯 Python SVD 性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sizes` | `list` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:156`

---

## `benchmark_svd_numpy`

```matha
def benchmark_svd_numpy(sizes: list)
```

**描述**: NumPy SVD 性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sizes` | `list` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:175`

---

## `benchmark_svd_cached`

```matha
def benchmark_svd_cached(sizes: list)
```

**描述**: 缓存 SVD 性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sizes` | `list` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:197`

---

## `benchmark_parallel_inverse`

```matha
def benchmark_parallel_inverse(n: int, n_matrices: int, n_workers: int)
```

**描述**: 并行矩阵求逆性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `n_matrices` | `int` | |
| `n_workers` | `int` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:230`

---

## `benchmark_parallel_svd`

```matha
def benchmark_parallel_svd(n: int, n_matrices: int, n_workers: int)
```

**描述**: 并行 SVD 性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `n_matrices` | `int` | |
| `n_workers` | `int` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:260`

---

## `print_comparison_report`

```matha
def print_comparison_report(inverse_results: dict, svd_results: dict)
```

**描述**: 打印性能对比报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `inverse_results` | `dict` | |
| `svd_results` | `dict` | |

Source: `src\benchmarks\benchmark_inverse_svd.py:292`

---

## `main`

```matha
def main()
```

**描述**: 主函数。

Source: `src\benchmarks\benchmark_inverse_svd.py:327`

---

# `benchmarks.benchmark_svd_analysis`

```matha

```

**描述**: Matha v4.4 — SVD 性能对比分析图表

本脚本生成详细的性能对比图表，展示纯 Python 和 NumPy 实现的 SVD 性能差异。

用法：
  python src/benchmarks/benchmark_svd_analysis.py
  python src/benchmarks/benchmark_svd_analysis.py --verbose
  python src/benchmarks/benchmark_svd_analysis.py --sizes 10 20 50 100

Source: `src\benchmarks\benchmark_svd_analysis.py:1`

---

## `BenchmarkResult`

```matha
class BenchmarkResult
```

**描述**: 基准测试结果。

**装饰器**: dataclass

Source: `src\benchmarks\benchmark_svd_analysis.py:28`

---

## `setup_logging`

```matha
def setup_logging(verbose: bool)
```

**描述**: 设置日志级别。

| 参数 | 类型 | 说明 |
|------|------|------|
| `verbose` | `bool` | |

Source: `src\benchmarks\benchmark_svd_analysis.py:38`

---

## `benchmark_svd_python`

```matha
def benchmark_svd_python(A_data: List[List[float]], iterations: int, warmup: int) -> float
```

**描述**: 基准测试纯 Python SVD。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A_data` | `List[List[float]]` | |
| `iterations` | `int` | |
| `warmup` | `int` | |

**返回**: `float`

Source: `src\benchmarks\benchmark_svd_analysis.py:48`

---

## `benchmark_svd_numpy`

```matha
def benchmark_svd_numpy(A_data: List[List[float]], iterations: int, warmup: int) -> Optional[float]
```

**描述**: 基准测试 NumPy SVD。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A_data` | `List[List[float]]` | |
| `iterations` | `int` | |
| `warmup` | `int` | |

**返回**: `Optional[float]`

Source: `src\benchmarks\benchmark_svd_analysis.py:69`

---

## `generate_random_matrix`

```matha
def generate_random_matrix(n: int, seed: int) -> List[List[float]]
```

**描述**: 生成随机矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `seed` | `int` | |

**返回**: `List[List[float]]`

Source: `src\benchmarks\benchmark_svd_analysis.py:92`

---

## `run_benchmark`

```matha
def run_benchmark(sizes: List[int]) -> List[BenchmarkResult]
```

**描述**: 运行所有基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sizes` | `List[int]` | |

**返回**: `List[BenchmarkResult]`

Source: `src\benchmarks\benchmark_svd_analysis.py:99`

---

## `print_performance_chart`

```matha
def print_performance_chart(results: List[BenchmarkResult], numpy_available: bool)
```

**描述**: 打印性能对比图表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `results` | `List[BenchmarkResult]` | |
| `numpy_available` | `bool` | |

Source: `src\benchmarks\benchmark_svd_analysis.py:143`

---

## `print_analysis_report`

```matha
def print_analysis_report(results: List[BenchmarkResult], numpy_available: bool)
```

**描述**: 打印性能分析报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `results` | `List[BenchmarkResult]` | |
| `numpy_available` | `bool` | |

Source: `src\benchmarks\benchmark_svd_analysis.py:173`

---

## `generate_csv_report`

```matha
def generate_csv_report(results: List[BenchmarkResult], output_path: str)
```

**描述**: 生成 CSV 格式报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `results` | `List[BenchmarkResult]` | |
| `output_path` | `str` | |

Source: `src\benchmarks\benchmark_svd_analysis.py:221`

---

## `main`

```matha
def main()
```

**描述**: 主函数。

Source: `src\benchmarks\benchmark_svd_analysis.py:238`

---

# `benchmarks.final_performance_summary`

```matha

```

**描述**: Matha v4.4 — 最终性能总结图表

包含纯 Python、NumPy 和稀疏 SVD 三种方案的详细对比。

Source: `src\benchmarks\final_performance_summary.py:1`

---

## `main`

```matha
def main()
```

**描述**: 生成最终性能总结图表。

Source: `src\benchmarks\final_performance_summary.py:19`

---

# `benchmarks.svd_final_summary`

```matha

```

**描述**: Matha v4.4 — SVD 最终性能总结

本脚本生成包含纯 Python、NumPy 和稀疏 SVD 三种方案的最终性能总结图表。

用法：
  python src/benchmarks/svd_final_summary.py
  python src/benchmarks/svd_final_summary.py --verbose

Source: `src\benchmarks\svd_final_summary.py:1`

---

## `setup_logging`

```matha
def setup_logging(verbose: bool)
```

**描述**: 设置日志级别。

| 参数 | 类型 | 说明 |
|------|------|------|
| `verbose` | `bool` | |

Source: `src\benchmarks\svd_final_summary.py:22`

---

## `generate_summary`

```matha
def generate_summary()
```

**描述**: 生成性能总结。

Source: `src\benchmarks\svd_final_summary.py:32`

---

# `benchmarks.svd_performance_prediction`

```matha

```

**描述**: Matha v4.4 — 稀疏 SVD 性能预测与可视化

本脚本基于现有性能数据，预测 1000x1000 规模矩阵的 SVD 耗时，
并生成可视化图表。

用法：
  python src/benchmarks/svd_performance_prediction.py
  python src/benchmarks/svd_performance_prediction.py --verbose
  python src/benchmarks/svd_performance_prediction.py --sizes 10 50 100 500 1000

Source: `src\benchmarks\svd_performance_prediction.py:1`

---

## `setup_logging`

```matha
def setup_logging(verbose: bool)
```

**描述**: 设置日志级别。

| 参数 | 类型 | 说明 |
|------|------|------|
| `verbose` | `bool` | |

Source: `src\benchmarks\svd_performance_prediction.py:26`

---

## `SVDPerformancePredictor`

```matha
class SVDPerformancePredictor
```

**描述**: SVD 性能预测器。

Source: `src\benchmarks\svd_performance_prediction.py:36`

---

### `__init__`

```matha
def __init__(self)
```

**描述**: 初始化预测器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\benchmarks\svd_performance_prediction.py:48`

---

### `fit_power_law`

```matha
def fit_power_law(self, sizes: List[int], times: List[float]) -> Dict
```

**描述**: 拟合幂律模型: time = a * n^b

使用最小二乘法拟合 log(time) = log(a) + b * log(n)

Returns:
    拟合参数 {a, b, r_squared}

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sizes` | `List[int]` | |
| `times` | `List[float]` | |

**返回**: `Dict`

Source: `src\benchmarks\svd_performance_prediction.py:55`

---

### `predict_time`

```matha
def predict_time(self, n: int, method: str, sparsity: float) -> float
```

**描述**: 预测指定规模矩阵的 SVD 耗时。

Args:
    n: 矩阵规模
    method: 算法类型 ('python', 'numpy', 'sparse')
    sparsity: 稀疏度（0-1，仅对 sparse 方法有效）

Returns:
    预测耗时（ms）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |
| `method` | `str` | |
| `sparsity` | `float` | |

**返回**: `float`

Source: `src\benchmarks\svd_performance_prediction.py:96`

---

### `predict_all`

```matha
def predict_all(self, target_sizes: List[int]) -> Dict
```

**描述**: 预测多个规模的耗时。

Args:
    target_sizes: 目标规模列表

Returns:
    预测结果字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target_sizes` | `List[int]` | |

**返回**: `Dict`

Source: `src\benchmarks\svd_performance_prediction.py:133`

---

### `generate_chart`

```matha
def generate_chart(self, results: Dict, output_path: Optional[str]) -> str
```

**描述**: 生成 ASCII 性能图表。

Args:
    results: 预测结果
    output_path: 输出路径（可选）

Returns:
    图表字符串

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `results` | `Dict` | |
| `output_path` | `Optional[str]` | |

**返回**: `str`

Source: `src\benchmarks\svd_performance_prediction.py:180`

---

### `generate_csv_report`

```matha
def generate_csv_report(self, results: Dict, output_path: str)
```

**描述**: 生成 CSV 报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `results` | `Dict` | |
| `output_path` | `str` | |

Source: `src\benchmarks\svd_performance_prediction.py:281`

---

## `main`

```matha
def main()
```

**描述**: 主函数。

Source: `src\benchmarks\svd_performance_prediction.py:303`

---

# `binary_codec`

```matha

```

**描述**: Matha 二进制/三进制转译模块。

提供数值与二进制/三进制字符串的互相转换，以及二进制↔三进制互转。
支持 Matha 专属编码格式（带 Matha 标记的进制编码），可与传统格式互译。

转译方向：
    整数 ⇄ 二进制字符串（"1010"）
    整数 ⇄ 三进制字符串（"210"）
    二进制字符串 ⇄ 三进制字符串
    整数 ⇄ Matha 专属二进制/三进制编码（"M2:1010" / "M3:210"）

运行时：python -m src.binary_codec  # 自测

Source: `src\binary_codec.py:1`

---

## `to_binary`

```matha
def to_binary(n: int) -> str
```

**描述**: 整数转二进制字符串（无前缀）。

10 → "1010"，0 → "0"，-5 → "-101"

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `str`

Source: `src\binary_codec.py:24`

---

## `from_binary`

```matha
def from_binary(s: str) -> int
```

**描述**: 二进制字符串转整数。

"1010" → 10，支持空格分隔（"1010 0011" → 163）

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `int`

Source: `src\binary_codec.py:40`

---

## `to_ternary`

```matha
def to_ternary(n: int) -> str
```

**描述**: 整数转三进制字符串（无前缀）。

21 → "210"，0 → "0"，-5 → "-12"

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `str`

Source: `src\binary_codec.py:54`

---

## `from_ternary`

```matha
def from_ternary(s: str) -> int
```

**描述**: 三进制字符串转整数。

"210" → 21，支持空格分隔

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `int`

Source: `src\binary_codec.py:70`

---

## `binary_to_ternary`

```matha
def binary_to_ternary(s: str) -> str
```

**描述**: 二进制字符串转三进制字符串。

"1010"(=10) → "101"(=10)  即 10 的三进制表示

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `str`

Source: `src\binary_codec.py:88`

---

## `ternary_to_binary`

```matha
def ternary_to_binary(s: str) -> str
```

**描述**: 三进制字符串转二进制字符串。

"210"(=21) → "10101"(=21)

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `str`

Source: `src\binary_codec.py:96`

---

## `to_matha_binary`

```matha
def to_matha_binary(n: int) -> str
```

**描述**: 整数转 Matha 专属二进制编码。

10 → "M2:1010"

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `str`

Source: `src\binary_codec.py:108`

---

## `to_matha_ternary`

```matha
def to_matha_ternary(n: int) -> str
```

**描述**: 整数转 Matha 专属三进制编码。

21 → "M3:210"

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `str`

Source: `src\binary_codec.py:116`

---

## `from_matha_code`

```matha
def from_matha_code(code: str) -> int
```

**描述**: Matha 专属编码转整数。

"M2:1010" → 10，"M3:210" → 21
无前缀时按十进制解析（兼容传统格式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |

**返回**: `int`

Source: `src\binary_codec.py:124`

---

## `matha_binary_to_ternary`

```matha
def matha_binary_to_ternary(code: str) -> str
```

**描述**: Matha 二进制编码转三进制编码。

"M2:1010" → "M3:101"

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |

**返回**: `str`

Source: `src\binary_codec.py:148`

---

## `matha_ternary_to_binary`

```matha
def matha_ternary_to_binary(code: str) -> str
```

**描述**: Matha 三进制编码转二进制编码。

"M3:210" → "M2:10101"

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |

**返回**: `str`

Source: `src\binary_codec.py:156`

---

## `encode`

```matha
def encode(n: int, radix: str) -> str
```

**描述**: 便捷编码入口。

radix: "matha-binary" / "matha-ternary" / "binary" / "ternary"

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `radix` | `str` | |

**返回**: `str`

Source: `src\binary_codec.py:164`

---

# `bootstrap`

```matha

```

**描述**: Matha 自举编译器：用 Matha 构建 Matha 的工具链。

架构：
  Phase 1: Matha (Python) → MIR → C 代码
  Phase 2: C 代码 → 原生机器码
  Phase 3: 用生成的编译器编译 Matha 源码

性能目标：
  - 解释器: ~75x 慢于 Python 原生
  - MIR+C: ~1-2x 快于 Python 原生 (安装 GCC 后)
  - MIR+Python: ~2-3x 快于解释器

Source: `src\bootstrap.py:1`

---

## `bootstrap_compile`

```matha
def bootstrap_compile(source: str, target: str, optimize: bool) -> str
```

**描述**: Bootstrap 编译 Matha 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `target` | `str` | |
| `optimize` | `bool` | |

**返回**: `str`

Source: `src\bootstrap.py:24`

---

## `compile_and_benchmark`

```matha
def compile_and_benchmark() -> dict
```

**描述**: 编译并基准测试。

**返回**: `dict`

Source: `src\bootstrap.py:57`

---

## `show_mir_example`

```matha
def show_mir_example() -> None
```

**描述**: 展示 MIR 示例。

**返回**: `None`

Source: `src\bootstrap.py:105`

---

# `codegen.backend`

```matha

```

**描述**: BackendGenerator：把 Matha 规格编译为 Python HTTP 服务成品。

输出文件：
  - server.py  可直接 `python server.py` 启动的 HTTP 服务

接口规格（["接口", 方法, 路径, 处理]）映射为路由：
  GET /api/hello  → 返回 JSON {"msg": "..."}
  POST /api/echo  → 回显请求体
  处理字段可以是返回 JSON 的 Python 表达式，或简单文本。

Source: `src\codegen\backend.py:1`

---

## `BackendGenerator`

```matha
class BackendGenerator(Generator)
```

**描述**: 后端服务生成器：AppSpec → Python http.server 脚本。

Source: `src\codegen\backend.py:17`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\backend.py:20`

---

### `_build_python`

```matha
def _build_python(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\backend.py:32`

---

### `_build_routes`

```matha
def _build_routes(self) -> str
```

**描述**: 构建路由处理代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\backend.py:67`

---

### `_handler_body`

```matha
def _handler_body(self, ep, body_var: str) -> str
```

**描述**: 生成处理函数体（返回 JSON 可序列化的表达式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ep` | `` | |
| `body_var` | `str` | |

**返回**: `str`

Source: `src\codegen\backend.py:103`

---

### `_class_name`

```matha
def _class_name(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\backend.py:115`

---

### `_escape_py`

```matha
def _escape_py(text: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\codegen\backend.py:126`

---

# `codegen.base`

```matha

```

**描述**: 代码生成器基类：规格树解析 + 分发。

规格树是 Matha 列表（Python list）的嵌套结构。本模块提供：
  - parse_app_spec(spec)  → 规范化的 AppSpec 字典
  - codegen(spec, out_dir) → 按类型分发到具体生成器，写文件，返回结果
  - Generator 基类：元素树遍历、属性序列化等公共工具

Source: `src\codegen\base.py:1`

---

## `CodegenError`

```matha
class CodegenError(Exception)
```

**描述**: 代码生成错误。

Source: `src\codegen\base.py:16`

---

## `Element`

```matha
class Element
```

**描述**: UI 元素：标签 + 文本 + 属性 + 子元素。

**装饰器**: dataclass

Source: `src\codegen\base.py:25`

---

### `is_void`

```matha
def is_void(self) -> bool
```

**描述**: HTML 自闭合标签（无子元素）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\codegen\base.py:32`

---

## `Endpoint`

```matha
class Endpoint
```

**描述**: 后端接口：方法 + 路径 + 处理逻辑。

**装饰器**: dataclass

Source: `src\codegen\base.py:38`

---

## `StyleRule`

```matha
class StyleRule
```

**描述**: 样式规则：选择器 + 属性。

**装饰器**: dataclass

Source: `src\codegen\base.py:46`

---

## `AppSpec`

```matha
class AppSpec
```

**描述**: 规范化的应用规格。

**装饰器**: dataclass

Source: `src\codegen\base.py:53`

---

## `parse_app_spec`

```matha
def parse_app_spec(spec: Any) -> AppSpec
```

**描述**: 把 Matha 列表规格树解析为 AppSpec。

输入格式：
    ["应用", 类型, 名称, [元素...]]            # 最简形式
    ["应用", 类型, 名称, [元素...], {字段}]    # 带额外字段

或直接 ["网页", 名称, [元素...]]（省略 "应用" 头）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `spec` | `Any` | |

**返回**: `AppSpec`

Source: `src\codegen\base.py:65`

---

## `_parse_item`

```matha
def _parse_item(item: Any, app: AppSpec) -> None
```

**描述**: 把单个规格项分类解析到 app 的对应列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `item` | `Any` | |
| `app` | `AppSpec` | |

**返回**: `None`

Source: `src\codegen\base.py:113`

---

## `_parse_element`

```matha
def _parse_element(item: Any) -> Optional[Element]
```

**描述**: 递归解析子元素。

| 参数 | 类型 | 说明 |
|------|------|------|
| `item` | `Any` | |

**返回**: `Optional[Element]`

Source: `src\codegen\base.py:164`

---

## `CodegenResult`

```matha
class CodegenResult
```

**描述**: 代码生成结果。

**装饰器**: dataclass

Source: `src\codegen\base.py:189`

---

### `as_dict`

```matha
def as_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\codegen\base.py:198`

---

## `codegen`

```matha
def codegen(spec: Any, out_dir: str) -> CodegenResult
```

**描述**: 按规格类型分发到对应生成器，写出文件。

Args:
    spec: Matha 列表规格树
    out_dir: 输出目录（None 则用 matha/output/<类型>/<名称>）

Returns:
    CodegenResult

| 参数 | 类型 | 说明 |
|------|------|------|
| `spec` | `Any` | |
| `out_dir` | `str` | |

**返回**: `CodegenResult`

Source: `src\codegen\base.py:216`

---

## `Generator`

```matha
class Generator
```

**描述**: 生成器基类：提供元素遍历、属性序列化、文件写出等公共工具。

子类需实现 generate() → CodegenResult。

Source: `src\codegen\base.py:264`

---

### `__init__`

```matha
def __init__(self, app: AppSpec, out_dir: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `app` | `AppSpec` | |
| `out_dir` | `str` | |

Source: `src\codegen\base.py:270`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\base.py:274`

---

### `_write`

```matha
def _write(self, filename: str, content: str) -> str
```

**描述**: 写出文件，返回绝对路径。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `filename` | `str` | |
| `content` | `str` | |

**返回**: `str`

Source: `src\codegen\base.py:279`

---

### `_serialize_attrs`

```matha
def _serialize_attrs(attrs: list[tuple[str, str]]) -> str
```

**描述**: 序列化属性对为 HTML 属性字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `attrs` | `list[tuple[str, str]]` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\codegen\base.py:289`

---

### `_escape_html`

```matha
def _escape_html(text: str) -> str
```

**描述**: HTML 文本转义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\codegen\base.py:306`

---

### `_element_to_html`

```matha
def _element_to_html(self, el: Element, indent: int) -> str
```

**描述**: 把 Element 序列化为 HTML 片段。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `el` | `Element` | |
| `indent` | `int` | |

**返回**: `str`

Source: `src\codegen\base.py:312`

---

# `codegen.desktop`

```matha

```

**描述**: DesktopGenerator：把 Matha 规格编译为 Python Tkinter 桌面应用成品。

输出文件：
  - main.py  可直接 `python main.py` 运行的桌面程序

支持的控件（16 种）：
  label/h1/h2/h3/p  → Label
  input             → Entry（单行输入）
  textarea/text     → Text（多行文本框）
  button            → Button
  checkbox          → Checkbutton（复选框）
  radio             → Radiobutton（单选框）
  select            → ttk.Combobox（下拉选择）
  list              → Listbox（列表框）
  slider            → Scale（滑块）
  canvas            → Canvas（画布）
  image             → Label + PhotoImage（图片）
  separator         → ttk.Separator（分隔线）
  frame/div         → Frame（容器）
  group             → LabelFrame（分组容器）
  table             → ttk.Treeview（表格）
  tab               → ttk.Notebook（标签页容器）

布局方式（通过属性指定）：
  默认：pack（垂直堆叠）
  grid：设置 row/col 属性 → grid(row=, column=)
  place：设置 x/y 属性 → place(x=, y=)

窗口属性（通过规格额外字段）：
  尺寸 = "400x300"
  背景 = "#f0f0f0"
  可调整 = "否"

事件处理：
  onclick 属性指定处理函数名，生成器自动绑定。
  处理函数可引用 self.控件名 取值/设值。
  支持弹窗：messagebox/filedialog/simpledialog/colorchooser。

Source: `src\codegen\desktop.py:1`

---

## `DesktopGenerator`

```matha
class DesktopGenerator(Generator)
```

**描述**: 桌面应用生成器：AppSpec → Python Tkinter 脚本。

Source: `src\codegen\desktop.py:45`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\desktop.py:48`

---

### `_build_python`

```matha
def _build_python(self) -> str
```

**描述**: 构建 Python Tkinter 脚本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\desktop.py:60`

---

### `_widget_code`

```matha
def _widget_code(self, el: Element, idx: int, var_name: str) -> tuple[str, list[str], list[str]]
```

**描述**: 生成单个控件的 Python 代码。

Returns:
    (控件创建代码, 处理函数定义列表, 变量声明列表)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `el` | `Element` | |
| `idx` | `int` | |
| `var_name` | `str` | |

**返回**: `tuple[str, list[str], list[str]]`

Source: `src\codegen\desktop.py:130`

---

### `_build_layout`

```matha
def _build_layout(self) -> list[str]
```

**描述**: 生成布局代码（pack/grid/place）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\codegen\desktop.py:305`

---

### `_attrs_dict`

```matha
def _attrs_dict(self, el: Element) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `el` | `Element` | |

**返回**: `dict[str, str]`

Source: `src\codegen\desktop.py:337`

---

### `_extract_handler`

```matha
def _extract_handler(onclick: str) -> str
```

**描述**: 从 onclick="foo()" 或 onclick="foo" 提取函数名 foo。

| 参数 | 类型 | 说明 |
|------|------|------|
| `onclick` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\codegen\desktop.py:341`

---

### `_class_name`

```matha
def _class_name(self) -> str
```

**描述**: 把应用名转为合法 Python 类名前缀。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\desktop.py:350`

---

### `_escape_py`

```matha
def _escape_py(text: str) -> str
```

**描述**: Python 字符串转义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\codegen\desktop.py:362`

---

# `codegen.game`

```matha

```

**描述**: GameGenerator：把 Matha 规格编译为 HTML5 Canvas 游戏。

游戏与建模同属「交互式可视化」体系：
  - 建模 = 静态 3D 场景展示（Model3DGenerator）
  - 游戏 = 动态 2D 交互场景（GameGenerator，Canvas + 游戏循环）

输出文件：
  - index.html  可直接双击运行的 Canvas 游戏

游戏规格元素（通过 AppSpec.elements 传入）：
  角色（player）   ：玩家控制的角色，支持键盘移动
  敌人（enemy）    ：自动移动的障碍物/敌人
  收集（collect）  ：可拾取的物品（加分）
  墙壁（wall）     ：不可穿越的障碍
  文字（text）     ：UI 文字（标题/提示）
  脚本（脚本）     ：自定义 JS 逻辑

额外字段（meta）：
  宽度 = "800"          画布宽
  高度 = "600"          画布高
  背景 = "#000"         画布背景色
  帧率 = "60"           目标帧率
  标题 = "贪吃蛇"       页面标题

游戏机制（自动生成）：
  - requestAnimationFrame 游戏循环
  - 键盘控制（方向键/WASD）
  - AABB 碰撞检测
  - 计分系统 + 生命值
  - 胜负判定
  - 重新开始

Source: `src\codegen\game.py:1`

---

## `GameGenerator`

```matha
class GameGenerator(Generator)
```

**描述**: 游戏生成器：AppSpec → HTML5 Canvas 游戏。

Source: `src\codegen\game.py:39`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\game.py:42`

---

### `_build_html`

```matha
def _build_html(self) -> str
```

**描述**: 构建完整 HTML 游戏页面。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\game.py:54`

---

### `_build_js`

```matha
def _build_js(self, players, enemies, collects, walls, cw: str, ch: str) -> str
```

**描述**: 生成游戏 JS 逻辑。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `players` | `` | |
| `enemies` | `` | |
| `collects` | `` | |
| `walls` | `` | |
| `cw` | `str` | |
| `ch` | `str` | |

**返回**: `str`

Source: `src\codegen\game.py:155`

---

### `_attrs_dict`

```matha
def _attrs_dict(self, el: Element) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `el` | `Element` | |

**返回**: `dict[str, str]`

Source: `src\codegen\game.py:332`

---

### `_escape_html`

```matha
def _escape_html(text: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\codegen\game.py:336`

---

# `codegen.kernel`

```matha

```

**描述**: KernelGenerator：把 Matha 规格编译为 x86 操作系统内核汇编代码。

输出文件（NASM 汇编，可用 nasm + ld 构建为 ISO/磁盘镜像）：
  - boot.asm       引导扇区（512 字节，MBR 兼容）
  - kernel.asm     内核核心（GDT/IDT/Syscall/VFS 骨架）
  - Makefile       构建脚本（nasm + ld）

Matha 规格示例：
  ["内核", "MyOS", "MyOS", [
    ["系统名", "MyOS", [], []],
    ["内核版本", "0.1", [], []],
    ["目标架构", "x86", [], []],
    ["系统调用", ["write", "exit", "read"], [], []],
    ["页大小", "4096", [], []],
  ]]

Source: `src\codegen\kernel.py:1`

---

## `KernelSpec`

```matha
class KernelSpec
```

**描述**: 内核规格解析。

Source: `src\codegen\kernel.py:27`

---

### `__init__`

```matha
def __init__(self, spec: list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `list` | |

Source: `src\codegen\kernel.py:30`

---

### `_parse`

```matha
def _parse(self, spec: list) -> None
```

**描述**: 解析规格树，提取内核参数。

规格格式：
  ["内核", "名称", "名称", [元素...], {额外字段}]
  或 ["应用", "内核", "名称", [元素...]]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `list` | |

**返回**: `None`

Source: `src\codegen\kernel.py:40`

---

## `KernelGenerator`

```matha
class KernelGenerator(Generator)
```

**描述**: x86 操作系统内核生成器。

Source: `src\codegen\kernel.py:86`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\kernel.py:92`

---

### `_gen_boot_asm`

```matha
def _gen_boot_asm(self, spec: KernelSpec) -> str
```

**描述**: 生成 512 字节引导扇区（MBR 兼容，实模式 → 保护模式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `KernelSpec` | |

**返回**: `str`

Source: `src\codegen\kernel.py:142`

---

### `_gen_kernel_asm`

```matha
def _gen_kernel_asm(self, spec: KernelSpec) -> str
```

**描述**: 生成内核核心（IDT + Syscall + 简单 VFS 骨架）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `KernelSpec` | |

**返回**: `str`

Source: `src\codegen\kernel.py:253`

---

### `_gen_makefile`

```matha
def _gen_makefile(self, spec: KernelSpec) -> str
```

**描述**: 生成构建脚本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `KernelSpec` | |

**返回**: `str`

Source: `src\codegen\kernel.py:475`

---

### `_build_syscall_table`

```matha
def _build_syscall_table(self, spec: KernelSpec) -> list
```

**描述**: 根据规格中的系统调用列表生成 syscall 名称。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `KernelSpec` | |

**返回**: `list`

Source: `src\codegen\kernel.py:524`

---

## `generate_kernel`

```matha
def generate_kernel(spec, out_dir)
```

**描述**: 快捷函数：生成内核代码。

spec: Matha 列表规格（同 AppSpec 格式）
out_dir: 输出目录（默认: D:/trae/matha/output/内核/<名称>/）

| 参数 | 类型 | 说明 |
|------|------|------|
| `spec` | `` | |
| `out_dir` | `` | |

Source: `src\codegen\kernel.py:529`

---

# `codegen.model3d`

```matha

```

**描述**: Model3DGenerator：把 Matha 规格编译为 Three.js 3D 模型场景。

建模与游戏同属「交互式可视化」体系：
  - 游戏 = 动态 2D 交互场景（GameGenerator，Canvas + 游戏循环）
  - 建模 = 静态/动画 3D 场景展示（Model3DGenerator，Three.js + WebGL）

输出文件：
  - index.html  可直接双击运行的 Three.js 3D 场景（CDN 加载 Three.js）

建模规格元素（通过 AppSpec.elements 传入）：
  立方体（box/cube）       ：参数 size/w/h/d
  球体（sphere/ball）      ：参数 r
  圆柱（cylinder）         ：参数 r/h
  圆锥（cone）             ：参数 r/h
  圆环（torus）            ：参数 r/tube
  平面（plane）            ：参数 w/h
  齿轮（gear）             ：参数 r/teeth
  建筑（building）         ：参数 w/h/d/floors
  光源（light）            ：参数 type/color/intensity
  相机（camera）           ：参数 x/y/z/fov
  文字（h1/p/text）        ：页面 UI 文字

额外字段（meta）：
  背景 = "#1a1a2e"        场景背景色
  动画 = "旋转"           旋转/弹跳/自转/无
  材质 = "金属"           金属/玻璃/标准/线框
  颜色 = "#e94560"        默认材质颜色
  轨道控制 = "是"         是否启用鼠标轨道控制（OrbitControls）

支持：
  - 参数化几何体（10+ 种）
  - 材质系统（MeshStandard/MeshPhysical/线框）
  - 光照（环境光/方向光/点光）
  - 透视相机 + 轨道控制
  - 动画（旋转/弹跳/自转）
  - 多对象组合场景

Source: `src\codegen\model3d.py:1`

---

## `Model3DGenerator`

```matha
class Model3DGenerator(Generator)
```

**描述**: 3D 建模生成器：AppSpec → Three.js WebGL 场景。

Source: `src\codegen\model3d.py:44`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\model3d.py:47`

---

### `_build_html`

```matha
def _build_html(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\model3d.py:59`

---

### `_build_meshes_js`

```matha
def _build_meshes_js(self, meshes: list, default_mat: str, default_color: str) -> str
```

**描述**: 生成几何体 JS 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `meshes` | `list` | |
| `default_mat` | `str` | |
| `default_color` | `str` | |

**返回**: `str`

Source: `src\codegen\model3d.py:181`

---

### `_geometry_js`

```matha
def _geometry_js(self, tag: str, a: dict) -> str | None
```

**描述**: 返回 Three.js 几何体构造代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tag` | `str` | |
| `a` | `dict` | |

**返回**: `str | None`

Source: `src\codegen\model3d.py:217`

---

### `_material_js`

```matha
def _material_js(self, material: str, color: str) -> str
```

**描述**: 返回材质构造代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `material` | `str` | |
| `color` | `str` | |

**返回**: `str`

Source: `src\codegen\model3d.py:264`

---

### `_build_lights_js`

```matha
def _build_lights_js(self, lights: list) -> str
```

**描述**: 生成光源 JS。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lights` | `list` | |

**返回**: `str`

Source: `src\codegen\model3d.py:281`

---

### `_build_camera_js`

```matha
def _build_camera_js(self, camera_cfg: dict | None) -> str
```

**描述**: 生成相机 JS。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `camera_cfg` | `dict | None` | |

**返回**: `str`

Source: `src\codegen\model3d.py:321`

---

### `_build_anim_js`

```matha
def _build_anim_js(self, animation: str, meshes: list) -> str
```

**描述**: 生成动画 JS。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `animation` | `str` | |
| `meshes` | `list` | |

**返回**: `str`

Source: `src\codegen\model3d.py:341`

---

### `_attrs_dict`

```matha
def _attrs_dict(self, el: Element) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `el` | `Element` | |

**返回**: `dict[str, str]`

Source: `src\codegen\model3d.py:357`

---

### `_escape_html`

```matha
def _escape_html(text: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\codegen\model3d.py:361`

---

# `codegen.system`

```matha

```

**描述**: SystemGenerator：把 Matha 规格编译为系统脚本成品。

输出文件：
  - run.sh   Unix/macOS 脚本
  - run.bat  Windows 批处理

系统脚本由 ["接口", 方法, 路径, 处理] 中的"方法"映射为命令类型：
  命令/exec → 执行命令
  文件/file → 创建文件（处理作为内容）
  目录/dir  → 创建目录
  默认       → echo 输出

Source: `src\codegen\system.py:1`

---

## `SystemGenerator`

```matha
class SystemGenerator(Generator)
```

**描述**: 系统脚本生成器：AppSpec → .sh / .bat 脚本。

Source: `src\codegen\system.py:19`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\system.py:22`

---

### `_build_sh`

```matha
def _build_sh(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\system.py:37`

---

### `_build_bat`

```matha
def _build_bat(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\system.py:52`

---

### `_safe_sh`

```matha
def _safe_sh(self, text: str) -> str
```

**描述**: Escapes a string for safe embedding in shell commands.

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\codegen\system.py:67`

---

### `_ep_to_sh`

```matha
def _ep_to_sh(self, ep) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ep` | `` | |

**返回**: `str`

Source: `src\codegen\system.py:71`

---

### `_ep_to_bat`

```matha
def _ep_to_bat(self, ep) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ep` | `` | |

**返回**: `str`

Source: `src\codegen\system.py:82`

---

# `codegen.web`

```matha

```

**描述**: WebGenerator：把 Matha 规格编译为 HTML/CSS/JS 网页成品。

输出文件：
  - index.html   主页面（含内联 CSS/JS，单文件可双击打开）
  - style.css    样式（若样式规则≥1）
  - script.js    脚本（若脚本片段≥1）

布局辅助：Matha 侧可用数学方程算出网格位置/尺寸，通过
  ["样式", 选择器, [("left", "24px"), ("top", "32px")]]
  传入计算结果，本生成器原样序列化为 CSS。

Source: `src\codegen\web.py:1`

---

## `WebGenerator`

```matha
class WebGenerator(Generator)
```

**描述**: 网页生成器：AppSpec → HTML/CSS/JS 文件。

Source: `src\codegen\web.py:19`

---

### `generate`

```matha
def generate(self) -> CodegenResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CodegenResult`

Source: `src\codegen\web.py:22`

---

### `_build_html`

```matha
def _build_html(self) -> str
```

**描述**: 构建完整 HTML 文档。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\web.py:46`

---

### `_build_css`

```matha
def _build_css(self) -> str
```

**描述**: 构建 CSS 文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\web.py:78`

---

### `_build_js`

```matha
def _build_js(self) -> str
```

**描述**: 构建 JS 文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\codegen\web.py:89`

---

# `collaboration`

```matha

```

**描述**: Matha 协作引擎 — WebSocket + 实时同步

提供多人协作编辑功能：
  - WebSocket 实时通信
  - 操作变换（OT）算法
  - 邀请系统
  - 实时聊天

Source: `src\collaboration.py:1`

---

## `Collaborator`

```matha
class Collaborator
```

**描述**: 协作者

**装饰器**: dataclass

Source: `src\collaboration.py:26`

---

## `Operation`

```matha
class Operation
```

**描述**: 操作（用于 OT 算法）

**装饰器**: dataclass

Source: `src\collaboration.py:37`

---

## `ChatMessage`

```matha
class ChatMessage
```

**描述**: 聊天消息

**装饰器**: dataclass

Source: `src\collaboration.py:50`

---

## `Invite`

```matha
class Invite
```

**描述**: 协作邀请

**装饰器**: dataclass

Source: `src\collaboration.py:61`

---

## `OperationTransformer`

```matha
class OperationTransformer
```

**描述**: 操作变换器（基于 JSON Patch 的 OT）

Source: `src\collaboration.py:75`

---

### `transform`

```matha
def transform(self, op_a: Operation, op_b: Operation) -> tuple[Operation, Operation]
```

**描述**: 变换两个并发操作，确保最终一致性。

策略：
- 如果操作目标不同，保持不变
- 如果操作目标相同且类型相同，优先早期操作
- 如果操作目标相同但类型不同，调整目标引用

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op_a` | `Operation` | |
| `op_b` | `Operation` | |

**返回**: `tuple[Operation, Operation]`

Source: `src\collaboration.py:78`

---

### `_adjust_after_delete`

```matha
def _adjust_after_delete(self, op: Operation, deleted: Operation) -> Operation
```

**描述**: 操作在被删除节点之后执行时的调整

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op` | `Operation` | |
| `deleted` | `Operation` | |

**返回**: `Operation`

Source: `src\collaboration.py:99`

---

### `apply_operations`

```matha
def apply_operations(self, operations: List[Operation], state: dict) -> dict
```

**描述**: 按顺序应用操作序列到状态

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `operations` | `List[Operation]` | |
| `state` | `dict` | |

**返回**: `dict`

Source: `src\collaboration.py:107`

---

### `_apply_single`

```matha
def _apply_single(self, op: Operation, state: dict) -> dict
```

**描述**: 应用单个操作

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op` | `Operation` | |
| `state` | `dict` | |

**返回**: `dict`

Source: `src\collaboration.py:113`

---

## `CollaborationSession`

```matha
class CollaborationSession
```

**描述**: 协作会话管理器

Source: `src\collaboration.py:136`

---

### `__init__`

```matha
def __init__(self, session_id: str, project_id: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `session_id` | `str` | |
| `project_id` | `str` | |

Source: `src\collaboration.py:139`

---

### `collaborator_count`

```matha
def collaborator_count(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\collaboration.py:156`

---

### `collaborators`

```matha
def collaborators(self) -> Dict[str, Collaborator]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Collaborator]`

**装饰器**: property

Source: `src\collaboration.py:160`

---

### `join`

```matha
def join(self, user_id: str, username: str, color: str) -> Collaborator
```

**描述**: 加入协作会话

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `user_id` | `str` | |
| `username` | `str` | |
| `color` | `str` | |

**返回**: `Collaborator`

Source: `src\collaboration.py:165`

---

### `leave`

```matha
def leave(self, user_id: str) -> None
```

**描述**: 离开协作会话

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `user_id` | `str` | |

**返回**: `None`

Source: `src\collaboration.py:188`

---

### `update_cursor`

```matha
def update_cursor(self, user_id: str, x: float, y: float) -> None
```

**描述**: 更新协作者光标位置

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `user_id` | `str` | |
| `x` | `float` | |
| `y` | `float` | |

**返回**: `None`

Source: `src\collaboration.py:199`

---

### `broadcast_operation`

```matha
def broadcast_operation(self, op: Operation) -> None
```

**描述**: 广播操作（OT 变换后）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op` | `Operation` | |

**返回**: `None`

Source: `src\collaboration.py:206`

---

### `get_operation_history`

```matha
def get_operation_history(self, limit: int) -> List[Operation]
```

**描述**: 获取操作历史

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |

**返回**: `List[Operation]`

Source: `src\collaboration.py:222`

---

### `send_message`

```matha
def send_message(self, user_id: str, username: str, content: str, is_system: bool) -> ChatMessage
```

**描述**: 发送聊天消息

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `user_id` | `str` | |
| `username` | `str` | |
| `content` | `str` | |
| `is_system` | `bool` | |

**返回**: `ChatMessage`

Source: `src\collaboration.py:228`

---

### `get_chat_history`

```matha
def get_chat_history(self, limit: int) -> List[ChatMessage]
```

**描述**: 获取聊天记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |

**返回**: `List[ChatMessage]`

Source: `src\collaboration.py:245`

---

### `create_invite`

```matha
def create_invite(self, inviter_id: str, inviter_name: str, max_members: int) -> Invite
```

**描述**: 创建协作邀请

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `inviter_id` | `str` | |
| `inviter_name` | `str` | |
| `max_members` | `int` | |

**返回**: `Invite`

Source: `src\collaboration.py:251`

---

### `redeem_invite`

```matha
def redeem_invite(self, code: str, user_id: str, username: str) -> Optional[Collaborator]
```

**描述**: 兑换邀请码

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `user_id` | `str` | |
| `username` | `str` | |

**返回**: `Optional[Collaborator]`

Source: `src\collaboration.py:271`

---

### `get_invite_info`

```matha
def get_invite_info(self, code: str) -> Optional[dict]
```

**描述**: 获取邀请信息

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |

**返回**: `Optional[dict]`

Source: `src\collaboration.py:285`

---

### `on_operation`

```matha
def on_operation(self, callback: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\collaboration.py:301`

---

### `on_chat`

```matha
def on_chat(self, callback: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\collaboration.py:304`

---

### `on_invite`

```matha
def on_invite(self, callback: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\collaboration.py:307`

---

### `on_member_join`

```matha
def on_member_join(self, callback: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\collaboration.py:310`

---

### `on_member_leave`

```matha
def on_member_leave(self, callback: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\collaboration.py:313`

---

### `get_state`

```matha
def get_state(self) -> dict
```

**描述**: 获取当前状态

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\collaboration.py:318`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

**描述**: 序列化为字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\collaboration.py:337`

---

## `create_session`

```matha
def create_session(session_id: str, project_id: str) -> CollaborationSession
```

**描述**: 创建协作会话

| 参数 | 类型 | 说明 |
|------|------|------|
| `session_id` | `str` | |
| `project_id` | `str` | |

**返回**: `CollaborationSession`

Source: `src\collaboration.py:348`

---

## `get_session`

```matha
def get_session(session_id: str) -> Optional[CollaborationSession]
```

**描述**: 获取协作会话

| 参数 | 类型 | 说明 |
|------|------|------|
| `session_id` | `str` | |

**返回**: `Optional[CollaborationSession]`

Source: `src\collaboration.py:356`

---

## `destroy_session`

```matha
def destroy_session(session_id: str) -> bool
```

**描述**: 销毁协作会话

| 参数 | 类型 | 说明 |
|------|------|------|
| `session_id` | `str` | |

**返回**: `bool`

Source: `src\collaboration.py:361`

---

## `list_sessions`

```matha
def list_sessions() -> List[dict]
```

**描述**: 列出所有会话

**返回**: `List[dict]`

Source: `src\collaboration.py:370`

---

# `compiler.aot`

```matha

```

**描述**: Matha AOT/JIT 编译器 + 常驻缓存解释器。

核心升级：
  1. AOT 编译：将 Matha AST 编译为等价的 Python 字节码
  2. Trampoline：消除递归栈溢出，实现 O(1) 尾递归
  3. 常驻缓存：按源文件哈希缓存已编译函数，避免重复编译
  4. SIMD 向量化：对数值数组运算自动向量化
  5. 循环展开：对小循环自动展开

性能目标：
  - 简单算术：接近 Python 原生
  - 递归函数：10-100x 加速（尾递归 → 循环）
  - 数组运算：2-10x 加速（SIMD 向量化）

Source: `src\compiler\aot.py:1`

---

## `Trampoline`

```matha
class Trampoline
```

**描述**: Trampoline 模式：将递归调用转换为循环，避免栈溢出。

用法：
    result = tramp.run(lambda: recursive_func(x))

Source: `src\compiler\aot.py:32`

---

### `run`

```matha
def run(fn: Callable) -> Any
```

**描述**: 运行 trampolinized 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `Callable` | |

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\compiler\aot.py:41`

---

### `yield_`

```matha
def yield_(fn: Callable) -> tuple
```

**描述**: 返回 trampoline step。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `Callable` | |

**返回**: `tuple`

**装饰器**: staticmethod

Source: `src\compiler\aot.py:50`

---

## `MathaAOTCompiler`

```matha
class MathaAOTCompiler
```

**描述**: 将 Matha AST 编译为优化的 Python 字节码。

Source: `src\compiler\aot.py:59`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\aot.py:66`

---

### `compile_program`

```matha
def compile_program(self, program_ast) -> dict
```

**描述**: 编译整个程序，返回可执行函数字典。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program_ast` | `` | |

**返回**: `dict`

Source: `src\compiler\aot.py:69`

---

### `_build_namespace`

```matha
def _build_namespace(self) -> dict
```

**描述**: 构建编译环境的命名空间。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\compiler\aot.py:81`

---

### `_ast_to_python`

```matha
def _ast_to_python(self, program_ast) -> str
```

**描述**: 将 Matha AST 转换为 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program_ast` | `` | |

**返回**: `str`

Source: `src\compiler\aot.py:111`

---

### `_compile_func`

```matha
def _compile_func(self, func_def) -> list[str]
```

**描述**: 编译函数定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_def` | `` | |

**返回**: `list[str]`

Source: `src\compiler\aot.py:132`

---

### `_compile_stmt`

```matha
def _compile_stmt(self, stmt) -> list[str]
```

**描述**: 编译语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `` | |

**返回**: `list[str]`

Source: `src\compiler\aot.py:145`

---

### `_compile_block`

```matha
def _compile_block(self, block) -> list[str]
```

**描述**: 编译代码块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `block` | `` | |

**返回**: `list[str]`

Source: `src\compiler\aot.py:169`

---

### `_compile_expr`

```matha
def _compile_expr(self, expr) -> str
```

**描述**: 编译表达式为 Python 代码字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `str`

Source: `src\compiler\aot.py:176`

---

### `compile_and_cache`

```matha
def compile_and_cache(self, source: str, program_ast) -> dict
```

**描述**: 编译并缓存结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `program_ast` | `` | |

**返回**: `dict`

Source: `src\compiler\aot.py:275`

---

## `LoopUnroller`

```matha
class LoopUnroller
```

**描述**: 对小循环进行自动展开优化。

Source: `src\compiler\aot.py:295`

---

### `unroll`

```matha
def unroll(code: str, threshold: int) -> str
```

**描述**: 展开小循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |
| `threshold` | `int` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\compiler\aot.py:299`

---

## `SIMDVectorizer`

```matha
class SIMDVectorizer
```

**描述**: 对数值数组运算自动向量化。

Source: `src\compiler\aot.py:321`

---

### `vectorize`

```matha
def vectorize(expr: str, array_name: str) -> str
```

**描述**: 将标量表达式转换为数组向量化版本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `array_name` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\compiler\aot.py:325`

---

### `is_vectorizable`

```matha
def is_vectorizable(code: str) -> bool
```

**描述**: 检查代码是否可向量化的。

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\compiler\aot.py:337`

---

## `AOTProfiler`

```matha
class AOTProfiler
```

**描述**: AOT 编译性能分析器。

Source: `src\compiler\aot.py:347`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\aot.py:350`

---

### `profile`

```matha
def profile(self, name: str, source: str, iterations: int) -> dict
```

**描述**: 对源码进行性能测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `source` | `str` | |
| `iterations` | `int` | |

**返回**: `dict`

Source: `src\compiler\aot.py:353`

---

### `report`

```matha
def report(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\compiler\aot.py:390`

---

## `CompilerCache`

```matha
class CompilerCache
```

**描述**: 编译器缓存管理。

Source: `src\compiler\aot.py:407`

---

### `__init__`

```matha
def __init__(self, cache_dir: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cache_dir` | `str` | |

**返回**: `None`

Source: `src\compiler\aot.py:410`

---

### `get`

```matha
def get(self, key: str) -> Optional[dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `Optional[dict]`

Source: `src\compiler\aot.py:416`

---

### `set`

```matha
def set(self, key: str, value: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `value` | `dict` | |

**返回**: `None`

Source: `src\compiler\aot.py:423`

---

### `invalidate`

```matha
def invalidate(self, key: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `None`

Source: `src\compiler\aot.py:428`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\aot.py:433`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\compiler\aot.py:440`

---

# `compiler.ir`

```matha

```

**描述**: Matha v4.2 — 统一 IR 编译器接口

将 Matha IR 编译到不同后端：
  - Python: 解释执行（开发调试）
  - LLVM: 原生机器码（高性能）
  - C: 跨平台编译

架构：
  Matha IR → IRCompiler → 目标语言 → 执行

用法：
  from src.compiler.ir import IRCompiler, IRBackend, CompileTarget

  compiler = IRCompiler()
  result = compiler.compile(mir_code, target=CompileTarget(backend=IRBackend.LLVM))

Source: `src\compiler\ir.py:1`

---

## `IRBackend`

```matha
class IRBackend(Enum)
```

**描述**: 编译后端类型。

Source: `src\compiler\ir.py:37`

---

## `CompileTarget`

```matha
class CompileTarget
```

**描述**: 编译目标配置。

**装饰器**: dataclass

Source: `src\compiler\ir.py:46`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 初始化后处理。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\compiler\ir.py:55`

---

## `CompileResult`

```matha
class CompileResult
```

**描述**: 编译结果。

**装饰器**: dataclass

Source: `src\compiler\ir.py:62`

---

### `is_ok`

```matha
def is_ok(self) -> bool
```

**描述**: 检查是否成功。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\compiler\ir.py:74`

---

### `unwrap`

```matha
def unwrap(self) -> Any
```

**描述**: 获取结果（失败时抛出异常）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\compiler\ir.py:78`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

**描述**: 序列化为字典。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\compiler\ir.py:84`

---

## `IRCompiler`

```matha
class IRCompiler(abc.ABC)
```

**描述**: 统一 IR 编译器接口。

所有后端编译器必须继承此类并实现 compile() 方法。

Source: `src\compiler\ir.py:100`

---

### `__init__`

```matha
def __init__(self, backend: IRBackend)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `backend` | `IRBackend` | |

Source: `src\compiler\ir.py:107`

---

### `compile`

```matha
def compile(self, mir_code: str, target: CompileTarget) -> CompileResult
```

**描述**: 编译 Matha IR 到目标后端。

Args:
    mir_code: Matha 中间表示代码
    target: 编译目标配置

Returns:
    CompileResult: 编译结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |
| `target` | `CompileTarget` | |

**返回**: `CompileResult`

**装饰器**: abc.abstractmethod

Source: `src\compiler\ir.py:112`

---

### `_cache_key`

```matha
def _cache_key(self, code: str) -> str
```

**描述**: 生成缓存键。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |

**返回**: `str`

Source: `src\compiler\ir.py:125`

---

### `_get_cached`

```matha
def _get_cached(self, key: str, target: CompileTarget) -> Optional[CompileResult]
```

**描述**: 获取缓存结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `target` | `CompileTarget` | |

**返回**: `Optional[CompileResult]`

Source: `src\compiler\ir.py:129`

---

### `_save_cache`

```matha
def _save_cache(self, key: str, result: CompileResult, target: CompileTarget)
```

**描述**: 保存缓存结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `result` | `CompileResult` | |
| `target` | `CompileTarget` | |

Source: `src\compiler\ir.py:149`

---

## `PythonCompiler`

```matha
class PythonCompiler(IRCompiler)
```

**描述**: Python 后端编译器。

Source: `src\compiler\ir.py:163`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\compiler\ir.py:166`

---

### `compile`

```matha
def compile(self, mir_code: str, target: CompileTarget) -> CompileResult
```

**描述**: 编译到 Python 代码并执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |
| `target` | `CompileTarget` | |

**返回**: `CompileResult`

Source: `src\compiler\ir.py:169`

---

### `_translate`

```matha
def _translate(self, mir_code: str) -> str
```

**描述**: Matha IR → Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |

**返回**: `str`

Source: `src\compiler\ir.py:213`

---

## `LLVMCompiler`

```matha
class LLVMCompiler(IRCompiler)
```

**描述**: LLVM 后端编译器。

Source: `src\compiler\ir.py:254`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\compiler\ir.py:257`

---

### `compile`

```matha
def compile(self, mir_code: str, target: CompileTarget) -> CompileResult
```

**描述**: 编译到 LLVM IR 并生成机器码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |
| `target` | `CompileTarget` | |

**返回**: `CompileResult`

Source: `src\compiler\ir.py:260`

---

### `_mir_to_llvm`

```matha
def _mir_to_llvm(self, mir_code: str) -> str
```

**描述**: Matha IR → LLVM IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |

**返回**: `str`

Source: `src\compiler\ir.py:315`

---

### `_compile_llvm`

```matha
def _compile_llvm(self, llvm_ir: str, cache_key: str, target: CompileTarget) -> Optional[str]
```

**描述**: 编译 LLVM IR 到机器码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |
| `cache_key` | `str` | |
| `target` | `CompileTarget` | |

**返回**: `Optional[str]`

Source: `src\compiler\ir.py:328`

---

### `_execute`

```matha
def _execute(self, exe_path: str) -> Optional[str]
```

**描述**: 执行编译后的程序。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exe_path` | `str` | |

**返回**: `Optional[str]`

Source: `src\compiler\ir.py:389`

---

## `CCompiler`

```matha
class CCompiler(IRCompiler)
```

**描述**: C 后端编译器。

Source: `src\compiler\ir.py:408`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\compiler\ir.py:411`

---

### `compile`

```matha
def compile(self, mir_code: str, target: CompileTarget) -> CompileResult
```

**描述**: 编译到 C 代码并生成可执行文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |
| `target` | `CompileTarget` | |

**返回**: `CompileResult`

Source: `src\compiler\ir.py:414`

---

### `_translate`

```matha
def _translate(self, mir_code: str) -> str
```

**描述**: Matha IR → C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |

**返回**: `str`

Source: `src\compiler\ir.py:469`

---

### `_compile_c`

```matha
def _compile_c(self, c_code: str, cache_key: str, target: CompileTarget) -> Optional[str]
```

**描述**: 编译 C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `c_code` | `str` | |
| `cache_key` | `str` | |
| `target` | `CompileTarget` | |

**返回**: `Optional[str]`

Source: `src\compiler\ir.py:482`

---

### `_execute`

```matha
def _execute(self, exe_path: str) -> Optional[str]
```

**描述**: 执行 C 程序。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exe_path` | `str` | |

**返回**: `Optional[str]`

Source: `src\compiler\ir.py:524`

---

## `CompilerFactory`

```matha
class CompilerFactory
```

**描述**: 编译器工厂，根据后端类型创建对应的编译器。

Source: `src\compiler\ir.py:543`

---

### `get_compiler`

```matha
def get_compiler(cls, backend: IRBackend) -> IRCompiler
```

**描述**: 获取编译器实例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `backend` | `IRBackend` | |

**返回**: `IRCompiler`

**装饰器**: classmethod

Source: `src\compiler\ir.py:549`

---

### `compile`

```matha
def compile(cls, mir_code: str, target: Optional[CompileTarget]) -> CompileResult
```

**描述**: 统一编译入口。

Args:
    mir_code: Matha IR 代码
    target: 编译目标（默认 Python）

Returns:
    CompileResult

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `mir_code` | `str` | |
| `target` | `Optional[CompileTarget]` | |

**返回**: `CompileResult`

**装饰器**: classmethod

Source: `src\compiler\ir.py:563`

---

## `compile_mir`

```matha
def compile_mir(mir_code: str, backend: str, **kwargs) -> CompileResult
```

**描述**: 便捷函数：编译 Matha IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mir_code` | `str` | |
| `backend` | `str` | |

**返回**: `CompileResult`

Source: `src\compiler\ir.py:585`

---

## `execute_mir`

```matha
def execute_mir(mir_code: str, backend: str, **kwargs) -> Any
```

**描述**: 便捷函数：执行 Matha IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mir_code` | `str` | |
| `backend` | `str` | |

**返回**: `Any`

Source: `src\compiler\ir.py:594`

---

# `compiler.jit`

```matha

```

**描述**: Matha JIT 编译器与优化器（v4.2 增强版）

支持：
  1. 表达式级 JIT 编译（将 Matha 表达式编译为 Python 字节码）
  2. 函数级 JIT 编译（将 Matha 函数编译为优化的 Python 函数）
  3. 常量折叠与死代码消除
  4. 内联优化
  5. 文件系统缓存（跨进程持久化）
  6. 编译统计与性能分析

用法：
  from src.compiler.jit import MathaJITCompiler

  compiler = MathaJITCompiler(cache_dir=".matha_cache")
  fn = compiler.compile_expr("sin(x) + cos(y)")
  result = fn(3.14, 1.57)

Source: `src\compiler\jit.py:1`

---

## `ConstantFolder`

```matha
class ConstantFolder
```

**描述**: 常量折叠优化器。

Source: `src\compiler\jit.py:38`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\jit.py:41`

---

### `fold`

```matha
def fold(self, expr_str: str, env: dict) -> Any
```

**描述**: 对表达式进行常量折叠。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `env` | `dict` | |

**返回**: `Any`

Source: `src\compiler\jit.py:44`

---

### `clear_cache`

```matha
def clear_cache(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\jit.py:64`

---

## `MathaJITCompiler`

```matha
class MathaJITCompiler
```

**描述**: Matha 表达式 JIT 编译器（v4.2 增强版）。

支持：
  - 算术运算: + - * / // % **
  - 比较运算: < > <= >= == !=
  - 逻辑运算: and or not
  - 函数调用: sin(x), cos(x), sqrt(x)...
  - 变量引用: x, y, z
  - 括号分组: (a + b) * c
  - 列表/字典字面量
  - 索引访问: arr[0], dict['key']
  - 文件系统缓存（跨进程持久化）

Source: `src\compiler\jit.py:72`

---

### `__init__`

```matha
def __init__(self, cache_dir: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cache_dir` | `str` | |

Source: `src\compiler\jit.py:88`

---

### `compile_expr`

```matha
def compile_expr(self, expr_str: str) -> Callable
```

**描述**: 编译 Matha 表达式为 Python 可执行函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |

**返回**: `Callable`

Source: `src\compiler\jit.py:98`

---

### `_matha_to_python`

```matha
def _matha_to_python(self, expr: str) -> str
```

**描述**: Matha 语法 → Python 语法转换。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\compiler\jit.py:169`

---

### `_cache_key`

```matha
def _cache_key(self, expr: str) -> str
```

**描述**: 生成缓存键。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\compiler\jit.py:201`

---

### `_cache_file`

```matha
def _cache_file(self, key: str) -> Path
```

**描述**: 获取缓存文件路径。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `Path`

Source: `src\compiler\jit.py:205`

---

### `_save_to_cache`

```matha
def _save_to_cache(self, expr: str, fn: Callable) -> None
```

**描述**: 保存编译结果到文件缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `fn` | `Callable` | |

**返回**: `None`

Source: `src\compiler\jit.py:209`

---

### `_load_from_cache`

```matha
def _load_from_cache(self, expr: str) -> Optional[Callable]
```

**描述**: 从文件缓存加载编译结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\compiler\jit.py:225`

---

### `clear_cache`

```matha
def clear_cache(self) -> None
```

**描述**: 清空所有缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\jit.py:239`

---

### `get_stats`

```matha
def get_stats(self) -> Dict
```

**描述**: 获取编译统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\compiler\jit.py:247`

---

### `compile_func`

```matha
def compile_func(self, func: Callable, auto_memoize: bool, pattern: Optional[str]) -> Callable
```

**描述**: 函数级 JIT 编译：将 Python/Matha 函数编译为优化版本。

功能：
  1. 从函数源码提取递归模式
  2. 自动注入 memoization 缓存
  3. 尾递归优化
  4. 字节码内联优化

Args:
    func: 要编译的函数
    auto_memoize: 是否自动添加 memoization
    pattern: 递归模式（fibonacci, factorial, gcd 等）

Returns:
    优化后的函数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `auto_memoize` | `bool` | |
| `pattern` | `Optional[str]` | |

**返回**: `Callable`

Source: `src\compiler\jit.py:264`

---

### `_extract_source`

```matha
def _extract_source(self, func: Callable) -> str
```

**描述**: 从函数中提取源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |

**返回**: `str`

Source: `src\compiler\jit.py:314`

---

### `_detect_recursion`

```matha
def _detect_recursion(self, source: str, func_name: str) -> Optional[str]
```

**描述**: 检测函数中的递归模式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `func_name` | `str` | |

**返回**: `Optional[str]`

Source: `src\compiler\jit.py:321`

---

### `_compile_python_func`

```matha
def _compile_python_func(self, func: Callable, source: str) -> Callable
```

**描述**: 将 Python 函数编译为优化的字节码版本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `source` | `str` | |

**返回**: `Callable`

Source: `src\compiler\jit.py:358`

---

### `get_func_stats`

```matha
def get_func_stats(self, func_name: str) -> Dict
```

**描述**: 获取指定函数的编译统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |

**返回**: `Dict`

Source: `src\compiler\jit.py:372`

---

## `jit_func`

```matha
def jit_func(func: Callable, auto_memoize: bool, pattern: Optional[str]) -> Callable
```

**描述**: 装饰器：JIT 编译函数并自动 memoize 递归函数。

用法：
    @jit_func
    def fib(n):
        if n <= 1: return n
        return fib(n-1) + fib(n-2)

    # 等价于:
    @jit_func(pattern='fibonacci')
    def fib(n): ...

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `auto_memoize` | `bool` | |
| `pattern` | `Optional[str]` | |

**返回**: `Callable`

Source: `src\compiler\jit.py:388`

---

## `get_jit_compiler`

```matha
def get_jit_compiler(cache_dir: str) -> MathaJITCompiler
```

**描述**: 获取全局 JIT 编译器实例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cache_dir` | `str` | |

**返回**: `MathaJITCompiler`

Source: `src\compiler\jit.py:412`

---

## `jit_compile`

```matha
def jit_compile(expr: str, cache_dir: str) -> Callable
```

**描述**: 便捷函数：JIT 编译表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `cache_dir` | `str` | |

**返回**: `Callable`

Source: `src\compiler\jit.py:420`

---

## `jit_stats`

```matha
def jit_stats() -> Dict
```

**描述**: 获取 JIT 编译器统计。

**返回**: `Dict`

Source: `src\compiler\jit.py:425`

---

# `compiler.llvm_backend`

```matha

```

**描述**: Matha LLVM 后端：Matha AST → LLVM IR → 原生机器码。

架构：
  1. Matha AST → Matha IR (中间表示)
  2. Matha IR → LLVM IR (文本格式)
  3. LLVM IR → 本机机器码 (通过 llc/clang subprocess)
  4. 原生机器码 → 可调用函数 (通过 ctypes)

性能目标：
  - 算术表达式：接近 Python 原生速度
  - 递归函数：通过 LLVM 尾递归消除 + 循环转换
  - 数值循环：向量化 (SIMD)

依赖：
  - 可选：llvmlite (Python LLVM bindings)
  - 必需：llc 或 clang (LLVM 工具链)

Source: `src\compiler\llvm_backend.py:1`

---

## `LLVMType`

```matha
class LLVMType(Enum)
```

Source: `src\compiler\llvm_backend.py:37`

---

## `LLVMValue`

```matha
class LLVMValue
```

**描述**: LLVM 值表示。

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:52`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\compiler\llvm_backend.py:58`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\compiler\llvm_backend.py:63`

---

## `MathaIRNode`

```matha
class MathaIRNode
```

**描述**: Matha 中间表示节点基类。

Source: `src\compiler\llvm_backend.py:71`

---

## `IRConstant`

```matha
class IRConstant(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:77`

---

## `IRVariable`

```matha
class IRVariable(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:83`

---

## `IRArithOp`

```matha
class IRArithOp(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:89`

---

## `IRCompareOp`

```matha
class IRCompareOp(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:97`

---

## `IRLogicalOp`

```matha
class IRLogicalOp(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:104`

---

## `IRFunctionCall`

```matha
class IRFunctionCall(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:110`

---

## `IRVariableAccess`

```matha
class IRVariableAccess(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:117`

---

## `IRAssignment`

```matha
class IRAssignment(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:123`

---

## `IRIfExpr`

```matha
class IRIfExpr(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:130`

---

## `IRLoop`

```matha
class IRLoop(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:138`

---

## `IRFunctionDef`

```matha
class IRFunctionDef(MathaIRNode)
```

**装饰器**: dataclass

Source: `src\compiler\llvm_backend.py:147`

---

## `MathaToIRConverter`

```matha
class MathaToIRConverter
```

**描述**: 将 Matha AST 转换为 Matha IR。

Source: `src\compiler\llvm_backend.py:159`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\llvm_backend.py:162`

---

### `_fresh_var`

```matha
def _fresh_var(self, lltype: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lltype` | `str` | |

**返回**: `str`

Source: `src\compiler\llvm_backend.py:165`

---

### `convert`

```matha
def convert(self, ast_node: Any) -> MathaIRNode
```

**描述**: 将 AST 节点转换为 IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ast_node` | `Any` | |

**返回**: `MathaIRNode`

Source: `src\compiler\llvm_backend.py:169`

---

### `_convert_binary`

```matha
def _convert_binary(self, node: Any) -> MathaIRNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `Any` | |

**返回**: `MathaIRNode`

Source: `src\compiler\llvm_backend.py:242`

---

### `_convert_unary`

```matha
def _convert_unary(self, node: Any) -> MathaIRNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `Any` | |

**返回**: `MathaIRNode`

Source: `src\compiler\llvm_backend.py:279`

---

### `_convert_func_app`

```matha
def _convert_func_app(self, node: Any) -> MathaIRNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `Any` | |

**返回**: `MathaIRNode`

Source: `src\compiler\llvm_backend.py:294`

---

### `_convert_lambda`

```matha
def _convert_lambda(self, node: Any) -> MathaIRNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `Any` | |

**返回**: `MathaIRNode`

Source: `src\compiler\llvm_backend.py:300`

---

### `_convert_if_expr`

```matha
def _convert_if_expr(self, node: Any) -> MathaIRNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `Any` | |

**返回**: `MathaIRNode`

Source: `src\compiler\llvm_backend.py:307`

---

### `convert_program`

```matha
def convert_program(self, program: Any) -> list[MathaIRNode]
```

**描述**: 转换整个程序。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `list[MathaIRNode]`

Source: `src\compiler\llvm_backend.py:314`

---

## `LLVMIRGenerator`

```matha
class LLVMIRGenerator
```

**描述**: 将 Matha IR 转换为 LLVM IR 文本。

Source: `src\compiler\llvm_backend.py:342`

---

### `__init__`

```matha
def __init__(self, module_name: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `module_name` | `str` | |

**返回**: `None`

Source: `src\compiler\llvm_backend.py:345`

---

### `_fresh_var`

```matha
def _fresh_var(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\compiler\llvm_backend.py:352`

---

### `_llvm_type`

```matha
def _llvm_type(self, matha_type: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_type` | `str` | |

**返回**: `str`

Source: `src\compiler\llvm_backend.py:356`

---

### `generate`

```matha
def generate(self, ir_nodes: list[MathaIRNode]) -> str
```

**描述**: 生成完整的 LLVM IR 模块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ir_nodes` | `list[MathaIRNode]` | |

**返回**: `str`

Source: `src\compiler\llvm_backend.py:367`

---

### `_emit_node`

```matha
def _emit_node(self, node: MathaIRNode) -> list[str]
```

**描述**: 生成单个 IR 节点的 LLVM IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `MathaIRNode` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:403`

---

### `_emit_constant`

```matha
def _emit_constant(self, node: IRConstant) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRConstant` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:427`

---

### `_emit_variable`

```matha
def _emit_variable(self, node: IRVariableAccess) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRVariableAccess` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:435`

---

### `_emit_arith`

```matha
def _emit_arith(self, node: IRArithOp) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRArithOp` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:441`

---

### `_emit_compare`

```matha
def _emit_compare(self, node: IRCompareOp) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRCompareOp` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:463`

---

### `_emit_logical`

```matha
def _emit_logical(self, node: IRLogicalOp) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRLogicalOp` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:476`

---

### `_emit_func_call`

```matha
def _emit_func_call(self, node: IRFunctionCall) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRFunctionCall` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:485`

---

### `_emit_assignment`

```matha
def _emit_assignment(self, node: IRAssignment) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRAssignment` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:493`

---

### `_emit_if`

```matha
def _emit_if(self, node: IRIfExpr) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRIfExpr` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:516`

---

### `_emit_loop`

```matha
def _emit_loop(self, node: IRLoop) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRLoop` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:541`

---

### `_emit_function_def`

```matha
def _emit_function_def(self, node: IRFunctionDef) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `IRFunctionDef` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_backend.py:564`

---

## `LLVMCompiler`

```matha
class LLVMCompiler
```

**描述**: 将 LLVM IR 编译为原生机器码。

Source: `src\compiler\llvm_backend.py:581`

---

### `__init__`

```matha
def __init__(self, toolchain: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `toolchain` | `str` | |

**返回**: `None`

Source: `src\compiler\llvm_backend.py:584`

---

### `compile`

```matha
def compile(self, llvm_ir: str, func_name: str) -> Optional[Callable]
```

**描述**: 编译 LLVM IR 为原生函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |
| `func_name` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\compiler\llvm_backend.py:588`

---

### `get_cached`

```matha
def get_cached(self, cache_key: str) -> Optional[Callable]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cache_key` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\compiler\llvm_backend.py:655`

---

### `cache_size`

```matha
def cache_size(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\compiler\llvm_backend.py:659`

---

### `clear_cache`

```matha
def clear_cache(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\llvm_backend.py:662`

---

## `LLVMHotJIT`

```matha
class LLVMHotJIT
```

**描述**: 热点追踪 + LLVM JIT 编译。

Source: `src\compiler\llvm_backend.py:670`

---

### `__init__`

```matha
def __init__(self, threshold: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threshold` | `int` | |

**返回**: `None`

Source: `src\compiler\llvm_backend.py:673`

---

### `record`

```matha
def record(self, name: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `None`

Source: `src\compiler\llvm_backend.py:679`

---

### `should_compile`

```matha
def should_compile(self, name: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\compiler\llvm_backend.py:682`

---

### `compile_and_cache`

```matha
def compile_and_cache(self, name: str, llvm_ir: str) -> Optional[Callable]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `llvm_ir` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\compiler\llvm_backend.py:685`

---

### `get_compiled`

```matha
def get_compiled(self, name: str) -> Optional[Callable]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\compiler\llvm_backend.py:693`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\compiler\llvm_backend.py:697`

---

## `matha_to_llvm_ir`

```matha
def matha_to_llvm_ir(ast_node: Any, module_name: str) -> str
```

**描述**: 将 Matha AST 转换为 LLVM IR 文本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ast_node` | `Any` | |
| `module_name` | `str` | |

**返回**: `str`

Source: `src\compiler\llvm_backend.py:709`

---

## `compile_llvm_ir`

```matha
def compile_llvm_ir(llvm_ir: str, func_name: str) -> Optional[Callable]
```

**描述**: 编译 LLVM IR 为原生函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `llvm_ir` | `str` | |
| `func_name` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\compiler\llvm_backend.py:717`

---

## `create_hot_jit`

```matha
def create_hot_jit(threshold: int) -> LLVMHotJIT
```

**描述**: 创建热点追踪 JIT 编译器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `threshold` | `int` | |

**返回**: `LLVMHotJIT`

Source: `src\compiler\llvm_backend.py:723`

---

# `compiler.llvm_hybrid`

```matha

```

**描述**: Matha LLVM 工具链 - 模拟模式完整版

当 LLVM 工具链不可用时，自动回退到模拟模式：
  - 生成有效的 LLVM IR 文本
  - 模拟编译过程
  - 提供性能基准对比

安装 LLVM 后可自动切换到原生编译模式。

Source: `src\compiler\llvm_hybrid.py:1`

---

## `SimulatedLLVMBackend`

```matha
class SimulatedLLVMBackend
```

**描述**: 模拟 LLVM 后端（无需实际安装 llc/clang）。

Source: `src\compiler\llvm_hybrid.py:27`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\llvm_hybrid.py:30`

---

### `compile`

```matha
def compile(self, llvm_ir: str, output_name: str) -> str
```

**描述**: 模拟编译 LLVM IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |
| `output_name` | `str` | |

**返回**: `str`

Source: `src\compiler\llvm_hybrid.py:34`

---

### `_validate_llvm_ir`

```matha
def _validate_llvm_ir(self, llvm_ir: str) -> list[str]
```

**描述**: 验证 LLVM IR 基本语法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |

**返回**: `list[str]`

Source: `src\compiler\llvm_hybrid.py:54`

---

### `_simulate_compilation`

```matha
def _simulate_compilation(self, llvm_ir: str, output_name: str) -> dict
```

**描述**: 模拟编译过程。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |
| `output_name` | `str` | |

**返回**: `dict`

Source: `src\compiler\llvm_hybrid.py:65`

---

### `run`

```matha
def run(self, exe_path: str, args: list) -> dict
```

**描述**: 运行模拟结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exe_path` | `str` | |
| `args` | `list` | |

**返回**: `dict`

Source: `src\compiler\llvm_hybrid.py:99`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\compiler\llvm_hybrid.py:108`

---

### `_check_llvm`

```matha
def _check_llvm() -> bool
```

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\compiler\llvm_hybrid.py:116`

---

## `HybridLLVMBackend`

```matha
class HybridLLVMBackend
```

**描述**: 混合后端：优先使用真实 LLVM，不可用时回退到模拟模式。

Source: `src\compiler\llvm_hybrid.py:129`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\llvm_hybrid.py:132`

---

### `_check_real_llvm`

```matha
def _check_real_llvm(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\compiler\llvm_hybrid.py:137`

---

### `compile`

```matha
def compile(self, llvm_ir: str, output_name: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |
| `output_name` | `str` | |

**返回**: `str`

Source: `src\compiler\llvm_hybrid.py:145`

---

### `_compile_real`

```matha
def _compile_real(self, llvm_ir: str, output_name: str) -> str
```

**描述**: 真实的 LLVM 编译流程。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |
| `output_name` | `str` | |

**返回**: `str`

Source: `src\compiler\llvm_hybrid.py:150`

---

### `run`

```matha
def run(self, exe_path: str, args: list) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exe_path` | `str` | |
| `args` | `list` | |

**返回**: `dict`

Source: `src\compiler\llvm_hybrid.py:188`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\compiler\llvm_hybrid.py:195`

---

## `benchmark_compiler`

```matha
def benchmark_compiler() -> dict
```

**描述**: 编译性能基准测试。

**返回**: `dict`

Source: `src\compiler\llvm_hybrid.py:207`

---

## `run_full_test`

```matha
def run_full_test() -> bool
```

**描述**: 运行完整编译测试。

**返回**: `bool`

Source: `src\compiler\llvm_hybrid.py:271`

---

## `matha_to_llvm`

```matha
def matha_to_llvm(source: str) -> str
```

**描述**: 将 Matha 源码转换为 LLVM IR 文本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `str`

Source: `src\compiler\llvm_hybrid.py:429`

---

# `compiler.matha_cc`

```matha

```

**描述**: Matha LLVM 工具链架构

┌─────────────────────────────────────────────────────────────────────┐
│                        Matha 工具链架构                               │
│                                                                     │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐            │
│  │  Matha 前端   │──▶│  Matha IR   │──▶│ LLVM IR      │            │
│  │ (词法/语法)   │   │ (中间表示)   │   │ (优化/翻译)   │            │
│  └──────────────┘   └──────────────┘   └──────┬───────┘            │
│                                               │                     │
│  ┌──────────────┐   ┌──────────────┐   ┌──────▼───────┐            │
│  │  Matha REPL  │   │  matha run   │   │ LLVM 后端     │            │
│  │  (交互)      │   │  (运行)      │   │ (llc/clang)  │            │
│  └──────────────┘   └──────────────┘   └──────┬───────┘            │
│                                               │                     │
│                                       ┌───────▼───────┐            │
│                                       │  原生机器码    │            │
│                                       │  (ELF/PE)     │            │
│                                       └───────────────┘            │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  Matha 专属优化 Pass:                                     │      │
│  │  • MathaTailRecPass    - 尾递归消除                        │      │
│  │  • MathaLoopUnrollPass - 循环展开                          │      │
│  │  • MathaSIMDPass       - 自动向量化                        │      │
│  │  • MathaConstFoldPass  - 常量折叠                          │      │
│  │  • MathaCurryFlattenPass- 柯里化扁平化                      │      │
│  └──────────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────────┘

Source: `src\compiler\matha_cc.py:1`

---

## `MathaToken`

```matha
class MathaToken
```

**描述**: Matha 词法 token。

Source: `src\compiler\matha_cc.py:48`

---

### `__init__`

```matha
def __init__(self, type_: str, value: str, line: int, col: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_` | `str` | |
| `value` | `str` | |
| `line` | `int` | |
| `col` | `int` | |

Source: `src\compiler\matha_cc.py:50`

---

### `__repr__`

```matha
def __repr__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\compiler\matha_cc.py:56`

---

## `MathaLexer`

```matha
class MathaLexer
```

**描述**: Matha 词法分析器。

Source: `src\compiler\matha_cc.py:60`

---

### `__init__`

```matha
def __init__(self, source: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

Source: `src\compiler\matha_cc.py:66`

---

### `tokenize`

```matha
def tokenize(self) -> list[MathaToken]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[MathaToken]`

Source: `src\compiler\matha_cc.py:72`

---

### `_skip_whitespace`

```matha
def _skip_whitespace(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:105`

---

### `_skip_line_comment`

```matha
def _skip_line_comment(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:114`

---

### `_skip_block_comment`

```matha
def _skip_block_comment(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:118`

---

### `_read_number`

```matha
def _read_number(self) -> MathaToken
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `MathaToken`

Source: `src\compiler\matha_cc.py:128`

---

### `_read_string`

```matha
def _read_string(self, quote: str) -> MathaToken
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `quote` | `str` | |

**返回**: `MathaToken`

Source: `src\compiler\matha_cc.py:139`

---

### `_read_identifier`

```matha
def _read_identifier(self) -> MathaToken
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `MathaToken`

Source: `src\compiler\matha_cc.py:149`

---

### `_read_operator`

```matha
def _read_operator(self) -> MathaToken
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `MathaToken`

Source: `src\compiler\matha_cc.py:158`

---

### `_is_unicode_id_start`

```matha
def _is_unicode_id_start(self, ch: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ch` | `str` | |

**返回**: `bool`

Source: `src\compiler\matha_cc.py:173`

---

### `_advance`

```matha
def _advance(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:180`

---

## `ASTNode`

```matha
class ASTNode
```

**描述**: AST 节点基类。

Source: `src\compiler\matha_cc.py:194`

---

### `__init__`

```matha
def __init__(self, line: int, col: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `int` | |
| `col` | `int` | |

Source: `src\compiler\matha_cc.py:196`

---

## `Program`

```matha
class Program(ASTNode)
```

Source: `src\compiler\matha_cc.py:201`

---

### `__init__`

```matha
def __init__(self, decls, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decls` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:202`

---

## `Binding`

```matha
class Binding(ASTNode)
```

Source: `src\compiler\matha_cc.py:207`

---

### `__init__`

```matha
def __init__(self, name, value, ann_type, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `` | |
| `value` | `` | |
| `ann_type` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:208`

---

## `FuncDef`

```matha
class FuncDef(ASTNode)
```

Source: `src\compiler\matha_cc.py:215`

---

### `__init__`

```matha
def __init__(self, name, params, return_type, body, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `` | |
| `params` | `` | |
| `return_type` | `` | |
| `body` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:216`

---

## `BinaryOp`

```matha
class BinaryOp(ASTNode)
```

Source: `src\compiler\matha_cc.py:224`

---

### `__init__`

```matha
def __init__(self, op, left, right, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op` | `` | |
| `left` | `` | |
| `right` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:225`

---

## `UnaryOp`

```matha
class UnaryOp(ASTNode)
```

Source: `src\compiler\matha_cc.py:232`

---

### `__init__`

```matha
def __init__(self, op, operand, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op` | `` | |
| `operand` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:233`

---

## `FuncApp`

```matha
class FuncApp(ASTNode)
```

Source: `src\compiler\matha_cc.py:239`

---

### `__init__`

```matha
def __init__(self, func, arg, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `` | |
| `arg` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:240`

---

## `Lambda`

```matha
class Lambda(ASTNode)
```

Source: `src\compiler\matha_cc.py:246`

---

### `__init__`

```matha
def __init__(self, params, body, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params` | `` | |
| `body` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:247`

---

## `IfExpr`

```matha
class IfExpr(ASTNode)
```

Source: `src\compiler\matha_cc.py:253`

---

### `__init__`

```matha
def __init__(self, cond, then, else_, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cond` | `` | |
| `then` | `` | |
| `else_` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:254`

---

## `WhileStmt`

```matha
class WhileStmt(ASTNode)
```

Source: `src\compiler\matha_cc.py:261`

---

### `__init__`

```matha
def __init__(self, cond, body, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cond` | `` | |
| `body` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:262`

---

## `ForStmt`

```matha
class ForStmt(ASTNode)
```

Source: `src\compiler\matha_cc.py:268`

---

### `__init__`

```matha
def __init__(self, target, iterable, body, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target` | `` | |
| `iterable` | `` | |
| `body` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:269`

---

## `Literal`

```matha
class Literal(ASTNode)
```

Source: `src\compiler\matha_cc.py:276`

---

### `__init__`

```matha
def __init__(self, value, kind, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `` | |
| `kind` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:277`

---

## `Variable`

```matha
class Variable(ASTNode)
```

Source: `src\compiler\matha_cc.py:283`

---

### `__init__`

```matha
def __init__(self, name, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:284`

---

## `ListLiteral`

```matha
class ListLiteral(ASTNode)
```

Source: `src\compiler\matha_cc.py:289`

---

### `__init__`

```matha
def __init__(self, items, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `items` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:290`

---

## `DictLiteral`

```matha
class DictLiteral(ASTNode)
```

Source: `src\compiler\matha_cc.py:295`

---

### `__init__`

```matha
def __init__(self, keys, values, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `keys` | `` | |
| `values` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:296`

---

## `Output`

```matha
class Output(ASTNode)
```

Source: `src\compiler\matha_cc.py:302`

---

### `__init__`

```matha
def __init__(self, expr, line, col)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |
| `line` | `` | |
| `col` | `` | |

Source: `src\compiler\matha_cc.py:303`

---

## `MathaParser`

```matha
class MathaParser
```

Source: `src\compiler\matha_cc.py:312`

---

### `__init__`

```matha
def __init__(self, tokens: list[MathaToken])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tokens` | `list[MathaToken]` | |

Source: `src\compiler\matha_cc.py:313`

---

### `parse`

```matha
def parse(self) -> Program
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Program`

Source: `src\compiler\matha_cc.py:317`

---

### `_peek`

```matha
def _peek(self) -> MathaToken
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `MathaToken`

Source: `src\compiler\matha_cc.py:323`

---

### `_advance`

```matha
def _advance(self) -> MathaToken
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `MathaToken`

Source: `src\compiler\matha_cc.py:326`

---

### `_expect`

```matha
def _expect(self, type_: str) -> MathaToken
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_` | `str` | |

**返回**: `MathaToken`

Source: `src\compiler\matha_cc.py:331`

---

### `_parse_decl`

```matha
def _parse_decl(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:340`

---

### `_parse_func_def`

```matha
def _parse_func_def(self) -> FuncDef
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `FuncDef`

Source: `src\compiler\matha_cc.py:350`

---

### `_parse_param_list`

```matha
def _parse_param_list(self) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list`

Source: `src\compiler\matha_cc.py:370`

---

### `_parse_binding`

```matha
def _parse_binding(self) -> Binding
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Binding`

Source: `src\compiler\matha_cc.py:382`

---

### `_parse_output`

```matha
def _parse_output(self) -> Output
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Output`

Source: `src\compiler\matha_cc.py:388`

---

### `_parse_expr`

```matha
def _parse_expr(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:396`

---

### `_parse_or`

```matha
def _parse_or(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:399`

---

### `_parse_and`

```matha
def _parse_and(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:407`

---

### `_parse_equality`

```matha
def _parse_equality(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:415`

---

### `_parse_comparison`

```matha
def _parse_comparison(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:423`

---

### `_parse_additive`

```matha
def _parse_additive(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:431`

---

### `_parse_multiplicative`

```matha
def _parse_multiplicative(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:439`

---

### `_parse_unary`

```matha
def _parse_unary(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:447`

---

### `_parse_primary`

```matha
def _parse_primary(self) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ASTNode`

Source: `src\compiler\matha_cc.py:458`

---

### `_parse_arg_list`

```matha
def _parse_arg_list(self) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list`

Source: `src\compiler\matha_cc.py:524`

---

### `_parse_list_literal`

```matha
def _parse_list_literal(self) -> ListLiteral
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ListLiteral`

Source: `src\compiler\matha_cc.py:537`

---

### `_parse_dict_literal`

```matha
def _parse_dict_literal(self) -> DictLiteral
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `DictLiteral`

Source: `src\compiler\matha_cc.py:550`

---

## `MathaIR`

```matha
class MathaIR
```

Source: `src\compiler\matha_cc.py:570`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:571`

---

### `_new_temp`

```matha
def _new_temp(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:578`

---

### `emit`

```matha
def emit(self, instr: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `instr` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:582`

---

### `emit_label`

```matha
def emit_label(self, name: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:588`

---

### `emit_call`

```matha
def emit_call(self, func: str, args: list, result: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `str` | |
| `args` | `list` | |
| `result` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:592`

---

### `emit_binary`

```matha
def emit_binary(self, op: str, left: str, right: str, result: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op` | `str` | |
| `left` | `str` | |
| `right` | `str` | |
| `result` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:598`

---

### `emit_load`

```matha
def emit_load(self, name: str, result: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `result` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:603`

---

### `emit_store`

```matha
def emit_store(self, name: str, value: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `value` | `str` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:608`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\compiler\matha_cc.py:611`

---

## `MathaFrontend`

```matha
class MathaFrontend
```

Source: `src\compiler\matha_cc.py:620`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:621`

---

### `compile`

```matha
def compile(self, program: Program) -> MathaIR
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Program` | |

**返回**: `MathaIR`

Source: `src\compiler\matha_cc.py:625`

---

### `_compile_decl`

```matha
def _compile_decl(self, decl: ASTNode) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `ASTNode` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:630`

---

### `_compile_func`

```matha
def _compile_func(self, func: FuncDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `FuncDef` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:639`

---

### `_compile_binding`

```matha
def _compile_binding(self, binding: Binding) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `binding` | `Binding` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:647`

---

### `_compile_expr`

```matha
def _compile_expr(self, expr: ASTNode) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `ASTNode` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:652`

---

### `_emit_literal`

```matha
def _emit_literal(self, lit: Literal) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lit` | `Literal` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:675`

---

### `_compile_binary`

```matha
def _compile_binary(self, expr: BinaryOp) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `BinaryOp` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:685`

---

### `_compile_unary`

```matha
def _compile_unary(self, expr: UnaryOp) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `UnaryOp` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:693`

---

### `_compile_func_app`

```matha
def _compile_func_app(self, expr: FuncApp) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `FuncApp` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:703`

---

### `_compile_if`

```matha
def _compile_if(self, expr: IfExpr) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `IfExpr` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:712`

---

### `_compile_list`

```matha
def _compile_list(self, expr: ListLiteral) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `ListLiteral` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:726`

---

### `_compile_dict`

```matha
def _compile_dict(self, expr: DictLiteral) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `DictLiteral` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:732`

---

## `MathaLLVMGenerator`

```matha
class MathaLLVMGenerator
```

Source: `src\compiler\matha_cc.py:744`

---

### `__init__`

```matha
def __init__(self, module_name: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `module_name` | `str` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:745`

---

### `generate`

```matha
def generate(self, matha_ir: MathaIR) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_ir` | `MathaIR` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:749`

---

### `save`

```matha
def save(self, path: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:786`

---

## `MathaBackend`

```matha
class MathaBackend
```

Source: `src\compiler\matha_cc.py:795`

---

### `__init__`

```matha
def __init__(self, toolchain: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `toolchain` | `str` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:796`

---

### `compile`

```matha
def compile(self, llvm_ir: str, output_name: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |
| `output_name` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:800`

---

### `run`

```matha
def run(self, exe_path: str, args: list) -> subprocess.CompletedProcess
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exe_path` | `str` | |
| `args` | `list` | |

**返回**: `subprocess.CompletedProcess`

Source: `src\compiler\matha_cc.py:833`

---

### `cache_stats`

```matha
def cache_stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\compiler\matha_cc.py:837`

---

## `MathaCompiler`

```matha
class MathaCompiler
```

Source: `src\compiler\matha_cc.py:845`

---

### `__init__`

```matha
def __init__(self, optimize: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `optimize` | `bool` | |

**返回**: `None`

Source: `src\compiler\matha_cc.py:846`

---

### `compile_file`

```matha
def compile_file(self, matha_file: str, output_name: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_file` | `str` | |
| `output_name` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:851`

---

### `compile_source`

```matha
def compile_source(self, source: str, output_name: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `output_name` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:856`

---

### `_optimize_llvm`

```matha
def _optimize_llvm(self, llvm_ir: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `llvm_ir` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:870`

---

### `run`

```matha
def run(self, source: str, args: list) -> subprocess.CompletedProcess
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `args` | `list` | |

**返回**: `subprocess.CompletedProcess`

Source: `src\compiler\matha_cc.py:885`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\compiler\matha_cc.py:894`

---

## `matha_compile`

```matha
def matha_compile(source: str, output_name: str, optimize: bool) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `output_name` | `str` | |
| `optimize` | `bool` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:902`

---

## `matha_run`

```matha
def matha_run(source: str, args: list) -> subprocess.CompletedProcess
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `args` | `list` | |

**返回**: `subprocess.CompletedProcess`

Source: `src\compiler\matha_cc.py:906`

---

## `matha_to_llvm`

```matha
def matha_to_llvm(source: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `str`

Source: `src\compiler\matha_cc.py:910`

---

# `compiler.matha_cc_cli`

```matha

```

**描述**: Matha LLVM 工具链 - matha-cc 编译器前端。

命令行接口：
  matha-cc compile input.matha -o output       # 编译为可执行文件
  matha-cc run input.matha [args...]            # 编译并运行
  matha-cc llvm input.matha -o output.ll       # 仅生成 LLVM IR
  matha-cc optimize input.matha -O2            # 编译并优化
  matha-cc info                                # 显示工具链信息

Source: `src\compiler\matha_cc_cli.py:1`

---

## `cmd_compile`

```matha
def cmd_compile(args) -> int
```

**描述**: 编译命令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\compiler\matha_cc_cli.py:21`

---

## `cmd_run`

```matha
def cmd_run(args) -> int
```

**描述**: 运行命令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\compiler\matha_cc_cli.py:36`

---

## `cmd_llvm`

```matha
def cmd_llvm(args) -> int
```

**描述**: 生成 LLVM IR 命令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\compiler\matha_cc_cli.py:54`

---

## `cmd_optimize`

```matha
def cmd_optimize(args) -> int
```

**描述**: 优化编译命令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\compiler\matha_cc_cli.py:71`

---

## `cmd_info`

```matha
def cmd_info(args) -> int
```

**描述**: 显示工具链信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\compiler\matha_cc_cli.py:86`

---

## `cmd_test`

```matha
def cmd_test(args) -> int
```

**描述**: 运行编译器测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\compiler\matha_cc_cli.py:116`

---

## `main`

```matha
def main(argv) -> int
```

**描述**: CLI 入口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `argv` | `` | |

**返回**: `int`

Source: `src\compiler\matha_cc_cli.py:147`

---

# `compiler.memoize`

```matha

```

**描述**: Matha 自动 Memoization 递归优化器

解决 Matha 递归算法性能瓶颈（Fibonacci 269ms → ~0.5ms）的核心组件。

功能：
  1. 自动检测递归函数模式
  2. 为递归函数注入 memoization 缓存
  3. 支持尾递归优化（循环转换）
  4. 支持 LRU 缓存淘汰策略
  5. 与 JIT 编译器集成

用法：
  from src.compiler.memoize import MemoizeOptimizer
  optimizer = MemoizeOptimizer(max_size=1024)
  fn = optimizer.memoize(fib, max_args=2)
  result = fn(30)  # 自动缓存，~0.5ms vs 269ms

Source: `src\compiler\memoize.py:1`

---

## `LRUCache`

```matha
class LRUCache
```

**描述**: 线程安全的 LRU 缓存，支持大小限制和自动淘汰。

Source: `src\compiler\memoize.py:35`

---

### `__init__`

```matha
def __init__(self, max_size: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_size` | `int` | |

Source: `src\compiler\memoize.py:38`

---

### `get`

```matha
def get(self, key: str) -> Optional[Any]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `Optional[Any]`

Source: `src\compiler\memoize.py:45`

---

### `put`

```matha
def put(self, key: str, value: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `value` | `Any` | |

**返回**: `None`

Source: `src\compiler\memoize.py:53`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\memoize.py:61`

---

### `size`

```matha
def size(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\compiler\memoize.py:68`

---

### `stats`

```matha
def stats(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

**装饰器**: property

Source: `src\compiler\memoize.py:72`

---

## `RecursivePattern`

```matha
class RecursivePattern
```

**描述**: 检测到的递归模式。

**装饰器**: dataclass

Source: `src\compiler\memoize.py:88`

---

## `RecursivePatternDetector`

```matha
class RecursivePatternDetector
```

**描述**: 从源码中检测递归模式。

Source: `src\compiler\memoize.py:97`

---

### `detect`

```matha
def detect(cls, func_source: str, func_name: str) -> Optional[RecursivePattern]
```

**描述**: 从源码中检测递归模式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `func_source` | `str` | |
| `func_name` | `str` | |

**返回**: `Optional[RecursivePattern]`

**装饰器**: classmethod

Source: `src\compiler\memoize.py:129`

---

## `MemoizeDecorator`

```matha
class MemoizeDecorator
```

**描述**: 通用的 memoization 装饰器，支持 LRU 缓存和自动缓存键生成。

Source: `src\compiler\memoize.py:176`

---

### `__init__`

```matha
def __init__(self, max_size: int, key_func: Optional[Callable])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_size` | `int` | |
| `key_func` | `Optional[Callable]` | |

Source: `src\compiler\memoize.py:179`

---

### `__call__`

```matha
def __call__(self, func: Callable) -> Callable
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |

**返回**: `Callable`

Source: `src\compiler\memoize.py:184`

---

### `_make_key`

```matha
def _make_key(self, func: Callable, args: Tuple, kwargs: Dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `args` | `Tuple` | |
| `kwargs` | `Dict` | |

**返回**: `str`

Source: `src\compiler\memoize.py:200`

---

### `_serialize`

```matha
def _serialize(obj: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `Any` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\compiler\memoize.py:214`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\memoize.py:224`

---

### `stats`

```matha
def stats(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

**装饰器**: property

Source: `src\compiler\memoize.py:228`

---

## `MemoizeOptimizer`

```matha
class MemoizeOptimizer
```

**描述**: Matha 自动 Memoization 递归优化器。

功能：
  1. 自动检测递归函数
  2. 为递归函数注入 LRU 缓存
  3. 支持尾递归优化
  4. 与 JIT 编译器集成
  5. 性能统计和报告

性能提升：
  - Fibonacci(30): 269ms → 0.5ms (538x)
  - Fibonacci(100): 10^20次调用 → O(n) 次调用
  - 阶乘(1000): 递归 1000次 → 1次查找

Source: `src\compiler\memoize.py:236`

---

### `__init__`

```matha
def __init__(self, max_cache_size: int, enable_tail_recursion: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_cache_size` | `int` | |
| `enable_tail_recursion` | `bool` | |

Source: `src\compiler\memoize.py:253`

---

### `memoize`

```matha
def memoize(self, func: Callable, max_args: Optional[int], pattern: Optional[str]) -> Callable
```

**描述**: 为函数添加自动 memoization。

Args:
    func: 要优化的函数
    max_args: 最大参数数量（用于缓存键生成）
    pattern: 递归模式名称（fibonacci, factorial, gcd 等）

Returns:
    已 memoized 的函数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `max_args` | `Optional[int]` | |
| `pattern` | `Optional[str]` | |

**返回**: `Callable`

Source: `src\compiler\memoize.py:266`

---

### `_try_tail_recursion`

```matha
def _try_tail_recursion(self, func: Callable, pattern: str) -> Optional[Callable]
```

**描述**: 尝试将递归函数转换为尾递归形式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `pattern` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\compiler\memoize.py:302`

---

### `optimize_fibonacci`

```matha
def optimize_fibonacci(self, n: int) -> int
```

**描述**: 直接优化的 Fibonacci 计算（最快的路径）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |

**返回**: `int`

Source: `src\compiler\memoize.py:331`

---

### `benchmark`

```matha
def benchmark(self, func: Callable, test_cases: List[Tuple], iterations: int) -> Dict
```

**描述**: 对 memoized 和原始函数进行基准测试对比。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `test_cases` | `List[Tuple]` | |
| `iterations` | `int` | |

**返回**: `Dict`

Source: `src\compiler\memoize.py:344`

---

### `get_stats`

```matha
def get_stats(self) -> Dict
```

**描述**: 获取优化器统计信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\compiler\memoize.py:381`

---

### `clear_all`

```matha
def clear_all(self) -> None
```

**描述**: 清空所有缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\compiler\memoize.py:389`

---

## `get_memoize_optimizer`

```matha
def get_memoize_optimizer(max_cache_size: int) -> MemoizeOptimizer
```

**描述**: 获取全局 memoization 优化器实例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `max_cache_size` | `int` | |

**返回**: `MemoizeOptimizer`

Source: `src\compiler\memoize.py:403`

---

## `memoize`

```matha
def memoize(func: Callable, **kwargs) -> Callable
```

**描述**: 便捷装饰器：自动为函数添加 memoization。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `Callable`

Source: `src\compiler\memoize.py:411`

---

## `fib_raw`

```matha
def fib_raw(n: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `int`

Source: `src\compiler\memoize.py:428`

---

## `factorial_raw`

```matha
def factorial_raw(n: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `int`

Source: `src\compiler\memoize.py:433`

---

# `compiler.native`

```matha

```

**描述**: Matha 原生编译层 v2.0
======================
协议解释生成器 + 驱动生成器 + 原生代码编译

架构：
  Matha 表达式 → 协议解析器 → 驱动规格 → 驱动生成器 → 目标代码
  Matha 表达式 → 原生编译器 → 汇编/C/Python 代码 → 执行

功能：
  1. ProtocolInterpreter  — 协议解释（UART/SPI/I2C/CAN 自动代码生成）
  2. DriverBuilder        — 构建各类硬件驱动（传感器/执行器/通信/显示/存储）
  3. NativeCompiler       — 原生代码编译（C/汇编/Python 后端）
  4. 与 hal_v2.py 无缝集成

Source: `src\compiler\native.py:1`

---

## `ProtocolInterpreter`

```matha
class ProtocolInterpreter
```

**描述**: 协议解释器。

将协议规格解释为可执行的代码和配置。
支持：UART, SPI, I2C, CAN, USB, MQTT, HTTP

Source: `src\compiler\native.py:38`

---

### `__init__`

```matha
def __init__(self, parser: ProtocolParser)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `parser` | `ProtocolParser` | |

Source: `src\compiler\native.py:46`

---

### `interpret`

```matha
def interpret(self, spec: ProtocolSpec) -> dict
```

**描述**: 解释协议规格，返回可执行代码和配置。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `ProtocolSpec` | |

**返回**: `dict`

Source: `src\compiler\native.py:52`

---

### `get_code`

```matha
def get_code(self, name: str, lang: str) -> Optional[str]
```

**描述**: 获取已解释协议的指定语言代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `lang` | `str` | |

**返回**: `Optional[str]`

Source: `src\compiler\native.py:68`

---

### `list_protocols`

```matha
def list_protocols(self) -> List[str]
```

**描述**: 列出所有已解释的协议。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

Source: `src\compiler\native.py:80`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\compiler\native.py:84`

---

## `DriverBuilder`

```matha
class DriverBuilder
```

**描述**: 硬件驱动构建器。

根据驱动规格生成目标语言代码，并注册到 FFI/驱动管理器。

Source: `src\compiler\native.py:96`

---

### `__init__`

```matha
def __init__(self, generator: DriverGenerator)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `generator` | `DriverGenerator` | |

Source: `src\compiler\native.py:103`

---

### `build`

```matha
def build(self, spec: DriverSpec) -> dict
```

**描述**: 构建驱动并返回结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `dict`

Source: `src\compiler\native.py:109`

---

### `build_sensor`

```matha
def build_sensor(self, name: str, math_expr: str, scale: float, offset: float, unit: str, protocol: Optional[ProtocolSpec], arch: Architecture) -> dict
```

**描述**: 便捷方法：构建传感器驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `math_expr` | `str` | |
| `scale` | `float` | |
| `offset` | `float` | |
| `unit` | `str` | |
| `protocol` | `Optional[ProtocolSpec]` | |
| `arch` | `Architecture` | |

**返回**: `dict`

Source: `src\compiler\native.py:117`

---

### `build_actuator`

```matha
def build_actuator(self, name: str, min_val: float, max_val: float, step: float, protocol: Optional[ProtocolSpec], arch: Architecture) -> dict
```

**描述**: 便捷方法：构建执行器驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `min_val` | `float` | |
| `max_val` | `float` | |
| `step` | `float` | |
| `protocol` | `Optional[ProtocolSpec]` | |
| `arch` | `Architecture` | |

**返回**: `dict`

Source: `src\compiler\native.py:131`

---

### `build_math`

```matha
def build_math(self, name: str, math_expr: str, arch: Architecture) -> dict
```

**描述**: 便捷方法：构建数学驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `math_expr` | `str` | |
| `arch` | `Architecture` | |

**返回**: `dict`

Source: `src\compiler\native.py:143`

---

### `list_built`

```matha
def list_built(self) -> List[str]
```

**描述**: 列出已构建的驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

Source: `src\compiler\native.py:153`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\compiler\native.py:157`

---

## `NativeCompiler`

```matha
class NativeCompiler
```

**描述**: 原生编译器。

将 Matha 表达式编译为各架构的原生代码（C/汇编/Python）。
支持安全副作用追踪和指针/内存控制。

Source: `src\compiler\native.py:169`

---

### `__init__`

```matha
def __init__(self, backend: NativeBackend, side_effect_engine: SafeSideEffectEngine, pointer_mgr: PointerManager)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `backend` | `NativeBackend` | |
| `side_effect_engine` | `SafeSideEffectEngine` | |
| `pointer_mgr` | `PointerManager` | |

Source: `src\compiler\native.py:177`

---

### `compile`

```matha
def compile(self, expr: str, arch: Architecture, func_name: str, target_lang: str, safety_level: str) -> dict
```

**描述**: 编译表达式，返回代码和元数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `arch` | `Architecture` | |
| `func_name` | `str` | |
| `target_lang` | `str` | |
| `safety_level` | `str` | |

**返回**: `dict`

Source: `src\compiler\native.py:187`

---

### `compile_to_c`

```matha
def compile_to_c(self, expr: str, func_name: str, arch: Architecture) -> dict
```

**描述**: 编译为 C 代码（默认）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |
| `arch` | `Architecture` | |

**返回**: `dict`

Source: `src\compiler\native.py:219`

---

### `compile_to_asm`

```matha
def compile_to_asm(self, expr: str, func_name: str, arch: Architecture) -> dict
```

**描述**: 编译为汇编代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |
| `arch` | `Architecture` | |

**返回**: `dict`

Source: `src\compiler\native.py:224`

---

### `compile_and_exec`

```matha
def compile_and_exec(self, expr: str, arch: Architecture, func_name: str) -> dict
```

**描述**: 编译并尝试执行（仅支持 Python 后端）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `arch` | `Architecture` | |
| `func_name` | `str` | |

**返回**: `dict`

Source: `src\compiler\native.py:229`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\compiler\native.py:250`

---

## `interpret_protocol`

```matha
def interpret_protocol(spec: ProtocolSpec) -> dict
```

**描述**: 解释协议规格。

| 参数 | 类型 | 说明 |
|------|------|------|
| `spec` | `ProtocolSpec` | |

**返回**: `dict`

Source: `src\compiler\native.py:262`

---

## `build_driver`

```matha
def build_driver(spec: DriverSpec) -> dict
```

**描述**: 构建硬件驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `spec` | `DriverSpec` | |

**返回**: `dict`

Source: `src\compiler\native.py:267`

---

## `native_compile`

```matha
def native_compile(expr: str, arch: Architecture, func_name: str, target_lang: str) -> dict
```

**描述**: 原生编译表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `arch` | `Architecture` | |
| `func_name` | `str` | |
| `target_lang` | `str` | |

**返回**: `dict`

Source: `src\compiler\native.py:272`

---

## `get_native_stats`

```matha
def get_native_stats() -> dict
```

**描述**: 获取原生编译层全部统计。

**返回**: `dict`

Source: `src\compiler\native.py:283`

---

# `concurrent_v2`

```matha

```

**描述**: Matha 并发扩展：进程池 + select + supervision tree。

Source: `src\concurrent_v2.py:1`

---

## `ProcessPool`

```matha
class ProcessPool
```

**描述**: Matha 进程池（跨进程并行计算）。

Source: `src\concurrent_v2.py:19`

---

### `__init__`

```matha
def __init__(self, max_workers: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_workers` | `int` | |

**返回**: `None`

Source: `src\concurrent_v2.py:22`

---

### `_get_pool`

```matha
def _get_pool(self) -> multiprocessing.Pool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `multiprocessing.Pool`

Source: `src\concurrent_v2.py:27`

---

### `map`

```matha
def map(self, fn: Callable, items: list, chunksize: int) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |
| `items` | `list` | |
| `chunksize` | `int` | |

**返回**: `list`

Source: `src\concurrent_v2.py:33`

---

### `apply`

```matha
def apply(self, fn: Callable, args: tuple) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |
| `args` | `tuple` | |

**返回**: `Any`

Source: `src\concurrent_v2.py:36`

---

### `apply_async`

```matha
def apply_async(self, fn: Callable, args: tuple) -> multiprocessing.pool.ApplyResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `Callable` | |
| `args` | `tuple` | |

**返回**: `multiprocessing.pool.ApplyResult`

Source: `src\concurrent_v2.py:39`

---

### `shutdown`

```matha
def shutdown(self, wait: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `wait` | `bool` | |

**返回**: `None`

Source: `src\concurrent_v2.py:42`

---

### `max_workers`

```matha
def max_workers(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\concurrent_v2.py:50`

---

## `SelectMonitor`

```matha
class SelectMonitor
```

**描述**: 类 Linux select 的多路复用监听。

Source: `src\concurrent_v2.py:58`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\concurrent_v2.py:61`

---

### `add_reader`

```matha
def add_reader(self, fd, callback: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fd` | `` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\concurrent_v2.py:66`

---

### `add_writer`

```matha
def add_writer(self, fd, callback: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fd` | `` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\concurrent_v2.py:69`

---

### `remove`

```matha
def remove(self, fd) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fd` | `` | |

**返回**: `None`

Source: `src\concurrent_v2.py:72`

---

### `poll`

```matha
def poll(self, timeout: float) -> list[tuple]
```

**描述**: 轮询就绪的 fd。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `timeout` | `float` | |

**返回**: `list[tuple]`

Source: `src\concurrent_v2.py:76`

---

### `_is_readable`

```matha
def _is_readable(self, fd) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fd` | `` | |

**返回**: `bool`

Source: `src\concurrent_v2.py:87`

---

### `_is_writable`

```matha
def _is_writable(self, fd) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fd` | `` | |

**返回**: `bool`

Source: `src\concurrent_v2.py:96`

---

## `WorkerState`

```matha
class WorkerState(Enum)
```

Source: `src\concurrent_v2.py:110`

---

## `Worker`

```matha
class Worker
```

**装饰器**: dataclass

Source: `src\concurrent_v2.py:118`

---

## `Supervisor`

```matha
class Supervisor
```

**描述**: Actor 监督树。

Source: `src\concurrent_v2.py:126`

---

### `__init__`

```matha
def __init__(self, name: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `None`

Source: `src\concurrent_v2.py:129`

---

### `supervise`

```matha
def supervise(self, name: str, max_restarts: int) -> Worker
```

**描述**: 监督一个 worker。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `max_restarts` | `int` | |

**返回**: `Worker`

Source: `src\concurrent_v2.py:135`

---

### `start_worker`

```matha
def start_worker(self, name: str, fn: Callable, *args) -> threading.Thread
```

**描述**: 启动 worker 线程。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `fn` | `Callable` | |

**返回**: `threading.Thread`

Source: `src\concurrent_v2.py:142`

---

### `_restart`

```matha
def _restart(self, name: str, fn: Callable, args: tuple) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `fn` | `Callable` | |
| `args` | `tuple` | |

**返回**: `None`

Source: `src\concurrent_v2.py:165`

---

### `get_state`

```matha
def get_state(self, name: str) -> Optional[WorkerState]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[WorkerState]`

Source: `src\concurrent_v2.py:169`

---

### `get_all_states`

```matha
def get_all_states(self) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict[str, str]`

Source: `src\concurrent_v2.py:173`

---

## `DistributedLock`

```matha
class DistributedLock
```

**描述**: 简化分布式锁（基于文件）。

Source: `src\concurrent_v2.py:181`

---

### `__init__`

```matha
def __init__(self, lock_name: str, timeout: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lock_name` | `str` | |
| `timeout` | `float` | |

**返回**: `None`

Source: `src\concurrent_v2.py:184`

---

### `acquire`

```matha
def acquire(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\concurrent_v2.py:189`

---

### `release`

```matha
def release(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\concurrent_v2.py:203`

---

### `__enter__`

```matha
def __enter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\concurrent_v2.py:210`

---

### `__exit__`

```matha
def __exit__(self, *args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\concurrent_v2.py:215`

---

# `cross_language_verifier`

```matha

```

**描述**: 跨语言交叉验证框架

验证同一算法在不同语言前端中生成相同的 IR/MIR。

工作流程：
  1. 定义跨语言算法（Python/Rust/Go/JS/C 各一份）
  2. 各前端编译为 IR
  3. 各 IR 转换为 MIR
  4. 用 MathaVM 执行所有 MIR
  5. 对比输出结果 → 一致性验证

使用示例：
  from src.cross_language_verifier import CrossLanguageVerifier

  verifier = CrossLanguageVerifier()
  result = verifier.verify("sin(3.14) + cos(1.57)", {
      "python": "x = sin(3.14) + cos(1.57)",
      "rust": "fn main() -> f64 { sin(3.14) + cos(1.57) }",
      "go": "func main() float64 { return sin(3.14) + cos(1.57) }",
      "js": "const x = sin(3.14) + cos(1.57)",
      "c": "double main() { return sin(3.14) + cos(1.57); }",
  })
  print(result)

Source: `src\cross_language_verifier.py:1`

---

## `LanguageResult`

```matha
class LanguageResult
```

**描述**: 单个语言的验证结果。

**装饰器**: dataclass

Source: `src\cross_language_verifier.py:50`

---

## `CrossLanguageResult`

```matha
class CrossLanguageResult
```

**描述**: 跨语言交叉验证结果。

**装饰器**: dataclass

Source: `src\cross_language_verifier.py:64`

---

### `passed`

```matha
def passed(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\cross_language_verifier.py:73`

---

## `CrossLanguageVerifier`

```matha
class CrossLanguageVerifier
```

**描述**: 跨语言交叉验证器。

功能：
1. 编译多种语言的源码为统一 IR
2. 将 IR 转换为 Matha MIR
3. 用 MathaVM 执行所有 MIR
4. 对比执行结果的一致性
5. 报告差异和分析

Source: `src\cross_language_verifier.py:82`

---

### `__init__`

```matha
def __init__(self, verbose: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `verbose` | `bool` | |

**返回**: `None`

Source: `src\cross_language_verifier.py:94`

---

### `verify`

```matha
def verify(self, algorithm: str, sources: dict[str, str]) -> CrossLanguageResult
```

**描述**: 验证同一算法在多种语言中的执行一致性。

Args:
    algorithm: 算法描述
    sources: {语言名: 源码}

Returns:
    CrossLanguageResult

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `algorithm` | `str` | |
| `sources` | `dict[str, str]` | |

**返回**: `CrossLanguageResult`

Source: `src\cross_language_verifier.py:102`

---

### `batch_verify`

```matha
def batch_verify(self, test_cases: list[dict]) -> dict
```

**描述**: 批量验证多个测试用例。

test_cases: [
    {"algorithm": "sin_test", "sources": {"python": "...", "rust": "..."}},
    ...
]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `test_cases` | `list[dict]` | |

**返回**: `dict`

Source: `src\cross_language_verifier.py:136`

---

### `_verify_language`

```matha
def _verify_language(self, language: str, source: str) -> LanguageResult
```

**描述**: 验证单个语言的编译和执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `language` | `str` | |
| `source` | `str` | |

**返回**: `LanguageResult`

Source: `src\cross_language_verifier.py:161`

---

### `_print_lang_result`

```matha
def _print_lang_result(self, lang: str, result: LanguageResult) -> None
```

**描述**: 打印单个语言的验证结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `result` | `LanguageResult` | |

**返回**: `None`

Source: `src\cross_language_verifier.py:199`

---

### `_generate_summary`

```matha
def _generate_summary(self, result: CrossLanguageResult) -> str
```

**描述**: 生成验证摘要。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `result` | `CrossLanguageResult` | |

**返回**: `str`

Source: `src\cross_language_verifier.py:212`

---

### `print_report`

```matha
def print_report(self, result: CrossLanguageResult) -> None
```

**描述**: 打印完整报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `result` | `CrossLanguageResult` | |

**返回**: `None`

Source: `src\cross_language_verifier.py:230`

---

# `csp_os_thread`

```matha

```

**描述**: Matha CSP OS线程并发模型 v2.0
==============================
解决 Python GIL 限制，实现真正的并行计算。

设计原则：
  1. OS 原生线程替代 asyncio 协程
  2. 共享 channel 替代共享内存
  3. 无锁数据结构（Lock-free）
  4. 进程级隔离（multiprocessing）

与 Go goroutine 的对比：
  - Go: goroutine 是用户态轻量线程，运行时调度
  - Matha: 使用 OS 线程 + 进程池，更贴近系统级并发

性能提升预期：
  - 纯计算任务：~8x 加速（8 线程）
  - I/O 密集型：~10x 加速（受 I/O 限制）
  - CPU 密集型：~6-7x 加速（受 GIL 限制，需进程级隔离）

Source: `src\csp_os_thread.py:1`

---

## `Channel`

```matha
class Channel
```

**描述**: CSP Channel：线程安全的无界队列。

支持操作：
  - ch <- value   (send)
  - v = <- ch     (recv)
  - select { ... } (multi-channel select)

Source: `src\csp_os_thread.py:40`

---

### `__init__`

```matha
def __init__(self, capacity: int)
```

**描述**: capacity=0 表示无界。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `capacity` | `int` | |

Source: `src\csp_os_thread.py:50`

---

### `send`

```matha
def send(self, value: T) -> None
```

**描述**: 发送值到 channel。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `T` | |

**返回**: `None`

Source: `src\csp_os_thread.py:58`

---

### `recv`

```matha
def recv(self, timeout: float) -> T
```

**描述**: 从 channel 接收值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `timeout` | `float` | |

**返回**: `T`

Source: `src\csp_os_thread.py:66`

---

### `close`

```matha
def close(self) -> None
```

**描述**: 关闭 channel。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\csp_os_thread.py:75`

---

### `is_closed`

```matha
def is_closed(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\csp_os_thread.py:80`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\csp_os_thread.py:83`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\csp_os_thread.py:91`

---

## `Goroutine`

```matha
class Goroutine
```

**描述**: 包装 OS 线程的轻量级 goroutine。

与 Go goroutine 的区别：
  - Go: 用户态协程，运行时调度（~2KB 栈）
  - Matha: OS 线程，内核调度（~8MB 栈）

Source: `src\csp_os_thread.py:99`

---

### `__init__`

```matha
def __init__(self, target: Callable, args: tuple, kwargs: dict)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target` | `Callable` | |
| `args` | `tuple` | |
| `kwargs` | `dict` | |

Source: `src\csp_os_thread.py:108`

---

### `start`

```matha
def start(self) -> 'Goroutine'
```

**描述**: 启动 goroutine。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'Goroutine'`

Source: `src\csp_os_thread.py:118`

---

### `_run`

```matha
def _run(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\csp_os_thread.py:129`

---

### `join`

```matha
def join(self, timeout: float) -> Optional[Any]
```

**描述**: 等待 goroutine 完成。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `timeout` | `float` | |

**返回**: `Optional[Any]`

Source: `src\csp_os_thread.py:137`

---

### `is_alive`

```matha
def is_alive(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\csp_os_thread.py:145`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\csp_os_thread.py:148`

---

## `SelectCase`

```matha
class SelectCase
```

**描述**: select 语句的一个分支。

**装饰器**: dataclass

Source: `src\csp_os_thread.py:162`

---

## `CSPRuntime`

```matha
class CSPRuntime
```

**描述**: CSP 运行时：管理 goroutine 和 channel。

支持操作：
  - go func()   (启动 goroutine)
  - ch <- v     (发送)
  - v = <- ch   (接收)
  - select { ... } (多路复用)

Source: `src\csp_os_thread.py:169`

---

### `__init__`

```matha
def __init__(self, max_workers: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_workers` | `int` | |

Source: `src\csp_os_thread.py:180`

---

### `go`

```matha
def go(self, target: Callable, *args, **kwargs) -> Goroutine
```

**描述**: 启动 goroutine。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target` | `Callable` | |

**返回**: `Goroutine`

Source: `src\csp_os_thread.py:186`

---

### `new_channel`

```matha
def new_channel(self, capacity: int) -> Channel
```

**描述**: 创建新 channel。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `capacity` | `int` | |

**返回**: `Channel`

Source: `src\csp_os_thread.py:194`

---

### `select`

```matha
def select(self, cases: List[SelectCase], timeout: float) -> tuple
```

**描述**: 多路复用选择。

使用 threading.Condition 实现 select。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cases` | `List[SelectCase]` | |
| `timeout` | `float` | |

**返回**: `tuple`

Source: `src\csp_os_thread.py:201`

---

### `wait_all`

```matha
def wait_all(self, timeout: float) -> List[Any]
```

**描述**: 等待所有 goroutine 完成。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `timeout` | `float` | |

**返回**: `List[Any]`

Source: `src\csp_os_thread.py:221`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\csp_os_thread.py:231`

---

## `ProcessPool`

```matha
class ProcessPool
```

**描述**: 进程级并行池，绕过 Python GIL 限制。

适用于 CPU 密集型任务。

Source: `src\csp_os_thread.py:244`

---

### `__init__`

```matha
def __init__(self, num_workers: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `num_workers` | `int` | |

Source: `src\csp_os_thread.py:251`

---

### `map`

```matha
def map(self, func: Callable, items: List[Any]) -> List[Any]
```

**描述**: 并行 map。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `items` | `List[Any]` | |

**返回**: `List[Any]`

Source: `src\csp_os_thread.py:255`

---

### `starmap`

```matha
def starmap(self, func: Callable, items: List[tuple]) -> List[Any]
```

**描述**: 并行 starmap。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `items` | `List[tuple]` | |

**返回**: `List[Any]`

Source: `src\csp_os_thread.py:260`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\csp_os_thread.py:265`

---

## `go`

```matha
def go(target: Callable, *args, **kwargs) -> Goroutine
```

**描述**: 快捷启动 goroutine。

| 参数 | 类型 | 说明 |
|------|------|------|
| `target` | `Callable` | |

**返回**: `Goroutine`

Source: `src\csp_os_thread.py:276`

---

## `channel`

```matha
def channel(capacity: int) -> Channel
```

**描述**: 快捷创建 channel。

| 参数 | 类型 | 说明 |
|------|------|------|
| `capacity` | `int` | |

**返回**: `Channel`

Source: `src\csp_os_thread.py:282`

---

## `parallel_map`

```matha
def parallel_map(func: Callable, items: List[Any], workers: int) -> List[Any]
```

**描述**: 并行 map（绕过 GIL）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `items` | `List[Any]` | |
| `workers` | `int` | |

**返回**: `List[Any]`

Source: `src\csp_os_thread.py:288`

---

## `compute_square`

```matha
def compute_square(x)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `` | |

Source: `src\csp_os_thread.py:316`

---

## `heavy_compute`

```matha
def heavy_compute(x)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `` | |

Source: `src\csp_os_thread.py:332`

---

# `demos.demo_calculus_matrix`

```matha

```

**描述**: Matha v4.4 — 符号微积分与矩阵运算整合演示

本脚本演示如何同时使用符号微积分和矩阵运算模块。

功能：
  1. 符号求导 + 矩阵计算
  2. 符号积分 + 数值积分验证
  3. 泰勒展开 + 多项式拟合
  4. 极限计算 + 收敛性分析
  5. 微分方程 + 数值解验证
  6. 综合应用：矩阵微积分

用法：
  python src/demos/demo_calculus_matrix.py                          # 运行全部
  python src/demos/demo_calculus_matrix.py --mode derivative        # 仅求导
  python src/demos/demo_calculus_matrix.py --mode integral          # 仅积分
  python src/demos/demo_calculus_matrix.py --mode matrix            # 仅矩阵
  python src/demos/demo_calculus_matrix.py --verbose                # 详细日志（DEBUG 级别）
  python src/demos/demo_calculus_matrix.py --expr "x**2"            # 自定义表达式
  python src/demos/demo_calculus_matrix.py --parallel               # 并行计算

Source: `src\demos\demo_calculus_matrix.py:1`

---

## `setup_logging`

```matha
def setup_logging(verbose: bool)
```

**描述**: 设置日志级别。默认 INFO，--verbose 时启用 DEBUG。

| 参数 | 类型 | 说明 |
|------|------|------|
| `verbose` | `bool` | |

Source: `src\demos\demo_calculus_matrix.py:37`

---

## `print_section`

```matha
def print_section(title: str)
```

**描述**: 打印章节标题。

| 参数 | 类型 | 说明 |
|------|------|------|
| `title` | `str` | |

Source: `src\demos\demo_calculus_matrix.py:47`

---

## `print_result`

```matha
def print_result(label: str, value)
```

**描述**: 打印结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `label` | `str` | |
| `value` | `` | |

Source: `src\demos\demo_calculus_matrix.py:54`

---

## `parallel_map`

```matha
def parallel_map(func, items, max_workers: int)
```

**描述**: 并行映射。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |
| `items` | `` | |
| `max_workers` | `int` | |

Source: `src\demos\demo_calculus_matrix.py:66`

---

## `demo1_derivative_matrix`

```matha
def demo1_derivative_matrix(expr: str)
```

**描述**: 演示1：符号求导与矩阵运算结合。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

Source: `src\demos\demo_calculus_matrix.py:80`

---

## `demo2_integral_verification`

```matha
def demo2_integral_verification(expr: str)
```

**描述**: 演示2：符号积分与数值验证。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

Source: `src\demos\demo_calculus_matrix.py:123`

---

## `demo3_taylor_matrix`

```matha
def demo3_taylor_matrix()
```

**描述**: 演示3：泰勒展开与矩阵拟合。

Source: `src\demos\demo_calculus_matrix.py:168`

---

## `demo4_limit_convergence`

```matha
def demo4_limit_convergence()
```

**描述**: 演示4：极限计算与收敛性分析。

Source: `src\demos\demo_calculus_matrix.py:205`

---

## `demo5_ode_matrix`

```matha
def demo5_ode_matrix()
```

**描述**: 演示5：微分方程与矩阵求解。

Source: `src\demos\demo_calculus_matrix.py:243`

---

## `demo6_matrix_calculus`

```matha
def demo6_matrix_calculus(use_numpy: bool, parallel: bool, n_workers: int, use_sparse: bool)
```

**描述**: 演示6：矩阵微积分综合应用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `use_numpy` | `bool` | |
| `parallel` | `bool` | |
| `n_workers` | `int` | |
| `use_sparse` | `bool` | |

Source: `src\demos\demo_calculus_matrix.py:281`

---

## `main`

```matha
def main()
```

**描述**: 主函数。

Source: `src\demos\demo_calculus_matrix.py:456`

---

# `deps`

```matha

```

**描述**: Matha 依赖层：全功能标准库实现 + 可选增强模块自动检测。

设计原则：
  - 无依赖可运行：所有功能均有纯 Python 标准库实现
  - 可选增强：检测到第三方库时自动切换高性能版本
  - 透明切换：上层代码无需修改，依赖层自动选择最佳实现

依赖分类：
  [S] Standalone - 仅用标准库，零外部依赖
  [O] Optional   - 可选增强，未安装时自动降级为标准库实现

包含模块：
  S: cache        - 内存缓存（LRU/TTL）
  S: serde        - 序列化/反序列化（JSON fallback）
  S: logging      - 结构化日志（logging fallback）
  S: platform     - 平台检测与路径处理
  S: io_utils     - 文件/目录操作增强
  S: timing       - 性能计时工具
  O: msgpack      - 高性能二进制序列化（降级为 json）
  O: ujson        - 极速 JSON（降级为 json）
  O: orjson       - 零拷贝 JSON（降级为 json）
  O: pyyaml       - YAML 支持（降级为 JSON）
  O: lz4         - 快速压缩（降级为无压缩）

Source: `src\deps.py:1`

---

## `get_platform`

```matha
def get_platform() -> str
```

**描述**: 获取当前平台标识。

**返回**: `str`

Source: `src\deps.py:39`

---

## `get_arch`

```matha
def get_arch() -> str
```

**描述**: 获取 CPU 架构。

**返回**: `str`

Source: `src\deps.py:51`

---

## `get_python_version`

```matha
def get_python_version() -> tuple[int, int, int]
```

**描述**: 获取 Python 版本。

**返回**: `tuple[int, int, int]`

Source: `src\deps.py:56`

---

## `is_portable_env`

```matha
def is_portable_env() -> bool
```

**描述**: 检测是否为无 pip 的便携式环境（如 pyarmor、Nuitka 静态编译）。

**返回**: `bool`

Source: `src\deps.py:61`

---

## `resolve_matha_root`

```matha
def resolve_matha_root() -> Path
```

**描述**: 解析 Matha 项目根目录。

**返回**: `Path`

Source: `src\deps.py:66`

---

## `LRUCache`

```matha
class LRUCache
```

**描述**: 线程安全的 LRU 缓存，纯标准库实现。

Source: `src\deps.py:80`

---

### `__init__`

```matha
def __init__(self, maxsize: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `maxsize` | `int` | |

Source: `src\deps.py:83`

---

### `get`

```matha
def get(self, key) -> object | None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `` | |

**返回**: `object | None`

Source: `src\deps.py:90`

---

### `put`

```matha
def put(self, key, value) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `` | |
| `value` | `` | |

**返回**: `None`

Source: `src\deps.py:99`

---

### `invalidate`

```matha
def invalidate(self, key) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `` | |

**返回**: `bool`

Source: `src\deps.py:107`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\deps.py:114`

---

### `size`

```matha
def size(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\deps.py:119`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\deps.py:124`

---

## `TTLCache`

```matha
class TTLCache
```

**描述**: 带 TTL 的缓存，纯标准库实现。

Source: `src\deps.py:135`

---

### `__init__`

```matha
def __init__(self, default_ttl: float, maxsize: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `default_ttl` | `float` | |
| `maxsize` | `int` | |

Source: `src\deps.py:138`

---

### `get`

```matha
def get(self, key, ttl: float) -> object | None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `` | |
| `ttl` | `float` | |

**返回**: `object | None`

Source: `src\deps.py:144`

---

### `put`

```matha
def put(self, key, value, ttl: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `` | |
| `value` | `` | |
| `ttl` | `float` | |

**返回**: `None`

Source: `src\deps.py:154`

---

### `invalidate`

```matha
def invalidate(self, key) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `` | |

**返回**: `bool`

Source: `src\deps.py:160`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\deps.py:167`

---

### `_evict_if_needed`

```matha
def _evict_if_needed(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\deps.py:171`

---

## `to_json`

```matha
def to_json(obj) -> str
```

**描述**: 对象序列化为 JSON 字符串（纯标准库）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `str`

Source: `src\deps.py:193`

---

## `from_json`

```matha
def from_json(text: str) -> object
```

**描述**: JSON 字符串反序列化为对象（纯标准库）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `object`

Source: `src\deps.py:198`

---

## `to_json_pretty`

```matha
def to_json_pretty(obj) -> str
```

**描述**: 对象序列化为格式化 JSON 字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `str`

Source: `src\deps.py:203`

---

## `_json_default`

```matha
def _json_default(obj)
```

**描述**: JSON 默认序列化器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

Source: `src\deps.py:208`

---

## `to_pickle`

```matha
def to_pickle(obj) -> bytes
```

**描述**: 对象序列化为 pickle 字节（纯标准库）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `bytes`

Source: `src\deps.py:221`

---

## `from_pickle`

```matha
def from_pickle(data: bytes) -> object
```

**描述**: pickle 字节反序列化为对象（纯标准库）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `object`

Source: `src\deps.py:226`

---

## `to_b64`

```matha
def to_b64(obj) -> str
```

**描述**: 对象序列化为 Base64 字符串（JSON 兼容）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `str`

Source: `src\deps.py:231`

---

## `from_b64`

```matha
def from_b64(text: str) -> object
```

**描述**: Base64 字符串反序列化为对象。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `object`

Source: `src\deps.py:236`

---

## `hash_obj`

```matha
def hash_obj(obj) -> str
```

**描述**: 计算对象的 SHA-256 哈希（用于缓存键）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `str`

Source: `src\deps.py:241`

---

## `_try_import_optional`

```matha
def _try_import_optional()
```

**描述**: 尝试导入可选依赖，失败则保持 None。

Source: `src\deps.py:255`

---

## `fast_json_dumps`

```matha
def fast_json_dumps(obj) -> str
```

**描述**: 尝试使用最快的 JSON 实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `str`

Source: `src\deps.py:283`

---

## `fast_json_loads`

```matha
def fast_json_loads(text: str) -> object
```

**描述**: 尝试使用最快的 JSON 实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `object`

Source: `src\deps.py:292`

---

## `fast_serialize`

```matha
def fast_serialize(obj) -> bytes
```

**描述**: 尝试使用最快的序列化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `bytes`

Source: `src\deps.py:301`

---

## `fast_deserialize`

```matha
def fast_deserialize(data: bytes) -> object
```

**描述**: 尝试使用最快的反序列化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `object`

Source: `src\deps.py:308`

---

## `compress_data`

```matha
def compress_data(data: bytes) -> bytes
```

**描述**: 压缩数据（优先 lz4，降级 zlib）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `bytes`

Source: `src\deps.py:315`

---

## `decompress_data`

```matha
def decompress_data(data: bytes) -> bytes
```

**描述**: 解压缩数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `bytes`

Source: `src\deps.py:322`

---

## `serialize_for_storage`

```matha
def serialize_for_storage(obj, use_compression: bool) -> bytes
```

**描述**: 序列化为可存储格式（带压缩）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |
| `use_compression` | `bool` | |

**返回**: `bytes`

Source: `src\deps.py:329`

---

## `deserialize_from_storage`

```matha
def deserialize_from_storage(data: bytes) -> object
```

**描述**: 从存储格式反序列化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `object`

Source: `src\deps.py:337`

---

## `MathaLogger`

```matha
class MathaLogger
```

**描述**: 轻量结构化日志器，纯标准库实现。

Source: `src\deps.py:352`

---

### `__init__`

```matha
def __init__(self, name: str, level: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `level` | `int` | |

Source: `src\deps.py:355`

---

### `info`

```matha
def info(self, msg: str, **kwargs) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `None`

Source: `src\deps.py:366`

---

### `debug`

```matha
def debug(self, msg: str, **kwargs) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `None`

Source: `src\deps.py:370`

---

### `warning`

```matha
def warning(self, msg: str, **kwargs) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `None`

Source: `src\deps.py:374`

---

### `error`

```matha
def error(self, msg: str, **kwargs) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `None`

Source: `src\deps.py:378`

---

### `set_level`

```matha
def set_level(self, level: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `int` | |

**返回**: `None`

Source: `src\deps.py:382`

---

### `is_debug`

```matha
def is_debug(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\deps.py:386`

---

## `safe_read_text`

```matha
def safe_read_text(path: str, encoding: str) -> str
```

**描述**: 安全读取文本文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `encoding` | `str` | |

**返回**: `str`

Source: `src\deps.py:394`

---

## `safe_write_text`

```matha
def safe_write_text(path: str, content: str, encoding: str) -> None
```

**描述**: 安全写入文本文件（自动创建目录）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `content` | `str` | |
| `encoding` | `str` | |

**返回**: `None`

Source: `src\deps.py:400`

---

## `safe_append_text`

```matha
def safe_append_text(path: str, content: str, encoding: str) -> None
```

**描述**: 安全追加文本文件（自动创建目录）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `content` | `str` | |
| `encoding` | `str` | |

**返回**: `None`

Source: `src\deps.py:407`

---

## `ensure_dir`

```matha
def ensure_dir(path: str) -> Path
```

**描述**: 确保目录存在。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

**返回**: `Path`

Source: `src\deps.py:414`

---

## `list_files`

```matha
def list_files(path: str, pattern: str) -> list
```

**描述**: 列出目录下匹配的文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `pattern` | `str` | |

**返回**: `list`

Source: `src\deps.py:421`

---

## `file_age_seconds`

```matha
def file_age_seconds(path: str) -> float
```

**描述**: 文件距今秒数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

**返回**: `float`

Source: `src\deps.py:426`

---

## `atomic_write`

```matha
def atomic_write(path: str, content: str, encoding: str) -> None
```

**描述**: 原子写入文件（先写临时文件再重命名）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `content` | `str` | |
| `encoding` | `str` | |

**返回**: `None`

Source: `src\deps.py:435`

---

## `timing`

```matha
def timing(func, *args, **kwargs) -> tuple
```

**描述**: 执行函数并返回 (结果, 耗时秒数)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

**返回**: `tuple`

Source: `src\deps.py:449`

---

## `benchmark`

```matha
def benchmark(func, *args, **kwargs) -> dict
```

**描述**: 性能基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

**返回**: `dict`

Source: `src\deps.py:457`

---

## `get_dependency_status`

```matha
def get_dependency_status() -> dict
```

**描述**: 返回当前依赖状态报告。

**返回**: `dict`

Source: `src\deps.py:478`

---

## `_try_import_yaml`

```matha
def _try_import_yaml() -> bool
```

**返回**: `bool`

Source: `src\deps.py:495`

---

# `diagnostics`

```matha

```

**描述**: Matha IDE 诊断与错误提示系统。

功能：
  1. 语法错误高亮：在源码中标记错误位置
  2. 语义错误诊断：未定义变量、类型不匹配等
  3. 智能修复建议：给出可能的修正方案
  4. LSP 兼容格式：输出 JSON 供 IDE 插件消费
  5. VSCode 扩展：提供实时诊断

Source: `src\diagnostics.py:1`

---

## `DiagnosticSeverity`

```matha
class DiagnosticSeverity(Enum)
```

Source: `src\diagnostics.py:24`

---

## `Diagnostic`

```matha
class Diagnostic
```

**描述**: 诊断信息（LSP 兼容格式）。

**装饰器**: dataclass

Source: `src\diagnostics.py:36`

---

### `to_lsp`

```matha
def to_lsp(self) -> dict
```

**描述**: 转换为 LSP Diagnostic 格式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\diagnostics.py:49`

---

### `to_json`

```matha
def to_json(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\diagnostics.py:64`

---

## `MathaErrorKind`

```matha
class MathaErrorKind(Enum)
```

Source: `src\diagnostics.py:72`

---

## `DiagnosticCollector`

```matha
class DiagnosticCollector
```

**描述**: 收集并管理诊断信息。

Source: `src\diagnostics.py:145`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\diagnostics.py:148`

---

### `add`

```matha
def add(self, diag: Diagnostic) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `diag` | `Diagnostic` | |

**返回**: `None`

Source: `src\diagnostics.py:153`

---

### `add_error`

```matha
def add_error(self, message: str, line: int, col: int, code: str, fix: str) -> Diagnostic
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `code` | `str` | |
| `fix` | `str` | |

**返回**: `Diagnostic`

Source: `src\diagnostics.py:160`

---

### `add_warning`

```matha
def add_warning(self, message: str, line: int, col: int, code: str, fix: str) -> Diagnostic
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `code` | `str` | |
| `fix` | `str` | |

**返回**: `Diagnostic`

Source: `src\diagnostics.py:169`

---

### `add_hint`

```matha
def add_hint(self, message: str, line: int, col: int) -> Diagnostic
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `line` | `int` | |
| `col` | `int` | |

**返回**: `Diagnostic`

Source: `src\diagnostics.py:178`

---

### `errors`

```matha
def errors(self) -> list[Diagnostic]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[Diagnostic]`

**装饰器**: property

Source: `src\diagnostics.py:187`

---

### `warnings`

```matha
def warnings(self) -> list[Diagnostic]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[Diagnostic]`

**装饰器**: property

Source: `src\diagnostics.py:191`

---

### `has_errors`

```matha
def has_errors(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\diagnostics.py:195`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\diagnostics.py:198`

---

### `summary`

```matha
def summary(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\diagnostics.py:203`

---

### `to_json`

```matha
def to_json(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\diagnostics.py:208`

---

## `SourceHighlighter`

```matha
class SourceHighlighter
```

**描述**: 源码错误高亮器。

Source: `src\diagnostics.py:217`

---

### `highlight`

```matha
def highlight(cls, source: str, line: int, col: int, message: str, severity: DiagnosticSeverity) -> str
```

**描述**: 高亮源码中的错误位置。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `source` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `message` | `str` | |
| `severity` | `DiagnosticSeverity` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\diagnostics.py:230`

---

### `highlight_range`

```matha
def highlight_range(cls, source: str, start_line: int, start_col: int, end_line: int, end_col: int, message: str, severity: DiagnosticSeverity) -> str
```

**描述**: 高亮源码中的错误范围。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `source` | `str` | |
| `start_line` | `int` | |
| `start_col` | `int` | |
| `end_line` | `int` | |
| `end_col` | `int` | |
| `message` | `str` | |
| `severity` | `DiagnosticSeverity` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\diagnostics.py:258`

---

### `print_diagnostics`

```matha
def print_diagnostics(cls, source: str, diagnostics: list[Diagnostic]) -> None
```

**描述**: 打印所有诊断信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `source` | `str` | |
| `diagnostics` | `list[Diagnostic]` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\diagnostics.py:282`

---

## `LSPServer`

```matha
class LSPServer
```

**描述**: 简单的 LSP 服务器实现（供 VSCode 插件使用）。

Source: `src\diagnostics.py:293`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\diagnostics.py:296`

---

### `analyze`

```matha
def analyze(self, source: str, uri: str) -> list[dict]
```

**描述**: 分析源码并返回诊断（LSP 格式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `uri` | `str` | |

**返回**: `list[dict]`

Source: `src\diagnostics.py:299`

---

### `_parse_diagnostics`

```matha
def _parse_diagnostics(self, source: str) -> None
```

**描述**: 从源码中解析诊断信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `None`

Source: `src\diagnostics.py:305`

---

### `get_completions`

```matha
def get_completions(self, source: str, line: int, col: int) -> list[dict]
```

**描述**: 提供自动补全建议。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `line` | `int` | |
| `col` | `int` | |

**返回**: `list[dict]`

Source: `src\diagnostics.py:329`

---

# `diagnostics_v2`

```matha

```

**描述**: Matha 增强诊断系统：代码高亮 + 修复建议 + 上下文分析 + 历史追踪。

Source: `src\diagnostics_v2.py:1`

---

## `Severity`

```matha
class Severity(Enum)
```

Source: `src\diagnostics_v2.py:17`

---

## `Diagnostic`

```matha
class Diagnostic
```

**描述**: LSP 兼容诊断。

**装饰器**: dataclass

Source: `src\diagnostics_v2.py:29`

---

### `to_lsp`

```matha
def to_lsp(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\diagnostics_v2.py:43`

---

### `to_json`

```matha
def to_json(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\diagnostics_v2.py:57`

---

## `ContextAnalyzer`

```matha
class ContextAnalyzer
```

**描述**: 分析错误上下文，提供更有意义的错误信息。

Source: `src\diagnostics_v2.py:88`

---

### `__init__`

```matha
def __init__(self, source: str, line: int, col: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `line` | `int` | |
| `col` | `int` | |

**返回**: `None`

Source: `src\diagnostics_v2.py:91`

---

### `get_context`

```matha
def get_context(self, radius: int) -> list[str]
```

**描述**: 获取错误位置的上下文代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `radius` | `int` | |

**返回**: `list[str]`

Source: `src\diagnostics_v2.py:97`

---

### `find_similar_vars`

```matha
def find_similar_vars(self, name: str) -> list[str]
```

**描述**: 查找相似名称的已定义变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `list[str]`

Source: `src\diagnostics_v2.py:103`

---

### `find_similar_funcs`

```matha
def find_similar_funcs(self, name: str, known_funcs: list[str]) -> list[str]
```

**描述**: 查找相似名称的已知函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `known_funcs` | `list[str]` | |

**返回**: `list[str]`

Source: `src\diagnostics_v2.py:112`

---

### `_levenshtein`

```matha
def _levenshtein(s1: str, s2: str) -> int
```

**描述**: 计算编辑距离。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s1` | `str` | |
| `s2` | `str` | |

**返回**: `int`

**装饰器**: staticmethod

Source: `src\diagnostics_v2.py:118`

---

## `ErrorHistory`

```matha
class ErrorHistory
```

**描述**: 错误历史追踪。

Source: `src\diagnostics_v2.py:140`

---

### `__init__`

```matha
def __init__(self, max_entries: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_entries` | `int` | |

**返回**: `None`

Source: `src\diagnostics_v2.py:143`

---

### `record`

```matha
def record(self, diagnostic: Diagnostic) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `diagnostic` | `Diagnostic` | |

**返回**: `None`

Source: `src\diagnostics_v2.py:148`

---

### `get_duplicates`

```matha
def get_duplicates(self) -> list[dict]
```

**描述**: 返回重复出现的错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\diagnostics_v2.py:160`

---

### `get_recent`

```matha
def get_recent(self, n: int) -> list[dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |

**返回**: `list[dict]`

Source: `src\diagnostics_v2.py:165`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\diagnostics_v2.py:168`

---

## `EnhancedDiagnosticCollector`

```matha
class EnhancedDiagnosticCollector
```

**描述**: 增强诊断收集器。

Source: `src\diagnostics_v2.py:177`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\diagnostics_v2.py:180`

---

### `add`

```matha
def add(self, diag: Diagnostic) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `diag` | `Diagnostic` | |

**返回**: `None`

Source: `src\diagnostics_v2.py:186`

---

### `add_error`

```matha
def add_error(self, message: str, line: int, col: int, code: str, fix: str, context: list[str]) -> Diagnostic
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `code` | `str` | |
| `fix` | `str` | |
| `context` | `list[str]` | |

**返回**: `Diagnostic`

Source: `src\diagnostics_v2.py:194`

---

### `add_warning`

```matha
def add_warning(self, message: str, line: int, col: int, code: str, fix: str) -> Diagnostic
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `code` | `str` | |
| `fix` | `str` | |

**返回**: `Diagnostic`

Source: `src\diagnostics_v2.py:205`

---

### `analyze_source`

```matha
def analyze_source(self, source: str, errors: list[str]) -> list[Diagnostic]
```

**描述**: 从源码和错误列表分析诊断。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `errors` | `list[str]` | |

**返回**: `list[Diagnostic]`

Source: `src\diagnostics_v2.py:214`

---

### `_find_line`

```matha
def _find_line(self, lines: list[str], message: str) -> int
```

**描述**: 在源码中查找错误行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lines` | `list[str]` | |
| `message` | `str` | |

**返回**: `int`

Source: `src\diagnostics_v2.py:248`

---

### `errors`

```matha
def errors(self) -> list[Diagnostic]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[Diagnostic]`

**装饰器**: property

Source: `src\diagnostics_v2.py:258`

---

### `warnings`

```matha
def warnings(self) -> list[Diagnostic]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[Diagnostic]`

**装饰器**: property

Source: `src\diagnostics_v2.py:262`

---

### `has_errors`

```matha
def has_errors(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\diagnostics_v2.py:266`

---

### `summary`

```matha
def summary(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\diagnostics_v2.py:269`

---

### `to_json`

```matha
def to_json(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\diagnostics_v2.py:274`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\diagnostics_v2.py:278`

---

## `SourceHighlighter`

```matha
class SourceHighlighter
```

**描述**: 源码错误高亮器。

Source: `src\diagnostics_v2.py:289`

---

### `highlight`

```matha
def highlight(cls, source: str, line: int, col: int, message: str, severity: Severity) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `source` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `message` | `str` | |
| `severity` | `Severity` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\diagnostics_v2.py:302`

---

### `print_diagnostics`

```matha
def print_diagnostics(cls, source: str, diagnostics: list[Diagnostic]) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `source` | `str` | |
| `diagnostics` | `list[Diagnostic]` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\diagnostics_v2.py:323`

---

# `doc_gen`

```matha

```

**描述**: Matha API 文档自动生成器

从源码中提取函数签名、类型注解、docstring，生成 Markdown 和 HTML 文档。

功能：
  1. 自动扫描 src/ 目录下的所有模块
  2. 提取类、函数、方法的签名和文档
  3. 生成 Markdown 格式 API 文档
  4. 生成 HTML 格式 API 文档（带导航）
  5. 生成 JSON 格式索引（供 IDE/工具使用）

用法：
  from src.doc_gen import DocGenerator
  gen = DocGenerator(src_dir="src", output_dir="docs")
  gen.generate_all()

Source: `src\doc_gen.py:1`

---

## `DocEntry`

```matha
class DocEntry
```

**描述**: 文档条目。

**装饰器**: dataclass

Source: `src\doc_gen.py:36`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\doc_gen.py:52`

---

## `DocExtractor`

```matha
class DocExtractor(ast.NodeVisitor)
```

**描述**: 从 AST 中提取文档信息。

Source: `src\doc_gen.py:68`

---

### `__init__`

```matha
def __init__(self, source_file: str, module_name: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source_file` | `str` | |
| `module_name` | `str` | |

Source: `src\doc_gen.py:71`

---

### `visit_Module`

```matha
def visit_Module(self, node: ast.Module) -> None
```

**描述**: 提取模块级文档。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Module` | |

**返回**: `None`

Source: `src\doc_gen.py:78`

---

### `visit_ClassDef`

```matha
def visit_ClassDef(self, node: ast.ClassDef) -> None
```

**描述**: 提取类定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ClassDef` | |

**返回**: `None`

Source: `src\doc_gen.py:95`

---

### `visit_FunctionDef`

```matha
def visit_FunctionDef(self, node: ast.FunctionDef) -> None
```

**描述**: 提取函数/方法定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FunctionDef` | |

**返回**: `None`

Source: `src\doc_gen.py:130`

---

### `visit_Import`

```matha
def visit_Import(self, node: ast.Import) -> None
```

**描述**: 记录导入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Import` | |

**返回**: `None`

Source: `src\doc_gen.py:192`

---

### `_decode_decorator`

```matha
def _decode_decorator(self, node: ast.expr) -> str
```

**描述**: 将装饰器 AST 节点转换为字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.expr` | |

**返回**: `str`

Source: `src\doc_gen.py:200`

---

### `_ast_to_str`

```matha
def _ast_to_str(self, node: ast.AST) -> str
```

**描述**: 将 AST 节点转换为源码字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.AST` | |

**返回**: `str`

Source: `src\doc_gen.py:207`

---

### `get_entries`

```matha
def get_entries(self) -> List[DocEntry]
```

**描述**: 获取所有文档条目。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[DocEntry]`

Source: `src\doc_gen.py:211`

---

## `DocGenerator`

```matha
class DocGenerator
```

**描述**: Matha API 文档自动生成器。

生成格式：
  - Markdown (.md) — 适合 GitHub/文档站点
  - HTML (.html) — 适合浏览器查看
  - JSON (.json) — 适合 IDE/工具集成

Source: `src\doc_gen.py:220`

---

### `__init__`

```matha
def __init__(self, src_dir: str, output_dir: str, project_name: str, project_version: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `src_dir` | `str` | |
| `output_dir` | `str` | |
| `project_name` | `str` | |
| `project_version` | `str` | |

Source: `src\doc_gen.py:230`

---

### `generate_all`

```matha
def generate_all(self) -> Dict[str, str]
```

**描述**: 生成所有格式的文档。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, str]`

Source: `src\doc_gen.py:239`

---

### `_scan_and_extract`

```matha
def _scan_and_extract(self) -> None
```

**描述**: 扫描源码目录并提取文档信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\doc_gen.py:252`

---

### `_get_module_name`

```matha
def _get_module_name(self, path: Path) -> Optional[str]
```

**描述**: 从文件路径推导模块名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `Path` | |

**返回**: `Optional[str]`

Source: `src\doc_gen.py:294`

---

### `_generate_markdown`

```matha
def _generate_markdown(self) -> str
```

**描述**: 生成 Markdown 文档。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\doc_gen.py:307`

---

### `_entry_to_markdown`

```matha
def _entry_to_markdown(self, entry: DocEntry) -> List[str]
```

**描述**: 将单个条目转换为 Markdown。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `entry` | `DocEntry` | |

**返回**: `List[str]`

Source: `src\doc_gen.py:365`

---

### `_generate_html`

```matha
def _generate_html(self) -> str
```

**描述**: 生成 HTML 文档。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\doc_gen.py:413`

---

### `_generate_json`

```matha
def _generate_json(self) -> str
```

**描述**: 生成 JSON 索引。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\doc_gen.py:510`

---

# `domains.acoustics`

```matha

```

**描述**: Matha 机械领域模块：声学（Acoustics）。

基于力学 + 电磁学 + mathlib 数学地基，演化声学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、声波基础（Sound Wave Basics）
  1) 声速（空气中）：c = 331.3 + 0.6T（T 为摄氏温度）
  2) 波长-频率-波速关系：c = fλ → λ = c/f, f = c/λ
  3) 声波周期：T = 1/f
  4) 角频率：ω = 2πf
  5) 波数：k = 2π/λ = ω/c
  6) 声速（一般介质）：c = √(E/ρ)（纵波）/ c = √(G/ρ)（横波）

二、声强与声压级（Sound Intensity & SPL）
  1) 声强：I = p²/(ρc)（p 为声压有效值）
  2) 声强级：L_I = 10·lg(I/I₀)，I₀ = 10⁻¹² W/m²
  3) 声压级：L_p = 20·lg(p/p₀)，p₀ = 20μPa
  4) 声功率级：L_W = 10·lg(W/W₀)，W₀ = 10⁻¹² W
  5) 声强（由功率和面积）：I = W/A
  6) 多声源叠加（分贝相加）：L_total = 10·lg(Σ10^(L_i/10))

三、多普勒效应（Doppler Effect）
  1) 声源不动、观察者运动：f' = f(c±v_o)/c
  2) 观察者不动、声源运动：f' = fc/(c∓v_s)
  3) 通用多普勒：f' = f(c±v_o)/(c∓v_s)
  4) 马赫数：Ma = v/c
  5) 马赫角：sinθ = c/v = 1/Ma

四、声学现象（Acoustic Phenomena）
  1) 拍频：f_beat = |f₁ - f₂|
  2) 驻波波长（两端固定）：λ_n = 2L/n
  3) 驻波频率（两端固定）：f_n = nv/(2L)
  4) 管道共鸣频率（开管/闭管）
  5) 空气吸收衰减：A = α·d（α 为吸收系数）
  6) 反平方律衰减：I₂ = I₁(r₁/r₂)²

五、管道与弦振动（Pipe & String Vibration）
  1) 弦振动频率（横波）：f = (1/2L)√(T/μ)，T 为张力，μ 为线密度
  2) 弦上波速：v = √(T/μ)
  3) 开管基频：f₁ = c/(2L)
  4) 闭管基频：f₁ = c/(4L)
  5) 开管谐波：f_n = nc/(2L)（n=1,2,3...）
  6) 闭管谐波：f_n = (2n-1)c/(4L)（n=1,2,3...）

设计原则：
  - 所有函数返回纯数值（float/int）
  - 多参函数一律 _curry2/_curry3/_curry4 封装
  - 前缀 声_ / 强级_ / 多普勒_ / 现象_ / 弦管_ 区分子领域
  - 声学参考常量作为常量注册

Source: `src\domains\acoustics.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\acoustics.py:62`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\acoustics.py:68`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\acoustics.py:76`

---

## `_声_空气声速`

```matha
def _声_空气声速(T_C)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_C` | `` | |

Source: `src\domains\acoustics.py:107`

---

## `_声_波长`

```matha
def _声_波长(c, f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `c` | `` | |
| `f` | `` | |

Source: `src\domains\acoustics.py:109`

---

## `_声_频率`

```matha
def _声_频率(c, wavelength)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `c` | `` | |
| `wavelength` | `` | |

Source: `src\domains\acoustics.py:111`

---

## `_声_周期`

```matha
def _声_周期(f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |

Source: `src\domains\acoustics.py:113`

---

## `_声_角频率`

```matha
def _声_角频率(f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |

Source: `src\domains\acoustics.py:115`

---

## `_声_波数`

```matha
def _声_波数(wavelength)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |

Source: `src\domains\acoustics.py:117`

---

## `_声_介质声速`

```matha
def _声_介质声速(E, rho)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `rho` | `` | |

Source: `src\domains\acoustics.py:119`

---

## `_声_横波声速`

```matha
def _声_横波声速(G, rho)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `G` | `` | |
| `rho` | `` | |

Source: `src\domains\acoustics.py:121`

---

## `_强级_声强由声压`

```matha
def _强级_声强由声压(p, rho, c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `rho` | `` | |
| `c` | `` | |

Source: `src\domains\acoustics.py:129`

---

## `_强级_声强级`

```matha
def _强级_声强级(I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |

Source: `src\domains\acoustics.py:131`

---

## `_强级_声压级`

```matha
def _强级_声压级(p)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |

Source: `src\domains\acoustics.py:133`

---

## `_强级_声功率级`

```matha
def _强级_声功率级(W)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `W` | `` | |

Source: `src\domains\acoustics.py:135`

---

## `_强级_声强由功率`

```matha
def _强级_声强由功率(W, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `W` | `` | |
| `A` | `` | |

Source: `src\domains\acoustics.py:137`

---

## `_强级_声强由级`

```matha
def _强级_声强由级(L_I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_I` | `` | |

Source: `src\domains\acoustics.py:139`

---

## `_强级_声压由级`

```matha
def _强级_声压由级(L_p)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_p` | `` | |

Source: `src\domains\acoustics.py:141`

---

## `_强级_分贝叠加`

```matha
def _强级_分贝叠加(L_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_list` | `` | |

Source: `src\domains\acoustics.py:143`

---

## `_多普勒_观察者运动`

```matha
def _多普勒_观察者运动(f, c, v_o)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `c` | `` | |
| `v_o` | `` | |

Source: `src\domains\acoustics.py:154`

---

## `_多普勒_声源运动`

```matha
def _多普勒_声源运动(f, c, v_s)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `c` | `` | |
| `v_s` | `` | |

Source: `src\domains\acoustics.py:156`

---

## `_多普勒_通用`

```matha
def _多普勒_通用(f, c, v_o, v_s)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `c` | `` | |
| `v_o` | `` | |
| `v_s` | `` | |

Source: `src\domains\acoustics.py:158`

---

## `_多普勒_马赫数`

```matha
def _多普勒_马赫数(v, c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `c` | `` | |

Source: `src\domains\acoustics.py:160`

---

## `_多普勒_马赫角`

```matha
def _多普勒_马赫角(v, c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `c` | `` | |

Source: `src\domains\acoustics.py:162`

---

## `_现象_拍频`

```matha
def _现象_拍频(f1, f2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f1` | `` | |
| `f2` | `` | |

Source: `src\domains\acoustics.py:170`

---

## `_现象_驻波波长`

```matha
def _现象_驻波波长(L, n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `n` | `` | |

Source: `src\domains\acoustics.py:172`

---

## `_现象_驻波频率`

```matha
def _现象_驻波频率(n, v, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `v` | `` | |
| `L` | `` | |

Source: `src\domains\acoustics.py:174`

---

## `_现象_反平方衰减`

```matha
def _现象_反平方衰减(I1, r1, r2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I1` | `` | |
| `r1` | `` | |
| `r2` | `` | |

Source: `src\domains\acoustics.py:176`

---

## `_现象_吸收衰减`

```matha
def _现象_吸收衰减(I1, alpha, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I1` | `` | |
| `alpha` | `` | |
| `d` | `` | |

Source: `src\domains\acoustics.py:178`

---

## `_现象_声压衰减`

```matha
def _现象_声压衰减(p1, r1, r2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `r1` | `` | |
| `r2` | `` | |

Source: `src\domains\acoustics.py:180`

---

## `_弦管_弦上波速`

```matha
def _弦管_弦上波速(T, mu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `mu` | `` | |

Source: `src\domains\acoustics.py:188`

---

## `_弦管_弦频率`

```matha
def _弦管_弦频率(T, mu, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `mu` | `` | |
| `L` | `` | |

Source: `src\domains\acoustics.py:190`

---

## `_弦管_弦泛音`

```matha
def _弦管_弦泛音(T, mu, L, n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `mu` | `` | |
| `L` | `` | |
| `n` | `` | |

Source: `src\domains\acoustics.py:192`

---

## `_弦管_开管基频`

```matha
def _弦管_开管基频(c, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `c` | `` | |
| `L` | `` | |

Source: `src\domains\acoustics.py:194`

---

## `_弦管_闭管基频`

```matha
def _弦管_闭管基频(c, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `c` | `` | |
| `L` | `` | |

Source: `src\domains\acoustics.py:196`

---

## `_弦管_开管谐波`

```matha
def _弦管_开管谐波(c, L, n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `c` | `` | |
| `L` | `` | |
| `n` | `` | |

Source: `src\domains\acoustics.py:198`

---

## `_弦管_闭管谐波`

```matha
def _弦管_闭管谐波(c, L, n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `c` | `` | |
| `L` | `` | |
| `n` | `` | |

Source: `src\domains\acoustics.py:200`

---

## `_register_acoustics`

```matha
def _register_acoustics(builtins: dict) -> None
```

**描述**: 将声学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（em 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\acoustics.py:252`

---

## `_acoustics_symtab_names`

```matha
def _acoustics_symtab_names() -> list[str]
```

**描述**: 返回声学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\acoustics.py:319`

---

# `domains.aerospace`

```matha

```

**描述**: Matha 航空航天领域模块：轨道力学、推进系统、结构分析、热防护。

覆盖：
  1) 轨道速度计算
  2) 推进剂消耗率
  3) 结构强度系数
  4) 热防护质量
  5) 再入角估算
  6) 比冲

Source: `src\domains\aerospace.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\aerospace.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\aerospace.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\aerospace.py:25`

---

## `_轨道速度计算`

```matha
def _轨道速度计算(轨道高度_km)
```

**描述**: 圆轨道速度 v = sqrt(GM/(R+h))。

| 参数 | 类型 | 说明 |
|------|------|------|
| `轨道高度_km` | `` | |

Source: `src\domains\aerospace.py:36`

---

## `_推进剂消耗率`

```matha
def _推进剂消耗率(推力_N, 比冲_s)
```

**描述**: 推进剂质量流率 kg/s。

| 参数 | 类型 | 说明 |
|------|------|------|
| `推力_N` | `` | |
| `比冲_s` | `` | |

Source: `src\domains\aerospace.py:44`

---

## `_结构强度系数`

```matha
def _结构强度系数(材料强度_MPa, 载荷_MPa)
```

**描述**: 结构强度安全系数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `材料强度_MPa` | `` | |
| `载荷_MPa` | `` | |

Source: `src\domains\aerospace.py:51`

---

## `_热防护质量`

```matha
def _热防护质量(热流密度_W_m2, 面积_m2, 防护材料热容_J_kgK)
```

**描述**: 热防护系统质量估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `热流密度_W_m2` | `` | |
| `面积_m2` | `` | |
| `防护材料热容_J_kgK` | `` | |

Source: `src\domains\aerospace.py:58`

---

## `_再入角估算`

```matha
def _再入角估算(轨道速度_ms, 目标高度_m)
```

**描述**: 再入角估算（度）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `轨道速度_ms` | `` | |
| `目标高度_m` | `` | |

Source: `src\domains\aerospace.py:66`

---

## `_比冲`

```matha
def _比冲(推力_N, 质量流率_kg_s)
```

**描述**: 比冲 Isp = F/(ṁ·g₀)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `推力_N` | `` | |
| `质量流率_kg_s` | `` | |

Source: `src\domains\aerospace.py:74`

---

## `_register_aerospace`

```matha
def _register_aerospace(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\aerospace.py:85`

---

## `_aerospace_symtab_names`

```matha
def _aerospace_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\aerospace.py:94`

---

# `domains.ai_data_science`

```matha

```

**描述**: Matha AI 与数据科学领域模块。

覆盖：
  1) 激活函数：sigmoid, relu, softmax, tanh
  2) 损失函数：MSE, MAE, cross_entropy, log_loss
  3) 优化器：SGD, Adam
  4) 前向/反向传播
  5) 矩阵运算

Source: `src\domains\ai_data_science.py:1`

---

## `sigmoid`

```matha
def sigmoid(x: float) -> float
```

**描述**: Sigmoid 激活函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:21`

---

## `sigmoid_deriv`

```matha
def sigmoid_deriv(x: float) -> float
```

**描述**: Sigmoid 导数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:30`

---

## `relu`

```matha
def relu(x: float) -> float
```

**描述**: ReLU 激活函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:36`

---

## `relu_deriv`

```matha
def relu_deriv(x: float) -> float
```

**描述**: ReLU 导数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:41`

---

## `softmax`

```matha
def softmax(values: list[float]) -> list[float]
```

**描述**: Softmax 激活函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `values` | `list[float]` | |

**返回**: `list[float]`

Source: `src\domains\ai_data_science.py:46`

---

## `tanh`

```matha
def tanh(x: float) -> float
```

**描述**: Tanh 激活函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:54`

---

## `mse`

```matha
def mse(y_true: list[float], y_pred: list[float]) -> float
```

**描述**: 均方误差。

| 参数 | 类型 | 说明 |
|------|------|------|
| `y_true` | `list[float]` | |
| `y_pred` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:63`

---

## `mae`

```matha
def mae(y_true: list[float], y_pred: list[float]) -> float
```

**描述**: 平均绝对误差。

| 参数 | 类型 | 说明 |
|------|------|------|
| `y_true` | `list[float]` | |
| `y_pred` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:69`

---

## `cross_entropy`

```matha
def cross_entropy(y_true: list[float], y_pred: list[float]) -> float
```

**描述**: 交叉熵损失。

| 参数 | 类型 | 说明 |
|------|------|------|
| `y_true` | `list[float]` | |
| `y_pred` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:75`

---

## `log_loss`

```matha
def log_loss(y_true: list[float], y_pred: list[float]) -> float
```

**描述**: 对数损失（同交叉熵）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `y_true` | `list[float]` | |
| `y_pred` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:85`

---

## `accuracy`

```matha
def accuracy(y_true: list[float], y_pred: list[float]) -> float
```

**描述**: 准确率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `y_true` | `list[float]` | |
| `y_pred` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:90`

---

## `dot_product`

```matha
def dot_product(a: list[float], b: list[float]) -> float
```

**描述**: 点积。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `list[float]` | |
| `b` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:101`

---

## `matrix_mult`

```matha
def matrix_mult(a: list[list[float]], b: list[list[float]]) -> list[list[float]]
```

**描述**: 矩阵乘法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `list[list[float]]` | |
| `b` | `list[list[float]]` | |

**返回**: `list[list[float]]`

Source: `src\domains\ai_data_science.py:106`

---

## `transpose`

```matha
def transpose(matrix: list[list[float]]) -> list[list[float]]
```

**描述**: 矩阵转置。

| 参数 | 类型 | 说明 |
|------|------|------|
| `matrix` | `list[list[float]]` | |

**返回**: `list[list[float]]`

Source: `src\domains\ai_data_science.py:119`

---

## `matrix_add`

```matha
def matrix_add(a: list[list[float]], b: list[list[float]]) -> list[list[float]]
```

**描述**: 矩阵加法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `list[list[float]]` | |
| `b` | `list[list[float]]` | |

**返回**: `list[list[float]]`

Source: `src\domains\ai_data_science.py:125`

---

## `AdamOptimizer`

```matha
class AdamOptimizer
```

**描述**: Adam 优化器。

Source: `src\domains\ai_data_science.py:134`

---

### `__init__`

```matha
def __init__(self, lr: float, beta1: float, beta2: float, eps: float)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lr` | `float` | |
| `beta1` | `float` | |
| `beta2` | `float` | |
| `eps` | `float` | |

Source: `src\domains\ai_data_science.py:137`

---

### `step`

```matha
def step(self, params: dict, grads: dict) -> dict
```

**描述**: 更新参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params` | `dict` | |
| `grads` | `dict` | |

**返回**: `dict`

Source: `src\domains\ai_data_science.py:146`

---

## `gradient_descent`

```matha
def gradient_descent(params: dict, grads: dict, lr: float) -> dict
```

**描述**: SGD 梯度下降。

| 参数 | 类型 | 说明 |
|------|------|------|
| `params` | `dict` | |
| `grads` | `dict` | |
| `lr` | `float` | |

**返回**: `dict`

Source: `src\domains\ai_data_science.py:161`

---

## `forward_prop`

```matha
def forward_prop(layers: list[list[float]], weights: list[list[list[float]]], biases: list[list[float]]) -> list[list[float]]
```

**描述**: 前向传播。

| 参数 | 类型 | 说明 |
|------|------|------|
| `layers` | `list[list[float]]` | |
| `weights` | `list[list[list[float]]]` | |
| `biases` | `list[list[float]]` | |

**返回**: `list[list[float]]`

Source: `src\domains\ai_data_science.py:172`

---

## `backward_prop`

```matha
def backward_prop(layers: list[list[float]], weights: list[list[list[float]]], outputs: list[list[float]], y_true: list[float]) -> tuple[list[list[list[float]]], list[list[float]]]
```

**描述**: 反向传播（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `layers` | `list[list[float]]` | |
| `weights` | `list[list[list[float]]]` | |
| `outputs` | `list[list[float]]` | |
| `y_true` | `list[float]` | |

**返回**: `tuple[list[list[list[float]]], list[list[float]]]`

Source: `src\domains\ai_data_science.py:182`

---

## `mean`

```matha
def mean(values: list[float]) -> float
```

**描述**: 平均值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `values` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:217`

---

## `variance`

```matha
def variance(values: list[float]) -> float
```

**描述**: 方差。

| 参数 | 类型 | 说明 |
|------|------|------|
| `values` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:222`

---

## `std`

```matha
def std(values: list[float]) -> float
```

**描述**: 标准差。

| 参数 | 类型 | 说明 |
|------|------|------|
| `values` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:228`

---

## `correlation`

```matha
def correlation(x: list[float], y: list[float]) -> float
```

**描述**: 皮尔逊相关系数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `list[float]` | |
| `y` | `list[float]` | |

**返回**: `float`

Source: `src\domains\ai_data_science.py:233`

---

## `_register_ai_data_science`

```matha
def _register_ai_data_science(builtins: dict) -> None
```

**描述**: 注册 AI 与数据科学内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\ai_data_science.py:269`

---

## `_ai_data_science_symtab_names`

```matha
def _ai_data_science_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\ai_data_science.py:293`

---

# `domains.algo_trading`

```matha

```

**描述**: Matha 算法交易领域模块：量化策略、回测引擎、风险管控、执行算法。

覆盖：
  1) 策略夏普比率
  2) 最大回撤估算
  3) 订单执行成本
  4) 滑点估算
  5) 波动率预测
  6) 相关性矩阵

Source: `src\domains\algo_trading.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\algo_trading.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\algo_trading.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\algo_trading.py:25`

---

## `_策略夏普比率`

```matha
def _策略夏普比率(策略收益_list, 无风险利率)
```

**描述**: 策略夏普比率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `策略收益_list` | `` | |
| `无风险利率` | `` | |

Source: `src\domains\algo_trading.py:32`

---

## `_最大回撤估算`

```matha
def _最大回撤估算(净值序列)
```

**描述**: 最大回撤（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `净值序列` | `` | |

Source: `src\domains\algo_trading.py:44`

---

## `_订单执行成本`

```matha
def _订单执行成本(订单金额, 买卖价差, 市场冲击)
```

**描述**: 订单执行成本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `订单金额` | `` | |
| `买卖价差` | `` | |
| `市场冲击` | `` | |

Source: `src\domains\algo_trading.py:58`

---

## `_滑点估算`

```matha
def _滑点估算(订单量, 市场深度, 价格波动率)
```

**描述**: 滑点估算（基点）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `订单量` | `` | |
| `市场深度` | `` | |
| `价格波动率` | `` | |

Source: `src\domains\algo_trading.py:65`

---

## `_波动率预测`

```matha
def _波动率预测(历史收益率_list, 预测期)
```

**描述**: 波动率预测（年化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `历史收益率_list` | `` | |
| `预测期` | `` | |

Source: `src\domains\algo_trading.py:72`

---

## `_相关性矩阵`

```matha
def _相关性矩阵(资产收益1, 资产收益2)
```

**描述**: 计算两资产相关系数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `资产收益1` | `` | |
| `资产收益2` | `` | |

Source: `src\domains\algo_trading.py:82`

---

## `_register_algo_trading`

```matha
def _register_algo_trading(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\algo_trading.py:101`

---

## `_algo_trading_symtab_names`

```matha
def _algo_trading_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\algo_trading.py:110`

---

# `domains.anatomy`

```matha

```

**描述**: Matha 领域扩展模块：解剖学（Anatomy）。

基于 Matha 数学基础与物理/生物常量体系，演化解剖学子领域功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、系统解剖（Systemic Anatomy）- 前缀 系统_
  1) 心胸比 CTR = 心脏横径 / 胸廓横径
  2) 股骨颈干角分类（外翻/正常/内翻）
  3) 股骨前倾角分类
  4) 颈椎曲度指数（前凸深度/椎体长度）
  5) 腰椎前凸 Cobb 角正切估算
  6) 骨盆入口指数：纵径/横径
  7) 脑室指数：侧脑室宽/脑横径
  8) 主动脉根部 Z 值（简化，按 BSA）
  9) 椎管矢状径临界判定（颈/腰段）
  10) 视神经鞘直径判定（颅高压筛查）

二、局部解剖（Regional Anatomy）- 前缀 局部_
  1) 甲状腺体积（椭球公式 π/6·a·b·c）
  2) 前列腺体积（椭球公式）
  3) 肝脏体积估算（径线乘积 × 系数）
  4) 脾脏体积（椭球公式）
  5) 肾脏体积（椭球公式）
  6) 睾丸体积（椭球公式 π/6·L·W·H）
  7) 心室壁质量估测
  8) 关节腔深度估算

三、表面解剖（Surface Anatomy）- 前缀 表面_
  1) 体表面积 Mosteller 公式 sqrt(H·W/3600)
  2) 体表面积 DuBois 公式 0.007184·W^0.425·H^0.725
  3) 烧伤九分法面积（按部位）
  4) 椎体节段定位（C2-T12 比例）
  5) 经皮进针深度估算
  6) 体表标志间距（解剖坐标）

四、影像解剖（Imaging Anatomy）- 前缀 影解_
  1) CT 椎管矢状径（按节段查参考值）
  2) 椎弓根间距参考值
  3) 脊髓圆锥位置判定
  4) 脑沟宽度参考值（按年龄）
  5) 侧脑室前角宽度参考值
  6) 垂体高度判定
  7) Evans 指数（侧脑室前角/颅横径）
  8) 肺结节体积倍增时间

五、临床解剖（Clinical Anatomy）- 前缀 临解_
  1) 心脏质量预测（按体重）
  2) 肝脏质量预测（按体重）
  3) 脾脏质量预测（按体重）
  4) 肾脏质量预测（按体重）
  5) 脑质量预测（按体重）
  6) 血容量估算（按体重与性别）
  7) 肺总量估算（按身高与性别）
  8) 脏器质量/体重比

数据库：
  - 人体骨骼分类计数表（206 块分类）
  - 主要脏器成人平均质量与比重（g 与 g/cm³）
  - 血管典型参数（主动脉/肺动脉/冠状动脉/颈动脉/股动脉）
  - 椎体节段参考数据（颈/胸/腰椎矢状径、椎弓根距）
  - 心脏腔室正常参考值

设计原则：
  - 与 medical / medtools / biology 保持一致：_curryN 柯里化、前缀区分
  - 函数返回纯数值或字符串/元组，不做语义包装
  - 除零 / 非法参数由 Python 自身抛错

Source: `src\domains\anatomy.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\anatomy.py:77`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\anatomy.py:83`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\anatomy.py:91`

---

## `_系统_心胸比`

```matha
def _系统_心胸比(cardiac_TD_mm, thoracic_TD_mm)
```

**描述**: 心胸比 CTR = 心脏横径 / 胸廓横径。>0.5 提示心脏增大。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cardiac_TD_mm` | `` | |
| `thoracic_TD_mm` | `` | |

Source: `src\domains\anatomy.py:189`

---

## `_系统_股骨颈干角分类`

```matha
def _系统_股骨颈干角分类(angle_deg)
```

**描述**: 股骨颈干角分类：<110 内翻，110-140 正常，>140 外翻。

| 参数 | 类型 | 说明 |
|------|------|------|
| `angle_deg` | `` | |

Source: `src\domains\anatomy.py:196`

---

## `_系统_股骨前倾角分类`

```matha
def _系统_股骨前倾角分类(angle_deg)
```

**描述**: 股骨前倾角分类（成人典型 12°）：<5 后倾，5-25 正常，>25 异常前倾。

| 参数 | 类型 | 说明 |
|------|------|------|
| `angle_deg` | `` | |

Source: `src\domains\anatomy.py:205`

---

## `_系统_颈椎曲度指数`

```matha
def _系统_颈椎曲度指数(posterior_depth_mm, vertebral_length_mm)
```

**描述**: 颈椎曲度指数 = 后缘深度 / 椎体长度。0.1-0.3 正常，<0.1 变直。

| 参数 | 类型 | 说明 |
|------|------|------|
| `posterior_depth_mm` | `` | |
| `vertebral_length_mm` | `` | |

Source: `src\domains\anatomy.py:214`

---

## `_系统_腰椎Cobb角正切`

```matha
def _系统_腰椎Cobb角正切(delta_y_mm, delta_x_mm)
```

**描述**: 由 Cobb 角端点垂足差估算正切角 = atan(Δy/Δx)（度）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `delta_y_mm` | `` | |
| `delta_x_mm` | `` | |

Source: `src\domains\anatomy.py:221`

---

## `_系统_骨盆入口指数`

```matha
def _系统_骨盆入口指数(AP_diameter_mm, transverse_diameter_mm)
```

**描述**: 骨盆入口指数 = 入口前后径 / 横径。男<1 女>1（产科）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `AP_diameter_mm` | `` | |
| `transverse_diameter_mm` | `` | |

Source: `src\domains\anatomy.py:228`

---

## `_系统_脑室指数`

```matha
def _系统_脑室指数(ventricle_width_mm, brain_width_mm)
```

**描述**: 脑室指数 = 侧脑室宽度 / 脑横径。>0.5 提示脑积水。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ventricle_width_mm` | `` | |
| `brain_width_mm` | `` | |

Source: `src\domains\anatomy.py:235`

---

## `_系统_主动脉Z值`

```matha
def _系统_主动脉Z值(aortic_root_mm, BSA)
```

**描述**: 主动脉根部 Z 值简化估算（基于 BSA 标化，非临床精确公式）：
预期 D = 14.4 + BSA*9.5，Z = (实测 - 预期) / 1.5。

| 参数 | 类型 | 说明 |
|------|------|------|
| `aortic_root_mm` | `` | |
| `BSA` | `` | |

Source: `src\domains\anatomy.py:242`

---

## `_系统_椎管矢状径判定`

```matha
def _系统_椎管矢状径判定(level, sagittal_mm)
```

**描述**: 按节段判定椎管矢状径是否狭窄（颈<13 / 腰<15 提示狭窄），返回 (是否狭窄, 参考值)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `level` | `` | |
| `sagittal_mm` | `` | |

Source: `src\domains\anatomy.py:249`

---

## `_系统_视神经鞘判定`

```matha
def _系统_视神经鞘判定(ONSD_mm)
```

**描述**: 视神经鞘直径判定：>5.4mm 提示颅内压升高。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ONSD_mm` | `` | |

Source: `src\domains\anatomy.py:256`

---

## `_局部_甲状腺体积`

```matha
def _局部_甲状腺体积(a_mm, b_mm, c_mm)
```

**描述**: 甲状腺体积（mL）= π/6 · a · b · c（椭球公式，mm³ → mL）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a_mm` | `` | |
| `b_mm` | `` | |
| `c_mm` | `` | |

Source: `src\domains\anatomy.py:265`

---

## `_局部_前列腺体积`

```matha
def _局部_前列腺体积(a_mm, b_mm, c_mm)
```

**描述**: 前列腺体积（mL）= π/6 · a · b · c。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a_mm` | `` | |
| `b_mm` | `` | |
| `c_mm` | `` | |

Source: `src\domains\anatomy.py:270`

---

## `_局部_肝脏体积`

```matha
def _局部_肝脏体积(L_mm, W_mm, H_mm, k)
```

**描述**: 肝脏体积（mL）= k · L · W · H / 1000，k ≈ 0.55。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_mm` | `` | |
| `W_mm` | `` | |
| `H_mm` | `` | |
| `k` | `` | |

Source: `src\domains\anatomy.py:275`

---

## `_局部_脾脏体积`

```matha
def _局部_脾脏体积(a_mm, b_mm, c_mm)
```

**描述**: 脾脏体积（mL）= π/6 · a · b · c。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a_mm` | `` | |
| `b_mm` | `` | |
| `c_mm` | `` | |

Source: `src\domains\anatomy.py:280`

---

## `_局部_肾脏体积`

```matha
def _局部_肾脏体积(L_mm, W_mm, H_mm)
```

**描述**: 肾脏体积（mL）= π/6 · L · W · H。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_mm` | `` | |
| `W_mm` | `` | |
| `H_mm` | `` | |

Source: `src\domains\anatomy.py:285`

---

## `_局部_睾丸体积`

```matha
def _局部_睾丸体积(L_mm, W_mm, H_mm)
```

**描述**: 睾丸体积（mL）= π/6 · L · W · H。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_mm` | `` | |
| `W_mm` | `` | |
| `H_mm` | `` | |

Source: `src\domains\anatomy.py:290`

---

## `_局部_左室质量`

```matha
def _局部_左室质量(IVSd_mm, LVDd_mm, LVPWd_mm)
```

**描述**: 左室质量估测（Devereux 校正版）：LVM(g) = 0.8·[1.04·(IVSd+LVDd+LVPWd)³ - LVDd³] + 0.6。

| 参数 | 类型 | 说明 |
|------|------|------|
| `IVSd_mm` | `` | |
| `LVDd_mm` | `` | |
| `LVPWd_mm` | `` | |

Source: `src\domains\anatomy.py:295`

---

## `_局部_关节腔深度`

```matha
def _局部_关节腔深度(depth_to_bone_mm, needle_advance_mm)
```

**描述**: 关节腔深度估算 = depth_to_bone - safety_margin（安全余量）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `depth_to_bone_mm` | `` | |
| `needle_advance_mm` | `` | |

Source: `src\domains\anatomy.py:302`

---

## `_表面_体表面积Mosteller`

```matha
def _表面_体表面积Mosteller(height_cm, weight_kg)
```

**描述**: Mosteller 公式：BSA(m²) = sqrt(H·W/3600)，H=cm, W=kg。

| 参数 | 类型 | 说明 |
|------|------|------|
| `height_cm` | `` | |
| `weight_kg` | `` | |

Source: `src\domains\anatomy.py:309`

---

## `_表面_体表面积DuBois`

```matha
def _表面_体表面积DuBois(height_cm, weight_kg)
```

**描述**: DuBois 公式：BSA(m²) = 0.007184·W^0.425·H^0.725。

| 参数 | 类型 | 说明 |
|------|------|------|
| `height_cm` | `` | |
| `weight_kg` | `` | |

Source: `src\domains\anatomy.py:314`

---

## `_表面_烧伤九分法`

```matha
def _表面_烧伤九分法(body_part)
```

**描述**: 成人烧伤九分法（按部位查体表面积百分比）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_part` | `` | |

Source: `src\domains\anatomy.py:319`

---

## `_表面_椎体节段定位`

```matha
def _表面_椎体节段定位(distance_from_C2_mm, segment_length_mm)
```

**描述**: 椎体节段定位 = 距 C2 距离 / 平均节段长（向下取整）→ 节段序号。

| 参数 | 类型 | 说明 |
|------|------|------|
| `distance_from_C2_mm` | `` | |
| `segment_length_mm` | `` | |

Source: `src\domains\anatomy.py:334`

---

## `_表面_经皮进针深度`

```matha
def _表面_经皮进针深度(depth_to_target_mm, compression_mm)
```

**描述**: 经皮进针深度 = 目标深度 + 压缩补偿。

| 参数 | 类型 | 说明 |
|------|------|------|
| `depth_to_target_mm` | `` | |
| `compression_mm` | `` | |

Source: `src\domains\anatomy.py:341`

---

## `_表面_体表标志间距`

```matha
def _表面_体表标志间距(landmark1, landmark2)
```

**描述**: 按标志对查标准间距（cm）。未收录返回 0.0。

| 参数 | 类型 | 说明 |
|------|------|------|
| `landmark1` | `` | |
| `landmark2` | `` | |

Source: `src\domains\anatomy.py:346`

---

## `_影解_椎管矢状径`

```matha
def _影解_椎管矢状径(level)
```

**描述**: 按椎体节段查椎管矢状径参考值（mm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `level` | `` | |

Source: `src\domains\anatomy.py:361`

---

## `_影解_椎弓根间距`

```matha
def _影解_椎弓根间距(region)
```

**描述**: 按脊柱区段查椎弓根间距参考最大值（mm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `region` | `` | |

Source: `src\domains\anatomy.py:366`

---

## `_影解_脊髓圆锥位置`

```matha
def _影解_脊髓圆锥位置(vertebral_level)
```

**描述**: 脊髓圆锥位置判定：L1-L2 正常，L3 以下提示低位。

| 参数 | 类型 | 说明 |
|------|------|------|
| `vertebral_level` | `` | |

Source: `src\domains\anatomy.py:371`

---

## `_影解_脑沟宽度`

```matha
def _影解_脑沟宽度(age_years, sulcus_mm)
```

**描述**: 按年龄判定脑沟宽度：青壮年<5mm 正常；老年>6mm 异常。

| 参数 | 类型 | 说明 |
|------|------|------|
| `age_years` | `` | |
| `sulcus_mm` | `` | |

Source: `src\domains\anatomy.py:381`

---

## `_影解_侧脑室前角宽度`

```matha
def _影解_侧脑室前角宽度(age_years)
```

**描述**: 按年龄返回侧脑室前角宽度参考上限（mm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `age_years` | `` | |

Source: `src\domains\anatomy.py:388`

---

## `_影解_垂体高度判定`

```matha
def _影解_垂体高度判定(height_mm)
```

**描述**: 垂体高度判定：>10mm 提示垂体增大。

| 参数 | 类型 | 说明 |
|------|------|------|
| `height_mm` | `` | |

Source: `src\domains\anatomy.py:395`

---

## `_影解_Evans指数`

```matha
def _影解_Evans指数(frontal_horn_mm, cranial_width_mm)
```

**描述**: Evans 指数 = 侧脑室前角宽/颅最大内径。>0.3 提示脑积水。

| 参数 | 类型 | 说明 |
|------|------|------|
| `frontal_horn_mm` | `` | |
| `cranial_width_mm` | `` | |

Source: `src\domains\anatomy.py:402`

---

## `_影解_肺结节倍增时间`

```matha
def _影解_肺结节倍增时间(D0_mm, D1_mm, days)
```

**描述**: 肺结节体积倍增时间（天）：V = π/6·D³；DT = ln2·Δt / ln(V1/V0)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `D0_mm` | `` | |
| `D1_mm` | `` | |
| `days` | `` | |

Source: `src\domains\anatomy.py:409`

---

## `_临解_心脏质量预测`

```matha
def _临解_心脏质量预测(body_weight_kg)
```

**描述**: 心脏质量预测 = body_weight × 0.0045（g）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_weight_kg` | `` | |

Source: `src\domains\anatomy.py:420`

---

## `_临解_肝脏质量预测`

```matha
def _临解_肝脏质量预测(body_weight_kg)
```

**描述**: 肝脏质量预测 = body_weight × 0.0225（g）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_weight_kg` | `` | |

Source: `src\domains\anatomy.py:425`

---

## `_临解_脾脏质量预测`

```matha
def _临解_脾脏质量预测(body_weight_kg)
```

**描述**: 脾脏质量预测 = body_weight × 0.0023（g）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_weight_kg` | `` | |

Source: `src\domains\anatomy.py:430`

---

## `_临解_肾脏质量预测`

```matha
def _临解_肾脏质量预测(body_weight_kg)
```

**描述**: 双肾质量预测 = body_weight × 0.0021 × 2（g，含双侧）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_weight_kg` | `` | |

Source: `src\domains\anatomy.py:435`

---

## `_临解_脑质量预测`

```matha
def _临解_脑质量预测(body_weight_kg)
```

**描述**: 脑质量预测 = body_weight × 0.021（g）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_weight_kg` | `` | |

Source: `src\domains\anatomy.py:440`

---

## `_临解_血容量`

```matha
def _临解_血容量(body_weight_kg, is_male)
```

**描述**: 血容量估算（mL）= body_weight × (70 if 男 else 65)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_weight_kg` | `` | |
| `is_male` | `` | |

Source: `src\domains\anatomy.py:445`

---

## `_临解_肺总量预测`

```matha
def _临解_肺总量预测(height_cm, is_male)
```

**描述**: 肺总量估算（mL）：男 50·H - 4500；女 45·H - 4000（H=cm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `height_cm` | `` | |
| `is_male` | `` | |

Source: `src\domains\anatomy.py:451`

---

## `_临解_脏器体重比`

```matha
def _临解_脏器体重比(organ_mass_g, body_weight_kg)
```

**描述**: 脏器质量/体重比 = organ_mass / (body_weight × 1000)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `organ_mass_g` | `` | |
| `body_weight_kg` | `` | |

Source: `src\domains\anatomy.py:458`

---

## `_register_anatomy`

```matha
def _register_anatomy(builtins: dict) -> None
```

**描述**: 将解剖学子领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\anatomy.py:467`

---

## `_anatomy_symtab_names`

```matha
def _anatomy_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\anatomy.py:561`

---

# `domains.architecture`

```matha

```

**描述**: Matha 领域扩展模块：建筑（Architecture）。

基于 Matha 数学基础与物理常量体系，演化建筑学子领域功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、建筑物理（Building Physics）- 前缀 建物_
  1) 热阻 R = d/λ（d 厚度 m，λ 导热系数 W/(m·K)）
  2) 传热系数 U = 1/ΣR（围护结构总热阻的倒数）
  3) 传热量 Φ = U·A·ΔT（W）
  4) 太阳辐射得热 SHGC = Q_solar / Q_incident
  5) 质量定律隔声 TL = 20·log₁₀(m) - 47（m 面密度 kg/m²）
  6) Sabine 混响时间 T = 0.161·V/A（V 房间体积 m³，A 总吸声量）
  7) 吸声量 A = Σ(α_i · S_i)
  8) 空气渗透换气次数 ACH = Q / V
  9) 采光系数 DF = (E_indoor / E_outdoor) × 100%
  10) 露点温度判定（基于室内温湿度）

二、建筑材料（Building Materials）- 前缀 建材_
  1) 混凝土水灰比 w/c = f'c 反算（Abrams 公式简化）
  2) 配筋率 ρ = As / (b·d)
  3) 砂浆配合比体积法
  4) 砖砌体抗压强度（按砖与砂浆等级查 GB 50003 表）
  5) 木材强度等级分类（按抗弯强度 f_m）
  6) 钢材屈服强度判定
  7) 混凝土配合比材料用量
  8) 砌体材料用量估算

三、建筑设计（Architectural Design）- 前缀 建设_
  1) 黄金比例 φ = (1+√5)/2
  2) 容积率 FAR = 总建筑面积 / 用地面积
  3) 建筑密度 = 建筑基底面积 / 用地面积
  4) 绿地率 = 绿地面积 / 用地面积
  5) 人均使用面积
  6) 模数协调：基本模数 1M = 100mm
  7) 建筑高度限值（按航空/日照）
  8) 日照间距系数

四、建筑施工（Construction）- 前缀 建施_
  1) 土方量（棱柱法）：V = L·(A1 + A2) / 2
  2) 混凝土体积：V = L·b·h
  3) 钢筋下料长度（含弯钩）
  4) 模板面积：A = 2·(L+h)·h
  5) 脚手架立杆承载力
  6) 塔吊起重量（按幅度查力矩）
  7) 开挖放坡系数 K = h/b
  8) 抹灰面积

五、建筑规范（Building Codes）- 前缀 建规_
  1) 疏散宽度 W = N × 宽度指标 / 100
  2) 最小楼梯宽度（按建筑类型）
  3) 通风换气量 Q = n × V_room
  4) 卫生器具数量（按使用人数）
  5) 停车位配建数（按建筑面积）
  6) 疏散距离限值
  7) 防火分区面积限值
  8) 自然采光最小窗地比

数据库：
  - 常用建筑材料导热系数 λ（混凝土/砖/木材/保温/玻璃/钢材）
  - 常用材料面密度（砌体墙/混凝土板/玻璃幕墙）
  - 常用吸声系数（混凝土/木地板/玻璃/窗帘/吸声板）
  - 砖与砂浆强度等级表
  - 木材强度等级表
  - 防火分区最大允许建筑面积（按耐火等级）

设计原则：
  - 与 anatomy / medical / medtools / structural / acoustics 保持一致：_curryN 柯里化、前缀区分
  - 函数返回纯数值或字符串，不做语义包装
  - 除零 / 非法参数由 Python 自身抛错

Source: `src\domains\architecture.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\architecture.py:80`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\architecture.py:86`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\architecture.py:94`

---

## `_建物_热阻`

```matha
def _建物_热阻(d_m, lambda_val)
```

**描述**: 材料热阻 R = d/λ（m²·K/W）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `d_m` | `` | |
| `lambda_val` | `` | |

Source: `src\domains\architecture.py:197`

---

## `_建物_传热系数`

```matha
def _建物_传热系数(R_total)
```

**描述**: 传热系数 U = 1/ΣR（W/(m²·K)）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R_total` | `` | |

Source: `src\domains\architecture.py:204`

---

## `_建物_传热量`

```matha
def _建物_传热量(U, area_m2, delta_T)
```

**描述**: 传热量 Φ = U·A·ΔT（W）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `U` | `` | |
| `area_m2` | `` | |
| `delta_T` | `` | |

Source: `src\domains\architecture.py:211`

---

## `_建物_太阳得热系数`

```matha
def _建物_太阳得热系数(Q_solar, Q_incident)
```

**描述**: SHGC = Q_solar / Q_incident。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q_solar` | `` | |
| `Q_incident` | `` | |

Source: `src\domains\architecture.py:216`

---

## `_建物_质量定律隔声`

```matha
def _建物_质量定律隔声(surface_density_kg_m2)
```

**描述**: 质量定律隔声 TL = 20·log₁₀(m) - 47（dB，m 面密度 kg/m²）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `surface_density_kg_m2` | `` | |

Source: `src\domains\architecture.py:223`

---

## `_建物_Sabine混响时间`

```matha
def _建物_Sabine混响时间(V_m3, A_total)
```

**描述**: Sabine 公式 T = 0.161·V/A（s）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V_m3` | `` | |
| `A_total` | `` | |

Source: `src\domains\architecture.py:230`

---

## `_建物_总吸声量`

```matha
def _建物_总吸声量(areas_m2, coeffs)
```

**描述**: 吸声量 A = Σ(α_i · S_i)。areas 与 coeffs 等长列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `areas_m2` | `` | |
| `coeffs` | `` | |

Source: `src\domains\architecture.py:237`

---

## `_建物_换气次数`

```matha
def _建物_换气次数(Q_m3h, V_room_m3)
```

**描述**: 空气渗透换气次数 ACH = Q/V（次/h）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q_m3h` | `` | |
| `V_room_m3` | `` | |

Source: `src\domains\architecture.py:244`

---

## `_建物_采光系数`

```matha
def _建物_采光系数(E_indoor, E_outdoor)
```

**描述**: 采光系数 DF = E_indoor/E_outdoor × 100（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `E_indoor` | `` | |
| `E_outdoor` | `` | |

Source: `src\domains\architecture.py:251`

---

## `_建物_露点判定`

```matha
def _建物_露点判定(T_air_C, RH)
```

**描述**: 按室内温湿度估算露点温度（℃）并判定是否结露。
简化：Td = T - (100 - RH)/5（Lawrence 近似）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_air_C` | `` | |
| `RH` | `` | |

Source: `src\domains\architecture.py:258`

---

## `_建材_水灰比反算`

```matha
def _建材_水灰比反算(fc_MPa, k, n)
```

**描述**: Abrams 公式简化反算：f'c = A / (w/c)^n，给定 f'c 求 w/c。
默认 A=22, n=0.61。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fc_MPa` | `` | |
| `k` | `` | |
| `n` | `` | |

Source: `src\domains\architecture.py:268`

---

## `_建材_配筋率`

```matha
def _建材_配筋率(As_mm2, b_mm, d_mm)
```

**描述**: 配筋率 ρ = As / (b·d)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `As_mm2` | `` | |
| `b_mm` | `` | |
| `d_mm` | `` | |

Source: `src\domains\architecture.py:276`

---

## `_建材_砂浆体积比`

```matha
def _建材_砂浆体积比(cement, sand, water)
```

**描述**: 砂浆体积配合比归一化：返回 (c/Σ, s/Σ, w/Σ)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cement` | `` | |
| `sand` | `` | |
| `water` | `` | |

Source: `src\domains\architecture.py:283`

---

## `_建材_砖砌体抗压强度`

```matha
def _建材_砖砌体抗压强度(brick_grade, mortar_grade)
```

**描述**: 按砖与砂浆等级（MU/M 后数值）查砌体抗压强度设计值（MPa）。
简化公式：f = 0.78·√(砖·砂浆)/4.5（GB 50003 范围内拟合）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `brick_grade` | `` | |
| `mortar_grade` | `` | |

Source: `src\domains\architecture.py:291`

---

## `_建材_木材强度分级`

```matha
def _建材_木材强度分级(f_m_MPa)
```

**描述**: 按抗弯强度判定木材等级：TC17/TC15/TC13/TC11 或不合格。

| 参数 | 类型 | 说明 |
|------|------|------|
| `f_m_MPa` | `` | |

Source: `src\domains\architecture.py:299`

---

## `_建材_钢材屈服判定`

```matha
def _建材_钢材屈服判定(fy_MPa)
```

**描述**: 按屈服强度判定钢材级别（HPB300/HRB400/HRB500）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fy_MPa` | `` | |

Source: `src\domains\architecture.py:312`

---

## `_建材_混凝土材料用量`

```matha
def _建材_混凝土材料用量(V_m3, water_kg, w_c, w_s)
```

**描述**: 按水灰比 w/c 与砂率 w_s 估算 V_m3 m³ 混凝土各材料总用量（kg）。
water_kg 为每 m³ 用水量；返回 (水泥, 水, 砂, 石)。
水泥 = 总用水/(w/c)；骨料总 = 总质量 - 水泥 - 总用水。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V_m3` | `` | |
| `water_kg` | `` | |
| `w_c` | `` | |
| `w_s` | `` | |

Source: `src\domains\architecture.py:323`

---

## `_建材_砌体材料用量`

```matha
def _建材_砌体材料用量(wall_area_m2, thickness_m, mortar_ratio)
```

**描述**: 估算砌体材料用量（按面积/厚度）：返回 (砌块体积, 砂浆体积) m³。

| 参数 | 类型 | 说明 |
|------|------|------|
| `wall_area_m2` | `` | |
| `thickness_m` | `` | |
| `mortar_ratio` | `` | |

Source: `src\domains\architecture.py:338`

---

## `_建设_黄金比例`

```matha
def _建设_黄金比例()
```

**描述**: 黄金比例 φ = (1+√5)/2 ≈ 1.6180339887。

Source: `src\domains\architecture.py:348`

---

## `_建设_容积率`

```matha
def _建设_容积率(GFA_m2, site_area_m2)
```

**描述**: 容积率 FAR = 总建筑面积 / 用地面积。

| 参数 | 类型 | 说明 |
|------|------|------|
| `GFA_m2` | `` | |
| `site_area_m2` | `` | |

Source: `src\domains\architecture.py:353`

---

## `_建设_建筑密度`

```matha
def _建设_建筑密度(footprint_m2, site_area_m2)
```

**描述**: 建筑密度 = 建筑基底面积 / 用地面积（0~1）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `footprint_m2` | `` | |
| `site_area_m2` | `` | |

Source: `src\domains\architecture.py:360`

---

## `_建设_绿地率`

```matha
def _建设_绿地率(green_m2, site_area_m2)
```

**描述**: 绿地率 = 绿地面积 / 用地面积（0~1）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `green_m2` | `` | |
| `site_area_m2` | `` | |

Source: `src\domains\architecture.py:367`

---

## `_建设_人均面积`

```matha
def _建设_人均面积(total_area_m2, occupants)
```

**描述**: 人均使用面积 = 总面积 / 人数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `total_area_m2` | `` | |
| `occupants` | `` | |

Source: `src\domains\architecture.py:374`

---

## `_建设_模数协调`

```matha
def _建设_模数协调(base_module_count)
```

**描述**: 按模数倍数返回尺寸：n × 1M = n × 100mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `base_module_count` | `` | |

Source: `src\domains\architecture.py:381`

---

## `_建设_建筑高度限值`

```matha
def _建设_建筑高度限值(aircraft_clear_m, solar_extra_m)
```

**描述**: 建筑高度限值 = 净空 - 余量（航空限高/日照叠加）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `aircraft_clear_m` | `` | |
| `solar_extra_m` | `` | |

Source: `src\domains\architecture.py:386`

---

## `_建设_日照间距系数`

```matha
def _建设_日照间距系数(building_height_m, sun_altitude_deg)
```

**描述**: 日照间距系数 = H / tan(α)（α 太阳高度角）。
返回 L = H/tan(α)（日照间距）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `building_height_m` | `` | |
| `sun_altitude_deg` | `` | |

Source: `src\domains\architecture.py:391`

---

## `_建施_土方量棱柱法`

```matha
def _建施_土方量棱柱法(L_m, A1_m2, A2_m2)
```

**描述**: 土方量棱柱法：V = L·(A1 + A2) / 2。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_m` | `` | |
| `A1_m2` | `` | |
| `A2_m2` | `` | |

Source: `src\domains\architecture.py:401`

---

## `_建施_混凝土体积`

```matha
def _建施_混凝土体积(L_m, b_m, h_m)
```

**描述**: 混凝土体积 V = L·b·h（m³）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_m` | `` | |
| `b_m` | `` | |
| `h_m` | `` | |

Source: `src\domains\architecture.py:406`

---

## `_建施_钢筋下料长度`

```matha
def _建施_钢筋下料长度(L_design_mm, hook_count, d_mm)
```

**描述**: 钢筋下料长度 = 设计长 + 弯钩增加（每个 10d）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_design_mm` | `` | |
| `hook_count` | `` | |
| `d_mm` | `` | |

Source: `src\domains\architecture.py:411`

---

## `_建施_模板面积`

```matha
def _建施_模板面积(L_m, h_m)
```

**描述**: 矩形截面模板面积：A = 2·(L + h)·h（侧模+底模近似）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_m` | `` | |
| `h_m` | `` | |

Source: `src\domains\architecture.py:416`

---

## `_建施_脚手架立杆承载力`

```matha
def _建施_脚手架立杆承载力(A_mm2, f_MPa, safety_factor)
```

**描述**: 单根立杆承载力 N = A·f / K（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A_mm2` | `` | |
| `f_MPa` | `` | |
| `safety_factor` | `` | |

Source: `src\domains\architecture.py:421`

---

## `_建施_塔吊起重量`

```matha
def _建施_塔吊起重量(moment_kNm, radius_m)
```

**描述**: 塔吊起重量 Q = M / R（kN，力矩 M / 幅度 R）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `moment_kNm` | `` | |
| `radius_m` | `` | |

Source: `src\domains\architecture.py:426`

---

## `_建施_放坡系数`

```matha
def _建施_放坡系数(h_m, b_m)
```

**描述**: 放坡系数 K = h / b（深度/放坡宽度）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `h_m` | `` | |
| `b_m` | `` | |

Source: `src\domains\architecture.py:433`

---

## `_建施_抹灰面积`

```matha
def _建施_抹灰面积(L_m, H_m, opening_m2)
```

**描述**: 墙面抹灰面积 = L·H - 洞口（m²）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L_m` | `` | |
| `H_m` | `` | |
| `opening_m2` | `` | |

Source: `src\domains\architecture.py:440`

---

## `_建规_疏散宽度`

```matha
def _建规_疏散宽度(occupants, width_index_m_per_100)
```

**描述**: 疏散总宽度 W = N × 宽度指标 / 100（m）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `occupants` | `` | |
| `width_index_m_per_100` | `` | |

Source: `src\domains\architecture.py:447`

---

## `_建规_最小楼梯宽度`

```matha
def _建规_最小楼梯宽度(building_type)
```

**描述**: 按建筑类型返回最小疏散楼梯宽度（m）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `building_type` | `` | |

Source: `src\domains\architecture.py:452`

---

## `_建规_通风换气量`

```matha
def _建规_通风换气量(ACH, V_room_m3)
```

**描述**: 通风换气量 Q = ACH × V（m³/h）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ACH` | `` | |
| `V_room_m3` | `` | |

Source: `src\domains\architecture.py:465`

---

## `_建规_卫生器具数`

```matha
def _建规_卫生器具数(occupants, fixture_index)
```

**描述**: 卫生器具数 = 使用人数 × 配建指标（每 20 人 1 个 → 0.05）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `occupants` | `` | |
| `fixture_index` | `` | |

Source: `src\domains\architecture.py:470`

---

## `_建规_停车位配建`

```matha
def _建规_停车位配建(GFA_m2, parking_index_per_100m2)
```

**描述**: 停车位配建数 = 总建筑面积 × 指标 / 100（取整）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `GFA_m2` | `` | |
| `parking_index_per_100m2` | `` | |

Source: `src\domains\architecture.py:475`

---

## `_建规_疏散距离限值`

```matha
def _建规_疏散距离限值(building_type, sprinklered)
```

**描述**: 疏散距离限值（m，按建筑类型与是否设喷淋）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `building_type` | `` | |
| `sprinklered` | `` | |

Source: `src\domains\architecture.py:480`

---

## `_建规_防火分区面积`

```matha
def _建规_防火分区面积(building_type)
```

**描述**: 防火分区最大允许建筑面积（m²，一级耐火）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `building_type` | `` | |

Source: `src\domains\architecture.py:491`

---

## `_建规_窗地比最小`

```matha
def _建规_窗地比最小(room_type)
```

**描述**: 按房间类型返回最小窗地比（窗洞面积/房间面积）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `room_type` | `` | |

Source: `src\domains\architecture.py:496`

---

## `_register_architecture`

```matha
def _register_architecture(builtins: dict) -> None
```

**描述**: 将建筑学子领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\architecture.py:511`

---

## `_architecture_symtab_names`

```matha
def _architecture_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\architecture.py:605`

---

# `domains.audio_video`

```matha

```

**描述**: Matha 音视频处理领域模块：音频采样、视频编码、流媒体、编解码。

覆盖：
  1) 音频采样率转换
  2) 视频码率估算
  3) 流媒体延迟
  4) 编解码压缩比
  5) 音频信噪比
  6) 视频帧率

Source: `src\domains\audio_video.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\audio_video.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\audio_video.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\audio_video.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\audio_video.py:31`

---

## `_音频采样率转换`

```matha
def _音频采样率转换(源采样率, 目标采样率, 声道数)
```

**描述**: 采样率转换后数据量（字节/秒）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `源采样率` | `` | |
| `目标采样率` | `` | |
| `声道数` | `` | |

Source: `src\domains\audio_video.py:40`

---

## `_视频码率估算`

```matha
def _视频码率估算(分辨率_w, 分辨率_h, 帧率, 压缩率)
```

**描述**: 视频码率估算（Mbps）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `分辨率_w` | `` | |
| `分辨率_h` | `` | |
| `帧率` | `` | |
| `压缩率` | `` | |

Source: `src\domains\audio_video.py:47`

---

## `_流媒体延迟`

```matha
def _流媒体延迟(缓冲秒数, 网络延迟_ms, 编码延迟_ms)
```

**描述**: 流媒体总延迟。

| 参数 | 类型 | 说明 |
|------|------|------|
| `缓冲秒数` | `` | |
| `网络延迟_ms` | `` | |
| `编码延迟_ms` | `` | |

Source: `src\domains\audio_video.py:55`

---

## `_编解码压缩比`

```matha
def _编解码压缩比(原始大小_MB, 压缩后大小_MB)
```

**描述**: 压缩比。

| 参数 | 类型 | 说明 |
|------|------|------|
| `原始大小_MB` | `` | |
| `压缩后大小_MB` | `` | |

Source: `src\domains\audio_video.py:60`

---

## `_音频信噪比`

```matha
def _音频信噪比(信号功率, 噪声功率)
```

**描述**: 音频信噪比（dB）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `信号功率` | `` | |
| `噪声功率` | `` | |

Source: `src\domains\audio_video.py:67`

---

## `_视频帧率稳定性`

```matha
def _视频帧率稳定性(目标帧率, 实际帧率_list)
```

**描述**: 帧率稳定性指数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `目标帧率` | `` | |
| `实际帧率_list` | `` | |

Source: `src\domains\audio_video.py:74`

---

## `_register_audio_video`

```matha
def _register_audio_video(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\audio_video.py:86`

---

## `_audio_video_symtab_names`

```matha
def _audio_video_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\audio_video.py:95`

---

# `domains.automation`

```matha

```

**描述**: Matha 自动化领域模块：PLC控制、传感器采集、执行器驱动、时序控制。

覆盖：
  1) PLC扫描周期估算
  2) 传感器采样率与精度
  3) 执行器响应时间
  4) 时序约束满足分析
  5) 自动化流程执行效率
  6) 异常检测率估算

Source: `src\domains\automation.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\automation.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\automation.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\automation.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\automation.py:31`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\automation.py:39`

---

## `_PLC扫描周期估算`

```matha
def _PLC扫描周期估算(iO点数, 扫描指令数, 指令周期_ns)
```

**描述**: PLC扫描周期 T = I/O耗时 + 指令执行耗时。

| 参数 | 类型 | 说明 |
|------|------|------|
| `iO点数` | `` | |
| `扫描指令数` | `` | |
| `指令周期_ns` | `` | |

Source: `src\domains\automation.py:54`

---

## `_传感器采样率`

```matha
def _传感器采样率(噪声密度, 带宽, 分辨率_bit)
```

**描述**: 根据噪声和带宽估算有效采样率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `噪声密度` | `` | |
| `带宽` | `` | |
| `分辨率_bit` | `` | |

Source: `src\domains\automation.py:61`

---

## `_执行器响应时间`

```matha
def _执行器响应时间(类型, 负载_kg, 行程_mm)
```

**描述**: 执行器响应时间估算（ms）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `类型` | `` | |
| `负载_kg` | `` | |
| `行程_mm` | `` | |

Source: `src\domains\automation.py:69`

---

## `_时序约束满足`

```matha
def _时序约束满足(周期_ms, 执行时间_ms, 开销_ms)
```

**描述**: 判断时序约束是否满足。

| 参数 | 类型 | 说明 |
|------|------|------|
| `周期_ms` | `` | |
| `执行时间_ms` | `` | |
| `开销_ms` | `` | |

Source: `src\domains\automation.py:75`

---

## `_自动化流程执行效率`

```matha
def _自动化流程执行效率(步骤数, 平均步时长_ms, 并行度)
```

**描述**: 自动化流程执行效率（吞吐率）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `步骤数` | `` | |
| `平均步时长_ms` | `` | |
| `并行度` | `` | |

Source: `src\domains\automation.py:80`

---

## `_异常检测率`

```matha
def _异常检测率(误报率, 漏报率, 样本数)
```

**描述**: 异常检测综合率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `误报率` | `` | |
| `漏报率` | `` | |
| `样本数` | `` | |

Source: `src\domains\automation.py:89`

---

## `_register_automation`

```matha
def _register_automation(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\automation.py:101`

---

## `_automation_symtab_names`

```matha
def _automation_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\automation.py:110`

---

# `domains.autonomous`

```matha

```

**描述**: Matha 自动驾驶领域模块：碰撞预测、路径规划、感知融合、决策控制。

覆盖：
  1) 碰撞时间估算
  2) 路径规划复杂度
  3) 传感器融合误差
  4) 决策响应时间
  5) 定位精度
  6) 油耗估算

Source: `src\domains\autonomous.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\autonomous.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\autonomous.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\autonomous.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\autonomous.py:31`

---

## `_碰撞时间估算`

```matha
def _碰撞时间估算(距离_m, 相对速度_ms)
```

**描述**: 碰撞时间 TTC = d/v。

| 参数 | 类型 | 说明 |
|------|------|------|
| `距离_m` | `` | |
| `相对速度_ms` | `` | |

Source: `src\domains\autonomous.py:40`

---

## `_路径规划复杂度`

```matha
def _路径规划复杂度(网格数, 分支因子)
```

**描述**: A*路径规划最坏情况复杂度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `网格数` | `` | |
| `分支因子` | `` | |

Source: `src\domains\autonomous.py:47`

---

## `_传感器融合误差`

```matha
def _传感器融合误差(传感器误差_list)
```

**描述**: 多传感器融合后误差（协方差近似）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `传感器误差_list` | `` | |

Source: `src\domains\autonomous.py:54`

---

## `_决策响应时间`

```matha
def _决策响应时间(感知延迟_ms, 计算延迟_ms, 执行延迟_ms)
```

**描述**: 决策总响应时间。

| 参数 | 类型 | 说明 |
|------|------|------|
| `感知延迟_ms` | `` | |
| `计算延迟_ms` | `` | |
| `执行延迟_ms` | `` | |

Source: `src\domains\autonomous.py:63`

---

## `_定位精度`

```matha
def _定位精度(卫星数, PDOP)
```

**描述**: GNSS定位精度估算（米）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `卫星数` | `` | |
| `PDOP` | `` | |

Source: `src\domains\autonomous.py:68`

---

## `_油耗估算`

```matha
def _油耗估算(车重_kg, 风阻系数, 速度_kmh, 路况)
```

**描述**: 油耗估算（L/100km）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `车重_kg` | `` | |
| `风阻系数` | `` | |
| `速度_kmh` | `` | |
| `路况` | `` | |

Source: `src\domains\autonomous.py:75`

---

## `_register_autonomous`

```matha
def _register_autonomous(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\autonomous.py:88`

---

## `_autonomous_symtab_names`

```matha
def _autonomous_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\autonomous.py:97`

---

# `domains.bio_computing`

```matha

```

**描述**: Matha 生物计算领域模块：生物信息学、基因组学、蛋白质结构、系统生物学。

覆盖：
  1) GC含量计算
  2) 分子质量估算
  3) 蛋白折叠能量
  4) 序列比对得分
  5) 系统稳定性
  6) 代谢通量

Source: `src\domains\bio_computing.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\bio_computing.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\bio_computing.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\bio_computing.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\bio_computing.py:31`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\bio_computing.py:39`

---

## `_GC含量计算`

```matha
def _GC含量计算(序列)
```

**描述**: DNA序列GC含量（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `序列` | `` | |

Source: `src\domains\bio_computing.py:50`

---

## `_分子质量估算`

```matha
def _分子质量估算(氨基酸序列)
```

**描述**: 蛋白质分子质量估算（Da）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `氨基酸序列` | `` | |

Source: `src\domains\bio_computing.py:58`

---

## `_蛋白折叠能量`

```matha
def _蛋白折叠能量(序列长度, 疏水比例)
```

**描述**: 简化蛋白折叠自由能估算（kcal/mol）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `序列长度` | `` | |
| `疏水比例` | `` | |

Source: `src\domains\bio_computing.py:64`

---

## `_序列比对得分`

```matha
def _序列比对得分(序列1, 序列2, 匹配奖, 错配罚, 空位罚)
```

**描述**: 序列比对打分（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `序列1` | `` | |
| `序列2` | `` | |
| `匹配奖` | `` | |
| `错配罚` | `` | |
| `空位罚` | `` | |

Source: `src\domains\bio_computing.py:71`

---

## `_系统稳定性`

```matha
def _系统稳定性(反馈强度, 延迟, 阻尼)
```

**描述**: 控制理论系统稳定性判据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `反馈强度` | `` | |
| `延迟` | `` | |
| `阻尼` | `` | |

Source: `src\domains\bio_computing.py:80`

---

## `_代谢通量`

```matha
def _代谢通量(酶浓度, Km, Vmax, 底物浓度)
```

**描述**: Michaelis-Menten代谢通量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `酶浓度` | `` | |
| `Km` | `` | |
| `Vmax` | `` | |
| `底物浓度` | `` | |

Source: `src\domains\bio_computing.py:88`

---

## `_register_bio_computing`

```matha
def _register_bio_computing(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\bio_computing.py:99`

---

## `_bio_computing_symtab_names`

```matha
def _bio_computing_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\bio_computing.py:108`

---

# `domains.biology`

```matha

```

**描述**: Matha 机械领域扩展模块：生物学（Biology）。

基于 Matha 数学基础与物理常量体系，演化生物学子领域功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、分子生物学（Molecular Biology）- 前缀 分子_
  1) 碱基互补配对规则（A↔T/U, C↔G）
  2) DNA 转录成 mRNA（T→U，反向读取简化保留顺序）
  3) DNA 互补链生成（5'-3' 写出互补）
  4) GC 含量计算（GC 比例）
  5) 熔解温度 Tm（Wallace 公式简化：T=2(A+T)+4(G+C) °C，或 近邻近似本模块用简化版）
  6) DNA/RNA 摩尔质量估算（每个核苷酸残基平均 ~325 Da（单链 DNA）/330（RNA），双链 ×2）
  7) PCR 扩增倍数：2^n（n 循环数）
  8) 质量 → 摩尔数：moles = mass / M（bp 数 × 650 Da 双链）
  9) 密码子查找（单个密码子查表返回单字母 AA / * 表示终止）
  10) mRNA 翻译（从起始 AUG 开始按三联体翻译成蛋白质序列）

二、细胞生物学（Cell Biology）- 前缀 细胞_
  1) 球体细胞体积：V = 4πr³/3
  2) 球体表面积：S = 4πr²
  3) 表面积/体积比 S/V（新陈代谢 / 物质交换判据）
  4) 倍增时间：t_d = ln2 / k（k 生长速率常数 1/s）
  5) 生长速率常数：k = ln2 / t_d
  6) 指数生长后细胞数：N = N0·exp(k·t) = N0·2^(t/t_d)
  7) 一维扩散特征时间：t ≈ x² / (2D)（D 扩散系数 m²/s）
  8) 细胞膜总电容估算：C_total = Cm·A，Cm≈1μF/cm²
  9) 倍增代数：n = log2(N/N0)

三、生物化学（Biochemistry）- 前缀 生化_
  1) 米氏方程：v = Vmax·[S] / (Km + [S])
  2) Lineweaver-Burk 双倒数：1/v = (Km/Vmax)(1/[S]) + 1/Vmax
  3) Henderson-Hasselbalch：pH = pKa + log([A⁻]/[HA])
  4) 缓冲容量 β = 2.303·C·Ka·[H⁺] / (Ka + [H⁺])²（C 总浓度）
  5) 能斯特方程（298K 常温）：E = E° + (0.05916/z)·log([Ox]/[Red]) 或用 RT/zF
  6) Arrhenius 方程：k = A·exp(-Ea/(RT))
  7) Hill 方程（协同性）：θ = [L]^n / (Kd^n + [L]^n) 或 (1 + Kd^n/[L]^n)⁻¹
  8) 竞争性抑制表观 Km：Km_app = Km·(1 + [I]/Ki)
  9) 非竞争性抑制表观 Vmax：Vmax_app = Vmax/(1 + [I]/Ki)

四、生理学与种群生态学（Physiology & Population）- 前缀 生理_ / 种群_
  1) BMI 指数：kg/m²
  2) 心脏输出量 CO = HR（次/分）× SV（每搏量 mL）
  3) 肺泡通气量 VA = (TV - VD) × f（每分钟）
  4) 动脉血氧含量：CaO2 = 1.34·Hb·SaO2 + 0.003·PaO2（mL O2/100mL 血）
  5) 基础代谢率（Mifflin-St Jeor）：男 10m+6.25h-5a+5；女 10m+6.25h-5a-161
  6) 种群指数增长：N(t) = N0·exp(r·t)
  7) 种群倍增时间：t_d = ln2/r
  8) 逻辑斯蒂增长：N(t) = K / (1 + (K/N0 - 1)·exp(-r·t))
  9) 世代净繁殖率 R0 与内禀增长率关系：r ≈ ln(R0)/T（T 世代时间）

五、微生物与免疫（Microbiology & Immunology）- 前缀 微生_ / 免疫_
  1) CFU 每毫升：CFU/mL = 平板计数 × 稀释倍数 / 接种体积 mL
  2) 杀菌对数减少值 LR = log10(N0/N)
  3) D 值（90% 杀死所需时间 / 温度）：D → N = N0·10^(-t/D)
  4) Z 值（使 D 值变化 10 倍的温度变化）：log10(D1/D2)=(T2-T1)/z
  5) 世代时间 g：由 N=N0·2^(t/g) → g = t / log2(N/N0)
  6) MOI（感染复数）：噬菌体 / 细菌颗粒数
  7) 杀菌存活率：S = N/N0 = 10^(-LR)
  8) 抗体效价估算（血清稀释几何平均）
  9) OD600 估算大肠杆菌浓度：个/mL ≈ 1 OD600 × 8e8

数据库：
  - 标准遗传密码表（64 个密码子 → 单字母氨基酸，终止 *）
  - 20 种氨基酸残基分子量（Da，精确到 2 位小数）
  - 4 种 dNTP 分子量 & 4 种 NTP 分子量（合成用）
  - 生理/生物物理常量（法拉第常数 F、摩尔气体 R、体温 K=310.15、25℃=298.15K、Cm 膜电容 1 μF/cm² 等）

设计原则：
  - 与 fluid_exp / statmech 保持一致：_curryN 柯里化、前缀区分
  - 函数返回纯数值或字符串，不做语义包装
  - 除零 / 非法序列由 Python 自身抛错

Source: `src\domains\biology.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\biology.py:82`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\biology.py:88`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\biology.py:96`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\biology.py:106`

---

## `_分子_DNA互补链`

```matha
def _分子_DNA互补链(seq)
```

**描述**: 返回 DNA 互补链（同方向 5'-3'：互补后反转）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

Source: `src\domains\biology.py:201`

---

## `_分子_DNA转录`

```matha
def _分子_DNA转录(seq)
```

**描述**: DNA 模板链 → mRNA（T→U，保留顺序，互补反义做了反转后再取 T→U）。
这里简单实现：输入编码链（5'-3'），直接 T→U 即 mRNA。

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

Source: `src\domains\biology.py:210`

---

## `_分子_GC含量`

```matha
def _分子_GC含量(seq)
```

**描述**: 返回 GC 碱基比例 (0~1)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

Source: `src\domains\biology.py:216`

---

## `_分子_Tm_Wallace`

```matha
def _分子_Tm_Wallace(seq)
```

**描述**: Wallace 公式：Tm(°C) = 2(A+T) + 4(G+C)（≤20 bp 探针近似）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

Source: `src\domains\biology.py:225`

---

## `_分子_dsDNA质量`

```matha
def _分子_dsDNA质量(bp)
```

**描述**: 双链 DNA 质量 Da ≈ bp × 650 Da/bp。

| 参数 | 类型 | 说明 |
|------|------|------|
| `bp` | `` | |

Source: `src\domains\biology.py:233`

---

## `_分子_ssDNA质量`

```matha
def _分子_ssDNA质量(nt)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `nt` | `` | |

Source: `src\domains\biology.py:238`

---

## `_分子_ssRNA质量`

```matha
def _分子_ssRNA质量(nt)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `nt` | `` | |

Source: `src\domains\biology.py:239`

---

## `_分子_PCR扩增`

```matha
def _分子_PCR扩增(n_cycle)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n_cycle` | `` | |

Source: `src\domains\biology.py:242`

---

## `_分子_PCR扩增效率`

```matha
def _分子_PCR扩增效率(n_cycle, eff)
```

**描述**: 实际扩增倍数：(1+eff)^n，eff∈[0,1]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n_cycle` | `` | |
| `eff` | `` | |

Source: `src\domains\biology.py:245`

---

## `_分子_质量转摩尔`

```matha
def _分子_质量转摩尔(mass_g, M_Da)
```

**描述**: 摩尔数 mol = mass_g / (M_Da × g/mol 当量)。Da 单位即 g/mol，直接除。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mass_g` | `` | |
| `M_Da` | `` | |

Source: `src\domains\biology.py:250`

---

## `_分子_密码子查找`

```matha
def _分子_密码子查找(codon)
```

**描述**: 传入 3 字母密码子（U 为 RNA 形式），返回单字母 AA 或 *。

| 参数 | 类型 | 说明 |
|------|------|------|
| `codon` | `` | |

Source: `src\domains\biology.py:255`

---

## `_分子_mRNA翻译`

```matha
def _分子_mRNA翻译(mRNA, from_start)
```

**描述**: 从 mRNA 翻译为蛋白质序列（单字母，终止符 *）。
from_start=True 则从第一个 AUG 开始按 3 读数；否则从头。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mRNA` | `` | |
| `from_start` | `` | |

Source: `src\domains\biology.py:263`

---

## `_分子_蛋白分子量`

```matha
def _分子_蛋白分子量(seq)
```

**描述**: 按单字母序列计算残基分子量（Da，不含水和修饰）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

Source: `src\domains\biology.py:282`

---

## `_分子_等电点简化`

```matha
def _分子_等电点简化()
```

Source: `src\domains\biology.py:287`

---

## `_细胞_球体体积`

```matha
def _细胞_球体体积(r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |

Source: `src\domains\biology.py:293`

---

## `_细胞_球体表面积`

```matha
def _细胞_球体表面积(r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |

Source: `src\domains\biology.py:294`

---

## `_细胞_表面积体积比`

```matha
def _细胞_表面积体积比(r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |

Source: `src\domains\biology.py:295`

---

## `_细胞_生长速率`

```matha
def _细胞_生长速率(t_d)
```

**描述**: k = ln2 / t_d，t_d 与 k 时间单位一致（1/时间）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `t_d` | `` | |

Source: `src\domains\biology.py:301`

---

## `_细胞_倍增时间`

```matha
def _细胞_倍增时间(k)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k` | `` | |

Source: `src\domains\biology.py:306`

---

## `_细胞_指数生长`

```matha
def _细胞_指数生长(N0, k, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `k` | `` | |
| `t` | `` | |

Source: `src\domains\biology.py:309`

---

## `_细胞_倍增代数`

```matha
def _细胞_倍增代数(N0, N)
```

**描述**: 从 N0 到 N 共经历几代倍增：n = log2(N/N0)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `N` | `` | |

Source: `src\domains\biology.py:312`

---

## `_细胞_特征扩散时间`

```matha
def _细胞_特征扩散时间(x, D)
```

**描述**: x 特征距离（m），D 扩散系数 m²/s → t(s) ≈ x²/(2D)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `` | |
| `D` | `` | |

Source: `src\domains\biology.py:319`

---

## `_细胞_膜电容`

```matha
def _细胞_膜电容(A_cm2)
```

**描述**: 细胞膜总电容（μF）：典型 Cm=1 μF/cm² × 面积 cm²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A_cm2` | `` | |

Source: `src\domains\biology.py:326`

---

## `_细胞_椭球体积`

```matha
def _细胞_椭球体积(a, b, c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `b` | `` | |
| `c` | `` | |

Source: `src\domains\biology.py:331`

---

## `_生化_米氏方程`

```matha
def _生化_米氏方程(Vmax, Km, S)
```

**描述**: v = Vmax·[S] / (Km + [S])。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Vmax` | `` | |
| `Km` | `` | |
| `S` | `` | |

Source: `src\domains\biology.py:336`

---

## `_生化_Lineweaver斜率`

```matha
def _生化_Lineweaver斜率(Vmax, Km)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Vmax` | `` | |
| `Km` | `` | |

Source: `src\domains\biology.py:341`

---

## `_生化_Lineweaver截距`

```matha
def _生化_Lineweaver截距(Vmax)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Vmax` | `` | |

Source: `src\domains\biology.py:342`

---

## `_生化_HH`

```matha
def _生化_HH(pKa, ratio_Aminus_over_HA)
```

**描述**: Henderson-Hasselbalch：pH = pKa + log([A⁻]/[HA])。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pKa` | `` | |
| `ratio_Aminus_over_HA` | `` | |

Source: `src\domains\biology.py:345`

---

## `_生化_缓冲容量`

```matha
def _生化_缓冲容量(C, Ka, pH)
```

**描述**: β = 2.303·C·Ka·[H⁺]/(Ka+[H⁺])²；[H⁺] = 10^-pH。

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |
| `Ka` | `` | |
| `pH` | `` | |

Source: `src\domains\biology.py:352`

---

## `_生化_能斯特`

```matha
def _生化_能斯特(E0, z, ratio_Ox_Red, T_K)
```

**描述**: E = E° + (RT/zF)·ln(Ox/Red)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `E0` | `` | |
| `z` | `` | |
| `ratio_Ox_Red` | `` | |
| `T_K` | `` | |

Source: `src\domains\biology.py:359`

---

## `_生化_阿伦尼乌斯`

```matha
def _生化_阿伦尼乌斯(A_pre, Ea_Jmol, T_K)
```

**描述**: k = A·exp(-Ea/(RT))。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A_pre` | `` | |
| `Ea_Jmol` | `` | |
| `T_K` | `` | |

Source: `src\domains\biology.py:364`

---

## `_生化_Hill方程`

```matha
def _生化_Hill方程(n_Hill, Kd, L)
```

**描述**: θ = [L]^n / (Kd^n + [L]^n)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n_Hill` | `` | |
| `Kd` | `` | |
| `L` | `` | |

Source: `src\domains\biology.py:369`

---

## `_生化_竞争抑制Km`

```matha
def _生化_竞争抑制Km(Km, I, Ki)
```

**描述**: Km_app = Km·(1 + [I]/Ki)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Km` | `` | |
| `I` | `` | |
| `Ki` | `` | |

Source: `src\domains\biology.py:376`

---

## `_生化_非竞争抑制Vmax`

```matha
def _生化_非竞争抑制Vmax(Vmax, I, Ki)
```

**描述**: Vmax_app = Vmax/(1 + [I]/Ki)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Vmax` | `` | |
| `I` | `` | |
| `Ki` | `` | |

Source: `src\domains\biology.py:381`

---

## `_生理_BMI`

```matha
def _生理_BMI(kg, m)
```

**描述**: 体重指数 kg/m²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `kg` | `` | |
| `m` | `` | |

Source: `src\domains\biology.py:388`

---

## `_生理_心输出量`

```matha
def _生理_心输出量(HR_bpm, SV_mL)
```

**描述**: CO = HR × SV（mL/min）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `HR_bpm` | `` | |
| `SV_mL` | `` | |

Source: `src\domains\biology.py:395`

---

## `_生理_肺泡通气量`

```matha
def _生理_肺泡通气量(TV_mL, VD_mL, f_bpm)
```

**描述**: VA = (TV - VD) × f（mL/min）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `TV_mL` | `` | |
| `VD_mL` | `` | |
| `f_bpm` | `` | |

Source: `src\domains\biology.py:400`

---

## `_生理_血氧含量`

```matha
def _生理_血氧含量(Hb_g_dL, SaO2, PaO2_mmHg)
```

**描述**: CaO2 = 1.34·Hb·SaO2 + 0.003·PaO2（mL O2/100mL 血）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Hb_g_dL` | `` | |
| `SaO2` | `` | |
| `PaO2_mmHg` | `` | |

Source: `src\domains\biology.py:405`

---

## `_生理_基础代谢`

```matha
def _生理_基础代谢(m_kg, h_cm, age, is_male)
```

**描述**: Mifflin-St Jeor：男 10m + 6.25h - 5a + 5；女 同 -161。单位 kcal/日。

| 参数 | 类型 | 说明 |
|------|------|------|
| `m_kg` | `` | |
| `h_cm` | `` | |
| `age` | `` | |
| `is_male` | `` | |

Source: `src\domains\biology.py:410`

---

## `_种群_指数增长`

```matha
def _种群_指数增长(N0, r, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `r` | `` | |
| `t` | `` | |

Source: `src\domains\biology.py:416`

---

## `_种群_倍增时间`

```matha
def _种群_倍增时间(r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |

Source: `src\domains\biology.py:419`

---

## `_种群_逻辑斯蒂`

```matha
def _种群_逻辑斯蒂(N0, r, K, t)
```

**描述**: N(t) = K / (1 + (K/N0 - 1)·exp(-rt))。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `r` | `` | |
| `K` | `` | |
| `t` | `` | |

Source: `src\domains\biology.py:422`

---

## `_种群_内禀增长率`

```matha
def _种群_内禀增长率(R0, T_generation)
```

**描述**: r ≈ ln(R0)/T（离散世代近似）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R0` | `` | |
| `T_generation` | `` | |

Source: `src\domains\biology.py:430`

---

## `_种群_世代总数`

```matha
def _种群_世代总数(r, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |
| `t` | `` | |

Source: `src\domains\biology.py:437`

---

## `_微生_CFU`

```matha
def _微生_CFU(plate_count, dilution_factor, volume_mL)
```

**描述**: CFU/mL = plate_count × dilution_factor / volume_mL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `plate_count` | `` | |
| `dilution_factor` | `` | |
| `volume_mL` | `` | |

Source: `src\domains\biology.py:442`

---

## `_微生_对数减少`

```matha
def _微生_对数减少(N0, N)
```

**描述**: LR = log10(N0/N)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `N` | `` | |

Source: `src\domains\biology.py:449`

---

## `_微生_杀菌存活率`

```matha
def _微生_杀菌存活率(LR)
```

**描述**: S = N/N0 = 10^(-LR)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `LR` | `` | |

Source: `src\domains\biology.py:456`

---

## `_微生_D值存活`

```matha
def _微生_D值存活(N0, t_min, D_min)
```

**描述**: N = N0·10^(-t/D)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `t_min` | `` | |
| `D_min` | `` | |

Source: `src\domains\biology.py:461`

---

## `_微生_Z值`

```matha
def _微生_Z值(logD_ratio, delta_T)
```

**描述**: 由 log(D1/D2) = (T2-T1)/z → z = ΔT / log(D1/D2)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `logD_ratio` | `` | |
| `delta_T` | `` | |

Source: `src\domains\biology.py:466`

---

## `_微生_世代时间`

```matha
def _微生_世代时间(N0, N, t_h)
```

**描述**: g = t / log2(N/N0)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `N` | `` | |
| `t_h` | `` | |

Source: `src\domains\biology.py:473`

---

## `_微生_MOI`

```matha
def _微生_MOI(phage_count, host_count)
```

**描述**: 感染复数 = 噬菌体颗粒 / 宿主细菌数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `phage_count` | `` | |
| `host_count` | `` | |

Source: `src\domains\biology.py:480`

---

## `_微生_OD600转细胞数`

```matha
def _微生_OD600转细胞数(OD600)
```

**描述**: 经验：大肠杆菌 OD600=1 ≈ 8e8 CFU/mL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `OD600` | `` | |

Source: `src\domains\biology.py:487`

---

## `_免疫_效价稀释`

```matha
def _免疫_效价稀释(initial_dilution, titer_positive)
```

**描述**: 简化：titer = 初始稀释倍数 × 终点阳性管几何平均。返回单一 positive 稀释下倒数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `initial_dilution` | `` | |
| `titer_positive` | `` | |

Source: `src\domains\biology.py:492`

---

## `_register_biology`

```matha
def _register_biology(builtins: dict) -> None
```

**描述**: 将生物学子领域内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（fluid_exp 之后、statmech 之前或之后任意位置）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\biology.py:501`

---

## `_biology_symtab_names`

```matha
def _biology_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\biology.py:597`

---

# `domains.blockchain`

```matha

```

**描述**: Matha 区块链与Web3开发领域模块。

覆盖：
  1) 区块与链操作
  2) 哈希与 Merkel 树
  3) 交易签名与验证
  4) PoW/PoS 共识
  5) 智能合约模拟
  6) Token 转账

Source: `src\domains\blockchain.py:1`

---

## `block_create`

```matha
def block_create(prev_hash: str, transactions: list[dict], nonce: int) -> dict
```

**描述**: 创建区块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `prev_hash` | `str` | |
| `transactions` | `list[dict]` | |
| `nonce` | `int` | |

**返回**: `dict`

Source: `src\domains\blockchain.py:23`

---

## `block_verify`

```matha
def block_verify(block: dict, prev_block: Optional[dict]) -> bool
```

**描述**: 验证区块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `block` | `dict` | |
| `prev_block` | `Optional[dict]` | |

**返回**: `bool`

Source: `src\domains\blockchain.py:36`

---

## `chain_validate`

```matha
def chain_validate(chain: list[dict]) -> bool
```

**描述**: 验证区块链。

| 参数 | 类型 | 说明 |
|------|------|------|
| `chain` | `list[dict]` | |

**返回**: `bool`

Source: `src\domains\blockchain.py:42`

---

## `hash_block`

```matha
def hash_block(data: str) -> str
```

**描述**: 区块哈希。

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `str` | |

**返回**: `str`

Source: `src\domains\blockchain.py:56`

---

## `merkle_root`

```matha
def merkle_root(leaves: list[str]) -> str
```

**描述**: Merkle 根。

| 参数 | 类型 | 说明 |
|------|------|------|
| `leaves` | `list[str]` | |

**返回**: `str`

Source: `src\domains\blockchain.py:61`

---

## `sign_transaction`

```matha
def sign_transaction(tx: dict, private_key: str) -> str
```

**描述**: 交易签名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tx` | `dict` | |
| `private_key` | `str` | |

**返回**: `str`

Source: `src\domains\blockchain.py:82`

---

## `verify_signature`

```matha
def verify_signature(tx: dict, signature: str, public_key: str) -> bool
```

**描述**: 验证签名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tx` | `dict` | |
| `signature` | `str` | |
| `public_key` | `str` | |

**返回**: `bool`

Source: `src\domains\blockchain.py:88`

---

## `poW_mine`

```matha
def poW_mine(block: dict, difficulty: int) -> dict
```

**描述**: PoW 挖矿。

| 参数 | 类型 | 说明 |
|------|------|------|
| `block` | `dict` | |
| `difficulty` | `int` | |

**返回**: `dict`

Source: `src\domains\blockchain.py:98`

---

## `poS_validate`

```matha
def poS_validate(proposer: str, stake: float, validators: list[dict]) -> bool
```

**描述**: PoS 验证（简化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `proposer` | `str` | |
| `stake` | `float` | |
| `validators` | `list[dict]` | |

**返回**: `bool`

Source: `src\domains\blockchain.py:115`

---

## `smart_contract_deploy`

```matha
def smart_contract_deploy(code: str, owner: str) -> str
```

**描述**: 部署智能合约。

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |
| `owner` | `str` | |

**返回**: `str`

Source: `src\domains\blockchain.py:130`

---

## `smart_contract_call`

```matha
def smart_contract_call(addr: str, func: str, args: list) -> dict
```

**描述**: 调用智能合约。

| 参数 | 类型 | 说明 |
|------|------|------|
| `addr` | `str` | |
| `func` | `str` | |
| `args` | `list` | |

**返回**: `dict`

Source: `src\domains\blockchain.py:137`

---

## `token_transfer`

```matha
def token_transfer(from_addr: str, to_addr: str, amount: float) -> bool
```

**描述**: Token 转账。

| 参数 | 类型 | 说明 |
|------|------|------|
| `from_addr` | `str` | |
| `to_addr` | `str` | |
| `amount` | `float` | |

**返回**: `bool`

Source: `src\domains\blockchain.py:153`

---

## `token_balance`

```matha
def token_balance(addr: str) -> float
```

**描述**: 查询 Token 余额。

| 参数 | 类型 | 说明 |
|------|------|------|
| `addr` | `str` | |

**返回**: `float`

Source: `src\domains\blockchain.py:166`

---

## `token_mint`

```matha
def token_mint(addr: str, amount: float) -> None
```

**描述**: 铸造 Token。

| 参数 | 类型 | 说明 |
|------|------|------|
| `addr` | `str` | |
| `amount` | `float` | |

**返回**: `None`

Source: `src\domains\blockchain.py:171`

---

## `_register_blockchain`

```matha
def _register_blockchain(builtins: dict) -> None
```

**描述**: 注册区块链内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\blockchain.py:200`

---

## `_blockchain_symtab_names`

```matha
def _blockchain_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\blockchain.py:218`

---

# `domains.building_struct`

```matha

```

**描述**: Matha 领域扩展模块：建筑结构工程（Building Structural Engineering）。

基于 Matha 数学基础与 structural.py（结构力学）+ architecture.py（建筑学）+ mechanics.py，
演化建筑结构设计子领域功能。所有函数以普通 Python callable 注册到解释器 builtins。

五大子领域（与 structural.py 结构力学互补，聚焦规范设计与构件验算）：

一、混凝土结构（Concrete Structures）- 前缀 混凝_
  1) 单筋矩形梁受弯承载力 M = α1·fc·b·x·(h0-0.4x) / 简化 Mu = 0.9·fy·As·h0
  2) 相对受压区高度 ξ = x/h0 = (fy·As)/(α1·fc·b·h0)
  3) 最小配筋率 ρmin = max(0.2%, 0.45·ft/fy)
  4) 最大配筋率 ρmax = ξb·fc/fy
  5) 界限相对受压区高度 ξb（按钢筋级别）
  6) 偏心受压构件偏心距增大系数 η = 1+ζ1·ζ2·(l0/h)²
  7) T 形截面翼缘有效宽度 bf'
  8) 受剪承载力 V = 0.7·ft·b·h0 + 1.25·fyv·Asv·h0/s
  9) 轴心受压承载力 N = 0.9·φ·(fc·A + fy·As')
  10) 裂缝宽度验算 wmax = αcr·ψ·σsk/Es·(1.9c+0.08·deq/ρte)

二、钢结构（Steel Structures）- 前缀 钢结_
  1) 抗弯强度 σ = M/(γx·Wn) ≤ f
  2) 抗剪强度 τ = V·S/(I·tw) ≤ fv
  3) 整体稳定 φb（典型值）
  4) 轴心受压稳定系数 φ（按 λ 查表近似）
  5) 局部稳定宽厚比限值
  6) 焊缝计算长度 lw = N/(he·ffw)
  7) 螺栓抗剪承载力 Nb = ns·πd²/4 · fv_b
  8) 螺栓抗拉承载力 Nt = πd²/4 · ft_b
  9) 高强螺栓摩擦型承载力 Nv = μ·nf·P
  10) 长细比 λ = l0/i

三、砌体结构（Masonry Structures）- 前缀 砌体_
  1) 抗压承载力 N ≤ φ·f·A
  2) 局压承载力 N ≤ γ·f·Al
  3) 局压强度提高系数 γ = 1 + 0.35·√(Al/Aln)
  4) 高厚比 β = H0/h
  5) 允许高厚比 [β]（按砂浆等级）
  6) 墙柱稳定性判定
  7) 受剪承载力 V ≤ (fv + 0.18σ0)·A
  8) 网状配筋砌体抗压强度提高系数

四、木结构（Timber Structures）- 前缀 木结_
  1) 顺纹抗压承载力 N = fc·An
  2) 抗弯承载力 M = fm·Wn
  3) 顺纹抗剪承载力 V = fv·b·I/S
  4) 齿连接承压承载力
  5) 螺栓连接承载力（单剪/双剪）
  6) 木构件稳定系数 φ（按 λ）
  7) 长细比 λ = l0/i

五、地基与基础（Foundation）- 前缀 基础_
  1) 地基承载力修正 f = fak + ηb·γ·(b-3) + ηd·γm·(d-0.5)
  2) 中心受压基础底面积 A ≥ N/(f - γm·d)
  3) 偏心受压基础底面积（初步）
  4) 基底附加应力 p0 = p - γm·d
  5) 集中力布辛奈斯克解 σz = 3P/(2π)·z³/R⁵
  6) 分层总和法沉降（单层）s = σz·H/Es
  7) 单桩承载力 Q = up·Σqsik·li + qpk·Ap
  8) 桩基承台冲切承载力

六、抗震设计（Seismic Design）- 前缀 抗震_
  1) 水平地震作用 F = α·G（底部剪力法等效）
  2) 地震影响系数 α（按烈度与场地特征周期查表）
  3) 重力荷载代表值 G = 恒载 + 0.5·活载
  4) 楼层最小地震剪力 λ·G
  5) 轴压比 μ = N/(fc·A) 限值判定
  6) 剪跨比 λ = M/(V·h0)
  7) 抗震等级调整系数（按烈度/结构类型）
  8) 弹性层间位移角限值 θe
  9) 弹塑性层间位移角限值 θp

数据库：
  - 混凝土强度等级（fc/ft/ftk/Ec，C15-C80）
  - 钢筋强度等级（HPB300/HRB400/HRB500 的 fy/fyk/Es）
  - 钢材强度等级（Q235/Q345/Q390/Q420 的 f/fv/E）
  - 砌体抗压强度（按砖与砂浆等级组合查 GB 50003）
  - 砂浆等级允许高厚比
  - 木材强度等级（TC/TB 系列）
  - 地基承载力修正系数 ηb/ηd
  - 桩侧/桩端阻力参考值
  - 地震影响系数最大值（按烈度 6-9 度）

设计原则：
  - 与 structural / architecture / mechanics 保持一致：_curryN 柯里化、前缀区分
  - 函数返回纯数值或字符串/元组，不做语义包装
  - 除零 / 非法参数由 Python 自身抛错
  - 公式基于中国规范 GB 50010/50017/50003/50005/50007/50011

Source: `src\domains\building_struct.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\building_struct.py:96`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\building_struct.py:102`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\building_struct.py:110`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\building_struct.py:120`

---

## `_混凝_受弯承载力简化`

```matha
def _混凝_受弯承载力简化(fy, As, h0)
```

**描述**: 单筋矩形梁受弯承载力简化：Mu = 0.9·fy·As·h0（kN·m，按规范简化估算）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fy` | `` | |
| `As` | `` | |
| `h0` | `` | |

Source: `src\domains\building_struct.py:253`

---

## `_混凝_相对受压区高度`

```matha
def _混凝_相对受压区高度(fy, As, fc, b, h0)
```

**描述**: ξ = x/h0 = (fy·As)/(α1·fc·b·h0)，α1=1.0（C50 及以下）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fy` | `` | |
| `As` | `` | |
| `fc` | `` | |
| `b` | `` | |
| `h0` | `` | |

Source: `src\domains\building_struct.py:258`

---

## `_混凝_最小配筋率`

```matha
def _混凝_最小配筋率(ft, fy)
```

**描述**: ρmin = max(0.2%, 0.45·ft/fy)。返回比值（非百分比）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ft` | `` | |
| `fy` | `` | |

Source: `src\domains\building_struct.py:266`

---

## `_混凝_最大配筋率`

```matha
def _混凝_最大配筋率(xi_b, fc, fy)
```

**描述**: ρmax = ξb·fc/fy（单筋矩形截面最大配筋率）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `xi_b` | `` | |
| `fc` | `` | |
| `fy` | `` | |

Source: `src\domains\building_struct.py:271`

---

## `_混凝_界限相对受压区高度`

```matha
def _混凝_界限相对受压区高度(rebar_grade)
```

**描述**: ξb 按钢筋级别查表（C50 及以下）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `rebar_grade` | `` | |

Source: `src\domains\building_struct.py:278`

---

## `_混凝_偏心距增大系数`

```matha
def _混凝_偏心距增大系数(l0, h, zeta1, zeta2)
```

**描述**: η = 1 + ζ1·ζ2·(l0/h)² / 1400·e0/h（简化默认 e0=h/2，分母=1400·0.5=700）。
此处输入 l0, h, ζ1, ζ2 → η = 1 + ζ1·ζ2·(l0/h)²/1400。

| 参数 | 类型 | 说明 |
|------|------|------|
| `l0` | `` | |
| `h` | `` | |
| `zeta1` | `` | |
| `zeta2` | `` | |

Source: `src\domains\building_struct.py:283`

---

## `_混凝_T形翼缘宽度`

```matha
def _混凝_T形翼缘宽度(b, hf, l0)
```

**描述**: T 形截面翼缘有效宽度 bf' 简化：min(b + 12·hf, l0/3, b + S_n)。
返回 min(b + 12·hf, l0/3)（无横肋情况）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `b` | `` | |
| `hf` | `` | |
| `l0` | `` | |

Source: `src\domains\building_struct.py:291`

---

## `_混凝_受剪承载力`

```matha
def _混凝_受剪承载力(ft, b, h0, fyv, Asv, s)
```

**描述**: V = 0.7·ft·b·h0 + 1.25·fyv·Asv·h0/s（kN，GB 50010 简化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ft` | `` | |
| `b` | `` | |
| `h0` | `` | |
| `fyv` | `` | |
| `Asv` | `` | |
| `s` | `` | |

Source: `src\domains\building_struct.py:297`

---

## `_混凝_轴压承载力`

```matha
def _混凝_轴压承载力(fc, A, fy, As_prime, phi)
```

**描述**: N = 0.9·φ·(fc·A + fy·As')（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fc` | `` | |
| `A` | `` | |
| `fy` | `` | |
| `As_prime` | `` | |
| `phi` | `` | |

Source: `src\domains\building_struct.py:303`

---

## `_混凝_裂缝宽度`

```matha
def _混凝_裂缝宽度(alpha_cr, psi, sigma_sk, Es, c, deq, rho_te)
```

**描述**: wmax = αcr·ψ·σsk/Es·(1.9c + 0.08·deq/ρte)（mm，GB 50007 公式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `alpha_cr` | `` | |
| `psi` | `` | |
| `sigma_sk` | `` | |
| `Es` | `` | |
| `c` | `` | |
| `deq` | `` | |
| `rho_te` | `` | |

Source: `src\domains\building_struct.py:308`

---

## `_钢结_抗弯强度`

```matha
def _钢结_抗弯强度(M, gamma_x, Wn)
```

**描述**: σ = M/(γx·Wn) ≤ f（MPa）。M 单位 kN·m，Wn 单位 cm³ → 转换。

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `gamma_x` | `` | |
| `Wn` | `` | |

Source: `src\domains\building_struct.py:317`

---

## `_钢结_抗剪强度`

```matha
def _钢结_抗剪强度(V, S, I, tw)
```

**描述**: τ = V·S/(I·tw) ≤ fv（MPa）。V 单位 kN，S/I 单位 mm → 转换。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `S` | `` | |
| `I` | `` | |
| `tw` | `` | |

Source: `src\domains\building_struct.py:324`

---

## `_钢结_整体稳定系数`

```matha
def _钢结_整体稳定系数(lambda_b)
```

**描述**: 梁整体稳定系数 φb 简化估算（λb 简化长细比）：
λb ≤ 0.6 → 1.0；> 0.6 → 用 1.07 - λb²/4400 折减。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lambda_b` | `` | |

Source: `src\domains\building_struct.py:331`

---

## `_钢结_轴压稳定系数`

```matha
def _钢结_轴压稳定系数(slenderness)
```

**描述**: 轴心受压稳定系数 φ 按长细比 λ 查 a 类截面近似公式：
λ ≤ 0.215 → φ = 1 - α·λ²；> 0.215 → φ = (1+ε+λ²)/(2λ²)·[1-√(1-(2+ε+λ²)/(λ²·(1+ε+λ²))²)]。
简化：λ ≤ 60 → 0.9; 60-120 → 0.9 - (λ-60)/600; > 120 → 0.8 - (λ-120)/800。

| 参数 | 类型 | 说明 |
|------|------|------|
| `slenderness` | `` | |

Source: `src\domains\building_struct.py:339`

---

## `_钢结_宽厚比限值`

```matha
def _钢结_宽厚比限值(level)
```

**描述**: 局部稳定宽厚比限值。level: 一般/严格。

| 参数 | 类型 | 说明 |
|------|------|------|
| `level` | `` | |

Source: `src\domains\building_struct.py:350`

---

## `_钢结_焊缝计算长度`

```matha
def _钢结_焊缝计算长度(N, he, ffw)
```

**描述**: 角焊缝计算长度 lw = N/(he·ffw·0.7)（mm，正面角焊缝强度增大系数 βf=1.22 已忽略）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `he` | `` | |
| `ffw` | `` | |

Source: `src\domains\building_struct.py:356`

---

## `_钢结_螺栓抗剪承载力`

```matha
def _钢结_螺栓抗剪承载力(ns, d, fv_b)
```

**描述**: 单个普通螺栓抗剪承载力 Nb = ns·πd²/4·fv_b（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ns` | `` | |
| `d` | `` | |
| `fv_b` | `` | |

Source: `src\domains\building_struct.py:363`

---

## `_钢结_螺栓抗拉承载力`

```matha
def _钢结_螺栓抗拉承载力(d, ft_b)
```

**描述**: 单个普通螺栓抗拉承载力 Nt = πd²/4·ft_b（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |
| `ft_b` | `` | |

Source: `src\domains\building_struct.py:368`

---

## `_钢结_高强螺栓摩擦型承载力`

```matha
def _钢结_高强螺栓摩擦型承载力(mu, nf, P)
```

**描述**: 单个高强螺栓摩擦型抗剪 Nv = μ·nf·P / 1.0（kN，承载力设计值）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |
| `nf` | `` | |
| `P` | `` | |

Source: `src\domains\building_struct.py:373`

---

## `_钢结_长细比`

```matha
def _钢结_长细比(l0, i)
```

**描述**: λ = l0/i。

| 参数 | 类型 | 说明 |
|------|------|------|
| `l0` | `` | |
| `i` | `` | |

Source: `src\domains\building_struct.py:378`

---

## `_砌体_抗压承载力`

```matha
def _砌体_抗压承载力(phi, f, A)
```

**描述**: N ≤ φ·f·A（kN）。f MPa, A mm²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `phi` | `` | |
| `f` | `` | |
| `A` | `` | |

Source: `src\domains\building_struct.py:387`

---

## `_砌体_局压承载力`

```matha
def _砌体_局压承载力(gamma, f, Al)
```

**描述**: Nl ≤ γ·f·Al（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `f` | `` | |
| `Al` | `` | |

Source: `src\domains\building_struct.py:392`

---

## `_砌体_局压提高系数`

```matha
def _砌体_局压提高系数(Al, Aln)
```

**描述**: γ = 1 + 0.35·√(Al/Aln)，≤ 3。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Al` | `` | |
| `Aln` | `` | |

Source: `src\domains\building_struct.py:397`

---

## `_砌体_高厚比`

```matha
def _砌体_高厚比(H0, h)
```

**描述**: β = H0/h（计算高厚比）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `H0` | `` | |
| `h` | `` | |

Source: `src\domains\building_struct.py:404`

---

## `_砌体_允许高厚比`

```matha
def _砌体_允许高厚比(mortar_grade)
```

**描述**: [β] 按砂浆等级查表（无门窗洞口墙）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mortar_grade` | `` | |

Source: `src\domains\building_struct.py:411`

---

## `_砌体_稳定性判定`

```matha
def _砌体_稳定性判定(H0, h, mortar_grade)
```

**描述**: 墙柱稳定性判定：β ≤ [β]。返回 (是否稳定, β, [β])。

| 参数 | 类型 | 说明 |
|------|------|------|
| `H0` | `` | |
| `h` | `` | |
| `mortar_grade` | `` | |

Source: `src\domains\building_struct.py:416`

---

## `_砌体_受剪承载力`

```matha
def _砌体_受剪承载力(fv, sigma0, A)
```

**描述**: V ≤ (fv + 0.18·σ0)·A（kN，GB 50003）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fv` | `` | |
| `sigma0` | `` | |
| `A` | `` | |

Source: `src\domains\building_struct.py:423`

---

## `_砌体_网状配筋提高系数`

```matha
def _砌体_网状配筋提高系数(rho_s, fyv)
```

**描述**: 网状配筋砌体抗压强度提高系数 Δf ≈ 2·ρ·fyv/100（简化估算，MPa）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho_s` | `` | |
| `fyv` | `` | |

Source: `src\domains\building_struct.py:428`

---

## `_木结_顺纹抗压承载力`

```matha
def _木结_顺纹抗压承载力(fc, An)
```

**描述**: N = fc·An（kN）。fc MPa, An mm²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fc` | `` | |
| `An` | `` | |

Source: `src\domains\building_struct.py:435`

---

## `_木结_抗弯承载力`

```matha
def _木结_抗弯承载力(fm, Wn)
```

**描述**: M = fm·Wn（kN·m）。fm MPa, Wn mm³。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fm` | `` | |
| `Wn` | `` | |

Source: `src\domains\building_struct.py:440`

---

## `_木结_顺纹抗剪承载力`

```matha
def _木结_顺纹抗剪承载力(fv, b, I, S)
```

**描述**: V = fv·b·I/S（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fv` | `` | |
| `b` | `` | |
| `I` | `` | |
| `S` | `` | |

Source: `src\domains\building_struct.py:445`

---

## `_木结_齿连接承压`

```matha
def _木结_齿连接承压(fc_alpha, Ac)
```

**描述**: 齿连接承压承载力 N = fc_α·Ac（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fc_alpha` | `` | |
| `Ac` | `` | |

Source: `src\domains\building_struct.py:452`

---

## `_木结_螺栓连接承载力`

```matha
def _木结_螺栓连接承载力(fv_bolt, a, d)
```

**描述**: 木结构螺栓单剪承载力 Nv = kv·d²·√a/10（简化估算，kN）。
fv_bolt 为螺栓钢材抗剪（MPa），a 为板厚 mm，d 为直径 mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fv_bolt` | `` | |
| `a` | `` | |
| `d` | `` | |

Source: `src\domains\building_struct.py:457`

---

## `_木结_稳定系数`

```matha
def _木结_稳定系数(slenderness)
```

**描述**: 木构件稳定系数 φ（按长细比）：
λ ≤ 75 → φ = 1 - (λ/100)²；> 75 → φ = 3000/λ²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `slenderness` | `` | |

Source: `src\domains\building_struct.py:463`

---

## `_木结_长细比`

```matha
def _木结_长细比(l0, i)
```

**描述**: λ = l0/i（与钢结构公式一致）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `l0` | `` | |
| `i` | `` | |

Source: `src\domains\building_struct.py:473`

---

## `_基础_承载力修正`

```matha
def _基础_承载力修正(fak, eta_b, gamma, b, eta_d, gamma_m, d)
```

**描述**: 地基承载力修正 f = fak + ηb·γ·(b-3) + ηd·γm·(d-0.5)（kPa）。
b<3 取 3；b>6 取 6；d<0.5 取 0.5。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fak` | `` | |
| `eta_b` | `` | |
| `gamma` | `` | |
| `b` | `` | |
| `eta_d` | `` | |
| `gamma_m` | `` | |
| `d` | `` | |

Source: `src\domains\building_struct.py:482`

---

## `_基础_中心受压面积`

```matha
def _基础_中心受压面积(N, f, gamma_m, d)
```

**描述**: 中心受压基础底面积 A ≥ N/(f - γm·d)（m²）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `f` | `` | |
| `gamma_m` | `` | |
| `d` | `` | |

Source: `src\domains\building_struct.py:490`

---

## `_基础_附加应力`

```matha
def _基础_附加应力(p, gamma_m, d)
```

**描述**: 基底附加应力 p0 = p - γm·d（kPa）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `gamma_m` | `` | |
| `d` | `` | |

Source: `src\domains\building_struct.py:498`

---

## `_基础_Boussinesq应力`

```matha
def _基础_Boussinesq应力(P, z, R)
```

**描述**: 集中力 P 在深度 z 处的附加应力 σz = 3P/(2π)·z³/R⁵（kPa）。
若未给 R（计算点到 P 的距离），用 z 计算（即 R=z，正下方）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `z` | `` | |
| `R` | `` | |

Source: `src\domains\building_struct.py:503`

---

## `_基础_分层沉降`

```matha
def _基础_分层沉降(sigma_z, H, Es)
```

**描述**: 单层压缩量 s = σz·H/Es（mm）。sigma_z kPa, H m, Es MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_z` | `` | |
| `H` | `` | |
| `Es` | `` | |

Source: `src\domains\building_struct.py:513`

---

## `_基础_单桩承载力`

```matha
def _基础_单桩承载力(up, qsik_list, li_list, qpk, Ap)
```

**描述**: 单桩竖向承载力 Q = up·Σqsik·li + qpk·Ap（kN）。
up 周长 m，qsik_list 侧阻力 kPa 列表，li_list 段长 m 列表，qpk 端阻 kPa，Ap 截面积 m²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `up` | `` | |
| `qsik_list` | `` | |
| `li_list` | `` | |
| `qpk` | `` | |
| `Ap` | `` | |

Source: `src\domains\building_struct.py:520`

---

## `_基础_承台冲切承载力`

```matha
def _基础_承台冲切承载力(ft, h0, um)
```

**描述**: 承台冲切承载力 F = 0.7·ft·h0·um（kN）。ft MPa, h0 mm, um mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ft` | `` | |
| `h0` | `` | |
| `um` | `` | |

Source: `src\domains\building_struct.py:530`

---

## `_抗震_水平地震作用`

```matha
def _抗震_水平地震作用(alpha, G)
```

**描述**: F = α·G（kN，底部剪力法等效单质点）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `alpha` | `` | |
| `G` | `` | |

Source: `src\domains\building_struct.py:537`

---

## `_抗震_地震影响系数`

```matha
def _抗震_地震影响系数(intensity, level)
```

**描述**: αmax 按烈度与水准查表（直接返回 αmax，Tg/α 需按场地与周期进一步计算）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `intensity` | `` | |
| `level` | `` | |

Source: `src\domains\building_struct.py:542`

---

## `_抗震_重力荷载代表值`

```matha
def _抗震_重力荷载代表值(dead_load, live_load, live_factor)
```

**描述**: G = 恒载 + ψ·活载（ψ 通常 0.5）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `dead_load` | `` | |
| `live_load` | `` | |
| `live_factor` | `` | |

Source: `src\domains\building_struct.py:547`

---

## `_抗震_楼层最小剪力`

```matha
def _抗震_楼层最小剪力(lambda_factor, G)
```

**描述**: Vmin = λ·G（kN）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lambda_factor` | `` | |
| `G` | `` | |

Source: `src\domains\building_struct.py:552`

---

## `_抗震_轴压比`

```matha
def _抗震_轴压比(N, fc, A)
```

**描述**: μ = N/(fc·A)（无量纲）。N kN, fc MPa, A mm²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `fc` | `` | |
| `A` | `` | |

Source: `src\domains\building_struct.py:557`

---

## `_抗震_轴压比判定`

```matha
def _抗震_轴压比判定(N, fc, A, limit)
```

**描述**: 轴压比限值判定，返回 (是否满足, μ, 限值)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `fc` | `` | |
| `A` | `` | |
| `limit` | `` | |

Source: `src\domains\building_struct.py:564`

---

## `_抗震_剪跨比`

```matha
def _抗震_剪跨比(M, V, h0)
```

**描述**: λ = M/(V·h0)（无量纲）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `V` | `` | |
| `h0` | `` | |

Source: `src\domains\building_struct.py:570`

---

## `_抗震_抗震等级调整系数`

```matha
def _抗震_抗震等级调整系数(structure_type, intensity)
```

**描述**: 按结构类型与烈度返回抗震等级调整系数（简化，1/2/3/4 级 → 1.0/0.8/0.75/0.7）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `structure_type` | `` | |
| `intensity` | `` | |

Source: `src\domains\building_struct.py:577`

---

## `_抗震_弹性层间位移角限值`

```matha
def _抗震_弹性层间位移角限值(structure_type)
```

**描述**: 弹性层间位移角限值 [θe]（GB 50011）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `structure_type` | `` | |

Source: `src\domains\building_struct.py:589`

---

## `_抗震_弹塑性层间位移角限值`

```matha
def _抗震_弹塑性层间位移角限值(structure_type)
```

**描述**: 弹塑性层间位移角限值 [θp]（GB 50011）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `structure_type` | `` | |

Source: `src\domains\building_struct.py:601`

---

## `_register_building_struct`

```matha
def _register_building_struct(builtins: dict) -> None
```

**描述**: 将建筑结构工程子领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\building_struct.py:614`

---

## `_curry6`

```matha
def _curry6(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\building_struct.py:737`

---

## `_curry7`

```matha
def _curry7(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\building_struct.py:751`

---

## `_building_struct_symtab_names`

```matha
def _building_struct_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\building_struct.py:769`

---

# `domains.celestial`

```matha

```

**描述**: Matha 机械领域模块：天体力学（Celestial Mechanics）。

基于力学 + 量子力学 + mathlib 数学地基，演化天体力学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、万有引力与轨道力学（Gravitation & Orbital Mechanics）
  1) 万有引力：F = Gm₁m₂/r²
  2) 引力势能：U = -Gm₁m₂/r
  3) 引力加速度：g = GM/r²
  4) 第一宇宙速度（环绕速度）：v₁ = √(GM/r)
  5) 第二宇宙速度（逃逸速度）：v₂ = √(2GM/r)
  6) 第三宇宙速度：v₃ = √(2GM/r - 2GM/R_sun)
  7) 轨道周期（圆轨道）：T = 2π√(r³/(GM))
  8) 同步轨道半径：r = (GMT²/(4π²))^(1/3)

二、开普勒定律（Kepler's Laws）
  1) 开普勒第三定律：T² = (4π²/GM)·a³ → T = 2π√(a³/(GM))
  2) 半长轴由周期：a = (GMT²/(4π²))^(1/3)
  3) 椭圆轨道周长（近似）：C ≈ π[3(a+b) - √((3a+b)(a+3b))]
  4) 椭圆面积：A = πab
  5) 偏心率：e = √(1 - b²/a²)
  6) 近地点/远地点距离：r_p = a(1-e), r_a = a(1+e)

三、轨道参数与活力公式（Orbital Parameters & Vis-viva）
  1) 活力公式：v² = GM(2/r - 1/a)
  2) 轨道角动量：L = m√(GMa(1-e²))
  3) 轨道能量：E = -GMm/(2a)
  4) 偏近点角与真近点角关系（开普勒方程）
  5) 拉格朗日点（近似）
  6) 霍曼转移轨道

四、潮汐与引力场（Tides & Gravitational Fields）
  1) 潮汐力（差动力）：F_tidal = 2GMm·Δr/r³
  2) 潮汐隆起高度
  3) 洛希极限：d = R·(2ρ_M/ρ_m)^(1/3)
  4) 引力场强度：g = GM/r²
  5) 引力势：Φ = -GM/r
  6) 多体引力合力

五、相对论修正（Relativistic Corrections）
  1) 史瓦西半径：r_s = 2GM/c²
  2) 水星近日点进动：Δφ = 6πGM/(c²a(1-e²))
  3) 引力时间膨胀：t' = t·√(1 - r_s/r)
  4) 引力红移：z = √(1/(1 - r_s/r)) - 1
  5) 光线偏折角：θ = 4GM/(c²b)（b 为冲击参数）
  6) 轨道速度的相对论修正

设计原则：
  - 所有角度输入/输出均为弧度
  - 多参函数一律 _curry2/_curry3/_curry4 封装
  - 前缀 引力_ / 开普勒_ / 轨道_ / 潮汐_ / 相对论_ 区分子领域
  - 天体物理常量与太阳系数据作为常量注册

Source: `src\domains\celestial.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\celestial.py:65`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\celestial.py:71`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\celestial.py:79`

---

## `_引力_万有引力`

```matha
def _引力_万有引力(m1, m2, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m1` | `` | |
| `m2` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:168`

---

## `_引力_引力势能`

```matha
def _引力_引力势能(m1, m2, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m1` | `` | |
| `m2` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:170`

---

## `_引力_引力加速度`

```matha
def _引力_引力加速度(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:172`

---

## `_引力_环绕速度`

```matha
def _引力_环绕速度(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:174`

---

## `_引力_逃逸速度`

```matha
def _引力_逃逸速度(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:176`

---

## `_引力_第三宇宙速度`

```matha
def _引力_第三宇宙速度(M_planet, r_planet, M_sun, r_orbit)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M_planet` | `` | |
| `r_planet` | `` | |
| `M_sun` | `` | |
| `r_orbit` | `` | |

Source: `src\domains\celestial.py:178`

---

## `_引力_圆轨道周期`

```matha
def _引力_圆轨道周期(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:183`

---

## `_引力_同步轨道半径`

```matha
def _引力_同步轨道半径(M, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `T` | `` | |

Source: `src\domains\celestial.py:185`

---

## `_引力_轨道角速度`

```matha
def _引力_轨道角速度(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:187`

---

## `_开普勒_周期`

```matha
def _开普勒_周期(a, M)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `M` | `` | |

Source: `src\domains\celestial.py:195`

---

## `_开普勒_半长轴`

```matha
def _开普勒_半长轴(M, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `T` | `` | |

Source: `src\domains\celestial.py:197`

---

## `_开普勒_椭圆面积`

```matha
def _开普勒_椭圆面积(a, b)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `b` | `` | |

Source: `src\domains\celestial.py:199`

---

## `_开普勒_偏心率`

```matha
def _开普勒_偏心率(a, b)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `b` | `` | |

Source: `src\domains\celestial.py:201`

---

## `_开普勒_近地点`

```matha
def _开普勒_近地点(a, e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `e` | `` | |

Source: `src\domains\celestial.py:203`

---

## `_开普勒_远地点`

```matha
def _开普勒_远地点(a, e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `e` | `` | |

Source: `src\domains\celestial.py:205`

---

## `_开普勒_椭圆周长`

```matha
def _开普勒_椭圆周长(a, b)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `b` | `` | |

Source: `src\domains\celestial.py:207`

---

## `_开普勒_平均运动`

```matha
def _开普勒_平均运动(T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |

Source: `src\domains\celestial.py:210`

---

## `_轨道_活力速度`

```matha
def _轨道_活力速度(M, r, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |
| `a` | `` | |

Source: `src\domains\celestial.py:218`

---

## `_轨道_角动量`

```matha
def _轨道_角动量(M, m, a, e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `m` | `` | |
| `a` | `` | |
| `e` | `` | |

Source: `src\domains\celestial.py:220`

---

## `_轨道_总能量`

```matha
def _轨道_总能量(M, m, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `m` | `` | |
| `a` | `` | |

Source: `src\domains\celestial.py:222`

---

## `_轨道_近地点速度`

```matha
def _轨道_近地点速度(M, a, e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `a` | `` | |
| `e` | `` | |

Source: `src\domains\celestial.py:224`

---

## `_轨道_远地点速度`

```matha
def _轨道_远地点速度(M, a, e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `a` | `` | |
| `e` | `` | |

Source: `src\domains\celestial.py:226`

---

## `_轨道_霍曼转移`

```matha
def _轨道_霍曼转移(M, r1, r2)
```

**描述**: 从圆轨道 r1 转移到圆轨道 r2 的霍曼转移速度增量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r1` | `` | |
| `r2` | `` | |

Source: `src\domains\celestial.py:228`

---

## `_轨道_霍曼转移时间`

```matha
def _轨道_霍曼转移时间(M, r1, r2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r1` | `` | |
| `r2` | `` | |

Source: `src\domains\celestial.py:239`

---

## `_轨道_逃逸能量`

```matha
def _轨道_逃逸能量(M, m, r)
```

**描述**: 将质量 m 从 r 处逃逸到无穷远所需能量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `m` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:243`

---

## `_潮汐_潮汐力`

```matha
def _潮汐_潮汐力(M, m, dr, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `m` | `` | |
| `dr` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:253`

---

## `_潮汐_潮汐加速度`

```matha
def _潮汐_潮汐加速度(M, dr, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `dr` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:255`

---

## `_潮汐_洛希极限刚体`

```matha
def _潮汐_洛希极限刚体(R, rho_M, rho_m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `rho_M` | `` | |
| `rho_m` | `` | |

Source: `src\domains\celestial.py:257`

---

## `_潮汐_洛希极限流体`

```matha
def _潮汐_洛希极限流体(R, rho_M, rho_m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `rho_M` | `` | |
| `rho_m` | `` | |

Source: `src\domains\celestial.py:259`

---

## `_潮汐_引力势`

```matha
def _潮汐_引力势(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:261`

---

## `_潮汐_引力场强度`

```matha
def _潮汐_引力场强度(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:263`

---

## `_相对论_史瓦西半径`

```matha
def _相对论_史瓦西半径(M)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |

Source: `src\domains\celestial.py:271`

---

## `_相对论_近日点进动`

```matha
def _相对论_近日点进动(M, a, e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `a` | `` | |
| `e` | `` | |

Source: `src\domains\celestial.py:273`

---

## `_相对论_时间膨胀`

```matha
def _相对论_时间膨胀(t, M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `t` | `` | |
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:275`

---

## `_相对论_引力红移`

```matha
def _相对论_引力红移(M, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `r` | `` | |

Source: `src\domains\celestial.py:279`

---

## `_相对论_光线偏折`

```matha
def _相对论_光线偏折(M, b)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `b` | `` | |

Source: `src\domains\celestial.py:283`

---

## `_相对论_黑洞温度`

```matha
def _相对论_黑洞温度(M)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |

Source: `src\domains\celestial.py:285`

---

## `_register_celestial`

```matha
def _register_celestial(builtins: dict) -> None
```

**描述**: 将天体力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（quantum 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\celestial.py:295`

---

## `_celestial_symtab_names`

```matha
def _celestial_symtab_names() -> list[str]
```

**描述**: 返回天体力学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\celestial.py:360`

---

# `domains.chaos_fractal`

```matha

```

**描述**: Matha 混沌理论与分型领域模块。

覆盖：
  1) Lorenz 吸引子
  2) Hénon 映射
  3) Logistic 映射
  4) Mandelbrot / Julia 集
  5) 分形维数
  6) Lyapunov 指数

Source: `src\domains\chaos_fractal.py:1`

---

## `lorenz_deriv`

```matha
def lorenz_deriv(x: float, y: float, z: float, sigma: float, rho: float, beta: float) -> tuple[float, float, float]
```

**描述**: Lorenz 方程导数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `y` | `float` | |
| `z` | `float` | |
| `sigma` | `float` | |
| `rho` | `float` | |
| `beta` | `float` | |

**返回**: `tuple[float, float, float]`

Source: `src\domains\chaos_fractal.py:22`

---

## `lorenz_attractor`

```matha
def lorenz_attractor(x0: float, y0: float, z0: float, dt: float, steps: int, sigma: float, rho: float, beta: float) -> list[tuple[float, float, float]]
```

**描述**: 计算 Lorenz 吸引子轨迹。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x0` | `float` | |
| `y0` | `float` | |
| `z0` | `float` | |
| `dt` | `float` | |
| `steps` | `int` | |
| `sigma` | `float` | |
| `rho` | `float` | |
| `beta` | `float` | |

**返回**: `list[tuple[float, float, float]]`

Source: `src\domains\chaos_fractal.py:31`

---

## `henon_map`

```matha
def henon_map(x: float, y: float, a: float, b: float) -> tuple[float, float]
```

**描述**: Hénon 映射。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `y` | `float` | |
| `a` | `float` | |
| `b` | `float` | |

**返回**: `tuple[float, float]`

Source: `src\domains\chaos_fractal.py:50`

---

## `henon_attractor`

```matha
def henon_attractor(x0: float, y0: float, a: float, b: float, steps: int) -> list[tuple[float, float]]
```

**描述**: Hénon 吸引子轨迹。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x0` | `float` | |
| `y0` | `float` | |
| `a` | `float` | |
| `b` | `float` | |
| `steps` | `int` | |

**返回**: `list[tuple[float, float]]`

Source: `src\domains\chaos_fractal.py:57`

---

## `logistic_map`

```matha
def logistic_map(x: float, r: float) -> float
```

**描述**: Logistic 映射：x_{n+1} = r * x_n * (1 - x_n)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `r` | `float` | |

**返回**: `float`

Source: `src\domains\chaos_fractal.py:72`

---

## `logistic_orbit`

```matha
def logistic_orbit(x0: float, r: float, steps: int) -> list[float]
```

**描述**: Logistic 映射轨道。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x0` | `float` | |
| `r` | `float` | |
| `steps` | `int` | |

**返回**: `list[float]`

Source: `src\domains\chaos_fractal.py:77`

---

## `mandelbrot_iter`

```matha
def mandelbrot_iter(cx: float, cy: float, max_iter: int) -> int
```

**描述**: Mandelbrot 迭代。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cx` | `float` | |
| `cy` | `float` | |
| `max_iter` | `int` | |

**返回**: `int`

Source: `src\domains\chaos_fractal.py:91`

---

## `mandelbrot_set`

```matha
def mandelbrot_set(width: int, height: int, x_min: float, x_max: float, y_min: float, y_max: float, max_iter: int) -> list[list[int]]
```

**描述**: 生成 Mandelbrot 集图像数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `width` | `int` | |
| `height` | `int` | |
| `x_min` | `float` | |
| `x_max` | `float` | |
| `y_min` | `float` | |
| `y_max` | `float` | |
| `max_iter` | `int` | |

**返回**: `list[list[int]]`

Source: `src\domains\chaos_fractal.py:103`

---

## `julia_iter`

```matha
def julia_iter(cx: float, cy: float, px: float, py: float, max_iter: int) -> int
```

**描述**: Julia 集迭代。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cx` | `float` | |
| `cy` | `float` | |
| `px` | `float` | |
| `py` | `float` | |
| `max_iter` | `int` | |

**返回**: `int`

Source: `src\domains\chaos_fractal.py:123`

---

## `julia_set`

```matha
def julia_set(width: int, height: int, cx: float, cy: float, max_iter: int) -> list[list[int]]
```

**描述**: 生成 Julia 集图像数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `width` | `int` | |
| `height` | `int` | |
| `cx` | `float` | |
| `cy` | `float` | |
| `max_iter` | `int` | |

**返回**: `list[list[int]]`

Source: `src\domains\chaos_fractal.py:135`

---

## `box_counting_dim`

```matha
def box_counting_dim(points: list[tuple[float, float]], min_scale: float, max_scale: float) -> float
```

**描述**: 盒子计数法估算分形维数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `points` | `list[tuple[float, float]]` | |
| `min_scale` | `float` | |
| `max_scale` | `float` | |

**返回**: `float`

Source: `src\domains\chaos_fractal.py:154`

---

## `lyapunov_exponent`

```matha
def lyapunov_exponent(logistic_r: float, x0: float, steps: int) -> float
```

**描述**: 计算 Logistic 映射的 Lyapunov 指数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `logistic_r` | `float` | |
| `x0` | `float` | |
| `steps` | `int` | |

**返回**: `float`

Source: `src\domains\chaos_fractal.py:192`

---

## `_register_chaos_fractal`

```matha
def _register_chaos_fractal(builtins: dict) -> None
```

**描述**: 注册混沌理论与分型内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\chaos_fractal.py:232`

---

## `_chaos_fractal_symtab_names`

```matha
def _chaos_fractal_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\chaos_fractal.py:248`

---

# `domains.chemistry`

```matha

```

**描述**: Matha 化学领域模块：普通化学 + 物理化学 + 有机化学。

覆盖：
  1) 普通化学：摩尔质量、溶液浓度、pH 计算、理想气体定律
  2) 物理化学：热力学（ΔH/ΔG/ΔS）、化学平衡（K/ΔG°）、电化学（Nernst）
  3) 有机化学：分子量计算、官能团识别、同分异构体计数

所有函数以柯里化 Python callable 注册到解释器 builtins。

Source: `src\domains\chemistry.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\chemistry.py:18`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\chemistry.py:23`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\chemistry.py:30`

---

## `_摩尔质量`

```matha
def _摩尔质量(formula: str) -> float
```

**描述**: 计算化合物摩尔质量（g/mol）。支持简化公式如 H2O、C6H12O6。

| 参数 | 类型 | 说明 |
|------|------|------|
| `formula` | `str` | |

**返回**: `float`

Source: `src\domains\chemistry.py:71`

---

## `_理想气体方程`

```matha
def _理想气体方程(P, V, n, T)
```

**描述**: 理想气体状态方程 PV=nRT，已知三个求第四个。

参数：四个参数中一个为 None，其余三个为已知值。
P: Pa, V: m³, n: mol, T: K

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `V` | `` | |
| `n` | `` | |
| `T` | `` | |

Source: `src\domains\chemistry.py:98`

---

## `_溶液浓度`

```matha
def _溶液浓度(mol_solute, vol_L)
```

**描述**: 摩尔浓度 c = n/V。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mol_solute` | `` | |
| `vol_L` | `` | |

Source: `src\domains\chemistry.py:115`

---

## `_pH计算`

```matha
def _pH计算(H_conc)
```

**描述**: pH = -log10[H+]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `H_conc` | `` | |

Source: `src\domains\chemistry.py:120`

---

## `_pOH计算`

```matha
def _pOH计算(OH_conc)
```

**描述**: pOH = -log10[OH-]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `OH_conc` | `` | |

Source: `src\domains\chemistry.py:127`

---

## `_Henderson_方程`

```matha
def _Henderson_方程(pKa, acid, base)
```

**描述**: Henderson-Hasselbalch 方程：pH = pKa + log10([base]/[acid])。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pKa` | `` | |
| `acid` | `` | |
| `base` | `` | |

Source: `src\domains\chemistry.py:134`

---

## `_Gibbs自由能`

```matha
def _Gibbs自由能(ΔH, ΔS, T)
```

**描述**: ΔG = ΔH - TΔS。ΔH: J/mol, ΔS: J/(mol·K), T: K。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ΔH` | `` | |
| `ΔS` | `` | |
| `T` | `` | |

Source: `src\domains\chemistry.py:145`

---

## `_平衡常数`

```matha
def _平衡常数(ΔG_std, T)
```

**描述**: K = exp(-ΔG°/RT)。ΔG_std: J/mol。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ΔG_std` | `` | |
| `T` | `` | |

Source: `src\domains\chemistry.py:150`

---

## `_Arrhenius方程`

```matha
def _Arrhenius方程(A, Ea, T)
```

**描述**: k = A·exp(-Ea/RT)。A: 频率因子, Ea: 活化能 J/mol。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |
| `Ea` | `` | |
| `T` | `` | |

Source: `src\domains\chemistry.py:157`

---

## `_Nernst方程`

```matha
def _Nernst方程(E_std, n, Q)
```

**描述**: Nernst 方程：E = E° - (RT/nF)·lnQ，25°C 简化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `E_std` | `` | |
| `n` | `` | |
| `Q` | `` | |

Source: `src\domains\chemistry.py:164`

---

## `_反应速率常溫比`

```matha
def _反应速率常溫比(k1, T1, Ea)
```

**描述**: 已知 k1 在 T1 下的速率常数，求 Ea 下的 k2（T2=298K）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `k1` | `` | |
| `T1` | `` | |
| `Ea` | `` | |

Source: `src\domains\chemistry.py:170`

---

## `_烷烃通式`

```matha
def _烷烃通式(n)
```

**描述**: CnH(2n+2)，返回分子式字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\chemistry.py:180`

---

## `_烯烃通式`

```matha
def _烯烃通式(n)
```

**描述**: CnH2n，返回分子式字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\chemistry.py:185`

---

## `_炔烃通式`

```matha
def _炔烃通式(n)
```

**描述**: CnH(2n-2)，返回分子式字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\chemistry.py:190`

---

## `_同分异构体数`

```matha
def _同分异构体数(n)
```

**描述**: 烷烃 CnH(2n+2) 的同分异构体数目（n≤10 精确值）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\chemistry.py:195`

---

## `_不饱和度`

```matha
def _不饱和度(formula)
```

**描述**: 计算不饱和度（双键等价数）。公式格式如 C6H12O6。

| 参数 | 类型 | 说明 |
|------|------|------|
| `formula` | `` | |

Source: `src\domains\chemistry.py:202`

---

## `_register_chemistry`

```matha
def _register_chemistry(builtins: dict) -> None
```

**描述**: 将化学领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\chemistry.py:227`

---

## `_curry1`

```matha
def _curry1(func)
```

**描述**: 一参柯里化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\chemistry.py:263`

---

## `_register_chemistry_symtab_names`

```matha
def _register_chemistry_symtab_names() -> list[str]
```

**描述**: 返回化学领域所有内建名。

**返回**: `list[str]`

Source: `src\domains\chemistry.py:270`

---

# `domains.comp_chem`

```matha

```

**描述**: Matha 计算化学领域模块：分子模拟、量子化学、反应动力学。

覆盖：
  1) 分子轨道能量
  2) 反应活化能
  3) 键长计算
  4) 振动频率
  5) 热力学稳定性
  6) 溶剂化能

Source: `src\domains\comp_chem.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\comp_chem.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\comp_chem.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\comp_chem.py:25`

---

## `_分子轨道能量`

```matha
def _分子轨道能量(Z, n)
```

**描述**: 类氢原子轨道能量（eV）。E = -13.6 * Z² / n²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Z` | `` | |
| `n` | `` | |

Source: `src\domains\comp_chem.py:32`

---

## `_反应活化能`

```matha
def _反应活化能(温度, 指前因子, 速率常数)
```

**描述**: Arrhenius方程反算活化能（kJ/mol）。Ea = R*T*ln(A/k)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `温度` | `` | |
| `指前因子` | `` | |
| `速率常数` | `` | |

Source: `src\domains\comp_chem.py:39`

---

## `_键长计算`

```matha
def _键长计算(原子1半径, 原子2半径, 键级)
```

**描述**: 共价键长估算（pm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `原子1半径` | `` | |
| `原子2半径` | `` | |
| `键级` | `` | |

Source: `src\domains\comp_chem.py:47`

---

## `_振动频率`

```matha
def _振动频率(力常数_Nm, 约化质量_kg)
```

**描述**: 简谐振动频率（Hz）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `力常数_Nm` | `` | |
| `约化质量_kg` | `` | |

Source: `src\domains\comp_chem.py:54`

---

## `_热力学稳定性`

```matha
def _热力学稳定性(生成焓_kJ, 熵_J_K, 温度_K)
```

**描述**: 吉布斯自由能判据。ΔG = ΔH - TΔS。

| 参数 | 类型 | 说明 |
|------|------|------|
| `生成焓_kJ` | `` | |
| `熵_J_K` | `` | |
| `温度_K` | `` | |

Source: `src\domains\comp_chem.py:61`

---

## `_溶剂化能`

```matha
def _溶剂化能(电荷, 半径_A, 介电常数)
```

**描述**: Born溶剂化能（kJ/mol）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `电荷` | `` | |
| `半径_A` | `` | |
| `介电常数` | `` | |

Source: `src\domains\comp_chem.py:68`

---

## `_register_comp_chem`

```matha
def _register_comp_chem(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\comp_chem.py:82`

---

## `_comp_chem_symtab_names`

```matha
def _comp_chem_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\comp_chem.py:91`

---

# `domains.computer_science`

```matha

```

**描述**: Matha 计算机科学领域模块：算法复杂度、数据结构、信息论、图论。

覆盖：
  1) 算法复杂度：大O计算、递归方程求解
  2) 数据结构：栈/队列/链表操作模拟
  3) 信息论：熵、互信息、编码效率
  4) 图论：遍历计数、最短路径估算
  5) 离散数学：布尔代数、集合运算、逻辑电路

Source: `src\domains\computer_science.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\computer_science.py:19`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\computer_science.py:24`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\computer_science.py:29`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\computer_science.py:36`

---

## `_大O比较`

```matha
def _大O比较(n, threshold)
```

**描述**: 根据 n 的大小估算可用算法复杂度等级。

n ≤ 10:      O(n!) 可行
n ≤ 20:      O(2^n) 可行
n ≤ 1000:    O(n³)  可行
n ≤ 100000:  O(n²)  可行
更大:        O(n log n) 或 O(n) 必须

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `threshold` | `` | |

Source: `src\domains\computer_science.py:50`

---

## `_递归深度估算`

```matha
def _递归深度估算(T_n, n)
```

**描述**: 估算递归算法的调用次数（简化版）。

T(n) = T(n-1) + O(1)  →  O(n)
T(n) = 2·T(n-1) + O(1) →  O(2^n)
T(n) = T(n-1) + T(n-2)  →  O(φ^n)  斐波那契

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_n` | `` | |
| `n` | `` | |

Source: `src\domains\computer_science.py:74`

---

## `_香农熵`

```matha
def _香农熵(probabilities)
```

**描述**: 计算离散随机变量的香农熵 H = -Σ p·log2(p)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `probabilities` | `` | |

Source: `src\domains\computer_science.py:91`

---

## `_信息量`

```matha
def _信息量(probability)
```

**描述**: 单个事件的自信息 I = -log2(p)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `probability` | `` | |

Source: `src\domains\computer_science.py:102`

---

## `_编码效率`

```matha
def _编码效率(熵, 平均码长)
```

**描述**: 编码效率 η = H/L，衡量编码接近理论极限的程度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `熵` | `` | |
| `平均码长` | `` | |

Source: `src\domains\computer_science.py:109`

---

## `_完全图边数`

```matha
def _完全图边数(n)
```

**描述**: n 个顶点的完全图边数：n(n-1)/2。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\computer_science.py:120`

---

## `_树边数`

```matha
def _树边数(n)
```

**描述**: n 个顶点的树边数：n-1。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\computer_science.py:125`

---

## `_dfs遍历数`

```matha
def _dfs遍历数(n, branching)
```

**描述**: DFS 遍历节点数上界估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `branching` | `` | |

Source: `src\domains\computer_science.py:130`

---

## `_最短路径估算`

```matha
def _最短路径估算(n, m)
```

**描述**: Dijkstra 最短路径复杂度估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `m` | `` | |

Source: `src\domains\computer_science.py:135`

---

## `_德摩根定律_not_and`

```matha
def _德摩根定律_not_and(a, b)
```

**描述**: ¬(a ∧ b) ≡ ¬a ∨ ¬b。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `b` | `` | |

Source: `src\domains\computer_science.py:144`

---

## `_德摩根定律_not_or`

```matha
def _德摩根定律_not_or(a, b)
```

**描述**: ¬(a ∨ b) ≡ ¬a ∧ ¬b。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `b` | `` | |

Source: `src\domains\computer_science.py:149`

---

## `_蕴含等价`

```matha
def _蕴含等价(p, q)
```

**描述**: p → q ≡ ¬p ∨ q。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `q` | `` | |

Source: `src\domains\computer_science.py:154`

---

## `_双蕴含等价`

```matha
def _双蕴含等价(p, q)
```

**描述**: p ↔ q ≡ (p → q) ∧ (q → p)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `q` | `` | |

Source: `src\domains\computer_science.py:159`

---

## `_真值表行数`

```matha
def _真值表行数(n)
```

**描述**: n 个命题变元的真值表行数：2^n。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\computer_science.py:164`

---

## `_栈操作操作`

```matha
def _栈操作操作(操作序列)
```

**描述**: 模拟栈操作序列，返回最终栈状态和错误次数。

操作序列：["push", 1, "push", 2, "pop", ...]

| 参数 | 类型 | 说明 |
|------|------|------|
| `操作序列` | `` | |

Source: `src\domains\computer_science.py:173`

---

## `_队列操作操作`

```matha
def _队列操作操作(操作序列)
```

**描述**: 模拟队列操作序列，返回最终队列状态和错误次数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `操作序列` | `` | |

Source: `src\domains\computer_science.py:205`

---

## `_register_computer_science`

```matha
def _register_computer_science(builtins: dict) -> None
```

**描述**: 将计算机科学领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\computer_science.py:238`

---

## `_register_computer_science_symtab_names`

```matha
def _register_computer_science_symtab_names() -> list[str]
```

**描述**: 返回计算机科学领域所有内建名。

**返回**: `list[str]`

Source: `src\domains\computer_science.py:267`

---

# `domains.creative_coding`

```matha

```

**描述**: Matha 创意编程与艺术表达领域模块。

覆盖：
  1) Perlin / Simplex 噪声
  2) 流场
  3) 粒子系统
  4) 分形艺术
  5) 色彩系统
  6) 音频可视化

Source: `src\domains\creative_coding.py:1`

---

## `PerlinNoise`

```matha
class PerlinNoise
```

**描述**: Perlin 噪声生成器。

Source: `src\domains\creative_coding.py:23`

---

### `__init__`

```matha
def __init__(self, seed: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `seed` | `int` | |

Source: `src\domains\creative_coding.py:26`

---

### `_shuffle`

```matha
def _shuffle(self, seed: int) -> list[int]
```

**描述**: 生成排列。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `seed` | `int` | |

**返回**: `list[int]`

Source: `src\domains\creative_coding.py:29`

---

### `_fade`

```matha
def _fade(self, t: float) -> float
```

**描述**: Fade 曲线：6t^5 - 15t^4 + 10t^3。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `t` | `float` | |

**返回**: `float`

Source: `src\domains\creative_coding.py:39`

---

### `_lerp`

```matha
def _lerp(self, a: float, b: float, t: float) -> float
```

**描述**: 线性插值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `a` | `float` | |
| `b` | `float` | |
| `t` | `float` | |

**返回**: `float`

Source: `src\domains\creative_coding.py:43`

---

### `_grad`

```matha
def _grad(self, hash_val: int, x: float, y: float) -> float
```

**描述**: 梯度点积。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `hash_val` | `int` | |
| `x` | `float` | |
| `y` | `float` | |

**返回**: `float`

Source: `src\domains\creative_coding.py:47`

---

### `noise_2d`

```matha
def noise_2d(self, x: float, y: float) -> float
```

**描述**: 2D Perlin 噪声。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |
| `y` | `float` | |

**返回**: `float`

Source: `src\domains\creative_coding.py:55`

---

### `noise_3d`

```matha
def noise_3d(self, x: float, y: float, z: float) -> float
```

**描述**: 3D Perlin 噪声。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |
| `y` | `float` | |
| `z` | `float` | |

**返回**: `float`

Source: `src\domains\creative_coding.py:71`

---

## `simplex_noise_2d`

```matha
def simplex_noise_2d(x: float, y: float) -> float
```

**描述**: 简化 Simplex 噪声。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `y` | `float` | |

**返回**: `float`

Source: `src\domains\creative_coding.py:80`

---

## `FlowField`

```matha
class FlowField
```

**描述**: 流场。

Source: `src\domains\creative_coding.py:90`

---

### `__init__`

```matha
def __init__(self, width: int, height: int, scale: float)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `width` | `int` | |
| `height` | `int` | |
| `scale` | `float` | |

Source: `src\domains\creative_coding.py:93`

---

### `_generate`

```matha
def _generate(self) -> None
```

**描述**: 生成流场。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\domains\creative_coding.py:100`

---

### `get_direction`

```matha
def get_direction(self, x: int, y: int) -> tuple[float, float]
```

**描述**: 获取指定位置的流向。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `int` | |
| `y` | `int` | |

**返回**: `tuple[float, float]`

Source: `src\domains\creative_coding.py:108`

---

## `particle_system`

```matha
def particle_system(n: int, width: float, height: float, source_x: float, source_y: float) -> list[dict]
```

**描述**: 初始化粒子系统。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `width` | `float` | |
| `height` | `float` | |
| `source_x` | `float` | |
| `source_y` | `float` | |

**返回**: `list[dict]`

Source: `src\domains\creative_coding.py:119`

---

## `particle_update`

```matha
def particle_update(particles: list[dict], dt: float, gravity: float) -> list[dict]
```

**描述**: 更新粒子。

| 参数 | 类型 | 说明 |
|------|------|------|
| `particles` | `list[dict]` | |
| `dt` | `float` | |
| `gravity` | `float` | |

**返回**: `list[dict]`

Source: `src\domains\creative_coding.py:137`

---

## `fractal_barnsley_fern`

```matha
def fractal_barnsley_fern(n: int) -> list[tuple[float, float]]
```

**描述**: Barnsley 蕨类分形。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `list[tuple[float, float]]`

Source: `src\domains\creative_coding.py:154`

---

## `fractal_sierpinski`

```matha
def fractal_sierpinski(n: int) -> list[tuple[float, float]]
```

**描述**: Sierpinski 三角形。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `list[tuple[float, float]]`

Source: `src\domains\creative_coding.py:177`

---

## `color_hsl_to_rgb`

```matha
def color_hsl_to_rgb(h: float, s: float, l: float) -> tuple[float, float, float]
```

**描述**: HSL 转 RGB。

| 参数 | 类型 | 说明 |
|------|------|------|
| `h` | `float` | |
| `s` | `float` | |
| `l` | `float` | |

**返回**: `tuple[float, float, float]`

Source: `src\domains\creative_coding.py:195`

---

## `color_lerp`

```matha
def color_lerp(c1: tuple[float, float, float], c2: tuple[float, float, float], t: float) -> tuple[float, float, float]
```

**描述**: 颜色插值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `c1` | `tuple[float, float, float]` | |
| `c2` | `tuple[float, float, float]` | |
| `t` | `float` | |

**返回**: `tuple[float, float, float]`

Source: `src\domains\creative_coding.py:216`

---

## `audio_reactive`

```matha
def audio_reactive(freq_bands: list[float], energy: float, sensitivity: float) -> list[float]
```

**描述**: 音频反应式可视化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `freq_bands` | `list[float]` | |
| `energy` | `float` | |
| `sensitivity` | `float` | |

**返回**: `list[float]`

Source: `src\domains\creative_coding.py:229`

---

## `visual_midi`

```matha
def visual_midi(note: int, frequency: float, duration: float) -> dict
```

**描述**: MIDI 音符可视化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `note` | `int` | |
| `frequency` | `float` | |
| `duration` | `float` | |

**返回**: `dict`

Source: `src\domains\creative_coding.py:234`

---

## `_register_creative_coding`

```matha
def _register_creative_coding(builtins: dict) -> None
```

**描述**: 注册创意编程内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\creative_coding.py:263`

---

## `_creative_coding_symtab_names`

```matha
def _creative_coding_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\creative_coding.py:279`

---

# `domains.digital_rights`

```matha

```

**描述**: Matha 数字版权领域模块：水印技术、区块链存证、访问控制。

覆盖：
  1) 水印嵌入强度
  2) 版权保护指数
  3) 访问控制粒度
  4) 哈希碰撞概率
  5) 密钥轮换周期
  6) 数字指纹

Source: `src\domains\digital_rights.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\digital_rights.py:18`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\digital_rights.py:22`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\digital_rights.py:26`

---

## `_水印嵌入强度`

```matha
def _水印嵌入强度(载体大小_bytes, 水印比特数, 嵌入密度)
```

**描述**: 水印嵌入强度评估。

| 参数 | 类型 | 说明 |
|------|------|------|
| `载体大小_bytes` | `` | |
| `水印比特数` | `` | |
| `嵌入密度` | `` | |

Source: `src\domains\digital_rights.py:33`

---

## `_版权保护指数`

```matha
def _版权保护指数(存证数, 验证通过率, 篡改检测数)
```

**描述**: 版权保护综合指数（0-100）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `存证数` | `` | |
| `验证通过率` | `` | |
| `篡改检测数` | `` | |

Source: `src\domains\digital_rights.py:40`

---

## `_访问控制粒度`

```matha
def _访问控制粒度(角色数, 权限数, 策略数)
```

**描述**: 访问控制粒度评估。

| 参数 | 类型 | 说明 |
|------|------|------|
| `角色数` | `` | |
| `权限数` | `` | |
| `策略数` | `` | |

Source: `src\domains\digital_rights.py:50`

---

## `_哈希碰撞概率`

```matha
def _哈希碰撞概率(哈希位数, 数据量)
```

**描述**: 生日攻击碰撞概率估算。P ≈ n² / 2^（b+1）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `哈希位数` | `` | |
| `数据量` | `` | |

Source: `src\domains\digital_rights.py:57`

---

## `_密钥轮换周期`

```matha
def _密钥轮换周期(密钥长度_bit, 计算能力_次秒)
```

**描述**: 密钥安全轮换周期（天）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `密钥长度_bit` | `` | |
| `计算能力_次秒` | `` | |

Source: `src\domains\digital_rights.py:64`

---

## `_数字指纹`

```matha
def _数字指纹(数据)
```

**描述**: 生成数据的SHA-256指纹（十六进制前16位）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `数据` | `` | |

Source: `src\domains\digital_rights.py:72`

---

## `_register_digital_rights`

```matha
def _register_digital_rights(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\digital_rights.py:84`

---

## `_digital_rights_symtab_names`

```matha
def _digital_rights_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\digital_rights.py:93`

---

# `domains.dynamics`

```matha

```

**描述**: Matha 机械领域模块：动力学（Dynamics）。

基于运动学 + mathlib 数学地基，演化动力学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、牛顿运动定律（Newton's Laws）
  1) 牛顿第二定律：F = ma；加速度 a = F/m
  2) 滑动摩擦力：f = μN；最大静摩擦 f_max = μ_s·N
  3) 牛顿第三定律：作用力与反作用力等大反向
  4) 合力（同向/垂直分量分解）

二、动量定理（Momentum & Impulse）
  1) 动量 p = mv
  2) 冲量 I = Ft
  3) 动量定理：Δp = Ft → 末动量 p₂ = p₁ + Ft
  4) 动量守恒（两体正碰）：m₁v₁ + m₂v₂ = m₁v₁' + m₂v₂'
  5) 完全弹性碰撞恢复系数 e = (v₂'-v₁')/(v₁-v₂)

三、功与能（Work & Energy）
  1) 功 W = Fs cosθ；功率 P = W/t = Fv
  2) 动能 E_k = ½mv²
  3) 重力势能 E_p = mgh
  4) 弹性势能 E_s = ½kx²
  5) 动能定理：W合 = ΔE_k = ½mv₂² - ½mv₁²
  6) 机械能守恒：E_k + E_p = const（无摩擦）

四、转动动力学（Rotational Dynamics）
  1) 力矩 M = r×F = rF sinθ
  2) 转动惯量：质点 I=mr²；细杆 I=⅓mL²；圆盘 I=½mr²；圆环 I=mr²；实心球 I=⅖mr²
  3) 转动定律 M = Iα（类比 F=ma）
  4) 角动量 L = Iω；角动量守恒
  5) 转动动能 E_rot = ½Iω²
  6) 平行轴定理 I = I_cm + md²

五、机械振动（Oscillations）
  1) 简谐振动周期：T = 2π√(m/k)；频率 f = 1/T；角频率 ω = 2π/T
  2) 弹簧振子：位移 x(t) = A cos(ωt)
  3) 单摆周期：T = 2π√(L/g)
  4) 复摆周期：T = 2π√(I/(mgd))
  5) 简谐振动总能量：E = ½kA²
  6) 阻尼振动角频率：ω_d = √(ω₀² - β²)

设计原则：
  - 所有函数返回纯数值（float/int），不抛语义错（除以零等 Python 自身抛）
  - 多参函数一律 _curry2/_curry3 封装，与 Matha 柯里化语义一致
  - 前缀 力_ / 动量_ / 功_ / 转动_ / 振动_ 区分子领域
  - 与运动学共享 mathlib 的 sin/cos/sqrt/pi/g 等

Source: `src\domains\dynamics.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\dynamics.py:60`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\dynamics.py:66`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\dynamics.py:74`

---

## `_牛顿_力`

```matha
def _牛顿_力(m, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `a` | `` | |

Source: `src\domains\dynamics.py:89`

---

## `_牛顿_加速度`

```matha
def _牛顿_加速度(F, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `m` | `` | |

Source: `src\domains\dynamics.py:90`

---

## `_牛顿_质量`

```matha
def _牛顿_质量(F, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `a` | `` | |

Source: `src\domains\dynamics.py:91`

---

## `_摩擦_滑动摩擦力`

```matha
def _摩擦_滑动摩擦力(mu, N)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |
| `N` | `` | |

Source: `src\domains\dynamics.py:94`

---

## `_摩擦_最大静摩擦`

```matha
def _摩擦_最大静摩擦(mu_s, N)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu_s` | `` | |
| `N` | `` | |

Source: `src\domains\dynamics.py:95`

---

## `_合力_同向`

```matha
def _合力_同向(F1, F2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F1` | `` | |
| `F2` | `` | |

Source: `src\domains\dynamics.py:98`

---

## `_合力_垂直`

```matha
def _合力_垂直(F1, F2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F1` | `` | |
| `F2` | `` | |

Source: `src\domains\dynamics.py:99`

---

## `_动量_动量`

```matha
def _动量_动量(m, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |

Source: `src\domains\dynamics.py:107`

---

## `_动量_冲量`

```matha
def _动量_冲量(F, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `t` | `` | |

Source: `src\domains\dynamics.py:109`

---

## `_动量_末动量`

```matha
def _动量_末动量(p1, F, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `F` | `` | |
| `t` | `` | |

Source: `src\domains\dynamics.py:111`

---

## `_动量_碰后速度1`

```matha
def _动量_碰后速度1(m1, v1, m2, v2, v2_after)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m1` | `` | |
| `v1` | `` | |
| `m2` | `` | |
| `v2` | `` | |
| `v2_after` | `` | |

Source: `src\domains\dynamics.py:115`

---

## `_弹性碰撞_速度1`

```matha
def _弹性碰撞_速度1(m1, v1, m2, v2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m1` | `` | |
| `v1` | `` | |
| `m2` | `` | |
| `v2` | `` | |

Source: `src\domains\dynamics.py:120`

---

## `_弹性碰撞_速度2`

```matha
def _弹性碰撞_速度2(m1, v1, m2, v2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m1` | `` | |
| `v1` | `` | |
| `m2` | `` | |
| `v2` | `` | |

Source: `src\domains\dynamics.py:122`

---

## `_碰撞_恢复系数`

```matha
def _碰撞_恢复系数(v1, v2, v1_after, v2_after)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v1` | `` | |
| `v2` | `` | |
| `v1_after` | `` | |
| `v2_after` | `` | |

Source: `src\domains\dynamics.py:125`

---

## `_功_功`

```matha
def _功_功(F, s, theta_rad)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `s` | `` | |
| `theta_rad` | `` | |

Source: `src\domains\dynamics.py:134`

---

## `_功_功率`

```matha
def _功_功率(W, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `W` | `` | |
| `t` | `` | |

Source: `src\domains\dynamics.py:136`

---

## `_功_功率力速`

```matha
def _功_功率力速(F, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `v` | `` | |

Source: `src\domains\dynamics.py:138`

---

## `_能量_动能`

```matha
def _能量_动能(m, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |

Source: `src\domains\dynamics.py:140`

---

## `_能量_重力势能`

```matha
def _能量_重力势能(m, h, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `h` | `` | |
| `g_val` | `` | |

Source: `src\domains\dynamics.py:142`

---

## `_能量_弹性势能`

```matha
def _能量_弹性势能(k, x)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k` | `` | |
| `x` | `` | |

Source: `src\domains\dynamics.py:144`

---

## `_能量_动能定理`

```matha
def _能量_动能定理(m, v1, v2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v1` | `` | |
| `v2` | `` | |

Source: `src\domains\dynamics.py:146`

---

## `_能量_守恒末速度`

```matha
def _能量_守恒末速度(v1, h1, h2, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v1` | `` | |
| `h1` | `` | |
| `h2` | `` | |
| `g_val` | `` | |

Source: `src\domains\dynamics.py:151`

---

## `_转动_力矩`

```matha
def _转动_力矩(r, F, theta_rad)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |
| `F` | `` | |
| `theta_rad` | `` | |

Source: `src\domains\dynamics.py:160`

---

## `_转动_角加速度`

```matha
def _转动_角加速度(M, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `I` | `` | |

Source: `src\domains\dynamics.py:162`

---

## `_转动惯量_质点`

```matha
def _转动惯量_质点(m, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `r` | `` | |

Source: `src\domains\dynamics.py:165`

---

## `_转动惯量_细杆端点`

```matha
def _转动惯量_细杆端点(m, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `L` | `` | |

Source: `src\domains\dynamics.py:166`

---

## `_转动惯量_细杆中心`

```matha
def _转动惯量_细杆中心(m, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `L` | `` | |

Source: `src\domains\dynamics.py:167`

---

## `_转动惯量_圆盘`

```matha
def _转动惯量_圆盘(m, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `r` | `` | |

Source: `src\domains\dynamics.py:168`

---

## `_转动惯量_圆环`

```matha
def _转动惯量_圆环(m, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `r` | `` | |

Source: `src\domains\dynamics.py:169`

---

## `_转动惯量_实心球`

```matha
def _转动惯量_实心球(m, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `r` | `` | |

Source: `src\domains\dynamics.py:170`

---

## `_转动惯量_空心球`

```matha
def _转动惯量_空心球(m, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `r` | `` | |

Source: `src\domains\dynamics.py:171`

---

## `_转动惯量_圆柱`

```matha
def _转动惯量_圆柱(m, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `r` | `` | |

Source: `src\domains\dynamics.py:172`

---

## `_转动惯量_平行轴`

```matha
def _转动惯量_平行轴(I_cm, m, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I_cm` | `` | |
| `m` | `` | |
| `d` | `` | |

Source: `src\domains\dynamics.py:175`

---

## `_转动_角动量`

```matha
def _转动_角动量(I, omega)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `omega` | `` | |

Source: `src\domains\dynamics.py:178`

---

## `_转动_角动量守恒`

```matha
def _转动_角动量守恒(I1, omega1, I2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I1` | `` | |
| `omega1` | `` | |
| `I2` | `` | |

Source: `src\domains\dynamics.py:180`

---

## `_转动_转动动能`

```matha
def _转动_转动动能(I, omega)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `omega` | `` | |

Source: `src\domains\dynamics.py:182`

---

## `_振动_周期`

```matha
def _振动_周期(m, k)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `k` | `` | |

Source: `src\domains\dynamics.py:190`

---

## `_振动_频率`

```matha
def _振动_频率(T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |

Source: `src\domains\dynamics.py:192`

---

## `_振动_角频率`

```matha
def _振动_角频率(T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |

Source: `src\domains\dynamics.py:194`

---

## `_振动_位移`

```matha
def _振动_位移(A, omega, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |
| `omega` | `` | |
| `t` | `` | |

Source: `src\domains\dynamics.py:196`

---

## `_振动_速度`

```matha
def _振动_速度(A, omega, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |
| `omega` | `` | |
| `t` | `` | |

Source: `src\domains\dynamics.py:198`

---

## `_振动_总能量`

```matha
def _振动_总能量(k, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k` | `` | |
| `A` | `` | |

Source: `src\domains\dynamics.py:200`

---

## `_振动_单摆周期`

```matha
def _振动_单摆周期(L, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `g_val` | `` | |

Source: `src\domains\dynamics.py:203`

---

## `_振动_复摆周期`

```matha
def _振动_复摆周期(I, m, g_val, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `m` | `` | |
| `g_val` | `` | |
| `d` | `` | |

Source: `src\domains\dynamics.py:205`

---

## `_振动_阻尼角频率`

```matha
def _振动_阻尼角频率(omega0, beta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `omega0` | `` | |
| `beta` | `` | |

Source: `src\domains\dynamics.py:207`

---

## `_register_dynamics`

```matha
def _register_dynamics(builtins: dict) -> None
```

**描述**: 将动力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（mechanics 之后）。
命名规则：
  - 牛顿定律：前缀 力_
  - 动量定理：前缀 动量_
  - 功与能：前缀 功_ / 前缀 能量_
  - 转动动力学：前缀 转动_ / 前缀 转动惯量_
  - 机械振动：前缀 振动_

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\dynamics.py:214`

---

## `_dynamics_symtab_names`

```matha
def _dynamics_symtab_names() -> list[str]
```

**描述**: 返回动力学所有内建名（用于语义分析注册，避免报未定义）。

**返回**: `list[str]`

Source: `src\domains\dynamics.py:288`

---

# `domains.economics`

```matha

```

**描述**: Matha 经济学领域模块：微观经济学 + 宏观经济学 + 金融数学。

覆盖：
  1) 微观经济学：供需均衡、弹性、边际分析、消费者剩余
  2) 宏观经济学：GDP 计算、乘数效应、通货膨胀
  3) 金融数学：复利、现值、年金、NPV、IRR 估算

Source: `src\domains\economics.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\economics.py:16`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\economics.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\economics.py:26`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\economics.py:33`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\economics.py:42`

---

## `_复利终值`

```matha
def _复利终值(P, r, n)
```

**描述**: 复利终值 FV = P(1+r)^n。P: 本金, r: 年利率, n: 年数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `r` | `` | |
| `n` | `` | |

Source: `src\domains\economics.py:58`

---

## `_复利现值`

```matha
def _复利现值(FV, r, n)
```

**描述**: 复利现值 PV = FV/(1+r)^n。

| 参数 | 类型 | 说明 |
|------|------|------|
| `FV` | `` | |
| `r` | `` | |
| `n` | `` | |

Source: `src\domains\economics.py:63`

---

## `_年金终值`

```matha
def _年金终值(PMT, r, n)
```

**描述**: 普通年金终值 FV = PMT · [(1+r)^n - 1] / r。

| 参数 | 类型 | 说明 |
|------|------|------|
| `PMT` | `` | |
| `r` | `` | |
| `n` | `` | |

Source: `src\domains\economics.py:70`

---

## `_年金现值`

```matha
def _年金现值(PMT, r, n)
```

**描述**: 普通年金现值 PV = PMT · [1 - (1+r)^(-n)] / r。

| 参数 | 类型 | 说明 |
|------|------|------|
| `PMT` | `` | |
| `r` | `` | |
| `n` | `` | |

Source: `src\domains\economics.py:77`

---

## `_NPV`

```matha
def _NPV(cash_flows, rate)
```

**描述**: 净现值 NPV = Σ CFt/(1+r)^t。cash_flows: [CF0, CF1, ...]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cash_flows` | `` | |
| `rate` | `` | |

Source: `src\domains\economics.py:84`

---

## `_IRR估算`

```matha
def _IRR估算(cash_flows, guess)
```

**描述**: IRR 估算（牛顿法，最多 100 次迭代）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cash_flows` | `` | |
| `guess` | `` | |

Source: `src\domains\economics.py:92`

---

## `_单利终值`

```matha
def _单利终值(P, r, t)
```

**描述**: 单利终值 FV = P(1 + r·t)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `r` | `` | |
| `t` | `` | |

Source: `src\domains\economics.py:108`

---

## `_实际利率`

```matha
def _实际利率(名义利率, 通胀率)
```

**描述**: 费雪方程：r_real ≈ r_nominal - inflation。

| 参数 | 类型 | 说明 |
|------|------|------|
| `名义利率` | `` | |
| `通胀率` | `` | |

Source: `src\domains\economics.py:113`

---

## `_需求价格弹性`

```matha
def _需求价格弹性(价格, 需求量, dQ_dP)
```

**描述**: 点弹性 Ed = (dQ/dP) · (P/Q)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `价格` | `` | |
| `需求量` | `` | |
| `dQ_dP` | `` | |

Source: `src\domains\economics.py:124`

---

## `_供给价格弹性`

```matha
def _供给价格弹性(价格, 供给量, dQ_dP)
```

**描述**: 供给点弹性 Es = (dQ/dP) · (P/Q)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `价格` | `` | |
| `供给量` | `` | |
| `dQ_dP` | `` | |

Source: `src\domains\economics.py:131`

---

## `_消费者剩余`

```matha
def _消费者剩余(需求函数系数, 均衡价格)
```

**描述**: 线性需求 Q = a - bP，消费者剩余 CS = (a - b·Pe)²/(2b)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `需求函数系数` | `` | |
| `均衡价格` | `` | |

Source: `src\domains\economics.py:138`

---

## `_生产者剩余`

```matha
def _生产者剩余(供给函数系数, 均衡价格)
```

**描述**: 线性供给 Q = c + dP，生产者剩余 PS = (Pe - c/d)²·d/2。

| 参数 | 类型 | 说明 |
|------|------|------|
| `供给函数系数` | `` | |
| `均衡价格` | `` | |

Source: `src\domains\economics.py:149`

---

## `_边际成本`

```matha
def _边际成本(Q, C_fixed, C_variable_per_unit)
```

**描述**: 边际成本 MC = dC/dQ = C_variable（线性成本）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |
| `C_fixed` | `` | |
| `C_variable_per_unit` | `` | |

Source: `src\domains\economics.py:160`

---

## `_总成本`

```matha
def _总成本(Q, C_fixed, C_variable_per_unit)
```

**描述**: 总成本 TC = FC + VC·Q。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |
| `C_fixed` | `` | |
| `C_variable_per_unit` | `` | |

Source: `src\domains\economics.py:165`

---

## `_GDP支出法`

```matha
def _GDP支出法(C, I, G, X, M)
```

**描述**: GDP = C + I + G + (X - M)。消费+投资+政府+净出口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |
| `I` | `` | |
| `G` | `` | |
| `X` | `` | |
| `M` | `` | |

Source: `src\domains\economics.py:174`

---

## `_GDP收入法`

```matha
def _GDP收入法(W, R, I_prof, Pi)
```

**描述**: GDP = 工资 + 租金 + 利息 + 利润（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `W` | `` | |
| `R` | `` | |
| `I_prof` | `` | |
| `Pi` | `` | |

Source: `src\domains\economics.py:179`

---

## `_乘数效应`

```matha
def _乘数效应(边际消费倾向)
```

**描述**: 投资乘数 k = 1/(1-MPC)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `边际消费倾向` | `` | |

Source: `src\domains\economics.py:184`

---

## `_物价指数`

```matha
def _物价指数(基期价格, 报告期价格)
```

**描述**: CPI = (报告期价格/基期价格) × 100。

| 参数 | 类型 | 说明 |
|------|------|------|
| `基期价格` | `` | |
| `报告期价格` | `` | |

Source: `src\domains\economics.py:191`

---

## `_通货膨胀率`

```matha
def _通货膨胀率(基期CPI, 本期CPI)
```

**描述**: 通胀率 = (CPI_t - CPI_0) / CPI_0 × 100%。

| 参数 | 类型 | 说明 |
|------|------|------|
| `基期CPI` | `` | |
| `本期CPI` | `` | |

Source: `src\domains\economics.py:198`

---

## `_人均GDP`

```matha
def _人均GDP(GDP, 人口)
```

**描述**: 人均 GDP = GDP / 人口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `GDP` | `` | |
| `人口` | `` | |

Source: `src\domains\economics.py:205`

---

## `_register_economics`

```matha
def _register_economics(builtins: dict) -> None
```

**描述**: 将经济学领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\economics.py:216`

---

## `_register_economics_symtab_names`

```matha
def _register_economics_symtab_names() -> list[str]
```

**描述**: 返回经济学领域所有内建名。

**返回**: `list[str]`

Source: `src\domains\economics.py:245`

---

# `domains.electrical`

```matha

```

**描述**: Matha 电气工程专业模块：电路分析、电磁场、信号与系统。

覆盖：
  1) 电路分析：欧姆定律、基尔霍夫定律、分压分流、功率计算
  2) 交流电路：阻抗、功率因数、RLC 谐振
  3) 电磁场：库仑定律、电场强度、磁感应强度
  4) 信号与系统：傅里叶变换基础、滤波器截止频率

Source: `src\domains\electrical.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\electrical.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\electrical.py:22`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\electrical.py:27`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\electrical.py:34`

---

## `_欧姆定律`

```matha
def _欧姆定律(V, R)
```

**描述**: 欧姆定律：I = V/R。V: V, R: Ω, 返回 I: A。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `R` | `` | |

Source: `src\domains\electrical.py:56`

---

## `_电功率`

```matha
def _电功率(V, I)
```

**描述**: 电功率 P = V·I。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `I` | `` | |

Source: `src\domains\electrical.py:63`

---

## `_电功率_R`

```matha
def _电功率_R(V, R)
```

**描述**: P = V²/R。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `R` | `` | |

Source: `src\domains\electrical.py:68`

---

## `_分压公式`

```matha
def _分压公式(V_in, R1, R2)
```

**描述**: 串联分压：V_out = V_in · R2/(R1+R2)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V_in` | `` | |
| `R1` | `` | |
| `R2` | `` | |

Source: `src\domains\electrical.py:75`

---

## `_分流公式`

```matha
def _分流公式(I_total, R1, R2)
```

**描述**: 并联分流：I1 = I_total · R2/(R1+R2)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `I_total` | `` | |
| `R1` | `` | |
| `R2` | `` | |

Source: `src\domains\electrical.py:83`

---

## `_串联电阻`

```matha
def _串联电阻(*resistors)
```

**描述**: 串联总电阻 R = R1 + R2 + ...

Source: `src\domains\electrical.py:91`

---

## `_并联电阻`

```matha
def _并联电阻(*resistors)
```

**描述**: 并联总电阻 1/R = 1/R1 + 1/R2 + ...

Source: `src\domains\electrical.py:96`

---

## `_感抗`

```matha
def _感抗(f, L)
```

**描述**: 感抗 X_L = 2πfL。f: Hz, L: H。

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `L` | `` | |

Source: `src\domains\electrical.py:110`

---

## `_容抗`

```matha
def _容抗(f, C)
```

**描述**: 容抗 X_C = 1/(2πfC)。f: Hz, C: F。

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `C` | `` | |

Source: `src\domains\electrical.py:115`

---

## `_RLC阻抗`

```matha
def _RLC阻抗(R, L, C, f)
```

**描述**: RLC 串联阻抗 Z = sqrt(R² + (XL - XC)²)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `L` | `` | |
| `C` | `` | |
| `f` | `` | |

Source: `src\domains\electrical.py:122`

---

## `_谐振频率`

```matha
def _谐振频率(L, C)
```

**描述**: RLC 谐振频率 f₀ = 1/(2π√(LC))。

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `C` | `` | |

Source: `src\domains\electrical.py:129`

---

## `_功率因数`

```matha
def _功率因数(Z, R)
```

**描述**: 功率因数 cosφ = R/Z。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Z` | `` | |
| `R` | `` | |

Source: `src\domains\electrical.py:136`

---

## `_库仑力`

```matha
def _库仑力(q1, q2, r)
```

**描述**: 库仑定律 F = k·q1·q2/r²。k = 1/(4πε₀)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `q1` | `` | |
| `q2` | `` | |
| `r` | `` | |

Source: `src\domains\electrical.py:147`

---

## `_电场强度`

```matha
def _电场强度(Q, r)
```

**描述**: 点电荷电场 E = Q/(4πε₀r²)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |
| `r` | `` | |

Source: `src\domains\electrical.py:155`

---

## `_长直导线磁场`

```matha
def _长直导线磁场(I, r)
```

**描述**: 无限长直导线磁场 B = μ₀I/(2πr)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `r` | `` | |

Source: `src\domains\electrical.py:163`

---

## `_通电导线受力`

```matha
def _通电导线受力(B, I, L, angle)
```

**描述**: 安培力 F = BIL·sinθ。

| 参数 | 类型 | 说明 |
|------|------|------|
| `B` | `` | |
| `I` | `` | |
| `L` | `` | |
| `angle` | `` | |

Source: `src\domains\electrical.py:170`

---

## `_RC截止频率`

```matha
def _RC截止频率(R, C)
```

**描述**: RC 低通滤波器截止频率 fc = 1/(2πRC)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `C` | `` | |

Source: `src\domains\electrical.py:179`

---

## `_RL截止频率`

```matha
def _RL截止频率(R, L)
```

**描述**: RL 低通滤波器截止频率 fc = R/(2πL)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `L` | `` | |

Source: `src\domains\electrical.py:186`

---

## `_傅里叶频率分辨率`

```matha
def _傅里叶频率分辨率(sample_rate, N)
```

**描述**: DFT 频率分辨率 Δf = fs/N。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sample_rate` | `` | |
| `N` | `` | |

Source: `src\domains\electrical.py:193`

---

## `_采样定理_min_rate`

```matha
def _采样定理_min_rate(f_max)
```

**描述**: 奈奎斯特最低采样率 fs ≥ 2·f_max。

| 参数 | 类型 | 说明 |
|------|------|------|
| `f_max` | `` | |

Source: `src\domains\electrical.py:200`

---

## `_register_electrical`

```matha
def _register_electrical(builtins: dict) -> None
```

**描述**: 将电气工程领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\electrical.py:209`

---

## `_register_electrical_symtab_names`

```matha
def _register_electrical_symtab_names() -> list[str]
```

**描述**: 返回电气工程领域所有内建名。

**返回**: `list[str]`

Source: `src\domains\electrical.py:245`

---

# `domains.em`

```matha

```

**描述**: Matha 机械领域模块：电磁学（Electromagnetism）。

基于动力学 + 热力学 + mathlib 数学地基，演化电磁学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、静电学（Electrostatics）
  1) 库仑定律：F = kq1q2/r²
  2) 电场强度：E = F/q = kQ/r²
  3) 电势：V = kQ/r
  4) 电势能：U = kq1q2/r
  5) 平行板电容器：C = ε₀S/d
  6) 电容器能量：W = ½CV²
  7) 高斯定律：Φ = Q/ε₀

二、直流电路（DC Circuits）
  1) 欧姆定律：V = IR
  2) 电阻串联：R = ΣR_i
  3) 电阻并联：1/R = Σ(1/R_i)
  4) 电功率：P = VI = I²R = V²/R
  5) 焦耳热：Q = I²Rt

三、磁场（Magnetism）
  1) 洛伦兹力：F = qvB sinθ
  2) 安培力：F = BIL sinθ
  3) 长直导线磁场：B = μ₀I/(2πr)
  4) 圆形线圈中心磁场：B = μ₀I/(2R)
  5) 螺线管磁场：B = μ₀nI
  6) 磁通量：Φ = BA cosθ

四、电磁感应（Electromagnetic Induction）
  1) 法拉第定律：ε = -N·dΦ/dt
  2) 动生电动势：ε = BLv
  3) 自感电动势：ε = -L·dI/dt
  4) 磁场能量：W = ½LI²
  5) 互感：M = N₂Φ₂₁/I₁

五、交流电路（AC Circuits）
  1) 感抗：X_L = ωL = 2πfL
  2) 容抗：X_C = 1/(ωC) = 1/(2πfC)
  3) 阻抗（串联RLC）：Z = √(R² + (X_L - X_C)²)
  4) 谐振频率：f₀ = 1/(2π√(LC))
  5) 有效值：V_rms = V_max/√2
  6) 功率因数：cosφ = R/Z
  7) 交流功率：P = V_rms·I_rms·cosφ

设计原则：
  - 所有函数返回纯数值（float/int），不抛语义错（除以零等 Python 自身抛）
  - 多参函数一律 _curry2/_curry3/_curry4/_curry5 封装
  - 前缀 电_ / 电路_ / 磁_ / 感应_ / 交流_ 区分子领域
  - 常用介电常数/电阻率作为常量注册

Source: `src\domains\em.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\em.py:63`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\em.py:69`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\em.py:77`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\em.py:87`

---

## `_电_库仑力`

```matha
def _电_库仑力(q1, q2, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q1` | `` | |
| `q2` | `` | |
| `r` | `` | |

Source: `src\domains\em.py:118`

---

## `_电_电场`

```matha
def _电_电场(Q, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |
| `r` | `` | |

Source: `src\domains\em.py:120`

---

## `_电_电势`

```matha
def _电_电势(Q, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |
| `r` | `` | |

Source: `src\domains\em.py:122`

---

## `_电_电势能`

```matha
def _电_电势能(q1, q2, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q1` | `` | |
| `q2` | `` | |
| `r` | `` | |

Source: `src\domains\em.py:124`

---

## `_电_平行板电容`

```matha
def _电_平行板电容(S, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `S` | `` | |
| `d` | `` | |

Source: `src\domains\em.py:126`

---

## `_电_电容储能`

```matha
def _电_电容储能(C, V)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |
| `V` | `` | |

Source: `src\domains\em.py:128`

---

## `_电_电容电荷`

```matha
def _电_电容电荷(C, V)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |
| `V` | `` | |

Source: `src\domains\em.py:130`

---

## `_电_高斯通量`

```matha
def _电_高斯通量(Q)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |

Source: `src\domains\em.py:132`

---

## `_电_电偶极矩`

```matha
def _电_电偶极矩(q, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `d` | `` | |

Source: `src\domains\em.py:134`

---

## `_电路_电压`

```matha
def _电路_电压(I, R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `R` | `` | |

Source: `src\domains\em.py:142`

---

## `_电路_电流`

```matha
def _电路_电流(V, R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `R` | `` | |

Source: `src\domains\em.py:144`

---

## `_电路_电阻`

```matha
def _电路_电阻(V, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `I` | `` | |

Source: `src\domains\em.py:146`

---

## `_电路_串联电阻`

```matha
def _电路_串联电阻(R_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R_list` | `` | |

Source: `src\domains\em.py:148`

---

## `_电路_并联电阻`

```matha
def _电路_并联电阻(R_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R_list` | `` | |

Source: `src\domains\em.py:151`

---

## `_电路_功率`

```matha
def _电路_功率(V, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `I` | `` | |

Source: `src\domains\em.py:157`

---

## `_电路_功率热`

```matha
def _电路_功率热(I, R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `R` | `` | |

Source: `src\domains\em.py:159`

---

## `_电路_功率压`

```matha
def _电路_功率压(V, R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `R` | `` | |

Source: `src\domains\em.py:161`

---

## `_电路_焦耳热`

```matha
def _电路_焦耳热(I, R, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `R` | `` | |
| `t` | `` | |

Source: `src\domains\em.py:163`

---

## `_磁_洛伦兹力`

```matha
def _磁_洛伦兹力(q, v, B, theta_rad)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `v` | `` | |
| `B` | `` | |
| `theta_rad` | `` | |

Source: `src\domains\em.py:171`

---

## `_磁_安培力`

```matha
def _磁_安培力(B, I, L, theta_rad)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `B` | `` | |
| `I` | `` | |
| `L` | `` | |
| `theta_rad` | `` | |

Source: `src\domains\em.py:173`

---

## `_磁_直导线磁场`

```matha
def _磁_直导线磁场(I, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `r` | `` | |

Source: `src\domains\em.py:175`

---

## `_磁_圆线圈中心磁场`

```matha
def _磁_圆线圈中心磁场(I, R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `R` | `` | |

Source: `src\domains\em.py:177`

---

## `_磁_螺线管磁场`

```matha
def _磁_螺线管磁场(n, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `I` | `` | |

Source: `src\domains\em.py:179`

---

## `_磁_磁通量`

```matha
def _磁_磁通量(B, A, theta_rad)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `B` | `` | |
| `A` | `` | |
| `theta_rad` | `` | |

Source: `src\domains\em.py:181`

---

## `_磁_回旋半径`

```matha
def _磁_回旋半径(m, v, q, B)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |
| `q` | `` | |
| `B` | `` | |

Source: `src\domains\em.py:183`

---

## `_磁_回旋频率`

```matha
def _磁_回旋频率(q, B, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `B` | `` | |
| `m` | `` | |

Source: `src\domains\em.py:185`

---

## `_感应_法拉第电动势`

```matha
def _感应_法拉第电动势(N, dPhi, dt)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `dPhi` | `` | |
| `dt` | `` | |

Source: `src\domains\em.py:193`

---

## `_感应_动生电动势`

```matha
def _感应_动生电动势(B, L, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `B` | `` | |
| `L` | `` | |
| `v` | `` | |

Source: `src\domains\em.py:195`

---

## `_感应_自感电动势`

```matha
def _感应_自感电动势(L, dI, dt)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `dI` | `` | |
| `dt` | `` | |

Source: `src\domains\em.py:197`

---

## `_感应_磁场能量`

```matha
def _感应_磁场能量(L, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `I` | `` | |

Source: `src\domains\em.py:199`

---

## `_感应_互感`

```matha
def _感应_互感(N2, Phi21, I1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N2` | `` | |
| `Phi21` | `` | |
| `I1` | `` | |

Source: `src\domains\em.py:201`

---

## `_感应_RL时间常数`

```matha
def _感应_RL时间常数(L, R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `R` | `` | |

Source: `src\domains\em.py:203`

---

## `_交流_感抗`

```matha
def _交流_感抗(f, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `L` | `` | |

Source: `src\domains\em.py:211`

---

## `_交流_容抗`

```matha
def _交流_容抗(f, C)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `C` | `` | |

Source: `src\domains\em.py:213`

---

## `_交流_阻抗`

```matha
def _交流_阻抗(R, XL, XC)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `XL` | `` | |
| `XC` | `` | |

Source: `src\domains\em.py:215`

---

## `_交流_谐振频率`

```matha
def _交流_谐振频率(L, C)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `C` | `` | |

Source: `src\domains\em.py:217`

---

## `_交流_有效值`

```matha
def _交流_有效值(V_max)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V_max` | `` | |

Source: `src\domains\em.py:219`

---

## `_交流_峰值`

```matha
def _交流_峰值(V_rms)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V_rms` | `` | |

Source: `src\domains\em.py:221`

---

## `_交流_功率因数`

```matha
def _交流_功率因数(R, Z)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `Z` | `` | |

Source: `src\domains\em.py:223`

---

## `_交流_有功功率`

```matha
def _交流_有功功率(V, I, cos_phi)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `I` | `` | |
| `cos_phi` | `` | |

Source: `src\domains\em.py:225`

---

## `_交流_视在功率`

```matha
def _交流_视在功率(V, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `I` | `` | |

Source: `src\domains\em.py:227`

---

## `_交流_无功功率`

```matha
def _交流_无功功率(V, I, cos_phi)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `I` | `` | |
| `cos_phi` | `` | |

Source: `src\domains\em.py:229`

---

## `_交流_品质因数`

```matha
def _交流_品质因数(R, L, C)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `L` | `` | |
| `C` | `` | |

Source: `src\domains\em.py:231`

---

## `_register_em`

```matha
def _register_em(builtins: dict) -> None
```

**描述**: 将电磁学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（thermo 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\em.py:270`

---

## `_em_symtab_names`

```matha
def _em_symtab_names() -> list[str]
```

**描述**: 返回电磁学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\em.py:343`

---

# `domains.embedded`

```matha

```

**描述**: Embedded Domain: 嵌入式系统内建函数。

覆盖：
  - ADC 模数转换计算
  - PWM 占空比/频率计算
  - 传感器公式（温度、光线、距离等）
  - 电机控制（步进、直流、舵机）
  - 通信协议（UART 波特率、I2C 地址计算）

Source: `src\domains\embedded.py:1`

---

## `_adc_value`

```matha
def _adc_value(voltage: float, ref_voltage: float, bits: int) -> int
```

**描述**: ADC 电压 → 数字值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `voltage` | `float` | |
| `ref_voltage` | `float` | |
| `bits` | `int` | |

**返回**: `int`

Source: `src\domains\embedded.py:19`

---

## `_adc_voltage`

```matha
def _adc_voltage(digital: int, ref_voltage: float, bits: int) -> float
```

**描述**: ADC 数字值 → 电压。

| 参数 | 类型 | 说明 |
|------|------|------|
| `digital` | `int` | |
| `ref_voltage` | `float` | |
| `bits` | `int` | |

**返回**: `float`

Source: `src\domains\embedded.py:26`

---

## `_dac_value`

```matha
def _dac_value(voltage: float, ref_voltage: float, bits: int) -> int
```

**描述**: DAC 电压 → 数字值（同 ADC）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `voltage` | `float` | |
| `ref_voltage` | `float` | |
| `bits` | `int` | |

**返回**: `int`

Source: `src\domains\embedded.py:34`

---

## `_pwm_duty_cycle`

```matha
def _pwm_duty_cycle(on_time: float, period: float) -> float
```

**描述**: 计算占空比（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `on_time` | `float` | |
| `period` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:43`

---

## `_pwm_period`

```matha
def _pwm_period(freq: float, duty: float) -> float
```

**描述**: 计算周期（秒），给定频率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `freq` | `float` | |
| `duty` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:50`

---

## `_pwm_on_time`

```matha
def _pwm_on_time(period: float, duty: float) -> float
```

**描述**: 计算高电平时间（秒）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `period` | `float` | |
| `duty` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:57`

---

## `_thermistor_temperature`

```matha
def _thermistor_temperature(R: float, R0: float, T0: float, B: float) -> float
```

**描述**: NTC 热敏电阻温度计算（K）。

R: 当前电阻 (Ω)
R0: 标称电阻 (Ω)，通常 25°C 时
T0: 标称温度 (K)，通常 298.15K (25°C)
B: B 值 (K)

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `float` | |
| `R0` | `float` | |
| `T0` | `float` | |
| `B` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:67`

---

## `_natural_log`

```matha
def _natural_log(x: float) -> float
```

**描述**: 自然对数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:81`

---

## `_thermistor_temperature`

```matha
def _thermistor_temperature(R: float, R0: float, T0: float, B: float) -> float
```

**描述**: NTC 热敏电阻温度计算（K）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `float` | |
| `R0` | `float` | |
| `T0` | `float` | |
| `B` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:87`

---

## `_thermistor_temperature_celsius`

```matha
def _thermistor_temperature_celsius(R: float, R0: float, T0: float, B: float) -> float
```

**描述**: NTC 热敏电阻温度计算（°C）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `float` | |
| `R0` | `float` | |
| `T0` | `float` | |
| `B` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:96`

---

## `_photocell_light`

```matha
def _photocell_light(R_dark: float, R_light: float, R_sensor: float) -> float
```

**描述**: 光敏电阻光照估算（相对百分比）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R_dark` | `float` | |
| `R_light` | `float` | |
| `R_sensor` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:103`

---

## `_ultrasonic_distance`

```matha
def _ultrasonic_distance(time_us: float, speed_of_sound: float) -> float
```

**描述**: 超声波测距（米）。time_us: 回波时间（微秒）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `time_us` | `float` | |
| `speed_of_sound` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:114`

---

## `_accel_angle`

```matha
def _accel_angle(ax: float, ay: float, az: float) -> tuple[float, float]
```

**描述**: 加速度计计算俯仰角和横滚角（度）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ax` | `float` | |
| `ay` | `float` | |
| `az` | `float` | |

**返回**: `tuple[float, float]`

Source: `src\domains\embedded.py:120`

---

## `_stepper_steps_to_degrees`

```matha
def _stepper_steps_to_degrees(steps: int, steps_per_rev: int, reduction: float) -> float
```

**描述**: 步进数 → 角度（度）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `steps` | `int` | |
| `steps_per_rev` | `int` | |
| `reduction` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:129`

---

## `_stepper_degrees_to_steps`

```matha
def _stepper_degrees_to_steps(degrees: float, steps_per_rev: int, reduction: float) -> int
```

**描述**: 角度 → 步进数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `degrees` | `float` | |
| `steps_per_rev` | `int` | |
| `reduction` | `float` | |

**返回**: `int`

Source: `src\domains\embedded.py:136`

---

## `_servo_angle`

```matha
def _servo_angle(pulse_us: float, min_pulse: float, max_pulse: float) -> float
```

**描述**: 脉冲宽度 → 舵机角度（度）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pulse_us` | `float` | |
| `min_pulse` | `float` | |
| `max_pulse` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:144`

---

## `_servo_pulse`

```matha
def _servo_pulse(angle: float, min_pulse: float, max_pulse: float) -> float
```

**描述**: 舵机角度 → 脉冲宽度（微秒）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `angle` | `float` | |
| `min_pulse` | `float` | |
| `max_pulse` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:151`

---

## `_uart_baud_error`

```matha
def _uart_baud_error(desired: float, crystal: float, divider: int) -> float
```

**描述**: UART 波特率误差计算（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `desired` | `float` | |
| `crystal` | `float` | |
| `divider` | `int` | |

**返回**: `float`

Source: `src\domains\embedded.py:160`

---

## `_i2c_pullup_resistor`

```matha
def _i2c_pullup_resistor(vcc: float, i2c_current: float) -> float
```

**描述**: 计算 I2C 上拉电阻（Ω）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `vcc` | `float` | |
| `i2c_current` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:171`

---

## `_spi_clock_divider`

```matha
def _spi_clock_divider(clock: float, sys_clock: float) -> int
```

**描述**: SPI 时钟分频器计算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `clock` | `float` | |
| `sys_clock` | `float` | |

**返回**: `int`

Source: `src\domains\embedded.py:178`

---

## `_dc_motor_rpm`

```matha
def _dc_motor_rpm(voltage: float, k_v: float) -> float
```

**描述**: 直流电机转速（RPM）。k_v: RPM/V。

| 参数 | 类型 | 说明 |
|------|------|------|
| `voltage` | `float` | |
| `k_v` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:195`

---

## `_dc_motor_current`

```matha
def _dc_motor_current(torque: float, k_t: float) -> float
```

**描述**: 直流电机堵转电流（A）。k_t: Nm/A。

| 参数 | 类型 | 说明 |
|------|------|------|
| `torque` | `float` | |
| `k_t` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:202`

---

## `_stepper_torque`

```matha
def _stepper_torque(freq: float, voltage: float, resistance: float) -> float
```

**描述**: 步进电机转矩估算（简化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `freq` | `float` | |
| `voltage` | `float` | |
| `resistance` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:209`

---

## `_battery_voltage`

```matha
def _battery_voltage(cells: int, cell_type: str) -> float
```

**描述**: 电池组电压估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cells` | `int` | |
| `cell_type` | `str` | |

**返回**: `float`

Source: `src\domains\embedded.py:223`

---

## `_battery_capacity_wh`

```matha
def _battery_capacity_wh(ah: float, voltage: float) -> float
```

**描述**: 电池容量（Wh）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ah` | `float` | |
| `voltage` | `float` | |

**返回**: `float`

Source: `src\domains\embedded.py:232`

---

## `_power_budget`

```matha
def _power_budget(items: list) -> float
```

**描述**: 功耗预算计算（W）。items: [(power_w, duty_cycle), ...]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `items` | `list` | |

**返回**: `float`

Source: `src\domains\embedded.py:237`

---

## `_register_embedded`

```matha
def _register_embedded(builtins: dict) -> None
```

**描述**: 将嵌入式领域内建注册到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\embedded.py:249`

---

## `_register_embedded_symtab_names`

```matha
def _register_embedded_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\embedded.py:288`

---

## `_curry2`

```matha
def _curry2(fn)
```

**描述**: 两参 → 柯里化 f(a)(b)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

Source: `src\domains\embedded.py:300`

---

## `_curry3`

```matha
def _curry3(fn)
```

**描述**: 三参 → 柯里化 f(a)(b)(c)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

Source: `src\domains\embedded.py:307`

---

## `_curry4`

```matha
def _curry4(fn)
```

**描述**: 四参 → 柯里化 f(a)(b)(c)(d)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

Source: `src\domains\embedded.py:316`

---

## `_curry2`

```matha
def _curry2(fn)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

Source: `src\domains\embedded.py:327`

---

# `domains.extended_modeling`

```matha

```

**描述**: 扩展建模函数：结构力学、流体力学、热力学、电气工程、控制理论。

覆盖新增学科门类：
  - 结构工程：梁弯曲、柱稳定、框架分析
  - 流体力学：管道流动、边界层、湍流模型
  - 热力学：热传导、对流、辐射
  - 电气工程：电力系统、电机驱动、功率电子
  - 控制理论：状态空间、根轨迹、频域分析
  - 材料科学：应力应变、疲劳寿命、断裂力学

Source: `src\domains\extended_modeling.py:1`

---

## `_梁弯曲正应力`

```matha
def _梁弯曲正应力(M: float, y: float, I: float) -> float
```

**描述**: 梁弯曲正应力 σ = My/I。

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `float` | |
| `y` | `float` | |
| `I` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:22`

---

## `_梁弯曲挠度`

```matha
def _梁弯曲挠度(载荷: float, L: float, E: float, I: float, 支座: str) -> float
```

**描述**: 梁弯曲挠度计算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `载荷` | `float` | |
| `L` | `float` | |
| `E` | `float` | |
| `I` | `float` | |
| `支座` | `str` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:29`

---

## `_柱临界载荷`

```matha
def _柱临界载荷(Paschen公式) -> float
```

**描述**: 柱临界载荷（欧拉公式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Paschen公式` | `` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:42`

---

## `_轴应力`

```matha
def _轴应力(扭矩: float, d: float) -> float
```

**描述**: 圆轴扭转剪应力 τ = 16T/(πd³)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `扭矩` | `float` | |
| `d` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:48`

---

## `_register_structural`

```matha
def _register_structural(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\extended_modeling.py:55`

---

## `_管道沿程损失`

```matha
def _管道沿程损失(Q: float, L: float, D: float, f: float) -> float
```

**描述**: 达西-魏斯巴赫公式：hf = f·L/D·v²/2g。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `float` | |
| `L` | `float` | |
| `D` | `float` | |
| `f` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:66`

---

## `_雷诺数`

```matha
def _雷诺数(v: float, D: float, nu: float) -> float
```

**描述**: 雷诺数 Re = vD/ν。

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `float` | |
| `D` | `float` | |
| `nu` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:75`

---

## `_伯努利方程`

```matha
def _伯努利方程(z1: float, p1: float, v1: float, z2: float, rho: float) -> float
```

**描述**: 伯努利方程求 p2。

| 参数 | 类型 | 说明 |
|------|------|------|
| `z1` | `float` | |
| `p1` | `float` | |
| `v1` | `float` | |
| `z2` | `float` | |
| `rho` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:82`

---

## `_register_fluid`

```matha
def _register_fluid(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\extended_modeling.py:89`

---

## `_热传导`

```matha
def _热传导(Q: float, A: float, dT: float, L: float, k: float) -> float
```

**描述**: 傅里叶热传导：Q = kA·dT/L → 求 k。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `float` | |
| `A` | `float` | |
| `dT` | `float` | |
| `L` | `float` | |
| `k` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:99`

---

## `_对流换热`

```matha
def _对流换热(h: float, A: float, Tw: float, Tf: float) -> float
```

**描述**: 牛顿冷却定律：Q = hA(Tw - Tf)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `h` | `float` | |
| `A` | `float` | |
| `Tw` | `float` | |
| `Tf` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:106`

---

## `_辐射换热`

```matha
def _辐射换热(ε: float, A: float, T1: float, T2: float, σ: float) -> float
```

**描述**: 斯蒂芬-玻尔兹曼辐射定律。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ε` | `float` | |
| `A` | `float` | |
| `T1` | `float` | |
| `T2` | `float` | |
| `σ` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:111`

---

## `_register_thermal`

```matha
def _register_thermal(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\extended_modeling.py:116`

---

## `_三相功率线电压`

```matha
def _三相功率线电压(VL: float, IL: float, pf: float) -> float
```

**描述**: 三相功率 P = √3·VL·IL·pf。

| 参数 | 类型 | 说明 |
|------|------|------|
| `VL` | `float` | |
| `IL` | `float` | |
| `pf` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:126`

---

## `_变压器变比`

```matha
def _变压器变比(N1: int, N2: int) -> float
```

**描述**: 变压器变比 k = N1/N2。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N1` | `int` | |
| `N2` | `int` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:131`

---

## `_电机扭矩`

```matha
def _电机扭矩(T: float, n: float, P: float) -> float
```

**描述**: 电机扭矩 T = 9550·P/n。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `float` | |
| `n` | `float` | |
| `P` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:138`

---

## `_register_power`

```matha
def _register_power(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\extended_modeling.py:145`

---

## `_PID输出`

```matha
def _PID输出(Kp: float, Ki: float, Kd: float, e: float, e_integral: float, e_derivative: float) -> float
```

**描述**: PID 控制器输出。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Kp` | `float` | |
| `Ki` | `float` | |
| `Kd` | `float` | |
| `e` | `float` | |
| `e_integral` | `float` | |
| `e_derivative` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:155`

---

## `_一阶惯性环节`

```matha
def _一阶惯性环节(T: float, t: float) -> float
```

**描述**: 一阶惯性环节响应：1 - e^(-t/T)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `float` | |
| `t` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:161`

---

## `_二阶系统参数`

```matha
def _二阶系统参数(ζ: float, ωn: float) -> dict
```

**描述**: 二阶系统性能指标。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ζ` | `float` | |
| `ωn` | `float` | |

**返回**: `dict`

Source: `src\domains\extended_modeling.py:168`

---

## `_register_control`

```matha
def _register_control(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\extended_modeling.py:179`

---

## `_胡克定律`

```matha
def _胡克定律(σ: float, E: float) -> float
```

**描述**: 胡克定律：ε = σ/E。

| 参数 | 类型 | 说明 |
|------|------|------|
| `σ` | `float` | |
| `E` | `float` | |

**返回**: `float`

Source: `src\domains\extended_modeling.py:189`

---

## `_疲劳寿命`

```matha
def _疲劳寿命(S: float, Sn: float, N: float) -> int
```

**描述**: Miner 疲劳寿命估算（简化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `S` | `float` | |
| `Sn` | `float` | |
| `N` | `float` | |

**返回**: `int`

Source: `src\domains\extended_modeling.py:196`

---

## `_注册材料科学`

```matha
def _注册材料科学(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\extended_modeling.py:203`

---

## `_register_extended_modeling`

```matha
def _register_extended_modeling(builtins: dict) -> None
```

**描述**: 注册所有扩展建模函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\extended_modeling.py:212`

---

## `_register_extended_modeling_symtab_names`

```matha
def _register_extended_modeling_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\extended_modeling.py:222`

---

# `domains.fintech`

```matha

```

**描述**: Matha 金融科技领域模块：期权定价、风险管理、量化交易、信用评级。

覆盖：
  1) Black-Scholes期权定价
  2) VaR风险价值
  3) 夏普比率
  4) 信用评分
  5) 流动性覆盖率
  6) 杠杆率

Source: `src\domains\fintech.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fintech.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fintech.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fintech.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fintech.py:31`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fintech.py:39`

---

## `_BlackScholes期权定价`

```matha
def _BlackScholes期权定价(S, K, T, r, sigma)
```

**描述**: Black-Scholes欧式看涨期权定价。

| 参数 | 类型 | 说明 |
|------|------|------|
| `S` | `` | |
| `K` | `` | |
| `T` | `` | |
| `r` | `` | |
| `sigma` | `` | |

Source: `src\domains\fintech.py:50`

---

## `_VaR风险价值`

```matha
def _VaR风险价值(组合价值, 日收益率标准差, 置信水平, 持有期天)
```

**描述**: VaR风险价值估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `组合价值` | `` | |
| `日收益率标准差` | `` | |
| `置信水平` | `` | |
| `持有期天` | `` | |

Source: `src\domains\fintech.py:62`

---

## `_夏普比率`

```matha
def _夏普比率(超额收益, 波动率)
```

**描述**: 夏普比率 = (R-Rf)/σ。

| 参数 | 类型 | 说明 |
|------|------|------|
| `超额收益` | `` | |
| `波动率` | `` | |

Source: `src\domains\fintech.py:69`

---

## `_信用评分`

```matha
def _信用评分(负债率, 收入稳定性, 逾期次数, 信用历史年限)
```

**描述**: 简化信用评分（0-100）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `负债率` | `` | |
| `收入稳定性` | `` | |
| `逾期次数` | `` | |
| `信用历史年限` | `` | |

Source: `src\domains\fintech.py:76`

---

## `_流动性覆盖率`

```matha
def _流动性覆盖率(优质流动资产, 净现金流出)
```

**描述**: LCR = HQLA / 净现金流出。

| 参数 | 类型 | 说明 |
|------|------|------|
| `优质流动资产` | `` | |
| `净现金流出` | `` | |

Source: `src\domains\fintech.py:88`

---

## `_杠杆率`

```matha
def _杠杆率(总资产, 权益资本)
```

**描述**: 杠杆率 = 权益资本 / 总资产。

| 参数 | 类型 | 说明 |
|------|------|------|
| `总资产` | `` | |
| `权益资本` | `` | |

Source: `src\domains\fintech.py:95`

---

## `_register_fintech`

```matha
def _register_fintech(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\fintech.py:106`

---

## `_fintech_symtab_names`

```matha
def _fintech_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\fintech.py:115`

---

# `domains.fluid`

```matha

```

**描述**: Matha 机械领域模块：流体力学（Fluid Mechanics）。

基于运动学 + 动力学 + mathlib 数学地基，演化流体力学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

四大子领域：

一、流体静力学（Fluid Statics）
  1) 静水压强：p = ρgh
  2) 帕斯卡定律：F2/F1 = A2/A1（液压机原理）
  3) 浮力（阿基米德原理）：F_b = ρgV
  4) 浮力稳定性判断（密度比较）
  5) 壁面静水压力（矩形壁）：F = ½ρgh²·b

二、流体运动学（Fluid Kinematics）
  1) 体积流量：Q = A·v
  2) 质量流量：ṁ = ρ·Q = ρ·A·v
  3) 连续性方程：A1·v1 = A2·v2
  4) 管道截面面积（圆管/矩形管）

三、流体动力学（Fluid Dynamics）
  1) 伯努利方程：p₁ + ½ρv₁² + ρgh₁ = p₂ + ½ρv₂² + ρgh₂
  2) 托里拆利定理：v = √(2gh)（小孔出流）
  3) 文丘里流量计：由压差求流速
  4) 皮托管测速：v = √(2Δp/ρ)
  5) 雷诺数：Re = ρvD/μ = vD/ν（层流/湍流判据）

四、粘性流动（Viscous Flow）
  1) 牛顿粘性定律：τ = μ·du/dy
  2) 泊肃叶定律（层流管流）：Q = πr⁴Δp/(8μL)
  3) 斯托克斯定律（小球沉降）：F_d = 6πηrv
  4) 终端速度：v_t = 2r²g(ρ_s-ρ_f)/(9η)
  5) 达西-韦斯巴赫方程（沿程水头损失）：h_f = f·(L/D)·v²/(2g)

设计原则：
  - 所有函数返回纯数值（float/int），不抛语义错（除以零等 Python 自身抛）
  - 多参函数一律 _curry2/_curry3/_curry4 封装，与 Matha 柯里化语义一致
  - 前缀 流体_ / 流_ / 粘_ 区分子领域
  - 与运动学/动力学共享 mathlib 的 sin/cos/sqrt/pi/g 等

Source: `src\domains\fluid.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid.py:50`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid.py:56`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid.py:64`

---

## `_静水_压强`

```matha
def _静水_压强(rho, h, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `h` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid.py:79`

---

## `_静水_深度`

```matha
def _静水_深度(p, rho, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `rho` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid.py:81`

---

## `_帕斯卡_液压力`

```matha
def _帕斯卡_液压力(F1, A1, A2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F1` | `` | |
| `A1` | `` | |
| `A2` | `` | |

Source: `src\domains\fluid.py:83`

---

## `_浮力_浮力`

```matha
def _浮力_浮力(rho, V, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `V` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid.py:85`

---

## `_浮力_漂浮判断`

```matha
def _浮力_漂浮判断(rho_obj, rho_fluid)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho_obj` | `` | |
| `rho_fluid` | `` | |

Source: `src\domains\fluid.py:87`

---

## `_静水_壁面总压力`

```matha
def _静水_壁面总压力(rho, h, b, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `h` | `` | |
| `b` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid.py:89`

---

## `_管道_圆管面积`

```matha
def _管道_圆管面积(d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |

Source: `src\domains\fluid.py:97`

---

## `_管道_矩形面积`

```matha
def _管道_矩形面积(w, h)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `w` | `` | |
| `h` | `` | |

Source: `src\domains\fluid.py:99`

---

## `_流量_体积流量`

```matha
def _流量_体积流量(A, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |
| `v` | `` | |

Source: `src\domains\fluid.py:101`

---

## `_流量_质量流量`

```matha
def _流量_质量流量(rho, A, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `A` | `` | |
| `v` | `` | |

Source: `src\domains\fluid.py:103`

---

## `_流量_连续性速度`

```matha
def _流量_连续性速度(A1, v1, A2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A1` | `` | |
| `v1` | `` | |
| `A2` | `` | |

Source: `src\domains\fluid.py:105`

---

## `_流量_流速`

```matha
def _流量_流速(Q, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |
| `A` | `` | |

Source: `src\domains\fluid.py:107`

---

## `_伯努利_压强2`

```matha
def _伯努利_压强2(p1, rho, v1, h1, v2, h2, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `rho` | `` | |
| `v1` | `` | |
| `h1` | `` | |
| `v2` | `` | |
| `h2` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid.py:116`

---

## `_伯努利_小孔流速`

```matha
def _伯努利_小孔流速(h, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `h` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid.py:120`

---

## `_伯努利_文丘里流速`

```matha
def _伯努利_文丘里流速(dp, rho, A1, A2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dp` | `` | |
| `rho` | `` | |
| `A1` | `` | |
| `A2` | `` | |

Source: `src\domains\fluid.py:123`

---

## `_伯努利_皮托管`

```matha
def _伯努利_皮托管(dp, rho)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dp` | `` | |
| `rho` | `` | |

Source: `src\domains\fluid.py:127`

---

## `_流_雷诺数`

```matha
def _流_雷诺数(rho, v, D, mu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `v` | `` | |
| `D` | `` | |
| `mu` | `` | |

Source: `src\domains\fluid.py:130`

---

## `_流_雷诺数运动`

```matha
def _流_雷诺数运动(v, D, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `D` | `` | |
| `nu` | `` | |

Source: `src\domains\fluid.py:132`

---

## `_流_流态判断`

```matha
def _流_流态判断(Re)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Re` | `` | |

Source: `src\domains\fluid.py:134`

---

## `_粘_粘性切应力`

```matha
def _粘_粘性切应力(mu, du, dy)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |
| `du` | `` | |
| `dy` | `` | |

Source: `src\domains\fluid.py:148`

---

## `_粘_泊肃叶流量`

```matha
def _粘_泊肃叶流量(r, dp, mu, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |
| `dp` | `` | |
| `mu` | `` | |
| `L` | `` | |

Source: `src\domains\fluid.py:150`

---

## `_粘_泊肃叶流速`

```matha
def _粘_泊肃叶流速(r, dp, mu, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |
| `dp` | `` | |
| `mu` | `` | |
| `L` | `` | |

Source: `src\domains\fluid.py:153`

---

## `_粘_斯托克斯阻力`

```matha
def _粘_斯托克斯阻力(eta, r, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `eta` | `` | |
| `r` | `` | |
| `v` | `` | |

Source: `src\domains\fluid.py:156`

---

## `_粘_终端速度`

```matha
def _粘_终端速度(r, g_val, rho_s, rho_f, eta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |
| `g_val` | `` | |
| `rho_s` | `` | |
| `rho_f` | `` | |
| `eta` | `` | |

Source: `src\domains\fluid.py:159`

---

## `_粘_达西水头损失`

```matha
def _粘_达西水头损失(f, L, D, v, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `L` | `` | |
| `D` | `` | |
| `v` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid.py:162`

---

## `_register_fluid`

```matha
def _register_fluid(builtins: dict) -> None
```

**描述**: 将流体力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（dynamics 之后）。
命名规则：
  - 流体静力学：前缀 流体_ / 前缀 浮力_
  - 流体运动学：前缀 流量_ / 前缀 管道_
  - 流体动力学：前缀 伯努利_ / 前缀 流_
  - 粘性流动：前缀 粘_

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\fluid.py:196`

---

## `_curry5`

```matha
def _curry5(func)
```

**描述**: 五参 → 柯里化 f(a)(b)(c)(d)(e)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid.py:253`

---

## `_fluid_symtab_names`

```matha
def _fluid_symtab_names() -> list[str]
```

**描述**: 返回流体力学所有内建名（用于语义分析注册，避免报未定义）。

**返回**: `list[str]`

Source: `src\domains\fluid.py:266`

---

# `domains.fluid_exp`

```matha

```

**描述**: Matha 机械领域扩展模块：流体力学进阶（Advanced Fluid Mechanics）。

基于已有 fluid.py 静力学/运动学/动力学/粘性流动基础，扩展五大进阶子领域。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、边界层理论（Boundary Layer Theory）- 前缀 边界_
  1) 层流边界层厚度（Blasius 解）：δ = 5.0·x/√Re_x
  2) 层流位移厚度：δ* = 1.721·x/√Re_x
  3) 层流动量厚度：θ = 0.664·x/√Re_x
  4) 层流壁面切应力：τ_w = 0.332·ρU²/√Re_x
  5) 层流平板摩擦阻力系数：C_f = 1.328/√Re_L
  6) 湍流边界层厚度（1/7 幂律）：δ = 0.37·x/(Re_x)^(1/5)
  7) 湍流平板摩擦阻力系数（Prandtl）：C_f = 0.074/(Re_L)^(1/5)
  8) 过渡混合摩擦系数（Schlichting）：C_f = 0.074/(Re_L)^(1/5) - 1742/Re_L
  9) 边界层形状因子：H = δ*/θ
  10) 局部雷诺数：Re_x = ρUx/μ = Ux/ν

二、可压缩流动（Compressible Flow）- 前缀 可压缩_
  1) 声速：c = √(γRT)
  2) 马赫数：Ma = v/c
  3) 等熵温度比：T0/T = 1 + (γ-1)/2·Ma²
  4) 等熵压强比：p0/p = [1 + (γ-1)/2·Ma²]^(γ/(γ-1))
  5) 等熵密度比：ρ0/ρ = [1 + (γ-1)/2·Ma²]^(1/(γ-1))
  6) 正激波压强比：p2/p1 = (2γMa₁²-(γ-1))/(γ+1)
  7) 正激波密度比：ρ2/ρ1 = ((γ+1)Ma₁²)/(2+(γ-1)Ma₁²)
  8) 正激波温度比：T2/T1 = (2γMa₁²-(γ-1))(2+(γ-1)Ma₁²)/((γ+1)²·Ma₁²)
  9) 正激波后马赫数：Ma₂ = √((Ma₁²(γ-1)+2)/(2γMa₁²-(γ-1)))
  10) 临界压强比（喉部）：p*/p0 = (2/(γ+1))^(γ/(γ-1))
  11) 临界温度比：T*/T0 = 2/(γ+1)
  12) 拉瓦尔喷管面积比：A/A* = (1/Ma)[(2/(γ+1))(1+(γ-1)/2·Ma²)]^((γ+1)/(2(γ-1)))
  13) 普朗特-迈耶函数：ν = √((γ+1)/(γ-1))·arctan(√((γ-1)/(γ+1)(Ma²-1))) - arctan(√(Ma²-1))

三、明渠水力学（Open Channel Hydraulics）- 前缀 明渠_
  1) 谢才公式：v = C·√(R·S)
  2) 曼宁公式（SI）：v = (1/n)·R^(2/3)·S^(1/2)
  3) 曼宁公式（英制）：v = 1.486/n · R^(2/3)·S^(1/2)
  4) 水力半径（矩形渠）：R = b·h/(b+2h)
  5) 水力半径（梯形渠）：R = (b·h+m·h²)/(b+2h√(1+m²))
  6) 弗劳德数：Fr = v/√(g·h)
  7) 流态判断（缓/临界/急流）
  8) 临界水深（矩形渠）：h_c = (q²/g)^(1/3)
  9) 临界流速：v_c = √(g·h_c)
  10) 水跃共轭水深（矩形渠）：y2/y1 = (1/2)[-1 + √(1+8·Fr₁²)]
  11) 水跃能量损失：ΔE = (y2-y1)³/(4·y1·y2)
  12) 比能：E = h + v²/(2g)

四、泵与风机（Pumps & Fans）- 前缀 泵_
  1) 泵扬程：H = (p2-p1)/ρg + (v2²-v1²)/(2g) + Δz
  2) 泵有效功率：P_w = ρ·g·Q·H
  3) 泵轴功率：P_shaft = ρ·g·Q·H / η
  4) 泵效率：η = P_w / P_shaft
  5) 风机全压功率：P_t = p_t · Q
  6) 比转速：n_s = n·√Q / H^(3/4)
  7) 相似律-流量：Q2/Q1 = (n2/n1)·(D2/D1)³
  8) 相似律-扬程：H2/H1 = (n2/n1)²·(D2/D1)²
  9) 相似律-功率：P2/P1 = (n2/n1)³·(D2/D1)^5
  10) 风机静压升：p_s = p_t - ½ρv²

五、局部损失与管网（Minor Losses & Pipe Networks）- 前缀 管损_
  1) 局部水头损失：h_m = ζ·v²/(2g)
  2) 当量长度法：L_eq = ζ·D / f
  3) 总水头损失：h_total = (fL/D + Σζ)·v²/(2g)
  4) 突扩局部阻力系数：ζ = (1-A1/A2)²
  5) 突缩局部阻力系数：ζ ≈ 0.5·(1-A2/A1)
  6) 管道比阻：S = 8·λ·L/(g·π²·D^5)
  7) 串联管道比阻：S_eq = S1+S2
  8) 并联管道比阻：1/√S_eq = 1/√S1 + 1/√S2
  9) 锐缘入口 ζ = 0.5
  10) 出口损失 ζ = 1.0
  11) 局部阻力查询（弯头、闸阀、蝶阀等）

Source: `src\domains\fluid_exp.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid_exp.py:79`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid_exp.py:85`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid_exp.py:93`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\fluid_exp.py:103`

---

## `_边界_局部雷诺数`

```matha
def _边界_局部雷诺数(U, x, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `U` | `` | |
| `x` | `` | |
| `nu` | `` | |

Source: `src\domains\fluid_exp.py:140`

---

## `_边界_层流厚度`

```matha
def _边界_层流厚度(U, x, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `U` | `` | |
| `x` | `` | |
| `nu` | `` | |

Source: `src\domains\fluid_exp.py:142`

---

## `_边界_层流位移厚度`

```matha
def _边界_层流位移厚度(U, x, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `U` | `` | |
| `x` | `` | |
| `nu` | `` | |

Source: `src\domains\fluid_exp.py:146`

---

## `_边界_层流动量厚度`

```matha
def _边界_层流动量厚度(U, x, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `U` | `` | |
| `x` | `` | |
| `nu` | `` | |

Source: `src\domains\fluid_exp.py:150`

---

## `_边界_层流壁面切应力`

```matha
def _边界_层流壁面切应力(rho, U, x, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `U` | `` | |
| `x` | `` | |
| `nu` | `` | |

Source: `src\domains\fluid_exp.py:154`

---

## `_边界_层流平板阻力系数`

```matha
def _边界_层流平板阻力系数(Re_L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Re_L` | `` | |

Source: `src\domains\fluid_exp.py:158`

---

## `_边界_湍流厚度`

```matha
def _边界_湍流厚度(U, x, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `U` | `` | |
| `x` | `` | |
| `nu` | `` | |

Source: `src\domains\fluid_exp.py:163`

---

## `_边界_湍流平板阻力系数`

```matha
def _边界_湍流平板阻力系数(Re_L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Re_L` | `` | |

Source: `src\domains\fluid_exp.py:167`

---

## `_边界_混合平板阻力系数`

```matha
def _边界_混合平板阻力系数(Re_L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Re_L` | `` | |

Source: `src\domains\fluid_exp.py:172`

---

## `_边界_形状因子`

```matha
def _边界_形状因子(delta_star, theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `delta_star` | `` | |
| `theta` | `` | |

Source: `src\domains\fluid_exp.py:179`

---

## `_可压缩_声速`

```matha
def _可压缩_声速(gamma, R_gas, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `R_gas` | `` | |
| `T` | `` | |

Source: `src\domains\fluid_exp.py:186`

---

## `_可压缩_马赫数`

```matha
def _可压缩_马赫数(v, c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `c` | `` | |

Source: `src\domains\fluid_exp.py:187`

---

## `_可压缩_等熵温度比`

```matha
def _可压缩_等熵温度比(gamma, Ma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma` | `` | |

Source: `src\domains\fluid_exp.py:189`

---

## `_可压缩_等熵压强比`

```matha
def _可压缩_等熵压强比(gamma, Ma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma` | `` | |

Source: `src\domains\fluid_exp.py:192`

---

## `_可压缩_等熵密度比`

```matha
def _可压缩_等熵密度比(gamma, Ma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma` | `` | |

Source: `src\domains\fluid_exp.py:196`

---

## `_可压缩_激波压强比`

```matha
def _可压缩_激波压强比(gamma, Ma1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma1` | `` | |

Source: `src\domains\fluid_exp.py:200`

---

## `_可压缩_激波密度比`

```matha
def _可压缩_激波密度比(gamma, Ma1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma1` | `` | |

Source: `src\domains\fluid_exp.py:203`

---

## `_可压缩_激波温度比`

```matha
def _可压缩_激波温度比(gamma, Ma1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma1` | `` | |

Source: `src\domains\fluid_exp.py:206`

---

## `_可压缩_激波后马赫数`

```matha
def _可压缩_激波后马赫数(gamma, Ma1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma1` | `` | |

Source: `src\domains\fluid_exp.py:211`

---

## `_可压缩_临界压强比`

```matha
def _可压缩_临界压强比(gamma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |

Source: `src\domains\fluid_exp.py:216`

---

## `_可压缩_临界温度比`

```matha
def _可压缩_临界温度比(gamma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |

Source: `src\domains\fluid_exp.py:219`

---

## `_可压缩_喷管面积比`

```matha
def _可压缩_喷管面积比(gamma, Ma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma` | `` | |

Source: `src\domains\fluid_exp.py:222`

---

## `_可压缩_普朗特迈耶角`

```matha
def _可压缩_普朗特迈耶角(gamma, Ma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `gamma` | `` | |
| `Ma` | `` | |

Source: `src\domains\fluid_exp.py:229`

---

## `_明渠_谢才流速`

```matha
def _明渠_谢才流速(C, R, S)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |
| `R` | `` | |
| `S` | `` | |

Source: `src\domains\fluid_exp.py:238`

---

## `_明渠_曼宁流速_SI`

```matha
def _明渠_曼宁流速_SI(n, R, S)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `R` | `` | |
| `S` | `` | |

Source: `src\domains\fluid_exp.py:240`

---

## `_明渠_曼宁流速_英制`

```matha
def _明渠_曼宁流速_英制(n, R, S)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `R` | `` | |
| `S` | `` | |

Source: `src\domains\fluid_exp.py:243`

---

## `_明渠_矩形水力半径`

```matha
def _明渠_矩形水力半径(b, h)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `b` | `` | |
| `h` | `` | |

Source: `src\domains\fluid_exp.py:246`

---

## `_明渠_梯形水力半径`

```matha
def _明渠_梯形水力半径(b, h, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `b` | `` | |
| `h` | `` | |
| `m` | `` | |

Source: `src\domains\fluid_exp.py:249`

---

## `_明渠_弗劳德数`

```matha
def _明渠_弗劳德数(v, h, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `h` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:254`

---

## `_明渠_流态判断`

```matha
def _明渠_流态判断(Fr)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Fr` | `` | |

Source: `src\domains\fluid_exp.py:257`

---

## `_明渠_临界水深`

```matha
def _明渠_临界水深(q, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:265`

---

## `_明渠_临界流速`

```matha
def _明渠_临界流速(h_c, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `h_c` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:268`

---

## `_明渠_水跃共轭水深`

```matha
def _明渠_水跃共轭水深(y1, Fr1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `y1` | `` | |
| `Fr1` | `` | |

Source: `src\domains\fluid_exp.py:271`

---

## `_明渠_水跃能量损失`

```matha
def _明渠_水跃能量损失(y1, y2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `y1` | `` | |
| `y2` | `` | |

Source: `src\domains\fluid_exp.py:274`

---

## `_明渠_比能`

```matha
def _明渠_比能(h, v, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `h` | `` | |
| `v` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:277`

---

## `_泵_扬程`

```matha
def _泵_扬程(p1, p2, rho, v1, v2, dz, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `p2` | `` | |
| `rho` | `` | |
| `v1` | `` | |
| `v2` | `` | |
| `dz` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:282`

---

## `_泵_有效功率`

```matha
def _泵_有效功率(rho, g_val, Q, H)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `g_val` | `` | |
| `Q` | `` | |
| `H` | `` | |

Source: `src\domains\fluid_exp.py:285`

---

## `_泵_轴功率`

```matha
def _泵_轴功率(rho, g_val, Q, H, eta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `g_val` | `` | |
| `Q` | `` | |
| `H` | `` | |
| `eta` | `` | |

Source: `src\domains\fluid_exp.py:288`

---

## `_泵_效率`

```matha
def _泵_效率(P_w, P_shaft)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P_w` | `` | |
| `P_shaft` | `` | |

Source: `src\domains\fluid_exp.py:291`

---

## `_泵_风机全压功率`

```matha
def _泵_风机全压功率(p_t, Q)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p_t` | `` | |
| `Q` | `` | |

Source: `src\domains\fluid_exp.py:296`

---

## `_泵_比转速`

```matha
def _泵_比转速(n_rpm, Q, H)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n_rpm` | `` | |
| `Q` | `` | |
| `H` | `` | |

Source: `src\domains\fluid_exp.py:298`

---

## `_泵_相似律流量`

```matha
def _泵_相似律流量(n1, n2, D1, D2, Q1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n1` | `` | |
| `n2` | `` | |
| `D1` | `` | |
| `D2` | `` | |
| `Q1` | `` | |

Source: `src\domains\fluid_exp.py:303`

---

## `_泵_相似律扬程`

```matha
def _泵_相似律扬程(n1, n2, D1, D2, H1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n1` | `` | |
| `n2` | `` | |
| `D1` | `` | |
| `D2` | `` | |
| `H1` | `` | |

Source: `src\domains\fluid_exp.py:306`

---

## `_泵_相似律功率`

```matha
def _泵_相似律功率(n1, n2, D1, D2, P1)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n1` | `` | |
| `n2` | `` | |
| `D1` | `` | |
| `D2` | `` | |
| `P1` | `` | |

Source: `src\domains\fluid_exp.py:309`

---

## `_泵_风机静压升`

```matha
def _泵_风机静压升(p_t, rho, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p_t` | `` | |
| `rho` | `` | |
| `v` | `` | |

Source: `src\domains\fluid_exp.py:312`

---

## `_管损_局部水头损失`

```matha
def _管损_局部水头损失(zeta, v, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `zeta` | `` | |
| `v` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:317`

---

## `_管损_当量长度`

```matha
def _管损_当量长度(zeta, D, f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `zeta` | `` | |
| `D` | `` | |
| `f` | `` | |

Source: `src\domains\fluid_exp.py:320`

---

## `_管损_总水头损失`

```matha
def _管损_总水头损失(f, L, D, v, sum_zeta, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `L` | `` | |
| `D` | `` | |
| `v` | `` | |
| `sum_zeta` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:325`

---

## `_管损_突扩阻力系数`

```matha
def _管损_突扩阻力系数(A1, A2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A1` | `` | |
| `A2` | `` | |

Source: `src\domains\fluid_exp.py:328`

---

## `_管损_突缩阻力系数`

```matha
def _管损_突缩阻力系数(A1, A2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A1` | `` | |
| `A2` | `` | |

Source: `src\domains\fluid_exp.py:331`

---

## `_管损_比阻`

```matha
def _管损_比阻(f, L, D, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `L` | `` | |
| `D` | `` | |
| `g_val` | `` | |

Source: `src\domains\fluid_exp.py:334`

---

## `_管损_串联比阻`

```matha
def _管损_串联比阻(S1, S2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `S1` | `` | |
| `S2` | `` | |

Source: `src\domains\fluid_exp.py:339`

---

## `_管损_并联比阻`

```matha
def _管损_并联比阻(S1, S2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `S1` | `` | |
| `S2` | `` | |

Source: `src\domains\fluid_exp.py:341`

---

## `_管损_锐缘入口系数`

```matha
def _管损_锐缘入口系数()
```

Source: `src\domains\fluid_exp.py:345`

---

## `_管损_出口损失系数`

```matha
def _管损_出口损失系数()
```

Source: `src\domains\fluid_exp.py:346`

---

## `_管损_阻力系数查询`

```matha
def _管损_阻力系数查询(name_key)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `name_key` | `` | |

Source: `src\domains\fluid_exp.py:347`

---

## `_register_fluid_exp`

```matha
def _register_fluid_exp(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\fluid_exp.py:352`

---

## `_fluid_exp_symtab_names`

```matha
def _fluid_exp_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\fluid_exp.py:441`

---

# `domains.game_dev`

```matha

```

**描述**: Matha 游戏开发领域模块。

覆盖：
  1) 游戏循环控制
  2) 精灵管理（创建、移动、碰撞）
  3) 粒子系统
  4) 物理引擎（重力、力）
  5) 音频控制
  6) 输入处理
  7) 渲染（2D/3D 投影）

Source: `src\domains\game_dev.py:1`

---

## `GameConfig`

```matha
class GameConfig
```

**描述**: 游戏配置。

**装饰器**: dataclass

Source: `src\domains\game_dev.py:25`

---

## `Sprite`

```matha
class Sprite
```

**描述**: 游戏精灵。

**装饰器**: dataclass

Source: `src\domains\game_dev.py:35`

---

## `Particle`

```matha
class Particle
```

**描述**: 粒子。

**装饰器**: dataclass

Source: `src\domains\game_dev.py:47`

---

## `game_loop`

```matha
def game_loop(dt: float, update_fn, render_fn) -> None
```

**描述**: 游戏主循环（抽象接口）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `dt` | `float` | |
| `update_fn` | `` | |
| `render_fn` | `` | |

**返回**: `None`

Source: `src\domains\game_dev.py:62`

---

## `sprite_create`

```matha
def sprite_create(x: float, y: float, w: float, h: float) -> dict
```

**描述**: 创建精灵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `y` | `float` | |
| `w` | `float` | |
| `h` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:72`

---

## `sprite_move`

```matha
def sprite_move(sprite: dict, dx: float, dy: float) -> dict
```

**描述**: 移动精灵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sprite` | `dict` | |
| `dx` | `float` | |
| `dy` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:77`

---

## `sprite_apply_force`

```matha
def sprite_apply_force(sprite: dict, fx: float, fy: float, mass: float) -> dict
```

**描述**: 对精灵施加力。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sprite` | `dict` | |
| `fx` | `float` | |
| `fy` | `float` | |
| `mass` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:84`

---

## `sprite_collide`

```matha
def sprite_collide(a: dict, b: dict) -> bool
```

**描述**: AABB 碰撞检测。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `dict` | |
| `b` | `dict` | |

**返回**: `bool`

Source: `src\domains\game_dev.py:91`

---

## `sprite_bounce`

```matha
def sprite_bounce(sprite: dict, width: float, height: float) -> dict
```

**描述**: 边界反弹。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sprite` | `dict` | |
| `width` | `float` | |
| `height` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:97`

---

## `particle_emitter`

```matha
def particle_emitter(x: float, y: float, count: int, speed: float) -> list[dict]
```

**描述**: 粒子发射器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `y` | `float` | |
| `count` | `int` | |
| `speed` | `float` | |

**返回**: `list[dict]`

Source: `src\domains\game_dev.py:110`

---

## `particle_update`

```matha
def particle_update(particles: list[dict], dt: float, gravity: float) -> list[dict]
```

**描述**: 更新粒子。

| 参数 | 类型 | 说明 |
|------|------|------|
| `particles` | `list[dict]` | |
| `dt` | `float` | |
| `gravity` | `float` | |

**返回**: `list[dict]`

Source: `src\domains\game_dev.py:124`

---

## `physics_gravity`

```matha
def physics_gravity(obj: dict, g: float, dt: float) -> dict
```

**描述**: 应用重力。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `dict` | |
| `g` | `float` | |
| `dt` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:141`

---

## `physics_apply_force`

```matha
def physics_apply_force(obj: dict, fx: float, fy: float, mass: float, dt: float) -> dict
```

**描述**: 施加力。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `dict` | |
| `fx` | `float` | |
| `fy` | `float` | |
| `mass` | `float` | |
| `dt` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:147`

---

## `physics_solve_collision`

```matha
def physics_solve_collision(a: dict, b: dict) -> tuple[dict, dict]
```

**描述**: 弹性碰撞求解。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `dict` | |
| `b` | `dict` | |

**返回**: `tuple[dict, dict]`

Source: `src\domains\game_dev.py:154`

---

## `audio_play`

```matha
def audio_play(freq: float, duration: float, volume: float) -> dict
```

**描述**: 播放音频（模拟）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `freq` | `float` | |
| `duration` | `float` | |
| `volume` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:170`

---

## `audio_stop`

```matha
def audio_stop() -> None
```

**描述**: 停止音频（模拟）。

**返回**: `None`

Source: `src\domains\game_dev.py:175`

---

## `audio_volume`

```matha
def audio_volume(vol: float) -> None
```

**描述**: 设置音量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `vol` | `float` | |

**返回**: `None`

Source: `src\domains\game_dev.py:180`

---

## `input_key`

```matha
def input_key(key: str) -> bool
```

**描述**: 检测按键（模拟）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `key` | `str` | |

**返回**: `bool`

Source: `src\domains\game_dev.py:189`

---

## `input_mouse`

```matha
def input_mouse() -> tuple[float, float]
```

**描述**: 获取鼠标位置（模拟）。

**返回**: `tuple[float, float]`

Source: `src\domains\game_dev.py:194`

---

## `input_gamepad`

```matha
def input_gamepad(axis: str) -> float
```

**描述**: 获取游戏杆输入（模拟）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `axis` | `str` | |

**返回**: `float`

Source: `src\domains\game_dev.py:199`

---

## `render_2d`

```matha
def render_2d(sprite: dict, screen_x: float, screen_y: float) -> dict
```

**描述**: 2D 渲染变换。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sprite` | `dict` | |
| `screen_x` | `float` | |
| `screen_y` | `float` | |

**返回**: `dict`

Source: `src\domains\game_dev.py:208`

---

## `render_3d`

```matha
def render_3d(point: tuple, camera_pos: tuple, target: tuple) -> tuple[float, float]
```

**描述**: 3D 到 2D 投影。

| 参数 | 类型 | 说明 |
|------|------|------|
| `point` | `tuple` | |
| `camera_pos` | `tuple` | |
| `target` | `tuple` | |

**返回**: `tuple[float, float]`

Source: `src\domains\game_dev.py:213`

---

## `camera_look_at`

```matha
def camera_look_at(eye: tuple, center: tuple, up: tuple) -> list[list[float]]
```

**描述**: 生成视图矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `eye` | `tuple` | |
| `center` | `tuple` | |
| `up` | `tuple` | |

**返回**: `list[list[float]]`

Source: `src\domains\game_dev.py:224`

---

## `_register_game_dev`

```matha
def _register_game_dev(builtins: dict) -> None
```

**描述**: 注册游戏开发内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\game_dev.py:275`

---

## `_game_dev_symtab_names`

```matha
def _game_dev_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\game_dev.py:299`

---

# `domains.genetic_algo`

```matha

```

**描述**: Matha 遗传算法与 AI 编写代码领域模块。

覆盖：
  1) 遗传算法核心（选择、交叉、变异）
  2) 神经进化（NEAT简化版）
  3) 自动代码生成
  4) 超参数搜索

Source: `src\domains\genetic_algo.py:1`

---

## `ga_evolve`

```matha
def ga_evolve(population: list[list[int]], fitness_fn: Callable, pop_size: int, max_gen: int, mutation_rate: float, crossover_rate: float) -> dict
```

**描述**: 遗传算法主循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `population` | `list[list[int]]` | |
| `fitness_fn` | `Callable` | |
| `pop_size` | `int` | |
| `max_gen` | `int` | |
| `mutation_rate` | `float` | |
| `crossover_rate` | `float` | |

**返回**: `dict`

Source: `src\domains\genetic_algo.py:20`

---

## `_roulette_select`

```matha
def _roulette_select(population: list, probs: list) -> list[int]
```

**描述**: 轮盘赌选择。

| 参数 | 类型 | 说明 |
|------|------|------|
| `population` | `list` | |
| `probs` | `list` | |

**返回**: `list[int]`

Source: `src\domains\genetic_algo.py:68`

---

## `_crossover`

```matha
def _crossover(parent1: list[int], parent2: list[int]) -> list[int]
```

**描述**: 单点交叉。

| 参数 | 类型 | 说明 |
|------|------|------|
| `parent1` | `list[int]` | |
| `parent2` | `list[int]` | |

**返回**: `list[int]`

Source: `src\domains\genetic_algo.py:79`

---

## `_mutate`

```matha
def _mutate(individual: list[int], rate: float) -> list[int]
```

**描述**: 位翻转变异。

| 参数 | 类型 | 说明 |
|------|------|------|
| `individual` | `list[int]` | |
| `rate` | `float` | |

**返回**: `list[int]`

Source: `src\domains\genetic_algo.py:86`

---

## `elitism_preserve`

```matha
def elitism_preserve(population: list[list[int]], fitness_fn: Callable, elite_count: int) -> list[list[int]]
```

**描述**: 精英保留。

| 参数 | 类型 | 说明 |
|------|------|------|
| `population` | `list[list[int]]` | |
| `fitness_fn` | `Callable` | |
| `elite_count` | `int` | |

**返回**: `list[list[int]]`

Source: `src\domains\genetic_algo.py:95`

---

## `neuro_evolve`

```matha
def neuro_evolve(genome_length: int, fitness_fn: Callable, pop_size: int, max_gen: int) -> dict
```

**描述**: 简化版神经进化（NEAT风格）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `genome_length` | `int` | |
| `fitness_fn` | `Callable` | |
| `pop_size` | `int` | |
| `max_gen` | `int` | |

**返回**: `dict`

Source: `src\domains\genetic_algo.py:106`

---

## `nesma_estimate`

```matha
def nesma_estimate(lines_of_code: int) -> float
```

**描述**: NESMA 功能点估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lines_of_code` | `int` | |

**返回**: `float`

Source: `src\domains\genetic_algo.py:113`

---

## `code_generation`

```matha
def code_generation(template: str, params: dict) -> str
```

**描述**: 模板代码生成。

| 参数 | 类型 | 说明 |
|------|------|------|
| `template` | `str` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\domains\genetic_algo.py:123`

---

## `code_optimizer`

```matha
def code_optimizer(code: str) -> str
```

**描述**: 简单代码优化（字符串层面）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `code` | `str` | |

**返回**: `str`

Source: `src\domains\genetic_algo.py:131`

---

## `hyperparameter_search`

```matha
def hyperparameter_search(search_space: dict, fitness_fn: Callable, n_iterations: int) -> dict
```

**描述**: 随机超参数搜索。

| 参数 | 类型 | 说明 |
|------|------|------|
| `search_space` | `dict` | |
| `fitness_fn` | `Callable` | |
| `n_iterations` | `int` | |

**返回**: `dict`

Source: `src\domains\genetic_algo.py:144`

---

## `_register_genetic_algo`

```matha
def _register_genetic_algo(builtins: dict) -> None
```

**描述**: 注册遗传算法内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\genetic_algo.py:180`

---

## `_genetic_algo_symtab_names`

```matha
def _genetic_algo_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\genetic_algo.py:191`

---

# `domains.graph`

```matha

```

**描述**: Matha 图算法模块实现

提供图论算法的核心实现：
  - 图数据结构
  - BFS/DFS 遍历
  - Dijkstra 最短路径
  - Prim/Kruskal 最小生成树
  - 拓扑排序
  - 连通分量分析

数学表达：
  所有算法遵循图论定义，确保正确性。

用法：
  from src.domains.graph import Graph, bfs, dfs, dijkstra, prim, kruskal

Source: `src\domains\graph.py:1`

---

## `Graph`

```matha
class Graph
```

**描述**: 有向/无向图数据结构

属性：
    n: 顶点数
    directed: 是否为有向图
    adjacency: 邻接表
    weights: 边权重 { (u, v): weight }

**装饰器**: dataclass

Source: `src\domains\graph.py:30`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 初始化邻接表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\domains\graph.py:46`

---

### `add_edge`

```matha
def add_edge(self, u: int, v: int, weight: float) -> None
```

**描述**: 添加边

Args:
    u: 起点
    v: 终点
    weight: 边权重（默认 1.0）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `u` | `int` | |
| `v` | `int` | |
| `weight` | `float` | |

**返回**: `None`

Source: `src\domains\graph.py:51`

---

### `remove_edge`

```matha
def remove_edge(self, u: int, v: int) -> None
```

**描述**: 删除边

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `u` | `int` | |
| `v` | `int` | |

**返回**: `None`

Source: `src\domains\graph.py:70`

---

### `has_edge`

```matha
def has_edge(self, u: int, v: int) -> bool
```

**描述**: 检查边是否存在

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `u` | `int` | |
| `v` | `int` | |

**返回**: `bool`

Source: `src\domains\graph.py:82`

---

### `neighbors`

```matha
def neighbors(self, u: int) -> List[int]
```

**描述**: 获取邻接顶点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `u` | `int` | |

**返回**: `List[int]`

Source: `src\domains\graph.py:86`

---

### `degree`

```matha
def degree(self, u: int) -> int
```

**描述**: 获取顶点度数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `u` | `int` | |

**返回**: `int`

Source: `src\domains\graph.py:92`

---

### `edges`

```matha
def edges(self) -> List[Tuple[int, int, float]]
```

**描述**: 获取所有边

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Tuple[int, int, float]]`

Source: `src\domains\graph.py:96`

---

### `density`

```matha
def density(self) -> float
```

**描述**: 计算图密度

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\domains\graph.py:110`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\domains\graph.py:117`

---

## `bfs`

```matha
def bfs(graph: Graph, start: int) -> Dict[int, int]
```

**描述**: BFS 遍历，返回各顶点到起点的距离

Args:
    graph: 图对象
    start: 起始顶点

Returns:
    距离字典 {顶点: 距离}

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |
| `start` | `int` | |

**返回**: `Dict[int, int]`

Source: `src\domains\graph.py:125`

---

## `bfs_path`

```matha
def bfs_path(graph: Graph, start: int, end: int) -> Optional[List[int]]
```

**描述**: BFS 最短路径

Args:
    graph: 图对象
    start: 起始顶点
    end: 目标顶点

Returns:
    路径列表，不存在则返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |
| `start` | `int` | |
| `end` | `int` | |

**返回**: `Optional[List[int]]`

Source: `src\domains\graph.py:153`

---

## `dfs`

```matha
def dfs(graph: Graph, start: int) -> List[int]
```

**描述**: DFS 遍历

Args:
    graph: 图对象
    start: 起始顶点

Returns:
    遍历顺序列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |
| `start` | `int` | |

**返回**: `List[int]`

Source: `src\domains\graph.py:195`

---

## `dfs_cycles`

```matha
def dfs_cycles(graph: Graph) -> List[List[int]]
```

**描述**: DFS 检测环（有向图）

Args:
    graph: 有向图对象

Returns:
    环列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |

**返回**: `List[List[int]]`

Source: `src\domains\graph.py:223`

---

## `dijkstra`

```matha
def dijkstra(graph: Graph, start: int) -> Tuple[List[float], List[Optional[int]]]
```

**描述**: Dijkstra 最短路径

Args:
    graph: 图对象（权重必须非负）
    start: 起始顶点

Returns:
    (距离列表, 前驱顶点列表)

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |
| `start` | `int` | |

**返回**: `Tuple[List[float], List[Optional[int]]]`

Source: `src\domains\graph.py:266`

---

## `dijkstra_path`

```matha
def dijkstra_path(graph: Graph, start: int, end: int) -> Optional[List[int]]
```

**描述**: Dijkstra 最短路径（单点对）

Args:
    graph: 图对象
    start: 起始顶点
    end: 目标顶点

Returns:
    路径列表，不存在则返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |
| `start` | `int` | |
| `end` | `int` | |

**返回**: `Optional[List[int]]`

Source: `src\domains\graph.py:304`

---

## `prim`

```matha
def prim(graph: Graph, start: int) -> List[Tuple[int, int, float]]
```

**描述**: Prim 算法最小生成树

Args:
    graph: 无向连通图
    start: 起始顶点

Returns:
    最小生成树的边列表 [(u, v, weight), ...]

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |
| `start` | `int` | |

**返回**: `List[Tuple[int, int, float]]`

Source: `src\domains\graph.py:334`

---

## `kruskal`

```matha
def kruskal(graph: Graph) -> List[Tuple[int, int, float]]
```

**描述**: Kruskal 算法最小生成树

Args:
    graph: 无向连通图

Returns:
    最小生成树的边列表 [(u, v, weight), ...]

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |

**返回**: `List[Tuple[int, int, float]]`

Source: `src\domains\graph.py:373`

---

## `topological_sort`

```matha
def topological_sort(graph: Graph) -> Optional[List[int]]
```

**描述**: 拓扑排序（Kahn 算法）

Args:
    graph: 有向无环图 (DAG)

Returns:
    拓扑排序结果，有环则返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |

**返回**: `Optional[List[int]]`

Source: `src\domains\graph.py:424`

---

## `connected_components`

```matha
def connected_components(graph: Graph) -> List[List[int]]
```

**描述**: 连通分量分析

Args:
    graph: 无向图

Returns:
    连通分量列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |

**返回**: `List[List[int]]`

Source: `src\domains\graph.py:467`

---

## `_strongly_connected_components`

```matha
def _strongly_connected_components(graph: Graph) -> List[List[int]]
```

**描述**: 有向图的强连通分量（简化版，使用 Kosaraju 算法）

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |

**返回**: `List[List[int]]`

Source: `src\domains\graph.py:502`

---

## `is_connected`

```matha
def is_connected(graph: Graph) -> bool
```

**描述**: 检查图是否连通

Args:
    graph: 无向图

Returns:
    是否连通

| 参数 | 类型 | 说明 |
|------|------|------|
| `graph` | `Graph` | |

**返回**: `bool`

Source: `src\domains\graph.py:545`

---

## `random_graph`

```matha
def random_graph(n: int, m: int, directed: bool, weight_range: Tuple[float, float]) -> Graph
```

**描述**: 生成随机图

Args:
    n: 顶点数
    m: 边数
    directed: 是否为有向图
    weight_range: 权重范围

Returns:
    随机图对象

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `m` | `int` | |
| `directed` | `bool` | |
| `weight_range` | `Tuple[float, float]` | |

**返回**: `Graph`

Source: `src\domains\graph.py:565`

---

## `complete_graph`

```matha
def complete_graph(n: int, weight: float) -> Graph
```

**描述**: 生成完全图

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `weight` | `float` | |

**返回**: `Graph`

Source: `src\domains\graph.py:595`

---

## `path_graph`

```matha
def path_graph(n: int, weight: float) -> Graph
```

**描述**: 生成路径图

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `weight` | `float` | |

**返回**: `Graph`

Source: `src\domains\graph.py:604`

---

## `cycle_graph`

```matha
def cycle_graph(n: int, weight: float) -> Graph
```

**描述**: 生成环图

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `weight` | `float` | |

**返回**: `Graph`

Source: `src\domains\graph.py:612`

---

## `_register_graph`

```matha
def _register_graph(builtins: dict) -> None
```

**描述**: 注册图算法内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\graph.py:625`

---

## `graph_symtab_names`

```matha
def graph_symtab_names() -> list[str]
```

**描述**: 返回图算法模块的符号表名列表。

**返回**: `list[str]`

Source: `src\domains\graph.py:646`

---

# `domains.graphics`

```matha

```

**描述**: Matha 图形学领域模块：图形变换、渲染管线、几何计算、颜色空间。

覆盖：
  1) 齐次变换矩阵
  2) 投影变换
  3) 裁剪区域
  4) 光栅化点数
  5) 颜色空间转换
  6) 抗锯齿系数

Source: `src\domains\graphics.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\graphics.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\graphics.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\graphics.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\graphics.py:31`

---

## `_齐次变换矩阵`

```matha
def _齐次变换矩阵(旋转角, 平移_x, 平移_y, 缩放)
```

**描述**: 生成2D齐次变换矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `旋转角` | `` | |
| `平移_x` | `` | |
| `平移_y` | `` | |
| `缩放` | `` | |

Source: `src\domains\graphics.py:40`

---

## `_投影变换`

```matha
def _投影变换(视锥_near, 视锥_far, fov_deg)
```

**描述**: 透视投影参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `视锥_near` | `` | |
| `视锥_far` | `` | |
| `fov_deg` | `` | |

Source: `src\domains\graphics.py:50`

---

## `_裁剪区域`

```matha
def _裁剪区域(边界_x_min, 边界_y_min, 边界_x_max, 边界_y_max)
```

**描述**: 裁剪区域面积。

| 参数 | 类型 | 说明 |
|------|------|------|
| `边界_x_min` | `` | |
| `边界_y_min` | `` | |
| `边界_x_max` | `` | |
| `边界_y_max` | `` | |

Source: `src\domains\graphics.py:60`

---

## `_光栅化点数`

```matha
def _光栅化点数(宽度_px, 高度_px, 抗锯齿等级)
```

**描述**: 光栅化后像素点数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `宽度_px` | `` | |
| `高度_px` | `` | |
| `抗锯齿等级` | `` | |

Source: `src\domains\graphics.py:67`

---

## `_颜色空间转换`

```matha
def _颜色空间转换(R, G, B)
```

**描述**: RGB转HSV。

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |
| `G` | `` | |
| `B` | `` | |

Source: `src\domains\graphics.py:74`

---

## `_抗锯齿系数`

```matha
def _抗锯齿系数(采样数)
```

**描述**: 抗锯齿质量系数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `采样数` | `` | |

Source: `src\domains\graphics.py:93`

---

## `_register_graphics`

```matha
def _register_graphics(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\graphics.py:104`

---

## `_graphics_symtab_names`

```matha
def _graphics_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\graphics.py:113`

---

# `domains.green_tech`

```matha

```

**描述**: Matha 绿色技术领域模块：碳足迹、能效优化、可再生能源。

覆盖：
  1) 碳足迹估算
  2) 能源效率
  3) 太阳能转化率
  4) 风力发电系数
  5) 电池循环寿命
  6) 减排量计算

Source: `src\domains\green_tech.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\green_tech.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\green_tech.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\green_tech.py:25`

---

## `_碳足迹估算`

```matha
def _碳足迹估算(能源_kWh, 排放因子)
```

**描述**: 碳足迹（kg CO2）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `能源_kWh` | `` | |
| `排放因子` | `` | |

Source: `src\domains\green_tech.py:32`

---

## `_能源效率`

```matha
def _能源效率(输出能量, 输入能量)
```

**描述**: 能源效率（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `输出能量` | `` | |
| `输入能量` | `` | |

Source: `src\domains\green_tech.py:37`

---

## `_太阳能转化率`

```matha
def _太阳能转化率(入射功率_W, 面积_m2, 效率)
```

**描述**: 太阳能转换功率（W）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `入射功率_W` | `` | |
| `面积_m2` | `` | |
| `效率` | `` | |

Source: `src\domains\green_tech.py:44`

---

## `_风力发电系数`

```matha
def _风力发电系数(风速_ms, 叶片半径_m, 空气密度)
```

**描述**: 风力发电功率（W）。P = 0.5·ρ·A·v³·Cp。

| 参数 | 类型 | 说明 |
|------|------|------|
| `风速_ms` | `` | |
| `叶片半径_m` | `` | |
| `空气密度` | `` | |

Source: `src\domains\green_tech.py:50`

---

## `_电池循环寿命`

```matha
def _电池循环寿命(容量衰减率_per_cycle, 目标容量保持率)
```

**描述**: 电池循环寿命估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `容量衰减率_per_cycle` | `` | |
| `目标容量保持率` | `` | |

Source: `src\domains\green_tech.py:57`

---

## `_减排量计算`

```matha
def _减排量计算(替代能源_kWh, 传统排放因子, 替代排放因子)
```

**描述**: 减排量（kg CO2）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `替代能源_kWh` | `` | |
| `传统排放因子` | `` | |
| `替代排放因子` | `` | |

Source: `src\domains\green_tech.py:64`

---

## `_register_green_tech`

```matha
def _register_green_tech(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\green_tech.py:73`

---

## `_green_tech_symtab_names`

```matha
def _green_tech_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\green_tech.py:82`

---

# `domains.hardware`

```matha

```

**描述**: Hardware Domain: 硬件控制与系统交互内建。

覆盖：
  - 进程管理：exec、kill、wait、ps
  - 网络通信：socket_send、socket_recv、http_get、dns_resolve
  - 文件系统扩展：目录操作、权限查询
  - 系统信息：cpu_count、memory_info、disk_info、uptime
  - GPIO/设备模拟：pin_mode、pin_write、pin_read（仿真层，适配真实硬件需扩展）
  - 环境交互：环境变量读写、系统命令执行

注意：GPIO/设备层在解释器层面为仿真模式；
      真实硬件访问需通过 sys.path 注入 platform 层（如 pigpio, pyserial, RPi.GPIO）。

Source: `src\domains\hardware.py:1`

---

## `_cpu_count`

```matha
def _cpu_count() -> int
```

**描述**: 返回逻辑 CPU 核心数。

**返回**: `int`

Source: `src\domains\hardware.py:30`

---

## `_memory_info`

```matha
def _memory_info() -> dict
```

**描述**: 返回内存信息（bytes）。

**返回**: `dict`

Source: `src\domains\hardware.py:35`

---

## `_platform_info`

```matha
def _platform_info() -> str
```

**描述**: 返回当前平台标识。

**返回**: `str`

Source: `src\domains\hardware.py:50`

---

## `_arch_info`

```matha
def _arch_info() -> str
```

**描述**: 返回架构信息。

**返回**: `str`

Source: `src\domains\hardware.py:55`

---

## `_exec_cmd`

```matha
def _exec_cmd(cmd: str, timeout: int) -> dict
```

**描述**: 执行系统命令，返回 {code, stdout, stderr}。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cmd` | `str` | |
| `timeout` | `int` | |

**返回**: `dict`

Source: `src\domains\hardware.py:64`

---

## `_ps`

```matha
def _ps() -> list[dict]
```

**描述**: 返回当前进程列表。

**返回**: `list[dict]`

Source: `src\domains\hardware.py:81`

---

## `_kill`

```matha
def _kill(pid: int, sig: int) -> bool
```

**描述**: 向进程发送信号。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pid` | `int` | |
| `sig` | `int` | |

**返回**: `bool`

Source: `src\domains\hardware.py:101`

---

## `_wait`

```matha
def _wait(pid: int, timeout: int) -> bool
```

**描述**: 等待进程结束。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pid` | `int` | |
| `timeout` | `int` | |

**返回**: `bool`

Source: `src\domains\hardware.py:110`

---

## `_socket_send`

```matha
def _socket_send(host: str, port: int, data: str, timeout: int) -> dict
```

**描述**: 发送 TCP 数据，返回 {sent, response}。

| 参数 | 类型 | 说明 |
|------|------|------|
| `host` | `str` | |
| `port` | `int` | |
| `data` | `str` | |
| `timeout` | `int` | |

**返回**: `dict`

Source: `src\domains\hardware.py:123`

---

## `_socket_recv`

```matha
def _socket_recv(host: str, port: int, timeout: int) -> str
```

**描述**: 连接并接收数据（HTTP GET 场景）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `host` | `str` | |
| `port` | `int` | |
| `timeout` | `int` | |

**返回**: `str`

Source: `src\domains\hardware.py:137`

---

## `_http_get`

```matha
def _http_get(url: str, timeout: int) -> dict
```

**描述**: HTTP GET 请求，返回 {status, body}。

| 参数 | 类型 | 说明 |
|------|------|------|
| `url` | `str` | |
| `timeout` | `int` | |

**返回**: `dict`

Source: `src\domains\hardware.py:151`

---

## `_dns_resolve`

```matha
def _dns_resolve(hostname: str) -> list[str]
```

**描述**: DNS 解析，返回 IP 列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `hostname` | `str` | |

**返回**: `list[str]`

Source: `src\domains\hardware.py:187`

---

## `_ping`

```matha
def _ping(host: str, count: int) -> dict
```

**描述**: Ping 测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `host` | `str` | |
| `count` | `int` | |

**返回**: `dict`

Source: `src\domains\hardware.py:196`

---

## `_gpio_init`

```matha
def _gpio_init(pin: int, mode: str) -> None
```

**描述**: 初始化 GPIO 引脚（仿真）。mode: 'in' | 'out' | 'pwm'。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\domains\hardware.py:213`

---

## `_gpio_set`

```matha
def _gpio_set(pin: int, value: int) -> None
```

**描述**: 设置 GPIO 引脚输出。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\domains\hardware.py:220`

---

## `_gpio_get`

```matha
def _gpio_get(pin: int) -> int
```

**描述**: 读取 GPIO 引脚输入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |

**返回**: `int`

Source: `src\domains\hardware.py:229`

---

## `_gpio_pwm`

```matha
def _gpio_pwm(pin: int, duty: float, freq: int) -> None
```

**描述**: 设置 PWM 输出（仿真）。duty: 0-100。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |
| `duty` | `float` | |
| `freq` | `int` | |

**返回**: `None`

Source: `src\domains\hardware.py:238`

---

## `_gpio_cleanup`

```matha
def _gpio_cleanup(pin: int) -> None
```

**描述**: 清理 GPIO 引脚。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |

**返回**: `None`

Source: `src\domains\hardware.py:247`

---

## `_list_dir`

```matha
def _list_dir(path: str) -> list[str]
```

**描述**: 列出目录内容。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

**返回**: `list[str]`

Source: `src\domains\hardware.py:256`

---

## `_mkdir`

```matha
def _mkdir(path: str, recursive: bool) -> bool
```

**描述**: 创建目录。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `recursive` | `bool` | |

**返回**: `bool`

Source: `src\domains\hardware.py:264`

---

## `_file_exists`

```matha
def _file_exists(path: str) -> bool
```

**描述**: 检查文件是否存在。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

**返回**: `bool`

Source: `src\domains\hardware.py:276`

---

## `_file_size`

```matha
def _file_size(path: str) -> int
```

**描述**: 返回文件大小（字节）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

**返回**: `int`

Source: `src\domains\hardware.py:281`

---

## `_env_get`

```matha
def _env_get(name: str, default: str) -> str
```

**描述**: 读取环境变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `default` | `str` | |

**返回**: `str`

Source: `src\domains\hardware.py:293`

---

## `_env_set`

```matha
def _env_set(name: str, value: str) -> None
```

**描述**: 设置环境变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `value` | `str` | |

**返回**: `None`

Source: `src\domains\hardware.py:298`

---

## `_register_hardware`

```matha
def _register_hardware(builtins: dict) -> None
```

**描述**: 将硬件控制内建注册到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\hardware.py:307`

---

## `_register_hardware_symtab_names`

```matha
def _register_hardware_symtab_names() -> list[str]
```

**描述**: 返回硬件域所有内建名。

**返回**: `list[str]`

Source: `src\domains\hardware.py:346`

---

## `_curry2`

```matha
def _curry2(fn)
```

**描述**: 两参 Python 函数 → 柯里化 f(a)(b)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

Source: `src\domains\hardware.py:359`

---

## `_curry3`

```matha
def _curry3(fn)
```

**描述**: 三参 → 柯里化 f(a)(b)(c)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

Source: `src\domains\hardware.py:366`

---

# `domains.hardware_reverse`

```matha

```

**描述**: Matha 硬件逆向工程领域模块：信号分析、协议解析、固件提取、功耗分析。

覆盖：
  1) 信号频率分析
  2) 协议解析率
  3) 固件完整性校验
  4) 逆向复杂度
  5) 时钟频率估算
  6) 功耗分析

Source: `src\domains\hardware_reverse.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\hardware_reverse.py:18`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\hardware_reverse.py:22`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\hardware_reverse.py:26`

---

## `_信号频率分析`

```matha
def _信号频率分析(采样率, 窗口长度_ms)
```

**描述**: FFT频率分辨率（Hz）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `采样率` | `` | |
| `窗口长度_ms` | `` | |

Source: `src\domains\hardware_reverse.py:33`

---

## `_协议解析率`

```matha
def _协议解析率(总消息数, 成功解析数)
```

**描述**: 协议消息解析成功率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `总消息数` | `` | |
| `成功解析数` | `` | |

Source: `src\domains\hardware_reverse.py:41`

---

## `_固件完整性校验`

```matha
def _固件完整性校验(固件大小_MB, 校验和匹配)
```

**描述**: 固件完整性评估。

| 参数 | 类型 | 说明 |
|------|------|------|
| `固件大小_MB` | `` | |
| `校验和匹配` | `` | |

Source: `src\domains\hardware_reverse.py:48`

---

## `_逆向复杂度`

```matha
def _逆向复杂度(代码行数, 函数数, 调用图复杂度)
```

**描述**: 逆向工程复杂度估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `代码行数` | `` | |
| `函数数` | `` | |
| `调用图复杂度` | `` | |

Source: `src\domains\hardware_reverse.py:55`

---

## `_时钟频率估算`

```matha
def _时钟频率估算(周期_ns)
```

**描述**: 从周期估算时钟频率（MHz）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `周期_ns` | `` | |

Source: `src\domains\hardware_reverse.py:62`

---

## `_功耗分析`

```matha
def _功耗分析(电压_V, 电流_mA, 占空比)
```

**描述**: 平均功耗（mW）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `电压_V` | `` | |
| `电流_mA` | `` | |
| `占空比` | `` | |

Source: `src\domains\hardware_reverse.py:69`

---

## `_register_hardware_reverse`

```matha
def _register_hardware_reverse(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\hardware_reverse.py:78`

---

## `_hardware_reverse_symtab_names`

```matha
def _hardware_reverse_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\hardware_reverse.py:87`

---

# `domains.hpc`

```matha

```

**描述**: Matha HPC领域模块：并行计算、负载均衡、通信开销、加速比。

覆盖：
  1) Amdahl加速比
  2) 并行效率
  3) 通信延迟估算
  4) 负载均衡度
  5) 内存带宽利用率
  6) 浮点运算峰值

Source: `src\domains\hpc.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\hpc.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\hpc.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\hpc.py:25`

---

## `_Amdahl加速比`

```matha
def _Amdahl加速比(串行比例, 核心数)
```

**描述**: Amdahl定律加速比 S = 1/(s + p/n)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `串行比例` | `` | |
| `核心数` | `` | |

Source: `src\domains\hpc.py:32`

---

## `_并行效率`

```matha
def _并行效率(加速比, 核心数)
```

**描述**: 并行效率 E = S/n。

| 参数 | 类型 | 说明 |
|------|------|------|
| `加速比` | `` | |
| `核心数` | `` | |

Source: `src\domains\hpc.py:40`

---

## `_通信延迟估算`

```matha
def _通信延迟估算(消息数, 消息大小_bytes, 带宽_Gbps)
```

**描述**: 通信总延迟（ms）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `消息数` | `` | |
| `消息大小_bytes` | `` | |
| `带宽_Gbps` | `` | |

Source: `src\domains\hpc.py:47`

---

## `_负载均衡度`

```matha
def _负载均衡度(任务列表)
```

**描述**: 负载均衡度（标准差/均值）。越低越均衡。

| 参数 | 类型 | 说明 |
|------|------|------|
| `任务列表` | `` | |

Source: `src\domains\hpc.py:55`

---

## `_内存带宽利用率`

```matha
def _内存带宽利用率(访问次数, 数据量_GB, 时间_s)
```

**描述**: 内存带宽利用率（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `访问次数` | `` | |
| `数据量_GB` | `` | |
| `时间_s` | `` | |

Source: `src\domains\hpc.py:67`

---

## `_浮点运算峰值`

```matha
def _浮点运算峰值(核心数, 频率_GHz, FLOPS_per_cycle)
```

**描述**: 峰值浮点运算能力（TFLOPS）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `核心数` | `` | |
| `频率_GHz` | `` | |
| `FLOPS_per_cycle` | `` | |

Source: `src\domains\hpc.py:76`

---

## `_register_hpc`

```matha
def _register_hpc(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\hpc.py:85`

---

## `_hpc_symtab_names`

```matha
def _hpc_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\hpc.py:94`

---

# `domains.iot_hardware`

```matha

```

**描述**: Matha IoT硬件领域模块：MQTT通信、传感器网络、边缘计算、设备管理。

覆盖：
  1) MQTT消息大小估算
  2) 传感器网络覆盖半径
  3) 边缘计算延迟
  4) 设备在线率估算
  5) 数据聚合效率
  6) 功耗预算分析

Source: `src\domains\iot_hardware.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\iot_hardware.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\iot_hardware.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\iot_hardware.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\iot_hardware.py:31`

---

## `_MQTT消息大小估算`

```matha
def _MQTT消息大小估算(主题长度, 载荷字节, QoS等级)
```

**描述**: MQTT消息总大小（字节）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `主题长度` | `` | |
| `载荷字节` | `` | |
| `QoS等级` | `` | |

Source: `src\domains\iot_hardware.py:40`

---

## `_传感器覆盖半径`

```matha
def _传感器覆盖半径(发射功率_dBm, 灵敏度_dBm, 频率_GHz)
```

**描述**: 自由空间路径损耗模型估算覆盖半径（米）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `发射功率_dBm` | `` | |
| `灵敏度_dBm` | `` | |
| `频率_GHz` | `` | |

Source: `src\domains\iot_hardware.py:49`

---

## `_边缘延迟计算`

```matha
def _边缘延迟计算(任务数, 每任务计算_ms, 网络延迟_ms)
```

**描述**: 边缘计算总延迟。

| 参数 | 类型 | 说明 |
|------|------|------|
| `任务数` | `` | |
| `每任务计算_ms` | `` | |
| `网络延迟_ms` | `` | |

Source: `src\domains\iot_hardware.py:63`

---

## `_设备在线率`

```matha
def _设备在线率(总设备数, 离线设备数, 监控周期_h)
```

**描述**: 设备在线率估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `总设备数` | `` | |
| `离线设备数` | `` | |
| `监控周期_h` | `` | |

Source: `src\domains\iot_hardware.py:68`

---

## `_数据聚合效率`

```matha
def _数据聚合效率(源数据量_MB, 聚合后量_MB, 压缩算法)
```

**描述**: 数据聚合效率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `源数据量_MB` | `` | |
| `聚合后量_MB` | `` | |
| `压缩算法` | `` | |

Source: `src\domains\iot_hardware.py:75`

---

## `_功耗预算`

```matha
def _功耗预算(组件列表)
```

**描述**: 功耗预算计算（W）。组件列表: [(功耗_W, 占空比), ...]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `组件列表` | `` | |

Source: `src\domains\iot_hardware.py:82`

---

## `_register_iot_hardware`

```matha
def _register_iot_hardware(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\iot_hardware.py:94`

---

## `_iot_hardware_symtab_names`

```matha
def _iot_hardware_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\iot_hardware.py:103`

---

# `domains.kernel`

```matha

```

**描述**: 内核数学函数注册。

供 SemanticAnalyzer 和 Interpreter 在初始化时调用，
将 kernel_math 模块的函数注册到内建符号表 / builtins。

Source: `src\domains\kernel.py:1`

---

# `domains.kernel_math`

```matha

```

**描述**: Kernel Math: 操作系统内核数学模型与计算公式。

覆盖：系统调用表、PCB 内存布局、页表开销、中断延迟、进程调度内核开销。
所有函数均为数学公式/估算，不生成实际可执行代码（代码生成由 codegen.kernel 负责）。

Source: `src\domains\kernel_math.py:1`

---

## `syscall_num`

```matha
def syscall_num(syscall_name: str) -> int
```

**描述**: 返回系统调用号（基于 Linux x86 32-bit 约定）。

常用 syscall 号：
    write=4, read=3, exit=1, fork=2, open=5, close=6,
    mmap=192, brk=45, ioctl=54, kill=37

| 参数 | 类型 | 说明 |
|------|------|------|
| `syscall_name` | `str` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:15`

---

## `syscall_entry_size`

```matha
def syscall_entry_size(syscall_count: int) -> int
```

**描述**: 系统调用表入口总大小（字节）。

每项 8 字节（32 位系统）：跳转目标(4B) + padding(4B)

| 参数 | 类型 | 说明 |
|------|------|------|
| `syscall_count` | `int` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:75`

---

## `syscall_latency_ns`

```matha
def syscall_latency_ns(kernel_mode: bool) -> float
```

**描述**: 系统调用开销（纳秒估算）。

kernel_mode=True: 纯内核路径（无用户态切换）
kernel_mode=False: 含用户态→内核态切换

| 参数 | 类型 | 说明 |
|------|------|------|
| `kernel_mode` | `bool` | |

**返回**: `float`

Source: `src\domains\kernel_math.py:83`

---

## `pcb_size`

```matha
def pcb_size(num_registers: int, stack_size: int) -> int
```

**描述**: 计算 PCB 结构体大小（字节）。

典型 x86 32-bit PCB 组成：
    - 进程状态 (4B)
    - PID / PPID (8B)
    - 寄存器快照 (num_registers × 4B)
    - 栈指针 + 栈大小 (8B)
    - 内存映射指针 (4B)
    - 文件描述符表指针 (4B)
    - 信号处理表 (32B)
    - 调度信息 (16B)
    - 对齐填充

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_registers` | `int` | |
| `stack_size` | `int` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:99`

---

## `pcb_alloc_cost`

```matha
def pcb_alloc_cost(num_tasks: int, pcb_size: int) -> int
```

**描述**: 分配 num_tasks 个 PCB 所需的总内存（字节）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_tasks` | `int` | |
| `pcb_size` | `int` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:118`

---

## `pcb_context_switch_cycles`

```matha
def pcb_context_switch_cycles(num_registers: int) -> int
```

**描述**: 上下文切换开销（时钟周期估算）。

包括：保存当前寄存器 → 切换页表 → 加载新寄存器

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_registers` | `int` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:123`

---

## `page_table_entries`

```matha
def page_table_entries(page_size: int, memory_size: int, page_table_entries_per_table: int) -> int
```

**描述**: 计算需要的页表项总数（含多级页表）。

x86 32-bit 使用两级页表：
    - PML4 不存在（32-bit 无 PAE）
    - PD（页目录）：1024 项 × 4B = 4KB
    - PT（页表）：1024 项 × 4B = 4KB

| 参数 | 类型 | 说明 |
|------|------|------|
| `page_size` | `int` | |
| `memory_size` | `int` | |
| `page_table_entries_per_table` | `int` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:138`

---

## `page_table_overhead_bytes`

```matha
def page_table_overhead_bytes(page_size: int, memory_size: int) -> int
```

**描述**: 页表结构本身占用的内存（字节）。

每级页表固定 4KB（1024 项 × 4B）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `page_size` | `int` | |
| `memory_size` | `int` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:157`

---

## `linear_address`

```matha
def linear_address(page_table: int, page_offset: int, page_size: int) -> int
```

**描述**: 物理地址 = page_table * page_size + page_offset。

| 参数 | 类型 | 说明 |
|------|------|------|
| `page_table` | `int` | |
| `page_offset` | `int` | |
| `page_size` | `int` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:171`

---

## `virtual_to_physical`

```matha
def virtual_to_physical(vaddr: int, page_size: int) -> tuple[int, int]
```

**描述**: 虚拟地址 → (页号, 页内偏移)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `vaddr` | `int` | |
| `page_size` | `int` | |

**返回**: `tuple[int, int]`

Source: `src\domains\kernel_math.py:176`

---

## `interrupt_latency_us`

```matha
def interrupt_latency_us(irq_number: int, pic_master: bool) -> float
```

**描述**: 中断响应延迟估算（微秒）。

IRQ0-7: 主 PIC（8259A）
IRQ8-15: 从 PIC（级联）

| 参数 | 类型 | 说明 |
|------|------|------|
| `irq_number` | `int` | |
| `pic_master` | `bool` | |

**返回**: `float`

Source: `src\domains\kernel_math.py:185`

---

## `exception_vector`

```matha
def exception_vector(vector: int) -> str
```

**描述**: 返回异常向量名称。

| 参数 | 类型 | 说明 |
|------|------|------|
| `vector` | `int` | |

**返回**: `str`

Source: `src\domains\kernel_math.py:196`

---

## `scheduler_tick_latency_us`

```matha
def scheduler_tick_latency_us(tsc_mhz: int) -> float
```

**描述**: 调度器时钟滴答开销（微秒）。

TSC (Time Stamp Counter) 频率 MHz。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tsc_mhz` | `int` | |

**返回**: `float`

Source: `src\domains\kernel_math.py:230`

---

## `context_switch_overhead_us`

```matha
def context_switch_overhead_us(tsc_mhz: int, num_regs: int) -> float
```

**描述**: 上下文切换总开销（微秒）。

包括：TLB flush + 寄存器保存/加载 + 栈切换 + 页表切换

| 参数 | 类型 | 说明 |
|------|------|------|
| `tsc_mhz` | `int` | |
| `num_regs` | `int` | |

**返回**: `float`

Source: `src\domains\kernel_math.py:239`

---

## `throughput_max_tasks`

```matha
def throughput_max_tasks(num_tasks: int, switch_us: float) -> float
```

**描述**: 最大吞吐任务数估算（基于上下文切换开销）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_tasks` | `int` | |
| `switch_us` | `float` | |

**返回**: `float`

Source: `src\domains\kernel_math.py:251`

---

## `kernel_mem_layout`

```matha
def kernel_mem_layout(text_size: int, data_size: int, bss_size: int, stack_size: int, page_size: int) -> dict
```

**描述**: 计算内核内存布局各段地址（x86 32-bit 传统布局）。

返回 dict: {段名: (起始地址, 大小)}

| 参数 | 类型 | 说明 |
|------|------|------|
| `text_size` | `int` | |
| `data_size` | `int` | |
| `bss_size` | `int` | |
| `stack_size` | `int` | |
| `page_size` | `int` | |

**返回**: `dict`

Source: `src\domains\kernel_math.py:265`

---

## `total_kernel_mem`

```matha
def total_kernel_mem(layout: dict) -> int
```

**描述**: 计算内核总内存占用（字节）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `layout` | `dict` | |

**返回**: `int`

Source: `src\domains\kernel_math.py:296`

---

## `_register_kernel_builtins`

```matha
def _register_kernel_builtins(builtins: dict) -> None
```

**描述**: 将 kernel math 函数注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\kernel_math.py:305`

---

## `kernel_symtab_names`

```matha
def kernel_symtab_names() -> list[str]
```

**描述**: 返回 kernel math 模块的符号表名列表。

**返回**: `list[str]`

Source: `src\domains\kernel_math.py:326`

---

# `domains.mech_design`

```matha

```

**描述**: Matha 领域扩展模块：机械设计（Mechanical Design / Machine Design）。

基于 Matha 数学基础 + mechanics.py（运动学/材料力学）+ dynamics.py（动力学）+ structural.py（结构力学），
演化机械设计通用零部件设计与验算功能。所有函数以普通 Python callable 注册到解释器 builtins。

六大子领域：

一、轴与连接件（Shaft & Connection）- 前缀 轴设_
  1) 轴的扭转强度校核 τ = T/(Wt) = 16T/(πd³)
  2) 轴的弯曲强度校核 σ = M/W = 32M/(πd³)
  3) 弯扭组合当量弯矩 Me = √(M² + (αT)²)
  4) 按扭转初估轴径 d ≥ ∛(9.55×10⁶P/(0.2·τ·n)) 简化
  5) 平键挤压强度校核 σp = 4T/(d·h·L)
  6) 平键剪切强度校核 τ = 2T/(d·b·L)
  7) 花键挤压强度简化 σp = 8T/(z·h·L·Dm)
  8) 过盈配合传递扭矩简化（摩擦式）T = π·p·d·L·μ·d/2

二、滚动轴承（Rolling Bearing）- 前缀 轴承_
  1) 基本额定寿命 L10 = (C/P)^ε × 10⁶ 转（ε=3 球/10/3 滚子）
  2) 寿命小时值 Lh = (10⁶/60n)·(C/P)^ε
  3) 当量动载荷 P = X·Fr + Y·Fa（X,Y 为系数简化默认 1）
  4) 静载荷额定寿命 C0 校核 P0 ≤ C0/S0
  5) 角接触轴承附加轴向力简化 Fa = e·Fr
  6) 寿命系数 fn = (33.3/n)^(1/ε) 或 (1000060/n)^(1/ε)

三、齿轮传动（Gear Drive）- 前缀 齿轮_
  1) 直齿圆柱齿轮接触疲劳 σH = ZE·ZH·√(2KT1·(u+1)/(φd·b·d1²·u))
  2) 直齿圆柱齿轮弯曲疲劳 σF = 2KT1/(φd·b·m²·z1)·YFa·YSa
  3) 齿轮中心距 a = m(z1+z2)/2
  4) 分度圆直径 d = m·z
  5) 齿宽 b = φd·d1
  6) 传动比 i = n1/n2 = z2/z1
  7) 齿轮圆周速度 v = π·d1·n1/(60×1000) m/s
  8) 许用接触应力 σHP = σHlim·Zn/S_Hmin（简化）

四、弹簧设计（Spring Design）- 前缀 弹簧_
  1) 圆柱螺旋压缩弹簧刚度 k = G·d⁴/(8·D³·n)
  2) 弹簧最大切应力 τmax = 8·K·D·Fmax/(π·d³)
  3) 曲度系数 K = (4C-1)/(4C-4) + 0.615/C（C=D/d）
  4) 弹簧变形量 λ = 8·F·D³·n/(G·d⁴)
  5) 弹簧总圈数 n1 = n + 2（两端圈各一圈，Y 型端部）
  6) 弹簧自由高度 H0 = n·p + 1.5·d（Y 型端部）
  7) 螺旋角 α = arctan(p/(π·D))

五、紧固件与连接件（Fasteners）- 前缀 联接_
  1) 受拉螺栓强度校核 σ = 1.3·F'/ (π·d1²/4)（预紧+静载）
  2) 受剪螺栓强度校核 τ = F/(m·π·d0²/4)
  3) 螺栓挤压强度校核 σp = F/(d0·Σt)
  4) 残余预紧力 F'' = χ·F（变载 χ=0.6~1.0，静载 χ=0.2~0.6）
  5) 螺栓总拉力 F' = F + F''（受轴向变载）
  6) 键类型查用：平键规格 b×h 按轴径 d
  7) 销的剪切强度 τ = 4F/(π·d²·z)

六、公差配合与可靠性（Tolerance & Reliability）- 前缀 公差_
  1) IT 标准公差值（IT01-IT18 简化公式 i = 0.45·∛D + 0.001·D，按公差等级系数）
  2) 基本偏差查询（简化：轴 h/H 基孔制/基轴制常用值）
  3) 配合间隙/过盈 Xmin/Ymax（间隙/过渡/过盈配合）
  4) 尺寸链极值法封闭环：封闭环 = Σ(增环) - Σ(减环)
  5) 尺寸链极值法公差：T0 = ΣTi（所有组成环公差和）
  6) 表面粗糙度 Ra 查用（加工方法典型值）
  7) 可靠度计算 R(t) = exp(-(t/η)^β)（威布尔分布）

数据库：
  - 常用轴用材料（45/40Cr/35CrMo 的 σ-1/σb）
  - 深沟球轴承额定动载荷 C（6200~6312 典型值）
  - 齿轮材料接触/弯曲疲劳极限（45/40Cr/20CrMnTi）
  - 弹簧材料许用切应力与切变模量（碳素/弹簧钢/不锈钢）
  - 螺栓强度等级（4.6~12.9 级的 σs/σb）
  - 键规格标准（轴径 d 对应平键 b×h）
  - IT 标准公差等级系数（IT5~IT12）
  - 表面粗糙度典型 Ra 值（加工方法对应）

设计原则：
  - 与现有领域保持一致：_curryN 柯里化、前缀区分子领域
  - 公式基于中国机械设计通用规范（GB/T）简化
  - 参数单位：长度 mm, 力 N, 应力 MPa, 功率 kW, 转速 r/min
  - 返回纯数值，除零等异常由 Python 自行抛错

Source: `src\domains\mech_design.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mech_design.py:86`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mech_design.py:90`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mech_design.py:96`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mech_design.py:104`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mech_design.py:114`

---

## `_轴设_扭转切应力`

```matha
def _轴设_扭转切应力(T, d)
```

**描述**: τ = T / Wt = 16T / (πd³)，T 单位 N·mm，d mm，返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `d` | `` | |

Source: `src\domains\mech_design.py:283`

---

## `_轴设_弯曲正应力`

```matha
def _轴设_弯曲正应力(M, d)
```

**描述**: σ = M / W = 32M / (πd³)，M 单位 N·mm，d mm，返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `d` | `` | |

Source: `src\domains\mech_design.py:290`

---

## `_轴设_弯扭当量弯矩`

```matha
def _轴设_弯扭当量弯矩(M, T, alpha)
```

**描述**: Me = √(M² + (α·T)²)，α 为折合系数（不变 0.3，脉动 0.6）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `T` | `` | |
| `alpha` | `` | |

Source: `src\domains\mech_design.py:297`

---

## `_轴设_按扭转初估直径`

```matha
def _轴设_按扭转初估直径(P, n, tau_allow)
```

**描述**: 按扭转强度初估轴径：d ≥ ∛(9.55e6·P / (0.2·τ·n))。
P kW，n r/min，tau_allow MPa → 返回 d mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `n` | `` | |
| `tau_allow` | `` | |

Source: `src\domains\mech_design.py:302`

---

## `_轴设_平键挤压强度`

```matha
def _轴设_平键挤压强度(T, d, h, L)
```

**描述**: σp = 4T / (d·h·L)，T N·mm，d,h,L mm → 返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `d` | `` | |
| `h` | `` | |
| `L` | `` | |

Source: `src\domains\mech_design.py:312`

---

## `_轴设_平键剪切强度`

```matha
def _轴设_平键剪切强度(T, d, b, L)
```

**描述**: τ = 2T / (d·b·L)，T N·mm，d,b,L mm → 返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `d` | `` | |
| `b` | `` | |
| `L` | `` | |

Source: `src\domains\mech_design.py:320`

---

## `_轴设_查平键规格`

```matha
def _轴设_查平键规格(d)
```

**描述**: 按轴径 d（mm）查平键规格 b×h（GB/T 1095 简化）。
返回 (b, h, t1, t2)，未匹配时返回 (0,0,0,0)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |

Source: `src\domains\mech_design.py:328`

---

## `_轴设_花键挤压强度`

```matha
def _轴设_花键挤压强度(T, z, h, L, Dm)
```

**描述**: 矩形花键挤压强度（简化）：σp = 8T / (z·h·L·Dm)，T N·mm，其他 mm，返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `z` | `` | |
| `h` | `` | |
| `L` | `` | |
| `Dm` | `` | |

Source: `src\domains\mech_design.py:337`

---

## `_轴设_过盈配合扭矩`

```matha
def _轴设_过盈配合扭矩(p, d, L, mu)
```

**描述**: 过盈配合传递摩擦扭矩 T = π·p·d·L·μ·d/2。
p 配合压强 MPa，d 配合直径 mm，L 配合长度 mm，μ 摩擦系数 → T N·mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `d` | `` | |
| `L` | `` | |
| `mu` | `` | |

Source: `src\domains\mech_design.py:345`

---

## `_轴承_额定寿命_转`

```matha
def _轴承_额定寿命_转(C, P, epsilon)
```

**描述**: L10 = (C/P)^ε × 10⁶ 转，C P 同单位（kN 或 N），ε=3球/10/3滚子。

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |
| `P` | `` | |
| `epsilon` | `` | |

Source: `src\domains\mech_design.py:353`

---

## `_轴承_额定寿命_小时`

```matha
def _轴承_额定寿命_小时(C, P, n, epsilon)
```

**描述**: Lh = (10⁶ / (60n)) × (C/P)^ε（小时），n r/min。

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |
| `P` | `` | |
| `n` | `` | |
| `epsilon` | `` | |

Source: `src\domains\mech_design.py:360`

---

## `_轴承_当量动载荷`

```matha
def _轴承_当量动载荷(Fr, Fa, X, Y, fp)
```

**描述**: P = fp·(X·Fr + Y·Fa)，fp 载荷系数（平稳1/中冲击1.2/重冲击1.5）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Fr` | `` | |
| `Fa` | `` | |
| `X` | `` | |
| `Y` | `` | |
| `fp` | `` | |

Source: `src\domains\mech_design.py:367`

---

## `_轴承_静载校核`

```matha
def _轴承_静载校核(P0, C0, S0)
```

**描述**: 静强度判定：P0 ≤ C0/S0 时返回 True 满足，否则 False。

| 参数 | 类型 | 说明 |
|------|------|------|
| `P0` | `` | |
| `C0` | `` | |
| `S0` | `` | |

Source: `src\domains\mech_design.py:372`

---

## `_轴承_附加轴向力`

```matha
def _轴承_附加轴向力(Fr, e)
```

**描述**: 角接触轴承附加轴向力简化 Fa = e·Fr（e 查简化值，7000C 取 0.68）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Fr` | `` | |
| `e` | `` | |

Source: `src\domains\mech_design.py:379`

---

## `_轴承_寿命系数`

```matha
def _轴承_寿命系数(n, epsilon)
```

**描述**: fn = (33.3 / n)^(1/ε)，n r/min，对应 C 单位 kN 时寿命速度系数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `epsilon` | `` | |

Source: `src\domains\mech_design.py:384`

---

## `_轴承_查深沟球_62xx`

```matha
def _轴承_查深沟球_62xx(model)
```

**描述**: 查 62xx 深沟球轴承参数 (C, C0, d, D, B)，不存在返回 (0,0,0,0,0)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `model` | `` | |

Source: `src\domains\mech_design.py:391`

---

## `_轴承_查深沟球_63xx`

```matha
def _轴承_查深沟球_63xx(model)
```

**描述**: 查 63xx 深沟球轴承参数 (C, C0, d, D, B)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `model` | `` | |

Source: `src\domains\mech_design.py:399`

---

## `_齿轮_分度圆直径`

```matha
def _齿轮_分度圆直径(m, z)
```

**描述**: d = m·z，模数 m mm，齿数 z → 直径 mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `z` | `` | |

Source: `src\domains\mech_design.py:409`

---

## `_齿轮_中心距`

```matha
def _齿轮_中心距(m, z1, z2)
```

**描述**: a = m(z1+z2)/2。

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `z1` | `` | |
| `z2` | `` | |

Source: `src\domains\mech_design.py:414`

---

## `_齿轮_传动比_齿数`

```matha
def _齿轮_传动比_齿数(z1, z2)
```

**描述**: i = z2/z1（n1/n2）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `z1` | `` | |
| `z2` | `` | |

Source: `src\domains\mech_design.py:419`

---

## `_齿轮_齿宽`

```matha
def _齿轮_齿宽(phi_d, d1)
```

**描述**: b = φd·d1，φd 齿宽系数（0.3~1.2 常用），d1 小轮分度圆 mm → b mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `phi_d` | `` | |
| `d1` | `` | |

Source: `src\domains\mech_design.py:426`

---

## `_齿轮_圆周速度`

```matha
def _齿轮_圆周速度(d1_mm, n1)
```

**描述**: v = π·d1·n1 / (60×1000)，d1 mm，n1 r/min → v m/s。

| 参数 | 类型 | 说明 |
|------|------|------|
| `d1_mm` | `` | |
| `n1` | `` | |

Source: `src\domains\mech_design.py:431`

---

## `_齿轮_转矩_Pn`

```matha
def _齿轮_转矩_Pn(P_kW, n1)
```

**描述**: T1 = 9.55e6·P/n1，P kW，n r/min → T1 N·mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `P_kW` | `` | |
| `n1` | `` | |

Source: `src\domains\mech_design.py:436`

---

## `_齿轮_弯曲应力`

```matha
def _齿轮_弯曲应力(T1, phi_d, m, z1, YFa, YSa, K)
```

**描述**: 直齿圆柱齿轮弯曲疲劳 σF = 2KT1·YFa·YSa / (φd·b·m²·z1)
简化：b = φd·d1 = φd·m·z1 代入 → σF = 2KT1·YFa·YSa / (φd·φd·m·z1·m²·z1)
这里按原式：b=φd·d1，用户给的是 φd（齿宽系数）。返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T1` | `` | |
| `phi_d` | `` | |
| `m` | `` | |
| `z1` | `` | |
| `YFa` | `` | |
| `YSa` | `` | |
| `K` | `` | |

Source: `src\domains\mech_design.py:443`

---

## `_齿轮_许用接触应力`

```matha
def _齿轮_许用接触应力(Hlim, Zn, S_Hmin)
```

**描述**: σHP = Hlim·Zn/S_Hmin，Zn 寿命系数。返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Hlim` | `` | |
| `Zn` | `` | |
| `S_Hmin` | `` | |

Source: `src\domains\mech_design.py:454`

---

## `_齿轮_许用弯曲应力`

```matha
def _齿轮_许用弯曲应力(Flim, Yn, S_Fmin, Yx)
```

**描述**: σFP = Flim·Yn·Yx / S_Fmin，Yn 寿命系数、Yx 尺寸系数。返回 MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Flim` | `` | |
| `Yn` | `` | |
| `S_Fmin` | `` | |
| `Yx` | `` | |

Source: `src\domains\mech_design.py:459`

---

## `_弹簧_旋绕比`

```matha
def _弹簧_旋绕比(D, d)
```

**描述**: C = D/d（旋绕比/弹簧指数），推荐 4~16。

| 参数 | 类型 | 说明 |
|------|------|------|
| `D` | `` | |
| `d` | `` | |

Source: `src\domains\mech_design.py:466`

---

## `_弹簧_曲度系数`

```matha
def _弹簧_曲度系数(C)
```

**描述**: K = (4C-1)/(4C-4) + 0.615/C（Wahl 系数）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `C` | `` | |

Source: `src\domains\mech_design.py:473`

---

## `_弹簧_切应力`

```matha
def _弹簧_切应力(F, D, d, K)
```

**描述**: τ = 8·K·D·F / (π·d³)。F N, D mm, d mm, K 曲度系数 → τ MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `D` | `` | |
| `d` | `` | |
| `K` | `` | |

Source: `src\domains\mech_design.py:480`

---

## `_弹簧_变形量`

```matha
def _弹簧_变形量(F, D, d, n, G)
```

**描述**: λ = 8·F·D³·n / (G·d⁴)，n 有效圈数，G 切变模量 MPa。返回 mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `D` | `` | |
| `d` | `` | |
| `n` | `` | |
| `G` | `` | |

Source: `src\domains\mech_design.py:487`

---

## `_弹簧_刚度`

```matha
def _弹簧_刚度(D, d, n, G)
```

**描述**: k = G·d⁴ / (8·D³·n) N/mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `D` | `` | |
| `d` | `` | |
| `n` | `` | |
| `G` | `` | |

Source: `src\domains\mech_design.py:495`

---

## `_弹簧_总圈数`

```matha
def _弹簧_总圈数(n, end_type)
```

**描述**: n1 = n + 2（YI型/Y型两端各磨平一圈），n1=n+1.5（YII型）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `end_type` | `` | |

Source: `src\domains\mech_design.py:503`

---

## `_弹簧_自由高度`

```matha
def _弹簧_自由高度(n, p, d, end_type)
```

**描述**: H0 = n·p + 2·d（Y 型端部不磨）；
H0 = n·p + 1.5·d（YI 型两端磨平）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `p` | `` | |
| `d` | `` | |
| `end_type` | `` | |

Source: `src\domains\mech_design.py:510`

---

## `_弹簧_螺旋角`

```matha
def _弹簧_螺旋角(p, D)
```

**描述**: α = arctan(p / (π·D))，返回弧度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `D` | `` | |

Source: `src\domains\mech_design.py:518`

---

## `_联接_受拉螺栓强度`

```matha
def _联接_受拉螺栓强度(F_pre, A_s, sigma_s, S)
```

**描述**: 受拉预紧螺栓：σ = 1.3·F_pre / A_s ≤ σs/S。
F_pre N，A_s 应力面积 mm²，σs MPa；False 不满足。
返回应力 MPa（调用者再比较 sigma_s/S）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F_pre` | `` | |
| `A_s` | `` | |
| `sigma_s` | `` | |
| `S` | `` | |

Source: `src\domains\mech_design.py:528`

---

## `_联接_受剪螺栓强度`

```matha
def _联接_受剪螺栓强度(F, m, d0)
```

**描述**: τ = 4F / (m·π·d0²)，m 剪切面数，d0 螺栓杆 mm，F N → τ MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `m` | `` | |
| `d0` | `` | |

Source: `src\domains\mech_design.py:537`

---

## `_联接_螺栓挤压强度`

```matha
def _联接_螺栓挤压强度(F, d0, sum_t)
```

**描述**: σp = F / (d0·Σt)，Σt 同方向被连接件最小厚度和 mm → σp MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `d0` | `` | |
| `sum_t` | `` | |

Source: `src\domains\mech_design.py:545`

---

## `_联接_螺栓总拉力`

```matha
def _联接_螺栓总拉力(F_work, F_double_prime)
```

**描述**: F' = F_work + F''，轴向工作载荷 F_work + 残余预紧 F'' → 总拉力 N。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F_work` | `` | |
| `F_double_prime` | `` | |

Source: `src\domains\mech_design.py:553`

---

## `_联接_残余预紧力`

```matha
def _联接_残余预紧力(F_work, chi)
```

**描述**: F'' = χ·F_work，χ 变载 0.6~1.0 / 静载 0.2~0.6。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F_work` | `` | |
| `chi` | `` | |

Source: `src\domains\mech_design.py:558`

---

## `_联接_查螺栓强度`

```matha
def _联接_查螺栓强度(grade)
```

**描述**: 查螺栓强度等级：(σs, σb) MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `grade` | `` | |

Source: `src\domains\mech_design.py:563`

---

## `_联接_查螺栓规格`

```matha
def _联接_查螺栓规格(spec)
```

**描述**: 查螺栓规格 (d, d1, d0, p) mm，spec 如 "M12"。

| 参数 | 类型 | 说明 |
|------|------|------|
| `spec` | `` | |

Source: `src\domains\mech_design.py:571`

---

## `_联接_销剪切强度`

```matha
def _联接_销剪切强度(F, d, z)
```

**描述**: τ = 4F / (π·d²·z)，d mm, z 剪切面数 → τ MPa。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `d` | `` | |
| `z` | `` | |

Source: `src\domains\mech_design.py:579`

---

## `_公差_IT值`

```matha
def _公差_IT值(grade_str, D_mm)
```

**描述**: IT 标准公差值（μm）：i = 0.45·∛D + 0.001·D，IT = a·i，
a 为等级系数；D_mm 为公称尺寸 mm。返回 μm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `grade_str` | `` | |
| `D_mm` | `` | |

Source: `src\domains\mech_design.py:589`

---

## `_公差_基孔制配合`

```matha
def _公差_基孔制配合(IT_grade, fit_type, D_mm)
```

**描述**: 基孔制配合（简化）：返回 (ES, EI, es, ei) μm。
fit_type:
  "H7h6" → 间隙（常用高精度）
  "H7g6" → 间隙（定心间隙配合）
  "H7js6" → 过渡
  "H7k6" → 过渡
  "H7n6" → 过渡偏紧
  "H7p6" → 过盈
  "H7s6" → 过盈
简化：孔 H，下偏差 EI=0；上偏差 ES = +IT_hole。
轴的偏差按基准简化。仅返回公差带上下限（μm），非完整 GB 值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `IT_grade` | `` | |
| `fit_type` | `` | |
| `D_mm` | `` | |

Source: `src\domains\mech_design.py:599`

---

## `_公差_尺寸链封闭环`

```matha
def _公差_尺寸链封闭环(plus_rings, minus_rings)
```

**描述**: 极值法：A0 = Σ(增环) - Σ(减环)。
plus_rings/minus_rings 为 list 或单值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `plus_rings` | `` | |
| `minus_rings` | `` | |

Source: `src\domains\mech_design.py:651`

---

## `_公差_尺寸链封闭环公差`

```matha
def _公差_尺寸链封闭环公差(tolerances)
```

**描述**: 极值法：T0 = ΣTi。tolerances 为组成环公差 list。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tolerances` | `` | |

Source: `src\domains\mech_design.py:659`

---

## `_公差_查表面粗糙度`

```matha
def _公差_查表面粗糙度(method)
```

**描述**: 按加工方法查典型 Ra（μm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `method` | `` | |

Source: `src\domains\mech_design.py:664`

---

## `_公差_威布尔可靠度`

```matha
def _公差_威布尔可靠度(t, eta, beta)
```

**描述**: R(t) = exp(-(t/η)^β)，η 特征寿命，β 形状参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `t` | `` | |
| `eta` | `` | |
| `beta` | `` | |

Source: `src\domains\mech_design.py:669`

---

## `_register_mech_design`

```matha
def _register_mech_design(builtins: dict) -> None
```

**描述**: 将机械设计内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\mech_design.py:678`

---

## `_curry6`

```matha
def _curry6(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mech_design.py:791`

---

## `_curry7`

```matha
def _curry7(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mech_design.py:805`

---

## `_mech_design_symtab_names`

```matha
def _mech_design_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\mech_design.py:823`

---

# `domains.mechanics`

```matha

```

**描述**: Matha 机械领域模块：运动学 + 材料力学。

基于 mathlib 数学地基演化的机械领域功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

两大子领域：

一、运动学（Kinematics）
  1) 匀速直线：位移 / 时间 / 速度互算
  2) 匀变速直线：位移、末速度、末速度²-初速度² 三大公式
  3) 抛体运动：平抛 / 斜抛 射程 / 最大高度 / 落地时间
  4) 圆周运动：线速度 / 角速度 / 向心加速度 / 周期

二、材料力学（Mechanics of Materials）
  1) 轴向拉压：应力 σ=F/A、应变 ε=ΔL/L、胡克定律 σ=Eε、变形 ΔL=FL/EA
  2) 剪切：剪应力 τ=F/A、剪切胡克 τ=Gγ
  3) 梁弯曲：截面惯性矩（矩形/圆形/圆环）、最大弯曲正应力 σ_max=My_max/I
  4) 圆轴扭转：极惯性矩 Ip、最大扭转剪应力 τ_max=Tr/Ip、扭转角 φ=TL/(GIp)
  5) 材料属性数据库：钢/铝/铜/铸铁/木材/混凝土 的 E/G/σ_s/σ_b/ρ

设计原则：
  - 所有函数返回纯数值（float/int），不抛语义错（除以零等 Python 自身抛）
  - 多参函数一律 _curry2 封装（f(a)(b) 柯里化），与 Matha 语义一致
  - 材料属性作为常量值直接注册（如 钢_E=2.06e11 Pa）
  - 与物理/地理/天文领域共享 mathlib 的数学能力（g、pi、sin/cos/sqrt 等）

Source: `src\domains\mechanics.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

**描述**: 两参 Python 函数 → 柯里化 f(a)(b)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mechanics.py:36`

---

## `_curry3`

```matha
def _curry3(func)
```

**描述**: 三参 → 柯里化 f(a)(b)(c)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mechanics.py:43`

---

## `_curry4`

```matha
def _curry4(func)
```

**描述**: 四参 → 柯里化 f(a)(b)(c)(d)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\mechanics.py:52`

---

## `_匀速_位移`

```matha
def _匀速_位移(v, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `t` | `` | |

Source: `src\domains\mechanics.py:69`

---

## `_匀速_速度`

```matha
def _匀速_速度(s, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `` | |
| `t` | `` | |

Source: `src\domains\mechanics.py:70`

---

## `_匀速_时间`

```matha
def _匀速_时间(s, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `` | |
| `v` | `` | |

Source: `src\domains\mechanics.py:71`

---

## `_匀变速_位移`

```matha
def _匀变速_位移(v0, a, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v0` | `` | |
| `a` | `` | |
| `t` | `` | |

Source: `src\domains\mechanics.py:74`

---

## `_匀变速_末速度`

```matha
def _匀变速_末速度(v0, a, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v0` | `` | |
| `a` | `` | |
| `t` | `` | |

Source: `src\domains\mechanics.py:75`

---

## `_匀变速_位移由速度`

```matha
def _匀变速_位移由速度(v0, v, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v0` | `` | |
| `v` | `` | |
| `a` | `` | |

Source: `src\domains\mechanics.py:77`

---

## `_自由落体_位移`

```matha
def _自由落体_位移(t, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `t` | `` | |
| `g_val` | `` | |

Source: `src\domains\mechanics.py:79`

---

## `_自由落体_末速度`

```matha
def _自由落体_末速度(t, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `t` | `` | |
| `g_val` | `` | |

Source: `src\domains\mechanics.py:80`

---

## `_平抛_落地时间`

```matha
def _平抛_落地时间(h, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `h` | `` | |
| `g_val` | `` | |

Source: `src\domains\mechanics.py:84`

---

## `_平抛_射程`

```matha
def _平抛_射程(v0, h, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v0` | `` | |
| `h` | `` | |
| `g_val` | `` | |

Source: `src\domains\mechanics.py:85`

---

## `_斜抛_射程`

```matha
def _斜抛_射程(v0, theta_rad, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v0` | `` | |
| `theta_rad` | `` | |
| `g_val` | `` | |

Source: `src\domains\mechanics.py:91`

---

## `_斜抛_最大高度`

```matha
def _斜抛_最大高度(v0, theta_rad, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v0` | `` | |
| `theta_rad` | `` | |
| `g_val` | `` | |

Source: `src\domains\mechanics.py:94`

---

## `_斜抛_飞行时间`

```matha
def _斜抛_飞行时间(v0, theta_rad, g_val)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v0` | `` | |
| `theta_rad` | `` | |
| `g_val` | `` | |

Source: `src\domains\mechanics.py:98`

---

## `_圆周_线速度`

```matha
def _圆周_线速度(omega, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `omega` | `` | |
| `r` | `` | |

Source: `src\domains\mechanics.py:103`

---

## `_圆周_角速度`

```matha
def _圆周_角速度(T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |

Source: `src\domains\mechanics.py:105`

---

## `_圆周_向心加速度_v`

```matha
def _圆周_向心加速度_v(v, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `r` | `` | |

Source: `src\domains\mechanics.py:107`

---

## `_圆周_向心加速度_w`

```matha
def _圆周_向心加速度_w(omega, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `omega` | `` | |
| `r` | `` | |

Source: `src\domains\mechanics.py:108`

---

## `_圆周_周期`

```matha
def _圆周_周期(v, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `r` | `` | |

Source: `src\domains\mechanics.py:110`

---

## `_拉压_应力`

```matha
def _拉压_应力(F, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `A` | `` | |

Source: `src\domains\mechanics.py:118`

---

## `_拉压_应变`

```matha
def _拉压_应变(dL, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dL` | `` | |
| `L` | `` | |

Source: `src\domains\mechanics.py:119`

---

## `_拉压_胡克`

```matha
def _拉压_胡克(E, epsilon)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `epsilon` | `` | |

Source: `src\domains\mechanics.py:120`

---

## `_拉压_变形`

```matha
def _拉压_变形(F, L, E, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `L` | `` | |
| `E` | `` | |
| `A` | `` | |

Source: `src\domains\mechanics.py:121`

---

## `_安全系数`

```matha
def _安全系数(sigma_s, sigma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_s` | `` | |
| `sigma` | `` | |

Source: `src\domains\mechanics.py:123`

---

## `_剪切_剪应力`

```matha
def _剪切_剪应力(F, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `A` | `` | |

Source: `src\domains\mechanics.py:126`

---

## `_剪切_剪应变`

```matha
def _剪切_剪应变(delta, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `delta` | `` | |
| `L` | `` | |

Source: `src\domains\mechanics.py:127`

---

## `_剪切_胡克`

```matha
def _剪切_胡克(G, gamma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `G` | `` | |
| `gamma` | `` | |

Source: `src\domains\mechanics.py:128`

---

## `_截面_矩形惯性矩`

```matha
def _截面_矩形惯性矩(b, h)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `b` | `` | |
| `h` | `` | |

Source: `src\domains\mechanics.py:131`

---

## `_截面_圆形惯性矩`

```matha
def _截面_圆形惯性矩(d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |

Source: `src\domains\mechanics.py:132`

---

## `_截面_圆环惯性矩`

```matha
def _截面_圆环惯性矩(D, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `D` | `` | |
| `d` | `` | |

Source: `src\domains\mechanics.py:133`

---

## `_弯曲_正应力`

```matha
def _弯曲_正应力(M, y, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `y` | `` | |
| `I` | `` | |

Source: `src\domains\mechanics.py:135`

---

## `_弯曲_最大正应力`

```matha
def _弯曲_最大正应力(M, y_max, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `y_max` | `` | |
| `I` | `` | |

Source: `src\domains\mechanics.py:136`

---

## `_扭转_极惯性矩`

```matha
def _扭转_极惯性矩(d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |

Source: `src\domains\mechanics.py:139`

---

## `_扭转_空心极惯性矩`

```matha
def _扭转_空心极惯性矩(D, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `D` | `` | |
| `d` | `` | |

Source: `src\domains\mechanics.py:140`

---

## `_扭转_剪应力`

```matha
def _扭转_剪应力(T, r, Ip)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `r` | `` | |
| `Ip` | `` | |

Source: `src\domains\mechanics.py:142`

---

## `_扭转_最大剪应力`

```matha
def _扭转_最大剪应力(T, d, Ip)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `d` | `` | |
| `Ip` | `` | |

Source: `src\domains\mechanics.py:143`

---

## `_扭转_扭转角`

```matha
def _扭转_扭转角(T, L, G, Ip)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `L` | `` | |
| `G` | `` | |
| `Ip` | `` | |

Source: `src\domains\mechanics.py:145`

---

## `_抗弯截面模量_圆形`

```matha
def _抗弯截面模量_圆形(d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |

Source: `src\domains\mechanics.py:148`

---

## `_抗弯截面模量_矩形`

```matha
def _抗弯截面模量_矩形(b, h)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `b` | `` | |
| `h` | `` | |

Source: `src\domains\mechanics.py:149`

---

## `_材料_属性`

```matha
def _材料_属性(mat_name: str, prop_key: str) -> float
```

**描述**: 读取材料属性。未命中抛 KeyError 转 MathaRuntimeError（注册侧处理）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mat_name` | `str` | |
| `prop_key` | `str` | |

**返回**: `float`

Source: `src\domains\mechanics.py:204`

---

## `_register_mechanics`

```matha
def _register_mechanics(builtins: dict) -> None
```

**描述**: 将机械领域内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（mathlib 之后）。
命名规则：
  - 运动学：前缀 运动_
  - 材料力学：前缀 材料_ / 前缀 梁_ / 前缀 轴_
  - 材料常量：材质_E / 材质_G / 材质_s（屈服）/ 材质_b（强度）/ 材质_rho / 材质_nu

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\mechanics.py:213`

---

## `_mechanics_symtab_names`

```matha
def _mechanics_symtab_names() -> list[str]
```

**描述**: 返回机械领域所有内建名（用于语义分析注册，避免报未定义）。

**返回**: `list[str]`

Source: `src\domains\mechanics.py:292`

---

# `domains.medical`

```matha

```

**描述**: Matha 领域扩展模块：医疗与医药和理疗（Medical, Pharmaceutical & Physiotherapy）。

基于 Matha 数学基础与生物/物理常量体系，演化临床医学子领域功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、药代动力学（Pharmacokinetics）- 前缀 药代_
  1) 消除速率常数：ke = ln2 / t½
  2) 半衰期：t½ = ln2 / ke
  3) 一室模型血药浓度：C(t) = C0·exp(-ke·t)
  4) 表观分布容积：Vd = Dose / C0
  5) 清除率：CL = ke·Vd
  6) 稳态平均浓度：Css = Dose / (CL·τ)  （τ 给药间隔）
  7) 生物利用度：F = AUC_po / AUC_iv
  8) AUC 梯形法（单区间）：AUC = (c1+c2)/2 · (t2-t1)
  9) 峰浓度：Cmax = Dose·F / Vd
  10) 谷浓度：Cmin = Cmax·exp(-ke·τ)
  11) 负荷剂量：LD = Css·Vd / F

二、药效学（Pharmacodynamics）- 前缀 药效_
  1) Emax 模型：E = Emax·C / (EC50 + C)
  2) Sigmoid Emax（Hill 型）：E = Emax·C^n / (EC50^n + C^n)
  3) 治疗指数：TI = TD50 / ED50
  4) 安全范围：SR = TD1 / ED99
  5) 竞争性拮抗浓度：[I] = (dr - 1)·Ki  （dr 剂量比）
  6) 部分激动剂内在活性：α = Emax_part / Emax_full
  7) 量反应 Hill 系数：n = ln9 / ln(ED84/ED16)

三、临床检验（Clinical Laboratory）- 前缀 检验_
  1) 肌酐清除率（Cockcroft-Gault）：CCr = (140-age)·wt / (72·SCr) · (0.85 if 女)
  2) eGFR（MDRD 简化）：eGFR = 175·SCr^-1.154·age^-0.203 · (0.742 if 女) · (1.212 if 黑人)
  3) 阴离子间隙：AG = Na - (Cl + HCO3)
  4) 血浆渗透压：Osm = 2·Na + Glu/18 + BUN/2.8
  5) 平均红细胞体积 MCV = Hct(%)·10 / RBC(×10¹²/L)
  6) 平均红细胞血红蛋白 MCH = Hb(g/L)·10 / RBC
  7) 平均红细胞血红蛋白浓度 MCHC = Hb(g/L) / Hct(L/L)
  8) 白蛋白校正钙：Ca_corr = Ca + 0.8·(4 - Alb)
  9) 白蛋白校正阴离子间隙：AG_corr = AG + 2.5·(4 - Alb)
  10) 血糖校正钠：Na_corr = Na + 1.6·(Glu - 100)/100
  11) 游离水清除率：CH2O = V·(1 - Osm_u/Osm_p)
  12) 校正红细胞沉降率（年龄法）：ESR_corr = ESR - (age - 50)/2  （简化）

四、影像与放疗（Imaging & Radiotherapy）- 前缀 影像_
  1) 当量剂量：H = D·wR  （Sv）
  2) 有效剂量：E = H·wT  （Sv）
  3) 半值层：HVL = ln2 / μ
  4) 放射性活度衰变：A(t) = A0·exp(-λ·t)，λ = ln2/t½
  5) CT 值（Hounsfield）：HU = 1000·(μ - μ水)/μ水
  6) 相对生物效应 RBE ≈ 1 + LET/100（LET≤100 线性近似，>100 饱和）
  7) 生物等效剂量 BED = n·d·(1 + d/(α/β))
  8) 2Gy 等效剂量 EQD2 = total·(d + α/β)/(2 + α/β)
  9) 平方反比律：I2 = I1·(d1/d2)²
  10) 半价层衰减：剩余分数 = (1/2)^n

五、理疗与康复（Physiotherapy & Rehabilitation）- 前缀 理疗_
  1) 最大心率：HRmax = 220 - age
  2) Karvonen 心率储备：THR = (HRmax - HRrest)·intensity + HRrest
  3) METs → VO2：VO2 = 3.5·METs  (mL/kg/min)
  4) Borg 评分 → VO2max：VO2max ≈ (Borg - 4)·3.5 + 3.5（6-20 量表的简化映射）
  5) 代谢热量消耗：kcal = METs·3.5·wt(kg)·min / 200
  6) 超声强度衰减：I(d) = I0·exp(-α·d)
  7) 等长收缩力矩：τ = F·r
  8) 步态速度：v = 步长·步频
  9) 关节活动度百分比：ROM% = 当前/正常·100
  10) FIM 分级：按总分返回独立性等级字符串

数据库：
  - 常用药物半衰期库（地高辛/华法林/氨茶碱/苯妥英/锂/庆大霉素/万古霉素/对乙酰氨基酚/阿司匹林/吗啡）
  - 辐射权重因子 wR（光子/电子/中子/α/质子）
  - 组织权重因子 wT（ICRP 103：性腺/骨髓/结肠/肺/胃/乳腺/肝/食道/甲状腺/皮肤/骨表面/膀胱/唾液腺/脑/其余）
  - 常见活动 METs 表（静卧/坐/步行慢/步行快/跑步/骑车/游泳）
  - 正常检验参考值常量

设计原则：
  - 与 biology / statmech / fluid_exp 保持一致：_curryN 柯里化、前缀区分
  - 函数返回纯数值或字符串，不做语义包装
  - 除零 / 非法参数由 Python 自身抛错

Source: `src\domains\medical.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\medical.py:87`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\medical.py:93`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\medical.py:101`

---

## `_药代_消除速率常数`

```matha
def _药代_消除速率常数(t_half)
```

**描述**: ke = ln2 / t½（t½ 单位须与 ke 一致，通常 h⁻¹）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `t_half` | `` | |

Source: `src\domains\medical.py:212`

---

## `_药代_半衰期`

```matha
def _药代_半衰期(ke)
```

**描述**: t½ = ln2 / ke。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ke` | `` | |

Source: `src\domains\medical.py:217`

---

## `_药代_一室浓度`

```matha
def _药代_一室浓度(C0, ke, t)
```

**描述**: 一室静注模型 C(t) = C0·exp(-ke·t)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `C0` | `` | |
| `ke` | `` | |
| `t` | `` | |

Source: `src\domains\medical.py:222`

---

## `_药代_表观分布容积`

```matha
def _药代_表观分布容积(Dose, C0)
```

**描述**: Vd = Dose / C0（Dose 单位与 C0 对应）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Dose` | `` | |
| `C0` | `` | |

Source: `src\domains\medical.py:227`

---

## `_药代_清除率`

```matha
def _药代_清除率(ke, Vd)
```

**描述**: CL = ke·Vd。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ke` | `` | |
| `Vd` | `` | |

Source: `src\domains\medical.py:232`

---

## `_药代_稳态浓度`

```matha
def _药代_稳态浓度(Dose, CL, tau)
```

**描述**: 多次给药稳态平均浓度 Css = Dose / (CL·τ)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Dose` | `` | |
| `CL` | `` | |
| `tau` | `` | |

Source: `src\domains\medical.py:237`

---

## `_药代_生物利用度`

```matha
def _药代_生物利用度(AUC_po, AUC_iv)
```

**描述**: 绝对生物利用度 F = AUC_po / AUC_iv。

| 参数 | 类型 | 说明 |
|------|------|------|
| `AUC_po` | `` | |
| `AUC_iv` | `` | |

Source: `src\domains\medical.py:242`

---

## `_药代_AUC梯形`

```matha
def _药代_AUC梯形(t1, c1, t2, c2)
```

**描述**: 单区间梯形法 AUC = (c1+c2)/2 · (t2-t1)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `t1` | `` | |
| `c1` | `` | |
| `t2` | `` | |
| `c2` | `` | |

Source: `src\domains\medical.py:247`

---

## `_药代_峰浓度`

```matha
def _药代_峰浓度(Dose, Vd, F)
```

**描述**: Cmax = Dose·F / Vd（血管外给药达峰近似）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Dose` | `` | |
| `Vd` | `` | |
| `F` | `` | |

Source: `src\domains\medical.py:252`

---

## `_药代_谷浓度`

```matha
def _药代_谷浓度(Cmax, ke, tau)
```

**描述**: Cmin = Cmax·exp(-ke·τ)（稳态谷浓度）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Cmax` | `` | |
| `ke` | `` | |
| `tau` | `` | |

Source: `src\domains\medical.py:257`

---

## `_药代_负荷剂量`

```matha
def _药代_负荷剂量(Css_target, Vd, F)
```

**描述**: 负荷剂量 LD = Css·Vd / F，使血药浓度迅速达到稳态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Css_target` | `` | |
| `Vd` | `` | |
| `F` | `` | |

Source: `src\domains\medical.py:262`

---

## `_药效_Emax模型`

```matha
def _药效_Emax模型(Emax, EC50, C)
```

**描述**: Emax 模型 E = Emax·C / (EC50 + C)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Emax` | `` | |
| `EC50` | `` | |
| `C` | `` | |

Source: `src\domains\medical.py:269`

---

## `_药效_Sigmoid_Emax`

```matha
def _药效_Sigmoid_Emax(Emax, EC50, C, n)
```

**描述**: Sigmoid Emax（Hill）E = Emax·C^n / (EC50^n + C^n)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Emax` | `` | |
| `EC50` | `` | |
| `C` | `` | |
| `n` | `` | |

Source: `src\domains\medical.py:275`

---

## `_药效_治疗指数`

```matha
def _药效_治疗指数(TD50, ED50)
```

**描述**: 治疗指数 TI = TD50 / ED50。

| 参数 | 类型 | 说明 |
|------|------|------|
| `TD50` | `` | |
| `ED50` | `` | |

Source: `src\domains\medical.py:285`

---

## `_药效_安全范围`

```matha
def _药效_安全范围(TD1, ED99)
```

**描述**: 安全范围 SR = TD1 / ED99。

| 参数 | 类型 | 说明 |
|------|------|------|
| `TD1` | `` | |
| `ED99` | `` | |

Source: `src\domains\medical.py:290`

---

## `_药效_竞争拮抗浓度`

```matha
def _药效_竞争拮抗浓度(dr, Ki)
```

**描述**: 竞争性拮抗所需拮抗剂浓度 [I] = (dr - 1)·Ki。dr 为剂量比。

| 参数 | 类型 | 说明 |
|------|------|------|
| `dr` | `` | |
| `Ki` | `` | |

Source: `src\domains\medical.py:295`

---

## `_药效_部分激动剂活性`

```matha
def _药效_部分激动剂活性(Emax_part, Emax_full)
```

**描述**: 部分激动剂内在活性 α = Emax_part / Emax_full。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Emax_part` | `` | |
| `Emax_full` | `` | |

Source: `src\domains\medical.py:300`

---

## `_药效_量反应斜率`

```matha
def _药效_量反应斜率(ED16, ED84)
```

**描述**: 由 ED16/ED84 估算 Hill 系数 n = ln9 / ln(ED84/ED16)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ED16` | `` | |
| `ED84` | `` | |

Source: `src\domains\medical.py:305`

---

## `_检验_肌酐清除率`

```matha
def _检验_肌酐清除率(age, weight, SCr, is_female)
```

**描述**: Cockcroft-Gault：CCr = (140-age)·wt / (72·SCr) ·(0.85 if 女)。单位 mL/min。

| 参数 | 类型 | 说明 |
|------|------|------|
| `age` | `` | |
| `weight` | `` | |
| `SCr` | `` | |
| `is_female` | `` | |

Source: `src\domains\medical.py:312`

---

## `_检验_eGFR_MDRD`

```matha
def _检验_eGFR_MDRD(SCr, age, is_female, is_black)
```

**描述**: MDRD 简化公式 eGFR = 175·SCr^-1.154·age^-0.203·(0.742 if 女)·(1.212 if 黑人)。单位 mL/min/1.73m²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `SCr` | `` | |
| `age` | `` | |
| `is_female` | `` | |
| `is_black` | `` | |

Source: `src\domains\medical.py:318`

---

## `_检验_阴离子间隙`

```matha
def _检验_阴离子间隙(Na, Cl, HCO3)
```

**描述**: AG = Na - (Cl + HCO3)。单位 mmol/L。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Na` | `` | |
| `Cl` | `` | |
| `HCO3` | `` | |

Source: `src\domains\medical.py:328`

---

## `_检验_渗透压`

```matha
def _检验_渗透压(Na, Glu, BUN)
```

**描述**: 血浆渗透压 Osm = 2·Na + Glu/18 + BUN/2.8。Glu/BUN 单位 mg/dL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Na` | `` | |
| `Glu` | `` | |
| `BUN` | `` | |

Source: `src\domains\medical.py:333`

---

## `_检验_MCV`

```matha
def _检验_MCV(Hct_pct, RBC_TperL)
```

**描述**: 平均红细胞体积 MCV = Hct(%)·10 / RBC(×10¹²/L)。单位 fL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Hct_pct` | `` | |
| `RBC_TperL` | `` | |

Source: `src\domains\medical.py:338`

---

## `_检验_MCH`

```matha
def _检验_MCH(Hb_gL, RBC_TperL)
```

**描述**: 平均红细胞血红蛋白 MCH = Hb(g/L) / RBC(×10¹²/L)。单位 pg。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Hb_gL` | `` | |
| `RBC_TperL` | `` | |

Source: `src\domains\medical.py:343`

---

## `_检验_MCHC`

```matha
def _检验_MCHC(Hb_gL, Hct_LperL)
```

**描述**: 平均红细胞血红蛋白浓度 MCHC = Hb(g/L) / Hct(L/L)。单位 g/L。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Hb_gL` | `` | |
| `Hct_LperL` | `` | |

Source: `src\domains\medical.py:348`

---

## `_检验_校正钙`

```matha
def _检验_校正钙(Ca, Alb)
```

**描述**: 白蛋白校正血钙 Ca_corr = Ca + 0.8·(4 - Alb)。单位 mg/dL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Ca` | `` | |
| `Alb` | `` | |

Source: `src\domains\medical.py:353`

---

## `_检验_白蛋白校正AG`

```matha
def _检验_白蛋白校正AG(AG, Alb)
```

**描述**: 白蛋白校正阴离子间隙 AG_corr = AG + 2.5·(4 - Alb)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `AG` | `` | |
| `Alb` | `` | |

Source: `src\domains\medical.py:358`

---

## `_检验_Na校正血糖`

```matha
def _检验_Na校正血糖(Na, Glu)
```

**描述**: 高血糖校正血钠 Na_corr = Na + 1.6·(Glu - 100)/100。Glu 单位 mg/dL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Na` | `` | |
| `Glu` | `` | |

Source: `src\domains\medical.py:363`

---

## `_检验_游离水清除率`

```matha
def _检验_游离水清除率(V_urine, Osm_u, Osm_p)
```

**描述**: 游离水清除率 CH2O = V·(1 - Osm_u/Osm_p)。V 单位 mL/h。

| 参数 | 类型 | 说明 |
|------|------|------|
| `V_urine` | `` | |
| `Osm_u` | `` | |
| `Osm_p` | `` | |

Source: `src\domains\medical.py:368`

---

## `_检验_校正沉降率`

```matha
def _检验_校正沉降率(ESR, age, is_female)
```

**描述**: 校正红细胞沉降率（简化年龄法）ESR_corr = ESR - (age - 50)/2（女性再加 5）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ESR` | `` | |
| `age` | `` | |
| `is_female` | `` | |

Source: `src\domains\medical.py:373`

---

## `_影像_当量剂量`

```matha
def _影像_当量剂量(D_Gy, wR)
```

**描述**: 当量剂量 H = D·wR。单位 Sv。

| 参数 | 类型 | 说明 |
|------|------|------|
| `D_Gy` | `` | |
| `wR` | `` | |

Source: `src\domains\medical.py:381`

---

## `_影像_有效剂量`

```matha
def _影像_有效剂量(H_Sv, wT)
```

**描述**: 有效剂量 E = H·wT。单位 Sv。

| 参数 | 类型 | 说明 |
|------|------|------|
| `H_Sv` | `` | |
| `wT` | `` | |

Source: `src\domains\medical.py:386`

---

## `_影像_半值层`

```matha
def _影像_半值层(mu)
```

**描述**: 半值层 HVL = ln2 / μ。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |

Source: `src\domains\medical.py:391`

---

## `_影像_放射性活度`

```matha
def _影像_放射性活度(A0, half_life, t)
```

**描述**: A(t) = A0·exp(-λ·t)，λ = ln2/t½。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A0` | `` | |
| `half_life` | `` | |
| `t` | `` | |

Source: `src\domains\medical.py:396`

---

## `_影像_CT值`

```matha
def _影像_CT值(mu, mu_water)
```

**描述**: CT 值（Hounsfield）HU = 1000·(μ - μ水)/μ水。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |
| `mu_water` | `` | |

Source: `src\domains\medical.py:402`

---

## `_影像_RBE`

```matha
def _影像_RBE(LET)
```

**描述**: 相对生物效应 RBE（LET≤100 线性近似 1+LET/100，>100 饱和至 2.0）。LET 单位 keV/μm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `LET` | `` | |

Source: `src\domains\medical.py:407`

---

## `_影像_BED`

```matha
def _影像_BED(n, d, alpha_beta)
```

**描述**: 生物等效剂量 BED = n·d·(1 + d/(α/β))。n 分次数，d 单次剂量 Gy。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `d` | `` | |
| `alpha_beta` | `` | |

Source: `src\domains\medical.py:416`

---

## `_影像_EQD2`

```matha
def _影像_EQD2(total_dose, d, alpha_beta)
```

**描述**: 2Gy 等效剂量 EQD2 = total·(d + α/β)/(2 + α/β)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `total_dose` | `` | |
| `d` | `` | |
| `alpha_beta` | `` | |

Source: `src\domains\medical.py:421`

---

## `_影像_平方反比`

```matha
def _影像_平方反比(I0, d1, d2)
```

**描述**: 平方反比律 I2 = I1·(d1/d2)²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `I0` | `` | |
| `d1` | `` | |
| `d2` | `` | |

Source: `src\domains\medical.py:426`

---

## `_影像_半价层衰减`

```matha
def _影像_半价层衰减(n)
```

**描述**: 经 n 个半值层后剩余强度分数 = (1/2)^n。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\medical.py:431`

---

## `_理疗_HRmax`

```matha
def _理疗_HRmax(age)
```

**描述**: 最大心率 HRmax = 220 - age。

| 参数 | 类型 | 说明 |
|------|------|------|
| `age` | `` | |

Source: `src\domains\medical.py:438`

---

## `_理疗_心率储备`

```matha
def _理疗_心率储备(age, HRrest, intensity)
```

**描述**: Karvonen 公式 THR = (HRmax - HRrest)·intensity + HRrest。

| 参数 | 类型 | 说明 |
|------|------|------|
| `age` | `` | |
| `HRrest` | `` | |
| `intensity` | `` | |

Source: `src\domains\medical.py:443`

---

## `_理疗_METs_VO2`

```matha
def _理疗_METs_VO2(METs)
```

**描述**: METs → VO2 = 3.5·METs（mL/kg/min）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `METs` | `` | |

Source: `src\domains\medical.py:449`

---

## `_理疗_Borg_VO2`

```matha
def _理疗_Borg_VO2(borg)
```

**描述**: Borg 6-20 量表 → VO2max 估算（简化）VO2max ≈ (Borg - 4)·3.5 + 3.5。

| 参数 | 类型 | 说明 |
|------|------|------|
| `borg` | `` | |

Source: `src\domains\medical.py:454`

---

## `_理疗_代谢热量`

```matha
def _理疗_代谢热量(METs, weight_kg, min)
```

**描述**: 运动热量消耗 kcal = METs·3.5·wt(kg)·min / 200。

| 参数 | 类型 | 说明 |
|------|------|------|
| `METs` | `` | |
| `weight_kg` | `` | |
| `min` | `` | |

Source: `src\domains\medical.py:459`

---

## `_理疗_超声衰减`

```matha
def _理疗_超声衰减(I0, alpha, depth)
```

**描述**: 超声强度衰减 I(d) = I0·exp(-α·d)。α 单位 1/(cm·MHz)，d 单位 cm·MHz。

| 参数 | 类型 | 说明 |
|------|------|------|
| `I0` | `` | |
| `alpha` | `` | |
| `depth` | `` | |

Source: `src\domains\medical.py:464`

---

## `_理疗_等长力矩`

```matha
def _理疗_等长力矩(F, r)
```

**描述**: 等长收缩力矩 τ = F·r。F 单位 N，r 单位 m，结果 N·m。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `r` | `` | |

Source: `src\domains\medical.py:469`

---

## `_理疗_步态速度`

```matha
def _理疗_步态速度(stride_length, cadence)
```

**描述**: 步态速度 v = 步长·步频。步长 m，步频 步/min → m/min。

| 参数 | 类型 | 说明 |
|------|------|------|
| `stride_length` | `` | |
| `cadence` | `` | |

Source: `src\domains\medical.py:474`

---

## `_理疗_关节活动度`

```matha
def _理疗_关节活动度(current_ROM, normal_ROM)
```

**描述**: 关节活动度百分比 ROM% = 当前/正常·100。

| 参数 | 类型 | 说明 |
|------|------|------|
| `current_ROM` | `` | |
| `normal_ROM` | `` | |

Source: `src\domains\medical.py:479`

---

## `_理疗_FIM分级`

```matha
def _理疗_FIM分级(score)
```

**描述**: FIM 总分分级（18-126）→ 独立性等级字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `score` | `` | |

Source: `src\domains\medical.py:484`

---

## `_register_medical`

```matha
def _register_medical(builtins: dict) -> None
```

**描述**: 将医疗与医药理疗子领域内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（biology 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\medical.py:501`

---

## `_medical_symtab_names`

```matha
def _medical_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\medical.py:600`

---

# `domains.medtools`

```matha

```

**描述**: Matha 领域扩展模块：中医/西医器械与设备（TCM / Western Medicine Tools & Equipment）。

基于 Matha 数学基础与物理常量体系，演化中医与西医器械、设备子领域功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、中医诊断与针灸（TCM Diagnosis & Acupuncture）- 前缀 中医_
  1) 脉率分类：按 bpm 判定迟/平/数/疾脉
  2) 骨度分寸：按体表标志查同身寸折算长度
  3) 针刺深度：按总深度与比例计算进针深度
  4) 艾灸温度：按距离平方反比估算施灸温度
  5) 拔罐负压：按大气压与罐口半径估算负压吸力
  6) 子午流注：按时辰映射十二经气血流注
  7) 体质指数：按 BMI 与偏倚评分校正
  8) 经穴间距：按骨度分寸与比例计算两穴间距
  9) 九针选用：按针刺深度选用九针针具
  10) 耳穴分区：按分区代码查耳穴区域

二、中药药剂（Herbal Pharmacy）- 前缀 中药_
  1) 古今剂量：按朝代折算两→克
  2) 煎煮浓缩：按药量与终体积计算浓度
  3) 君臣佐使：按四组药量计算君药占比
  4) 儿童剂量Clark：按体重折算儿童剂量
  5) 儿童剂量Young：按年龄折算儿童剂量
  6) 煎药水量：按药量与比例计算加水体积
  7) 浸泡时间：按药材类型查浸泡时间
  8) 毒性限量：按毒性药材判定是否超限
  9) 折干率：按鲜重与折干比计算干重
  10) 配伍七情：按两药类型查配伍关系
  11) 分次服用：按总量与每日次数计算单次量

三、手术器械与力学（Surgical Instruments & Mechanics）- 前缀 手术_
  1) 缝合线张力：按 USP 号查抗拉张力
  2) 缝合线直径：按 USP 号查直径
  3) 缝合针弯矩：按力与针半径计算弯矩
  4) 止血带压力：按肢体周径与收缩压估算
  5) 螺钉拔出力：按直径与骨密度估算
  6) 克氏针选择：按骨类型选直径
  7) 放大镜视野：按放大倍数计算视野
  8) 牵引重量：按体重与比例计算
  9) 钳夹力：按握力与杠杆比计算
  10) 电刀功率：按组织类型查功率

四、医疗设备与仪器（Medical Equipment & Instruments）- 前缀 设备_
  1) 输液滴速：按 mL/h 与滴系数计算滴/分
  2) 注射泵流速：按剂量、浓度、时长计算 mL/h
  3) 分钟通气量：按潮气量与频率计算
  4) I:E 比校验：按吸呼时间化简比值
  5) 除颤能量：按体重与 J/kg 计算
  6) 超声穿透深度：按频率估算
  7) 血透超滤率：按目标量与时长计算
  8) 心电采样率：按诊断类型查采样率
  9) 输液泵压力限：按管内径估算
  10) 心率报警限：按年龄查上下限

五、康复器械与假体（Rehabilitation Devices & Prosthetics）- 前缀 康复_
  1) 接受腔压力：按体重与接触面积计算
  2) 轮椅推进力：按体重与坡度计算
  3) 弹簧常数：按载荷与变形量计算
  4) 外骨骼力矩：按所需力矩与辅助比计算
  5) 关节接触应力：按载荷与接触面积计算
  6) 肌腱张力：按截面积与安全系数计算
  7) 假体磨损：按载荷与循环次数估算
  8) 辅助器高度：按身高与比例计算
  9) 压缩袜压力：按踝部压力与梯度比计算
  10) CPM 角度增量：按目标 ROM 与天数计算

数据库：
  - 骨度分寸表（常用体表标志同身寸折算）
  - 古今剂量折算表（汉/唐/宋/明/清/现代）
  - 毒性药材极量表（附子/川乌/草乌/马钱子等）
  - 缝合线规格表（USP 2-0 ~ 7-0）
  - 子午流注十二经时辰表
  - 呼吸机默认参数表（成人/儿童/新生儿）

设计原则：
  - 与 medical / biology 保持一致：_curryN 柯里化、前缀区分
  - 函数返回纯数值或字符串/元组，不做语义包装
  - 除零 / 非法参数由 Python 自身抛错

Source: `src\domains\medtools.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\medtools.py:89`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\medtools.py:95`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\medtools.py:103`

---

## `_中医_脉率分类`

```matha
def _中医_脉率分类(bpm)
```

**描述**: 按脉率（次/分）分类：<60 迟脉，<=90 平脉，<=120 数脉，>120 疾脉。

| 参数 | 类型 | 说明 |
|------|------|------|
| `bpm` | `` | |

Source: `src\domains\medtools.py:190`

---

## `_中医_骨度分寸`

```matha
def _中医_骨度分寸(landmark)
```

**描述**: 按体表标志查骨度分寸（同身寸），未收录返回 0.0。

| 参数 | 类型 | 说明 |
|------|------|------|
| `landmark` | `` | |

Source: `src\domains\medtools.py:201`

---

## `_中医_针刺深度`

```matha
def _中医_针刺深度(depth_total, ratio)
```

**描述**: 按总深度与比例计算进针深度 = depth_total * ratio。

| 参数 | 类型 | 说明 |
|------|------|------|
| `depth_total` | `` | |
| `ratio` | `` | |

Source: `src\domains\medtools.py:206`

---

## `_中医_艾灸温度`

```matha
def _中医_艾灸温度(T_source, d, d_ref)
```

**描述**: 艾灸温度按距离平方反比估算 T = T_source * (d_ref/d)^2，d<=0 时返回源温。

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_source` | `` | |
| `d` | `` | |
| `d_ref` | `` | |

Source: `src\domains\medtools.py:211`

---

## `_中医_拔罐负压`

```matha
def _中医_拔罐负压(P_atm, r_inner_cm)
```

**描述**: 拔罐负压吸力：delta_P=P_atm*0.4, A=pi*(r/100)^2, F=delta_P*1000*A。

| 参数 | 类型 | 说明 |
|------|------|------|
| `P_atm` | `` | |
| `r_inner_cm` | `` | |

Source: `src\domains\medtools.py:218`

---

## `_中医_子午流注`

```matha
def _中医_子午流注(hour)
```

**描述**: 按时辰（24h）映射子午流注经脉，返回 'X时·Y经'。

| 参数 | 类型 | 说明 |
|------|------|------|
| `hour` | `` | |

Source: `src\domains\medtools.py:229`

---

## `_中医_体质指数`

```matha
def _中医_体质指数(BMI, bias_score)
```

**描述**: 校正体质指数 = BMI * (1 + bias_score/100)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `BMI` | `` | |
| `bias_score` | `` | |

Source: `src\domains\medtools.py:236`

---

## `_中医_经穴间距`

```matha
def _中医_经穴间距(landmark, frac1, frac2)
```

**描述**: 按骨度分寸与两穴比例计算间距 = |frac1-frac2| * total。

| 参数 | 类型 | 说明 |
|------|------|------|
| `landmark` | `` | |
| `frac1` | `` | |
| `frac2` | `` | |

Source: `src\domains\medtools.py:241`

---

## `_中医_九针选用`

```matha
def _中医_九针选用(depth_mm)
```

**描述**: 按针刺深度选用九针：<3 鍉针，<10 毫针，<20 长针，否则大针。

| 参数 | 类型 | 说明 |
|------|------|------|
| `depth_mm` | `` | |

Source: `src\domains\medtools.py:247`

---

## `_中医_耳穴分区`

```matha
def _中医_耳穴分区(zone_code)
```

**描述**: 按耳穴分区代码查区域名称。

| 参数 | 类型 | 说明 |
|------|------|------|
| `zone_code` | `` | |

Source: `src\domains\medtools.py:258`

---

## `_中药_古今剂量`

```matha
def _中药_古今剂量(liang, dynasty)
```

**描述**: 按朝代折算两→克 = liang * factor。

| 参数 | 类型 | 说明 |
|------|------|------|
| `liang` | `` | |
| `dynasty` | `` | |

Source: `src\domains\medtools.py:270`

---

## `_中药_煎煮浓缩`

```matha
def _中药_煎煮浓缩(herb_g, final_mL)
```

**描述**: 煎煮后浓度 = herb_g / final_mL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `herb_g` | `` | |
| `final_mL` | `` | |

Source: `src\domains\medtools.py:276`

---

## `_中药_君臣佐使`

```matha
def _中药_君臣佐使(jun, chen, zuo, shi)
```

**描述**: 君药占比 = jun / (jun+chen+zuo+shi)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `jun` | `` | |
| `chen` | `` | |
| `zuo` | `` | |
| `shi` | `` | |

Source: `src\domains\medtools.py:281`

---

## `_中药_儿童剂量Clark`

```matha
def _中药_儿童剂量Clark(adult_dose, child_kg, adult_kg)
```

**描述**: Clark 公式：儿童剂量 = adult_dose * child_kg / adult_kg。

| 参数 | 类型 | 说明 |
|------|------|------|
| `adult_dose` | `` | |
| `child_kg` | `` | |
| `adult_kg` | `` | |

Source: `src\domains\medtools.py:287`

---

## `_中药_儿童剂量Young`

```matha
def _中药_儿童剂量Young(adult_dose, age)
```

**描述**: Young 公式：儿童剂量 = adult_dose * age / (age + 12)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `adult_dose` | `` | |
| `age` | `` | |

Source: `src\domains\medtools.py:292`

---

## `_中药_煎药水量`

```matha
def _中药_煎药水量(herb_g, ratio)
```

**描述**: 煎药加水体积 = herb_g * ratio。

| 参数 | 类型 | 说明 |
|------|------|------|
| `herb_g` | `` | |
| `ratio` | `` | |

Source: `src\domains\medtools.py:297`

---

## `_中药_浸泡时间`

```matha
def _中药_浸泡时间(herb_type)
```

**描述**: 按药材类型查浸泡时间（分钟）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `herb_type` | `` | |

Source: `src\domains\medtools.py:302`

---

## `_中药_毒性限量`

```matha
def _中药_毒性限量(herb, dose_g)
```

**描述**: 判定毒性药材是否超限，返回 (是否超限, 极量)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `herb` | `` | |
| `dose_g` | `` | |

Source: `src\domains\medtools.py:311`

---

## `_中药_折干率`

```matha
def _中药_折干率(fresh_g, dry_ratio)
```

**描述**: 折干重 = fresh_g * dry_ratio。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fresh_g` | `` | |
| `dry_ratio` | `` | |

Source: `src\domains\medtools.py:317`

---

## `_中药_配伍七情`

```matha
def _中药_配伍七情(herb1_type, herb2_type)
```

**描述**: 按两药配伍类型查七情关系，未匹配返回单行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `herb1_type` | `` | |
| `herb2_type` | `` | |

Source: `src\domains\medtools.py:333`

---

## `_中药_分次服用`

```matha
def _中药_分次服用(total_mL, times_per_day)
```

**描述**: 单次服用量 = total_mL / times_per_day。

| 参数 | 类型 | 说明 |
|------|------|------|
| `total_mL` | `` | |
| `times_per_day` | `` | |

Source: `src\domains\medtools.py:338`

---

## `_手术_缝合线张力`

```matha
def _手术_缝合线张力(usp)
```

**描述**: 按 USP 号查缝合线抗拉张力（N）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `usp` | `` | |

Source: `src\domains\medtools.py:345`

---

## `_手术_缝合线直径`

```matha
def _手术_缝合线直径(usp)
```

**描述**: 按 USP 号查缝合线直径（mm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `usp` | `` | |

Source: `src\domains\medtools.py:351`

---

## `_手术_缝合针弯矩`

```matha
def _手术_缝合针弯矩(F, r_needle_mm)
```

**描述**: 缝合针弯矩 = F * r_needle_mm。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `r_needle_mm` | `` | |

Source: `src\domains\medtools.py:357`

---

## `_手术_止血带压力`

```matha
def _手术_止血带压力(limb_circumference_cm, SBP_mmHg)
```

**描述**: 止血带压力 = SBP + 50 + (周径/10)*10。

| 参数 | 类型 | 说明 |
|------|------|------|
| `limb_circumference_cm` | `` | |
| `SBP_mmHg` | `` | |

Source: `src\domains\medtools.py:362`

---

## `_手术_螺钉拔出力`

```matha
def _手术_螺钉拔出力(d_mm, bone_density_gcm3)
```

**描述**: 螺钉拔出力 = 50 * d * 骨密度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `d_mm` | `` | |
| `bone_density_gcm3` | `` | |

Source: `src\domains\medtools.py:368`

---

## `_手术_克氏针选择`

```matha
def _手术_克氏针选择(bone_type)
```

**描述**: 按骨类型选克氏针直径（mm）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `bone_type` | `` | |

Source: `src\domains\medtools.py:374`

---

## `_手术_放大镜视野`

```matha
def _手术_放大镜视野(magnification, FOV_at_1x_mm)
```

**描述**: 放大镜视野 = FOV_at_1x / magnification。

| 参数 | 类型 | 说明 |
|------|------|------|
| `magnification` | `` | |
| `FOV_at_1x_mm` | `` | |

Source: `src\domains\medtools.py:383`

---

## `_手术_牵引重量`

```matha
def _手术_牵引重量(body_kg, fraction)
```

**描述**: 牵引重量 = body_kg * fraction。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_kg` | `` | |
| `fraction` | `` | |

Source: `src\domains\medtools.py:388`

---

## `_手术_钳夹力`

```matha
def _手术_钳夹力(F_handle, lever_ratio)
```

**描述**: 钳夹力 = F_handle * lever_ratio。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F_handle` | `` | |
| `lever_ratio` | `` | |

Source: `src\domains\medtools.py:393`

---

## `_手术_电刀功率`

```matha
def _手术_电刀功率(tissue_type)
```

**描述**: 按组织类型查电刀功率（W）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tissue_type` | `` | |

Source: `src\domains\medtools.py:398`

---

## `_设备_输液滴速`

```matha
def _设备_输液滴速(mL_per_h, drop_factor)
```

**描述**: 输液滴速（滴/分）= mL_per_h * drop_factor / 60。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mL_per_h` | `` | |
| `drop_factor` | `` | |

Source: `src\domains\medtools.py:408`

---

## `_设备_注射泵流速`

```matha
def _设备_注射泵流速(dose_mg, conc_mg_per_mL, duration_h)
```

**描述**: 注射泵流速（mL/h）= dose / (conc * duration)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `dose_mg` | `` | |
| `conc_mg_per_mL` | `` | |
| `duration_h` | `` | |

Source: `src\domains\medtools.py:413`

---

## `_设备_分钟通气量`

```matha
def _设备_分钟通气量(VT_mL, RR)
```

**描述**: 分钟通气量（mL/min）= VT * RR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `VT_mL` | `` | |
| `RR` | `` | |

Source: `src\domains\medtools.py:418`

---

## `_设备_IE比校验`

```matha
def _设备_IE比校验(I_time, E_time)
```

**描述**: I:E 比化简：对吸呼时间各 ×10 取整后用 gcd 化简。

| 参数 | 类型 | 说明 |
|------|------|------|
| `I_time` | `` | |
| `E_time` | `` | |

Source: `src\domains\medtools.py:423`

---

## `_设备_除颤能量`

```matha
def _设备_除颤能量(body_kg, J_per_kg)
```

**描述**: 除颤能量（J）= body_kg * J_per_kg。

| 参数 | 类型 | 说明 |
|------|------|------|
| `body_kg` | `` | |
| `J_per_kg` | `` | |

Source: `src\domains\medtools.py:433`

---

## `_设备_超声穿透深度`

```matha
def _设备_超声穿透深度(freq_MHz)
```

**描述**: 超声穿透深度（cm）≈ 40 / freq_MHz。

| 参数 | 类型 | 说明 |
|------|------|------|
| `freq_MHz` | `` | |

Source: `src\domains\medtools.py:438`

---

## `_设备_血透超滤率`

```matha
def _设备_血透超滤率(target_mL, duration_h)
```

**描述**: 血透超滤率（mL/h）= target / duration。

| 参数 | 类型 | 说明 |
|------|------|------|
| `target_mL` | `` | |
| `duration_h` | `` | |

Source: `src\domains\medtools.py:443`

---

## `_设备_心电采样率`

```matha
def _设备_心电采样率(diagnostic_type)
```

**描述**: 按诊断类型查心电采样率（Hz）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `diagnostic_type` | `` | |

Source: `src\domains\medtools.py:448`

---

## `_设备_输液泵压力限`

```matha
def _设备_输液泵压力限(tube_ID_mm)
```

**描述**: 输液泵压力限 = 100 + tube_ID * 30。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tube_ID_mm` | `` | |

Source: `src\domains\medtools.py:456`

---

## `_设备_心率报警限`

```matha
def _设备_心率报警限(age)
```

**描述**: 按年龄查心率报警上下限，返回 (下限, 上限)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `age` | `` | |

Source: `src\domains\medtools.py:461`

---

## `_康复_接受腔压力`

```matha
def _康复_接受腔压力(weight_N, contact_area_cm2)
```

**描述**: 接受腔压力（kPa）= weight / (area*1e-4) / 1000。

| 参数 | 类型 | 说明 |
|------|------|------|
| `weight_N` | `` | |
| `contact_area_cm2` | `` | |

Source: `src\domains\medtools.py:474`

---

## `_康复_轮椅推进力`

```matha
def _康复_轮椅推进力(weight_kg, slope_deg)
```

**描述**: 轮椅推进力（N）= weight * g * sin(slope)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `weight_kg` | `` | |
| `slope_deg` | `` | |

Source: `src\domains\medtools.py:480`

---

## `_康复_弹簧常数`

```matha
def _康复_弹簧常数(load_N, deformation_mm)
```

**描述**: 弹簧常数（N/mm）= load / deformation。

| 参数 | 类型 | 说明 |
|------|------|------|
| `load_N` | `` | |
| `deformation_mm` | `` | |

Source: `src\domains\medtools.py:485`

---

## `_康复_外骨骼力矩`

```matha
def _康复_外骨骼力矩(torque_needed, assist_ratio)
```

**描述**: 外骨骼力矩 = torque_needed * assist_ratio。

| 参数 | 类型 | 说明 |
|------|------|------|
| `torque_needed` | `` | |
| `assist_ratio` | `` | |

Source: `src\domains\medtools.py:490`

---

## `_康复_关节接触应力`

```matha
def _康复_关节接触应力(load_N, contact_area_mm2)
```

**描述**: 关节接触应力（MPa）= load / area。

| 参数 | 类型 | 说明 |
|------|------|------|
| `load_N` | `` | |
| `contact_area_mm2` | `` | |

Source: `src\domains\medtools.py:495`

---

## `_康复_肌腱张力`

```matha
def _康复_肌腱张力(area_mm2, UTS_MPa, safety)
```

**描述**: 肌腱张力（N）= area * UTS * safety。

| 参数 | 类型 | 说明 |
|------|------|------|
| `area_mm2` | `` | |
| `UTS_MPa` | `` | |
| `safety` | `` | |

Source: `src\domains\medtools.py:500`

---

## `_康复_假体磨损`

```matha
def _康复_假体磨损(load_N, cycles, wear_coeff)
```

**描述**: 假体磨损量 = load * cycles * wear_coeff。

| 参数 | 类型 | 说明 |
|------|------|------|
| `load_N` | `` | |
| `cycles` | `` | |
| `wear_coeff` | `` | |

Source: `src\domains\medtools.py:505`

---

## `_康复_辅助器高度`

```matha
def _康复_辅助器高度(height_cm, ratio)
```

**描述**: 辅助器高度 = height * ratio。

| 参数 | 类型 | 说明 |
|------|------|------|
| `height_cm` | `` | |
| `ratio` | `` | |

Source: `src\domains\medtools.py:510`

---

## `_康复_压缩袜压力`

```matha
def _康复_压缩袜压力(ankle_pressure, gradient)
```

**描述**: 压缩袜梯度压力 = ankle_pressure * gradient。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ankle_pressure` | `` | |
| `gradient` | `` | |

Source: `src\domains\medtools.py:515`

---

## `_康复_CPM角度增量`

```matha
def _康复_CPM角度增量(target_ROM, days)
```

**描述**: CPM 每日角度增量 = target_ROM / days。

| 参数 | 类型 | 说明 |
|------|------|------|
| `target_ROM` | `` | |
| `days` | `` | |

Source: `src\domains\medtools.py:520`

---

## `_register_medtools`

```matha
def _register_medtools(builtins: dict) -> None
```

**描述**: 将中医/西医器械与设备子领域内建注册到解释器 builtins。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\medtools.py:527`

---

## `_medtools_symtab_names`

```matha
def _medtools_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\medtools.py:624`

---

# `domains.metaverse_arch`

```matha

```

**描述**: Matha 元宇宙架构领域模块：渲染引擎、物理模拟、用户交互。

覆盖：
  1) 渲染帧率估算
  2) 物理模拟步长
  3) 碰撞检测复杂度
  4) 用户并发数
  5) 资产加载延迟
  6) 网络同步延迟

Source: `src\domains\metaverse_arch.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\metaverse_arch.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\metaverse_arch.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\metaverse_arch.py:25`

---

## `_渲染帧率估算`

```matha
def _渲染帧率估算(三角形数, 像素数, 每三角形耗时_ns)
```

**描述**: 渲染帧率估算（FPS）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `三角形数` | `` | |
| `像素数` | `` | |
| `每三角形耗时_ns` | `` | |

Source: `src\domains\metaverse_arch.py:32`

---

## `_物理模拟步长`

```matha
def _物理模拟步长(物体数, 约束数, 可用算力_MFlops)
```

**描述**: 物理模拟最大稳定步长（ms）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `物体数` | `` | |
| `约束数` | `` | |
| `可用算力_MFlops` | `` | |

Source: `src\domains\metaverse_arch.py:40`

---

## `_碰撞检测复杂度`

```matha
def _碰撞检测复杂度(物体数)
```

**描述**: O(n²)碰撞检测复杂度估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `物体数` | `` | |

Source: `src\domains\metaverse_arch.py:48`

---

## `_用户并发数`

```matha
def _用户并发数(服务器带宽_Gbps, 每用户带宽_Mbps)
```

**描述**: 最大并发用户数估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `服务器带宽_Gbps` | `` | |
| `每用户带宽_Mbps` | `` | |

Source: `src\domains\metaverse_arch.py:53`

---

## `_资产加载延迟`

```matha
def _资产加载延迟(资产大小_MB, 网络带宽_Mbps, 压缩率)
```

**描述**: 资产加载延迟（ms）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `资产大小_MB` | `` | |
| `网络带宽_Mbps` | `` | |
| `压缩率` | `` | |

Source: `src\domains\metaverse_arch.py:60`

---

## `_网络同步延迟`

```matha
def _网络同步延迟(往返时间_ms, 插值窗口_ms)
```

**描述**: 网络同步后延迟（ms）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `往返时间_ms` | `` | |
| `插值窗口_ms` | `` | |

Source: `src\domains\metaverse_arch.py:68`

---

## `_register_metaverse_arch`

```matha
def _register_metaverse_arch(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\metaverse_arch.py:77`

---

## `_metaverse_arch_symtab_names`

```matha
def _metaverse_arch_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\metaverse_arch.py:86`

---

# `domains.nuclear`

```matha

```

**描述**: Matha 机械领域模块：核物理（Nuclear Physics）。

基于量子力学 + 天体力学 + 电磁学 + mathlib 数学地基，演化核物理功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、核结合能与质量亏损（Nuclear Binding Energy & Mass Defect）
  1) 质量亏损：Δm = Z·mp + N·mn - M_nucleus
  2) 结合能：B = Δm·c²
  3) 比结合能：B/A
  4) 半经验质量公式（Weizsäcker）：B = aV·A - aS·A^(2/3) - aC·Z²/A^(1/3) - aA·(A-2Z)²/A + δ
  5) 结合能曲线分析

二、放射性衰变（Radioactive Decay）
  1) 衰变定律：N(t) = N₀·e^(-λt)
  2) 半衰期：T₁/₂ = ln2/λ
  3) 平均寿命：τ = 1/λ
  4) 活度：A = λN
  5) 碳-14年代测定
  6) 衰变链

三、核反应（Nuclear Reactions）
  1) Q值：Q = (m_initial - m_final)·c²
  2) 阈能：E_th = Q·(1 + m_a/m_A)（吸热反应）
  3) 反应能阈
  4) 裂变能量释放
  5) 聚变能量释放

四、粒子物理（Particle Physics）
  1) 粒子动能（相对论）：K = (γ-1)mc²
  2) 洛伦兹因子：γ = 1/√(1-β²)
  3) 相对论动量：p = γmv
  4) 质能等价：E = mc²
  5) 德布罗意波长（相对论）
  6) 粒子相互作用截面

五、核能与反应堆（Nuclear Energy & Reactors）
  1) 反应堆功率
  2) 临界条件
  3) 增殖系数
  4) 中子慢化
  5) 辐射剂量
  6) 屏蔽计算

设计原则：
  - 所有角度输入/输出均为弧度
  - 多参函数一律 _curry2/_curry3/_curry4 封装
  - 前缀 核_ / 衰变_ / 反应_ / 粒子_ / 反应堆_ 区分子领域
  - 核物理常量与同位素数据作为常量注册

Source: `src\domains\nuclear.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\nuclear.py:61`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\nuclear.py:67`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\nuclear.py:75`

---

## `_核_质量亏损`

```matha
def _核_质量亏损(Z, N, M_nucleus)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Z` | `` | |
| `N` | `` | |
| `M_nucleus` | `` | |

Source: `src\domains\nuclear.py:126`

---

## `_核_结合能J`

```matha
def _核_结合能J(delta_m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `delta_m` | `` | |

Source: `src\domains\nuclear.py:130`

---

## `_核_结合能MeV`

```matha
def _核_结合能MeV(delta_m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `delta_m` | `` | |

Source: `src\domains\nuclear.py:132`

---

## `_核_比结合能`

```matha
def _核_比结合能(delta_m, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `delta_m` | `` | |
| `A` | `` | |

Source: `src\domains\nuclear.py:134`

---

## `_核_结合能由U`

```matha
def _核_结合能由U(delta_m_u)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `delta_m_u` | `` | |

Source: `src\domains\nuclear.py:136`

---

## `_核_半经验结合能`

```matha
def _核_半经验结合能(A, Z)
```

**描述**: Weizsäcker 半经验质量公式，返回结合能（MeV）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |
| `Z` | `` | |

Source: `src\domains\nuclear.py:139`

---

## `_核_半经验比结合能`

```matha
def _核_半经验比结合能(A, Z)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |
| `Z` | `` | |

Source: `src\domains\nuclear.py:165`

---

## `_核_α衰变Q`

```matha
def _核_α衰变Q(M_parent, M_daughter, M_alpha)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M_parent` | `` | |
| `M_daughter` | `` | |
| `M_alpha` | `` | |

Source: `src\domains\nuclear.py:168`

---

## `_核_β衰变Q`

```matha
def _核_β衰变Q(M_parent, M_daughter)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M_parent` | `` | |
| `M_daughter` | `` | |

Source: `src\domains\nuclear.py:172`

---

## `_衰变_剩余核数`

```matha
def _衰变_剩余核数(N0, lam, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `lam` | `` | |
| `t` | `` | |

Source: `src\domains\nuclear.py:181`

---

## `_衰变_活度`

```matha
def _衰变_活度(lam, N)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `lam` | `` | |
| `N` | `` | |

Source: `src\domains\nuclear.py:183`

---

## `_衰变_半衰期`

```matha
def _衰变_半衰期(lam)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `lam` | `` | |

Source: `src\domains\nuclear.py:185`

---

## `_衰变_衰变常数`

```matha
def _衰变_衰变常数(T_half)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_half` | `` | |

Source: `src\domains\nuclear.py:187`

---

## `_衰变_平均寿命`

```matha
def _衰变_平均寿命(lam)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `lam` | `` | |

Source: `src\domains\nuclear.py:189`

---

## `_衰变_已衰变数`

```matha
def _衰变_已衰变数(N0, lam, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `lam` | `` | |
| `t` | `` | |

Source: `src\domains\nuclear.py:191`

---

## `_衰变_双衰变剩余`

```matha
def _衰变_双衰变剩余(N0, lam1, lam2, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `lam1` | `` | |
| `lam2` | `` | |
| `t` | `` | |

Source: `src\domains\nuclear.py:193`

---

## `_衰变_碳14测年`

```matha
def _衰变_碳14测年(N0, N_now)
```

**描述**: 碳-14年代测定，半衰期 5730 年。返回年代（年）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N0` | `` | |
| `N_now` | `` | |

Source: `src\domains\nuclear.py:197`

---

## `_衰变_Ci转Bq`

```matha
def _衰变_Ci转Bq(Ci)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Ci` | `` | |

Source: `src\domains\nuclear.py:204`

---

## `_衰变_Bq转Ci`

```matha
def _衰变_Bq转Ci(Bq)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Bq` | `` | |

Source: `src\domains\nuclear.py:205`

---

## `_反应_Q值`

```matha
def _反应_Q值(m_initial, m_final)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m_initial` | `` | |
| `m_final` | `` | |

Source: `src\domains\nuclear.py:213`

---

## `_反应_Q值U`

```matha
def _反应_Q值U(m_initial_u, m_final_u)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m_initial_u` | `` | |
| `m_final_u` | `` | |

Source: `src\domains\nuclear.py:217`

---

## `_反应_阈能`

```matha
def _反应_阈能(Q_MeV, m_a, m_A)
```

**描述**: Q < 0 时的反应阈能（MeV）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q_MeV` | `` | |
| `m_a` | `` | |
| `m_A` | `` | |

Source: `src\domains\nuclear.py:221`

---

## `_反应_裂变能`

```matha
def _反应_裂变能(n, E_per_fission_MeV)
```

**描述**: n 次裂变释放的能量（MeV）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `E_per_fission_MeV` | `` | |

Source: `src\domains\nuclear.py:228`

---

## `_反应_聚变能`

```matha
def _反应_聚变能(n, E_per_fusion_MeV)
```

**描述**: n 次聚变释放的能量（MeV）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `E_per_fusion_MeV` | `` | |

Source: `src\domains\nuclear.py:233`

---

## `_反应_裂变功率`

```matha
def _反应_裂变功率(rate, E_per_fission_MeV)
```

**描述**: rate = 裂变率（次/秒），返回功率（W）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `rate` | `` | |
| `E_per_fission_MeV` | `` | |

Source: `src\domains\nuclear.py:238`

---

## `_反应_临界质量估算`

```matha
def _反应_临界质量估算(rho, sigma_f, nu, v_th)
```

**描述**: 简化的临界质量估算（kg），rho密度, sigma_f裂变截面, nu每次裂变中子数, v_th热中子速度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `rho` | `` | |
| `sigma_f` | `` | |
| `nu` | `` | |
| `v_th` | `` | |

Source: `src\domains\nuclear.py:243`

---

## `_粒子_洛伦兹因子`

```matha
def _粒子_洛伦兹因子(v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |

Source: `src\domains\nuclear.py:255`

---

## `_粒子_相对论总能量`

```matha
def _粒子_相对论总能量(m, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |

Source: `src\domains\nuclear.py:257`

---

## `_粒子_相对论动能`

```matha
def _粒子_相对论动能(m, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |

Source: `src\domains\nuclear.py:259`

---

## `_粒子_相对论动量`

```matha
def _粒子_相对论动量(m, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |

Source: `src\domains\nuclear.py:263`

---

## `_粒子_质能等价`

```matha
def _粒子_质能等价(m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |

Source: `src\domains\nuclear.py:267`

---

## `_粒子_质能等价MeV`

```matha
def _粒子_质能等价MeV(m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |

Source: `src\domains\nuclear.py:269`

---

## `_粒子_德布罗意波长相对论`

```matha
def _粒子_德布罗意波长相对论(m, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |

Source: `src\domains\nuclear.py:271`

---

## `_粒子_速度由动能`

```matha
def _粒子_速度由动能(K, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `K` | `` | |
| `m` | `` | |

Source: `src\domains\nuclear.py:275`

---

## `_粒子_动量由能量`

```matha
def _粒子_动量由能量(E_total, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E_total` | `` | |
| `m` | `` | |

Source: `src\domains\nuclear.py:279`

---

## `_反应堆_有效增殖系数`

```matha
def _反应堆_有效增殖系数(k_inf, L2, B2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k_inf` | `` | |
| `L2` | `` | |
| `B2` | `` | |

Source: `src\domains\nuclear.py:288`

---

## `_反应堆_临界判断`

```matha
def _反应堆_临界判断(k_eff)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k_eff` | `` | |

Source: `src\domains\nuclear.py:290`

---

## `_反应堆_反应性`

```matha
def _反应堆_反应性(k_eff)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k_eff` | `` | |

Source: `src\domains\nuclear.py:292`

---

## `_反应堆_周期`

```matha
def _反应堆_周期(l_neutron, rho)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `l_neutron` | `` | |
| `rho` | `` | |

Source: `src\domains\nuclear.py:294`

---

## `_反应堆_慢化比`

```matha
def _反应堆_慢化比(A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |

Source: `src\domains\nuclear.py:296`

---

## `_反应堆_慢化碰撞数`

```matha
def _反应堆_慢化碰撞数(E0, E_th, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E0` | `` | |
| `E_th` | `` | |
| `A` | `` | |

Source: `src\domains\nuclear.py:301`

---

## `_反应堆_吸收剂量`

```matha
def _反应堆_吸收剂量(energy, mass)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `energy` | `` | |
| `mass` | `` | |

Source: `src\domains\nuclear.py:305`

---

## `_反应堆_当量剂量`

```matha
def _反应堆_当量剂量(D, w_R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `D` | `` | |
| `w_R` | `` | |

Source: `src\domains\nuclear.py:307`

---

## `_反应堆_半减弱层`

```matha
def _反应堆_半减弱层(mu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |

Source: `src\domains\nuclear.py:309`

---

## `_反应堆_屏蔽强度`

```matha
def _反应堆_屏蔽强度(I0, mu, x)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I0` | `` | |
| `mu` | `` | |
| `x` | `` | |

Source: `src\domains\nuclear.py:311`

---

## `_register_nuclear`

```matha
def _register_nuclear(builtins: dict) -> None
```

**描述**: 将核物理内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（celestial 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\nuclear.py:318`

---

## `_nuclear_symtab_names`

```matha
def _nuclear_symtab_names() -> list[str]
```

**描述**: 返回核物理所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\nuclear.py:387`

---

# `domains.optics`

```matha

```

**描述**: Matha 机械领域模块：光学（Optics）。

基于力学 + 电磁学 + 声学 + mathlib 数学地基，演化光学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、几何光学（Geometric Optics）
  1) 折射定律（斯涅尔定律）：n₁sinθ₁ = n₂sinθ₂
  2) 全反射临界角：sinθc = n₂/n₁
  3) 球面镜焦距：f = R/2
  4) 球面镜成像：1/v + 1/u = 1/f
  5) 薄透镜成像：1/v - 1/u = 1/f（笛卡尔符号）
  6) 透镜焦距（造焦公式）：1/f = (n-1)(1/R₁ - 1/R₂)
  7) 放大率：m = -v/u
  8) 光速与折射率：v = c/n

二、波动光学（Wave Optics）
  1) 双缝干涉条纹间距：Δy = λD/d
  2) 薄膜干涉（垂直入射）：2nd = mλ（亮纹）
  3) 单缝衍射暗纹位置：a·sinθ = mλ
  4) 光栅方程：d·sinθ = mλ
  5) 光栅分辨本领：R = mN
  6) 马吕斯定律：I = I₀cos²θ
  7) 布儒斯特角：tanθB = n₂/n₁

三、光度学（Photometry）
  1) 光通量与发光强度：Φ = 4πI（各向同性）
  2) 照度（平方反比）：E = I/r²
  3) 照度（斜入射）：E = (I/r²)cosθ
  4) 光亮度：L = I/A
  5) 光视效能：K = Φ/P

四、光学仪器（Optical Instruments）
  1) 放大镜放大率：M = 25/f + 1（正常眼）
  2) 显微镜放大率：M = (L/f_o)(25/f_e)
  3) 望远镜放大率：M = -f_o/f_e
  4) 数值孔径：NA = n·sinα
  5) 最小分辨角（瑞利判据）：θ = 1.22λ/D
  6) 眼睛近点/远点调节

五、色散与光谱（Dispersion & Spectrum）
  1) 柯西方程：n(λ) = A + B/λ²
  2) 色散率：dn/dλ = -2B/λ³
  3) 光子能量：E = hf = hc/λ
  4) 光子动量：p = h/λ
  5) 红移：z = (λ_obs - λ_emit)/λ_emit
  6) 多普勒红移速度：v = zc（非相对论）

设计原则：
  - 所有角度输入/输出均为弧度（rad）
  - 多参函数一律 _curry2/_curry3/_curry4 封装
  - 前缀 几何_ / 波动_ / 光度_ / 仪器_ / 色散_ 区分子领域
  - 常用折射率/光波长作为常量注册

Source: `src\domains\optics.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\optics.py:65`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\optics.py:71`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\optics.py:79`

---

## `_几何_折射角`

```matha
def _几何_折射角(n1, theta1, n2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n1` | `` | |
| `theta1` | `` | |
| `n2` | `` | |

Source: `src\domains\optics.py:106`

---

## `_几何_全反射角`

```matha
def _几何_全反射角(n1, n2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n1` | `` | |
| `n2` | `` | |

Source: `src\domains\optics.py:108`

---

## `_几何_球面镜焦距`

```matha
def _几何_球面镜焦距(R)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R` | `` | |

Source: `src\domains\optics.py:110`

---

## `_几何_像距`

```matha
def _几何_像距(u, f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `u` | `` | |
| `f` | `` | |

Source: `src\domains\optics.py:112`

---

## `_几何_放大率`

```matha
def _几何_放大率(v, u)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |
| `u` | `` | |

Source: `src\domains\optics.py:114`

---

## `_几何_透镜焦距`

```matha
def _几何_透镜焦距(n, R1, R2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `R1` | `` | |
| `R2` | `` | |

Source: `src\domains\optics.py:116`

---

## `_几何_介质光速`

```matha
def _几何_介质光速(n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\optics.py:119`

---

## `_几何_折射率`

```matha
def _几何_折射率(v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `` | |

Source: `src\domains\optics.py:121`

---

## `_几何_透镜组合`

```matha
def _几何_透镜组合(f1, f2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f1` | `` | |
| `f2` | `` | |

Source: `src\domains\optics.py:123`

---

## `_波动_双缝条纹间距`

```matha
def _波动_双缝条纹间距(wavelength, D, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |
| `D` | `` | |
| `d` | `` | |

Source: `src\domains\optics.py:131`

---

## `_波动_薄膜光程差`

```matha
def _波动_薄膜光程差(n, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `d` | `` | |

Source: `src\domains\optics.py:133`

---

## `_波动_单缝暗纹角`

```matha
def _波动_单缝暗纹角(wavelength, a, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |
| `a` | `` | |
| `m` | `` | |

Source: `src\domains\optics.py:135`

---

## `_波动_光栅衍射角`

```matha
def _波动_光栅衍射角(wavelength, d_grating, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |
| `d_grating` | `` | |
| `m` | `` | |

Source: `src\domains\optics.py:137`

---

## `_波动_光栅分辨本领`

```matha
def _波动_光栅分辨本领(m, N)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `N` | `` | |

Source: `src\domains\optics.py:139`

---

## `_波动_马吕斯定律`

```matha
def _波动_马吕斯定律(I0, theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I0` | `` | |
| `theta` | `` | |

Source: `src\domains\optics.py:141`

---

## `_波动_布儒斯特角`

```matha
def _波动_布儒斯特角(n1, n2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n1` | `` | |
| `n2` | `` | |

Source: `src\domains\optics.py:143`

---

## `_波动_双缝光程差`

```matha
def _波动_双缝光程差(d, theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |
| `theta` | `` | |

Source: `src\domains\optics.py:145`

---

## `_光度_光通量`

```matha
def _光度_光通量(I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |

Source: `src\domains\optics.py:153`

---

## `_光度_照度`

```matha
def _光度_照度(I, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `r` | `` | |

Source: `src\domains\optics.py:155`

---

## `_光度_斜照度`

```matha
def _光度_斜照度(I, r, theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `r` | `` | |
| `theta` | `` | |

Source: `src\domains\optics.py:157`

---

## `_光度_亮度`

```matha
def _光度_亮度(I, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `A` | `` | |

Source: `src\domains\optics.py:159`

---

## `_光度_光视效能`

```matha
def _光度_光视效能(Phi, P)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Phi` | `` | |
| `P` | `` | |

Source: `src\domains\optics.py:161`

---

## `_光度_发光强度`

```matha
def _光度_发光强度(Phi, Omega)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Phi` | `` | |
| `Omega` | `` | |

Source: `src\domains\optics.py:163`

---

## `_仪器_放大镜`

```matha
def _仪器_放大镜(f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |

Source: `src\domains\optics.py:171`

---

## `_仪器_显微镜`

```matha
def _仪器_显微镜(L, f_o, f_e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `L` | `` | |
| `f_o` | `` | |
| `f_e` | `` | |

Source: `src\domains\optics.py:173`

---

## `_仪器_望远镜`

```matha
def _仪器_望远镜(f_o, f_e)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f_o` | `` | |
| `f_e` | `` | |

Source: `src\domains\optics.py:175`

---

## `_仪器_数值孔径`

```matha
def _仪器_数值孔径(n, alpha)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `alpha` | `` | |

Source: `src\domains\optics.py:177`

---

## `_仪器_最小分辨角`

```matha
def _仪器_最小分辨角(wavelength, D)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |
| `D` | `` | |

Source: `src\domains\optics.py:179`

---

## `_仪器_分辨极限`

```matha
def _仪器_分辨极限(wavelength, NA)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |
| `NA` | `` | |

Source: `src\domains\optics.py:181`

---

## `_仪器_人眼分辨角`

```matha
def _仪器_人眼分辨角()
```

Source: `src\domains\optics.py:183`

---

## `_色散_柯西折射率`

```matha
def _色散_柯西折射率(A, B, wavelength)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `` | |
| `B` | `` | |
| `wavelength` | `` | |

Source: `src\domains\optics.py:191`

---

## `_色散_色散率`

```matha
def _色散_色散率(B, wavelength)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `B` | `` | |
| `wavelength` | `` | |

Source: `src\domains\optics.py:193`

---

## `_色散_光子能量频率`

```matha
def _色散_光子能量频率(f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |

Source: `src\domains\optics.py:195`

---

## `_色散_光子能量波长`

```matha
def _色散_光子能量波长(wavelength)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |

Source: `src\domains\optics.py:196`

---

## `_色散_光子动量`

```matha
def _色散_光子动量(wavelength)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `wavelength` | `` | |

Source: `src\domains\optics.py:198`

---

## `_色散_红移`

```matha
def _色散_红移(lam_obs, lam_emit)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `lam_obs` | `` | |
| `lam_emit` | `` | |

Source: `src\domains\optics.py:200`

---

## `_色散_红移速度`

```matha
def _色散_红移速度(z)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `z` | `` | |

Source: `src\domains\optics.py:202`

---

## `_register_optics`

```matha
def _register_optics(builtins: dict) -> None
```

**描述**: 将光学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（acoustics 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\optics.py:252`

---

## `_optics_symtab_names`

```matha
def _optics_symtab_names() -> list[str]
```

**描述**: 返回光学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\optics.py:318`

---

# `domains.os_network`

```matha

```

**描述**: Matha OS与网络领域模块：进程调度、内存管理、文件系统、网络协议。

覆盖：
  1) 进程调度等待时间
  2) 内存页表开销
  3) 文件系统碎片率
  4) TCP重传率估算
  5) DNS查询延迟
  6) 网络带宽利用率

Source: `src\domains\os_network.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\os_network.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\os_network.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\os_network.py:25`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\os_network.py:31`

---

## `_进程调度等待时间`

```matha
def _进程调度等待时间(进程数, 时间片_ms, 优先级数)
```

**描述**: FIFO/RR调度等待时间估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `进程数` | `` | |
| `时间片_ms` | `` | |
| `优先级数` | `` | |

Source: `src\domains\os_network.py:40`

---

## `_内存页表开销`

```matha
def _内存页表开销(内存大小_MB, 页面大小_kb)
```

**描述**: 页表内存开销（KB）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `内存大小_MB` | `` | |
| `页面大小_kb` | `` | |

Source: `src\domains\os_network.py:47`

---

## `_文件碎片率`

```matha
def _文件碎片率(已用块数, 总块数, 连续块数)
```

**描述**: 文件系统碎片率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `已用块数` | `` | |
| `总块数` | `` | |
| `连续块数` | `` | |

Source: `src\domains\os_network.py:55`

---

## `_TCP重传率`

```matha
def _TCP重传率(丢包率, 重传阈值)
```

**描述**: TCP重传率估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `丢包率` | `` | |
| `重传阈值` | `` | |

Source: `src\domains\os_network.py:62`

---

## `_DNS查询延迟`

```matha
def _DNS查询延迟(服务器距离_km, 解析跳数)
```

**描述**: DNS查询延迟（ms）。光速传输+每跳处理延迟。

| 参数 | 类型 | 说明 |
|------|------|------|
| `服务器距离_km` | `` | |
| `解析跳数` | `` | |

Source: `src\domains\os_network.py:69`

---

## `_带宽利用率`

```matha
def _带宽利用率(吞吐量_Mbps, 链路容量_Mbps)
```

**描述**: 带宽利用率（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `吞吐量_Mbps` | `` | |
| `链路容量_Mbps` | `` | |

Source: `src\domains\os_network.py:76`

---

## `_register_os_network`

```matha
def _register_os_network(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\os_network.py:87`

---

## `_os_network_symtab_names`

```matha
def _os_network_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\os_network.py:96`

---

# `domains.quantum`

```matha

```

**描述**: Matha 机械领域模块：量子力学（Quantum Mechanics）。

基于电磁学 + 光学 + mathlib 数学地基，演化量子力学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、波函数与薛定谔方程（Wave Function & Schrödinger Equation）
  1) 德布罗意波长：λ = h/p = h/(mv)
  2) 概率密度：ρ = |ψ|²
  3) 波函数归一化检查：∫|ψ|²dx = 1
  4) 动能算符期望值（简化）：<T> = p²/(2m)
  5) 含时薛定谔方程能量：E = ℏω
  6) 定态薛定谔方程（自由粒子）：E = ℏ²k²/(2m)
  7) 波数与动量：p = ℏk

二、不确定性原理（Uncertainty Principle）
  1) 位置-动量不确定性：Δx·Δp ≥ ℏ/2
  2) 能量-时间不确定性：ΔE·Δt ≥ ℏ/2
  3) 最小不确定性波包：Δx·Δp = ℏ/2
  4) 由位置不确定度求动量不确定度：Δp = ℏ/(2Δx)

三、角动量与自旋（Angular Momentum & Spin）
  1) 轨道角动量模：|L| = ℏ√(l(l+1))
  2) 角动量z分量：Lz = m_l·ℏ
  3) 自旋角动量模：|S| = ℏ√(s(s+1))
  4) 自旋z分量：Sz = m_s·ℏ
  5) 总角动量模：|J| = ℏ√(j(j+1))
  6) 磁矩：μ = -g·(e/2m)·J

四、势阱与能级（Potential Well & Energy Levels）
  1) 无限深方势阱能级：En = n²π²ℏ²/(2mL²)
  2) 氢原子能级：En = -13.6/n² eV
  3) 一维谐振子能级：En = (n+1/2)ℏω
  4) 谐振子基态波函数宽度：a = √(ℏ/(mω))
  5) 势垒高度与穿透深度
  6) 有限深势阱束缚态数

五、量子隧穿与散射（Quantum Tunneling & Scattering）
  1) 隧穿概率（WKB近似）：T ≈ exp(-2κa)
  2) 衰减常数：κ = √(2m(V₀-E))/ℏ
  3) 势垒透射系数（方势垒）：T = [1 + V₀²sinh²(κa)/(4E(V₀-E))]⁻¹
  4) 光电效应：Kmax = hf - φ
  5) 康普顿波长：λC = h/(mc)
  6) 康普顿散射波长偏移：Δλ = (h/mc)(1-cosθ)

设计原则：
  - 所有角度输入/输出均为弧度
  - 多参函数一律 _curry2/_curry3/_curry4 封装
  - 前缀 波_ / 不确定_ / 角动_ / 势阱_ / 隧穿_ 区分子领域
  - 量子力学常量作为常量注册

Source: `src\domains\quantum.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\quantum.py:62`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\quantum.py:68`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\quantum.py:76`

---

## `_波_德布罗意波长`

```matha
def _波_德布罗意波长(m, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `v` | `` | |

Source: `src\domains\quantum.py:119`

---

## `_波_德布罗意波长动量`

```matha
def _波_德布罗意波长动量(p)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |

Source: `src\domains\quantum.py:121`

---

## `_波_概率密度`

```matha
def _波_概率密度(psi)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `psi` | `` | |

Source: `src\domains\quantum.py:123`

---

## `_波_能量由频率`

```matha
def _波_能量由频率(omega)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `omega` | `` | |

Source: `src\domains\quantum.py:125`

---

## `_波_自由粒子能量`

```matha
def _波_自由粒子能量(k, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k` | `` | |
| `m` | `` | |

Source: `src\domains\quantum.py:127`

---

## `_波_动量由波数`

```matha
def _波_动量由波数(k)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k` | `` | |

Source: `src\domains\quantum.py:129`

---

## `_波_波数由动量`

```matha
def _波_波数由动量(p)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |

Source: `src\domains\quantum.py:131`

---

## `_波_动能期望`

```matha
def _波_动能期望(p, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `m` | `` | |

Source: `src\domains\quantum.py:133`

---

## `_波_角频率由能量`

```matha
def _波_角频率由能量(E)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |

Source: `src\domains\quantum.py:135`

---

## `_不确定_动量不确定度`

```matha
def _不确定_动量不确定度(dx)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dx` | `` | |

Source: `src\domains\quantum.py:143`

---

## `_不确定_位置不确定度`

```matha
def _不确定_位置不确定度(dp)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dp` | `` | |

Source: `src\domains\quantum.py:145`

---

## `_不确定_能量不确定度`

```matha
def _不确定_能量不确定度(dt)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dt` | `` | |

Source: `src\domains\quantum.py:147`

---

## `_不确定_时间不确定度`

```matha
def _不确定_时间不确定度(dE)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dE` | `` | |

Source: `src\domains\quantum.py:149`

---

## `_不确定_下限`

```matha
def _不确定_下限()
```

Source: `src\domains\quantum.py:151`

---

## `_不确定_验证`

```matha
def _不确定_验证(dx, dp)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dx` | `` | |
| `dp` | `` | |

Source: `src\domains\quantum.py:153`

---

## `_角动_轨道模`

```matha
def _角动_轨道模(l)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `l` | `` | |

Source: `src\domains\quantum.py:161`

---

## `_角动_轨道z`

```matha
def _角动_轨道z(ml)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `ml` | `` | |

Source: `src\domains\quantum.py:163`

---

## `_角动_自旋模`

```matha
def _角动_自旋模(s)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `` | |

Source: `src\domains\quantum.py:165`

---

## `_角动_自旋z`

```matha
def _角动_自旋z(ms)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `ms` | `` | |

Source: `src\domains\quantum.py:167`

---

## `_角动_总模`

```matha
def _角动_总模(j)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `j` | `` | |

Source: `src\domains\quantum.py:169`

---

## `_角动_总z`

```matha
def _角动_总z(mj)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mj` | `` | |

Source: `src\domains\quantum.py:171`

---

## `_角动_磁矩`

```matha
def _角动_磁矩(g, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `g` | `` | |
| `m` | `` | |

Source: `src\domains\quantum.py:173`

---

## `_角动_朗德g`

```matha
def _角动_朗德g(J, L, S)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `J` | `` | |
| `L` | `` | |
| `S` | `` | |

Source: `src\domains\quantum.py:175`

---

## `_势阱_无限深能级`

```matha
def _势阱_无限深能级(n, m, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `m` | `` | |
| `L` | `` | |

Source: `src\domains\quantum.py:186`

---

## `_势阱_氢原子能级J`

```matha
def _势阱_氢原子能级J(n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\quantum.py:188`

---

## `_势阱_氢原子能级eV`

```matha
def _势阱_氢原子能级eV(n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\quantum.py:190`

---

## `_势阱_谐振子能级`

```matha
def _势阱_谐振子能级(n, omega)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `omega` | `` | |

Source: `src\domains\quantum.py:192`

---

## `_势阱_谐振子特征长度`

```matha
def _势阱_谐振子特征长度(m, omega)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `omega` | `` | |

Source: `src\domains\quantum.py:194`

---

## `_势阱_谐振子振幅`

```matha
def _势阱_谐振子振幅(E, m, omega)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `m` | `` | |
| `omega` | `` | |

Source: `src\domains\quantum.py:196`

---

## `_势阱_玻尔半径`

```matha
def _势阱_玻尔半径()
```

Source: `src\domains\quantum.py:198`

---

## `_势阱_氢原子半径`

```matha
def _势阱_氢原子半径(n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\domains\quantum.py:200`

---

## `_势阱_氢原子电离能`

```matha
def _势阱_氢原子电离能()
```

Source: `src\domains\quantum.py:202`

---

## `_隧穿_衰减常数`

```matha
def _隧穿_衰减常数(m, V0, E)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `V0` | `` | |
| `E` | `` | |

Source: `src\domains\quantum.py:210`

---

## `_隧穿_WKB概率`

```matha
def _隧穿_WKB概率(kappa, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `kappa` | `` | |
| `a` | `` | |

Source: `src\domains\quantum.py:216`

---

## `_隧穿_WKB概率直接`

```matha
def _隧穿_WKB概率直接(m, V0, E, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `V0` | `` | |
| `E` | `` | |
| `a` | `` | |

Source: `src\domains\quantum.py:218`

---

## `_隧穿_方势垒透射`

```matha
def _隧穿_方势垒透射(E, V0, m, a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `V0` | `` | |
| `m` | `` | |
| `a` | `` | |

Source: `src\domains\quantum.py:223`

---

## `_隧穿_光电效应`

```matha
def _隧穿_光电效应(f, phi)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `phi` | `` | |

Source: `src\domains\quantum.py:234`

---

## `_隧穿_截止频率`

```matha
def _隧穿_截止频率(phi)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `phi` | `` | |

Source: `src\domains\quantum.py:236`

---

## `_隧穿_康普顿波长`

```matha
def _隧穿_康普顿波长(m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |

Source: `src\domains\quantum.py:238`

---

## `_隧穿_康普顿偏移`

```matha
def _隧穿_康普顿偏移(theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `theta` | `` | |

Source: `src\domains\quantum.py:240`

---

## `_register_quantum`

```matha
def _register_quantum(builtins: dict) -> None
```

**描述**: 将量子力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（structural 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\quantum.py:251`

---

## `_quantum_symtab_names`

```matha
def _quantum_symtab_names() -> list[str]
```

**描述**: 返回量子力学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\quantum.py:320`

---

# `domains.quantum_compute`

```matha

```

**描述**: Matha 量子计算领域模块。

覆盖：
  1) 量子门：H, X, Y, Z, CNOT, Toffoli, SWAP
  2) 量子电路：Bell态, GHZ态, QFT
  3) 量子算法：Grover, Shor简化版
  4) 量子态操作

注意：不依赖 numpy，使用纯 Python 实现以避免依赖问题。

Source: `src\domains\quantum_compute.py:1`

---

## `hadamard`

```matha
def hadamard()
```

**描述**: Hadamard 门。

Source: `src\domains\quantum_compute.py:46`

---

## `pauli_x`

```matha
def pauli_x()
```

**描述**: Pauli-X 门（量子非门）。

Source: `src\domains\quantum_compute.py:50`

---

## `pauli_y`

```matha
def pauli_y()
```

**描述**: Pauli-Y 门。

Source: `src\domains\quantum_compute.py:54`

---

## `pauli_z`

```matha
def pauli_z()
```

**描述**: Pauli-Z 门。

Source: `src\domains\quantum_compute.py:58`

---

## `cnot`

```matha
def cnot()
```

**描述**: CNOT 门。

Source: `src\domains\quantum_compute.py:62`

---

## `swap_gate`

```matha
def swap_gate()
```

**描述**: SWAP 门。

Source: `src\domains\quantum_compute.py:66`

---

## `toffoli`

```matha
def toffoli()
```

**描述**: Toffoli（CCNOT）门（8x8矩阵）。

Source: `src\domains\quantum_compute.py:70`

---

## `bell_state`

```matha
def bell_state()
```

**描述**: Bell 态 |Phi+> = (|00> + |11>) / sqrt(2)。

Source: `src\domains\quantum_compute.py:82`

---

## `ghz_state`

```matha
def ghz_state(n_qubits: int)
```

**描述**: GHZ 态 |0...0> + |1...1>。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n_qubits` | `int` | |

Source: `src\domains\quantum_compute.py:86`

---

## `qubit_state`

```matha
def qubit_state(theta: float, phi: float)
```

**描述**: 单量子比特状态 |psi> = cos(theta/2)|0> + e^(i*phi)*sin(theta/2)|1>。

| 参数 | 类型 | 说明 |
|------|------|------|
| `theta` | `float` | |
| `phi` | `float` | |

Source: `src\domains\quantum_compute.py:94`

---

## `grover_iterate`

```matha
def grover_iterate(state, oracle: Callable, n_iterations: int)
```

**描述**: Grover 迭代（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `state` | `` | |
| `oracle` | `Callable` | |
| `n_iterations` | `int` | |

Source: `src\domains\quantum_compute.py:106`

---

## `shor_period_finding`

```matha
def shor_period_finding(a: int, N: int, max_period: int)
```

**描述**: Shor 算法周期寻找（简化版经典部分）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `int` | |
| `N` | `int` | |
| `max_period` | `int` | |

Source: `src\domains\quantum_compute.py:115`

---

## `quantum_fourier_transform`

```matha
def quantum_fourier_transform(state)
```

**描述**: 量子傅里叶变换。

| 参数 | 类型 | 说明 |
|------|------|------|
| `state` | `` | |

Source: `src\domains\quantum_compute.py:124`

---

## `circuit_depth`

```matha
def circuit_depth(gates: list, n_qubits: int) -> int
```

**描述**: 计算电路深度（简化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `gates` | `list` | |
| `n_qubits` | `int` | |

**返回**: `int`

Source: `src\domains\quantum_compute.py:139`

---

## `gate_decompose`

```matha
def gate_decompose(cnot_count: int) -> list
```

**描述**: 门分解（CNOT → 基础门）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cnot_count` | `int` | |

**返回**: `list`

Source: `src\domains\quantum_compute.py:151`

---

## `_register_quantum_compute`

```matha
def _register_quantum_compute(builtins: dict) -> None
```

**描述**: 注册量子计算内建到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\quantum_compute.py:176`

---

## `_quantum_compute_symtab_names`

```matha
def _quantum_compute_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\quantum_compute.py:195`

---

# `domains.real_hardware`

```matha

```

**描述**: 真实硬件驱动层。

为 Matha 提供真实硬件访问能力：
  - GPIO（Raspberry Pi / Arduino）
  - UART / Serial 通信
  - I2C / SPI 总线
  - ADC/DAC 模数转换
  - PWM 输出
  - 传感器读取（温度、光线、距离等）
  - 电机控制（步进、直流、舵机）
  - 显示驱动（OLED, LCD）

注意：需要安装对应的硬件库（如 pigpio, pyserial, smbus2）。

Source: `src\domains\real_hardware.py:1`

---

## `GPIOHardware`

```matha
class GPIOHardware
```

**描述**: Raspberry Pi GPIO 驱动。

Source: `src\domains\real_hardware.py:26`

---

### `__new__`

```matha
def __new__(cls) -> 'GPIOHardware'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'GPIOHardware'`

Source: `src\domains\real_hardware.py:32`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:37`

---

### `setup`

```matha
def setup(self, pin: int, mode: str) -> None
```

**描述**: 设置 GPIO 引脚模式。mode: IN | OUT | PWM。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:58`

---

### `output`

```matha
def output(self, pin: int, value: int) -> None
```

**描述**: 设置 GPIO 引脚输出。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:68`

---

### `input`

```matha
def input(self, pin: int) -> int
```

**描述**: 读取 GPIO 引脚输入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |

**返回**: `int`

Source: `src\domains\real_hardware.py:77`

---

### `PWM`

```matha
def PWM(self, pin: int, frequency: float, duty_cycle: float) -> None
```

**描述**: 设置 PWM 输出。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `frequency` | `float` | |
| `duty_cycle` | `float` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:86`

---

### `cleanup`

```matha
def cleanup(self) -> None
```

**描述**: 清理 GPIO 资源。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:97`

---

## `SerialDriver`

```matha
class SerialDriver
```

**描述**: UART/Serial 通信驱动。

Source: `src\domains\real_hardware.py:112`

---

### `__init__`

```matha
def __init__(self, port: str, baudrate: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port` | `str` | |
| `baudrate` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:115`

---

### `write`

```matha
def write(self, data: bytes) -> int
```

**描述**: 发送数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `bytes` | |

**返回**: `int`

Source: `src\domains\real_hardware.py:128`

---

### `read`

```matha
def read(self, size: int) -> bytes
```

**描述**: 接收数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `size` | `int` | |

**返回**: `bytes`

Source: `src\domains\real_hardware.py:134`

---

### `readline`

```matha
def readline(self) -> str
```

**描述**: 读取一行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\domains\real_hardware.py:140`

---

### `close`

```matha
def close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:146`

---

## `I2CDriver`

```matha
class I2CDriver
```

**描述**: I2C 总线驱动。

Source: `src\domains\real_hardware.py:156`

---

### `__init__`

```matha
def __init__(self, bus: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bus` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:159`

---

### `write_byte`

```matha
def write_byte(self, addr: int, value: int) -> None
```

**描述**: 写入单个字节。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:171`

---

### `write_bytes`

```matha
def write_bytes(self, addr: int, data: list[int]) -> None
```

**描述**: 写入多个字节。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |
| `data` | `list[int]` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:176`

---

### `read_byte`

```matha
def read_byte(self, addr: int) -> int
```

**描述**: 读取单个字节。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |

**返回**: `int`

Source: `src\domains\real_hardware.py:181`

---

### `read_bytes`

```matha
def read_bytes(self, addr: int, length: int) -> list[int]
```

**描述**: 读取多个字节。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |
| `length` | `int` | |

**返回**: `list[int]`

Source: `src\domains\real_hardware.py:187`

---

### `scan`

```matha
def scan(self) -> list[int]
```

**描述**: 扫描 I2C 总线上的设备地址。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[int]`

Source: `src\domains\real_hardware.py:193`

---

## `ADCDriver`

```matha
class ADCDriver
```

**描述**: ADC 模数转换驱动。

Source: `src\domains\real_hardware.py:205`

---

### `__init__`

```matha
def __init__(self, channel: int, ref_voltage: float, bits: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `channel` | `int` | |
| `ref_voltage` | `float` | |
| `bits` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:208`

---

### `read`

```matha
def read(self) -> int
```

**描述**: 读取 ADC 值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\domains\real_hardware.py:226`

---

### `read_voltage`

```matha
def read_voltage(self) -> float
```

**描述**: 读取 ADC 电压值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\domains\real_hardware.py:236`

---

## `TemperatureSensor`

```matha
class TemperatureSensor
```

**描述**: 温度传感器（DS18B20 / DHT11 / BME280）。

Source: `src\domains\real_hardware.py:247`

---

### `__init__`

```matha
def __init__(self, sensor_type: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sensor_type` | `str` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:250`

---

### `read`

```matha
def read(self) -> dict
```

**描述**: 读取温度（°C）和湿度（%）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\domains\real_hardware.py:261`

---

## `DistanceSensor`

```matha
class DistanceSensor
```

**描述**: 超声波测距传感器（HC-SR04）。

Source: `src\domains\real_hardware.py:276`

---

### `__init__`

```matha
def __init__(self, trigger_pin: int, echo_pin: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `trigger_pin` | `int` | |
| `echo_pin` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:279`

---

### `read`

```matha
def read(self) -> float
```

**描述**: 读取距离（米）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\domains\real_hardware.py:284`

---

## `StepperMotor`

```matha
class StepperMotor
```

**描述**: 步进电机控制（A4988 / DRV8825）。

Source: `src\domains\real_hardware.py:312`

---

### `__init__`

```matha
def __init__(self, step_pin: int, dir_pin: int, steps_per_rev: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `step_pin` | `int` | |
| `dir_pin` | `int` | |
| `steps_per_rev` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:315`

---

### `step`

```matha
def step(self, steps: int, speed: float) -> None
```

**描述**: 步进指定步数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `steps` | `int` | |
| `speed` | `float` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:322`

---

### `rotate_degrees`

```matha
def rotate_degrees(self, degrees: float, speed: float) -> None
```

**描述**: 旋转指定角度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `degrees` | `float` | |
| `speed` | `float` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:339`

---

## `ServoMotor`

```matha
class ServoMotor
```

**描述**: 舵机控制（SG90 / MG996R）。

Source: `src\domains\real_hardware.py:345`

---

### `__init__`

```matha
def __init__(self, pin: int, min_pulse: float, max_pulse: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `min_pulse` | `float` | |
| `max_pulse` | `float` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:348`

---

### `set_angle`

```matha
def set_angle(self, angle: float) -> None
```

**描述**: 设置舵机角度（0-180）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `angle` | `float` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:355`

---

## `OLEDDisplay`

```matha
class OLEDDisplay
```

**描述**: OLED 显示屏驱动（SSD1306）。

Source: `src\domains\real_hardware.py:376`

---

### `__init__`

```matha
def __init__(self, width: int, height: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `width` | `int` | |
| `height` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:379`

---

### `clear`

```matha
def clear(self) -> None
```

**描述**: 清屏。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:384`

---

### `text`

```matha
def text(self, x: int, y: int, string: str, size: int) -> None
```

**描述**: 显示文本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `int` | |
| `y` | `int` | |
| `string` | `str` | |
| `size` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:388`

---

### `pixel`

```matha
def pixel(self, x: int, y: int, on: bool) -> None
```

**描述**: 设置像素。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `int` | |
| `y` | `int` | |
| `on` | `bool` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:392`

---

## `HardwareDriverRegistry`

```matha
class HardwareDriverRegistry
```

**描述**: 硬件驱动注册表。

Source: `src\domains\real_hardware.py:401`

---

### `register`

```matha
def register(cls, name: str, driver_class: type) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `name` | `str` | |
| `driver_class` | `type` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\domains\real_hardware.py:407`

---

### `create`

```matha
def create(cls, name: str, **kwargs) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `name` | `str` | |

**返回**: `Any`

**装饰器**: classmethod

Source: `src\domains\real_hardware.py:411`

---

### `list_drivers`

```matha
def list_drivers(cls) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `list[str]`

**装饰器**: classmethod

Source: `src\domains\real_hardware.py:417`

---

## `gpio`

```matha
def gpio(pin: int, mode: str) -> None
```

**描述**: 便捷 GPIO 初始化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:437`

---

## `gpio_write`

```matha
def gpio_write(pin: int, value: int) -> None
```

**描述**: 便捷 GPIO 写入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:443`

---

## `gpio_read`

```matha
def gpio_read(pin: int) -> int
```

**描述**: 便捷 GPIO 读取。

| 参数 | 类型 | 说明 |
|------|------|------|
| `pin` | `int` | |

**返回**: `int`

Source: `src\domains\real_hardware.py:449`

---

## `temperature`

```matha
def temperature() -> dict
```

**描述**: 读取温度传感器。

**返回**: `dict`

Source: `src\domains\real_hardware.py:455`

---

## `distance`

```matha
def distance() -> float
```

**描述**: 读取距离传感器。

**返回**: `float`

Source: `src\domains\real_hardware.py:461`

---

## `motor_step`

```matha
def motor_step(steps: int, speed: float) -> None
```

**描述**: 步进电机控制。

| 参数 | 类型 | 说明 |
|------|------|------|
| `steps` | `int` | |
| `speed` | `float` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:467`

---

## `servo_angle`

```matha
def servo_angle(angle: float) -> None
```

**描述**: 舵机角度控制。

| 参数 | 类型 | 说明 |
|------|------|------|
| `angle` | `float` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:473`

---

## `_register_real_hardware`

```matha
def _register_real_hardware(builtins: dict) -> None
```

**描述**: 将真实硬件驱动注册到解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\real_hardware.py:479`

---

# `domains.registry`

```matha

```

**描述**: Matha 领域注册中心：统一管理所有学科/专业/应用领域的扩展模块。

支持动态注册、热加载、领域查询、领域级优化 Pass 集成。

架构：
  DomainRegistry          领域注册表
  ├── AI_DataScience      人工智能与数据科学
  ├── SoftwareAppDev      软件与应用程序开发
  ├── GameImmersion       游戏与沉浸式体验开发
  ├── Automation          自动化与效率提升
  ├── IoTHardware         物联网与硬件控制
  ├── OSNetwork           底层系统与网络安全
  ├── BlockchainWeb3      区块链与Web3开发
  ├── AudioVideo          音视频与多媒体处理
  ├── GraphicsRender      图形学与渲染技术
  ├── HPC                 科学计算与高性能计算
  ├── FinTech             金融科技与量化交易
  ├── AutonomousDriving   自动驾驶与具身智能
  ├── AerospaceDefense    航空航天与国防工业
  ├── BioComputing        生物计算与合成生物学
  ├── HardwareReverse     极客硬件与逆向工程
  ├── SpatialMeta         空间计算与元宇宙基础设施
  ├── AlgoTrading         算法交易与高频撮合引擎
  ├── CompChem            计算化学与材料科学
  ├── GreenTech           绿色科技
  ├── MetaverseArch       元宇宙与虚拟世界的建筑师
  ├── DigitalRights       数字版权与去中心化信任
  ├── CreativeCoding      创意编程与艺术表达
  ├── GeekGray            极客文化与灰产边缘
  ├── GeneticAlgorithm    遗传算法与AI编写代码
  ├── QuantumComputing    量子计算
  ├── ChaosFractal        混沌理论与分型
  ├── ComputationalLaw    计算法学
  └── CustomDomain        自定义领域（用户扩展）

使用示例：
  registry = DomainRegistry()
  registry.register_domain("AI", "ai_data_science")
  result = registry.run("math", target="c", domain="AI")

Source: `src\domains\registry.py:1`

---

## `DomainMeta`

```matha
class DomainMeta
```

**描述**: 领域元数据。

**装饰器**: dataclass

Source: `src\domains\registry.py:56`

---

### `load_module`

```matha
def load_module(self) -> Any
```

**描述**: 动态加载领域模块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\domains\registry.py:68`

---

### `get_builtins`

```matha
def get_builtins(self) -> dict
```

**描述**: 获取领域内置函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\domains\registry.py:72`

---

## `DomainRegistry`

```matha
class DomainRegistry
```

**描述**: 领域注册中心：统一管理所有学科/专业/应用领域。

Source: `src\domains\registry.py:85`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\domains\registry.py:646`

---

### `register`

```matha
def register(self, domain_key: str, meta: Optional[DomainMeta]) -> bool
```

**描述**: 注册领域。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `domain_key` | `str` | |
| `meta` | `Optional[DomainMeta]` | |

**返回**: `bool`

Source: `src\domains\registry.py:651`

---

### `unregister`

```matha
def unregister(self, domain_key: str) -> bool
```

**描述**: 注销领域。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `domain_key` | `str` | |

**返回**: `bool`

Source: `src\domains\registry.py:671`

---

### `get`

```matha
def get(self, domain_key: str) -> Optional[DomainMeta]
```

**描述**: 获取领域元数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `domain_key` | `str` | |

**返回**: `Optional[DomainMeta]`

Source: `src\domains\registry.py:679`

---

### `load`

```matha
def load(self, domain_key: str) -> Optional[Any]
```

**描述**: 加载领域模块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `domain_key` | `str` | |

**返回**: `Optional[Any]`

Source: `src\domains\registry.py:683`

---

### `get_builtins`

```matha
def get_builtins(self, domain_key: str) -> dict
```

**描述**: 获取领域内置函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `domain_key` | `str` | |

**返回**: `dict`

Source: `src\domains\registry.py:695`

---

### `list_domains`

```matha
def list_domains(self, category: Optional[str]) -> list[dict]
```

**描述**: 列出所有领域。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `category` | `Optional[str]` | |

**返回**: `list[dict]`

Source: `src\domains\registry.py:703`

---

### `add_callback`

```matha
def add_callback(self, event: str, callback: Callable) -> None
```

**描述**: 注册事件回调。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `event` | `str` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\domains\registry.py:719`

---

### `fire`

```matha
def fire(self, event: str, **kwargs) -> None
```

**描述**: 触发事件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `event` | `str` | |

**返回**: `None`

Source: `src\domains\registry.py:725`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取注册表统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\domains\registry.py:733`

---

## `get_registry`

```matha
def get_registry() -> DomainRegistry
```

**描述**: 获取全局领域注册表。

**返回**: `DomainRegistry`

Source: `src\domains\registry.py:753`

---

## `domain_compile`

```matha
def domain_compile(source: str, target: str, domain: Optional[str]) -> str
```

**描述**: 使用指定领域编译源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `target` | `str` | |
| `domain` | `Optional[str]` | |

**返回**: `str`

Source: `src\domains\registry.py:764`

---

# `domains.software_app`

```matha

```

**描述**: Matha 软件与应用程序开发领域模块（真实实现版）。

覆盖：
  1) HTTP 请求（本地模拟 + 真实URL校验）
  2) SQLite 数据库操作
  3) JWT 编解码（HMAC-SHA256）
  4) 密码哈希（bcrypt风格PBKDF2）
  5) 内存缓存（LRU）
  6) 任务队列

Source: `src\domains\software_app.py:1`

---

## `HTTPClient`

```matha
class HTTPClient
```

**描述**: 本地 HTTP 请求客户端。

Source: `src\domains\software_app.py:30`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\domains\software_app.py:33`

---

### `register_mock`

```matha
def register_mock(self, url: str, response: dict) -> None
```

**描述**: 注册模拟响应。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `url` | `str` | |
| `response` | `dict` | |

**返回**: `None`

Source: `src\domains\software_app.py:36`

---

### `request`

```matha
def request(self, method: str, url: str, **kwargs) -> dict
```

**描述**: 发送请求（模拟）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `method` | `str` | |
| `url` | `str` | |

**返回**: `dict`

Source: `src\domains\software_app.py:40`

---

## `http_get`

```matha
def http_get(url: str, headers: Optional[dict]) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `url` | `str` | |
| `headers` | `Optional[dict]` | |

**返回**: `dict`

Source: `src\domains\software_app.py:57`

---

## `http_post`

```matha
def http_post(url: str, body: dict, headers: Optional[dict]) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `url` | `str` | |
| `body` | `dict` | |
| `headers` | `Optional[dict]` | |

**返回**: `dict`

Source: `src\domains\software_app.py:61`

---

## `http_put`

```matha
def http_put(url: str, body: dict, headers: Optional[dict]) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `url` | `str` | |
| `body` | `dict` | |
| `headers` | `Optional[dict]` | |

**返回**: `dict`

Source: `src\domains\software_app.py:65`

---

## `http_delete`

```matha
def http_delete(url: str, headers: Optional[dict]) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `url` | `str` | |
| `headers` | `Optional[dict]` | |

**返回**: `dict`

Source: `src\domains\software_app.py:69`

---

## `SQLiteDatabase`

```matha
class SQLiteDatabase
```

**描述**: SQLite 数据库封装。

Source: `src\domains\software_app.py:77`

---

### `__init__`

```matha
def __init__(self, db_path: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `db_path` | `str` | |

Source: `src\domains\software_app.py:80`

---

### `execute`

```matha
def execute(self, sql: str, params: tuple) -> list[dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sql` | `str` | |
| `params` | `tuple` | |

**返回**: `list[dict]`

Source: `src\domains\software_app.py:84`

---

### `close`

```matha
def close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\domains\software_app.py:94`

---

## `_get_db`

```matha
def _get_db(name: str, path: str) -> SQLiteDatabase
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `path` | `str` | |

**返回**: `SQLiteDatabase`

Source: `src\domains\software_app.py:102`

---

## `db_query`

```matha
def db_query(table: str, conditions: Optional[dict], columns: Optional[list[str]], limit: int) -> list[dict]
```

**描述**: 查询数据库表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `table` | `str` | |
| `conditions` | `Optional[dict]` | |
| `columns` | `Optional[list[str]]` | |
| `limit` | `int` | |

**返回**: `list[dict]`

Source: `src\domains\software_app.py:108`

---

## `db_insert`

```matha
def db_insert(table: str, data: dict) -> int
```

**描述**: 插入数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `table` | `str` | |
| `data` | `dict` | |

**返回**: `int`

Source: `src\domains\software_app.py:122`

---

## `db_update`

```matha
def db_update(table: str, row_id: int, updates: dict) -> bool
```

**描述**: 更新数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `table` | `str` | |
| `row_id` | `int` | |
| `updates` | `dict` | |

**返回**: `bool`

Source: `src\domains\software_app.py:131`

---

## `db_delete`

```matha
def db_delete(table: str, row_id: int) -> bool
```

**描述**: 删除数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `table` | `str` | |
| `row_id` | `int` | |

**返回**: `bool`

Source: `src\domains\software_app.py:141`

---

## `_b64url_encode`

```matha
def _b64url_encode(data: bytes) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `bytes` | |

**返回**: `str`

Source: `src\domains\software_app.py:155`

---

## `_b64url_decode`

```matha
def _b64url_decode(s: str) -> bytes
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `bytes`

Source: `src\domains\software_app.py:159`

---

## `jwt_encode`

```matha
def jwt_encode(payload: dict, secret: str, exp_hours: float) -> str
```

**描述**: 编码 JWT。

| 参数 | 类型 | 说明 |
|------|------|------|
| `payload` | `dict` | |
| `secret` | `str` | |
| `exp_hours` | `float` | |

**返回**: `str`

Source: `src\domains\software_app.py:164`

---

## `jwt_decode`

```matha
def jwt_decode(token: str, secret: str) -> Optional[dict]
```

**描述**: 解码并验证 JWT。

| 参数 | 类型 | 说明 |
|------|------|------|
| `token` | `str` | |
| `secret` | `str` | |

**返回**: `Optional[dict]`

Source: `src\domains\software_app.py:175`

---

## `bcrypt_hash`

```matha
def bcrypt_hash(password: str, rounds: int) -> str
```

**描述**: PBKDF2 密码哈希。

| 参数 | 类型 | 说明 |
|------|------|------|
| `password` | `str` | |
| `rounds` | `int` | |

**返回**: `str`

Source: `src\domains\software_app.py:201`

---

## `bcrypt_verify`

```matha
def bcrypt_verify(password: str, hash_value: str) -> bool
```

**描述**: 验证密码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `password` | `str` | |
| `hash_value` | `str` | |

**返回**: `bool`

Source: `src\domains\software_app.py:208`

---

## `cache_get`

```matha
def cache_get(key: str) -> Optional[Any]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `key` | `str` | |

**返回**: `Optional[Any]`

Source: `src\domains\software_app.py:228`

---

## `cache_set`

```matha
def cache_set(key: str, value: Any, ttl: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `key` | `str` | |
| `value` | `Any` | |
| `ttl` | `float` | |

**返回**: `None`

Source: `src\domains\software_app.py:236`

---

## `cache_invalidate`

```matha
def cache_invalidate(key: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `key` | `str` | |

**返回**: `bool`

Source: `src\domains\software_app.py:241`

---

## `cache_size`

```matha
def cache_size() -> int
```

**返回**: `int`

Source: `src\domains\software_app.py:249`

---

## `queue_enqueue`

```matha
def queue_enqueue(item: Any) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `item` | `Any` | |

**返回**: `int`

Source: `src\domains\software_app.py:262`

---

## `queue_dequeue`

```matha
def queue_dequeue() -> Optional[Any]
```

**返回**: `Optional[Any]`

Source: `src\domains\software_app.py:268`

---

## `queue_size`

```matha
def queue_size() -> int
```

**返回**: `int`

Source: `src\domains\software_app.py:275`

---

## `_register_software_app`

```matha
def _register_software_app(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\software_app.py:284`

---

## `_software_app_symtab_names`

```matha
def _software_app_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\software_app.py:306`

---

# `domains.spatial_meta`

```matha

```

**描述**: Matha 空间元数据领域模块：空间索引、元数据管理、地理编码、坐标变换。

覆盖：
  1) 空间索引效率
  2) 元数据查询延迟
  3) 地理编码精度
  4) 坐标变换误差
  5) 边界框交集
  6) 缓冲区分析

Source: `src\domains\spatial_meta.py:1`

---

## `_curry1`

```matha
def _curry1(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\spatial_meta.py:17`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\spatial_meta.py:21`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\spatial_meta.py:25`

---

## `_空间索引效率`

```matha
def _空间索引效率(要素数, 索引深度)
```

**描述**: 空间索引查询效率估算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `要素数` | `` | |
| `索引深度` | `` | |

Source: `src\domains\spatial_meta.py:32`

---

## `_元数据查询延迟`

```matha
def _元数据查询延迟(元数据量_MB, 索引大小_MB)
```

**描述**: 元数据查询延迟（ms）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `元数据量_MB` | `` | |
| `索引大小_MB` | `` | |

Source: `src\domains\spatial_meta.py:39`

---

## `_地理编码精度`

```matha
def _地理编码精度(坐标系统, 分辨率_m)
```

**描述**: 地理编码精度（米）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `坐标系统` | `` | |
| `分辨率_m` | `` | |

Source: `src\domains\spatial_meta.py:46`

---

## `_坐标变换误差`

```matha
def _坐标变换误差(源精度_m, 目标精度_m, 变换次数)
```

**描述**: 累积坐标变换误差。

| 参数 | 类型 | 说明 |
|------|------|------|
| `源精度_m` | `` | |
| `目标精度_m` | `` | |
| `变换次数` | `` | |

Source: `src\domains\spatial_meta.py:51`

---

## `_边界框交集`

```matha
def _边界框交集(bbox1_xmin, bbox1_ymin, bbox1_xmax, bbox1_ymax, bbox2_xmin, bbox2_ymin, bbox2_xmax, bbox2_ymax)
```

**描述**: 两个边界框是否相交。

| 参数 | 类型 | 说明 |
|------|------|------|
| `bbox1_xmin` | `` | |
| `bbox1_ymin` | `` | |
| `bbox1_xmax` | `` | |
| `bbox1_ymax` | `` | |
| `bbox2_xmin` | `` | |
| `bbox2_ymin` | `` | |
| `bbox2_xmax` | `` | |
| `bbox2_ymax` | `` | |

Source: `src\domains\spatial_meta.py:58`

---

## `_缓冲区分析`

```matha
def _缓冲区分析(中心_x, 中心_y, 半径_m, 目标点_x, 目标点_y)
```

**描述**: 目标点是否在缓冲区内。

| 参数 | 类型 | 说明 |
|------|------|------|
| `中心_x` | `` | |
| `中心_y` | `` | |
| `半径_m` | `` | |
| `目标点_x` | `` | |
| `目标点_y` | `` | |

Source: `src\domains\spatial_meta.py:65`

---

## `_curry8`

```matha
def _curry8(func)
```

**描述**: 八参数柯里化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\spatial_meta.py:76`

---

## `_register_spatial_meta`

```matha
def _register_spatial_meta(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\spatial_meta.py:95`

---

## `_spatial_meta_symtab_names`

```matha
def _spatial_meta_symtab_names() -> list[str]
```

**返回**: `list[str]`

Source: `src\domains\spatial_meta.py:104`

---

# `domains.statmech`

```matha

```

**描述**: Matha 机械领域模块：统计力学（Statistical Mechanics）。

基于热力学 + 量子力学 + 核物理 + mathlib 数学地基，演化统计力学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、麦克斯韦-玻尔兹曼分布（Maxwell-Boltzmann Distribution）
  1) 最概然速率：vp = √(2kT/m)
  2) 平均速率：v̄ = √(8kT/(πm))
  3) 方均根速率：vrms = √(3kT/m)
  4) 速率分布函数：f(v) = 4π(m/(2πkT))^(3/2)·v²·exp(-mv²/(2kT))
  5) 理想气体压强：P = nkT
  6) 理想气体状态方程：PV = NkT
  7) 分子碰撞频率
  8) 平均自由程

二、配分函数（Partition Functions）
  1) 玻尔兹曼因子：exp(-E/(kT))
  2) 单粒子配分函数：Z = Σexp(-εi/(kT))
  3) 简并能级配分函数：Z = Σgi·exp(-εi/(kT))
  4) 谐振子配分函数：Z = 1/(2sinh(ℏω/(2kT)))
  5) 配分函数→内能：U = kT²·∂lnZ/∂T
  6) 配分函数→熵：S = k(lnZ + T·∂lnZ/∂T)

三、熵与自由能（Entropy & Free Energy）
  1) 玻尔兹曼熵：S = k·lnW
  2) 配分函数→自由能：F = -kT·lnZ
  3) 吉布斯自由能：G = H - TS
  4) 焓：H = U + PV
  5) 热容（等容）：Cv = (∂U/∂T)v
  6) 热容（等压）：Cp = Cv + Nk
  7) 理想气体熵变：ΔS = Nk·ln(V2/V1) + Nk·ln(T2/T1)

四、量子统计（Quantum Statistics）
  1) 费米-狄拉克分布：f(E) = 1/(exp((E-μ)/(kT))+1)
  2) 玻色-爱因斯坦分布：f(E) = 1/(exp((E-μ)/(kT))-1)
  3) 费米能级（T=0）：EF = (ℏ²/2m)(3π²n)^(2/3)
  4) 德拜温度：θD = ℏωD/k
  5) 德拜热容（低温）：Cv ≈ (12π⁴/5)·Nk·(T/θD)³
  6) 维恩位移常数

五、涨落与关联（Fluctuations & Correlations）
  1) 能量涨落：⟨(ΔE)²⟩ = kT²·Cv
  2) 粒子数涨落：⟨(ΔN)²⟩ = kT·(∂N/∂μ)
  3) 相对涨落：√(⟨(ΔE)²⟩)/⟨E⟩
  4) 密度涨落
  5) 布朗运动（均方位移）：⟨x²⟩ = 2Dt
  6) 爱因斯坦关系：D = kT/(6πηr)

设计原则：
  - 所有温度输入均为开尔文 K
  - 多参函数一律 _curry2/_curry3/_curry4 封装
  - 前缀 速率_ / 配分_ / 熵_ / 统计_ / 涨落_ 区分子领域
  - 统计力学常量作为常量注册

Source: `src\domains\statmech.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\statmech.py:66`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\statmech.py:72`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\statmech.py:80`

---

## `_速率_最概然速率`

```matha
def _速率_最概然速率(m, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:119`

---

## `_速率_平均速率`

```matha
def _速率_平均速率(m, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:121`

---

## `_速率_方均根速率`

```matha
def _速率_方均根速率(m, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:123`

---

## `_速率_分布函数`

```matha
def _速率_分布函数(m, T, v)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `T` | `` | |
| `v` | `` | |

Source: `src\domains\statmech.py:125`

---

## `_速率_理想气体压强`

```matha
def _速率_理想气体压强(n, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:129`

---

## `_速率_理想气体压力`

```matha
def _速率_理想气体压力(N, T, V)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `T` | `` | |
| `V` | `` | |

Source: `src\domains\statmech.py:131`

---

## `_速率_平均自由程`

```matha
def _速率_平均自由程(n, sigma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `sigma` | `` | |

Source: `src\domains\statmech.py:133`

---

## `_速率_碰撞频率`

```matha
def _速率_碰撞频率(n, sigma, v_bar)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `sigma` | `` | |
| `v_bar` | `` | |

Source: `src\domains\statmech.py:135`

---

## `_配分_玻尔兹曼因子`

```matha
def _配分_玻尔兹曼因子(E, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:143`

---

## `_配分_离散配分函数`

```matha
def _配分_离散配分函数(energies, T)
```

**描述**: energies: 能级列表 [ε1, ε2, ...]

| 参数 | 类型 | 说明 |
|------|------|------|
| `energies` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:145`

---

## `_配分_简并配分函数`

```matha
def _配分_简并配分函数(levels, T)
```

**描述**: levels: [(ε1, g1), (ε2, g2), ...]

| 参数 | 类型 | 说明 |
|------|------|------|
| `levels` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:150`

---

## `_配分_谐振子配分函数`

```matha
def _配分_谐振子配分函数(omega, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `omega` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:155`

---

## `_配分_转动配分函数`

```matha
def _配分_转动配分函数(I, T, sigma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `T` | `` | |
| `sigma` | `` | |

Source: `src\domains\statmech.py:159`

---

## `_配分_内能`

```matha
def _配分_内能(Z_func, T)
```

**描述**: Z_func: 温度的函数（Python callable），返回 Z。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Z_func` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:163`

---

## `_配分_熵`

```matha
def _配分_熵(Z_func, T)
```

**描述**: Z_func: 温度的函数（Python callable），返回 Z。

| 参数 | 类型 | 说明 |
|------|------|------|
| `Z_func` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:171`

---

## `_配分_自由能`

```matha
def _配分_自由能(Z, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Z` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:180`

---

## `_配分_自由能压力`

```matha
def _配分_自由能压力(F_func, V)
```

**描述**: F_func: 体积的函数（Python callable），返回 F。

| 参数 | 类型 | 说明 |
|------|------|------|
| `F_func` | `` | |
| `V` | `` | |

Source: `src\domains\statmech.py:182`

---

## `_熵_玻尔兹曼熵`

```matha
def _熵_玻尔兹曼熵(W)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `W` | `` | |

Source: `src\domains\statmech.py:193`

---

## `_熵_玻尔兹曼熵2`

```matha
def _熵_玻尔兹曼熵2(W)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `W` | `` | |

Source: `src\domains\statmech.py:195`

---

## `_熵_吉布斯熵`

```matha
def _熵_吉布斯熵(probs)
```

**描述**: probs: 概率列表 [p1, p2, ...]

| 参数 | 类型 | 说明 |
|------|------|------|
| `probs` | `` | |

Source: `src\domains\statmech.py:197`

---

## `_熵_萨克尔泰特罗德`

```matha
def _熵_萨克尔泰特罗德(N, V, m, U)
```

**描述**: 单原子理想气体熵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `V` | `` | |
| `m` | `` | |
| `U` | `` | |

Source: `src\domains\statmech.py:201`

---

## `_熵_等温熵变`

```matha
def _熵_等温熵变(N, V1, V2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `V1` | `` | |
| `V2` | `` | |

Source: `src\domains\statmech.py:206`

---

## `_熵_理想气体熵变`

```matha
def _熵_理想气体熵变(N, V1, V2, T1, T2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `V1` | `` | |
| `V2` | `` | |
| `T1` | `` | |
| `T2` | `` | |

Source: `src\domains\statmech.py:208`

---

## `_熵_焓`

```matha
def _熵_焓(U, P, V)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `U` | `` | |
| `P` | `` | |
| `V` | `` | |

Source: `src\domains\statmech.py:211`

---

## `_熵_吉布斯自由能`

```matha
def _熵_吉布斯自由能(H, T, S)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `H` | `` | |
| `T` | `` | |
| `S` | `` | |

Source: `src\domains\statmech.py:213`

---

## `_熵_等容热容`

```matha
def _熵_等容热容(N)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |

Source: `src\domains\statmech.py:215`

---

## `_熵_等压热容`

```matha
def _熵_等压热容(N)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |

Source: `src\domains\statmech.py:217`

---

## `_熵_热容比`

```matha
def _熵_热容比(Cp, Cv)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Cp` | `` | |
| `Cv` | `` | |

Source: `src\domains\statmech.py:219`

---

## `_统计_费米狄拉克`

```matha
def _统计_费米狄拉克(E, mu, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `mu` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:227`

---

## `_统计_玻色爱因斯坦`

```matha
def _统计_玻色爱因斯坦(E, mu, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `mu` | `` | |
| `T` | `` | |

Source: `src\domains\statmech.py:235`

---

## `_统计_费米能级`

```matha
def _统计_费米能级(m, n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `n` | `` | |

Source: `src\domains\statmech.py:243`

---

## `_统计_费米温度`

```matha
def _统计_费米温度(EF)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `EF` | `` | |

Source: `src\domains\statmech.py:246`

---

## `_统计_费米速度`

```matha
def _统计_费米速度(EF, m)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `EF` | `` | |
| `m` | `` | |

Source: `src\domains\statmech.py:248`

---

## `_统计_德拜频率`

```matha
def _统计_德拜频率(v_s, n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v_s` | `` | |
| `n` | `` | |

Source: `src\domains\statmech.py:250`

---

## `_统计_德拜温度`

```matha
def _统计_德拜温度(omega_D)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `omega_D` | `` | |

Source: `src\domains\statmech.py:252`

---

## `_统计_德拜热容低温`

```matha
def _统计_德拜热容低温(N, T, theta_D)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `T` | `` | |
| `theta_D` | `` | |

Source: `src\domains\statmech.py:254`

---

## `_统计_维恩位移`

```matha
def _统计_维恩位移(T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |

Source: `src\domains\statmech.py:257`

---

## `_统计_黑体辐射功率`

```matha
def _统计_黑体辐射功率(T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |

Source: `src\domains\statmech.py:259`

---

## `_统计_光子气体内能密度`

```matha
def _统计_光子气体内能密度(T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |

Source: `src\domains\statmech.py:261`

---

## `_涨落_能量涨落`

```matha
def _涨落_能量涨落(T, Cv)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `Cv` | `` | |

Source: `src\domains\statmech.py:272`

---

## `_涨落_粒子数涨落`

```matha
def _涨落_粒子数涨落(T, dN_dmu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `dN_dmu` | `` | |

Source: `src\domains\statmech.py:274`

---

## `_涨落_相对能量涨落`

```matha
def _涨落_相对能量涨落(T, Cv, E_avg)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `Cv` | `` | |
| `E_avg` | `` | |

Source: `src\domains\statmech.py:276`

---

## `_涨落_布朗位移`

```matha
def _涨落_布朗位移(D, t)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `D` | `` | |
| `t` | `` | |

Source: `src\domains\statmech.py:280`

---

## `_涨落_爱因斯坦扩散系数`

```matha
def _涨落_爱因斯坦扩散系数(T, eta, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `eta` | `` | |
| `r` | `` | |

Source: `src\domains\statmech.py:282`

---

## `_涨落_密度涨落`

```matha
def _涨落_密度涨落(T, kappa_T, V)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `kappa_T` | `` | |
| `V` | `` | |

Source: `src\domains\statmech.py:284`

---

## `_涨落_体积涨落`

```matha
def _涨落_体积涨落(T, V, kappa_T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `V` | `` | |
| `kappa_T` | `` | |

Source: `src\domains\statmech.py:286`

---

## `_涨落_温度涨落`

```matha
def _涨落_温度涨落(T, Cv)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `Cv` | `` | |

Source: `src\domains\statmech.py:288`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\statmech.py:295`

---

## `_register_statmech`

```matha
def _register_statmech(builtins: dict) -> None
```

**描述**: 将统计力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（nuclear 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\statmech.py:307`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\statmech.py:373`

---

## `_statmech_symtab_names`

```matha
def _statmech_symtab_names() -> list[str]
```

**描述**: 返回统计力学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\statmech.py:386`

---

# `domains.structural`

```matha

```

**描述**: Matha 机械领域模块：结构力学（Structural Mechanics）。

基于材料力学 + 动力学 + mathlib 数学地基，演化结构力学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、应力状态分析（Stress State Analysis）
  1) 莫尔圆主应力：σ₁,₂ = (σx+σy)/2 ± √[((σx-σy)/2)² + τxy²]
  2) 莫尔圆最大剪应力：τmax = (σ₁-σ₂)/2
  3) 任意截面应力：σn = (σx+σy)/2 + (σx-σy)/2·cos2θ + τxy·sin2θ
  4) 主应力方向：tan2θp = 2τxy/(σx-σy)
  5) 广义胡克定律（三向）：εx = (σx-ν(σy+σz))/E
  6) 第四强度理论（von Mises）：σeq = √(σ₁²+σ₂²+σ₃²-σ₁σ₂-σ₂σ₃-σ₃σ₁)
  7) 第三强度理论（Tresca）：σeq = σ₁-σ₃
  8) 体积应变：θ = εx+εy+εz = (1-2ν)(σx+σy+σz)/E

二、梁的弯曲（Beam Bending）
  1) 简支梁均布载荷：Mmax = qL²/8, δmax = 5qL⁴/(384EI)
  2) 简支梁跨中集中力：Mmax = PL/4, δmax = PL³/(48EI)
  3) 悬臂梁均布载荷：Mmax = qL²/2, δmax = qL⁴/(8EI)
  4) 悬臂梁端部集中力：Mmax = PL, δmax = PL³/(3EI)
  5) 梁端转角
  6) 弯曲正应力：σ = M·y/I

三、压杆稳定（Column Buckling）
  1) 欧拉临界力：Pcr = π²EI/(μL)²
  2) 欧拉临界应力：σcr = π²E/λ²
  3) 长细比：λ = μL/r
  4) 回转半径：r = √(I/A)
  5) 临界长细比：λp = π√(E/σp)
  6) 安全工作压力：P_allow = Pcr/n

四、桁架与结构（Truss & Structures）
  1) 桁架杆件内力（给定角度）
  2) 简支梁支座反力（均布载荷）：RA = RB = qL/2
  3) 简支梁支座反力（跨中集中力）：RA = RB = P/2
  4) 悬臂梁固定端反力
  5) 超静定次数：n = m + r - 2j（m 杆数, r 支座反力数, j 节点数）

五、应变能与冲击（Strain Energy & Impact）
  1) 轴向应变能：U = N²L/(2EA)
  2) 弯曲应变能：U = M²L/(2EI)
  3) 剪切应变能：U = V²L/(2GA)
  4) 冲击动荷系数：Kd = 1+√(1+2h/Δst)
  5) 卡氏定理位移：δ = ∂U/∂P（简化形式）
  6) 应变能密度：u = σ²/(2E)

设计原则：
  - 所有角度输入/输出均为弧度
  - 多参函数一律 _curry2/_curry3/_curry4/_curry5 封装
  - 前缀 应力_ / 梁_ / 压杆_ / 桁架_ / 能量_ 区分子领域
  - 截面几何性质在 mechanics.py 已有，此处引用

Source: `src\domains\structural.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\structural.py:64`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\structural.py:70`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\structural.py:78`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\structural.py:88`

---

## `_应力_主应力1`

```matha
def _应力_主应力1(sigma_x, sigma_y, tau_xy)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `tau_xy` | `` | |

Source: `src\domains\structural.py:105`

---

## `_应力_主应力2`

```matha
def _应力_主应力2(sigma_x, sigma_y, tau_xy)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `tau_xy` | `` | |

Source: `src\domains\structural.py:111`

---

## `_应力_最大剪应力`

```matha
def _应力_最大剪应力(sigma_x, sigma_y, tau_xy)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `tau_xy` | `` | |

Source: `src\domains\structural.py:117`

---

## `_应力_主应力方向`

```matha
def _应力_主应力方向(sigma_x, sigma_y, tau_xy)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `tau_xy` | `` | |

Source: `src\domains\structural.py:122`

---

## `_应力_截面正应力`

```matha
def _应力_截面正应力(sigma_x, sigma_y, tau_xy, theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `tau_xy` | `` | |
| `theta` | `` | |

Source: `src\domains\structural.py:128`

---

## `_应力_截面剪应力`

```matha
def _应力_截面剪应力(sigma_x, sigma_y, tau_xy, theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `tau_xy` | `` | |
| `theta` | `` | |

Source: `src\domains\structural.py:133`

---

## `_应力_广义胡克应变`

```matha
def _应力_广义胡克应变(sigma_x, sigma_y, sigma_z, E, nu, axis)
```

**描述**: axis: 'x'=0, 'y'=1, 'z'=2

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `sigma_z` | `` | |
| `E` | `` | |
| `nu` | `` | |
| `axis` | `` | |

Source: `src\domains\structural.py:137`

---

## `_应力_vonMises`

```matha
def _应力_vonMises(s1, s2, s3)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `s1` | `` | |
| `s2` | `` | |
| `s3` | `` | |

Source: `src\domains\structural.py:144`

---

## `_应力_Tresca`

```matha
def _应力_Tresca(s1, s2, s3)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `s1` | `` | |
| `s2` | `` | |
| `s3` | `` | |

Source: `src\domains\structural.py:148`

---

## `_应力_体积应变`

```matha
def _应力_体积应变(sigma_x, sigma_y, sigma_z, E, nu)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma_x` | `` | |
| `sigma_y` | `` | |
| `sigma_z` | `` | |
| `E` | `` | |
| `nu` | `` | |

Source: `src\domains\structural.py:152`

---

## `_梁_简支均布弯矩`

```matha
def _梁_简支均布弯矩(q, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `L` | `` | |

Source: `src\domains\structural.py:162`

---

## `_梁_简支均布挠度`

```matha
def _梁_简支均布挠度(q, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:164`

---

## `_梁_简支均布转角`

```matha
def _梁_简支均布转角(q, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:166`

---

## `_梁_简支集中弯矩`

```matha
def _梁_简支集中弯矩(P, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `L` | `` | |

Source: `src\domains\structural.py:168`

---

## `_梁_简支集中挠度`

```matha
def _梁_简支集中挠度(P, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:170`

---

## `_梁_简支集中转角`

```matha
def _梁_简支集中转角(P, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:172`

---

## `_梁_悬臂均布弯矩`

```matha
def _梁_悬臂均布弯矩(q, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `L` | `` | |

Source: `src\domains\structural.py:176`

---

## `_梁_悬臂均布挠度`

```matha
def _梁_悬臂均布挠度(q, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:178`

---

## `_梁_悬臂集中弯矩`

```matha
def _梁_悬臂集中弯矩(P, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `L` | `` | |

Source: `src\domains\structural.py:180`

---

## `_梁_悬臂集中挠度`

```matha
def _梁_悬臂集中挠度(P, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:182`

---

## `_梁_悬臂集中转角`

```matha
def _梁_悬臂集中转角(P, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:184`

---

## `_梁_弯曲正应力`

```matha
def _梁_弯曲正应力(M, y, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `y` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:187`

---

## `_梁_矩形剪应力`

```matha
def _梁_矩形剪应力(V, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `A` | `` | |

Source: `src\domains\structural.py:189`

---

## `_压杆_欧拉临界力`

```matha
def _压杆_欧拉临界力(E, I, mu, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `I` | `` | |
| `mu` | `` | |
| `L` | `` | |

Source: `src\domains\structural.py:197`

---

## `_压杆_欧拉临界应力`

```matha
def _压杆_欧拉临界应力(E, lam)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `lam` | `` | |

Source: `src\domains\structural.py:199`

---

## `_压杆_长细比`

```matha
def _压杆_长细比(mu, L, r)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |
| `L` | `` | |
| `r` | `` | |

Source: `src\domains\structural.py:201`

---

## `_压杆_回转半径`

```matha
def _压杆_回转半径(I, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `I` | `` | |
| `A` | `` | |

Source: `src\domains\structural.py:203`

---

## `_压杆_临界长细比`

```matha
def _压杆_临界长细比(E, sigma_p)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `E` | `` | |
| `sigma_p` | `` | |

Source: `src\domains\structural.py:205`

---

## `_压杆_安全压力`

```matha
def _压杆_安全压力(P_cr, n)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P_cr` | `` | |
| `n` | `` | |

Source: `src\domains\structural.py:207`

---

## `_压杆_长细比几何`

```matha
def _压杆_长细比几何(mu, L, I, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mu` | `` | |
| `L` | `` | |
| `I` | `` | |
| `A` | `` | |

Source: `src\domains\structural.py:209`

---

## `_桁架_简支均布反力`

```matha
def _桁架_简支均布反力(q, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `L` | `` | |

Source: `src\domains\structural.py:219`

---

## `_桁架_简支集中反力`

```matha
def _桁架_简支集中反力(P)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |

Source: `src\domains\structural.py:221`

---

## `_桁架_悬臂均布反力`

```matha
def _桁架_悬臂均布反力(q, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `q` | `` | |
| `L` | `` | |

Source: `src\domains\structural.py:223`

---

## `_桁架_悬臂集中反力`

```matha
def _桁架_悬臂集中反力(P)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `P` | `` | |

Source: `src\domains\structural.py:225`

---

## `_桁架_杆件内力`

```matha
def _桁架_杆件内力(F, theta)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `F` | `` | |
| `theta` | `` | |

Source: `src\domains\structural.py:227`

---

## `_桁架_超静定次数`

```matha
def _桁架_超静定次数(m, r, j)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `r` | `` | |
| `j` | `` | |

Source: `src\domains\structural.py:229`

---

## `_桁架_合力`

```matha
def _桁架_合力(Fx_list, Fy_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Fx_list` | `` | |
| `Fy_list` | `` | |

Source: `src\domains\structural.py:231`

---

## `_能量_轴向应变能`

```matha
def _能量_轴向应变能(N, L, E, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `N` | `` | |
| `L` | `` | |
| `E` | `` | |
| `A` | `` | |

Source: `src\domains\structural.py:242`

---

## `_能量_弯曲应变能`

```matha
def _能量_弯曲应变能(M, L, E, I)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `M` | `` | |
| `L` | `` | |
| `E` | `` | |
| `I` | `` | |

Source: `src\domains\structural.py:244`

---

## `_能量_剪切应变能`

```matha
def _能量_剪切应变能(V, L, G, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V` | `` | |
| `L` | `` | |
| `G` | `` | |
| `A` | `` | |

Source: `src\domains\structural.py:246`

---

## `_能量_动荷系数`

```matha
def _能量_动荷系数(h, delta_st)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `h` | `` | |
| `delta_st` | `` | |

Source: `src\domains\structural.py:248`

---

## `_能量_应变能密度`

```matha
def _能量_应变能密度(sigma, E)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `sigma` | `` | |
| `E` | `` | |

Source: `src\domains\structural.py:250`

---

## `_能量_扭转应变能`

```matha
def _能量_扭转应变能(T, L, G, Ip)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T` | `` | |
| `L` | `` | |
| `G` | `` | |
| `Ip` | `` | |

Source: `src\domains\structural.py:252`

---

## `_能量_总应变能`

```matha
def _能量_总应变能(U_axial, U_bending, U_shear)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `U_axial` | `` | |
| `U_bending` | `` | |
| `U_shear` | `` | |

Source: `src\domains\structural.py:254`

---

## `_register_structural`

```matha
def _register_structural(builtins: dict) -> None
```

**描述**: 将结构力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（optics 之后）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\structural.py:262`

---

## `_structural_symtab_names`

```matha
def _structural_symtab_names() -> list[str]
```

**描述**: 返回结构力学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\structural.py:325`

---

# `domains.thermo`

```matha

```

**描述**: Matha 机械领域模块：热力学（Thermodynamics）。

基于流体力学 + 动力学 + mathlib 数学地基，演化热力学功能。
所有函数以普通 Python callable 注册到解释器 builtins，Matha 代码可直接调用。

五大子领域：

一、气体状态方程（Gas Laws）
  1) 理想气体状态方程：pV = nRT = (m/M)RT
  2) 玻意耳定律（等温）：p1V1 = p2V2
  3) 查理定律（等压）：V1/T1 = V2/T2
  4) 盖-吕萨克定律（等容）：p1/T1 = p2/T2
  5) 混合气体分压（道尔顿）：p_total = Σp_i
  6) 摩尔数 / 密度由状态方程反算

二、热力学过程（Thermodynamic Processes）
  1) 等温过程做功：W = nRT ln(V2/V1)
  2) 等压过程做功：W = pΔV
  3) 等容过程：W = 0
  4) 绝热过程：pV^γ = const；绝热做功 W = (p1V1 - p2V2)/(γ-1)
  5) 多方过程：pV^n = const
  6) 热力学第一定律：ΔU = Q - W（或 Q = ΔU + W）

三、热传递（Heat Transfer）
  1) 热传导（傅里叶定律）：Q/t = kA(T2-T1)/d
  2) 热对流（牛顿冷却定律）：Q/t = hA(T_s - T_f)
  3) 热辐射（斯特藩-玻尔兹曼）：Q/t = σεA(T⁴)
  4) 热阻：R = d/(kA)
  5) 传热速率（串联热阻）

四、热机与效率（Heat Engines）
  1) 卡诺效率：η = 1 - T_c/T_h
  2) 奥托循环效率：η = 1 - 1/r^(γ-1)（r 为压缩比）
  3) 热机效率：η = W/Q_h = (Q_h - Q_c)/Q_h
  4) 制冷系数：COP = Q_c/W = T_c/(T_h - T_c)
  5) 热泵系数：COP_hp = Q_h/W = T_h/(T_h - T_c)

五、相变与热物性（Phase Change & Properties）
  1) 显热：Q = mcΔT
  2) 潜热：Q = mL（L 为熔化热/汽化热）
  3) 线膨胀：ΔL = αL₀ΔT
  4) 体膨胀：ΔV = βV₀ΔT
  5) 热容：C = mc（热容）；比热容比值 γ = c_p/c_v
  6) 理想气体内能：U = nC_v T = (f/2)nRT（f 为自由度）

设计原则：
  - 所有温度输入/输出均为开尔文 K（SI 标准）
  - 摄氏↔开尔文转换函数提供
  - 多参函数一律 _curry2/_curry3/_curry4/_curry5 封装
  - 前缀 热_ / 过程_ / 传热_ / 热机_ / 相变_ 区分子领域
  - 常用气体比热/热导率作为常量注册

Source: `src\domains\thermo.py:1`

---

## `_curry2`

```matha
def _curry2(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\thermo.py:62`

---

## `_curry3`

```matha
def _curry3(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\thermo.py:68`

---

## `_curry4`

```matha
def _curry4(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\thermo.py:76`

---

## `_curry5`

```matha
def _curry5(func)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\domains\thermo.py:86`

---

## `_热_c2k`

```matha
def _热_c2k(T_C)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_C` | `` | |

Source: `src\domains\thermo.py:115`

---

## `_热_k2c`

```matha
def _热_k2c(T_K)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_K` | `` | |

Source: `src\domains\thermo.py:117`

---

## `_热_气压`

```matha
def _热_气压(n, T, V)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `T` | `` | |
| `V` | `` | |

Source: `src\domains\thermo.py:120`

---

## `_热_气体体积`

```matha
def _热_气体体积(n, T, p)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `T` | `` | |
| `p` | `` | |

Source: `src\domains\thermo.py:122`

---

## `_热_摩尔数`

```matha
def _热_摩尔数(p, V, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `V` | `` | |
| `T` | `` | |

Source: `src\domains\thermo.py:124`

---

## `_热_质量转摩尔`

```matha
def _热_质量转摩尔(m, M)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `M` | `` | |

Source: `src\domains\thermo.py:126`

---

## `_热_玻意耳压强`

```matha
def _热_玻意耳压强(p1, V1, V2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `V1` | `` | |
| `V2` | `` | |

Source: `src\domains\thermo.py:129`

---

## `_热_查理体积`

```matha
def _热_查理体积(V1, T1, T2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `V1` | `` | |
| `T1` | `` | |
| `T2` | `` | |

Source: `src\domains\thermo.py:131`

---

## `_热_盖吕萨克压强`

```matha
def _热_盖吕萨克压强(p1, T1, T2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `T1` | `` | |
| `T2` | `` | |

Source: `src\domains\thermo.py:133`

---

## `_热_道尔顿分压`

```matha
def _热_道尔顿分压(p_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p_list` | `` | |

Source: `src\domains\thermo.py:136`

---

## `_过程_等温功`

```matha
def _过程_等温功(n, T, V1, V2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `T` | `` | |
| `V1` | `` | |
| `V2` | `` | |

Source: `src\domains\thermo.py:144`

---

## `_过程_等压功`

```matha
def _过程_等压功(p, V1, V2)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `` | |
| `V1` | `` | |
| `V2` | `` | |

Source: `src\domains\thermo.py:146`

---

## `_过程_等容功`

```matha
def _过程_等容功(*_)
```

Source: `src\domains\thermo.py:148`

---

## `_过程_绝热功`

```matha
def _过程_绝热功(p1, V1, p2, V2, gamma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `V1` | `` | |
| `p2` | `` | |
| `V2` | `` | |
| `gamma` | `` | |

Source: `src\domains\thermo.py:150`

---

## `_过程_绝热压强`

```matha
def _过程_绝热压强(p1, V1, V2, gamma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `` | |
| `V1` | `` | |
| `V2` | `` | |
| `gamma` | `` | |

Source: `src\domains\thermo.py:152`

---

## `_过程_绝热温度`

```matha
def _过程_绝热温度(T1, V1, V2, gamma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T1` | `` | |
| `V1` | `` | |
| `V2` | `` | |
| `gamma` | `` | |

Source: `src\domains\thermo.py:154`

---

## `_过程_第一定律`

```matha
def _过程_第一定律(Q, W)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `` | |
| `W` | `` | |

Source: `src\domains\thermo.py:157`

---

## `_过程_内能变化`

```matha
def _过程_内能变化(n, Cv, dT)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |
| `Cv` | `` | |
| `dT` | `` | |

Source: `src\domains\thermo.py:159`

---

## `_传热_热传导`

```matha
def _传热_热传导(k, A, T2, T1, d)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `k` | `` | |
| `A` | `` | |
| `T2` | `` | |
| `T1` | `` | |
| `d` | `` | |

Source: `src\domains\thermo.py:167`

---

## `_传热_热对流`

```matha
def _传热_热对流(h, A, T_s, T_f)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `h` | `` | |
| `A` | `` | |
| `T_s` | `` | |
| `T_f` | `` | |

Source: `src\domains\thermo.py:169`

---

## `_传热_热辐射`

```matha
def _传热_热辐射(epsilon, A, T_hot, T_cold)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `epsilon` | `` | |
| `A` | `` | |
| `T_hot` | `` | |
| `T_cold` | `` | |

Source: `src\domains\thermo.py:172`

---

## `_传热_热阻`

```matha
def _传热_热阻(d, k, A)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `d` | `` | |
| `k` | `` | |
| `A` | `` | |

Source: `src\domains\thermo.py:175`

---

## `_传热_串联传热`

```matha
def _传热_串联传热(dT, R_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `dT` | `` | |
| `R_list` | `` | |

Source: `src\domains\thermo.py:177`

---

## `_传热_串联热阻`

```matha
def _传热_串联热阻(R_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R_list` | `` | |

Source: `src\domains\thermo.py:181`

---

## `_传热_并联热阻`

```matha
def _传热_并联热阻(R_list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `R_list` | `` | |

Source: `src\domains\thermo.py:184`

---

## `_热机_卡诺效率`

```matha
def _热机_卡诺效率(T_h, T_c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_h` | `` | |
| `T_c` | `` | |

Source: `src\domains\thermo.py:196`

---

## `_热机_奥托效率`

```matha
def _热机_奥托效率(r, gamma)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `r` | `` | |
| `gamma` | `` | |

Source: `src\domains\thermo.py:198`

---

## `_热机_效率`

```matha
def _热机_效率(Q_h, Q_c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q_h` | `` | |
| `Q_c` | `` | |

Source: `src\domains\thermo.py:200`

---

## `_热机_制冷系数`

```matha
def _热机_制冷系数(T_c, T_h)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_c` | `` | |
| `T_h` | `` | |

Source: `src\domains\thermo.py:202`

---

## `_热机_热泵系数`

```matha
def _热机_热泵系数(T_h, T_c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `T_h` | `` | |
| `T_c` | `` | |

Source: `src\domains\thermo.py:204`

---

## `_热机_卡诺功`

```matha
def _热机_卡诺功(Q_h, T_h, T_c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q_h` | `` | |
| `T_h` | `` | |
| `T_c` | `` | |

Source: `src\domains\thermo.py:206`

---

## `_相变_显热`

```matha
def _相变_显热(m, c, dT)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `c` | `` | |
| `dT` | `` | |

Source: `src\domains\thermo.py:214`

---

## `_相变_潜热`

```matha
def _相变_潜热(m, L)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `L` | `` | |

Source: `src\domains\thermo.py:216`

---

## `_相变_线膨胀`

```matha
def _相变_线膨胀(alpha, L0, dT)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `alpha` | `` | |
| `L0` | `` | |
| `dT` | `` | |

Source: `src\domains\thermo.py:218`

---

## `_相变_体膨胀`

```matha
def _相变_体膨胀(beta, V0, dT)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `beta` | `` | |
| `V0` | `` | |
| `dT` | `` | |

Source: `src\domains\thermo.py:220`

---

## `_相变_热容`

```matha
def _相变_热容(m, c)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `` | |
| `c` | `` | |

Source: `src\domains\thermo.py:222`

---

## `_相变_比热容比`

```matha
def _相变_比热容比(cp, cv)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cp` | `` | |
| `cv` | `` | |

Source: `src\domains\thermo.py:224`

---

## `_相变_理想气体内能`

```matha
def _相变_理想气体内能(f, n, T)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `` | |
| `n` | `` | |
| `T` | `` | |

Source: `src\domains\thermo.py:226`

---

## `_相变_迈耶关系`

```matha
def _相变_迈耶关系(cp, cv)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cp` | `` | |
| `cv` | `` | |

Source: `src\domains\thermo.py:228`

---

## `_register_thermo`

```matha
def _register_thermo(builtins: dict) -> None
```

**描述**: 将热力学内建注册到解释器 builtins。

在 Interpreter.__init__ 中调用（fluid 之后）。
命名规则：
  - 气体状态：前缀 热_
  - 热力学过程：前缀 过程_
  - 热传递：前缀 传热_
  - 热机效率：前缀 热机_
  - 相变热物性：前缀 相变_

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\domains\thermo.py:301`

---

## `_thermo_symtab_names`

```matha
def _thermo_symtab_names() -> list[str]
```

**描述**: 返回热力学所有内建名（用于语义分析注册）。

**返回**: `list[str]`

Source: `src\domains\thermo.py:392`

---

# `driver_matrix`

```matha

```

**描述**: Matha 驱动矩阵系统 — 完整的驱动/引擎类型覆盖

覆盖范围:
  1. 核心性能驱动 (显卡/芯片组/硬盘/SSD/NVMe/内存控制器)
  2. 外部与接口驱动 (声卡/网卡/输入设备/USB/HDMI/雷电)
  3. 外设与办公驱动 (打印机/扫描仪/投影仪/3D打印机)
  4. 数据库驱动 (JDBC/ODBC/SQLite/PostgreSQL/MySQL/Redis)
  5. 数据开发驱动 (ETL/数据流/批处理/流处理)
  6. 事件驱动 (EventLoop/消息队列/观察者模式)
  7. 测试驱动 (TDD/单元测试/集成测试/性能测试)
  8. 领域驱动设计 (DDD/聚合根/值对象/领域事件)
  9. 行为驱动开发 (BDD/Gherkin/Given-When-Then)
  10. 契约驱动开发 (契约测试/API版本/向后兼容)
  11. 模型驱动构架 (MDA/M2/M3/代码生成/元模型)
  12. 意图驱动 (Intent Parser/自然语言→代码/智能体)
  13. 虚拟设备驱动 (VirtualDevice/QEMU/KVM/模拟器)
  14. 内核模式驱动 (Kernel Driver/内核模块/内核态)
  15. 文件系统驱动 (VFS/NTFS/ext4/FAT/云存储)
  16. 事件驱动架构 (EDA/CQRS/Event Sourcing)
  17. AI 驱动 (LLM/神经网络/机器学习/推理引擎)
  18. 业务驱动 (Business Logic/工作流/规则引擎)
  19. 数据驱动 (Data Pipeline/数据网格/数据湖)
  20. 需求驱动 (Requirements/Traceability/需求追踪)
  21. 技术驱动 (Tech Stack/架构决策/技术雷达)
  22. 电机驱动 (Stepper/Servo/BLDC/步进电机)
  23. LED 驱动 (GPIO PWM/LED矩阵/RGB/点阵屏)
  24. 显示驱动 (OLED/LCD/HDMI/Framebuffer)
  25. 软件定义驱动 (SDR/虚拟设备/软件定义硬件)
  26. 风压驱动 (气压/风速/风洞/流体控制)
  27. 机械驱动 (机械臂/凸轮/连杆/齿轮)
  28. 定制驱动 (Custom/用户定义/动态生成)
  29. 创造驱动 (Creative/AI生成/程序化生成)

Source: `src\driver_matrix.py:1`

---

## `DriverCategory`

```matha
class DriverCategory(Enum)
```

**描述**: 驱动大类。

Source: `src\driver_matrix.py:58`

---

## `DriverSubType`

```matha
class DriverSubType(Enum)
```

**描述**: 驱动子类型。

Source: `src\driver_matrix.py:91`

---

## `DriverSpec`

```matha
class DriverSpec
```

**描述**: 统一的驱动规格定义。

**装饰器**: dataclass

Source: `src\driver_matrix.py:252`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\driver_matrix.py:265`

---

## `MathaDriverMatrix`

```matha
class MathaDriverMatrix
```

**描述**: Matha 完整驱动矩阵 — 覆盖所有驱动/引擎类型。

所有驱动均可:
  1. 自动生成 Python/C/Matha 代码
  2. 通过 FFI 注册为 Matha 内建函数
  3. 嵌入到 multi_paradigm 多范式引擎中
  4. 支持安全沙箱执行
  5. 支持裸机编译 (RISC-V/ARM/x86)

Source: `src\driver_matrix.py:283`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\driver_matrix.py:295`

---

### `_init_all_drivers`

```matha
def _init_all_drivers(self)
```

**描述**: 初始化完整驱动矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\driver_matrix.py:307`

---

### `_register`

```matha
def _register(self, spec: DriverSpec)
```

**描述**: 注册驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

Source: `src\driver_matrix.py:844`

---

### `_update_stats`

```matha
def _update_stats(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\driver_matrix.py:856`

---

### `list_all`

```matha
def list_all(self) -> List[dict]
```

**描述**: 列出所有驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[dict]`

Source: `src\driver_matrix.py:863`

---

### `list_by_category`

```matha
def list_by_category(self, category: DriverCategory) -> List[dict]
```

**描述**: 按类别列出驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `category` | `DriverCategory` | |

**返回**: `List[dict]`

Source: `src\driver_matrix.py:867`

---

### `list_by_architecture`

```matha
def list_by_architecture(self, arch: Architecture) -> List[dict]
```

**描述**: 按架构列出驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `arch` | `Architecture` | |

**返回**: `List[dict]`

Source: `src\driver_matrix.py:871`

---

### `get_core_drivers`

```matha
def get_core_drivers(self) -> List[dict]
```

**描述**: 获取核心驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[dict]`

Source: `src\driver_matrix.py:875`

---

### `get`

```matha
def get(self, category: DriverCategory, sub_type: DriverSubType) -> Optional[dict]
```

**描述**: 获取单个驱动规格。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `category` | `DriverCategory` | |
| `sub_type` | `DriverSubType` | |

**返回**: `Optional[dict]`

Source: `src\driver_matrix.py:879`

---

### `generate_code`

```matha
def generate_code(self, category: DriverCategory, sub_type: DriverSubType, target_arch: Architecture, target_lang: str) -> dict
```

**描述**: 生成指定驱动的代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `category` | `DriverCategory` | |
| `sub_type` | `DriverSubType` | |
| `target_arch` | `Architecture` | |
| `target_lang` | `str` | |

**返回**: `dict`

Source: `src\driver_matrix.py:885`

---

### `_generate_driver_code`

```matha
def _generate_driver_code(self, spec: DriverSpec, arch: Architecture, lang: str) -> str
```

**描述**: 根据驱动规格生成代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |
| `arch` | `Architecture` | |
| `lang` | `str` | |

**返回**: `str`

Source: `src\driver_matrix.py:910`

---

### `_gen_python_driver`

```matha
def _gen_python_driver(self, spec: DriverSpec) -> str
```

**描述**: 生成 Python 驱动代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\driver_matrix.py:920`

---

### `_gen_c_driver`

```matha
def _gen_c_driver(self, spec: DriverSpec, arch: Architecture) -> str
```

**描述**: 生成 C 驱动代码 (裸机)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |
| `arch` | `Architecture` | |

**返回**: `str`

Source: `src\driver_matrix.py:989`

---

### `_gen_matha_driver`

```matha
def _gen_matha_driver(self, spec: DriverSpec) -> str
```

**描述**: 生成 Matha 原生驱动代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\driver_matrix.py:1058`

---

### `generate_all_python`

```matha
def generate_all_python(self) -> List[dict]
```

**描述**: 生成所有驱动的 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[dict]`

Source: `src\driver_matrix.py:1097`

---

### `generate_all_c`

```matha
def generate_all_c(self) -> List[dict]
```

**描述**: 生成所有驱动的 C 代码 (RISC-V 裸机)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[dict]`

Source: `src\driver_matrix.py:1106`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取驱动矩阵统计信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\driver_matrix.py:1117`

---

### `print_matrix`

```matha
def print_matrix(self)
```

**描述**: 打印完整驱动矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\driver_matrix.py:1127`

---

# `enhanced_intent`

```matha

```

**描述**: Matha 增强意图解析器 — v2.3

在 v2.2 基础上集成结构化异常系统，支持：
  1. 精确错误定位（阶段 + 行号 + 列号）
  2. 错误链传播（cause + children）
  3. 自动恢复策略
  4. 用户友好错误报告
  5. Result 类型错误传播（? 运算符语义）

Source: `src\enhanced_intent.py:1`

---

## `EnhancedIntentParser`

```matha
class EnhancedIntentParser(IntentParserBase)
```

**描述**: 增强意图解析器 — 带结构化异常处理。

Source: `src\enhanced_intent.py:33`

---

### `parse`

```matha
def parse(self, text: str, target_lang: str) -> Result
```

**描述**: 解析自然语言，返回 Result[Intent, MathaError]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:36`

---

### `_classify_with_recovery`

```matha
def _classify_with_recovery(self, text: str, intent: Intent) -> Result
```

**描述**: 意图分类，带自动恢复。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `intent` | `Intent` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:75`

---

### `_extract_params_with_recovery`

```matha
def _extract_params_with_recovery(self, text: str, intent: Intent) -> Result
```

**描述**: 参数提取，带自动恢复。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `intent` | `Intent` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:99`

---

### `_generate_code`

```matha
def _generate_code(self, intent: Intent) -> Result
```

**描述**: 代码生成，带错误处理。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:133`

---

### `_get_candidates`

```matha
def _get_candidates(self, text: str) -> list[str]
```

**描述**: 获取可能的意图候选。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `list[str]`

Source: `src\enhanced_intent.py:143`

---

### `execute_and_verify`

```matha
def execute_and_verify(self, intent: Intent) -> Result
```

**描述**: 执行意图并验证结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:163`

---

### `explain_with_errors`

```matha
def explain_with_errors(self, text: str) -> str
```

**描述**: 解析并生成带错误信息的自然语言解释。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\enhanced_intent.py:185`

---

### `_format_error_report`

```matha
def _format_error_report(self, error: MathaError, original_text: str) -> str
```

**描述**: 格式化错误报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `error` | `MathaError` | |
| `original_text` | `str` | |

**返回**: `str`

Source: `src\enhanced_intent.py:195`

---

## `ErrorAggregator`

```matha
class ErrorAggregator
```

**描述**: 聚合多个解析错误，生成综合报告。

Source: `src\enhanced_intent.py:215`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\enhanced_intent.py:218`

---

### `add`

```matha
def add(self, error: MathaError) -> 'ErrorAggregator'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `error` | `MathaError` | |

**返回**: `'ErrorAggregator'`

Source: `src\enhanced_intent.py:221`

---

### `add_result`

```matha
def add_result(self, result: Result) -> 'ErrorAggregator'
```

**描述**: 从 Result 中提取错误并添加。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `result` | `Result` | |

**返回**: `'ErrorAggregator'`

Source: `src\enhanced_intent.py:225`

---

### `aggregate`

```matha
def aggregate(self) -> CompositeError
```

**描述**: 聚合所有错误为 CompositeError。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `CompositeError`

Source: `src\enhanced_intent.py:231`

---

### `report`

```matha
def report(self) -> str
```

**描述**: 生成聚合报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\enhanced_intent.py:241`

---

## `IntentParseContext`

```matha
class IntentParseContext
```

**描述**: 意图解析上下文，提供链式错误处理。

Source: `src\enhanced_intent.py:260`

---

### `__init__`

```matha
def __init__(self, text: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

Source: `src\enhanced_intent.py:263`

---

### `parse`

```matha
def parse(self) -> Result
```

**描述**: 执行解析，收集所有错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:269`

---

### `recover`

```matha
def recover(self) -> Optional[MathaError]
```

**描述**: 尝试自动恢复。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[MathaError]`

Source: `src\enhanced_intent.py:280`

---

### `report`

```matha
def report(self) -> str
```

**描述**: 生成完整报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\enhanced_intent.py:290`

---

### `__enter__`

```matha
def __enter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\enhanced_intent.py:299`

---

### `__exit__`

```matha
def __exit__(self, exc_type, exc_val, exc_tb)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exc_type` | `` | |
| `exc_val` | `` | |
| `exc_tb` | `` | |

Source: `src\enhanced_intent.py:302`

---

## `parse_intent_safe`

```matha
def parse_intent_safe(text: str, target_lang: str) -> Result
```

**描述**: 安全解析：返回 Result[Intent, MathaError]。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:315`

---

## `explain_intent_safe`

```matha
def explain_intent_safe(text: str, target_lang: str) -> str
```

**描述**: 安全解释：返回自然语言报告（含错误信息）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `str`

Source: `src\enhanced_intent.py:321`

---

## `execute_intent`

```matha
def execute_intent(text: str, target_lang: str) -> Result
```

**描述**: 解析 + 执行 + 返回结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Result`

Source: `src\enhanced_intent.py:327`

---

# `errors`

```matha

```

**描述**: Matha 结构化异常系统 — v2.3

设计原则：
  1. 类型安全：每个错误阶段有明确的错误类型
  2. 错误链：错误可嵌套，保留完整上下文
  3. 错误组合：多个错误可聚合为一个 CompositeError
  4. 错误恢复：提供 recover() 尝试替代方案
  5. 用户友好：auto_report() 生成自然语言错误报告

使用示例：
    try:
        result = parse_intent("计算100以内所有素数")
    except MathaIntentError as e:
        print(e.report())
        print(e.suggestions())

Source: `src\errors.py:1`

---

## `ErrorStage`

```matha
class ErrorStage(Enum)
```

**描述**: 错误发生阶段。

Source: `src\errors.py:35`

---

## `ErrorSeverity`

```matha
class ErrorSeverity(Enum)
```

**描述**: 错误严重程度。

Source: `src\errors.py:47`

---

### `__lt__`

```matha
def __lt__(self, other: 'ErrorSeverity') -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'ErrorSeverity'` | |

**返回**: `bool`

Source: `src\errors.py:54`

---

### `__gt__`

```matha
def __gt__(self, other: 'ErrorSeverity') -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'ErrorSeverity'` | |

**返回**: `bool`

Source: `src\errors.py:57`

---

## `MathaError`

```matha
class MathaError(Exception)
```

**描述**: Matha 结构化异常基类（继承 Exception 以支持 raise/except）。

Source: `src\errors.py:65`

---

### `__init__`

```matha
def __init__(self, message: str, stage: ErrorStage, severity: ErrorSeverity, code: str, suggestions: list, context: dict, cause: 'MathaError', stack: list, children: list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `stage` | `ErrorStage` | |
| `severity` | `ErrorSeverity` | |
| `code` | `str` | |
| `suggestions` | `list` | |
| `context` | `dict` | |
| `cause` | `'MathaError'` | |
| `stack` | `list` | |
| `children` | `list` | |

Source: `src\errors.py:70`

---

### `with_cause`

```matha
def with_cause(self, cause: 'MathaError') -> 'MathaError'
```

**描述**: 链接因果错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cause` | `'MathaError'` | |

**返回**: `'MathaError'`

Source: `src\errors.py:86`

---

### `add_child`

```matha
def add_child(self, child: 'MathaError') -> 'MathaError'
```

**描述**: 添加子错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `child` | `'MathaError'` | |

**返回**: `'MathaError'`

Source: `src\errors.py:92`

---

### `add_suggestion`

```matha
def add_suggestion(self, suggestion: str) -> 'MathaError'
```

**描述**: 添加恢复建议。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `suggestion` | `str` | |

**返回**: `'MathaError'`

Source: `src\errors.py:97`

---

### `report`

```matha
def report(self, indent: int) -> str
```

**描述**: 生成错误报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `indent` | `int` | |

**返回**: `str`

Source: `src\errors.py:103`

---

### `suggestions_text`

```matha
def suggestions_text(self) -> str
```

**描述**: 生成建议文本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\errors.py:122`

---

### `to_result`

```matha
def to_result(self) -> Err
```

**描述**: 转换为 Result 错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Err`

Source: `src\errors.py:131`

---

### `_format_trace`

```matha
def _format_trace(self) -> str
```

**描述**: 格式化调用栈。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\errors.py:135`

---

## `ParseError`

```matha
class ParseError(MathaError)
```

**描述**: 语法解析错误。

Source: `src\errors.py:144`

---

### `__init__`

```matha
def __init__(self, message: str, line: int, col: int, expected: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `expected` | `str` | |

Source: `src\errors.py:146`

---

### `add_suggestion`

```matha
def add_suggestion(self, suggestion: str) -> 'ParseError'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `suggestion` | `str` | |

**返回**: `'ParseError'`

Source: `src\errors.py:158`

---

## `ClassifyError`

```matha
class ClassifyError(MathaError)
```

**描述**: 意图分类错误。

Source: `src\errors.py:163`

---

### `__init__`

```matha
def __init__(self, message: str, candidates: list[str])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `candidates` | `list[str]` | |

Source: `src\errors.py:165`

---

## `ParamExtractError`

```matha
class ParamExtractError(MathaError)
```

**描述**: 参数提取错误。

Source: `src\errors.py:176`

---

### `__init__`

```matha
def __init__(self, message: str, expected_type: str, actual_type: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `expected_type` | `str` | |
| `actual_type` | `str` | |

Source: `src\errors.py:178`

---

## `CodeGenError`

```matha
class CodeGenError(MathaError)
```

**描述**: 代码生成错误。

Source: `src\errors.py:190`

---

### `__init__`

```matha
def __init__(self, message: str, target_lang: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `target_lang` | `str` | |

Source: `src\errors.py:192`

---

## `ExecError`

```matha
class ExecError(MathaError)
```

**描述**: 代码执行错误。

Source: `src\errors.py:202`

---

### `__init__`

```matha
def __init__(self, message: str, exception: Exception)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `exception` | `Exception` | |

Source: `src\errors.py:204`

---

## `CompositeError`

```matha
class CompositeError(MathaError)
```

**描述**: 复合错误：多个错误聚合。

Source: `src\errors.py:216`

---

### `__init__`

```matha
def __init__(self, message: str, errors: list[MathaError])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `errors` | `list[MathaError]` | |

Source: `src\errors.py:218`

---

### `recover`

```matha
def recover(self) -> Optional[MathaError]
```

**描述**: 尝试从复合错误中恢复。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[MathaError]`

Source: `src\errors.py:231`

---

## `ok_with_context`

```matha
def ok_with_context(value, **ctx) -> Result
```

**描述**: 创建带上下文的 Ok。

| 参数 | 类型 | 说明 |
|------|------|------|
| `value` | `` | |

**返回**: `Result`

Source: `src\errors.py:248`

---

## `err_with_stage`

```matha
def err_with_stage(message: str, stage: ErrorStage, **kwargs) -> Result
```

**描述**: 创建带阶段的 Err。

| 参数 | 类型 | 说明 |
|------|------|------|
| `message` | `str` | |
| `stage` | `ErrorStage` | |

**返回**: `Result`

Source: `src\errors.py:253`

---

## `map_errors`

```matha
def map_errors(fn, *args, **kwargs) -> Result
```

**描述**: 执行函数并映射错误到结构化错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

**返回**: `Result`

Source: `src\errors.py:259`

---

## `RecoveryStrategy`

```matha
class RecoveryStrategy
```

**描述**: 错误恢复策略注册表（读写锁分离，v2.4 优化）。

锁层次结构：
  _suggestion_lock (Level 0): 最细粒度，仅保护 suggestions 列表
  _read_lock   (Level 1): 保护 _strategies 字典读取
  _write_lock  (Level 2): 保护 _strategies 字典写入

规则：持有高层锁时不获取低层锁，避免死锁。

Source: `src\errors.py:277`

---

### `register`

```matha
def register(cls, stage: ErrorStage)
```

**描述**: 装饰器：注册恢复策略（写锁，Level 2）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `stage` | `ErrorStage` | |

**装饰器**: classmethod

Source: `src\errors.py:298`

---

### `try_recover`

```matha
def try_recover(cls, error: MathaError) -> Optional[MathaError]
```

**描述**: 尝试所有注册的恢复策略（读锁 → 锁外执行 → 建议锁）。

锁使用策略：
  1. 获取读锁 → 复制策略列表 → 释放读锁（快，O(n) 但 n 很小）
  2. 在锁外执行策略函数（慢，不持有锁，允许其他线程并发读取）
  3. 若需要修改 error.suggestions，获取建议锁（快，仅追加字符串）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `error` | `MathaError` | |

**返回**: `Optional[MathaError]`

**装饰器**: classmethod

Source: `src\errors.py:309`

---

### `get_strategy_count`

```matha
def get_strategy_count(cls, stage: ErrorStage) -> int
```

**描述**: 获取指定阶段的策略数量（读锁保护）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `stage` | `ErrorStage` | |

**返回**: `int`

**装饰器**: classmethod

Source: `src\errors.py:338`

---

### `_mark_builtin`

```matha
def _mark_builtin(cls, fn) -> None
```

**描述**: 标记策略为内置（测试 clear 时保留）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `fn` | `` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\errors.py:347`

---

### `clear`

```matha
def clear(cls) -> None
```

**描述**: 清空测试期间添加的策略，保留内置策略。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\errors.py:352`

---

## `_recover_classify`

```matha
def _recover_classify(error: MathaError) -> Optional[MathaError]
```

**描述**: 意图分类失败的恢复策略。

| 参数 | 类型 | 说明 |
|------|------|------|
| `error` | `MathaError` | |

**返回**: `Optional[MathaError]`

**装饰器**: RecoveryStrategy.register(ErrorStage.CLASSIFYING)

Source: `src\errors.py:367`

---

## `_recover_params`

```matha
def _recover_params(error: MathaError) -> Optional[MathaError]
```

**描述**: 参数提取失败的恢复策略。

| 参数 | 类型 | 说明 |
|------|------|------|
| `error` | `MathaError` | |

**返回**: `Optional[MathaError]`

**装饰器**: RecoveryStrategy.register(ErrorStage.PARAM_EXTRACTING)

Source: `src\errors.py:378`

---

## `_recover_codegen`

```matha
def _recover_codegen(error: MathaError) -> Optional[MathaError]
```

**描述**: 代码生成失败的恢复策略。

| 参数 | 类型 | 说明 |
|------|------|------|
| `error` | `MathaError` | |

**返回**: `Optional[MathaError]`

**装饰器**: RecoveryStrategy.register(ErrorStage.CODE_GENERATING)

Source: `src\errors.py:391`

---

## `_recover_exec`

```matha
def _recover_exec(error: MathaError) -> Optional[MathaError]
```

**描述**: 执行失败的恢复策略。

| 参数 | 类型 | 说明 |
|------|------|------|
| `error` | `MathaError` | |

**返回**: `Optional[MathaError]`

**装饰器**: RecoveryStrategy.register(ErrorStage.EXECUTING)

Source: `src\errors.py:402`

---

## `classify_error`

```matha
def classify_error(msg: str, candidates: list) -> MathaError
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `msg` | `str` | |
| `candidates` | `list` | |

**返回**: `MathaError`

Source: `src\errors.py:421`

---

## `parse_error`

```matha
def parse_error(msg: str, line: int, col: int, expected: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `msg` | `str` | |
| `line` | `int` | |
| `col` | `int` | |
| `expected` | `str` | |

Source: `src\errors.py:425`

---

## `param_error`

```matha
def param_error(msg: str, expected: str, actual: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `msg` | `str` | |
| `expected` | `str` | |
| `actual` | `str` | |

Source: `src\errors.py:429`

---

## `codegen_error`

```matha
def codegen_error(msg: str, lang: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `msg` | `str` | |
| `lang` | `str` | |

Source: `src\errors.py:433`

---

## `exec_error`

```matha
def exec_error(msg: str, exc: Exception)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `msg` | `str` | |
| `exc` | `Exception` | |

Source: `src\errors.py:437`

---

## `composite_error`

```matha
def composite_error(msg: str, errors: list)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `msg` | `str` | |
| `errors` | `list` | |

Source: `src\errors.py:441`

---

# `exceptions`

```matha

```

**描述**: Matha v4.4 异常处理优化模块

本模块提供统一的异常处理工具和最佳实践。

Source: `src\exceptions.py:1`

---

## `MathaError`

```matha
class MathaError(Exception)
```

**描述**: Matha 基础异常类。

Source: `src\exceptions.py:15`

---

## `MatrixError`

```matha
class MatrixError(MathaError)
```

**描述**: 矩阵运算异常。

Source: `src\exceptions.py:20`

---

## `DimensionMismatchError`

```matha
class DimensionMismatchError(MatrixError)
```

**描述**: 矩阵维度不匹配异常。

Source: `src\exceptions.py:25`

---

## `SingularMatrixError`

```matha
class SingularMatrixError(MatrixError)
```

**描述**: 奇异矩阵异常。

Source: `src\exceptions.py:30`

---

## `SymbolicError`

```matha
class SymbolicError(MathaError)
```

**描述**: 符号计算异常。

Source: `src\exceptions.py:35`

---

## `ImportError`

```matha
class ImportError(MathaError)
```

**描述**: 导入异常。

Source: `src\exceptions.py:40`

---

## `ConfigurationError`

```matha
class ConfigurationError(MathaError)
```

**描述**: 配置异常。

Source: `src\exceptions.py:45`

---

## `safe_execute`

```matha
def safe_execute(func: Callable, *args, **kwargs) -> Any
```

**描述**: 安全执行函数，捕获并记录异常。

Args:
    func: 要执行的函数
    *args: 位置参数
    **kwargs: 关键字参数

Yields:
    函数返回值

Raises:
    重新抛出异常（记录后）

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `Any`

**装饰器**: contextmanager

Source: `src\exceptions.py:51`

---

## `with_error_handling`

```matha
def with_error_handling(func: Callable) -> Callable
```

**描述**: 装饰器：为函数添加错误处理。

用法:
    @with_error_handling
    def my_function(x):
        return x ** 2

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `Callable`

Source: `src\exceptions.py:85`

---

## `validate_matrix_shape`

```matha
def validate_matrix_shape(rows: int, cols: int) -> None
```

**描述**: 验证矩阵维度。

Args:
    rows: 行数
    cols: 列数

Raises:
    ValueError: 维度无效

| 参数 | 类型 | 说明 |
|------|------|------|
| `rows` | `int` | |
| `cols` | `int` | |

**返回**: `None`

Source: `src\exceptions.py:110`

---

## `validate_numeric`

```matha
def validate_numeric(value: Any, name: str) -> None
```

**描述**: 验证数值类型。

Args:
    value: 要验证的值
    name: 参数名称（用于错误消息）

Raises:
    TypeError: 类型错误

| 参数 | 类型 | 说明 |
|------|------|------|
| `value` | `Any` | |
| `name` | `str` | |

**返回**: `None`

Source: `src\exceptions.py:127`

---

## `check_dependency`

```matha
def check_dependency(module_name: str, feature: str) -> bool
```

**描述**: 检查依赖是否可用。

Args:
    module_name: 模块名称
    feature: 功能描述

Returns:
    是否可用

| 参数 | 类型 | 说明 |
|------|------|------|
| `module_name` | `str` | |
| `feature` | `str` | |

**返回**: `bool`

Source: `src\exceptions.py:142`

---

# `ffi`

```matha

```

**描述**: Matha FFI 层 v1.3.0
====================
外部函数接口（Foreign Function Interface）— 桥接 Matha 与 Python/其他语言。

功能：
  • register_func    — 注册 Python 函数到 Matha 符号系统
  • call_external    — 从 Matha 调用外部函数
  • import_py_module — 导入 Python 模块作为 Matha 库
  • import_math_func — 导入数学函数
  • FFI Bridge       — 跨语言调用协议
  • 动态函数注册表

Source: `src\ffi.py:1`

---

## `FFIImport`

```matha
class FFIImport
```

**描述**: FFI 导入描述。

**装饰器**: dataclass

Source: `src\ffi.py:32`

---

## `FFICall`

```matha
class FFICall
```

**描述**: FFI 调用记录。

**装饰器**: dataclass

Source: `src\ffi.py:42`

---

## `MathaFFIBridge`

```matha
class MathaFFIBridge
```

**描述**: Matha 外部函数接口桥接器。

支持：
  1. Python 函数注册 → Matha 内建函数
  2. Python 模块导入 → Matha 命名空间
  3. 跨语言调用协议（通过 FFI 描述符）
  4. 动态函数注册/注销
  5. 调用链日志和性能追踪

Source: `src\ffi.py:52`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ffi.py:64`

---

### `_register_math_builtins`

```matha
def _register_math_builtins(self)
```

**描述**: 注册数学内建函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ffi.py:75`

---

### `register`

```matha
def register(self, name: str, func: Callable, params: List[str], doc: str)
```

**描述**: 注册 Python 函数到 Matha FFI。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `Callable` | |
| `params` | `List[str]` | |
| `doc` | `str` | |

Source: `src\ffi.py:101`

---

### `unregister`

```matha
def unregister(self, name: str)
```

**描述**: 注销 Matha 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

Source: `src\ffi.py:112`

---

### `import_module`

```matha
def import_module(self, module_name: str, alias: str, exclude: List[str], include: List[str])
```

**描述**: 导入 Python 模块到 Matha 命名空间。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `module_name` | `str` | |
| `alias` | `str` | |
| `exclude` | `List[str]` | |
| `include` | `List[str]` | |

Source: `src\ffi.py:122`

---

### `import_numpy`

```matha
def import_numpy(self)
```

**描述**: 导入 NumPy 作为 'np' 命名空间。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ffi.py:149`

---

### `call`

```matha
def call(self, name: str, *args) -> Any
```

**描述**: 调用已注册的 FFI 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\ffi.py:171`

---

### `_resolve`

```matha
def _resolve(self, name: str) -> Optional[Callable]
```

**描述**: 解析函数名（支持嵌套命名空间如 np.sin）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\ffi.py:200`

---

### `get_function`

```matha
def get_function(self, name: str) -> Optional[Callable]
```

**描述**: 获取已注册函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\ffi.py:209`

---

### `is_registered`

```matha
def is_registered(self, name: str) -> bool
```

**描述**: 检查函数是否已注册。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\ffi.py:214`

---

### `list_functions`

```matha
def list_functions(self, source: str) -> List[dict]
```

**描述**: 列出所有注册函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `List[dict]`

Source: `src\ffi.py:221`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取 FFI 调用统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\ffi.py:235`

---

### `clear_log`

```matha
def clear_log(self)
```

**描述**: 清空调用日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\ffi.py:249`

---

## `get_ffi`

```matha
def get_ffi() -> MathaFFIBridge
```

**描述**: 获取 FFI 桥接器单例。

**返回**: `MathaFFIBridge`

Source: `src\ffi.py:261`

---

## `ffi_register`

```matha
def ffi_register(name: str, func: Callable, **kwargs)
```

**描述**: 便捷注册函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `func` | `Callable` | |

Source: `src\ffi.py:269`

---

## `ffi_call`

```matha
def ffi_call(name: str, *args) -> Any
```

**描述**: 便捷调用函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |

**返回**: `Any`

Source: `src\ffi.py:274`

---

## `my_add`

```matha
def my_add(a, b)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `` | |
| `b` | `` | |

Source: `src\ffi.py:289`

---

# `firewall`

```matha

```

**描述**: Matha 防火墙引擎 — 用 Matha 语言约束 Matha 代码执行

设计原则：
  1. 三层权限模型：沙箱 / 受限 / 全功能
  2. 白名单 + 黑名单双轨制
  3. 支持 Matha 自身语言定义防火墙规则
  4. 每个执行上下文独立隔离
  5. 所有拦截都抛可识别的 MathaFirewallException

架构：
  FirewallLevel  —— 权限级别枚举
  FirewallRule    —— 单条规则（builtin名 + allow/deny + 描述）
  FirewallPolicy  —— 完整策略（三层默认 + 可自定义）
  MathaFirewall   —— 防火墙引擎，包装 Interpreter
  FirewallException — 可识别的拦截异常

Source: `src\firewall.py:1`

---

## `MathaFirewallException`

```matha
class MathaFirewallException(MathaRuntimeError)
```

**描述**: 防火墙拦截异常 — 可被 catch 并给出小白友好提示。

Source: `src\firewall.py:34`

---

### `__init__`

```matha
def __init__(self, builtin_name: str, level: str, reason: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `builtin_name` | `str` | |
| `level` | `str` | |
| `reason` | `str` | |

Source: `src\firewall.py:37`

---

## `FirewallLevel`

```matha
class FirewallLevel(str, Enum)
```

**描述**: 防火墙权限级别。

Source: `src\firewall.py:52`

---

## `FirewallRule`

```matha
class FirewallRule
```

**描述**: 一条防火墙规则。

**装饰器**: dataclass

Source: `src\firewall.py:64`

---

### `applies_to`

```matha
def applies_to(self, level: str) -> bool
```

**描述**: 检查规则是否适用于给定级别（兼容中文名和英文名）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `str` | |

**返回**: `bool`

Source: `src\firewall.py:72`

---

## `FirewallPolicy`

```matha
class FirewallPolicy
```

**描述**: 防火墙策略 — 白名单 + 黑名单 + 自定义规则。

Source: `src\firewall.py:147`

---

### `__init__`

```matha
def __init__(self, level: FirewallLevel)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `FirewallLevel` | |

Source: `src\firewall.py:150`

---

### `_load_defaults`

```matha
def _load_defaults(self, level: FirewallLevel) -> None
```

**描述**: 加载默认规则，正确分离 allow/deny。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `FirewallLevel` | |

**返回**: `None`

Source: `src\firewall.py:156`

---

### `deny`

```matha
def deny(self, builtin: str, reason: str, tags: Optional[list[str]]) -> 'FirewallPolicy'
```

**描述**: 添加黑名单规则（覆盖白名单）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `builtin` | `str` | |
| `reason` | `str` | |
| `tags` | `Optional[list[str]]` | |

**返回**: `'FirewallPolicy'`

Source: `src\firewall.py:172`

---

### `allow`

```matha
def allow(self, builtin: str, reason: str, tags: Optional[list[str]]) -> 'FirewallPolicy'
```

**描述**: 添加白名单规则。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `builtin` | `str` | |
| `reason` | `str` | |
| `tags` | `Optional[list[str]]` | |

**返回**: `'FirewallPolicy'`

Source: `src\firewall.py:177`

---

### `is_blocked`

```matha
def is_blocked(self, builtin_name: str) -> tuple[bool, str]
```

**描述**: 检查某个内建是否被拦截。返回 (是否被拦, 原因)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `builtin_name` | `str` | |

**返回**: `tuple[bool, str]`

Source: `src\firewall.py:182`

---

### `list_blocked`

```matha
def list_blocked(self) -> list[str]
```

**描述**: 返回当前级别下被拦截的内建列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\firewall.py:194`

---

### `to_matha_rule_text`

```matha
def to_matha_rule_text(self) -> str
```

**描述**: 将策略转换为 Matha 可读取的规则文本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\firewall.py:205`

---

## `MathaFirewall`

```matha
class MathaFirewall
```

**描述**: Matha 防火墙引擎 — 包装 Interpreter，在执行前后施加安全检查。

用法：
    fw = MathaFirewall(FirewallLevel.沙箱)
    outputs, trace = fw.run(program_ast)

    # 或用自然语言字符串
    outputs, trace = fw.interpret("#1：[3 + 5]")

Source: `src\firewall.py:219`

---

### `__init__`

```matha
def __init__(self, level: FirewallLevel, policy: Optional[FirewallPolicy])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `FirewallLevel` | |
| `policy` | `Optional[FirewallPolicy]` | |

Source: `src\firewall.py:231`

---

### `_wrap_interp_call`

```matha
def _wrap_interp_call(self) -> None
```

**描述**: 拦截解释器的 call 和 _apply，确保所有内建调用都经过防火墙检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\firewall.py:241`

---

### `run`

```matha
def run(self, program: ast.Program) -> tuple[list, list[str]]
```

**描述**: 执行 AST 程序，通过防火墙检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `ast.Program` | |

**返回**: `tuple[list, list[str]]`

Source: `src\firewall.py:294`

---

### `interpret`

```matha
def interpret(self, source: str) -> tuple[list, list[str]]
```

**描述**: 从 Matha 源码字符串执行（解析 + 防火墙 + 解释）。
返回 (outputs, trace)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[list, list[str]]`

Source: `src\firewall.py:308`

---

### `call`

```matha
def call(self, name: str, *args) -> object
```

**描述**: 防火墙公开的 call 接口（供外部直接调用）。
内部调用已通过 _wrap_interp_call 自动路由。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `object`

Source: `src\firewall.py:319`

---

### `run_interactive`

```matha
def run_interactive(self, code: str) -> dict
```

**描述**: 交互式执行 — 适合 AI Assistant 使用。
返回结构化结果（包含防火墙统计）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |

**返回**: `dict`

Source: `src\firewall.py:330`

---

### `set_level`

```matha
def set_level(self, level: FirewallLevel) -> None
```

**描述**: 切换防火墙级别。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `FirewallLevel` | |

**返回**: `None`

Source: `src\firewall.py:364`

---

### `add_rule`

```matha
def add_rule(self, rule: FirewallRule) -> None
```

**描述**: 添加自定义规则。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `rule` | `FirewallRule` | |

**返回**: `None`

Source: `src\firewall.py:373`

---

### `get_status`

```matha
def get_status(self) -> dict
```

**描述**: 获取防火墙当前状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\firewall.py:380`

---

### `reset`

```matha
def reset(self) -> None
```

**描述**: 重置统计和解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\firewall.py:390`

---

## `_install_firewall_builtins`

```matha
def _install_firewall_builtins(interp: Interpreter) -> None
```

**描述**: 将防火墙能力注册为 Matha 内建函数，使 Matha 代码可以：
  - 检查当前防火墙级别
  - 查询被拦截的内建
  - 查看防火墙状态
  - 请求临时提升权限

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `Interpreter` | |

**返回**: `None`

Source: `src\firewall.py:402`

---

## `configure_logging`

```matha
def configure_logging(level: int) -> None
```

**描述**: 配置防火墙日志输出到 stderr。

用法：
    from src.firewall import configure_logging
    configure_logging()          # DEBUG 级别（详细）
    configure_logging(logging.WARNING)  # 仅警告

也可通过环境变量：
    $env:MATHA_FIREWALL_DEBUG = "1"

| 参数 | 类型 | 说明 |
|------|------|------|
| `level` | `int` | |

**返回**: `None`

Source: `src\firewall.py:420`

---

## `sandbox_interpret`

```matha
def sandbox_interpret(source: str) -> tuple[list, list[str]]
```

**描述**: 沙箱模式执行 Matha 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `tuple[list, list[str]]`

Source: `src\firewall.py:449`

---

## `restricted_interpret`

```matha
def restricted_interpret(source: str) -> tuple[list, list[str]]
```

**描述**: 受限模式执行 Matha 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `tuple[list, list[str]]`

Source: `src\firewall.py:455`

---

## `full_interpret`

```matha
def full_interpret(source: str) -> tuple[list, list[str]]
```

**描述**: 全功能模式执行 Matha 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `tuple[list, list[str]]`

Source: `src\firewall.py:461`

---

# `foreign`

```matha

```

**描述**: Matha 外部语言互操作子系统：双实现对比驱动的自我升级。

让 Matha 能调用其它编程语言（Python 宿主内嵌 + subprocess 通用通道），
核心用途是「双实现对比」——用外部语言构建参考实现，Matha 同时构建一份
等价实现，在沙箱中对比输出，通过则升级 Matha 实现，不通过则回滚。

设计要点：
  - ForeignRunner：执行外部语言代码。
      * Python：宿主内 eval/exec，在独立 globals 命名空间运行（隔离副作用），
        支持「定义函数 + 调用」模式。
      * 其它语言：subprocess 调用解释器，通过临时文件 + JSON I/O 传递参数
        与返回值，进程级隔离。
  - DualComparator：对同一组测试输入，分别调用 Matha 函数与外部函数，
        逐项对比返回值，产出 CompareResult。
  - compare_upgrade：沙箱试运行 Matha 源码 → 在沙箱中对比 → 全通过则 commit，
        任一不一致或出错则 rollback（复用自我升级子系统的安全语义）。

安全模型：复用沙箱隔离。
  - 外部代码在独立命名空间/子进程运行，不污染 Matha 解释器本体。
  - 对比升级的 Matha 试运行在 Sandbox 内进行，失败回滚零污染。

Source: `src\foreign.py:1`

---

## `ForeignRunner`

```matha
class ForeignRunner
```

**描述**: 执行外部编程语言代码。

Python：宿主内 eval/exec，在独立 globals 中运行（隔离副作用）。
其它语言：subprocess 调用解释器，JSON 传递输入输出，进程级隔离。

Source: `src\foreign.py:55`

---

### `eval`

```matha
def eval(self, lang: str, code: str, inputs: Optional[dict]) -> Any
```

**描述**: 求值外部语言代码，返回结果。

- Python：表达式用 eval 返回值；语句用 exec，返回 None
  （可通过 inputs 注入变量，执行后可从返回的 globals 取值）。
- 其它语言：整段脚本执行，stdout 尝试 JSON 解析，失败返回原文本。

Args:
    lang: 语言名（python/javascript/node/ruby）
    code: 源代码
    inputs: 注入到执行环境的变量（仅 Python 模式生效）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `code` | `str` | |
| `inputs` | `Optional[dict]` | |

**返回**: `Any`

Source: `src\foreign.py:62`

---

### `call`

```matha
def call(self, lang: str, code: str, func_name: str, args: list) -> Any
```

**描述**: 执行外部代码并调用其中定义的函数，返回函数返回值。

Python：exec 定义函数后直接调用（宿主内）。
其它语言：生成「定义 + 调用 + JSON 输出」的 wrapper 脚本，
subprocess 执行后解析 stdout。

Args:
    lang: 语言名
    code: 定义函数的源代码
    func_name: 要调用的函数名
    args: 参数列表（会 JSON 序列化传给外部函数）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `code` | `str` | |
| `func_name` | `str` | |
| `args` | `list` | |

**返回**: `Any`

Source: `src\foreign.py:79`

---

### `_eval_python`

```matha
def _eval_python(self, code: str, inputs: Optional[dict]) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `inputs` | `Optional[dict]` | |

**返回**: `Any`

Source: `src\foreign.py:100`

---

### `_call_python`

```matha
def _call_python(self, code: str, func_name: str, args: list) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `func_name` | `str` | |
| `args` | `list` | |

**返回**: `Any`

Source: `src\foreign.py:110`

---

### `_eval_subprocess`

```matha
def _eval_subprocess(self, lang: str, code: str, inputs: Optional[dict]) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `code` | `str` | |
| `inputs` | `Optional[dict]` | |

**返回**: `Any`

Source: `src\foreign.py:120`

---

### `_call_subprocess`

```matha
def _call_subprocess(self, lang: str, code: str, func_name: str, args: list) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `code` | `str` | |
| `func_name` | `str` | |
| `args` | `list` | |

**返回**: `Any`

Source: `src\foreign.py:143`

---

### `_build_call_wrapper`

```matha
def _build_call_wrapper(self, lang: str, code: str, func_name: str, args: list) -> str
```

**描述**: 生成「定义函数 + 用 JSON 参数调用 + JSON 输出」的 wrapper。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `code` | `str` | |
| `func_name` | `str` | |
| `args` | `list` | |

**返回**: `str`

Source: `src\foreign.py:166`

---

### `_ext`

```matha
def _ext(lang: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `lang` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\foreign.py:194`

---

### `_parse_output`

```matha
def _parse_output(out: str) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `out` | `str` | |

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\foreign.py:200`

---

## `CompareResult`

```matha
class CompareResult
```

**描述**: 双实现对比结果。

**装饰器**: dataclass

Source: `src\foreign.py:214`

---

### `as_dict`

```matha
def as_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\foreign.py:221`

---

## `DualComparator`

```matha
class DualComparator
```

**描述**: 双实现对比器：Matha 函数 vs 外部语言函数。

对同一组测试输入，分别调用两个实现，逐项对比返回值。

Source: `src\foreign.py:230`

---

### `__init__`

```matha
def __init__(self, interp: Interpreter, matha_func: str, foreign_lang: str, foreign_code: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interp` | `Interpreter` | |
| `matha_func` | `str` | |
| `foreign_lang` | `str` | |
| `foreign_code` | `str` | |

Source: `src\foreign.py:236`

---

### `compare`

```matha
def compare(self, test_cases: list) -> CompareResult
```

**描述**: 对每个测试输入（参数列表）对比两个实现的返回值。

Args:
    test_cases: list of arg-lists，如 [[1,2], [3,4]]

Returns:
    CompareResult

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `test_cases` | `list` | |

**返回**: `CompareResult`

Source: `src\foreign.py:244`

---

### `_foreign_func_name`

```matha
def _foreign_func_name(self) -> str
```

**描述**: 外部代码中的函数名：默认与 Matha 函数同名。

外部代码可能用不同命名（如 snake_case），此处简化为同名；
若需不同名，可在 foreign_code 中定义后通过子类覆盖。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\foreign.py:280`

---

## `_values_equal`

```matha
def _values_equal(a: Any, b: Any) -> bool
```

**描述**: 对比 Matha 值与外部值是否相等（容忍类型差异）。

- 数值：直接 == （int/float 互通）
- 字符串：直接 ==
- list/tuple：逐元素递归
- dict：key/value 递归
- bool 严格区分（True != 1）

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Any` | |
| `b` | `Any` | |

**返回**: `bool`

Source: `src\foreign.py:289`

---

## `compare_upgrade`

```matha
def compare_upgrade(parent: Interpreter, matha_src: str, matha_func: str, foreign_lang: str, foreign_code: str, test_cases: list, extra_verify) -> UpgradeResult
```

**描述**: 双实现对比驱动的自我升级。

流程：
  1. 创建沙箱（克隆本体）
  2. 在沙箱中加载 Matha 源码（试运行）
  3. 运行出错 → rollback，返回失败（本体未触碰）
  4. 在沙箱中用 DualComparator 对比 Matha 函数与外部参考实现
  5. 对比不通过 → rollback，返回失败（含差异详情）
  6. extra_verify 给定时调用，返回假或抛错 → rollback
  7. 全通过 → commit，返回成功（含变更 diff）

Args:
    parent: 本体解释器
    matha_src: 待升级的 Matha 源码（定义 matha_func）
    matha_func: 要对比的 Matha 函数名
    foreign_lang: 参考实现语言（python/javascript/ruby）
    foreign_code: 参考实现源码（定义同名函数）
    test_cases: 对比用的测试输入列表（每个是参数列表）
    extra_verify: 可选的额外校验 callable(sandbox) -> bool

Returns:
    UpgradeResult（成功时 输出 含对比摘要，变更 含 diff）

| 参数 | 类型 | 说明 |
|------|------|------|
| `parent` | `Interpreter` | |
| `matha_src` | `str` | |
| `matha_func` | `str` | |
| `foreign_lang` | `str` | |
| `foreign_code` | `str` | |
| `test_cases` | `list` | |
| `extra_verify` | `` | |

**返回**: `UpgradeResult`

Source: `src\foreign.py:320`

---

# `growth`

```matha

```

**描述**: Matha 自主成长系统：可扩展模块注册、自诊断、自修改、成长循环

架构设计：
  1. ExtensionRegistry: 动态插件注册中心，支持热插拔
  2. SelfDiagnostic: 自诊断引擎，分析性能瓶颈和代码质量
  3. SelfModifier: 自修改引擎，安全地修改自身代码
  4. GrowthLoop: 成长循环，自动执行诊断→修改→验证

核心原则：
  - 所有修改在沙箱中测试，验证通过后才提交
  - 支持回滚：任何修改都可撤销
  - 可扩展：新 Pass/模块/目标可通过注册表动态添加

Source: `src\growth.py:1`

---

## `Extension`

```matha
class Extension
```

**描述**: 扩展单元：任何可注册的功能模块。

**装饰器**: dataclass

Source: `src\growth.py:32`

---

### `load`

```matha
def load(self) -> Any
```

**描述**: 动态加载扩展。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\growth.py:42`

---

## `ExtensionRegistry`

```matha
class ExtensionRegistry
```

**描述**: 扩展注册表：管理所有可生长的组件。

Source: `src\growth.py:48`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth.py:51`

---

### `register`

```matha
def register(self, ext: Extension) -> None
```

**描述**: 注册扩展。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ext` | `Extension` | |

**返回**: `None`

Source: `src\growth.py:56`

---

### `unregister`

```matha
def unregister(self, name: str) -> bool
```

**描述**: 注销扩展。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\growth.py:61`

---

### `get`

```matha
def get(self, name: str) -> Optional[Extension]
```

**描述**: 获取扩展。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Extension]`

Source: `src\growth.py:69`

---

### `get_instance`

```matha
def get_instance(self, name: str) -> Any
```

**描述**: 获取扩展实例（懒加载）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\growth.py:73`

---

### `list_extensions`

```matha
def list_extensions(self, kind: Optional[str]) -> list[dict]
```

**描述**: 列出所有扩展。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kind` | `Optional[str]` | |

**返回**: `list[dict]`

Source: `src\growth.py:82`

---

### `add_callback`

```matha
def add_callback(self, event: str, callback: Callable) -> None
```

**描述**: 注册事件回调。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `event` | `str` | |
| `callback` | `Callable` | |

**返回**: `None`

Source: `src\growth.py:95`

---

### `fire`

```matha
def fire(self, event: str, **kwargs) -> None
```

**描述**: 触发事件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `event` | `str` | |

**返回**: `None`

Source: `src\growth.py:101`

---

## `DiagnosticResult`

```matha
class DiagnosticResult
```

**描述**: 诊断结果。

**装饰器**: dataclass

Source: `src\growth.py:115`

---

## `SelfDiagnostic`

```matha
class SelfDiagnostic
```

**描述**: 自诊断引擎：分析代码质量、性能瓶颈、潜在问题。

Source: `src\growth.py:124`

---

### `__init__`

```matha
def __init__(self, registry: ExtensionRegistry) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `registry` | `ExtensionRegistry` | |

**返回**: `None`

Source: `src\growth.py:127`

---

### `diagnose`

```matha
def diagnose(self, source: str) -> list[DiagnosticResult]
```

**描述**: 运行所有诊断检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list[DiagnosticResult]`

Source: `src\growth.py:131`

---

### `_check_syntax`

```matha
def _check_syntax(self, source: str) -> list[DiagnosticResult]
```

**描述**: 语法检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list[DiagnosticResult]`

Source: `src\growth.py:156`

---

### `_check_performance`

```matha
def _check_performance(self, source: str) -> list[DiagnosticResult]
```

**描述**: 性能检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list[DiagnosticResult]`

Source: `src\growth.py:172`

---

### `_check_quality`

```matha
def _check_quality(self, source: str) -> list[DiagnosticResult]
```

**描述**: 代码质量检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list[DiagnosticResult]`

Source: `src\growth.py:193`

---

### `_check_extensions`

```matha
def _check_extensions(self) -> list[DiagnosticResult]
```

**描述**: 扩展可用性检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[DiagnosticResult]`

Source: `src\growth.py:209`

---

### `get_summary`

```matha
def get_summary(self) -> dict
```

**描述**: 获取诊断摘要。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\growth.py:235`

---

## `Modification`

```matha
class Modification
```

**描述**: 代码修改单元。

**装饰器**: dataclass

Source: `src\growth.py:253`

---

## `SelfModifier`

```matha
class SelfModifier
```

**描述**: 自修改引擎：安全地修改自身代码。

Source: `src\growth.py:263`

---

### `__init__`

```matha
def __init__(self, registry: ExtensionRegistry, diagnostic: SelfDiagnostic) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `registry` | `ExtensionRegistry` | |
| `diagnostic` | `SelfDiagnostic` | |

**返回**: `None`

Source: `src\growth.py:266`

---

### `propose_fix`

```matha
def propose_fix(self, diagnostic: DiagnosticResult) -> Optional[Modification]
```

**描述**: 根据诊断结果提出修改建议。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `diagnostic` | `DiagnosticResult` | |

**返回**: `Optional[Modification]`

Source: `src\growth.py:272`

---

### `_propose_enable_pass`

```matha
def _propose_enable_pass(self, message: str) -> Optional[Modification]
```

**描述**: 提议启用优化 Pass。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |

**返回**: `Optional[Modification]`

Source: `src\growth.py:283`

---

### `apply`

```matha
def apply(self, modification: Modification) -> bool
```

**描述**: 应用修改。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `modification` | `Modification` | |

**返回**: `bool`

Source: `src\growth.py:299`

---

### `rollback`

```matha
def rollback(self, modification: Modification) -> None
```

**描述**: 回滚修改。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `modification` | `Modification` | |

**返回**: `None`

Source: `src\growth.py:316`

---

### `_sandbox_validate`

```matha
def _sandbox_validate(self, modification: Modification) -> bool
```

**描述**: 在沙箱中验证修改。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `modification` | `Modification` | |

**返回**: `bool`

Source: `src\growth.py:321`

---

### `_execute_modification`

```matha
def _execute_modification(self, modification: Modification) -> None
```

**描述**: 执行修改。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `modification` | `Modification` | |

**返回**: `None`

Source: `src\growth.py:326`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取修改统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\growth.py:332`

---

## `GrowthState`

```matha
class GrowthState
```

**描述**: 成长状态。

**装饰器**: dataclass

Source: `src\growth.py:346`

---

## `GrowthLoop`

```matha
class GrowthLoop
```

**描述**: 成长循环：自动执行诊断→修改→验证→迭代。

Source: `src\growth.py:354`

---

### `__init__`

```matha
def __init__(self, registry: ExtensionRegistry) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `registry` | `ExtensionRegistry` | |

**返回**: `None`

Source: `src\growth.py:357`

---

### `run`

```matha
def run(self, source: str, max_iterations: Optional[int]) -> dict
```

**描述**: 运行成长循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `max_iterations` | `Optional[int]` | |

**返回**: `dict`

Source: `src\growth.py:365`

---

### `enable_pass`

```matha
def enable_pass(self, pass_name: str) -> bool
```

**描述**: 动态启用优化 Pass。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pass_name` | `str` | |

**返回**: `bool`

Source: `src\growth.py:434`

---

### `add_pass`

```matha
def add_pass(self, module: str, class_name: str, description: str) -> bool
```

**描述**: 动态添加优化 Pass。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `module` | `str` | |
| `class_name` | `str` | |
| `description` | `str` | |

**返回**: `bool`

Source: `src\growth.py:442`

---

### `get_state`

```matha
def get_state(self) -> dict
```

**描述**: 获取成长状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\growth.py:454`

---

## `create_growth_system`

```matha
def create_growth_system() -> GrowthLoop
```

**描述**: 创建成长系统。

**返回**: `GrowthLoop`

Source: `src\growth.py:469`

---

## `grow`

```matha
def grow(source: str, verbose: bool) -> dict
```

**描述**: 快捷函数：运行成长循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `verbose` | `bool` | |

**返回**: `dict`

Source: `src\growth.py:495`

---

# `growth_engine`

```matha

```

**描述**: Matha 自主成长引擎 v1.2.18
============================
功能：
  1. 资源完整性检测与自动补齐
  2. 跨功能缺陷联动 / 平衡 / 交互
  3. 自动自检各模块缺陷
  4. 直接调用各功能互相辅助
  5. 网络自动搜索（WebSearch MCP）
  6. 性能/资源不足自动提交成长需求
  7. 成长缺陷自动替换 / 升级回滚

Source: `src\growth_engine.py:1`

---

## `Severity`

```matha
class Severity(Enum)
```

Source: `src\growth_engine.py:36`

---

## `DefectCategory`

```matha
class DefectCategory(Enum)
```

**描述**: 缺陷分类

Source: `src\growth_engine.py:43`

---

## `AuditPhase`

```matha
class AuditPhase(Enum)
```

**描述**: 审计阶段

Source: `src\growth_engine.py:54`

---

## `RemediationAction`

```matha
class RemediationAction(Enum)
```

**描述**: 修复动作

Source: `src\growth_engine.py:63`

---

## `Defect`

```matha
class Defect
```

**描述**: 缺陷记录

**装饰器**: dataclass

Source: `src\growth_engine.py:74`

---

## `ResourceEntry`

```matha
class ResourceEntry
```

**描述**: 资源条目

**装饰器**: dataclass

Source: `src\growth_engine.py:90`

---

## `GrowthReport`

```matha
class GrowthReport
```

**描述**: 成长报告

**装饰器**: dataclass

Source: `src\growth_engine.py:101`

---

## `GrowthEngine`

```matha
class GrowthEngine
```

**描述**: 自主成长引擎 — 自检 + 联动 + 搜索 + 补丁 + 升级

Source: `src\growth_engine.py:118`

---

### `__init__`

```matha
def __init__(self, assistant)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `assistant` | `` | |

Source: `src\growth_engine.py:123`

---

### `_build_resource_spec`

```matha
def _build_resource_spec(self) -> list[dict]
```

**描述**: 构建资源审计清单。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\growth_engine.py:140`

---

### `_check_keywords`

```matha
def _check_keywords(self, kwargs: dict) -> tuple[bool, str]
```

**描述**: 检查关键词覆盖率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kwargs` | `dict` | |

**返回**: `tuple[bool, str]`

Source: `src\growth_engine.py:162`

---

### `_check_variations`

```matha
def _check_variations(self, kwargs: dict) -> tuple[bool, str]
```

**描述**: 检查变体表达覆盖率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kwargs` | `dict` | |

**返回**: `tuple[bool, str]`

Source: `src\growth_engine.py:172`

---

### `_check_rules`

```matha
def _check_rules(self, kwargs: dict) -> tuple[bool, str]
```

**描述**: 检查常识规则覆盖率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kwargs` | `dict` | |

**返回**: `tuple[bool, str]`

Source: `src\growth_engine.py:182`

---

### `_check_concepts`

```matha
def _check_concepts(self, kwargs: dict) -> tuple[bool, str]
```

**描述**: 检查数学概念覆盖率。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kwargs` | `dict` | |

**返回**: `tuple[bool, str]`

Source: `src\growth_engine.py:192`

---

### `identify_concept_gaps`

```matha
def identify_concept_gaps(self) -> list[str]
```

**描述**: 识别数学概念知识库中的缺失项，返回建议补充的概念名列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\growth_engine.py:202`

---

### `_check_growth`

```matha
def _check_growth(self, kwargs: dict) -> tuple[bool, str]
```

**描述**: 检查成长系统完整性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kwargs` | `dict` | |

**返回**: `tuple[bool, str]`

Source: `src\growth_engine.py:232`

---

### `_check_security`

```matha
def _check_security(self, kwargs: dict) -> tuple[bool, str]
```

**描述**: 检查网络安全引擎。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kwargs` | `dict` | |

**返回**: `tuple[bool, str]`

Source: `src\growth_engine.py:243`

---

### `_check_firewall`

```matha
def _check_firewall(self, kwargs: dict) -> tuple[bool, str]
```

**描述**: 检查防火墙系统。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `kwargs` | `dict` | |

**返回**: `tuple[bool, str]`

Source: `src\growth_engine.py:253`

---

### `audit_resources`

```matha
def audit_resources(self) -> list[ResourceEntry]
```

**描述**: 完整资源审计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[ResourceEntry]`

Source: `src\growth_engine.py:263`

---

### `_add_defect`

```matha
def _add_defect(self, category: DefectCategory, severity: Severity, message: str, source: str) -> Defect
```

**描述**: 添加缺陷记录。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `category` | `DefectCategory` | |
| `severity` | `Severity` | |
| `message` | `str` | |
| `source` | `str` | |

**返回**: `Defect`

Source: `src\growth_engine.py:300`

---

### `_resolve_defect`

```matha
def _resolve_defect(self, defect_id: str, resolved_by: str, patch_code: str, action: RemediationAction) -> None
```

**描述**: 解决缺陷。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `defect_id` | `str` | |
| `resolved_by` | `str` | |
| `patch_code` | `str` | |
| `action` | `RemediationAction` | |

**返回**: `None`

Source: `src\growth_engine.py:325`

---

### `get_defects`

```matha
def get_defects(self, status: str, severity: Severity) -> list[Defect]
```

**描述**: 获取缺陷列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `status` | `str` | |
| `severity` | `Severity` | |

**返回**: `list[Defect]`

Source: `src\growth_engine.py:348`

---

### `get_defect_stats`

```matha
def get_defect_stats(self) -> dict
```

**描述**: 获取缺陷统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\growth_engine.py:355`

---

### `link_defects`

```matha
def link_defects(self, id1: str, id2: str) -> bool
```

**描述**: 建立两个缺陷之间的关联。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `id1` | `str` | |
| `id2` | `str` | |

**返回**: `bool`

Source: `src\growth_engine.py:375`

---

### `auto_link_related`

```matha
def auto_link_related(self, defect: Defect, all_defects: list[Defect]) -> list[str]
```

**描述**: 自动识别并关联相关缺陷。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `defect` | `Defect` | |
| `all_defects` | `list[Defect]` | |

**返回**: `list[str]`

Source: `src\growth_engine.py:395`

---

### `balance_defects`

```matha
def balance_defects(self, defect: Defect) -> list[str]
```

**描述**: 对高严重度缺陷进行跨功能平衡（关联其他功能发现类似问题）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `defect` | `Defect` | |

**返回**: `list[str]`

Source: `src\growth_engine.py:417`

---

### `self_diagnose`

```matha
def self_diagnose(self) -> list[Defect]
```

**描述**: 全面自检所有模块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[Defect]`

Source: `src\growth_engine.py:450`

---

### `diagnose_intent_parser`

```matha
def diagnose_intent_parser(self) -> None
```

**描述**: 自检意图解析器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth_engine.py:464`

---

### `diagnose_interpreter`

```matha
def diagnose_interpreter(self) -> None
```

**描述**: 自检解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth_engine.py:496`

---

### `diagnose_firewall`

```matha
def diagnose_firewall(self) -> None
```

**描述**: 自检防火墙。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth_engine.py:523`

---

### `diagnose_security`

```matha
def diagnose_security(self) -> None
```

**描述**: 自检网络安全引擎。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth_engine.py:538`

---

### `diagnose_growth`

```matha
def diagnose_growth(self) -> None
```

**描述**: 自检成长系统。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth_engine.py:564`

---

### `diagnose_cross_function`

```matha
def diagnose_cross_function(self) -> None
```

**描述**: 自检跨功能联动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth_engine.py:587`

---

### `web_search`

```matha
def web_search(self, query: str) -> Optional[str]
```

**描述**: 通过网络搜索获取补丁方案（优先使用 MCP WebSearch）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `query` | `str` | |

**返回**: `Optional[str]`

Source: `src\growth_engine.py:614`

---

### `search_and_patch`

```matha
def search_and_patch(self, defect: Defect) -> Optional[str]
```

**描述**: 搜索缺陷的补丁方案并生成补丁。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `defect` | `Defect` | |

**返回**: `Optional[str]`

Source: `src\growth_engine.py:653`

---

### `_generate_patch_from_search`

```matha
def _generate_patch_from_search(self, search_result: str, defect: Defect) -> str
```

**描述**: 从搜索结果生成补丁代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `search_result` | `str` | |
| `defect` | `Defect` | |

**返回**: `str`

Source: `src\growth_engine.py:668`

---

### `generate_patch`

```matha
def generate_patch(self, defect: Defect) -> Optional[str]
```

**描述**: 根据缺陷自动生成补丁代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `defect` | `Defect` | |

**返回**: `Optional[str]`

Source: `src\growth_engine.py:680`

---

### `auto_expand_concepts`

```matha
def auto_expand_concepts(self, max_new: int) -> int
```

**描述**: 自动扩充 MATH_CONCEPTS 知识库。返回新增概念数量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_new` | `int` | |

**返回**: `int`

Source: `src\growth_engine.py:727`

---

### `auto_expand_keywords`

```matha
def auto_expand_keywords(self) -> int
```

**描述**: 自动扩充 KEYWORD_MAP 中的关键词。返回新增数量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\growth_engine.py:760`

---

### `run_upgrade_pipeline`

```matha
def run_upgrade_pipeline(self, patch_code: str, verify_fn) -> bool
```

**描述**: 执行升级管道：应用补丁 → 验证 → 失败则回滚。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `patch_code` | `str` | |
| `verify_fn` | `` | |

**返回**: `bool`

Source: `src\growth_engine.py:785`

---

### `_sandbox_verify`

```matha
def _sandbox_verify(self, patch_code: str) -> bool
```

**描述**: 沙箱内验证补丁语法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `patch_code` | `str` | |

**返回**: `bool`

Source: `src\growth_engine.py:840`

---

### `_apply_patch`

```matha
def _apply_patch(self, patch_code: str) -> bool
```

**描述**: 应用补丁（模拟）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `patch_code` | `str` | |

**返回**: `bool`

Source: `src\growth_engine.py:848`

---

### `_rollback_patch`

```matha
def _rollback_patch(self) -> None
```

**描述**: 回滚补丁。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\growth_engine.py:854`

---

### `_verify_after_patch`

```matha
def _verify_after_patch(self) -> bool
```

**描述**: 补丁后验证。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\growth_engine.py:858`

---

### `auto_remediate`

```matha
def auto_remediate(self, defect: Defect) -> bool
```

**描述**: 自动修复缺陷（按严重度选择策略）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `defect` | `Defect` | |

**返回**: `bool`

Source: `src\growth_engine.py:870`

---

### `auto_remediate_by_message`

```matha
def auto_remediate_by_message(self, message: str, category: str) -> bool
```

**描述**: 根据消息内容查找并修复对应缺陷（内循环调用接口）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `category` | `str` | |

**返回**: `bool`

Source: `src\growth_engine.py:924`

---

### `run_growth_cycle`

```matha
def run_growth_cycle(self, max_iterations: int) -> GrowthReport
```

**描述**: 执行完整成长循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_iterations` | `int` | |

**返回**: `GrowthReport`

Source: `src\growth_engine.py:940`

---

### `get_growth_stats`

```matha
def get_growth_stats(self) -> dict
```

**描述**: 返回成长统计（含引擎数据）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\growth_engine.py:1016`

---

### `trigger_growth`

```matha
def trigger_growth(self) -> dict
```

**描述**: 触发一次完整成长循环并返回报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\growth_engine.py:1033`

---

## `create_growth_engine`

```matha
def create_growth_engine(assistant) -> GrowthEngine
```

**描述**: 创建成长引擎实例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `assistant` | `` | |

**返回**: `GrowthEngine`

Source: `src\growth_engine.py:1043`

---

## `run_growth_cycle`

```matha
def run_growth_cycle(assistant, max_iterations: int) -> dict
```

**描述**: 快捷入口：运行一次成长循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `assistant` | `` | |
| `max_iterations` | `int` | |

**返回**: `dict`

Source: `src\growth_engine.py:1047`

---

# `hal`

```matha

```

**描述**: Matha 硬件抽象层 HAL：真实驱动 + 跨平台 + GPU 加速。

Source: `src\hal.py:1`

---

## `Platform`

```matha
class Platform(Enum)
```

Source: `src\hal.py:19`

---

## `detect_platform`

```matha
def detect_platform() -> Platform
```

**返回**: `Platform`

Source: `src\hal.py:27`

---

## `GPIOInterface`

```matha
class GPIOInterface
```

**描述**: GPIO 抽象接口。

Source: `src\hal.py:45`

---

### `setup`

```matha
def setup(self, pin: int, mode: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\hal.py:48`

---

### `output`

```matha
def output(self, pin: int, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\hal.py:51`

---

### `input`

```matha
def input(self, pin: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |

**返回**: `int`

Source: `src\hal.py:54`

---

### `PWM`

```matha
def PWM(self, pin: int, frequency: float, duty: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `frequency` | `float` | |
| `duty` | `float` | |

**返回**: `None`

Source: `src\hal.py:57`

---

### `cleanup`

```matha
def cleanup(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:60`

---

## `WindowsGPIO`

```matha
class WindowsGPIO(GPIOInterface)
```

**描述**: Windows GPIO 仿真（通过虚拟引脚）。

Source: `src\hal.py:64`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:67`

---

### `setup`

```matha
def setup(self, pin: int, mode: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\hal.py:70`

---

### `output`

```matha
def output(self, pin: int, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\hal.py:75`

---

### `input`

```matha
def input(self, pin: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |

**返回**: `int`

Source: `src\hal.py:80`

---

### `PWM`

```matha
def PWM(self, pin: int, frequency: float, duty: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `frequency` | `float` | |
| `duty` | `float` | |

**返回**: `None`

Source: `src\hal.py:85`

---

### `cleanup`

```matha
def cleanup(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:90`

---

## `LinuxARMGPIO`

```matha
class LinuxARMGPIO(GPIOInterface)
```

**描述**: Linux ARM GPIO（Raspberry Pi）。

Source: `src\hal.py:94`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:97`

---

### `_try_import`

```matha
def _try_import(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:102`

---

### `setup`

```matha
def setup(self, pin: int, mode: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\hal.py:111`

---

### `output`

```matha
def output(self, pin: int, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\hal.py:118`

---

### `input`

```matha
def input(self, pin: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |

**返回**: `int`

Source: `src\hal.py:124`

---

### `PWM`

```matha
def PWM(self, pin: int, frequency: float, duty: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `frequency` | `float` | |
| `duty` | `float` | |

**返回**: `None`

Source: `src\hal.py:130`

---

### `cleanup`

```matha
def cleanup(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:137`

---

## `SerialDriver`

```matha
class SerialDriver
```

**描述**: UART/Serial 驱动。

Source: `src\hal.py:148`

---

### `__init__`

```matha
def __init__(self, port: str, baudrate: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port` | `str` | |
| `baudrate` | `int` | |

**返回**: `None`

Source: `src\hal.py:151`

---

### `_try_open`

```matha
def _try_open(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:157`

---

### `write`

```matha
def write(self, data: bytes) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `bytes` | |

**返回**: `int`

Source: `src\hal.py:167`

---

### `read`

```matha
def read(self, size: int) -> bytes
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `size` | `int` | |

**返回**: `bytes`

Source: `src\hal.py:172`

---

### `readline`

```matha
def readline(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\hal.py:177`

---

### `close`

```matha
def close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:182`

---

## `I2CDriver`

```matha
class I2CDriver
```

**描述**: I2C 总线驱动。

Source: `src\hal.py:192`

---

### `__init__`

```matha
def __init__(self, bus: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bus` | `int` | |

**返回**: `None`

Source: `src\hal.py:195`

---

### `write_byte`

```matha
def write_byte(self, addr: int, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\hal.py:205`

---

### `read_byte`

```matha
def read_byte(self, addr: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |

**返回**: `int`

Source: `src\hal.py:209`

---

### `scan`

```matha
def scan(self) -> list[int]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[int]`

Source: `src\hal.py:214`

---

### `close`

```matha
def close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:220`

---

## `ADCDriver`

```matha
class ADCDriver
```

**描述**: ADC 模数转换驱动。

Source: `src\hal.py:229`

---

### `__init__`

```matha
def __init__(self, channel: int, ref_voltage: float, bits: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `channel` | `int` | |
| `ref_voltage` | `float` | |
| `bits` | `int` | |

**返回**: `None`

Source: `src\hal.py:232`

---

### `read`

```matha
def read(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\hal.py:249`

---

### `read_voltage`

```matha
def read_voltage(self) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\hal.py:255`

---

## `TemperatureSensor`

```matha
class TemperatureSensor
```

**描述**: 温度传感器（DS18B20 / DHT11 / BME280）。

Source: `src\hal.py:264`

---

### `__init__`

```matha
def __init__(self, sensor_type: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sensor_type` | `str` | |

**返回**: `None`

Source: `src\hal.py:267`

---

### `read`

```matha
def read(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\hal.py:277`

---

## `DistanceSensor`

```matha
class DistanceSensor
```

**描述**: 超声波测距传感器（HC-SR04）。

Source: `src\hal.py:289`

---

### `__init__`

```matha
def __init__(self, trigger_pin: int, echo_pin: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `trigger_pin` | `int` | |
| `echo_pin` | `int` | |

**返回**: `None`

Source: `src\hal.py:292`

---

### `read`

```matha
def read(self) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\hal.py:297`

---

## `GPUSupport`

```matha
class GPUSupport
```

**描述**: CUDA GPU 加速支持。

Source: `src\hal.py:326`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:329`

---

### `_try_detect`

```matha
def _try_detect(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:334`

---

### `available`

```matha
def available(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\hal.py:348`

---

### `devices`

```matha
def devices(self) -> list[dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

**装饰器**: property

Source: `src\hal.py:352`

---

### `to_gpu`

```matha
def to_gpu(self, array: list) -> Any
```

**描述**: 将数组传输到 GPU。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `array` | `list` | |

**返回**: `Any`

Source: `src\hal.py:355`

---

### `to_cpu`

```matha
def to_cpu(self, gpu_array: Any) -> list
```

**描述**: 将数组从 GPU 传回 CPU。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `gpu_array` | `Any` | |

**返回**: `list`

Source: `src\hal.py:362`

---

## `HALConfig`

```matha
class HALConfig
```

**描述**: HAL 配置。

**装饰器**: dataclass

Source: `src\hal.py:375`

---

## `HardwareAbstractionLayer`

```matha
class HardwareAbstractionLayer
```

**描述**: 硬件抽象层：统一 API 跨平台。

Source: `src\hal.py:384`

---

### `__init__`

```matha
def __init__(self, config: Optional[HALConfig]) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `config` | `Optional[HALConfig]` | |

**返回**: `None`

Source: `src\hal.py:387`

---

### `_init_drivers`

```matha
def _init_drivers(self) -> None
```

**描述**: 根据平台初始化驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:398`

---

### `gpio_setup`

```matha
def gpio_setup(self, pin: int, mode: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\hal.py:412`

---

### `gpio_write`

```matha
def gpio_write(self, pin: int, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\hal.py:416`

---

### `gpio_read`

```matha
def gpio_read(self, pin: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |

**返回**: `int`

Source: `src\hal.py:420`

---

### `gpio_pwm`

```matha
def gpio_pwm(self, pin: int, freq: float, duty: float) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |
| `freq` | `float` | |
| `duty` | `float` | |

**返回**: `None`

Source: `src\hal.py:425`

---

### `gpio_cleanup`

```matha
def gpio_cleanup(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:429`

---

### `serial_write`

```matha
def serial_write(self, data: bytes) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `bytes` | |

**返回**: `int`

Source: `src\hal.py:434`

---

### `serial_read`

```matha
def serial_read(self, size: int) -> bytes
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `size` | `int` | |

**返回**: `bytes`

Source: `src\hal.py:439`

---

### `serial_close`

```matha
def serial_close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:444`

---

### `i2c_write`

```matha
def i2c_write(self, addr: int, value: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |
| `value` | `int` | |

**返回**: `None`

Source: `src\hal.py:449`

---

### `i2c_read`

```matha
def i2c_read(self, addr: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |

**返回**: `int`

Source: `src\hal.py:453`

---

### `i2c_scan`

```matha
def i2c_scan(self) -> list[int]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[int]`

Source: `src\hal.py:458`

---

### `i2c_close`

```matha
def i2c_close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:463`

---

### `adc_read`

```matha
def adc_read(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\hal.py:468`

---

### `adc_voltage`

```matha
def adc_voltage(self) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\hal.py:472`

---

### `temperature`

```matha
def temperature(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\hal.py:478`

---

### `distance`

```matha
def distance(self) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\hal.py:483`

---

### `gpu_available`

```matha
def gpu_available(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\hal.py:491`

---

### `gpu_devices`

```matha
def gpu_devices(self) -> list[dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

**装饰器**: property

Source: `src\hal.py:495`

---

### `to_gpu`

```matha
def to_gpu(self, data: list) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `list` | |

**返回**: `Any`

Source: `src\hal.py:498`

---

### `to_cpu`

```matha
def to_cpu(self, gpu_data: Any) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `gpu_data` | `Any` | |

**返回**: `list`

Source: `src\hal.py:503`

---

### `platform_info`

```matha
def platform_info(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\hal.py:509`

---

### `cleanup`

```matha
def cleanup(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hal.py:519`

---

## `hal`

```matha
def hal() -> HardwareAbstractionLayer
```

**描述**: 获取 HAL 单例。

**返回**: `HardwareAbstractionLayer`

Source: `src\hal.py:532`

---

## `register_hardware_builtins`

```matha
def register_hardware_builtins(builtins: dict) -> None
```

**描述**: 将 HAL 函数注册为 Matha 内建。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\hal.py:540`

---

# `hardware.benchmark`

```matha

```

**描述**: Matha HAL 高频操作性能分析与优化方案

基于 1000 次 GPIO 写入的性能测试，分析瓶颈并给出优化建议。

Source: `src\hardware\benchmark.py:1`

---

## `benchmark_with_logging`

```matha
def benchmark_with_logging()
```

**描述**: 基准测试：带日志的高频操作。

Source: `src\hardware\benchmark.py:11`

---

## `benchmark_without_logging`

```matha
def benchmark_without_logging()
```

**描述**: 基准测试：不带日志的高频操作。

Source: `src\hardware\benchmark.py:55`

---

## `benchmark_batch_write`

```matha
def benchmark_batch_write()
```

**描述**: 批量写入测试。

Source: `src\hardware\benchmark.py:84`

---

## `analyze_bottlenecks`

```matha
def analyze_bottlenecks()
```

**描述**: 分析性能瓶颈。

Source: `src\hardware\benchmark.py:114`

---

# `hardware.hal`

```matha

```

**描述**: Matha v4.0 — 硬件抽象层（HAL）

设计原则：
  1. 统一接口：所有硬件操作通过标准 I/O 接口
  2. 抽象隔离：Matha 代码不依赖具体硬件实现
  3. 插件扩展：新硬件通过插件注册
  4. 安全沙箱：硬件操作需要权限验证

架构：
  Matha 代码 → HAL API → 设备驱动 → 实体硬件

Source: `src\hardware\hal.py:1`

---

## `AsyncHALLogger`

```matha
class AsyncHALLogger
```

**描述**: 异步日志记录器（v4.2 批处理优化版）。

使用独立线程异步写入日志，避免阻塞硬件操作。
核心优化：
1. 批量消费：每次从队列中取出多条日志一次性写入，减少 I/O 次数
2. 自适应批次大小：根据负载动态调整批次大小
3. 低优先级缓冲：非紧急日志可延迟批量刷新

生产环境下默认关闭，仅 DEBUG 级别时输出。

Source: `src\hardware\hal.py:32`

---

### `__init__`

```matha
def __init__(self, name: str, maxsize: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `maxsize` | `int` | |

Source: `src\hardware\hal.py:52`

---

### `start`

```matha
def start(self)
```

**描述**: 启动异步日志线程。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:65`

---

### `stop`

```matha
def stop(self)
```

**描述**: 停止异步日志线程，确保刷新剩余日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:77`

---

### `_flush_remaining`

```matha
def _flush_remaining(self)
```

**描述**: 刷新队列中剩余的所有日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:90`

---

### `_flush_batch`

```matha
def _flush_batch(self)
```

**描述**: 后台线程：批量异步刷新日志。

优化策略：
- 批量消费：每次取出最多 BATCH_SIZE 条日志
- 自适应调整：根据队列深度动态调整批次大小
- 超时刷新：超过 FLUSH_INTERVAL_MS 强制刷新

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:102`

---

### `_write_batch`

```matha
def _write_batch(self, batch: list)
```

**描述**: 批量写入日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `batch` | `list` | |

Source: `src\hardware\hal.py:136`

---

### `_adjust_batch_size`

```matha
def _adjust_batch_size(self, actual_batch_size: int)
```

**描述**: 根据实际批次大小自适应调整。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `actual_batch_size` | `int` | |

Source: `src\hardware\hal.py:151`

---

### `_should_drop`

```matha
def _should_drop(self) -> bool
```

**描述**: 判断是否应该丢弃日志（高负载保护）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\hardware\hal.py:160`

---

### `info`

```matha
def info(self, msg: str, *args)
```

**描述**: 异步记录 INFO 日志（批量优化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

Source: `src\hardware\hal.py:165`

---

### `debug`

```matha
def debug(self, msg: str, *args)
```

**描述**: 异步记录 DEBUG 日志（批量优化）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

Source: `src\hardware\hal.py:179`

---

### `warning`

```matha
def warning(self, msg: str, *args)
```

**描述**: 异步记录 WARNING 日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

Source: `src\hardware\hal.py:189`

---

### `error`

```matha
def error(self, msg: str, *args)
```

**描述**: 异步记录 ERROR 日志（不丢弃）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

Source: `src\hardware\hal.py:199`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取日志统计信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\hardware\hal.py:211`

---

## `_gpio_writer_worker`

```matha
def _gpio_writer_worker(worker_id: int, pin: int, iterations: int, result_queue: mp.Queue)
```

**描述**: GPIO 写入 Worker（进程级，模块顶层定义以支持序列化）。
每个进程拥有独立 HAL 实例，无 GIL 竞争。

| 参数 | 类型 | 说明 |
|------|------|------|
| `worker_id` | `int` | |
| `pin` | `int` | |
| `iterations` | `int` | |
| `result_queue` | `mp.Queue` | |

Source: `src\hardware\hal.py:231`

---

## `_gpio_batch_writer_worker`

```matha
def _gpio_batch_writer_worker(worker_id: int, pins: List[int], iterations: int, result_queue: mp.Queue)
```

**描述**: 批量 GPIO 写入 Worker（进程级）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `worker_id` | `int` | |
| `pins` | `List[int]` | |
| `iterations` | `int` | |
| `result_queue` | `mp.Queue` | |

Source: `src\hardware\hal.py:277`

---

## `run_multiprocess_stress_test`

```matha
def run_multiprocess_stress_test(num_workers: int, pin: int, iterations_per_worker: int, target_frequency: int, use_batch: bool, batch_pins: Optional[List[int]]) -> Dict[str, Any]
```

**描述**: 运行 multiprocessing 压力测试（绕过 GIL）。

Args:
    num_workers: Worker 进程数
    pin: GPIO 引脚
    iterations_per_worker: 每个 Worker 迭代次数
    target_frequency: 目标频率 (Hz)
    use_batch: 是否批量写入
    batch_pins: 批量写入引脚列表

Returns:
    性能摘要字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_workers` | `int` | |
| `pin` | `int` | |
| `iterations_per_worker` | `int` | |
| `target_frequency` | `int` | |
| `use_batch` | `bool` | |
| `batch_pins` | `Optional[List[int]]` | |

**返回**: `Dict[str, Any]`

Source: `src\hardware\hal.py:322`

---

## `DeviceType`

```matha
class DeviceType(Enum)
```

**描述**: 硬件设备类型。

Source: `src\hardware\hal.py:391`

---

## `DeviceState`

```matha
class DeviceState(Enum)
```

**描述**: 设备状态。

Source: `src\hardware\hal.py:402`

---

## `DeviceConfig`

```matha
class DeviceConfig
```

**描述**: 设备配置。

**装饰器**: dataclass

Source: `src\hardware\hal.py:415`

---

## `IODevice`

```matha
class IODevice(abc.ABC)
```

**描述**: I/O 设备抽象基类。

所有硬件设备必须继承此类并实现 read/write 方法。

Source: `src\hardware\hal.py:424`

---

### `__init__`

```matha
def __init__(self, config: DeviceConfig)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `config` | `DeviceConfig` | |

Source: `src\hardware\hal.py:431`

---

### `read`

```matha
def read(self) -> Any
```

**描述**: 从设备读取数据。

Returns:
    读取的数据，失败返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\hardware\hal.py:440`

---

### `write`

```matha
def write(self, value: Any) -> bool
```

**描述**: 向设备写入数据。

Returns:
    是否写入成功

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |

**返回**: `bool`

Source: `src\hardware\hal.py:460`

---

### `configure`

```matha
def configure(self, **kwargs) -> bool
```

**描述**: 配置设备参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\hardware\hal.py:481`

---

### `online`

```matha
def online(self) -> bool
```

**描述**: 上线设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\hardware\hal.py:487`

---

### `offline`

```matha
def offline(self) -> bool
```

**描述**: 下线设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\hardware\hal.py:492`

---

### `reset`

```matha
def reset(self) -> bool
```

**描述**: 重置设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\hardware\hal.py:497`

---

### `name`

```matha
def name(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

**装饰器**: property

Source: `src\hardware\hal.py:506`

---

### `address`

```matha
def address(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

**装饰器**: property

Source: `src\hardware\hal.py:510`

---

### `device_type`

```matha
def device_type(self) -> DeviceType
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `DeviceType`

**装饰器**: property

Source: `src\hardware\hal.py:514`

---

### `access_count`

```matha
def access_count(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\hardware\hal.py:518`

---

## `ScreenDevice`

```matha
class ScreenDevice(IODevice)
```

**描述**: 屏幕输出设备。

Source: `src\hardware\hal.py:526`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:529`

---

### `_print_to_screen`

```matha
def _print_to_screen(self, value: Any)
```

**描述**: 输出到屏幕。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |

Source: `src\hardware\hal.py:537`

---

## `KeyboardDevice`

```matha
class KeyboardDevice(IODevice)
```

**描述**: 键盘输入设备。

Source: `src\hardware\hal.py:542`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:545`

---

### `_read_from_keyboard`

```matha
def _read_from_keyboard(self) -> str
```

**描述**: 从键盘读取。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\hardware\hal.py:553`

---

## `FileDevice`

```matha
class FileDevice(IODevice)
```

**描述**: 文件存储设备。

Source: `src\hardware\hal.py:558`

---

### `__init__`

```matha
def __init__(self, base_path: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `base_path` | `str` | |

Source: `src\hardware\hal.py:561`

---

### `read`

```matha
def read(self, path: str) -> Optional[str]
```

**描述**: 读取文件内容。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `Optional[str]`

Source: `src\hardware\hal.py:568`

---

### `write`

```matha
def write(self, content: str, path: str) -> bool
```

**描述**: 写入文件内容。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `content` | `str` | |
| `path` | `str` | |

**返回**: `bool`

Source: `src\hardware\hal.py:578`

---

## `NetworkDevice`

```matha
class NetworkDevice(IODevice)
```

**描述**: 网络设备。

Source: `src\hardware\hal.py:590`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:593`

---

### `http_get`

```matha
def http_get(self, url: str) -> Optional[str]
```

**描述**: HTTP GET 请求。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `url` | `str` | |

**返回**: `Optional[str]`

Source: `src\hardware\hal.py:600`

---

### `http_post`

```matha
def http_post(self, url: str, data: dict) -> Optional[str]
```

**描述**: HTTP POST 请求。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `url` | `str` | |
| `data` | `dict` | |

**返回**: `Optional[str]`

Source: `src\hardware\hal.py:609`

---

## `SerialDevice`

```matha
class SerialDevice(IODevice)
```

**描述**: 串口通信设备。

用于连接 Arduino、传感器、模块等串口设备。

Source: `src\hardware\hal.py:619`

---

### `__init__`

```matha
def __init__(self, port: str, baudrate: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port` | `str` | |
| `baudrate` | `int` | |

Source: `src\hardware\hal.py:626`

---

### `_open`

```matha
def _open(self)
```

**描述**: 打开串口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:635`

---

### `_close`

```matha
def _close(self)
```

**描述**: 关闭串口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:648`

---

### `read`

```matha
def read(self) -> Optional[str]
```

**描述**: 从串口读取数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[str]`

Source: `src\hardware\hal.py:655`

---

### `write`

```matha
def write(self, value: str) -> bool
```

**描述**: 向串口写入数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `str` | |

**返回**: `bool`

Source: `src\hardware\hal.py:674`

---

### `__del__`

```matha
def __del__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:690`

---

## `GPIODevice`

```matha
class GPIODevice(IODevice)
```

**描述**: GPIO 设备。

用于控制 GPIO 引脚（如 Raspberry Pi GPIO）。

Source: `src\hardware\hal.py:694`

---

### `__init__`

```matha
def __init__(self, pin: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pin` | `int` | |

Source: `src\hardware\hal.py:701`

---

### `read`

```matha
def read(self) -> Optional[bool]
```

**描述**: 读取 GPIO 引脚状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[bool]`

Source: `src\hardware\hal.py:710`

---

### `write`

```matha
def write(self, value: bool) -> bool
```

**描述**: 写入 GPIO 引脚状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `bool` | |

**返回**: `bool`

Source: `src\hardware\hal.py:725`

---

### `pwm_write`

```matha
def pwm_write(self, value: float)
```

**描述**: 写入 PWM 值（0.0-1.0）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `float` | |

Source: `src\hardware\hal.py:737`

---

## `I2CDevice`

```matha
class I2CDevice(IODevice)
```

**描述**: I2C 设备。

用于连接 I2C 传感器（如温湿度、加速度计等）。

Source: `src\hardware\hal.py:742`

---

### `__init__`

```matha
def __init__(self, address: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `address` | `int` | |

Source: `src\hardware\hal.py:749`

---

### `read`

```matha
def read(self) -> Optional[Dict[str, Any]]
```

**描述**: 从 I2C 设备读取数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[Dict[str, Any]]`

Source: `src\hardware\hal.py:757`

---

### `write`

```matha
def write(self, value: bytes) -> bool
```

**描述**: 向 I2C 设备写入数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `bytes` | |

**返回**: `bool`

Source: `src\hardware\hal.py:762`

---

## `HardwareAbstractionLayer`

```matha
class HardwareAbstractionLayer
```

**描述**: Matha 硬件抽象层（HAL）。

统一管理所有硬件设备，提供标准化的 I/O 接口。

Source: `src\hardware\hal.py:771`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:778`

---

### `_register_builtin_devices`

```matha
def _register_builtin_devices(self)
```

**描述**: 注册内置设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal.py:782`

---

### `register`

```matha
def register(self, device: IODevice)
```

**描述**: 注册设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `device` | `IODevice` | |

Source: `src\hardware\hal.py:789`

---

### `unregister`

```matha
def unregister(self, name: str)
```

**描述**: 注销设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

Source: `src\hardware\hal.py:794`

---

### `get`

```matha
def get(self, name: str) -> Optional[IODevice]
```

**描述**: 获取设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[IODevice]`

Source: `src\hardware\hal.py:800`

---

### `get_by_address`

```matha
def get_by_address(self, address: str) -> Optional[IODevice]
```

**描述**: 按地址获取设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `address` | `str` | |

**返回**: `Optional[IODevice]`

Source: `src\hardware\hal.py:804`

---

### `list_devices`

```matha
def list_devices(self) -> List[Dict]
```

**描述**: 列出所有设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Dict]`

Source: `src\hardware\hal.py:811`

---

### `read`

```matha
def read(self, device_name: str) -> Optional[Any]
```

**描述**: 从设备读取数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `device_name` | `str` | |

**返回**: `Optional[Any]`

Source: `src\hardware\hal.py:824`

---

### `write`

```matha
def write(self, device_name: str, value: Any, **kwargs) -> bool
```

**描述**: 向设备写入数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `device_name` | `str` | |
| `value` | `Any` | |

**返回**: `bool`

Source: `src\hardware\hal.py:831`

---

### `batch_write`

```matha
def batch_write(self, operations: List[tuple]) -> List[bool]
```

**描述**: 批量写入多个设备。

Args:
    operations: 列表，每个元素为 (device_name, value) 元组

Returns:
    每个操作的执行结果列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `operations` | `List[tuple]` | |

**返回**: `List[bool]`

Source: `src\hardware\hal.py:838`

---

## `MathaHardwareOps`

```matha
class MathaHardwareOps
```

**描述**: Matha 硬件操作语法。

提供自然语言到硬件操作的映射。

Source: `src\hardware\hal.py:863`

---

### `__init__`

```matha
def __init__(self, hal: HardwareAbstractionLayer)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `hal` | `HardwareAbstractionLayer` | |

Source: `src\hardware\hal.py:870`

---

### `读取`

```matha
def 读取(self, device_name: str, path: str) -> Optional[Any]
```

**描述**: 从设备读取数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `device_name` | `str` | |
| `path` | `str` | |

**返回**: `Optional[Any]`

Source: `src\hardware\hal.py:873`

---

### `写入`

```matha
def 写入(self, device_name: str, value: Any, path: str) -> bool
```

**描述**: 向设备写入数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `device_name` | `str` | |
| `value` | `Any` | |
| `path` | `str` | |

**返回**: `bool`

Source: `src\hardware\hal.py:881`

---

### `批量写入`

```matha
def 批量写入(self, operations: List[tuple]) -> List[bool]
```

**描述**: 批量写入多个设备。

Matha 语法示例：
    批量写入 [(显示屏, "Hello"), (LED, True)]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `operations` | `List[tuple]` | |

**返回**: `List[bool]`

Source: `src\hardware\hal.py:889`

---

### `打开`

```matha
def 打开(self, device_name: str) -> bool
```

**描述**: 打开设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `device_name` | `str` | |

**返回**: `bool`

Source: `src\hardware\hal.py:898`

---

### `关闭`

```matha
def 关闭(self, device_name: str) -> bool
```

**描述**: 关闭设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `device_name` | `str` | |

**返回**: `bool`

Source: `src\hardware\hal.py:905`

---

### `列出设备`

```matha
def 列出设备(self) -> List[Dict]
```

**描述**: 列出所有设备。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Dict]`

Source: `src\hardware\hal.py:912`

---

# `hardware.hal_multiprocessing`

```matha

```

**描述**: HAL Multiprocessing 并发写入（重导出模块）

本模块从 src.hardware.hal 重导出 multiprocessing worker 函数，
供外部代码方便导入。

用法：
  from src.hardware.hal import (
      run_multiprocess_stress_test,
      _gpio_writer_worker,
      _gpio_batch_writer_worker,
  )
  # 或
  from src.hardware.hal_multiprocessing import (
      run_multiprocess_stress_test,
      gpio_writer_worker,      # 别名
      gpio_batch_writer_worker,
  )

Source: `src\hardware\hal_multiprocessing.py:1`

---

# `hardware.hal_v2`

```matha

```

**描述**: Matha 硬件抽象层 v2.0
=======================
安全副作用 / 指针与内存控制 / 裸机支持 / 协议解释生成器

架构：
  Matha 符号 → 安全副作用引擎 → 指针/内存管理器 → HAL 设备 → 实体硬件/模拟
  Matha 符号 → 原生编译器 → 协议解释器 → 驱动代码生成

功能：
  1. SafeSideEffect  — 可追踪副作用，支持沙箱隔离
  2. PointerManager  — 指针算术、内存分配/释放、越界检测
  3. BareMetalTarget — 裸机编译目标（x86_64/ARM/RISC-V/AVR）
  4. ProtocolParser  — UART/SPI/I2C/CAN 协议解释生成器
  5. DriverGenerator — 构建各类硬件驱动代码

Source: `src\hardware\hal_v2.py:1`

---

## `SideEffectType`

```matha
class SideEffectType(Enum)
```

**描述**: 副作用类型。

Source: `src\hardware\hal_v2.py:37`

---

## `SideEffect`

```matha
class SideEffect
```

**描述**: 副作用描述符。

**装饰器**: dataclass(frozen=True)

Source: `src\hardware\hal_v2.py:49`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal_v2.py:59`

---

## `SafeSideEffectEngine`

```matha
class SafeSideEffectEngine
```

**描述**: 安全副作用引擎。

功能：
  1. 追踪每个函数/表达式的副作用类型
  2. 执行前权限检查（只读/读写/执行）
  3. 沙箱隔离（限制 IO/HARDWARE/SYSTEM 操作）
  4. 副作用审计日志

Source: `src\hardware\hal_v2.py:63`

---

### `__init__`

```matha
def __init__(self, mode: str)
```

**描述**: mode: "sandbox"（沙箱，限制 IO/HARDWARE/SYSTEM）
      "restricted"（受限，允许读写，限制 I/O）
      "full"（全功能，无限制）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mode` | `str` | |

Source: `src\hardware\hal_v2.py:74`

---

### `register_func`

```matha
def register_func(self, name: str, effect_type: SideEffectType, permission: str) -> None
```

**描述**: 注册函数及其副作用类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `effect_type` | `SideEffectType` | |
| `permission` | `str` | |

**返回**: `None`

Source: `src\hardware\hal_v2.py:89`

---

### `check_permission`

```matha
def check_permission(self, func_name: str, required_perm: str) -> bool
```

**描述**: 检查执行权限。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `required_perm` | `str` | |

**返回**: `bool`

Source: `src\hardware\hal_v2.py:97`

---

### `execute_with_check`

```matha
def execute_with_check(self, func: Callable, *args, **kwargs) -> Any
```

**描述**: 在权限检查后执行函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |

**返回**: `Any`

Source: `src\hardware\hal_v2.py:109`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取副作用引擎统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:132`

---

### `_count_by_type`

```matha
def _count_by_type(self) -> Dict[str, int]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, int]`

Source: `src\hardware\hal_v2.py:144`

---

### `clear_audit`

```matha
def clear_audit(self) -> None
```

**描述**: 清空审计日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\hardware\hal_v2.py:151`

---

## `MemoryPage`

```matha
class MemoryPage
```

**描述**: 内存页。

Source: `src\hardware\hal_v2.py:161`

---

### `__init__`

```matha
def __init__(self, page_id: int, base_addr: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `page_id` | `int` | |
| `base_addr` | `int` | |

Source: `src\hardware\hal_v2.py:165`

---

### `read`

```matha
def read(self, offset: int, size: int) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `offset` | `int` | |
| `size` | `int` | |

**返回**: `Any`

Source: `src\hardware\hal_v2.py:173`

---

### `write`

```matha
def write(self, offset: int, value: Any, size: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `offset` | `int` | |
| `value` | `Any` | |
| `size` | `int` | |

**返回**: `None`

Source: `src\hardware\hal_v2.py:183`

---

### `__repr__`

```matha
def __repr__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal_v2.py:195`

---

## `Pointer`

```matha
class Pointer
```

**描述**: 数学指针（带安全检查）。

Source: `src\hardware\hal_v2.py:199`

---

### `__init__`

```matha
def __init__(self, manager: 'PointerManager', addr: int, name: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `manager` | `'PointerManager'` | |
| `addr` | `int` | |
| `name` | `str` | |

Source: `src\hardware\hal_v2.py:202`

---

### `addr`

```matha
def addr(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\hardware\hal_v2.py:210`

---

### `name`

```matha
def name(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

**装饰器**: property

Source: `src\hardware\hal_v2.py:214`

---

### `get`

```matha
def get(self, offset: int, size: int) -> Any
```

**描述**: 读取指针指向的内存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `offset` | `int` | |
| `size` | `int` | |

**返回**: `Any`

Source: `src\hardware\hal_v2.py:217`

---

### `set`

```matha
def set(self, value: Any, offset: int, size: int) -> None
```

**描述**: 写入指针指向的内存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |
| `offset` | `int` | |
| `size` | `int` | |

**返回**: `None`

Source: `src\hardware\hal_v2.py:221`

---

### `plus`

```matha
def plus(self, offset: int) -> 'Pointer'
```

**描述**: 指针算术：返回新指针。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `offset` | `int` | |

**返回**: `'Pointer'`

Source: `src\hardware\hal_v2.py:225`

---

### `deref`

```matha
def deref(self) -> Any
```

**描述**: 解引用：读取指针指向的值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\hardware\hal_v2.py:229`

---

### `__repr__`

```matha
def __repr__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal_v2.py:233`

---

### `__eq__`

```matha
def __eq__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\hardware\hal_v2.py:236`

---

### `__hash__`

```matha
def __hash__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal_v2.py:239`

---

## `PointerManager`

```matha
class PointerManager
```

**描述**: 指针与内存管理器。

功能：
  1. 堆内存分配/释放（带越界检测）
  2. 指针算术（+,-,*,/）
  3. 内存安全检查（只读页写入检测）
  4. 生命周期追踪（防止悬空指针）

Source: `src\hardware\hal_v2.py:243`

---

### `__init__`

```matha
def __init__(self, page_count: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `page_count` | `int` | |

Source: `src\hardware\hal_v2.py:254`

---

### `alloc`

```matha
def alloc(self, size: int, name: str) -> Pointer
```

**描述**: 分配内存，返回 Pointer。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `size` | `int` | |
| `name` | `str` | |

**返回**: `Pointer`

Source: `src\hardware\hal_v2.py:276`

---

### `free`

```matha
def free(self, ptr: Pointer) -> bool
```

**描述**: 释放内存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ptr` | `Pointer` | |

**返回**: `bool`

Source: `src\hardware\hal_v2.py:312`

---

### `read`

```matha
def read(self, addr: int, size: int) -> Any
```

**描述**: 安全读取内存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |
| `size` | `int` | |

**返回**: `Any`

Source: `src\hardware\hal_v2.py:330`

---

### `write`

```matha
def write(self, addr: int, value: Any, size: int) -> None
```

**描述**: 安全写入内存（检查只读页）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `addr` | `int` | |
| `value` | `Any` | |
| `size` | `int` | |

**返回**: `None`

Source: `src\hardware\hal_v2.py:338`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取内存统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:350`

---

## `Architecture`

```matha
class Architecture(Enum)
```

**描述**: 目标架构。

Source: `src\hardware\hal_v2.py:367`

---

## `BareMetalTarget`

```matha
class BareMetalTarget
```

**描述**: 裸机编译目标配置。

**装饰器**: dataclass

Source: `src\hardware\hal_v2.py:380`

---

### `__post_init__`

```matha
def __post_init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal_v2.py:392`

---

### `_default_linker_script`

```matha
def _default_linker_script(self) -> str
```

**描述**: 生成默认链接脚本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:396`

---

## `ProtocolType`

```matha
class ProtocolType(Enum)
```

**描述**: 通信协议类型。

Source: `src\hardware\hal_v2.py:415`

---

## `ProtocolSpec`

```matha
class ProtocolSpec
```

**描述**: 协议规格描述。

**装饰器**: dataclass

Source: `src\hardware\hal_v2.py:427`

---

## `ProtocolParser`

```matha
class ProtocolParser
```

**描述**: 协议解释生成器。

将协议规格解析为可执行的代码片段（Python/C/数学表达式）。

Source: `src\hardware\hal_v2.py:442`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal_v2.py:449`

---

### `parse`

```matha
def parse(self, spec: ProtocolSpec) -> dict
```

**描述**: 解析协议规格，返回代码生成指令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `ProtocolSpec` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:458`

---

### `_parse_uart`

```matha
def _parse_uart(self, spec: ProtocolSpec) -> dict
```

**描述**: UART 协议 → 代码生成指令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `ProtocolSpec` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:465`

---

### `_parse_spi`

```matha
def _parse_spi(self, spec: ProtocolSpec) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `ProtocolSpec` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:495`

---

### `_parse_i2c`

```matha
def _parse_i2c(self, spec: ProtocolSpec) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `ProtocolSpec` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:514`

---

### `_parse_can`

```matha
def _parse_can(self, spec: ProtocolSpec) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `ProtocolSpec` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:535`

---

## `DriverKind`

```matha
class DriverKind(Enum)
```

**描述**: 驱动类型。

Source: `src\hardware\hal_v2.py:559`

---

## `DriverSpec`

```matha
class DriverSpec
```

**描述**: 驱动规格。

**装饰器**: dataclass

Source: `src\hardware\hal_v2.py:571`

---

## `DriverGenerator`

```matha
class DriverGenerator
```

**描述**: 驱动代码生成器。

根据 DriverSpec 生成目标语言代码。

Source: `src\hardware\hal_v2.py:583`

---

### `__init__`

```matha
def __init__(self, protocol_parser: ProtocolParser)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `protocol_parser` | `ProtocolParser` | |

Source: `src\hardware\hal_v2.py:590`

---

### `generate`

```matha
def generate(self, spec: DriverSpec) -> dict
```

**描述**: 生成驱动代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:595`

---

### `_gen_sensor_driver`

```matha
def _gen_sensor_driver(self, spec: DriverSpec) -> str
```

**描述**: 传感器驱动模板。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:629`

---

### `_gen_actuator_driver`

```matha
def _gen_actuator_driver(self, spec: DriverSpec) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:652`

---

### `_gen_math_driver`

```matha
def _gen_math_driver(self, spec: DriverSpec) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:669`

---

### `_gen_comm_driver`

```matha
def _gen_comm_driver(self, spec: DriverSpec) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:678`

---

### `_gen_generic_driver`

```matha
def _gen_generic_driver(self, spec: DriverSpec) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:694`

---

### `list_generated`

```matha
def list_generated(self) -> List[dict]
```

**描述**: 列出已生成的驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[dict]`

Source: `src\hardware\hal_v2.py:711`

---

## `NativeBackend`

```matha
class NativeBackend
```

**描述**: 原生编译后端。

将 Matha 符号表达式编译为各架构的原生代码。

Source: `src\hardware\hal_v2.py:720`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\hardware\hal_v2.py:727`

---

### `register_target`

```matha
def register_target(self, target: BareMetalTarget) -> None
```

**描述**: 注册编译目标。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target` | `BareMetalTarget` | |

**返回**: `None`

Source: `src\hardware\hal_v2.py:732`

---

### `compile`

```matha
def compile(self, expr: str, arch: Architecture, func_name: str, target_lang: str) -> str
```

**描述**: 编译表达式为指定架构的原生代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `arch` | `Architecture` | |
| `func_name` | `str` | |
| `target_lang` | `str` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:737`

---

### `_compile_to_c`

```matha
def _compile_to_c(self, expr: str, func_name: str, target: BareMetalTarget) -> str
```

**描述**: 编译为 C 代码（含裸机头文件）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |
| `target` | `BareMetalTarget` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:761`

---

### `_compile_to_python`

```matha
def _compile_to_python(self, expr: str, func_name: str) -> str
```

**描述**: 编译为 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:781`

---

### `_compile_to_asm`

```matha
def _compile_to_asm(self, expr: str, func_name: str, arch: Architecture) -> str
```

**描述**: 生成汇编代码框架。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |
| `arch` | `Architecture` | |

**返回**: `str`

Source: `src\hardware\hal_v2.py:787`

---

### `get_targets`

```matha
def get_targets(self) -> List[str]
```

**描述**: 列出已注册的目标架构。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

Source: `src\hardware\hal_v2.py:810`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\hardware\hal_v2.py:814`

---

## `get_side_effect_engine`

```matha
def get_side_effect_engine(mode: str) -> SafeSideEffectEngine
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `mode` | `str` | |

**返回**: `SafeSideEffectEngine`

Source: `src\hardware\hal_v2.py:833`

---

## `get_pointer_manager`

```matha
def get_pointer_manager(page_count: int) -> PointerManager
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `page_count` | `int` | |

**返回**: `PointerManager`

Source: `src\hardware\hal_v2.py:840`

---

## `get_protocol_parser`

```matha
def get_protocol_parser() -> ProtocolParser
```

**返回**: `ProtocolParser`

Source: `src\hardware\hal_v2.py:847`

---

## `get_driver_generator`

```matha
def get_driver_generator() -> DriverGenerator
```

**返回**: `DriverGenerator`

Source: `src\hardware\hal_v2.py:854`

---

## `get_native_backend`

```matha
def get_native_backend() -> NativeBackend
```

**返回**: `NativeBackend`

Source: `src\hardware\hal_v2.py:861`

---

## `get_hardware_stats`

```matha
def get_hardware_stats() -> dict
```

**描述**: 获取硬件层全部统计信息。

**返回**: `dict`

Source: `src\hardware\hal_v2.py:873`

---

# `inner_loop`

```matha

```

**描述**: Matha 全功能内循环 v1.2.22
============================
将 Matha 所有功能模块构成一个自我闭环的持续改进系统：

  感知层  →  认知层  →  执行层  →  验证层  →  持久化
     ↑                                                      │
     └──────────────── 反馈循环 ←←←←←←←←←←←←←←←←←←←←←←←←←←←┘

三层自能力：
  • 自扩展：基于交互模式自动扩展知识、意图、关键词
  • 自升级：版本管理、自动补丁、失败回滚
  • 自优化：性能监控、自适应间隔、内存清理

模块集成：
  • 意图解析器 (FriendlyIntentParser)   — 自然语言 → 数学代码
  • 解释器 (Interpreter)               — Matha 代码执行
  • 防火墙 (MathaFirewall)             — 三层权限防护
  • 安全引擎 (NetSecurityEngine)       — 网络威胁检测/清杀
  • 成长引擎 (GrowthEngine)            — 自检/补丁/升级
  • AI助手 (MathaAIAssistant)          — 用户交互入口
  • Web服务 (APIHandler)               — 对外暴露接口

内循环模式：
  - 单次触发：trigger_once()         运行一轮完整内循环
  - 持续监控：start_loop()           后台持续运行（可配置间隔）
  - 事件驱动：on_interaction()       每次用户交互后触发诊断
  - 定时调度：schedule_cycle()       定时执行内循环

Source: `src\inner_loop.py:1`

---

## `LoopState`

```matha
class LoopState
```

**描述**: 内循环运行状态。

**装饰器**: dataclass

Source: `src\inner_loop.py:54`

---

## `InteractionRecord`

```matha
class InteractionRecord
```

**描述**: 单次用户交互记录。

**装饰器**: dataclass

Source: `src\inner_loop.py:72`

---

## `MathaInnerLoop`

```matha
class MathaInnerLoop
```

**描述**: Matha 全功能内循环 — 集成所有模块的持续改进系统。

生命周期：
  1. 感知：收集交互、错误、资源状态
  2. 认知：自检各模块，识别缺陷和差距
  3. 执行：自动修复、扩充资源、跨模块协作
  4. 验证：运行测试，确保修复有效
  5. 持久化：保存状态和成长记录

Source: `src\inner_loop.py:87`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\inner_loop.py:99`

---

### `init_modules`

```matha
def init_modules(self)
```

**描述**: 初始化所有核心模块（v1.3.0 + v2.0 HAL）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\inner_loop.py:125`

---

### `on_interaction`

```matha
def on_interaction(self, text: str, result: dict) -> InteractionRecord
```

**描述**: 记录一次用户交互，触发轻量诊断。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `result` | `dict` | |

**返回**: `InteractionRecord`

Source: `src\inner_loop.py:186`

---

### `collect_resource_snapshot`

```matha
def collect_resource_snapshot(self) -> dict
```

**描述**: 收集当前资源快照。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\inner_loop.py:213`

---

### `collect_interaction_summary`

```matha
def collect_interaction_summary(self) -> dict
```

**描述**: 收集最近交互摘要。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\inner_loop.py:228`

---

### `cognitive_diagnose`

```matha
def cognitive_diagnose(self) -> dict
```

**描述**: 认知层：全面自检，收集缺陷信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\inner_loop.py:245`

---

### `execute_remediation`

```matha
def execute_remediation(self, diagnosis: dict) -> dict
```

**描述**: 执行层：根据诊断结果自动修复。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `diagnosis` | `dict` | |

**返回**: `dict`

Source: `src\inner_loop.py:288`

---

### `_cross_module_collaboration`

```matha
def _cross_module_collaboration(self) -> int
```

**描述**: 跨模块协作：各模块互相调用，验证联动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\inner_loop.py:332`

---

### `_handle_error_buffer`

```matha
def _handle_error_buffer(self) -> int
```

**描述**: 处理错误缓冲区：自动学习失败模式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\inner_loop.py:367`

---

### `_parse_intent_from_text`

```matha
def _parse_intent_from_text(self, text: str)
```

**描述**: 从文本解析意图类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

Source: `src\inner_loop.py:380`

---

### `self_extend_concepts`

```matha
def self_extend_concepts(self) -> int
```

**描述**: 自扩展：基于现有概念种子，从失败交互中派生新概念。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\inner_loop.py:390`

---

### `self_extend_intents`

```matha
def self_extend_intents(self) -> int
```

**描述**: 自扩展：基于交互数据自动发现新意图类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\inner_loop.py:505`

---

### `self_upgrade_check`

```matha
def self_upgrade_check(self) -> dict
```

**描述**: 自升级检查：检测版本状态和可用升级。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\inner_loop.py:540`

---

### `self_upgrade_apply`

```matha
def self_upgrade_apply(self, target_version: str) -> dict
```

**描述**: 自升级：应用补丁并升级到目标版本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target_version` | `str` | |

**返回**: `dict`

Source: `src\inner_loop.py:569`

---

### `self_upgrade_rollback`

```matha
def self_upgrade_rollback(self) -> bool
```

**描述**: 自升级回滚：回滚到最后稳定版本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\inner_loop.py:603`

---

### `self_optimize_performance`

```matha
def self_optimize_performance(self) -> dict
```

**描述**: 自优化：分析性能并自动优化配置。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\inner_loop.py:618`

---

### `verify_fixes`

```matha
def verify_fixes(self, pre_diagnosis: dict) -> dict
```

**描述**: 验证修复效果：对比修复前后状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pre_diagnosis` | `dict` | |

**返回**: `dict`

Source: `src\inner_loop.py:690`

---

### `save_state`

```matha
def save_state(self, path: str) -> str
```

**描述**: 持久化内循环状态到文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `str`

Source: `src\inner_loop.py:715`

---

### `load_state`

```matha
def load_state(self, path: str) -> bool
```

**描述**: 从文件加载内循环状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `bool`

Source: `src\inner_loop.py:747`

---

### `run_cycle`

```matha
def run_cycle(self, verbose: bool) -> dict
```

**描述**: 执行一轮完整内循环：
感知 → 认知 → 执行 → 验证 → 持久化

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `verbose` | `bool` | |

**返回**: `dict`

Source: `src\inner_loop.py:780`

---

### `trigger_once`

```matha
def trigger_once(self, verbose: bool) -> dict
```

**描述**: 触发单次内循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `verbose` | `bool` | |

**返回**: `dict`

Source: `src\inner_loop.py:1104`

---

### `start_loop`

```matha
def start_loop(self, interval: float, verbose: bool)
```

**描述**: 启动持续内循环模式（后台线程）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interval` | `float` | |
| `verbose` | `bool` | |

Source: `src\inner_loop.py:1110`

---

### `_loop_worker`

```matha
def _loop_worker(self)
```

**描述**: 持续循环工作线程。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\inner_loop.py:1123`

---

### `stop_loop`

```matha
def stop_loop(self)
```

**描述**: 停止持续内循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\inner_loop.py:1132`

---

### `get_status`

```matha
def get_status(self) -> dict
```

**描述**: 获取当前内循环状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\inner_loop.py:1140`

---

### `get_state`

```matha
def get_state(self) -> dict
```

**描述**: 获取完整内循环状态（含模块信息）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\inner_loop.py:1155`

---

## `get_inner_loop`

```matha
def get_inner_loop() -> MathaInnerLoop
```

**描述**: 获取或创建内循环单例。

**返回**: `MathaInnerLoop`

Source: `src\inner_loop.py:1175`

---

## `trigger_inner_loop`

```matha
def trigger_inner_loop(verbose: bool) -> dict
```

**描述**: 触发一次完整内循环（便捷入口）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `verbose` | `bool` | |

**返回**: `dict`

Source: `src\inner_loop.py:1183`

---

## `start_inner_loop`

```matha
def start_inner_loop(interval: float)
```

**描述**: 启动持续内循环（便捷入口）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interval` | `float` | |

Source: `src\inner_loop.py:1189`

---

## `stop_inner_loop`

```matha
def stop_inner_loop()
```

**描述**: 停止持续内循环（便捷入口）。

Source: `src\inner_loop.py:1195`

---

## `get_loop_status`

```matha
def get_loop_status() -> dict
```

**描述**: 获取内循环状态（便捷入口）。

**返回**: `dict`

Source: `src\inner_loop.py:1201`

---

# `intent.intent_decomposer`

```matha

```

**描述**: Matha v4.0 — 意图分解引擎（IDE）核心实现

设计原则：
  1. 短文本（≤20字）→ 快速路径（正则匹配，<50ms）
  2. 中文本（20-100字）→ 标点拆分 + 子意图合并
  3. 长文本（>100字）→ LLM 辅助意图树分解
  4. 自进化：成功映射自动存储为新模板

架构：
  自然语言 → IDE 分解 → 意图树 → 数学映射 → 可执行代码

Source: `src\intent\intent_decomposer.py:1`

---

## `IntentNodeType`

```matha
class IntentNodeType(Enum)
```

**描述**: 意图树节点类型。

Source: `src\intent\intent_decomposer.py:31`

---

## `IntentNode`

```matha
class IntentNode
```

**描述**: 意图树节点。

**装饰器**: dataclass

Source: `src\intent\intent_decomposer.py:42`

---

### `is_complete`

```matha
def is_complete(self) -> bool
```

**描述**: 检查节点是否完整。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\intent\intent_decomposer.py:53`

---

### `to_math_code`

```matha
def to_math_code(self) -> str
```

**描述**: 将意图树转换为数学代码（MIR 格式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:63`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

**描述**: 序列化为字典。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\intent\intent_decomposer.py:72`

---

### `from_dict`

```matha
def from_dict(cls, data: Dict) -> 'IntentNode'
```

**描述**: 从字典反序列化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `data` | `Dict` | |

**返回**: `'IntentNode'`

**装饰器**: classmethod

Source: `src\intent\intent_decomposer.py:86`

---

## `IntentDecomposer`

```matha
class IntentDecomposer
```

**描述**: 意图分解引擎（IDE）。

将任意长度自然语言拆分为意图树，并映射为数学表达式。

Source: `src\intent\intent_decomposer.py:106`

---

### `__init__`

```matha
def __init__(self, use_llm: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `use_llm` | `bool` | |

Source: `src\intent\intent_decomposer.py:162`

---

### `decompose`

```matha
def decompose(self, text: str) -> IntentNode
```

**描述**: 将自然语言分解为意图树。

策略：
1. 短文本（≤20字）→ 直接匹配快速规则
2. 中文本（20-100字）→ 标点拆分 + 子意图合并
3. 长文本（>100字）→ LLM 辅助分解（如启用）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentNode`

Source: `src\intent\intent_decomposer.py:171`

---

### `_decompose_short`

```matha
def _decompose_short(self, text: str) -> IntentNode
```

**描述**: 短文本：直接匹配快速规则。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentNode`

Source: `src\intent\intent_decomposer.py:193`

---

### `_decompose_medium`

```matha
def _decompose_medium(self, text: str) -> IntentNode
```

**描述**: 中文本：拆分 + 合并。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentNode`

Source: `src\intent\intent_decomposer.py:218`

---

### `_decompose_long`

```matha
def _decompose_long(self, text: str) -> IntentNode
```

**描述**: 长文本：LLM 辅助分解。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentNode`

Source: `src\intent\intent_decomposer.py:256`

---

### `_llm_decompose`

```matha
def _llm_decompose(self, text: str) -> IntentNode
```

**描述**: 使用 LLM 进行意图分解。

返回包含完整意图树的根节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentNode`

Source: `src\intent\intent_decomposer.py:264`

---

### `_heuristic_long_decompose`

```matha
def _heuristic_long_decompose(self, text: str) -> IntentNode
```

**描述**: 长文本启发式分解（无 LLM 时的降级方案）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentNode`

Source: `src\intent\intent_decomposer.py:274`

---

### `_split_by_punctuation`

```matha
def _split_by_punctuation(self, text: str) -> List[str]
```

**描述**: 按中文/英文标点拆分文本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `List[str]`

Source: `src\intent\intent_decomposer.py:306`

---

### `_simulate_llm_decomposition`

```matha
def _simulate_llm_decomposition(self, text: str) -> IntentNode
```

**描述**: 模拟 LLM 分解（用于测试和演示）。

实际实现应调用真实 LLM API。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentNode`

Source: `src\intent\intent_decomposer.py:312`

---

### `learn`

```matha
def learn(self, input_text: str, math_expr: str, success: bool)
```

**描述**: 从成功映射中学习新模板。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `input_text` | `str` | |
| `math_expr` | `str` | |
| `success` | `bool` | |

Source: `src\intent\intent_decomposer.py:388`

---

### `_load_templates`

```matha
def _load_templates(self) -> Dict
```

**描述**: 加载已存储的模板。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\intent\intent_decomposer.py:415`

---

### `_extract_pattern`

```matha
def _extract_pattern(self, text: str) -> str
```

**描述**: 提取文本模式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:423`

---

## `_extract_arithmetic`

```matha
def _extract_arithmetic(text: str) -> str
```

**描述**: 提取算术表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:434`

---

## `_extract_math_func`

```matha
def _extract_math_func(text: str) -> str
```

**描述**: 提取数学函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:450`

---

## `_extract_comparison`

```matha
def _extract_comparison(text: str) -> str
```

**描述**: 提取比较表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:473`

---

## `_extract_array_op`

```matha
def _extract_array_op(text: str) -> str
```

**描述**: 提取数组操作。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:487`

---

## `_extract_string_op`

```matha
def _extract_string_op(text: str) -> str
```

**描述**: 提取字符串操作。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:513`

---

## `_extract_prime_search`

```matha
def _extract_prime_search(text: str) -> str
```

**描述**: 提取素数搜索。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:525`

---

## `_extract_range`

```matha
def _extract_range(text: str) -> str
```

**描述**: 提取范围。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:540`

---

## `_extract_conditional`

```matha
def _extract_conditional(text: str) -> str
```

**描述**: 提取条件表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\intent_decomposer.py:549`

---

# `intent.llm_parser`

```matha

```

**描述**: Matha v4.2 — LLM 意图解析器

集成 Claude/DeepSeek/GPT 等 LLM API，将自然语言转换为结构化意图。

架构：
  自然语言 → LLM 解析 → 结构化意图 → MIR 代码生成

支持的后端：
  - claude (Anthropic)
  - deepseek (DeepSeek)
  - gpt (OpenAI)
  - local (Ollama 本地模型)

用法：
  from src.intent.llm_parser import LLMIntentParser

  # 方式 1: Claude
  parser = LLMIntentParser(api_key="sk-ant-...", model="claude-3-5-sonnet")

  # 方式 2: DeepSeek（低成本）
  parser = LLMIntentParser(api_key="sk-...", model="deepseek-chat")

  # 方式 3: 本地 Ollama
  parser = LLMIntentParser(model="llama3.2", base_url="http://localhost:11434/v1")

  # 解析意图
  intent = parser.parse("计算 100 以内所有素数")
  print(intent.suggested_code)

Source: `src\intent\llm_parser.py:1`

---

## `IntentType`

```matha
class IntentType(Enum)
```

**描述**: 计算意图类型。

Source: `src\intent\llm_parser.py:47`

---

## `Intent`

```matha
class Intent
```

**描述**: 计算意图描述。

**装饰器**: dataclass

Source: `src\intent\llm_parser.py:66`

---

### `is_valid`

```matha
def is_valid(self) -> bool
```

**描述**: 检查意图是否有效。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\intent\llm_parser.py:78`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

**描述**: 序列化为字典。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\intent\llm_parser.py:82`

---

## `LLMIntentParser`

```matha
class LLMIntentParser
```

**描述**: 基于 LLM API 的意图解析器（v4.2 核心组件）。

支持的后端：
  - claude (Anthropic)
  - deepseek (DeepSeek)
  - gpt (OpenAI)
  - local (Ollama 本地模型)

使用方式：
    parser = LLMIntentParser(api_key="sk-...", model="claude-3-5-sonnet")
    intent = parser.parse("计算 100 以内所有素数")

Source: `src\intent\llm_parser.py:140`

---

### `__init__`

```matha
def __init__(self, api_key: Optional[str], model: str, base_url: Optional[str], timeout: float, cache_dir: str)
```

**描述**: 初始化 LLM 意图解析器。

Args:
    api_key: API 密钥（可选，从环境变量读取）
    model: 模型名称
    base_url: API 基础 URL（可选）
    timeout: 请求超时时间（秒）
    cache_dir: 缓存目录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `api_key` | `Optional[str]` | |
| `model` | `str` | |
| `base_url` | `Optional[str]` | |
| `timeout` | `float` | |
| `cache_dir` | `str` | |

Source: `src\intent\llm_parser.py:194`

---

### `_get_client`

```matha
def _get_client(self)
```

**描述**: 延迟获取 API 客户端。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\intent\llm_parser.py:227`

---

### `parse`

```matha
def parse(self, text: str, strict: bool) -> Intent
```

**描述**: 解析自然语言，返回意图对象。

Args:
    text: 用户输入的自然语言
    strict: 是否严格模式（置信度 < 0.7 时返回错误）

Returns:
    Intent 对象

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `strict` | `bool` | |

**返回**: `Intent`

Source: `src\intent\llm_parser.py:275`

---

### `_call_llm`

```matha
def _call_llm(self, text: str) -> Intent
```

**描述**: 调用 LLM API 解析意图。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Intent`

Source: `src\intent\llm_parser.py:324`

---

### `_call_gemini`

```matha
def _call_gemini(self, user_prompt: str) -> str
```

**描述**: 调用 Gemini API 解析意图。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `user_prompt` | `str` | |

**返回**: `str`

Source: `src\intent\llm_parser.py:375`

---

### `_parse_llm_response`

```matha
def _parse_llm_response(self, raw_json: str, original_text: str) -> Intent
```

**描述**: 解析 LLM 返回的 JSON 响应。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `raw_json` | `str` | |
| `original_text` | `str` | |

**返回**: `Intent`

Source: `src\intent\llm_parser.py:385`

---

### `_fallback_regex_parse`

```matha
def _fallback_regex_parse(self, text: str) -> Intent
```

**描述**: 降级到正则解析。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Intent`

Source: `src\intent\llm_parser.py:419`

---

### `_cache_key`

```matha
def _cache_key(self, text: str) -> str
```

**描述**: 生成缓存键。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\intent\llm_parser.py:443`

---

### `clear_cache`

```matha
def clear_cache(self)
```

**描述**: 清空缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\intent\llm_parser.py:448`

---

### `get_supported_models`

```matha
def get_supported_models(self) -> List[str]
```

**描述**: 获取支持的模型列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

Source: `src\intent\llm_parser.py:452`

---

### `_get_gemini_client`

```matha
def _get_gemini_client(self)
```

**描述**: 获取 Gemini 客户端。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\intent\llm_parser.py:473`

---

### `_get_chatglm_client`

```matha
def _get_chatglm_client(self)
```

**描述**: 获取 ChatGLM 客户端（通过 OpenAI 兼容接口）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\intent\llm_parser.py:485`

---

## `parse_intent`

```matha
def parse_intent(text: str, model: str, **kwargs) -> Intent
```

**描述**: 便捷函数：解析自然语言为意图。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `model` | `str` | |

**返回**: `Intent`

Source: `src\intent\llm_parser.py:500`

---

## `explain_intent`

```matha
def explain_intent(text: str, model: str, **kwargs) -> str
```

**描述**: 便捷函数：解释意图。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `model` | `str` | |

**返回**: `str`

Source: `src\intent\llm_parser.py:506`

---

# `intent.mir_generator`

```matha

```

**描述**: Matha v4.2 — MIR 代码生成器

将结构化意图转换为可执行的机械语言（MIR）代码。

架构：
  结构化意图 → MIR Generator → MIR AST → 代码生成

用法：
  from src.intent.mir_generator import MIRGenerator

  generator = MIRGenerator()
  mir_code = generator.generate(intent)

Source: `src\intent\mir_generator.py:1`

---

## `MIRNodeKind`

```matha
class MIRNodeKind(Enum)
```

**描述**: MIR 节点类型。

Source: `src\intent\mir_generator.py:35`

---

## `MIRNode`

```matha
class MIRNode
```

**描述**: MIR AST 节点。

**装饰器**: dataclass

Source: `src\intent\mir_generator.py:72`

---

### `to_math_code`

```matha
def to_math_code(self, indent: int) -> str
```

**描述**: 序列化为可读的机械语言代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `indent` | `int` | |

**返回**: `str`

Source: `src\intent\mir_generator.py:79`

---

## `MIRGenerator`

```matha
class MIRGenerator
```

**描述**: MIR 代码生成器。

将结构化意图转换为可执行的机械语言代码。

Source: `src\intent\mir_generator.py:116`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\intent\mir_generator.py:137`

---

### `generate`

```matha
def generate(self, intent: Intent) -> MIRNode
```

**描述**: 根据意图生成 MIR AST。

Args:
    intent: 结构化意图

Returns:
    MIRNode: 机械语言 AST

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `MIRNode`

Source: `src\intent\mir_generator.py:141`

---

### `_generate_from_intent`

```matha
def _generate_from_intent(self, intent: Intent) -> MIRNode
```

**描述**: 根据意图类型生成 MIR 节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `MIRNode`

Source: `src\intent\mir_generator.py:166`

---

### `generate_from_text`

```matha
def generate_from_text(self, text: str, intent: Optional[Intent]) -> str
```

**描述**: 便捷方法：根据文本生成 MIR 代码。

Args:
    text: 自然语言文本
    intent: 可选的意图对象（如已解析）

Returns:
    str: 机械语言代码

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `intent` | `Optional[Intent]` | |

**返回**: `str`

Source: `src\intent\mir_generator.py:192`

---

### `_cache_key`

```matha
def _cache_key(self, intent: Intent) -> str
```

**描述**: 生成缓存键。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `str`

Source: `src\intent\mir_generator.py:217`

---

### `_parse_mir_ast`

```matha
def _parse_mir_ast(self, mir_code: str) -> MIRNode
```

**描述**: 解析已缓存的 MIR 代码为 AST（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_code` | `str` | |

**返回**: `MIRNode`

Source: `src\intent\mir_generator.py:227`

---

### `clear_cache`

```matha
def clear_cache(self)
```

**描述**: 清空缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\intent\mir_generator.py:231`

---

### `get_stats`

```matha
def get_stats(self) -> Dict
```

**描述**: 获取生成器统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\intent\mir_generator.py:235`

---

## `generate_mir`

```matha
def generate_mir(text: str, intent: Optional[Intent]) -> str
```

**描述**: 便捷函数：生成 MIR 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `intent` | `Optional[Intent]` | |

**返回**: `str`

Source: `src\intent\mir_generator.py:247`

---

## `explain_mir`

```matha
def explain_mir(mir_code: str) -> str
```

**描述**: 解释 MIR 代码的数学含义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mir_code` | `str` | |

**返回**: `str`

Source: `src\intent\mir_generator.py:253`

---

# `intent_parser`

```matha

```

**描述**: Matha 意图识别解析器 — v2.2

将自然语言输入转换为计算意图描述，
供 Matha 引擎自动编译和执行。

架构：
  自然语言 → 意图分类 → 参数提取 → 目标语言选择 → 代码生成

Source: `src\intent_parser.py:1`

---

## `IntentType`

```matha
class IntentType(Enum)
```

**描述**: 计算意图类型。

Source: `src\intent_parser.py:21`

---

## `Intent`

```matha
class Intent
```

**描述**: 计算意图描述。

**装饰器**: dataclass

Source: `src\intent_parser.py:39`

---

### `is_valid`

```matha
def is_valid(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\intent_parser.py:49`

---

## `IntentClassifier`

```matha
class IntentClassifier
```

**描述**: 意图分类器。

Source: `src\intent_parser.py:57`

---

### `classify`

```matha
def classify(self, text: str) -> IntentType
```

**描述**: 对文本进行分类，返回最匹配的意图类型。

优先级：MATH_FUNC > ARITHMETIC > STRING_OP > ARRAY_OP > ...

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `IntentType`

Source: `src\intent_parser.py:106`

---

## `ParamExtractor`

```matha
class ParamExtractor
```

**描述**: 从自然语言中提取参数。

Source: `src\intent_parser.py:157`

---

### `extract_numbers`

```matha
def extract_numbers(self, text: str) -> list[float]
```

**描述**: 提取所有数字。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `list[float]`

Source: `src\intent_parser.py:167`

---

### `extract_variables`

```matha
def extract_variables(self, text: str) -> list[str]
```

**描述**: 提取变量名（排除关键词）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `list[str]`

Source: `src\intent_parser.py:171`

---

### `extract_range`

```matha
def extract_range(self, text: str) -> Optional[tuple[int, int]]
```

**描述**: 提取范围 (start, end)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Optional[tuple[int, int]]`

Source: `src\intent_parser.py:179`

---

## `CodeGenerator`

```matha
class CodeGenerator
```

**描述**: 根据意图生成目标语言代码。

Source: `src\intent_parser.py:191`

---

### `generate_python`

```matha
def generate_python(self, intent: Intent) -> str
```

**描述**: 生成 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `str`

Source: `src\intent_parser.py:194`

---

### `_gen_arithmetic`

```matha
def _gen_arithmetic(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:212`

---

### `_gen_string_op`

```matha
def _gen_string_op(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:218`

---

### `_gen_array_op`

```matha
def _gen_array_op(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:222`

---

### `_gen_math_func`

```matha
def _gen_math_func(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:226`

---

### `_gen_conditional`

```matha
def _gen_conditional(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:232`

---

### `_gen_loop`

```matha
def _gen_loop(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:235`

---

### `_gen_conversion`

```matha
def _gen_conversion(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:240`

---

### `_gen_generic`

```matha
def _gen_generic(self, intent: Intent, params: dict) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `params` | `dict` | |

**返回**: `str`

Source: `src\intent_parser.py:243`

---

### `generate`

```matha
def generate(self, intent: Intent, target_lang: str) -> str
```

**描述**: 根据目标语言生成代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `target_lang` | `str` | |

**返回**: `str`

Source: `src\intent_parser.py:246`

---

### `_gen_rust`

```matha
def _gen_rust(self, intent: Intent) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `str`

Source: `src\intent_parser.py:256`

---

### `_gen_go`

```matha
def _gen_go(self, intent: Intent) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `str`

Source: `src\intent_parser.py:259`

---

## `IntentParser`

```matha
class IntentParser
```

**描述**: 意图解析器 — 自然语言 → 计算意图 → 代码。

Source: `src\intent_parser.py:267`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\intent_parser.py:270`

---

### `parse`

```matha
def parse(self, text: str, target_lang: str) -> Intent
```

**描述**: 解析自然语言，返回计算意图。

参数:
    text: 自然语言输入
    target_lang: 目标编程语言（默认 python）

返回:
    Intent 对象，包含意图类型、参数、生成代码等

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Intent`

Source: `src\intent_parser.py:275`

---

### `_describe_intent`

```matha
def _describe_intent(self, itype: IntentType, text: str) -> str
```

**描述**: 将意图类型转为描述。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `itype` | `IntentType` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\intent_parser.py:317`

---

### `_calc_confidence`

```matha
def _calc_confidence(self, intent: Intent, text: str) -> float
```

**描述**: 计算置信度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |
| `text` | `str` | |

**返回**: `float`

Source: `src\intent_parser.py:334`

---

### `explain`

```matha
def explain(self, intent: Intent) -> str
```

**描述**: 将意图转为自然语言解释。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `intent` | `Intent` | |

**返回**: `str`

Source: `src\intent_parser.py:354`

---

## `parse_intent`

```matha
def parse_intent(text: str, target_lang: str) -> Intent
```

**描述**: 便捷函数：解析自然语言为计算意图。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Intent`

Source: `src\intent_parser.py:386`

---

## `explain_intent`

```matha
def explain_intent(text: str, target_lang: str) -> str
```

**描述**: 便捷函数：解析并解释自然语言意图。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `str`

Source: `src\intent_parser.py:392`

---

# `interop`

```matha

```

**描述**: Matha 跨语言互操作层。

支持：
  1. Python ↔ Matha 双向互操作
  2. JavaScript ↔ Matha 互操作（通过 Node.js subprocess）
  3. Rust/C ↔ Matha 互操作（通过 ctypes/FFI）
  4. 自动类型转换
  5. 共享内存与进程间通信

Source: `src\interop.py:1`

---

## `PythonInterop`

```matha
class PythonInterop
```

**描述**: Python 与 Matha 的双向互操作。

Source: `src\interop.py:23`

---

### `__init__`

```matha
def __init__(self, interpreter) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interpreter` | `` | |

**返回**: `None`

Source: `src\interop.py:26`

---

### `register_py_func`

```matha
def register_py_func(self, matha_name: str, py_func: Callable) -> None
```

**描述**: 将 Python 函数注册为 Matha 内建。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_name` | `str` | |
| `py_func` | `Callable` | |

**返回**: `None`

Source: `src\interop.py:31`

---

### `call_py_func`

```matha
def call_py_func(self, name: str, *args) -> Any
```

**描述**: 从 Matha 调用 Python 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\interop.py:35`

---

### `register_matha_func`

```matha
def register_matha_func(self, name: str, matha_code: str) -> None
```

**描述**: 注册 Matha 函数供 Python 调用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `matha_code` | `str` | |

**返回**: `None`

Source: `src\interop.py:41`

---

### `call_matha_func`

```matha
def call_matha_func(self, name: str, *args) -> Any
```

**描述**: 从 Python 调用 Matha 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\interop.py:45`

---

## `JSInterop`

```matha
class JSInterop
```

**描述**: JavaScript 与 Matha 的互操作（通过 Node.js）。

Source: `src\interop.py:62`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\interop.py:65`

---

### `register_js_func`

```matha
def register_js_func(self, name: str, js_code: str) -> None
```

**描述**: 注册 JS 函数供 Matha 调用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `js_code` | `str` | |

**返回**: `None`

Source: `src\interop.py:68`

---

### `call_js_func`

```matha
def call_js_func(self, name: str, *args) -> Any
```

**描述**: 从 Matha 调用 JS 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\interop.py:72`

---

### `call_js_module`

```matha
def call_js_module(self, module: str, func: str, *args) -> Any
```

**描述**: 调用已安装的 npm 模块函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `module` | `str` | |
| `func` | `str` | |

**返回**: `Any`

Source: `src\interop.py:94`

---

## `CInterop`

```matha
class CInterop
```

**描述**: C/Rust 库与 Matha 的互操作（通过 ctypes）。

Source: `src\interop.py:117`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\interop.py:120`

---

### `load_library`

```matha
def load_library(self, name: str, path: str) -> None
```

**描述**: 加载 C/Rust 动态链接库。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `path` | `str` | |

**返回**: `None`

Source: `src\interop.py:124`

---

### `register_func`

```matha
def register_func(self, lib_name: str, func_name: str, argtypes: list[type], restype: type) -> None
```

**描述**: 注册 C 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lib_name` | `str` | |
| `func_name` | `str` | |
| `argtypes` | `list[type]` | |
| `restype` | `type` | |

**返回**: `None`

Source: `src\interop.py:130`

---

### `call_func`

```matha
def call_func(self, name: str, *args) -> Any
```

**描述**: 调用 C 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\interop.py:141`

---

## `IPC`

```matha
class IPC
```

**描述**: 进程间通信（共享内存 / 消息队列）。

Source: `src\interop.py:152`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\interop.py:155`

---

### `send`

```matha
def send(self, data: Any) -> None
```

**描述**: 发送消息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `Any` | |

**返回**: `None`

Source: `src\interop.py:160`

---

### `receive`

```matha
def receive(self, timeout: float) -> Optional[Any]
```

**描述**: 接收消息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `timeout` | `float` | |

**返回**: `Optional[Any]`

Source: `src\interop.py:164`

---

### `set_shared`

```matha
def set_shared(self, key: str, value: Any) -> None
```

**描述**: 设置共享内存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `value` | `Any` | |

**返回**: `None`

Source: `src\interop.py:171`

---

### `get_shared`

```matha
def get_shared(self, key: str) -> Any
```

**描述**: 读取共享内存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `Any`

Source: `src\interop.py:175`

---

## `MathaInterop`

```matha
class MathaInterop
```

**描述**: Matha 统一互操作入口。

Source: `src\interop.py:184`

---

### `__init__`

```matha
def __init__(self, interpreter) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interpreter` | `` | |

**返回**: `None`

Source: `src\interop.py:187`

---

### `register_function`

```matha
def register_function(self, name: str, lang: str, func: Callable) -> None
```

**描述**: 统一注册函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `lang` | `str` | |
| `func` | `Callable` | |

**返回**: `None`

Source: `src\interop.py:193`

---

### `call_function`

```matha
def call_function(self, name: str, lang: str, *args) -> Any
```

**描述**: 统一调用函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `lang` | `str` | |

**返回**: `Any`

Source: `src\interop.py:203`

---

## `get_interop`

```matha
def get_interop() -> MathaInterop
```

**描述**: 获取互操作实例。

**返回**: `MathaInterop`

Source: `src\interop.py:228`

---

# `interp`

```matha

```

**描述**: Matha 最小树走式解释器（后端原型 + 字符串/列表 + 词法命令绑定）。

让 Matha 程序的具体部分真正执行：
  - func 定义 → 可调用闭包（支持单参 / 柯里化多参）
  - @ set_up → 初始化状态变量（无值默认 0）
  - 机械段（mech unit）：代码块（绑定 / 输出）、命令链（追踪 + 绑定）、GenStmt
  - 表达式求值：字面量、变量、算术 / 关系运算、函数应用、lambda
  - 标准库内建：ord / chr / len / get / slice / append / list
  - 函数式词法扫描：扫描(src)(pos)(line)(col)(toks) 递归组装 Token
    （Matha 自身 lambda + 递归 + 柯里化，无命令占位符）

定位：规格级→可执行规格的后端。命令字面量现为纯规格追踪标记；
实际字符 I/O 由 matha/lexer.matha 的函数式扫描器承担。

用法：
    from src.interp import interpret, lexer_bootstrap_interpret
    outputs, trace = interpret(source)
    tokens = lexer_bootstrap_interpret(matha_source_text)  # 函数式扫描

调试日志：
    解释器核心循环（声明执行 / 语句分派 / 表达式求值 / 词法主控 / 函数应用）
    均埋了 logging 钩子，默认关闭。两种开启方式：

    1) 环境变量（最便捷，无需改代码）：
        $env:MATHA_DEBUG = "1"      # PowerShell
        set MATHA_DEBUG=1            # cmd
        export MATHA_DEBUG=1         # bash
        之后运行任意测试/脚本即可在 stderr 看到带缩进的执行轨迹。

    2) 代码内显式开启（可精确控制粒度）：
        from src.interp import Interpreter, configure_debug_logging
        configure_debug_logging()    # 装 stderr handler
        interp = Interpreter(debug=True)
        interp.run(program)

        或单次调用：
        from src.interp import interpret
        outputs, trace = interpret(source, debug=True)

    日志命名空间 "matha.interp"，层级：
        INFO  —— 声明/段/命令字面量/Token 组装/函数调用边界
        DEBUG —— 语句分派、表达式求值、绑定、二元运算细节
    每条日志带递归深度缩进（. ）便于追踪嵌套调用栈。

Source: `src\interp.py:1`

---

## `_curry_module`

```matha
def _curry_module(n: int, func)
```

**描述**: 模块级柯里化（供 _build_domain_builtins 使用，对应 Interpreter._curry 的静态方法）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `func` | `` | |

Source: `src\interp.py:57`

---

## `_build_domain_builtins`

```matha
def _build_domain_builtins() -> dict
```

**描述**: 构建包含所有领域内建符号的 dict（模块级缓存，仅调用一次）。

**返回**: `dict`

Source: `src\interp.py:66`

---

## `configure_debug_logging`

```matha
def configure_debug_logging(stream, level: int) -> None
```

**描述**: 装一个 stderr（或指定 stream）handler，开启 matha.interp 详细日志。

幂等：重复调用不会叠加 handler。

| 参数 | 类型 | 说明 |
|------|------|------|
| `stream` | `` | |
| `level` | `int` | |

**返回**: `None`

Source: `src\interp.py:143`

---

## `_env_debug_flag`

```matha
def _env_debug_flag() -> bool
```

**描述**: 读取 MATHA_DEBUG 环境变量。1/true/yes/on（不区分大小写）为真。

**返回**: `bool`

Source: `src\interp.py:163`

---

## `MathaRuntimeError`

```matha
class MathaRuntimeError(Exception)
```

**描述**: 运行时错误。

Source: `src\interp.py:169`

---

## `builtin_ord`

```matha
def builtin_ord(c: str) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `c` | `str` | |

**返回**: `int`

Source: `src\interp.py:177`

---

## `builtin_chr`

```matha
def builtin_chr(n: int) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `str`

Source: `src\interp.py:183`

---

## `builtin_len`

```matha
def builtin_len(seq) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

**返回**: `int`

Source: `src\interp.py:189`

---

## `builtin_get`

```matha
def builtin_get(seq)
```

**描述**: get(seq)(index) → seq[index]

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

Source: `src\interp.py:195`

---

## `builtin_slice`

```matha
def builtin_slice(seq)
```

**描述**: slice(seq)(start)(end) → seq[start:end]

| 参数 | 类型 | 说明 |
|------|------|------|
| `seq` | `` | |

Source: `src\interp.py:209`

---

## `builtin_append`

```matha
def builtin_append(lst)
```

**描述**: append(lst)(elem) → 新列表 lst + [elem]

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |

Source: `src\interp.py:222`

---

## `builtin_list`

```matha
def builtin_list(*args)
```

**描述**: list() → []；list(x) → [x]

Source: `src\interp.py:231`

---

## `builtin_token`

```matha
def builtin_token(ttype)
```

**描述**: token(类型)(文本)(行)(列) -> Token 字典（柯里化四参构造子）。

供函数式词法器组装 Token；与原命令占位符时代的
{"类型":..,"文本":..,"行":..,"列":..} 字典格式一致，保持兼容。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ttype` | `` | |

Source: `src\interp.py:236`

---

## `builtin_parse_json`

```matha
def builtin_parse_json(text: str)
```

**描述**: 解析_JSON(json字符串) → 嵌套列表/字典。

把 JSON 数组 [a, b, c] 解析为 Python list，
供 codegen 的规格树使用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

Source: `src\interp.py:251`

---

## `builtin_read_file`

```matha
def builtin_read_file(path: str) -> str
```

**描述**: 读文件(path) → 文件内容字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

**返回**: `str`

Source: `src\interp.py:270`

---

## `builtin_write_file`

```matha
def builtin_write_file(path: str, content: str) -> None
```

**描述**: 写文件(path, 内容) → 无返回值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `content` | `str` | |

**返回**: `None`

Source: `src\interp.py:279`

---

## `builtin_append_file`

```matha
def builtin_append_file(path: str, content: str) -> None
```

**描述**: 追加文件(path, 内容) → 无返回值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `content` | `str` | |

**返回**: `None`

Source: `src\interp.py:288`

---

## `builtin_parse_json_file`

```matha
def builtin_parse_json_file(path: str)
```

**描述**: 读JSON文件(path) → 嵌套列表/字典。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

Source: `src\interp.py:297`

---

## `builtin_write_json_file`

```matha
def builtin_write_json_file(path: str, data) -> None
```

**描述**: 写JSON文件(path, 数据) → 无返回值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |
| `data` | `` | |

**返回**: `None`

Source: `src\interp.py:307`

---

## `builtin_字符串截取`

```matha
def builtin_字符串截取(s: str, start: int, end: int) -> str
```

**描述**: 截取(文本, 起始[, 结束]) → 子串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |
| `start` | `int` | |
| `end` | `int` | |

**返回**: `str`

Source: `src\interp.py:320`

---

## `builtin_字符串替换`

```matha
def builtin_字符串替换(s: str, old: str, new: str) -> str
```

**描述**: 替换(文本, 旧, 新) → 新字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |
| `old` | `str` | |
| `new` | `str` | |

**返回**: `str`

Source: `src\interp.py:327`

---

## `builtin_字符串查找`

```matha
def builtin_字符串查找(s: str, sub: str) -> int
```

**描述**: 查找(文本, 子串) → 索引（未找到返回 -1）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |
| `sub` | `str` | |

**返回**: `int`

Source: `src\interp.py:332`

---

## `builtin_字符串分割`

```matha
def builtin_字符串分割(s: str, sep: str) -> list
```

**描述**: 分割(文本[, 分隔符]) → 列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |
| `sep` | `str` | |

**返回**: `list`

Source: `src\interp.py:337`

---

## `builtin_字符串拼接`

```matha
def builtin_字符串拼接(sep: str, *parts) -> str
```

**描述**: 拼接(分隔符, 部分1, 部分2, ...) → 字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `sep` | `str` | |

**返回**: `str`

Source: `src\interp.py:342`

---

## `builtin_字符串去空白`

```matha
def builtin_字符串去空白(s: str) -> str
```

**描述**: 去空白(文本) → 去除首尾空白。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `str`

Source: `src\interp.py:347`

---

## `builtin_字符串小写`

```matha
def builtin_字符串小写(s: str) -> str
```

**描述**: 小写(文本) → 全小写。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `str`

Source: `src\interp.py:352`

---

## `builtin_字符串大写`

```matha
def builtin_字符串大写(s: str) -> str
```

**描述**: 大写(文本) → 全大写。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `str`

Source: `src\interp.py:357`

---

## `builtin_列表映射`

```matha
def builtin_列表映射(lst, fn)
```

**描述**: 映射(列表, 函数) → 新列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |
| `fn` | `` | |

Source: `src\interp.py:365`

---

## `builtin_列表过滤`

```matha
def builtin_列表过滤(lst, fn)
```

**描述**: 过滤(列表, 函数) → 新列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |
| `fn` | `` | |

Source: `src\interp.py:372`

---

## `builtin_列表反转`

```matha
def builtin_列表反转(lst) -> list
```

**描述**: 反转(列表) → 新列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |

**返回**: `list`

Source: `src\interp.py:379`

---

## `builtin_列表排序`

```matha
def builtin_列表排序(lst, reverse: bool) -> list
```

**描述**: 排序(列表[, 降序]) → 新列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |
| `reverse` | `bool` | |

**返回**: `list`

Source: `src\interp.py:386`

---

## `builtin_列表展平`

```matha
def builtin_列表展平(nested) -> list
```

**描述**: 展平(嵌套列表) → 一维列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `nested` | `` | |

**返回**: `list`

Source: `src\interp.py:393`

---

## `builtin_列表求和`

```matha
def builtin_列表求和(lst) -> float
```

**描述**: 求和(列表) → 数值和。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |

**返回**: `float`

Source: `src\interp.py:406`

---

## `builtin_列表去重`

```matha
def builtin_列表去重(lst) -> list
```

**描述**: 去重(列表) → 去重后列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |

**返回**: `list`

Source: `src\interp.py:413`

---

## `_curry_callable`

```matha
def _curry_callable(fn, arity)
```

**描述**: 给一个 Python callable 做柯里化包装，使其匹配 Matha 的单参应用。

对 n 参函数 f(a, b, c)：当应用第一个参数后，返回等待 b、c 的闭包。
对变长或未知 arity 的函数，若函数返回 callable（如 slice、append），
则直接应用第一层，后续再应用由解释器继续调用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |
| `arity` | `` | |

Source: `src\interp.py:465`

---

## `Interpreter`

```matha
class Interpreter
```

**描述**: Matha 树走式解释器。

Source: `src\interp.py:476`

---

### `__init__`

```matha
def __init__(self, debug: bool | None)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `debug` | `bool | None` | |

Source: `src\interp.py:479`

---

### `_log`

```matha
def _log(self, level: int, msg: str) -> None
```

**描述**: 带递归深度缩进的日志。debug=False 时直接 no-op，零开销。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `int` | |
| `msg` | `str` | |

**返回**: `None`

Source: `src\interp.py:503`

---

### `_log_enter`

```matha
def _log_enter(self, tag: str, detail: str) -> None
```

**描述**: 进入某个递归节点：打印 + 深度 +1。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tag` | `str` | |
| `detail` | `str` | |

**返回**: `None`

Source: `src\interp.py:510`

---

### `_log_exit`

```matha
def _log_exit(self, tag: str, result: object) -> None
```

**描述**: 离开某个递归节点：深度 -1 + 打印结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tag` | `str` | |
| `result` | `object` | |

**返回**: `None`

Source: `src\interp.py:516`

---

### `_fmt`

```matha
def _fmt(v: object) -> str
```

**描述**: 值的安全格式化（避免巨型对象刷屏）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `object` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\interp.py:524`

---

### `run`

```matha
def run(self, program: ast.Program) -> tuple[list, list[str]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `ast.Program` | |

**返回**: `tuple[list, list[str]]`

Source: `src\interp.py:537`

---

### `call`

```matha
def call(self, name: str, *args) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `object`

Source: `src\interp.py:553`

---

### `_register`

```matha
def _register(self, decl) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `` | |

**返回**: `None`

Source: `src\interp.py:577`

---

### `_exec_decl`

```matha
def _exec_decl(self, decl) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `` | |

**返回**: `None`

Source: `src\interp.py:593`

---

### `_exec_set_up`

```matha
def _exec_set_up(self, node: ast.SetUp) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.SetUp` | |

**返回**: `None`

Source: `src\interp.py:621`

---

### `_exec_if_stmt`

```matha
def _exec_if_stmt(self, stmt: ast.IfStmt) -> None
```

**描述**: 执行 if-else 语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `ast.IfStmt` | |

**返回**: `None`

Source: `src\interp.py:635`

---

### `_exec_while_stmt`

```matha
def _exec_while_stmt(self, stmt: ast.WhileStmt) -> None
```

**描述**: 执行 while 循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `ast.WhileStmt` | |

**返回**: `None`

Source: `src\interp.py:643`

---

### `_exec_for_stmt`

```matha
def _exec_for_stmt(self, stmt: ast.ForStmt) -> None
```

**描述**: 执行 for 循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `ast.ForStmt` | |

**返回**: `None`

Source: `src\interp.py:653`

---

### `_exec_match_stmt`

```matha
def _exec_match_stmt(self, stmt: ast.MatchStmt) -> None
```

**描述**: 执行 match 模式匹配语句，输出匹配结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `ast.MatchStmt` | |

**返回**: `None`

Source: `src\interp.py:664`

---

### `_eval_match`

```matha
def _eval_match(self, stmt: ast.MatchStmt) -> object
```

**描述**: 求值 match 表达式，返回匹配结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `ast.MatchStmt` | |

**返回**: `object`

Source: `src\interp.py:670`

---

### `_match_pattern`

```matha
def _match_pattern(self, pattern, value) -> bool
```

**描述**: 简单模式匹配：字面量相等或通配符 _。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pattern` | `` | |
| `value` | `` | |

**返回**: `bool`

Source: `src\interp.py:689`

---

### `_exec_codeblock_or_stmt`

```matha
def _exec_codeblock_or_stmt(self, node) -> None
```

**描述**: 统一处理代码块或单条语句的执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `` | |

**返回**: `None`

Source: `src\interp.py:707`

---

### `_exec_stmt`

```matha
def _exec_stmt(self, stmt) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `` | |

**返回**: `None`

Source: `src\interp.py:717`

---

### `_exec_stmt_or_expr`

```matha
def _exec_stmt_or_expr(self, node) -> None
```

**描述**: 执行语句或表达式：语句走 _exec_stmt，表达式走 _eval。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `` | |

**返回**: `None`

Source: `src\interp.py:804`

---

### `_exec_gen_stmt`

```matha
def _exec_gen_stmt(self, stmt: ast.GenStmt) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `ast.GenStmt` | |

**返回**: `None`

Source: `src\interp.py:815`

---

### `_eval`

```matha
def _eval(self, expr) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `object`

Source: `src\interp.py:837`

---

### `_eval_variable`

```matha
def _eval_variable(self, node: ast.Variable) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Variable` | |

**返回**: `object`

Source: `src\interp.py:1008`

---

### `_eval_binary`

```matha
def _eval_binary(self, node: ast.BinaryOp) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.BinaryOp` | |

**返回**: `object`

Source: `src\interp.py:1027`

---

### `_eval_func_app`

```matha
def _eval_func_app(self, node: ast.FuncApp) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FuncApp` | |

**返回**: `object`

Source: `src\interp.py:1111`

---

### `_apply`

```matha
def _apply(self, func, arg) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `` | |
| `arg` | `` | |

**返回**: `object`

Source: `src\interp.py:1126`

---

### `_call_func`

```matha
def _call_func(self, fdef: ast.FuncDef, args: list) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fdef` | `ast.FuncDef` | |
| `args` | `list` | |

**返回**: `object`

Source: `src\interp.py:1140`

---

### `_call_lambda`

```matha
def _call_lambda(self, lam: ast.Lambda, captured: dict, args: list) -> object
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lam` | `ast.Lambda` | |
| `captured` | `dict` | |
| `args` | `list` | |

**返回**: `object`

Source: `src\interp.py:1145`

---

### `_target_name`

```matha
def _target_name(self, target) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target` | `` | |

**返回**: `str`

Source: `src\interp.py:1193`

---

### `_param_name`

```matha
def _param_name(self, param) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `param` | `` | |

**返回**: `str`

Source: `src\interp.py:1198`

---

### `_curry`

```matha
def _curry(n: int, func)
```

**描述**: 生成 n 参柯里化函数：逐个收集参数，满 n 个时调用 func。

Matha 函数应用是柯里化的（f(a)(b)），多参 Python 内建需
用此包裹才能在 Matha 中逐参应用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `func` | `` | |

**装饰器**: staticmethod

Source: `src\interp.py:1210`

---

### `foreign_eval`

```matha
def foreign_eval(self, lang: str, code: str, inputs)
```

**描述**: 求值外部语言代码（Python 宿主内 / 其它语言 subprocess）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `code` | `str` | |
| `inputs` | `` | |

Source: `src\interp.py:1222`

---

### `_b_foreign_eval`

```matha
def _b_foreign_eval(self, lang: str, code: str)
```

**描述**: 内建 外部求值(语言, 代码) → 求值结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `code` | `str` | |

Source: `src\interp.py:1227`

---

### `_b_compare_impl`

```matha
def _b_compare_impl(self, matha_func: str, foreign_lang: str, foreign_code: str, test_cases: list) -> bool
```

**描述**: 内建 对比实现(Matha函数名, 外部语言, 外部代码, 输入列表) → bool。

对每个测试输入（参数列表）对比 Matha 函数与外部函数返回值，
全部一致返回 True，否则 False。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_func` | `str` | |
| `foreign_lang` | `str` | |
| `foreign_code` | `str` | |
| `test_cases` | `list` | |

**返回**: `bool`

Source: `src\interp.py:1231`

---

### `_b_compare_upgrade`

```matha
def _b_compare_upgrade(self, matha_src: str, matha_func: str, foreign_lang: str, foreign_code: str, test_cases: list) -> list
```

**描述**: 内建 对比升级(Matha源码, Matha函数名, 外部语言, 外部代码, 输入列表)。

沙箱试运行 Matha 源码 → 与外部参考对比 → 全通过则提交。
成功：返回新增/改写函数名列表。失败：抛 MathaRuntimeError。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_src` | `str` | |
| `matha_func` | `str` | |
| `foreign_lang` | `str` | |
| `foreign_code` | `str` | |
| `test_cases` | `list` | |

**返回**: `list`

Source: `src\interp.py:1242`

---

### `probe`

```matha
def probe(self)
```

**描述**: 返回本解释器的只读探针视图（Probe）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\interp.py:1265`

---

### `sandbox`

```matha
def sandbox(self)
```

**描述**: 创建隔离沙箱（克隆本解释器状态）；本体不受沙箱内运行影响。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\interp.py:1270`

---

### `upgrade`

```matha
def upgrade(self, source: str, verify)
```

**描述**: 加载 Matha 源码到沙箱试运行，通过后合并到本解释器。

verify: 可选 callable(sandbox) -> bool，commit 前校验。
返回 UpgradeResult（成功时 变更 含 diff）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `verify` | `` | |

Source: `src\interp.py:1275`

---

### `_install_self_builtins`

```matha
def _install_self_builtins(self) -> None
```

**描述**: 注册依赖本解释器实例的状态化内建（探针 / 沙箱 / 升级）。

- 0 参语义（探针_状态 / 探针_函数列表）：Matha 语法层无空括号
  调用，但 parser 将 () 解析为 IntegerLit(0)，故 探针_状态()
  实为「应用 探针_状态 于 0」；此处用忽略参数的 callable 实现。
- 沙箱克隆后必须重新调用本方法，使状态化内建指向沙箱自身解释器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\interp.py:1284`

---

### `_b_probe_state`

```matha
def _b_probe_state(self, _) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `dict`

Source: `src\interp.py:1356`

---

### `_b_func_names`

```matha
def _b_func_names(self, _) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1359`

---

### `_b_has`

```matha
def _b_has(self, name: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\interp.py:1362`

---

### `_b_dry_run`

```matha
def _b_dry_run(self, source: str) -> bool
```

**描述**: 沙箱试运行源码，不提交。返回是否成功（True=无错）。

Matha 侧无法内省 dict，故返回 bool 便于条件分支。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `bool`

Source: `src\interp.py:1366`

---

### `_b_upgrade`

```matha
def _b_upgrade(self, source: str) -> list
```

**描述**: 沙箱试运行 + 通过则提交。

成功：返回新增/改写的函数名列表（Matha 可 len/get 消费）。
失败：抛 MathaRuntimeError（本体未被污染，已 rollback）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list`

Source: `src\interp.py:1377`

---

### `_b_auto_debug`

```matha
def _b_auto_debug(self, source: str, max_attempts: int) -> dict
```

**描述**: 自主调试：捕获错误 → 生成修复候选 → 沙箱验证 → 提交。

返回 dict（成功/错误/修复方案/变更）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `max_attempts` | `int` | |

**返回**: `dict`

Source: `src\interp.py:1391`

---

### `_b_auto_optimize`

```matha
def _b_auto_optimize(self, func_name: str, strategy: str) -> dict
```

**描述**: 自主优化：识别热点 → 记忆化优化 → 等价对比 → 提交。

返回 dict（成功/热点/优化方案/加速比/变更）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `strategy` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1399`

---

### `_b_self_grow`

```matha
def _b_self_grow(self, source: str, desc: str) -> dict
```

**描述**: 自主成长：从源码学习新能力 → 沙箱验证 → 注册。

返回 dict（成功/学习源/新能力/变更）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `desc` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1407`

---

### `_b_library_list`

```matha
def _b_library_list(self, _) -> list
```

**描述**: 资源_列表() → 资源库所有条目列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1418`

---

### `_b_library_read`

```matha
def _b_library_read(self, path: str, _) -> str
```

**描述**: 资源_读取(路径) → 资源内容（只读，保护隔离）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |
| `_` | `` | |

**返回**: `str`

Source: `src\interp.py:1423`

---

### `_b_library_load`

```matha
def _b_library_load(self, path: str, _) -> dict
```

**描述**: 资源_加载(路径) → 沙箱加载资源到本体。

返回 dict（成功/新函数/错误）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |
| `_` | `` | |

**返回**: `dict`

Source: `src\interp.py:1429`

---

### `_b_library_grow`

```matha
def _b_library_grow(self, requirement: str, discipline: str, name: str) -> dict
```

**描述**: 资源_成长(需求)(学科)(名称) → 自主成长生成新资源。

子文件资源不足时，资源库主动扩展生成公式代码。
返回 dict（成功/新资源/学科/错误）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `requirement` | `str` | |
| `discipline` | `str` | |
| `name` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1437`

---

### `_b_gen_web`

```matha
def _b_gen_web(self, spec, out_dir: str) -> dict
```

**描述**: 生成_网页(规格[, 输出目录]) → 编译为 HTML/CSS/JS 成品。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `` | |
| `out_dir` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1452`

---

### `_b_gen_desktop`

```matha
def _b_gen_desktop(self, spec, out_dir: str) -> dict
```

**描述**: 生成_桌面(规格[, 输出目录]) → 编译为 Python Tkinter 桌面程序。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `` | |
| `out_dir` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1459`

---

### `_b_gen_service`

```matha
def _b_gen_service(self, spec, out_dir: str) -> dict
```

**描述**: 生成_服务(规格[, 输出目录]) → 编译为 Python HTTP 服务。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `` | |
| `out_dir` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1466`

---

### `_b_gen_system`

```matha
def _b_gen_system(self, spec, out_dir: str) -> dict
```

**描述**: 生成_系统(规格[, 输出目录]) → 编译为系统脚本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `` | |
| `out_dir` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1473`

---

### `_b_gen_game`

```matha
def _b_gen_game(self, spec, out_dir: str) -> dict
```

**描述**: 生成_游戏(规格[, 输出目录]) → 编译为 HTML5 Canvas 游戏。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `` | |
| `out_dir` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1480`

---

### `_b_gen_model3d`

```matha
def _b_gen_model3d(self, spec, out_dir: str) -> dict
```

**描述**: 生成_建模(规格[, 输出目录]) → 编译为 Three.js 3D 模型场景。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `` | |
| `out_dir` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1487`

---

### `_normalize_spec`

```matha
def _normalize_spec(self, spec)
```

**描述**: 规格归一化：字符串 → 解析为 JSON 列表；列表原样返回。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `` | |

Source: `src\interp.py:1494`

---

### `_b_build_software`

```matha
def _b_build_software(self, requirement: str) -> dict
```

**描述**: 软件_构建(需求[, 类型]) → 自主构建成品软件。

需求描述（如 "计算器网页"）→ 生成规格 → codegen → 输出。
类型：网页/桌面/服务/系统（自动推断）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `requirement` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1503`

---

### `_b_export_ast`

```matha
def _b_export_ast(self, source: str) -> dict
```

**描述**: 导出_AST(源码) → 把 Matha 源码的 AST 导出为 JSON dict。

任何语言可解析此 JSON 识别 Matha 程序结构。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1514`

---

### `_b_export_tokens`

```matha
def _b_export_tokens(self, source: str) -> list
```

**描述**: 导出_Token(源码) → 把 Matha 源码的 Token 流导出为 list。

任何语言可解析此列表做词法分析。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list`

Source: `src\interp.py:1522`

---

### `_b_transpile_python`

```matha
def _b_transpile_python(self, source: str) -> str
```

**描述**: 转译_Python(源码) → 把 Matha 源码转译为 Python 源码字符串。

Python 可直接运行转译后的代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\interp.py:1530`

---

### `_b_transpile_js`

```matha
def _b_transpile_js(self, source: str) -> str
```

**描述**: 转译_JS(源码) → 把 Matha 源码转译为 JavaScript 源码字符串。

JavaScript 环境可直接运行转译后的代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\interp.py:1538`

---

### `_b_export_symtab`

```matha
def _b_export_symtab(self, fmt: str) -> str
```

**描述**: 导出_符号表([格式]) → 导出 Matha 完整符号表。

格式："json" | "typescript" | "markdown" | "python"
让其它语言知道 Matha 提供了哪些函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fmt` | `str` | |

**返回**: `str`

Source: `src\interp.py:1546`

---

### `_b_net_scan`

```matha
def _b_net_scan(self, source_ips: list) -> list
```

**描述**: 威胁检测(IP列表) → 威胁列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source_ips` | `list` | |

**返回**: `list`

Source: `src\interp.py:1571`

---

### `_b_net_detect_virus`

```matha
def _b_net_detect_virus(self, behavior: str) -> list
```

**描述**: 检测病毒(行为描述) → 匹配签名列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `behavior` | `str` | |

**返回**: `list`

Source: `src\interp.py:1576`

---

### `_b_net_quarantine`

```matha
def _b_net_quarantine(self, threat_id: str) -> dict
```

**描述**: 隔离威胁(威胁ID) → 操作结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1581`

---

### `_b_net_eliminate`

```matha
def _b_net_eliminate(self, threat_id: str) -> dict
```

**描述**: 清杀威胁(威胁ID) → 操作结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1586`

---

### `_b_net_eliminate_all`

```matha
def _b_net_eliminate_all(self, _) -> dict
```

**描述**: 清杀全部(级别?) → 批量清杀结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `dict`

Source: `src\interp.py:1591`

---

### `_b_net_fw_rules`

```matha
def _b_net_fw_rules(self, _) -> list
```

**描述**: 生成防火墙规则(威胁列表?) → 规则列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1596`

---

### `_b_net_risk_assess`

```matha
def _b_net_risk_assess(self, ip: str) -> dict
```

**描述**: 风险评估(IP) → 风险评估结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ip` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1601`

---

### `_b_net_risk_report`

```matha
def _b_net_risk_report(self, _) -> dict
```

**描述**: 风险报告() → 整体风险报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `dict`

Source: `src\interp.py:1606`

---

### `_b_net_threat_report`

```matha
def _b_net_threat_report(self, threat_id: str) -> dict
```

**描述**: 威胁报告(威胁ID) → 威胁详情。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1611`

---

### `_b_net_add_sig`

```matha
def _b_net_add_sig(self, name: str, pattern: str, level: str, category: str, description: str) -> bool
```

**描述**: 添加威胁签名(名称,模式,等级,分类,描述) → 是否成功。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `pattern` | `str` | |
| `level` | `str` | |
| `category` | `str` | |
| `description` | `str` | |

**返回**: `bool`

Source: `src\interp.py:1616`

---

### `_b_net_list_sigs`

```matha
def _b_net_list_sigs(self, _) -> list
```

**描述**: 列出签名() → 所有威胁签名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1622`

---

### `_b_net_threat_count`

```matha
def _b_net_threat_count(self, _) -> int
```

**描述**: 威胁数量() → 当前威胁总数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `int`

Source: `src\interp.py:1627`

---

### `_b_net_eliminated_count`

```matha
def _b_net_eliminated_count(self, _) -> int
```

**描述**: 已清杀数量() → 已清杀威胁数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `int`

Source: `src\interp.py:1632`

---

### `_b_net_fw_list`

```matha
def _b_net_fw_list(self, _) -> list
```

**描述**: 防火墙规则列表() → 已生成规则。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1637`

---

### `_b_net_threat_status`

```matha
def _b_net_threat_status(self, threat_id: str) -> dict
```

**描述**: 威胁状态(威胁ID) → 威胁当前状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1642`

---

### `_b_net_create_virus`

```matha
def _b_net_create_virus(self, name: str, category: str, level: str, behavior: str, payload: str) -> dict
```

**描述**: 创造病毒(名称,分类,等级,行为,载荷) → 病毒定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `category` | `str` | |
| `level` | `str` | |
| `behavior` | `str` | |
| `payload` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1649`

---

### `_b_net_create_virus_batch`

```matha
def _b_net_create_virus_batch(self, count: int, categories: list) -> list
```

**描述**: 批量创造病毒(数量,分类列表?) → 病毒列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `count` | `int` | |
| `categories` | `list` | |

**返回**: `list`

Source: `src\interp.py:1655`

---

### `_b_net_analyze_virus`

```matha
def _b_net_analyze_virus(self, virus_id: str) -> dict
```

**描述**: 分析病毒(病毒ID) → 病毒分析报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `virus_id` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1660`

---

### `_b_net_patch_vuln`

```matha
def _b_net_patch_vuln(self, vuln_id: str, severity: str) -> dict
```

**描述**: 修补漏洞(漏洞ID,等级?) → 修补结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `vuln_id` | `str` | |
| `severity` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1665`

---

### `_b_net_simulate_spread`

```matha
def _b_net_simulate_spread(self, virus_id: str, network_size: int) -> dict
```

**描述**: 模拟传播(病毒ID,节点数?) → 传播模拟结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `virus_id` | `str` | |
| `network_size` | `int` | |

**返回**: `dict`

Source: `src\interp.py:1670`

---

### `_b_net_neutralize`

```matha
def _b_net_neutralize(self, threat_id: str, method: str) -> dict
```

**描述**: 高级中和(威胁ID,方法?) → 中和结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |
| `method` | `str` | |

**返回**: `dict`

Source: `src\interp.py:1675`

---

### `_b_net_quarantine_all`

```matha
def _b_net_quarantine_all(self, _) -> dict
```

**描述**: 隔离全部() → 隔离结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `dict`

Source: `src\interp.py:1680`

---

### `_b_net_virus_library`

```matha
def _b_net_virus_library(self, _) -> list
```

**描述**: 病毒库列表() → 所有病毒列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1685`

---

### `_b_net_create_scenario`

```matha
def _b_net_create_scenario(self, name: str, virus_count: int, network_size: int) -> dict
```

**描述**: 创建测试场景(场景名,病毒数,节点数?) → 场景摘要。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `virus_count` | `int` | |
| `network_size` | `int` | |

**返回**: `dict`

Source: `src\interp.py:1690`

---

### `_b_net_quarantine_log`

```matha
def _b_net_quarantine_log(self, _) -> list
```

**描述**: 隔离日志() → 隔离操作记录。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1696`

---

### `_b_net_elimination_log`

```matha
def _b_net_elimination_log(self, _) -> list
```

**描述**: 清杀日志() → 清杀操作记录。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_` | `` | |

**返回**: `list`

Source: `src\interp.py:1701`

---

## `interpret`

```matha
def interpret(source: str, debug: bool | None) -> tuple[list, list[str]]
```

**描述**: 解析并执行 Matha 源码，返回 (outputs, trace)。

debug=None 服从 MATHA_DEBUG 环境变量；显式 True/False 优先。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `debug` | `bool | None` | |

**返回**: `tuple[list, list[str]]`

Source: `src\interp.py:1711`

---

## `lexer_bootstrap_interpret`

```matha
def lexer_bootstrap_interpret(source: str, debug: bool | None) -> list[dict]
```

**描述**: 用 Matha 自举 lexer 的函数式扫描器 tokenize 一段源码字符串。

matha/lexer.matha 中定义的纯函数式 扫描(src)(pos)(line)(col)(toks) 由
Matha 自身语法（lambda + 递归 + 柯里化）实现字符 I/O 与 Token 组装，
无需命令占位符。本函数加载 lexer.matha 注册函数后直接调用 扫描。

debug=None 服从 MATHA_DEBUG 环境变量；显式 True/False 优先。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `debug` | `bool | None` | |

**返回**: `list[dict]`

Source: `src\interp.py:1721`

---

# `jupyter.matha_magic`

```matha

```

**描述**: Matha Jupyter 集成

提供：
1. IPython 魔法命令（%matha, %%matha）
2. 自动意图解析
3. 可执行代码单元
4. 交互式计算

用法：
  %load_ext matha.jupyter
  %matha 计算 100 以内所有素数
  %%matha
  求解方程 x^2 - 3x + 2 = 0

Source: `src\jupyter\matha_magic.py:1`

---

## `MathaMagics`

```matha
class MathaMagics
```

**描述**: Matha Jupyter 魔法命令。

Source: `src\jupyter\matha_magic.py:25`

---

### `__init__`

```matha
def __init__(self, shell)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `shell` | `` | |

Source: `src\jupyter\matha_magic.py:28`

---

### `_init_magics`

```matha
def _init_magics(self)
```

**描述**: 注册 IPython 魔法命令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\jupyter\matha_magic.py:32`

---

## `load_ipython_extension`

```matha
def load_ipython_extension(ipython)
```

**描述**: IPython 扩展加载入口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ipython` | `` | |

Source: `src\jupyter\matha_magic.py:107`

---

## `unload_ipython_extension`

```matha
def unload_ipython_extension(ipython)
```

**描述**: IPython 扩展卸载入口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ipython` | `` | |

Source: `src\jupyter\matha_magic.py:115`

---

# `jupyter.notebook_example`

```matha

```

**描述**: Matha Jupyter 集成 — Notebook 示例

演示如何在 Jupyter Notebook 中使用 Matha 意图分解引擎。

Source: `src\jupyter\notebook_example.py:1`

---

# `lang_features`

```matha

```

**描述**: Matha 语言特性扩展：with/decorator/generator/async语法支持。

Source: `src\lang_features.py:1`

---

## `MathaContextManager`

```matha
class MathaContextManager
```

**描述**: Matha 上下文管理器。

Source: `src\lang_features.py:15`

---

### `__init__`

```matha
def __init__(self, enter_fn: Callable, exit_fn: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `enter_fn` | `Callable` | |
| `exit_fn` | `Callable` | |

**返回**: `None`

Source: `src\lang_features.py:18`

---

### `__enter__`

```matha
def __enter__(self) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\lang_features.py:23`

---

### `__exit__`

```matha
def __exit__(self, exc_type, exc_val, exc_tb) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exc_type` | `` | |
| `exc_val` | `` | |
| `exc_tb` | `` | |

**返回**: `bool`

Source: `src\lang_features.py:27`

---

## `with_statement`

```matha
def with_statement(resource_fn: Callable, body_fn: Callable) -> Any
```

**描述**: Matha with 语句实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `resource_fn` | `Callable` | |
| `body_fn` | `Callable` | |

**返回**: `Any`

Source: `src\lang_features.py:35`

---

## `decorator`

```matha
def decorator(decorator_fn: Callable) -> Callable
```

**描述**: Matha 装饰器语法糖。

| 参数 | 类型 | 说明 |
|------|------|------|
| `decorator_fn` | `Callable` | |

**返回**: `Callable`

Source: `src\lang_features.py:49`

---

## `static_method`

```matha
def static_method(fn: Callable) -> staticmethod
```

**描述**: Matha @staticmethod 等价物。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `Callable` | |

**返回**: `staticmethod`

Source: `src\lang_features.py:56`

---

## `class_method`

```matha
def class_method(fn: Callable) -> classmethod
```

**描述**: Matha @classmethod 等价物。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `Callable` | |

**返回**: `classmethod`

Source: `src\lang_features.py:61`

---

## `property_getter`

```matha
def property_getter(fn: Callable) -> property
```

**描述**: Matha @property 等价物。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `Callable` | |

**返回**: `property`

Source: `src\lang_features.py:66`

---

## `MathaGenerator`

```matha
class MathaGenerator
```

**描述**: Matha 生成器对象。

Source: `src\lang_features.py:75`

---

### `__init__`

```matha
def __init__(self, gen_fn: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `gen_fn` | `Callable` | |

**返回**: `None`

Source: `src\lang_features.py:78`

---

### `__iter__`

```matha
def __iter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\lang_features.py:81`

---

### `__next__`

```matha
def __next__(self) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\lang_features.py:84`

---

### `send`

```matha
def send(self, value: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |

**返回**: `Any`

Source: `src\lang_features.py:87`

---

### `close`

```matha
def close(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\lang_features.py:90`

---

## `generator`

```matha
def generator(gen_fn: Callable) -> MathaGenerator
```

**描述**: 创建 Matha 生成器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `gen_fn` | `Callable` | |

**返回**: `MathaGenerator`

Source: `src\lang_features.py:94`

---

## `yield_value`

```matha
def yield_value(value: Any) -> Any
```

**描述**: Matha yield 语句等价物（在生成器函数内调用）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `value` | `Any` | |

**返回**: `Any`

Source: `src\lang_features.py:99`

---

## `MathaAsyncFunction`

```matha
class MathaAsyncFunction
```

**描述**: Matha async 函数包装。

Source: `src\lang_features.py:109`

---

### `__init__`

```matha
def __init__(self, async_fn: Callable) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `async_fn` | `Callable` | |

**返回**: `None`

Source: `src\lang_features.py:112`

---

### `__await__`

```matha
def __await__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\lang_features.py:119`

---

## `async_function`

```matha
def async_function(fn: Callable) -> MathaAsyncFunction
```

**描述**: Matha async 函数装饰器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `Callable` | |

**返回**: `MathaAsyncFunction`

Source: `src\lang_features.py:123`

---

## `await_expression`

```matha
def await_expression(coro: Any) -> Any
```

**描述**: Matha await 表达式等价物。

| 参数 | 类型 | 说明 |
|------|------|------|
| `coro` | `Any` | |

**返回**: `Any`

Source: `src\lang_features.py:131`

---

## `MathaProperty`

```matha
class MathaProperty
```

**描述**: Matha 属性对象。

Source: `src\lang_features.py:143`

---

### `__init__`

```matha
def __init__(self, fget, fset, fdel, doc)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fget` | `` | |
| `fset` | `` | |
| `fdel` | `` | |
| `doc` | `` | |

Source: `src\lang_features.py:146`

---

### `__get__`

```matha
def __get__(self, obj, objtype)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `obj` | `` | |
| `objtype` | `` | |

Source: `src\lang_features.py:152`

---

### `__set__`

```matha
def __set__(self, obj, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `obj` | `` | |
| `value` | `` | |

Source: `src\lang_features.py:159`

---

### `__delete__`

```matha
def __delete__(self, obj)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `obj` | `` | |

Source: `src\lang_features.py:164`

---

### `getter`

```matha
def getter(self, fget)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fget` | `` | |

Source: `src\lang_features.py:169`

---

### `setter`

```matha
def setter(self, fset)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fset` | `` | |

Source: `src\lang_features.py:172`

---

### `deleter`

```matha
def deleter(self, fdel)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fdel` | `` | |

Source: `src\lang_features.py:175`

---

## `matha_property`

```matha
def matha_property(fget, fset, fdel, doc)
```

**描述**: Matha @property 语法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fget` | `` | |
| `fset` | `` | |
| `fdel` | `` | |
| `doc` | `` | |

Source: `src\lang_features.py:179`

---

## `matha_metaclass`

```matha
def matha_metaclass(name: str, bases: tuple, namespace: dict) -> type
```

**描述**: Matha 元类工厂。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `bases` | `tuple` | |
| `namespace` | `dict` | |

**返回**: `type`

Source: `src\lang_features.py:190`

---

## `gc_set_threshold`

```matha
def gc_set_threshold(memcounts: tuple) -> None
```

**描述**: 设置 GC 阈值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `memcounts` | `tuple` | |

**返回**: `None`

Source: `src\lang_features.py:199`

---

## `gc_get_threshold`

```matha
def gc_get_threshold() -> tuple
```

**返回**: `tuple`

Source: `src\lang_features.py:205`

---

## `gc_collect`

```matha
def gc_collect() -> int
```

**返回**: `int`

Source: `src\lang_features.py:210`

---

## `register_language_features`

```matha
def register_language_features(builtins: dict) -> None
```

**描述**: 将语言特性注册为 Matha 内建。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\lang_features.py:219`

---

# `lexer`

```matha

```

**描述**: Matha 词法分析器。

对应 EBNF §2 词法层 + §16 解析器验证中的词法规则。

关键消解规则（§16）：
    - 最长匹配：……  > …；>> > >；<< > <；<= > <；>= > >
    - #N: vs #：：# 后跟数字 → 段号；# 后直接跟全角： → 无段号
    - 分数括号半/全角归一：（x/y）→ (x/y) 内部表示
    - 整数/浮点 + CJK 单位：紧邻 CJK 字符合并为带单位字面量
    - 标注 */文字/* 或 */文字*公式/*：*/ 起始，/* 结束

Source: `src\lexer.py:1`

---

## `is_cjk`

```matha
def is_cjk(ch: str) -> bool
```

**描述**: 判断字符是否为 CJK 表意文字（可用作标识符 / 单位）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ch` | `str` | |

**返回**: `bool`

Source: `src\lexer.py:199`

---

## `is_greek_letter`

```matha
def is_greek_letter(ch: str) -> bool
```

**描述**: 判断字符是否为希腊字母（可用作标识符）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ch` | `str` | |

**返回**: `bool`

Source: `src\lexer.py:216`

---

## `is_ascii_letter`

```matha
def is_ascii_letter(ch: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `ch` | `str` | |

**返回**: `bool`

Source: `src\lexer.py:221`

---

## `is_digit`

```matha
def is_digit(ch: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `ch` | `str` | |

**返回**: `bool`

Source: `src\lexer.py:225`

---

## `is_unicode_letter`

```matha
def is_unicode_letter(ch: str) -> bool
```

**描述**: 判断字符是否为 Unicode 字母（覆盖所有语言：ASCII、CJK、日文、阿拉伯、希腊等）。

使用 str.isalpha() 判定，等价于 Unicode 类别 L*（Letter）。
覆盖范围：
  - ASCII a-z / A-Z (Ll, Lu)
  - CJK 汉字（Lo）
  - 日文假名/片假名（Lo）
  - 希腊字母（Ll, Lu）
  - 阿拉伯字母（Lo）
  - 韩文（Lo）
  - 其他书写系统的字母
不覆盖（正确拒绝）：
  - emoji（So 类别）
  - 数学符号（Sm 类别，如 ∑ ∞ ≈）
  - BoxDrawing（So 类别，如 ┌ ┐ └ ┘）
  - 其他符号

| 参数 | 类型 | 说明 |
|------|------|------|
| `ch` | `str` | |

**返回**: `bool`

Source: `src\lexer.py:237`

---

## `is_unicode_id_continue`

```matha
def is_unicode_id_continue(ch: str) -> bool
```

**描述**: 判断字符是否可作为标识符续字符（字母/数字/下划线）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ch` | `str` | |

**返回**: `bool`

Source: `src\lexer.py:258`

---

## `_starts_multi_char_op`

```matha
def _starts_multi_char_op(source: str, pos: int) -> bool
```

**描述**: 检查 pos 位置是否是多字符运算符的起始。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `pos` | `int` | |

**返回**: `bool`

Source: `src\lexer.py:263`

---

## `LexerError`

```matha
class LexerError(Exception)
```

**描述**: 词法错误。

Source: `src\lexer.py:271`

---

### `__init__`

```matha
def __init__(self, msg: str, line: int, col: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |
| `line` | `int` | |
| `col` | `int` | |

Source: `src\lexer.py:274`

---

## `Lexer`

```matha
class Lexer
```

**描述**: Matha 词法分析器。

用法：
    lex = Lexer(source_text)
    tokens = list(lex.tokenize())

Source: `src\lexer.py:280`

---

### `__init__`

```matha
def __init__(self, source: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

Source: `src\lexer.py:288`

---

### `_peek`

```matha
def _peek(self, offset: int) -> str
```

**描述**: 查看 offset 处字符，不前进。越界返回空串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `offset` | `int` | |

**返回**: `str`

Source: `src\lexer.py:297`

---

### `_advance`

```matha
def _advance(self) -> str
```

**描述**: 消费当前字符并前进。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\lexer.py:302`

---

### `_match`

```matha
def _match(self, text: str) -> bool
```

**描述**: 若当前位置匹配 text 则消费并返回 True。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `bool`

Source: `src\lexer.py:313`

---

### `_skip_whitespace_inline`

```matha
def _skip_whitespace_inline(self) -> None
```

**描述**: 跳过行内空白（空格/制表符），不跳换行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\lexer.py:321`

---

### `tokenize`

```matha
def tokenize(self)
```

**描述**: 生成 Token 流（含 NEWLINE / EOF）。

缩进块（INDENT/DEDENT）暂未实现，预留接口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\lexer.py:328`

---

### `_skip_comment`

```matha
def _skip_comment(self) -> None
```

**描述**: 跳过 (* ... *) 注释。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\lexer.py:418`

---

### `_single_char`

```matha
def _single_char(self, ch: str) -> Token | None
```

**描述**: 处理单字符 Token。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ch` | `str` | |

**返回**: `Token | None`

Source: `src\lexer.py:438`

---

### `_number`

```matha
def _number(self) -> Token
```

**描述**: 解析数字字面量（整数/浮点），可带 CJK 单位。

对应 EBNF：
    <integer> = <digit> , { <digit> } , [ <unit> ]
    <float>   = <integer> , "." , { <digit> } , [ <unit> ]

支持进制前缀（M3.4）：
    0b1010  → 二进制（值=10）
    0t210   → 三进制（值=21）
    0xFF    → 十六进制（值=255）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Token`

Source: `src\lexer.py:445`

---

### `_radix_literal`

```matha
def _radix_literal(self, prefix: str, start_line: int, start_col: int) -> Token
```

**描述**: 解析进制字面量：0b(二进制) / 0t(三进制) / 0x(十六进制)。

保留原始形式（如 0b1010），值由 parser 按前缀转换为 int。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `prefix` | `str` | |
| `start_line` | `int` | |
| `start_col` | `int` | |

**返回**: `Token`

Source: `src\lexer.py:492`

---

### `_string`

```matha
def _string(self) -> Token
```

**描述**: 解析字符串字面量 "..."（转义规则草案）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Token`

Source: `src\lexer.py:506`

---

### `_identifier`

```matha
def _identifier(self) -> Token
```

**描述**: 解析标识符 / 关键字。

对应 EBNF：
    <identifier> = <letter> , { <letter> | <digit> | "_" }
    <letter>     = <unicode_letter>

连续的 Unicode 字母/数字/下划线/ CJK 字符会作为一个整体标识符。
遇到多字符运算符时停止，不崩溃，降级为 SYMBOL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Token`

Source: `src\lexer.py:525`

---

# `library`

```matha

```

**描述**: Matha 资源库管理器：保护隔离 + 读取 + 自主成长扩展。

资源库架构：
  - 模板：读取和构建（？#：{...}）
  - 子文件：编写公式代码（#：{...}，专用于编程，不能出现在资源库内）
  - 资源库：数学核心机械语言与分支的保护隔离仓库
    * 保护隔离：只读，防止损坏
    * 自主成长数据库：子文件资源不足时主动扩展生成
    * 子文件通过 资源_加载 读取资源库中的公式代码

资源库结构：
  matha/library/
    ├── core/           数学核心（算术、几何、三角）
    ├── mechanics/      机械分支（轴、轴承、应力）
    ├── structural/     结构力学分支（梁、柱）
    ├── physics/        物理分支（力学）
    └── index.matha     资源索引

保护隔离机制：
  - 资源库文件标记为只读（_protected 集合）
  - 加载到沙箱试运行，不直接修改本体
  - 自主成长扩展时，新资源先验证再入库
  - 子文件不能写入资源库

自主成长扩展：
  - 子文件请求资源不存在时
  - 资源库通过 SelfGrower 生成新公式代码
  - 沙箱验证后写入资源库对应分支目录
  - 更新索引

Source: `src\library.py:1`

---

## `LibraryEntry`

```matha
class LibraryEntry
```

**描述**: 资源库条目。

**装饰器**: dataclass

Source: `src\library.py:59`

---

### `as_dict`

```matha
def as_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\library.py:67`

---

## `GrowResult`

```matha
class GrowResult
```

**描述**: 资源库自主成长结果。

**装饰器**: dataclass

Source: `src\library.py:77`

---

### `as_dict`

```matha
def as_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\library.py:85`

---

## `Library`

```matha
class Library
```

**描述**: 资源库管理器：保护隔离 + 读取 + 自主成长扩展。

核心原则：
  1. 保护隔离：资源库文件只读，子文件不能写入
  2. 沙箱加载：资源加载到沙箱试运行，不污染本体
  3. 自主成长：资源不足时主动生成新公式代码入库
  4. 子文件专用编程：子文件不进入资源库

Source: `src\library.py:94`

---

### `__init__`

```matha
def __init__(self, root: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `root` | `str` | |

Source: `src\library.py:104`

---

### `_scan`

```matha
def _scan(self) -> None
```

**描述**: 扫描资源库目录，建立索引。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\library.py:112`

---

### `list`

```matha
def list(self) -> list[dict]
```

**描述**: 列出所有资源条目。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\library.py:144`

---

### `list_by_discipline`

```matha
def list_by_discipline(self, discipline: str) -> list[dict]
```

**描述**: 按学科分支列出资源。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `discipline` | `str` | |

**返回**: `list[dict]`

Source: `src\library.py:148`

---

### `has`

```matha
def has(self, path: str) -> bool
```

**描述**: 资源是否存在。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `bool`

Source: `src\library.py:153`

---

### `read`

```matha
def read(self, path: str) -> Optional[str]
```

**描述**: 读取资源内容（只读，保护隔离）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `Optional[str]`

Source: `src\library.py:157`

---

### `get_entry`

```matha
def get_entry(self, path: str) -> Optional[LibraryEntry]
```

**描述**: 获取资源条目。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `Optional[LibraryEntry]`

Source: `src\library.py:162`

---

### `load`

```matha
def load(self, path: str, interp: Interpreter) -> dict
```

**描述**: 将资源加载到解释器的沙箱中试运行。

保护隔离：资源在沙箱中执行，不直接修改本体。
通过后合并到本体（资源是受信任的公式定义）。

Returns:
    {"成功": bool, "新函数": [...], "错误": str|None}

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |
| `interp` | `Interpreter` | |

**返回**: `dict`

Source: `src\library.py:168`

---

### `load_discipline`

```matha
def load_discipline(self, discipline: str, interp: Interpreter) -> dict
```

**描述**: 加载整个学科分支的所有资源。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `discipline` | `str` | |
| `interp` | `Interpreter` | |

**返回**: `dict`

Source: `src\library.py:189`

---

### `grow`

```matha
def grow(self, interp: Interpreter, requirement: str, discipline: str, name: str) -> GrowResult
```

**描述**: 资源库自主成长：当子文件资源不足时，主动生成新公式代码。

流程：
  1. 根据需求描述生成候选 Matha 公式代码
  2. 沙箱试运行验证
  3. 验证通过 → 写入资源库对应分支目录
  4. 更新索引缓存

Args:
    interp: 解释器实例（用于沙箱验证）
    requirement: 需求描述（如 "计算圆环面积"）
    discipline: 目标学科分支
    name: 资源名（None 则从需求生成）

Returns:
    GrowResult

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interp` | `Interpreter` | |
| `requirement` | `str` | |
| `discipline` | `str` | |
| `name` | `str` | |

**返回**: `GrowResult`

Source: `src\library.py:209`

---

### `_generate_candidate`

```matha
def _generate_candidate(self, requirement: str, discipline: str, name: str) -> Optional[str]
```

**描述**: 根据需求描述生成候选 Matha 公式代码。

基于学科模板生成基础公式框架。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `requirement` | `str` | |
| `discipline` | `str` | |
| `name` | `str` | |

**返回**: `Optional[str]`

Source: `src\library.py:272`

---

### `is_protected`

```matha
def is_protected(self, path: str) -> bool
```

**描述**: 资源是否受保护（只读）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `str` | |

**返回**: `bool`

Source: `src\library.py:318`

---

### `disciplines`

```matha
def disciplines(self) -> list[str]
```

**描述**: 列出所有学科分支。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\library.py:322`

---

## `get_library`

```matha
def get_library() -> Library
```

**描述**: 获取全局资源库单例。

**返回**: `Library`

Source: `src\library.py:331`

---

# `logging_utils`

```matha

```

**描述**: Matha v4.4 — 性能日志增强模块

本模块为线性代数核心函数添加详细的日志输出，方便调试和性能分析。

Source: `src\logging_utils.py:1`

---

## `log_matrix_operation`

```matha
def log_matrix_operation(func)
```

**描述**: 装饰器：为矩阵运算添加日志。

用法:
    @log_matrix_operation
    def matrix_multiply(A, B):
        ...

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\logging_utils.py:13`

---

## `log_svd_operation`

```matha
def log_svd_operation(func)
```

**描述**: 装饰器：为 SVD 分解添加日志。

用法:
    @log_svd_operation
    def svd_decompose(A):
        ...

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\logging_utils.py:43`

---

## `log_inverse_operation`

```matha
def log_inverse_operation(func)
```

**描述**: 装饰器：为矩阵求逆添加日志。

用法:
    @log_inverse_operation
    def matrix_inverse(A):
        ...

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\logging_utils.py:76`

---

## `log_cache_operation`

```matha
def log_cache_operation(cache_name: str, cache: dict, key, result)
```

**描述**: 记录缓存操作日志。

Args:
    cache_name: 缓存名称
    cache: 缓存字典
    key: 缓存键
    result: 缓存结果（可选）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cache_name` | `str` | |
| `cache` | `dict` | |
| `key` | `` | |
| `result` | `` | |

Source: `src\logging_utils.py:108`

---

# `lsp`

```matha

```

**描述**: Matha Tree-sitter LSP 增强 — 补全 + 跳转 + 诊断

解决 Matha IDE 支持弱的问题。

功能：
  1. 符号补全（函数/类/变量/关键词）
  2. 符号跳转（定义/引用）
  3. 悬停提示（类型/文档）
  4. 诊断（语法错误/类型错误/未定义变量）
  5. 格式化工具集成

用法：
  from src.lsp import MathaLSP
  lsp = MathaLSP()
  completions = lsp.complete("fib(", position=(10, 5))
  diagnostics = lsp.diagnostics(source_code)

Source: `src\lsp.py:1`

---

## `LSPKind`

```matha
class LSPKind(Enum)
```

**描述**: LSP 符号类型。

Source: `src\lsp.py:31`

---

## `LSPPosition`

```matha
class LSPPosition
```

**描述**: LSP 位置。

**装饰器**: dataclass

Source: `src\lsp.py:46`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\lsp.py:51`

---

## `LSPRange`

```matha
class LSPRange
```

**描述**: LSP 范围。

**装饰器**: dataclass

Source: `src\lsp.py:56`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\lsp.py:61`

---

## `CompletionItem`

```matha
class CompletionItem
```

**描述**: 补全项。

**装饰器**: dataclass

Source: `src\lsp.py:69`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\lsp.py:80`

---

## `Diagnostic`

```matha
class Diagnostic
```

**描述**: 诊断信息。

**装饰器**: dataclass

Source: `src\lsp.py:99`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\lsp.py:107`

---

## `SymbolRegistry`

```matha
class SymbolRegistry
```

**描述**: Matha 符号注册表。

Source: `src\lsp.py:121`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\lsp.py:124`

---

### `_register_builtins`

```matha
def _register_builtins(self)
```

**描述**: 注册 Python 内置符号。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\lsp.py:129`

---

### `_register_matha_keywords`

```matha
def _register_matha_keywords(self)
```

**描述**: 注册 Matha 关键字。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\lsp.py:142`

---

### `register`

```matha
def register(self, name: str, kind: LSPKind, detail: str, documentation: str)
```

**描述**: 注册自定义符号。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `kind` | `LSPKind` | |
| `detail` | `str` | |
| `documentation` | `str` | |

Source: `src\lsp.py:164`

---

### `get`

```matha
def get(self, name: str) -> Optional[Dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Dict]`

Source: `src\lsp.py:174`

---

### `get_all`

```matha
def get_all(self) -> Dict[str, Dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Dict]`

Source: `src\lsp.py:177`

---

## `MathaLSP`

```matha
class MathaLSP
```

**描述**: Matha LSP (Language Server Protocol) 服务器。

实现：
  - textDocument/completion
  - textDocument/hover
  - textDocument/diagnostics
  - textDocument/definition

Source: `src\lsp.py:185`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\lsp.py:196`

---

### `complete`

```matha
def complete(self, source: str, position: Tuple[int, int], context: Optional[Dict]) -> List[CompletionItem]
```

**描述**: 获取补全列表。

Args:
    source: 源代码
    position: (line, character) 光标位置
    context: 补全上下文

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `position` | `Tuple[int, int]` | |
| `context` | `Optional[Dict]` | |

**返回**: `List[CompletionItem]`

Source: `src\lsp.py:205`

---

### `hover`

```matha
def hover(self, source: str, position: Tuple[int, int]) -> Optional[Dict]
```

**描述**: 获取悬停信息。

Args:
    source: 源代码
    position: (line, character)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `position` | `Tuple[int, int]` | |

**返回**: `Optional[Dict]`

Source: `src\lsp.py:302`

---

### `diagnostics`

```matha
def diagnostics(self, source: str, filepath: str) -> List[Diagnostic]
```

**描述**: 生成诊断信息。

Args:
    source: 源代码
    filepath: 文件路径

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `filepath` | `str` | |

**返回**: `List[Diagnostic]`

Source: `src\lsp.py:365`

---

### `find_definition`

```matha
def find_definition(self, source: str, position: Tuple[int, int]) -> Optional[Dict]
```

**描述**: 查找符号定义。

Args:
    source: 源代码
    position: (line, character)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `position` | `Tuple[int, int]` | |

**返回**: `Optional[Dict]`

Source: `src\lsp.py:484`

---

### `generate_readme`

```matha
def generate_readme(self, source: str) -> str
```

**描述**: 从源码生成快速文档。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\lsp.py:538`

---

# `math_driver`

```matha

```

**描述**: Matha 数学驱动层 v1.3.0
========================
数学抽象封装硬件功能 — 将数学运算抽象为可执行的硬件驱动接口。

功能：
  • MathDriver     — 硬件级数学驱动（线性代数/微积分/信号处理）
  • HardwareBridge — 硬件抽象层（模拟/数字 I/O 映射）
  • MathLibrary    — 数学库管理器
  • MathHardware   — 硬件功能封装
  • 吞噬/同化：通过 FFI 和 CodeGen 将其他语言的能力纳入 Matha

吞噬策略：
  1. Python → FFI 直接调用
  2. JavaScript → CodeGen 翻译为 JS → 浏览器执行
  3. C → CodeGen 翻译为 C → 编译执行
  4. 数学公式 → CodeGen 翻译为任意语言

Source: `src\math_driver.py:1`

---

## `HardwareType`

```matha
class HardwareType(str, Enum)
```

Source: `src\math_driver.py:37`

---

## `HardwareSpec`

```matha
class HardwareSpec
```

**描述**: 硬件规格描述。

**装饰器**: dataclass

Source: `src\math_driver.py:47`

---

## `MathDriver`

```matha
class MathDriver
```

**描述**: 数学驱动 — 封装硬件级数学运算。

每个驱动对应一类数学运算：
  • LinearAlgebraDriver — 矩阵运算
  • CalculusDriver       — 微积分运算
  • SignalDriver         — 信号处理
  • GeometryDriver       — 几何计算
  • OptimizationDriver   — 优化求解

**装饰器**: dataclass

Source: `src\math_driver.py:58`

---

### `register_op`

```matha
def register_op(self, name: str, func: Callable)
```

**描述**: 注册数学运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `Callable` | |

Source: `src\math_driver.py:76`

---

### `execute`

```matha
def execute(self, op_name: str, *args, **kwargs) -> Any
```

**描述**: 执行数学运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op_name` | `str` | |

**返回**: `Any`

Source: `src\math_driver.py:81`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\math_driver.py:94`

---

## `LinearAlgebraDriver`

```matha
class LinearAlgebraDriver(MathDriver)
```

**描述**: 线性代数驱动 — 矩阵/向量运算。

Source: `src\math_driver.py:107`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:110`

---

### `_register_builtin_ops`

```matha
def _register_builtin_ops(self)
```

**描述**: 注册内置线性代数运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:114`

---

### `_dot`

```matha
def _dot(a: list, b: list) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `list` | |
| `b` | `list` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:129`

---

### `_cross`

```matha
def _cross(a: list, b: list) -> List[float]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `list` | |
| `b` | `list` | |

**返回**: `List[float]`

**装饰器**: staticmethod

Source: `src\math_driver.py:133`

---

### `_mat_mul`

```matha
def _mat_mul(a: list, b: list) -> List[List[float]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `list` | |
| `b` | `list` | |

**返回**: `List[List[float]]`

**装饰器**: staticmethod

Source: `src\math_driver.py:141`

---

### `_mat_add`

```matha
def _mat_add(a: list, b: list) -> List[List[float]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `list` | |
| `b` | `list` | |

**返回**: `List[List[float]]`

**装饰器**: staticmethod

Source: `src\math_driver.py:152`

---

### `_mat_transpose`

```matha
def _mat_transpose(m: list) -> List[List[float]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `list` | |

**返回**: `List[List[float]]`

**装饰器**: staticmethod

Source: `src\math_driver.py:156`

---

### `_mat_det`

```matha
def _mat_det(m: list) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `list` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:160`

---

### `_mat_inv`

```matha
def _mat_inv(m: list) -> Optional[List[List[float]]]
```

**描述**: 高斯-若尔当消元法求逆矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `list` | |

**返回**: `Optional[List[List[float]]]`

**装饰器**: staticmethod

Source: `src\math_driver.py:171`

---

### `_eigenvalues`

```matha
def _eigenvalues(m: list) -> List[float]
```

**描述**: 2x2 矩阵的特征值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `list` | |

**返回**: `List[float]`

**装饰器**: staticmethod

Source: `src\math_driver.py:191`

---

### `_norm`

```matha
def _norm(v: list, p: int) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `list` | |
| `p` | `int` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:205`

---

### `_mat_power`

```matha
def _mat_power(m: list, n: int) -> List[List[float]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `m` | `list` | |
| `n` | `int` | |

**返回**: `List[List[float]]`

**装饰器**: staticmethod

Source: `src\math_driver.py:209`

---

## `CalculusDriver`

```matha
class CalculusDriver(MathDriver)
```

**描述**: 微积分驱动 — 数值微积分运算。

Source: `src\math_driver.py:224`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:227`

---

### `_register_builtin_ops`

```matha
def _register_builtin_ops(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:231`

---

### `_derivative`

```matha
def _derivative(func: Callable, x: float, h: float) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `x` | `float` | |
| `h` | `float` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:240`

---

### `_integral`

```matha
def _integral(func: Callable, a: float, b: float, n: int) -> float
```

**描述**: Simpson 积分。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `a` | `float` | |
| `b` | `float` | |
| `n` | `int` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:244`

---

### `_taylor`

```matha
def _taylor(func: Callable, x: float, x0: float, n: int) -> float
```

**描述**: 泰勒展开数值近似。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `x` | `float` | |
| `x0` | `float` | |
| `n` | `int` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:254`

---

### `_limit`

```matha
def _limit(func: Callable, x: float, direction: str) -> float
```

**描述**: 数值极限。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `x` | `float` | |
| `direction` | `str` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:272`

---

### `_newton`

```matha
def _newton(func: Callable, x0: float, tol: float, max_iter: int) -> float
```

**描述**: 牛顿法求根。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `x0` | `float` | |
| `tol` | `float` | |
| `max_iter` | `int` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:284`

---

## `SignalDriver`

```matha
class SignalDriver(MathDriver)
```

**描述**: 信号处理驱动 — FFT、滤波、调制等。

Source: `src\math_driver.py:303`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:306`

---

### `_register_builtin_ops`

```matha
def _register_builtin_ops(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:310`

---

### `_fft`

```matha
def _fft(signal: List[complex]) -> List[complex]
```

**描述**: 朴素 FFT（O(n²)）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `signal` | `List[complex]` | |

**返回**: `List[complex]`

**装饰器**: staticmethod

Source: `src\math_driver.py:320`

---

### `_ifft`

```matha
def _ifft(signal: List[complex]) -> List[complex]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `signal` | `List[complex]` | |

**返回**: `List[complex]`

**装饰器**: staticmethod

Source: `src\math_driver.py:334`

---

### `_convolve`

```matha
def _convolve(a: List[float], b: List[float]) -> List[float]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `List[float]` | |
| `b` | `List[float]` | |

**返回**: `List[float]`

**装饰器**: staticmethod

Source: `src\math_driver.py:340`

---

### `_lowpass`

```matha
def _lowpass(signal: List[float], cutoff: float) -> List[float]
```

**描述**: 简单移动平均低通滤波。

| 参数 | 类型 | 说明 |
|------|------|------|
| `signal` | `List[float]` | |
| `cutoff` | `float` | |

**返回**: `List[float]`

**装饰器**: staticmethod

Source: `src\math_driver.py:349`

---

### `_highpass`

```matha
def _highpass(signal: List[float], cutoff: float) -> List[float]
```

**描述**: 高通滤波 = 原始 - 低通。

| 参数 | 类型 | 说明 |
|------|------|------|
| `signal` | `List[float]` | |
| `cutoff` | `float` | |

**返回**: `List[float]`

**装饰器**: staticmethod

Source: `src\math_driver.py:356`

---

### `_magnitude`

```matha
def _magnitude(signal: List[complex]) -> List[float]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `signal` | `List[complex]` | |

**返回**: `List[float]`

**装饰器**: staticmethod

Source: `src\math_driver.py:362`

---

## `GeometryDriver`

```matha
class GeometryDriver(MathDriver)
```

**描述**: 几何计算驱动。

Source: `src\math_driver.py:370`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:373`

---

### `_register_builtin_ops`

```matha
def _register_builtin_ops(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:377`

---

### `_polygon_area`

```matha
def _polygon_area(points: List[Tuple[float, float]]) -> float
```

**描述**: 鞋带公式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `points` | `List[Tuple[float, float]]` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:387`

---

## `OptimizationDriver`

```matha
class OptimizationDriver(MathDriver)
```

**描述**: 优化求解驱动。

Source: `src\math_driver.py:402`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:405`

---

### `_register_builtin_ops`

```matha
def _register_builtin_ops(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:409`

---

### `_gradient_descent`

```matha
def _gradient_descent(func: Callable, x0: float, lr: float, tol: float, max_iter: int) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `x0` | `float` | |
| `lr` | `float` | |
| `tol` | `float` | |
| `max_iter` | `int` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:416`

---

### `_binary_search`

```matha
def _binary_search(func: Callable, lo: float, hi: float, tol: float) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `lo` | `float` | |
| `hi` | `float` | |
| `tol` | `float` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:428`

---

### `_golden_section`

```matha
def _golden_section(func: Callable, lo: float, hi: float, tol: float) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |
| `lo` | `float` | |
| `hi` | `float` | |
| `tol` | `float` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\math_driver.py:441`

---

## `MathDriverManager`

```matha
class MathDriverManager
```

**描述**: 数学驱动管理器 — 统一调度所有硬件级数学驱动。

吞噬/同化策略：
  1. 新语言函数 → 注册为 MathDriver 运算
  2. 数学公式 → 翻译为目标语言代码
  3. 硬件抽象 → 统一 API 调用

Source: `src\math_driver.py:460`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:470`

---

### `_init_drivers`

```matha
def _init_drivers(self)
```

**描述**: 初始化所有标准驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\math_driver.py:476`

---

### `register_driver`

```matha
def register_driver(self, driver: MathDriver)
```

**描述**: 注册驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `driver` | `MathDriver` | |

Source: `src\math_driver.py:488`

---

### `execute`

```matha
def execute(self, driver_name: str, op_name: str, *args, **kwargs) -> Any
```

**描述**: 执行驱动运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `driver_name` | `str` | |
| `op_name` | `str` | |

**返回**: `Any`

Source: `src\math_driver.py:493`

---

### `list_drivers`

```matha
def list_drivers(self) -> List[dict]
```

**描述**: 列出所有驱动。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[dict]`

Source: `src\math_driver.py:500`

---

### `get_stats`

```matha
def get_stats(self) -> dict
```

**描述**: 获取所有驱动统计。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\math_driver.py:508`

---

### `consume`

```matha
def consume(self, lang: str, functions: List[dict])
```

**描述**: 吞噬/同化其他语言的函数。

lang: "python" / "javascript" / "c"
functions: [{"name": "func_name", "params": ["a","b"], "expr": "a+b"}]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `functions` | `List[dict]` | |

Source: `src\math_driver.py:516`

---

### `_wrap_function`

```matha
def _wrap_function(self, lang: str, func_info: dict) -> Callable
```

**描述**: 将外部语言函数包装为可调用 Python 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `func_info` | `dict` | |

**返回**: `Callable`

Source: `src\math_driver.py:532`

---

## `get_driver_manager`

```matha
def get_driver_manager() -> MathDriverManager
```

**返回**: `MathDriverManager`

Source: `src\math_driver.py:581`

---

# `matha_growth`

```matha

```

**描述**: Matha 自成长系统 v2 — 递归内联、循环展开、内存优化

实现 Matha 源码的自动分析与优化循环：
  源码 → 多语言前端编译 → IR 分析 → 优化建议 → 生成改进版本 → 验证

优化规则集（v2）：
  1. 常量折叠：x = 3.0 + 4.0 → x = 7.0
  2. 函数内联（单用）：def f(x): return x*x; f(3) → 3*3
  3. 函数内联（递归）：def fib(n): return n if n<2 else fib(n-1)+fib(n-2) → 深度限制内联
  4. 死代码消除：unused = 999.0 → 移除
  5. 常量传播：a = 10.0; b = a + 1 → b = 10.0 + 1
  6. 循环展开：for i in range(4): s += i → s = 0 + 1 + 2 + 3
  7. 内存优化：临时变量栈提升（消除中间分配）
  8. 三角恒等式：sin(x) + cos(π/2-x) → sin²x + cos²x = 1 等简化

Source: `src\matha_growth.py:1`

---

## `GrowthReport`

```matha
class GrowthReport
```

**描述**: 单次成长循环的结果。

**装饰器**: dataclass

Source: `src\matha_growth.py:38`

---

## `MathaGrowthEngine`

```matha
class MathaGrowthEngine
```

**描述**: Matha 自成长引擎：自动分析、优化、验证源码。

Source: `src\matha_growth.py:57`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\matha_growth.py:60`

---

### `grow`

```matha
def grow(self, source: str) -> GrowthReport
```

**描述**: 主入口：迭代优化源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `GrowthReport`

Source: `src\matha_growth.py:64`

---

### `_single_growth_iteration`

```matha
def _single_growth_iteration(self, source: str, iteration: int) -> GrowthReport
```

**描述**: 单次迭代：分析 + 优化 + 验证。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `iteration` | `int` | |

**返回**: `GrowthReport`

Source: `src\matha_growth.py:78`

---

### `_diagnose`

```matha
def _diagnose(self, source: str) -> list[str]
```

**描述**: 源码诊断：发现可优化点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list[str]`

Source: `src\matha_growth.py:99`

---

### `_apply_optimizations`

```matha
def _apply_optimizations(self, source: str) -> tuple[str, list[str]]
```

**描述**: 应用所有优化规则。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, list[str]]`

Source: `src\matha_growth.py:110`

---

### `_verify`

```matha
def _verify(self, original: str, improved: str) -> bool
```

**描述**: 交叉语言验证：对比原始与优化后的一致性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `original` | `str` | |
| `improved` | `str` | |

**返回**: `bool`

Source: `src\matha_growth.py:138`

---

### `_apply_const_folding`

```matha
def _apply_const_folding(self, source: str) -> tuple[str, int]
```

**描述**: 常量折叠：x = 3.0 + 4.0 → x = 7.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, int]`

Source: `src\matha_growth.py:150`

---

### `_apply_dead_code_elimination`

```matha
def _apply_dead_code_elimination(self, source: str) -> tuple[str, int]
```

**描述**: 死代码消除：移除未使用的变量赋值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, int]`

Source: `src\matha_growth.py:166`

---

### `_apply_const_propagation`

```matha
def _apply_const_propagation(self, source: str) -> tuple[str, int]
```

**描述**: 常量传播：a = 10; b = a + 1 → b = 10 + 1

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, int]`

Source: `src\matha_growth.py:184`

---

### `_apply_function_inlining`

```matha
def _apply_function_inlining(self, source: str) -> tuple[str, int]
```

**描述**: 函数内联（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, int]`

Source: `src\matha_growth.py:194`

---

### `_apply_loop_unrolling`

```matha
def _apply_loop_unrolling(self, source: str) -> tuple[str, int]
```

**描述**: 循环展开（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, int]`

Source: `src\matha_growth.py:209`

---

### `_apply_memory_optimization`

```matha
def _apply_memory_optimization(self, source: str) -> tuple[str, int]
```

**描述**: 内存优化：临时变量栈提升。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, int]`

Source: `src\matha_growth.py:225`

---

### `_apply_trig_identities`

```matha
def _apply_trig_identities(self, source: str) -> tuple[str, int]
```

**描述**: P4: 三角恒等式优化规则。

实现规则：
- sin²x + cos²x = 1
- sin(π/2 - x) = cos(x)
- cos(π/2 - x) = sin(x)
- sin(2x) = 2sin(x)cos(x)
- tan(x) = sin(x)/cos(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[str, int]`

Source: `src\matha_growth.py:235`

---

# `mathlib`

```matha

```

**描述**: Matha 数学函数库：各领域演化的公共地基。

理念：物理/化学/机械/天文/地理等领域本质是数学分支，用数学统一演化。
本模块提供各领域共享的数学基础：
  - 常量：pi、e、tau、phi（黄金比）
  - 三角函数：sin/cos/tan/asin/acos/atan/atan2（弧度制）
  - 双曲函数：sinh/cosh/tanh
  - 对数指数：log（自然对数）/log10/log2/exp/sqrt/pow
  - 取整绝对值：abs/floor/ceil/round/trunc
  - 极值统计：min/max/sum
  - 角度弧度换算：deg2rad/rad2deg
  - 物理常量：G（引力常数）/c（光速）/g（重力加速度）

所有函数以普通 Python callable 形式注册到解释器 BUILTINS，
Matha 代码可直接调用（如 sin(0)=0、sqrt(2)≈1.414）。

领域应用示例（用数学演化）：
  - 物理：v = sqrt(2 * g * h)        （自由落体末速度）
  - 天文：T = 2 * pi * sqrt(r^3/GM)  （开普勒第三定律）
  - 地理：d = r * acos(sin(a)*sin(b)+cos(a)*cos(b)*cos(da-db))（大圆距离）
  - 化学：n = m / M                   （摩尔数 = 质量/摩尔质量）

Source: `src\mathlib.py:1`

---

## `_curry3`

```matha
def _curry3(func)
```

**描述**: 将三参 Python 函数转为柯里化：f(a)(b)(c)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\mathlib.py:56`

---

## `_register_math_builtins`

```matha
def _register_math_builtins(builtins: dict) -> None
```

**描述**: 将数学函数与常量注册到解释器 builtins 字典。

在 Interpreter.__init__ 中调用：
    from src.mathlib import _register_math_builtins
    _register_math_builtins(self.builtins)

常量直接作为值注册（非 callable），函数作为 callable 注册。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\mathlib.py:65`

---

## `_curry2`

```matha
def _curry2(func)
```

**描述**: 将两参 Python 函数转为柯里化：f(a)(b)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\mathlib.py:167`

---

## `_max_variadic`

```matha
def _max_variadic(*args)
```

**描述**: max 支持多参或单列表。

Source: `src\mathlib.py:174`

---

## `_min_variadic`

```matha
def _min_variadic(*args)
```

**描述**: min 支持多参或单列表。

Source: `src\mathlib.py:181`

---

## `_sign`

```matha
def _sign(x)
```

**描述**: 符号函数：x>0 返回 1，x<0 返回 -1，x==0 返回 0。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `` | |

Source: `src\mathlib.py:188`

---

## `_register_unit_builtins`

```matha
def _register_unit_builtins(builtins: dict) -> None
```

**描述**: 注册单位换算内建。

每个换算注册为 换算名(值) -> 转换值 的单参函数。
温度换算因非线性，单独注册为 lambda。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\mathlib.py:230`

---

## `_median`

```matha
def _median(lst)
```

**描述**: 计算中位数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |

Source: `src\mathlib.py:246`

---

## `_variance`

```matha
def _variance(lst)
```

**描述**: 计算方差（总体方差）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `lst` | `` | |

Source: `src\mathlib.py:258`

---

## `_covariance`

```matha
def _covariance(x, y)
```

**描述**: 计算协方差。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `` | |
| `y` | `` | |

Source: `src\mathlib.py:266`

---

## `_correlation`

```matha
def _correlation(x, y)
```

**描述**: 计算皮尔逊相关系数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `` | |
| `y` | `` | |

Source: `src\mathlib.py:275`

---

## `_power_set`

```matha
def _power_set(s)
```

**描述**: 生成幂集（所有子集）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `` | |

Source: `src\mathlib.py:285`

---

## `_pascal_triangle`

```matha
def _pascal_triangle(n)
```

**描述**: 生成杨辉三角前 n 行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\mathlib.py:293`

---

## `_is_prime`

```matha
def _is_prime(n)
```

**描述**: 素数判定。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\mathlib.py:307`

---

## `_sieve_of_eratosthenes`

```matha
def _sieve_of_eratosthenes(n)
```

**描述**: 埃氏筛，返回 n 以内的所有素数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `` | |

Source: `src\mathlib.py:321`

---

## `_norm_pdf`

```matha
def _norm_pdf(x, mu, sigma)
```

**描述**: 正态分布概率密度函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `` | |
| `mu` | `` | |
| `sigma` | `` | |

Source: `src\mathlib.py:334`

---

## `_curry2`

```matha
def _curry2(func)
```

**描述**: 两参柯里化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\mathlib.py:341`

---

## `_curry3`

```matha
def _curry3(func)
```

**描述**: 三参柯里化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |

Source: `src\mathlib.py:348`

---

# `mir`

```matha

```

**描述**: Matha 专属 IR (MIR - Matha Intermediate Representation)

MIR 设计原则：
  1. 数学领域优化：直接表达数学运算语义
  2. 静态单赋值形式 (SSA)：便于优化
  3. 类型精确：double/int/bool 明确区分
  4. 可生成 C/Python 代码：双目标后端

Source: `src\mir.py:1`

---

## `MIRType`

```matha
class MIRType(Enum)
```

**描述**: MIR 类型。

Source: `src\mir.py:23`

---

## `MIRInstrType`

```matha
class MIRInstrType(Enum)
```

**描述**: MIR 指令类型。

Source: `src\mir.py:38`

---

## `MIRInstr`

```matha
class MIRInstr
```

**描述**: MIR 指令基类。

**装饰器**: dataclass

Source: `src\mir.py:129`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:137`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:140`

---

## `MIRArithInstr`

```matha
class MIRArithInstr(MIRInstr)
```

**描述**: 算术运算指令。

**装饰器**: dataclass

Source: `src\mir.py:145`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:149`

---

## `MIRUnaryInstr`

```matha
class MIRUnaryInstr(MIRInstr)
```

**描述**: 一元运算指令。

**装饰器**: dataclass

Source: `src\mir.py:156`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:160`

---

## `MIRCallInstr`

```matha
class MIRCallInstr(MIRInstr)
```

**描述**: 函数调用指令。

**装饰器**: dataclass

Source: `src\mir.py:165`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:171`

---

## `MIRCImportInstr`

```matha
class MIRCImportInstr(MIRInstr)
```

**描述**: 从 C 库导入函数。

**装饰器**: dataclass

Source: `src\mir.py:177`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:182`

---

### `get_c_signature`

```matha
def get_c_signature(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:185`

---

## `MIRCompareInstr`

```matha
class MIRCompareInstr(MIRInstr)
```

**描述**: 比较运算指令。

**装饰器**: dataclass

Source: `src\mir.py:191`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:195`

---

## `MIRLogicalInstr`

```matha
class MIRLogicalInstr(MIRInstr)
```

**描述**: 逻辑运算指令。

**装饰器**: dataclass

Source: `src\mir.py:200`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:204`

---

## `MIRBranchInstr`

```matha
class MIRBranchInstr(MIRInstr)
```

**描述**: 分支指令。

**装饰器**: dataclass

Source: `src\mir.py:211`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:215`

---

## `MIRCondBranchInstr`

```matha
class MIRCondBranchInstr(MIRInstr)
```

**描述**: 条件分支指令。

**装饰器**: dataclass

Source: `src\mir.py:220`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:226`

---

## `MIRLabelInstr`

```matha
class MIRLabelInstr(MIRInstr)
```

**描述**: 标签指令。

**装饰器**: dataclass

Source: `src\mir.py:231`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:235`

---

## `MIRReturnInstr`

```matha
class MIRReturnInstr(MIRInstr)
```

**描述**: 返回指令。

**装饰器**: dataclass

Source: `src\mir.py:240`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:244`

---

## `MIRStoreInstr`

```matha
class MIRStoreInstr(MIRInstr)
```

**描述**: 存储指令。

**装饰器**: dataclass

Source: `src\mir.py:251`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:255`

---

## `MIRLoadInstr`

```matha
class MIRLoadInstr(MIRInstr)
```

**描述**: 加载指令。

**装饰器**: dataclass

Source: `src\mir.py:260`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:264`

---

## `MIRConstInstr`

```matha
class MIRConstInstr(MIRInstr)
```

**描述**: 常量指令。

**装饰器**: dataclass

Source: `src\mir.py:269`

---

### `format`

```matha
def format(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:273`

---

## `MIRFunction`

```matha
class MIRFunction
```

**描述**: MIR 函数。

**装饰器**: dataclass

Source: `src\mir.py:282`

---

## `MIRProgram`

```matha
class MIRProgram
```

**描述**: MIR 程序。

**装饰器**: dataclass

Source: `src\mir.py:294`

---

## `MIRGenerator`

```matha
class MIRGenerator
```

**描述**: 将 Matha AST 转换为 MIR。

Source: `src\mir.py:306`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir.py:355`

---

### `_new_temp`

```matha
def _new_temp(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir.py:360`

---

### `_ensure_scope`

```matha
def _ensure_scope(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\mir.py:364`

---

### `generate`

```matha
def generate(self, ast: Any) -> MIRProgram
```

**描述**: 生成 MIR 程序。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ast` | `Any` | |

**返回**: `MIRProgram`

Source: `src\mir.py:369`

---

### `_compile_program`

```matha
def _compile_program(self, program: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `None`

Source: `src\mir.py:377`

---

### `_compile_decl`

```matha
def _compile_decl(self, decl: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `Any` | |

**返回**: `None`

Source: `src\mir.py:383`

---

### `_compile_func_def`

```matha
def _compile_func_def(self, func_def: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_def` | `Any` | |

**返回**: `None`

Source: `src\mir.py:403`

---

### `_compile_binding`

```matha
def _compile_binding(self, binding: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `binding` | `Any` | |

**返回**: `None`

Source: `src\mir.py:420`

---

### `_compile_expr`

```matha
def _compile_expr(self, expr: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `str`

Source: `src\mir.py:432`

---

### `_compile_literal`

```matha
def _compile_literal(self, lit: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lit` | `Any` | |

**返回**: `str`

Source: `src\mir.py:476`

---

### `_compile_variable`

```matha
def _compile_variable(self, var: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `Any` | |

**返回**: `str`

Source: `src\mir.py:485`

---

### `_compile_binary`

```matha
def _compile_binary(self, op_expr: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op_expr` | `Any` | |

**返回**: `str`

Source: `src\mir.py:490`

---

### `_compile_unary`

```matha
def _compile_unary(self, op_expr: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op_expr` | `Any` | |

**返回**: `str`

Source: `src\mir.py:517`

---

### `_compile_func_app`

```matha
def _compile_func_app(self, func_app: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_app` | `Any` | |

**返回**: `str`

Source: `src\mir.py:535`

---

### `_compile_lambda`

```matha
def _compile_lambda(self, lam: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lam` | `Any` | |

**返回**: `str`

Source: `src\mir.py:566`

---

### `_compile_if`

```matha
def _compile_if(self, if_expr: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `if_expr` | `Any` | |

**返回**: `str`

Source: `src\mir.py:569`

---

### `_compile_list`

```matha
def _compile_list(self, lst: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lst` | `Any` | |

**返回**: `str`

Source: `src\mir.py:590`

---

### `_compile_dict`

```matha
def _compile_dict(self, dct: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `dct` | `Any` | |

**返回**: `str`

Source: `src\mir.py:598`

---

### `_compile_path`

```matha
def _compile_path(self, path: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `path` | `Any` | |

**返回**: `str`

Source: `src\mir.py:607`

---

### `_compile_tuple`

```matha
def _compile_tuple(self, tpl: Any) -> str
```

**描述**: 编译元组表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tpl` | `Any` | |

**返回**: `str`

Source: `src\mir.py:616`

---

### `_compile_index`

```matha
def _compile_index(self, idx: Any) -> str
```

**描述**: 编译索引表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `idx` | `Any` | |

**返回**: `str`

Source: `src\mir.py:625`

---

### `_compile_slice`

```matha
def _compile_slice(self, slc: Any) -> str
```

**描述**: 编译切片表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `slc` | `Any` | |

**返回**: `str`

Source: `src\mir.py:635`

---

### `_compile_set`

```matha
def _compile_set(self, set_node: Any) -> str
```

**描述**: 编译集合构造表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `set_node` | `Any` | |

**返回**: `str`

Source: `src\mir.py:646`

---

### `_add_required_imports`

```matha
def _add_required_imports(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir.py:655`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\mir.py:663`

---

## `generate_mir`

```matha
def generate_mir(ast: Any) -> MIRProgram
```

**描述**: 生成 MIR 程序。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ast` | `Any` | |

**返回**: `MIRProgram`

Source: `src\mir.py:683`

---

## `mir_to_dict`

```matha
def mir_to_dict(mir: MIRProgram) -> dict
```

**描述**: 将 MIR 转换为字典。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mir` | `MIRProgram` | |

**返回**: `dict`

Source: `src\mir.py:689`

---

# `mir2_frontend`

```matha

```

**描述**: Matha 多语言前端实现 - 将其他语言源码转换为 Matha MIR

当前实现：
  - Python → Matha MIR (基于 AST 模块)
  - 预留接口：Rust/Go/JS/C → Matha MIR

设计原则：
  1. 每门语言有独立的前端模块
  2. 统一输出 MIR²（二级中间表示）
  3. 类型推断 + 效应分析
  4. 错误恢复 + 渐进式解析

Source: `src\mir2_frontend.py:1`

---

## `Effect`

```matha
class Effect(Enum)
```

**描述**: 效应类型。

Source: `src\mir2_frontend.py:25`

---

## `TypeInfo`

```matha
class TypeInfo(Enum)
```

**描述**: 类型信息。

Source: `src\mir2_frontend.py:35`

---

## `MIR2Expr`

```matha
class MIR2Expr
```

**描述**: MIR² 表达式。

**装饰器**: dataclass

Source: `src\mir2_frontend.py:48`

---

### `__repr__`

```matha
def __repr__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mir2_frontend.py:58`

---

## `MIR2Function`

```matha
class MIR2Function
```

**描述**: MIR² 函数。

**装饰器**: dataclass

Source: `src\mir2_frontend.py:63`

---

## `MIR2Program`

```matha
class MIR2Program
```

**描述**: MIR² 程序。

**装饰器**: dataclass

Source: `src\mir2_frontend.py:75`

---

## `PythonFrontend`

```matha
class PythonFrontend
```

**描述**: Python → MIR² 前端。

Source: `src\mir2_frontend.py:87`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mir2_frontend.py:90`

---

### `compile`

```matha
def compile(self, source: str) -> Any
```

**描述**: 编译 Python 源码为 CompileResult（兼容多语言前端接口）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `Any`

Source: `src\mir2_frontend.py:95`

---

### `_compile_function`

```matha
def _compile_function(self, node: ast.FunctionDef) -> MIR2Function
```

**描述**: 编译函数定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FunctionDef` | |

**返回**: `MIR2Function`

Source: `src\mir2_frontend.py:121`

---

### `_compile_stmt`

```matha
def _compile_stmt(self, stmt: ast.stmt, func: MIR2Function) -> None
```

**描述**: 编译语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `ast.stmt` | |
| `func` | `MIR2Function` | |

**返回**: `None`

Source: `src\mir2_frontend.py:139`

---

### `_compile_assignment`

```matha
def _compile_assignment(self, node: ast.Assign, context: Any) -> None
```

**描述**: 编译赋值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Assign` | |
| `context` | `Any` | |

**返回**: `None`

Source: `src\mir2_frontend.py:154`

---

### `_compile_expr`

```matha
def _compile_expr(self, node: ast.expr, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.expr` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:195`

---

### `_compile_constant`

```matha
def _compile_constant(self, node: ast.Constant) -> MIR2Expr
```

**描述**: 编译常量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Constant` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:224`

---

### `_compile_name`

```matha
def _compile_name(self, node: ast.Name, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译名称引用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Name` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:238`

---

### `_compile_binop`

```matha
def _compile_binop(self, node: ast.BinOp, func: MIR2Function) -> MIR2Expr
```

**描述**: 编译二元运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.BinOp` | |
| `func` | `MIR2Function` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:246`

---

### `_compile_unaryop`

```matha
def _compile_unaryop(self, node: ast.UnaryOp, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译一元运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.UnaryOp` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:253`

---

### `_compile_call`

```matha
def _compile_call(self, node: ast.Call, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译函数调用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Call` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:261`

---

### `_compile_if`

```matha
def _compile_if(self, node: ast.If, func: MIR2Function) -> None
```

**描述**: 编译 if 语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.If` | |
| `func` | `MIR2Function` | |

**返回**: `None`

Source: `src\mir2_frontend.py:274`

---

### `_compile_while`

```matha
def _compile_while(self, node: ast.While, func: Optional[MIR2Function]) -> None
```

**描述**: 编译 while 语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.While` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `None`

Source: `src\mir2_frontend.py:284`

---

### `_compile_for`

```matha
def _compile_for(self, node: ast.For, func: Optional[MIR2Function]) -> None
```

**描述**: 编译 for 语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.For` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `None`

Source: `src\mir2_frontend.py:293`

---

### `_compile_return`

```matha
def _compile_return(self, node: ast.Return, func: MIR2Function) -> None
```

**描述**: 编译 return 语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Return` | |
| `func` | `MIR2Function` | |

**返回**: `None`

Source: `src\mir2_frontend.py:303`

---

### `_compile_import`

```matha
def _compile_import(self, node: ast.Import, program: MIR2Program) -> None
```

**描述**: 编译 import 语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Import` | |
| `program` | `MIR2Program` | |

**返回**: `None`

Source: `src\mir2_frontend.py:309`

---

### `_compile_import_from`

```matha
def _compile_import_from(self, node: ast.ImportFrom, program: MIR2Program) -> None
```

**描述**: 编译 from ... import 语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ImportFrom` | |
| `program` | `MIR2Program` | |

**返回**: `None`

Source: `src\mir2_frontend.py:314`

---

### `_map_binop`

```matha
def _map_binop(self, op_name: str) -> str
```

**描述**: 映射二元运算符。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op_name` | `str` | |

**返回**: `str`

Source: `src\mir2_frontend.py:321`

---

### `_map_unaryop`

```matha
def _map_unaryop(self, op_name: str) -> str
```

**描述**: 映射一元运算符。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op_name` | `str` | |

**返回**: `str`

Source: `src\mir2_frontend.py:331`

---

### `_infer_effect`

```matha
def _infer_effect(self, node: ast.FunctionDef) -> Effect
```

**描述**: 推断函数效应。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FunctionDef` | |

**返回**: `Effect`

Source: `src\mir2_frontend.py:336`

---

### `_get_name`

```matha
def _get_name(self, node: ast.AST) -> str
```

**描述**: 提取名称。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.AST` | |

**返回**: `str`

Source: `src\mir2_frontend.py:348`

---

### `_compile_boolop`

```matha
def _compile_boolop(self, node: ast.BoolOp, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译布尔运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.BoolOp` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:356`

---

### `_compile_compare`

```matha
def _compile_compare(self, node: ast.Compare, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译比较运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Compare` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:364`

---

### `_compile_list`

```matha
def _compile_list(self, node: ast.List, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.List` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:373`

---

### `_compile_dict`

```matha
def _compile_dict(self, node: ast.Dict, func: Optional[MIR2Function]) -> MIR2Expr
```

**描述**: 编译字典。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Dict` | |
| `func` | `Optional[MIR2Function]` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:380`

---

### `_compile_subscript`

```matha
def _compile_subscript(self, node: ast.Subscript, func: MIR2Expr) -> MIR2Expr
```

**描述**: 编译下标访问。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Subscript` | |
| `func` | `MIR2Expr` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:388`

---

### `_compile_attribute`

```matha
def _compile_attribute(self, node: ast.Attribute, func: MIR2Expr) -> MIR2Expr
```

**描述**: 编译属性访问。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Attribute` | |
| `func` | `MIR2Expr` | |

**返回**: `MIR2Expr`

Source: `src\mir2_frontend.py:394`

---

### `_map_cmpop`

```matha
def _map_cmpop(self, op_name: str) -> str
```

**描述**: 映射比较运算符。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `op_name` | `str` | |

**返回**: `str`

Source: `src\mir2_frontend.py:399`

---

## `MultiLanguageFrontend`

```matha
class MultiLanguageFrontend
```

**描述**: 多语言前端统一入口。

Source: `src\mir2_frontend.py:413`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mir2_frontend.py:416`

---

### `register`

```matha
def register(self, lang: str, frontend: Any) -> None
```

**描述**: 注册语言前端。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `frontend` | `Any` | |

**返回**: `None`

Source: `src\mir2_frontend.py:421`

---

### `compile`

```matha
def compile(self, source: str, language: str) -> MIR2Program
```

**描述**: 编译源码为 MIR²。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `language` | `str` | |

**返回**: `MIR2Program`

Source: `src\mir2_frontend.py:425`

---

### `supported_languages`

```matha
def supported_languages(self) -> list[str]
```

**描述**: 返回支持的语言列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\mir2_frontend.py:432`

---

# `mir_codegen`

```matha

```

**描述**: Matha MIR → C/Python 代码生成器

生成高效 C 代码，可直接编译为原生机器码。
生成优化 Python 代码，可直接解释执行。

优化策略：
  1. 内联数学函数调用
  2. 消除冗余计算
  3. 寄存器分配
  4. 常量折叠
  5. 死代码消除

Source: `src\mir_codegen.py:1`

---

## `MIRToCGenerator`

```matha
class MIRToCGenerator
```

**描述**: 将 Matha MIR 转换为 C 代码。

Source: `src\mir_codegen.py:22`

---

### `__init__`

```matha
def __init__(self, optimize: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `optimize` | `bool` | |

**返回**: `None`

Source: `src\mir_codegen.py:48`

---

### `generate`

```matha
def generate(self, mir_program: any) -> str
```

**描述**: 生成完整 C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_program` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:53`

---

### `_generate_function`

```matha
def _generate_function(self, name: str, func: any) -> list[str]
```

**描述**: 生成函数定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `any` | |

**返回**: `list[str]`

Source: `src\mir_codegen.py:74`

---

### `_generate_locals`

```matha
def _generate_locals(self, func: any) -> tuple[list[str], list[str]]
```

**描述**: 生成本地变量声明和初始化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `any` | |

**返回**: `tuple[list[str], list[str]]`

Source: `src\mir_codegen.py:121`

---

### `_generate_instructions`

```matha
def _generate_instructions(self, instructions: list) -> list[str]
```

**描述**: 生成指令代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `instructions` | `list` | |

**返回**: `list[str]`

Source: `src\mir_codegen.py:136`

---

### `_generate_instr_code`

```matha
def _generate_instr_code(self, instr: any) -> Optional[str]
```

**描述**: 生成单条指令的 C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `instr` | `any` | |

**返回**: `Optional[str]`

Source: `src\mir_codegen.py:145`

---

### `_optimize`

```matha
def _optimize(self, source: str) -> str
```

**描述**: 优化 C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\mir_codegen.py:201`

---

## `MIRToPythonGenerator`

```matha
class MIRToPythonGenerator
```

**描述**: 将 Matha MIR 转换为优化的 Python 代码。

Source: `src\mir_codegen.py:218`

---

### `__init__`

```matha
def __init__(self, optimize: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `optimize` | `bool` | |

**返回**: `None`

Source: `src\mir_codegen.py:221`

---

### `generate`

```matha
def generate(self, mir_program: any) -> str
```

**描述**: 生成 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_program` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:224`

---

### `_generate_function`

```matha
def _generate_function(self, name: str, func: any) -> list[str]
```

**描述**: 生成 Python 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `any` | |

**返回**: `list[str]`

Source: `src\mir_codegen.py:241`

---

### `_generate_instr_code`

```matha
def _generate_instr_code(self, instr: any) -> str
```

**描述**: 生成单条指令的 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `instr` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:271`

---

## `MathaCodeGenerator`

```matha
class MathaCodeGenerator
```

**描述**: Matha 统一代码生成器。

Source: `src\mir_codegen.py:335`

---

### `__init__`

```matha
def __init__(self, target: str, optimize: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target` | `str` | |
| `optimize` | `bool` | |

**返回**: `None`

Source: `src\mir_codegen.py:338`

---

### `generate`

```matha
def generate(self, mir_program: any) -> str
```

**描述**: 生成目标代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_program` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:344`

---

### `compile_and_run`

```matha
def compile_and_run(self, mir_program: any, args: list) -> tuple
```

**描述**: 编译并运行生成的代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_program` | `any` | |
| `args` | `list` | |

**返回**: `tuple`

Source: `src\mir_codegen.py:353`

---

## `generate_c_code`

```matha
def generate_c_code(mir_program: any) -> str
```

**描述**: 生成 C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mir_program` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:379`

---

## `generate_python_code`

```matha
def generate_python_code(mir_program: any) -> str
```

**描述**: 生成 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `mir_program` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:387`

---

## `compile_to_c`

```matha
def compile_to_c(ast_or_mir: any) -> str
```

**描述**: 编译 MIR 为 C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ast_or_mir` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:395`

---

## `compile_to_python`

```matha
def compile_to_python(ast_or_mir: any) -> str
```

**描述**: 编译 MIR 为 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ast_or_mir` | `any` | |

**返回**: `str`

Source: `src\mir_codegen.py:400`

---

# `mir_converter`

```matha

```

**描述**: Matha MIR → Matha 代码生成器

将 MIR 中间表示转换回 Matha 源语言，实现三向互转。

Source: `src\mir_converter.py:1`

---

## `MIRToMathaGenerator`

```matha
class MIRToMathaGenerator
```

**描述**: 将 MIR 转换为 Matha 源语言。

Source: `src\mir_converter.py:13`

---

### `__init__`

```matha
def __init__(self, indent: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `indent` | `int` | |

**返回**: `None`

Source: `src\mir_converter.py:34`

---

### `generate`

```matha
def generate(self, mir_program: any) -> str
```

**描述**: 生成 Matha 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_program` | `any` | |

**返回**: `str`

Source: `src\mir_converter.py:39`

---

### `_generate_main`

```matha
def _generate_main(self, func: any) -> list[str]
```

**描述**: 生成 main 函数体。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `any` | |

**返回**: `list[str]`

Source: `src\mir_converter.py:63`

---

### `_generate_function`

```matha
def _generate_function(self, name: str, func: any) -> list[str]
```

**描述**: 生成 Matha 函数定义。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `any` | |

**返回**: `list[str]`

Source: `src\mir_converter.py:76`

---

### `_generate_instr_code`

```matha
def _generate_instr_code(self, instr: any) -> Optional[str]
```

**描述**: 生成单条指令的 Matha 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `instr` | `any` | |

**返回**: `Optional[str]`

Source: `src\mir_converter.py:91`

---

### `_resolve_var`

```matha
def _resolve_var(self, var: str) -> str
```

**描述**: 解析变量，替换为 Matha 变量名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `str` | |

**返回**: `str`

Source: `src\mir_converter.py:149`

---

### `_new_var`

```matha
def _new_var(self) -> str
```

**描述**: 生成新变量名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir_converter.py:154`

---

## `MathaToMIRGenerator`

```matha
class MathaToMIRGenerator
```

**描述**: 将 Matha 源码转换回 MIR（反向生成器）。

Source: `src\mir_converter.py:160`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_converter.py:163`

---

### `_new_temp`

```matha
def _new_temp(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir_converter.py:166`

---

### `generate_from_matha`

```matha
def generate_from_matha(self, matha_source: str) -> str
```

**描述**: 从 Matha 源码生成 MIR 文本表示。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_source` | `str` | |

**返回**: `str`

Source: `src\mir_converter.py:170`

---

## `MathaConverter`

```matha
class MathaConverter
```

**描述**: Matha 三向转换器：Matha ↔ C ↔ Python。

Source: `src\mir_converter.py:196`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_converter.py:201`

---

### `convert`

```matha
def convert(self, source: str, source_lang: str, target_lang: str) -> str
```

**描述**: 将 source 从 source_lang 转换为 target_lang。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `source_lang` | `str` | |
| `target_lang` | `str` | |

**返回**: `str`

Source: `src\mir_converter.py:207`

---

### `_to_mir`

```matha
def _to_mir(self, source: str, lang: str) -> any
```

**描述**: 将源语言转换为 MIR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `lang` | `str` | |

**返回**: `any`

Source: `src\mir_converter.py:217`

---

### `_from_mir`

```matha
def _from_mir(self, mir_program: any, lang: str) -> str
```

**描述**: 从 MIR 转换为目标语言。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_program` | `any` | |
| `lang` | `str` | |

**返回**: `str`

Source: `src\mir_converter.py:233`

---

### `_c_to_mir`

```matha
def _c_to_mir(self, c_code: str) -> any
```

**描述**: 将 C 代码解析为 MIR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `c_code` | `str` | |

**返回**: `any`

Source: `src\mir_converter.py:246`

---

### `_python_to_mir`

```matha
def _python_to_mir(self, py_code: str) -> any
```

**描述**: 将 Python 代码解析为 MIR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `py_code` | `str` | |

**返回**: `any`

Source: `src\mir_converter.py:297`

---

### `batch_convert`

```matha
def batch_convert(self, source: str, target_langs: list) -> dict
```

**描述**: 批量转换为多种目标语言。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `target_langs` | `list` | |

**返回**: `dict`

Source: `src\mir_converter.py:348`

---

## `matha_to_mir`

```matha
def matha_to_mir(matha_source: str) -> str
```

**描述**: 将 Matha 源码转换为 MIR 文本表示。

| 参数 | 类型 | 说明 |
|------|------|------|
| `matha_source` | `str` | |

**返回**: `str`

Source: `src\mir_converter.py:363`

---

## `convert`

```matha
def convert(source: str, source_lang: str, target_lang: str) -> str
```

**描述**: 通用转换器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `source_lang` | `str` | |
| `target_lang` | `str` | |

**返回**: `str`

Source: `src\mir_converter.py:369`

---

## `convert_all`

```matha
def convert_all(source: str, source_lang: str) -> dict
```

**描述**: 转换为所有支持的语言。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `source_lang` | `str` | |

**返回**: `dict`

Source: `src\mir_converter.py:375`

---

# `mir_opt`

```matha

```

**描述**: Matha MIR 优化 Pass - 完整实现

优化策略：
  1. 常量折叠：预计算常量表达式
  2. 死代码消除：移除未使用的变量和不可达代码
  3. 内联优化：将小函数内联到调用处
  4. 窥孔优化：消除冗余指令
  5. 公共子表达式消除：识别并消除重复计算
  6. 复制传播：用常量/变量替换中间变量
  7. 强度削弱：用低成本操作替换高成本操作
  8. 代数简化：应用代数恒等式简化表达式

Source: `src\mir_opt.py:1`

---

## `_get_var`

```matha
def _get_var(name: str) -> str
```

**描述**: 提取变量名（去除 % 前缀）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |

**返回**: `str`

Source: `src\mir_opt.py:24`

---

## `_get_op`

```matha
def _get_op(instr: Any) -> str
```

**描述**: 安全获取操作符。

| 参数 | 类型 | 说明 |
|------|------|------|
| `instr` | `Any` | |

**返回**: `str`

Source: `src\mir_opt.py:29`

---

## `_is_const`

```matha
def _is_const(val: Any) -> bool
```

**描述**: 检查值是否为常量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `val` | `Any` | |

**返回**: `bool`

Source: `src\mir_opt.py:34`

---

## `_parse_const`

```matha
def _parse_const(val: Any) -> float | None
```

**描述**: 解析常量值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `val` | `Any` | |

**返回**: `float | None`

Source: `src\mir_opt.py:47`

---

## `_eval_const`

```matha
def _eval_const(left: float, right: float, op: str) -> float | None
```

**描述**: 评估常量运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `left` | `float` | |
| `right` | `float` | |
| `op` | `str` | |

**返回**: `float | None`

Source: `src\mir_opt.py:59`

---

## `_simplify_expr`

```matha
def _simplify_expr(left: float, right: float, op: str) -> tuple[str, bool]
```

**描述**: 简化表达式，返回 (简化后的字符串, 是否可简化)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `left` | `float` | |
| `right` | `float` | |
| `op` | `str` | |

**返回**: `tuple[str, bool]`

Source: `src\mir_opt.py:73`

---

## `MathaConstFoldPass`

```matha
class MathaConstFoldPass
```

**描述**: 常量折叠优化：将常量表达式预计算。

Source: `src\mir_opt.py:112`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:115`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:118`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:149`

---

## `MathaSimplifyPass`

```matha
class MathaSimplifyPass
```

**描述**: 代数简化：应用代数恒等式简化表达式。

Source: `src\mir_opt.py:157`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:160`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:163`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:191`

---

## `MathaDeadCodeElimPass`

```matha
class MathaDeadCodeElimPass
```

**描述**: 死代码消除：移除未使用的变量、不可达代码和冗余指令。

Source: `src\mir_opt.py:199`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:202`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:206`

---

### `_eliminate_dead_code`

```matha
def _eliminate_dead_code(self, func: Any) -> Any
```

**描述**: 基于数据流分析消除死代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:213`

---

### `_remove_unreachable`

```matha
def _remove_unreachable(self, func: Any) -> Any
```

**描述**: 移除不可达代码（分支后的死代码）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:247`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:262`

---

## `MathaCommonSubexprElimPass`

```matha
class MathaCommonSubexprElimPass
```

**描述**: 公共子表达式消除：识别并消除重复计算。

Source: `src\mir_opt.py:270`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:273`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:276`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:309`

---

## `MathaCopyPropagationPass`

```matha
class MathaCopyPropagationPass
```

**描述**: 复制传播：用常量/变量替换中间变量。

Source: `src\mir_opt.py:317`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:320`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:323`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:367`

---

## `MathaStrengthReductionPass`

```matha
class MathaStrengthReductionPass
```

**描述**: 强度削弱：用低成本操作替换高成本操作。

Source: `src\mir_opt.py:375`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:378`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:381`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:445`

---

## `MathaInlinePass`

```matha
class MathaInlinePass
```

**描述**: 内联优化：将小函数内联到调用处。

Source: `src\mir_opt.py:453`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:456`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:459`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:479`

---

## `MathaPeepholeOptimizer`

```matha
class MathaPeepholeOptimizer
```

**描述**: 窥孔优化：消除冗余指令。

Source: `src\mir_opt.py:487`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:490`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:493`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:523`

---

## `MathaOptimizationPipeline`

```matha
class MathaOptimizationPipeline
```

**描述**: 优化管道：按顺序运行所有优化 Pass。

Source: `src\mir_opt.py:531`

---

### `__init__`

```matha
def __init__(self, aggressive: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `aggressive` | `bool` | |

**返回**: `None`

Source: `src\mir_opt.py:534`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:549`

---

### `get_summary`

```matha
def get_summary(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir_opt.py:554`

---

## `MathaTailRecPass`

```matha
class MathaTailRecPass
```

**描述**: 尾递归消除：将尾递归调用转换为循环。

Source: `src\mir_opt.py:573`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:576`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:579`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:595`

---

## `MathaLoopUnrollPass`

```matha
class MathaLoopUnrollPass
```

**描述**: 循环展开：对小循环进行展开优化。

Source: `src\mir_opt.py:603`

---

### `__init__`

```matha
def __init__(self, threshold: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threshold` | `int` | |

**返回**: `None`

Source: `src\mir_opt.py:606`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:610`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:633`

---

## `MathaSIMDPass`

```matha
class MathaSIMDPass
```

**描述**: 自动向量化：将标量运算标记为可向量化。

Source: `src\mir_opt.py:641`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:644`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:647`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:667`

---

## `MathaCurryFlattenPass`

```matha
class MathaCurryFlattenPass
```

**描述**: 柯里化扁平化：将柯里化函数调用展平。

Source: `src\mir_opt.py:675`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mir_opt.py:678`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:681`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\mir_opt.py:697`

---

## `MathaOptimizationPipeline`

```matha
class MathaOptimizationPipeline
```

**描述**: 优化管道：按顺序运行所有优化 Pass。

Source: `src\mir_opt.py:705`

---

### `__init__`

```matha
def __init__(self, aggressive: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `aggressive` | `bool` | |

**返回**: `None`

Source: `src\mir_opt.py:708`

---

### `run`

```matha
def run(self, program: Any) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `Any`

Source: `src\mir_opt.py:727`

---

### `get_summary`

```matha
def get_summary(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\mir_opt.py:732`

---

# `mobile_compat`

```matha

```

**描述**: Matha v4.4 移动端兼容性实现

本模块提供 Matha 在移动端（平板/手机）的兼容性支持：
- 无依赖环境运行（纯 Python NumPy 兼容层）
- 内存优化
- 简洁 API

Source: `src\mobile_compat.py:1`

---

## `MobileCompatibility`

```matha
class MobileCompatibility
```

**描述**: 移动端兼容性管理器

提供：
1. 依赖检测与降级
2. 内存优化配置
3. 简化 API

Source: `src\mobile_compat.py:16`

---

### `__new__`

```matha
def __new__(cls)
```

**描述**: 单例模式：确保只创建一个实例

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

Source: `src\mobile_compat.py:28`

---

### `__init__`

```matha
def __init__(self)
```

**描述**: 初始化（仅执行一次）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mobile_compat.py:36`

---

### `_detect_dependencies`

```matha
def _detect_dependencies(self)
```

**描述**: 检测可用依赖

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mobile_compat.py:47`

---

### `is_mobile`

```matha
def is_mobile(self) -> bool
```

**描述**: 是否运行在移动设备上（带缓存）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\mobile_compat.py:57`

---

### `numpy_available`

```matha
def numpy_available(self) -> bool
```

**描述**: NumPy 兼容层是否可用

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\mobile_compat.py:96`

---

### `get_optimized_matrix_ops`

```matha
def get_optimized_matrix_ops(self)
```

**描述**: 获取优化的矩阵运算函数

Returns:
    dict: 优化的矩阵运算函数映射

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mobile_compat.py:100`

---

### `create_simplified_api`

```matha
def create_simplified_api(self)
```

**描述**: 创建简化的移动端 API

Returns:
    SimplifiedAPI: 简化的 API 对象

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mobile_compat.py:142`

---

### `optimize_for_mobile`

```matha
def optimize_for_mobile(self)
```

**描述**: 优化移动端配置

启用：
- 内存限制
- 简化计算
- 减少缓存

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\mobile_compat.py:201`

---

## `get_mobile_api`

```matha
def get_mobile_api()
```

**描述**: 获取移动端 API

Returns:
    SimplifiedAPI: 简化的 API 对象

Source: `src\mobile_compat.py:215`

---

## `is_mobile_device`

```matha
def is_mobile_device() -> bool
```

**描述**: 检测是否为移动设备

Returns:
    bool: 是否运行在移动设备上

**返回**: `bool`

Source: `src\mobile_compat.py:226`

---

# `mobile_full`

```matha

```

**描述**: Matha 移动端完整实现

提供完整的移动端支持和 Flutter 外壳：
  - 设备检测增强
  - 触摸交互优化
  - Flutter 外壳框架
  - 离线缓存层

Source: `src\mobile_full.py:1`

---

## `MobileConfig`

```matha
class MobileConfig
```

**描述**: 移动端配置

**装饰器**: dataclass

Source: `src\mobile_full.py:22`

---

## `MobileDeviceDetector`

```matha
class MobileDeviceDetector
```

**描述**: 增强的移动设备检测

Source: `src\mobile_full.py:32`

---

### `detect`

```matha
def detect(cls) -> bool
```

**描述**: 检测是否运行在移动设备上

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `bool`

**装饰器**: classmethod

Source: `src\mobile_full.py:38`

---

## `MobileAPI`

```matha
class MobileAPI
```

**描述**: 移动端简化的数学 API

Source: `src\mobile_full.py:68`

---

### `__init__`

```matha
def __init__(self, config: Optional[MobileConfig])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `config` | `Optional[MobileConfig]` | |

Source: `src\mobile_full.py:71`

---

### `_detect_backend`

```matha
def _detect_backend(self) -> None
```

**描述**: 检测可用后端

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\mobile_full.py:77`

---

### `zeros`

```matha
def zeros(self, shape: tuple) -> Any
```

**描述**: 创建零矩阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `shape` | `tuple` | |

**返回**: `Any`

Source: `src\mobile_full.py:97`

---

### `eye`

```matha
def eye(self, n: int) -> Any
```

**描述**: 单位矩阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |

**返回**: `Any`

Source: `src\mobile_full.py:115`

---

### `matmul`

```matha
def matmul(self, a: Any, b: Any) -> Any
```

**描述**: 矩阵乘法

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `a` | `Any` | |
| `b` | `Any` | |

**返回**: `Any`

Source: `src\mobile_full.py:128`

---

### `svd`

```matha
def svd(self, matrix: Any) -> tuple
```

**描述**: SVD 分解

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matrix` | `Any` | |

**返回**: `tuple`

Source: `src\mobile_full.py:135`

---

### `clear_cache`

```matha
def clear_cache(self) -> int
```

**描述**: 清除缓存，释放内存

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\mobile_full.py:141`

---

## `FlutterShell`

```matha
class FlutterShell
```

**描述**: Flutter 外壳框架

提供 Flutter 应用的接口定义，实际 Flutter 代码需单独开发。
此处提供 Python 端的协议定义。

Source: `src\mobile_full.py:149`

---

### `create_init_message`

```matha
def create_init_message(app_name: str, version: str) -> dict
```

**描述**: 创建初始化消息

| 参数 | 类型 | 说明 |
|------|------|------|
| `app_name` | `str` | |
| `version` | `str` | |

**返回**: `dict`

**装饰器**: staticmethod

Source: `src\mobile_full.py:164`

---

### `create_math_request`

```matha
def create_math_request(request_id: str, operation: str, params: list) -> dict
```

**描述**: 创建数学计算请求

| 参数 | 类型 | 说明 |
|------|------|------|
| `request_id` | `str` | |
| `operation` | `str` | |
| `params` | `list` | |

**返回**: `dict`

**装饰器**: staticmethod

Source: `src\mobile_full.py:175`

---

### `create_math_response`

```matha
def create_math_response(request_id: str, result: Any, error: Optional[str]) -> dict
```

**描述**: 创建数学计算响应

| 参数 | 类型 | 说明 |
|------|------|------|
| `request_id` | `str` | |
| `result` | `Any` | |
| `error` | `Optional[str]` | |

**返回**: `dict`

**装饰器**: staticmethod

Source: `src\mobile_full.py:185`

---

### `create_storage_request`

```matha
def create_storage_request(request_id: str, action: str, key: str, value: Any) -> dict
```

**描述**: 创建存储请求

| 参数 | 类型 | 说明 |
|------|------|------|
| `request_id` | `str` | |
| `action` | `str` | |
| `key` | `str` | |
| `value` | `Any` | |

**返回**: `dict`

**装饰器**: staticmethod

Source: `src\mobile_full.py:195`

---

### `create_collab_request`

```matha
def create_collab_request(request_id: str, action: str, data: dict) -> dict
```

**描述**: 创建协作请求

| 参数 | 类型 | 说明 |
|------|------|------|
| `request_id` | `str` | |
| `action` | `str` | |
| `data` | `dict` | |

**返回**: `dict`

**装饰器**: staticmethod

Source: `src\mobile_full.py:206`

---

## `get_mobile_config`

```matha
def get_mobile_config() -> MobileConfig
```

**描述**: 获取移动端配置

**返回**: `MobileConfig`

Source: `src\mobile_full.py:223`

---

## `get_mobile_api`

```matha
def get_mobile_api() -> MobileAPI
```

**描述**: 获取移动端 API

**返回**: `MobileAPI`

Source: `src\mobile_full.py:231`

---

## `is_mobile`

```matha
def is_mobile() -> bool
```

**描述**: 检测是否运行在移动设备上

**返回**: `bool`

Source: `src\mobile_full.py:239`

---

## `get_mobile_state`

```matha
def get_mobile_state() -> dict
```

**描述**: 获取移动端状态

**返回**: `dict`

Source: `src\mobile_full.py:247`

---

# `multi_lang_codegen`

```matha

```

**描述**: Matha 多语言代码生成引擎 v2.0
==============================
支持语言：Python / JavaScript / C / C++ / Rust / Go / Java / Matha

新增语言后端：
  - CppGenerator   — C++ 高性能计算
  - RustGenerator  — Rust 内存安全 + 零成本抽象
  - GoGenerator    — Go 原生并发 + GC
  - JavaGenerator  — Java JVM 跨平台

符号简化兼容：
  - >> 步进迭代  →  for x in range(a, b)
  - >> 属于判断  →  x in S
  - >> 路径距离  →  distance(a, b)
  - >> 链式调用  →  a |> b (使用 Pipe 运算符，Python 3.12+)

设计原则：
  1. 所有后端共享同一 AST 解释器
  2. 生成的代码可直接编译/运行验证
  3. 支持 MultiLangVerifier 交叉验证

Source: `src\multi_lang_codegen.py:1`

---

## `SymbolCompat`

```matha
class SymbolCompat
```

**描述**: 简化 >> 四重语义为单语义 + 显式转换函数。

原始语义 → 简化后:
  >> 步进迭代   →  for x in range(a, b)
  >> 属于判断   →  x in S
  >> 路径距离   →  distance(a, b)
  >> 链式调用   →  a | b  (pipe)

Source: `src\multi_lang_codegen.py:36`

---

### `simplify`

```matha
def simplify(expr: str) -> str
```

**描述**: 将简化符号表达式转为标准表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\multi_lang_codegen.py:59`

---

### `to_pipe`

```matha
def to_pipe(expr: str) -> str
```

**描述**: 转换为管道表达式 (Python 3.12+ support)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\multi_lang_codegen.py:68`

---

## `CppGenerator`

```matha
class CppGenerator
```

**描述**: 生成 C++ 代码（支持面向对象 + 模板 + STL）。

Source: `src\multi_lang_codegen.py:77`

---

### `__init__`

```matha
def __init__(self, func_name: str, return_type: str, use_templates: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `return_type` | `str` | |
| `use_templates` | `bool` | |

Source: `src\multi_lang_codegen.py:80`

---

### `emit`

```matha
def emit(self, line: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

Source: `src\multi_lang_codegen.py:89`

---

### `block`

```matha
def block(self, fn)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

Source: `src\multi_lang_codegen.py:92`

---

### `get_code`

```matha
def get_code(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:97`

---

### `generate_function`

```matha
def generate_function(self, params: List[tuple], expr: str) -> str
```

**描述**: 生成完整函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:100`

---

### `generate_class`

```matha
def generate_class(self, class_name: str, methods: List[dict]) -> str
```

**描述**: 生成 C++ 类。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `class_name` | `str` | |
| `methods` | `List[dict]` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:125`

---

### `generate_main`

```matha
def generate_main(self, func_name: str, test_cases: List[tuple]) -> str
```

**描述**: 生成 main 测试函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `test_cases` | `List[tuple]` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:140`

---

### `_expr_to_cpp`

```matha
def _expr_to_cpp(self, expr: str) -> str
```

**描述**: 表达式转 C++。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:154`

---

## `RustGenerator`

```matha
class RustGenerator
```

**描述**: 生成 Rust 代码（内存安全 + 零成本抽象）。

Source: `src\multi_lang_codegen.py:175`

---

### `__init__`

```matha
def __init__(self, func_name: str, return_type: str, generic: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `return_type` | `str` | |
| `generic` | `bool` | |

Source: `src\multi_lang_codegen.py:178`

---

### `get_code`

```matha
def get_code(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:186`

---

### `generate_function`

```matha
def generate_function(self, params: List[tuple], expr: str) -> str
```

**描述**: 生成完整函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:189`

---

### `generate_trait`

```matha
def generate_trait(self, trait_name: str, methods: List[dict]) -> str
```

**描述**: 生成 Rust Trait。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `trait_name` | `str` | |
| `methods` | `List[dict]` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:215`

---

### `generate_main`

```matha
def generate_main(self) -> str
```

**描述**: 生成 main 测试入口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:228`

---

### `_expr_to_rust`

```matha
def _expr_to_rust(self, expr: str) -> str
```

**描述**: 表达式转 Rust。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:236`

---

### `_get_expected`

```matha
def _get_expected(self, params: List[tuple]) -> str
```

**描述**: 生成预期值占位符。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params` | `List[tuple]` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:252`

---

## `GoGenerator`

```matha
class GoGenerator
```

**描述**: 生成 Go 代码（原生并发 + GC + 快速编译）。

Source: `src\multi_lang_codegen.py:261`

---

### `__init__`

```matha
def __init__(self, func_name: str, return_type: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `return_type` | `str` | |

Source: `src\multi_lang_codegen.py:264`

---

### `get_code`

```matha
def get_code(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:270`

---

### `generate_function`

```matha
def generate_function(self, params: List[tuple], expr: str) -> str
```

**描述**: 生成完整函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:273`

---

### `generate_goroutine`

```matha
def generate_goroutine(self, func_name: str, args: List[tuple]) -> str
```

**描述**: 生成 goroutine 并发示例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `args` | `List[tuple]` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:301`

---

### `_expr_to_go`

```matha
def _expr_to_go(self, expr: str) -> str
```

**描述**: 表达式转 Go。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:313`

---

## `JavaGenerator`

```matha
class JavaGenerator
```

**描述**: 生成 Java 代码（JVM 跨平台 + 强类型）。

Source: `src\multi_lang_codegen.py:334`

---

### `__init__`

```matha
def __init__(self, func_name: str, return_type: str, class_name: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `return_type` | `str` | |
| `class_name` | `str` | |

Source: `src\multi_lang_codegen.py:337`

---

### `get_code`

```matha
def get_code(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:345`

---

### `generate_class`

```matha
def generate_class(self, params: List[tuple], expr: str) -> str
```

**描述**: 生成完整 Java 类。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:348`

---

### `generate_interface`

```matha
def generate_interface(self, interface_name: str, methods: List[dict]) -> str
```

**描述**: 生成 Java 接口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interface_name` | `str` | |
| `methods` | `List[dict]` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:372`

---

### `_expr_to_java`

```matha
def _expr_to_java(self, expr: str) -> str
```

**描述**: 表达式转 Java。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:383`

---

## `CodeGenResult`

```matha
class CodeGenResult
```

**描述**: 代码生成结果。

**装饰器**: dataclass

Source: `src\multi_lang_codegen.py:405`

---

## `MultiLangCodeGen`

```matha
class MultiLangCodeGen
```

**描述**: 多语言代码生成统一入口。

Source: `src\multi_lang_codegen.py:413`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\multi_lang_codegen.py:428`

---

### `generate`

```matha
def generate(self, language: str, func_name: str, params: List[tuple], expr: str, return_type: str) -> CodeGenResult
```

**描述**: 生成指定语言的代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `language` | `str` | |
| `func_name` | `str` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |
| `return_type` | `str` | |

**返回**: `CodeGenResult`

Source: `src\multi_lang_codegen.py:431`

---

### `generate_all`

```matha
def generate_all(self, func_name: str, params: List[tuple], expr: str, return_type: str) -> Dict[str, CodeGenResult]
```

**描述**: 生成所有支持语言的代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |
| `return_type` | `str` | |

**返回**: `Dict[str, CodeGenResult]`

Source: `src\multi_lang_codegen.py:473`

---

### `get_result`

```matha
def get_result(self, language: str) -> Optional[CodeGenResult]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `language` | `str` | |

**返回**: `Optional[CodeGenResult]`

Source: `src\multi_lang_codegen.py:481`

---

### `summary`

```matha
def summary(self) -> str
```

**描述**: 生成生成摘要。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:484`

---

## `generate_cpp`

```matha
def generate_cpp(func_name: str, params: List[tuple], expr: str, return_type: str) -> str
```

**描述**: 快捷生成 C++ 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |
| `return_type` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:497`

---

## `generate_rust`

```matha
def generate_rust(func_name: str, params: List[tuple], expr: str, return_type: str) -> str
```

**描述**: 快捷生成 Rust 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |
| `return_type` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:504`

---

## `generate_go`

```matha
def generate_go(func_name: str, params: List[tuple], expr: str, return_type: str) -> str
```

**描述**: 快捷生成 Go 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |
| `return_type` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:511`

---

## `generate_java`

```matha
def generate_java(func_name: str, params: List[tuple], expr: str, return_type: str, class_name: str) -> str
```

**描述**: 快捷生成 Java 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[tuple]` | |
| `expr` | `str` | |
| `return_type` | `str` | |
| `class_name` | `str` | |

**返回**: `str`

Source: `src\multi_lang_codegen.py:518`

---

# `multi_lang_frontend`

```matha

```

**描述**: 多语言前端统一接口

为 Rust/Go/JavaScript/C 提供统一的源码解析 → Matha MIR 转换接口。

架构：
  ┌────────────────────────────────────────────────────────────────┐
  │                    MultiLanguageFrontend                      │
  │                                                                │
  │  register("rust", RustFrontend())                             │
  │  register("go",    GoFrontend())                              │
  │  register("js",    JSFrontend())                              │
  │  register("c",     CFrontend())                               │
  │  register("python",PythonFrontend())  # 已有                   │
  │                                                                │
  │  compile(source, language) → MIRProgram                        │
  │  infer_types(source, language) → dict                          │
  │  analyze_effects(source, language) → dict                      │
  └────────────────────────────────────────────────────────────────┘

Source: `src\multi_lang_frontend.py:1`

---

## `IRKind`

```matha
class IRKind(Enum)
```

**描述**: 语言无关 IR 节点种类。

Source: `src\multi_lang_frontend.py:53`

---

## `IRNode`

```matha
class IRNode
```

**描述**: 语言无关 IR 节点。

**装饰器**: dataclass

Source: `src\multi_lang_frontend.py:75`

---

### `with_type`

```matha
def with_type(self, typ: Type) -> 'IRNode'
```

**描述**: 返回类型注解后的新节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `typ` | `Type` | |

**返回**: `'IRNode'`

Source: `src\multi_lang_frontend.py:86`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_frontend.py:92`

---

## `CompileResult`

```matha
class CompileResult
```

**描述**: 前端编译结果。

**装饰器**: dataclass

Source: `src\multi_lang_frontend.py:101`

---

### `success`

```matha
def success(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\multi_lang_frontend.py:114`

---

### `to_mir`

```matha
def to_mir(self) -> MIRProgram
```

**描述**: 转换为 Matha MIR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `MIRProgram`

Source: `src\multi_lang_frontend.py:117`

---

## `_ir_to_mir`

```matha
def _ir_to_mir(node: IRNode) -> list[MIRInstr]
```

**描述**: 将 IR 节点转换为 MIR 指令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `node` | `IRNode` | |

**返回**: `list[MIRInstr]`

Source: `src\multi_lang_frontend.py:128`

---

## `RustFrontend`

```matha
class RustFrontend
```

**描述**: Rust → Matha IR 前端（基于正则解析，无需 rustc crate）。

Source: `src\multi_lang_frontend.py:170`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

**描述**: 编译 Rust 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:189`

---

### `_parse_params`

```matha
def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]
```

**描述**: 解析参数列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params_str` | `str` | |

**返回**: `tuple[list[str], dict[str, Type]]`

Source: `src\multi_lang_frontend.py:225`

---

### `_resolve_type`

```matha
def _resolve_type(self, type_str: str) -> Type
```

**描述**: 解析 Rust 类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_str` | `str` | |

**返回**: `Type`

Source: `src\multi_lang_frontend.py:246`

---

### `_parse_body`

```matha
def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]
```

**描述**: 解析函数体。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:265`

---

### `_parse_expr`

```matha
def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]
```

**描述**: 解析表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:335`

---

### `_parse_atom`

```matha
def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]
```

**描述**: 解析原子表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `atom` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:392`

---

### `_tokenize_expr`

```matha
def _tokenize_expr(self, expr: str) -> list[str]
```

**描述**: 表达式分词。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `list[str]`

Source: `src\multi_lang_frontend.py:418`

---

### `_has_io`

```matha
def _has_io(self, body: str) -> bool
```

**描述**: 检测是否有 IO 操作。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body` | `str` | |

**返回**: `bool`

Source: `src\multi_lang_frontend.py:422`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

**描述**: 类型推断。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:427`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

**描述**: 效应分析。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:442`

---

## `GoFrontend`

```matha
class GoFrontend
```

**描述**: Go → Matha IR 前端。

Source: `src\multi_lang_frontend.py:452`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:465`

---

### `_parse_py_params`

```matha
def _parse_py_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]
```

**描述**: 解析 Python 风格参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params_str` | `str` | |

**返回**: `tuple[list[str], dict[str, Type]]`

Source: `src\multi_lang_frontend.py:517`

---

### `_parse_py_return`

```matha
def _parse_py_return(self, ret_expr: str, param_types: dict[str, Type]) -> list[IRNode]
```

**描述**: 从 Python return 表达式生成 IR 节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ret_expr` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:529`

---

### `_parse_py_expr`

```matha
def _parse_py_expr(self, expr_str: str, param_types: Optional[dict[str, Type]]) -> list[IRNode]
```

**描述**: 解析 Python 风格表达式生成 IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `param_types` | `Optional[dict[str, Type]]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:538`

---

### `_parse_py_atom`

```matha
def _parse_py_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `atom` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:595`

---

### `_parse_params`

```matha
def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params_str` | `str` | |

**返回**: `tuple[list[str], dict[str, Type]]`

Source: `src\multi_lang_frontend.py:613`

---

### `_resolve_type`

```matha
def _resolve_type(self, type_str: str) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_str` | `str` | |

**返回**: `Type`

Source: `src\multi_lang_frontend.py:637`

---

### `_parse_body`

```matha
def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:645`

---

### `_parse_expr`

```matha
def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:692`

---

### `_parse_atom`

```matha
def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `atom` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:734`

---

### `_tokenize_expr`

```matha
def _tokenize_expr(self, expr: str) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `list[str]`

Source: `src\multi_lang_frontend.py:752`

---

### `_has_io`

```matha
def _has_io(self, body: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body` | `str` | |

**返回**: `bool`

Source: `src\multi_lang_frontend.py:755`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:758`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:766`

---

## `JSFrontend`

```matha
class JSFrontend
```

**描述**: JavaScript → Matha IR 前端。

Source: `src\multi_lang_frontend.py:775`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:793`

---

### `_parse_params`

```matha
def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params_str` | `str` | |

**返回**: `tuple[list[str], dict[str, Type]]`

Source: `src\multi_lang_frontend.py:825`

---

### `_resolve_type`

```matha
def _resolve_type(self, type_str: str) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_str` | `str` | |

**返回**: `Type`

Source: `src\multi_lang_frontend.py:840`

---

### `_parse_body`

```matha
def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:846`

---

### `_parse_expr`

```matha
def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:901`

---

### `_parse_atom`

```matha
def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `atom` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:966`

---

### `_tokenize_expr`

```matha
def _tokenize_expr(self, expr: str) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `list[str]`

Source: `src\multi_lang_frontend.py:984`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:987`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:991`

---

## `CFrontend`

```matha
class CFrontend
```

**描述**: C → Matha IR 前端。

Source: `src\multi_lang_frontend.py:1002`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:1016`

---

### `_parse_py_params`

```matha
def _parse_py_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]
```

**描述**: 解析 Python 风格参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params_str` | `str` | |

**返回**: `tuple[list[str], dict[str, Type]]`

Source: `src\multi_lang_frontend.py:1068`

---

### `_parse_py_return`

```matha
def _parse_py_return(self, ret_expr: str, param_types: dict[str, Type]) -> list[IRNode]
```

**描述**: 从 Python return 表达式生成 IR 节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ret_expr` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:1080`

---

### `_parse_py_expr`

```matha
def _parse_py_expr(self, expr_str: str, param_types: Optional[dict[str, Type]]) -> list[IRNode]
```

**描述**: 解析 Python 风格表达式生成 IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `param_types` | `Optional[dict[str, Type]]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:1089`

---

### `_parse_py_atom`

```matha
def _parse_py_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `atom` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:1146`

---

### `_parse_params`

```matha
def _parse_params(self, params_str: str) -> tuple[list[str], dict[str, Type]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `params_str` | `str` | |

**返回**: `tuple[list[str], dict[str, Type]]`

Source: `src\multi_lang_frontend.py:1164`

---

### `_resolve_type`

```matha
def _resolve_type(self, type_str: str) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_str` | `str` | |

**返回**: `Type`

Source: `src\multi_lang_frontend.py:1198`

---

### `_parse_body`

```matha
def _parse_body(self, body: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:1206`

---

### `_parse_expr`

```matha
def _parse_expr(self, expr_str: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:1251`

---

### `_parse_atom`

```matha
def _parse_atom(self, atom: str, param_types: dict[str, Type]) -> list[IRNode]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `atom` | `str` | |
| `param_types` | `dict[str, Type]` | |

**返回**: `list[IRNode]`

Source: `src\multi_lang_frontend.py:1305`

---

### `_tokenize_expr`

```matha
def _tokenize_expr(self, expr: str) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `list[str]`

Source: `src\multi_lang_frontend.py:1325`

---

### `_has_io`

```matha
def _has_io(self, body: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body` | `str` | |

**返回**: `bool`

Source: `src\multi_lang_frontend.py:1328`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:1331`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:1340`

---

## `MultiLanguageFrontend`

```matha
class MultiLanguageFrontend
```

**描述**: 多语言前端统一入口。

Source: `src\multi_lang_frontend.py:1349`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\multi_lang_frontend.py:1352`

---

### `register`

```matha
def register(self, language: str, frontend: Any) -> None
```

**描述**: 注册语言前端。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `language` | `str` | |
| `frontend` | `Any` | |

**返回**: `None`

Source: `src\multi_lang_frontend.py:1355`

---

### `compile`

```matha
def compile(self, source: str, language: str) -> CompileResult
```

**描述**: 编译源码为 IR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `language` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:1359`

---

### `infer_types`

```matha
def infer_types(self, source: str, language: str) -> dict[str, Type]
```

**描述**: 类型推断。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `language` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:1367`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str, language: str) -> dict[str, str]
```

**描述**: 效应分析。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |
| `language` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:1374`

---

### `supported_languages`

```matha
def supported_languages(self) -> list[str]
```

**描述**: 返回支持的语言列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\multi_lang_frontend.py:1381`

---

### `to_mir`

```matha
def to_mir(self, result: CompileResult) -> MIRProgram
```

**描述**: 将编译结果转换为 MIR。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `result` | `CompileResult` | |

**返回**: `MIRProgram`

Source: `src\multi_lang_frontend.py:1385`

---

## `_TS_RustAdapter`

```matha
class _TS_RustAdapter
```

**描述**: Rust tree-sitter 适配器。

Source: `src\multi_lang_frontend.py:1396`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:1403`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:1418`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:1422`

---

## `_TS_GoAdapter`

```matha
class _TS_GoAdapter
```

**描述**: Go tree-sitter 适配器。

Source: `src\multi_lang_frontend.py:1427`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:1433`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:1448`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:1451`

---

## `_TS_JSAdapter`

```matha
class _TS_JSAdapter
```

**描述**: JS tree-sitter 适配器。

Source: `src\multi_lang_frontend.py:1455`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:1459`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:1475`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:1478`

---

## `_TS_CAdapter`

```matha
class _TS_CAdapter
```

**描述**: C tree-sitter 适配器。

Source: `src\multi_lang_frontend.py:1483`

---

### `compile`

```matha
def compile(self, source: str) -> CompileResult
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `CompileResult`

Source: `src\multi_lang_frontend.py:1490`

---

### `infer_types`

```matha
def infer_types(self, source: str) -> dict[str, Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, Type]`

Source: `src\multi_lang_frontend.py:1505`

---

### `analyze_effects`

```matha
def analyze_effects(self, source: str) -> dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict[str, str]`

Source: `src\multi_lang_frontend.py:1508`

---

## `get_frontend`

```matha
def get_frontend() -> MultiLanguageFrontend
```

**描述**: 获取全局前端实例。

**返回**: `MultiLanguageFrontend`

Source: `src\multi_lang_frontend.py:1516`

---

# `multi_lang_verifier`

```matha

```

**描述**: Matha 多语言交叉验证引擎 v2.0
==============================
通过多语言代码生成 + 跨语言执行对比，实现：
  1. Matha ↔ C++ 双向验证
  2. Matha ↔ Rust 双向验证
  3. Matha ↔ Go 双向验证
  4. Matha ↔ Java 双向验证
  5. 自动编译 + 执行 + 对比
  6. 失败自动回滚

核心能力：
  - 生成多语言参考实现
  - 编译 C++/Rust/Go/Java 到原生可执行文件
  - 执行所有实现并对比结果
  - 自动生成性能基准对比
  - 验证通过后自动升级 Matha 实现

Source: `src\multi_lang_verifier.py:1`

---

## `CompareResult`

```matha
class CompareResult
```

**描述**: 单次对比结果。

**装饰器**: dataclass

Source: `src\multi_lang_verifier.py:38`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_verifier.py:49`

---

## `MultiLangVerification`

```matha
class MultiLangVerification
```

**描述**: 多语言交叉验证汇总。

**装饰器**: dataclass

Source: `src\multi_lang_verifier.py:57`

---

### `all_passed`

```matha
def all_passed(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\multi_lang_verifier.py:67`

---

### `summary`

```matha
def summary(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_verifier.py:70`

---

## `MultiLangCodeGenerator`

```matha
class MultiLangCodeGenerator
```

**描述**: 生成用于验证的多语言参考实现代码。

Source: `src\multi_lang_verifier.py:89`

---

### `gen_python`

```matha
def gen_python(func_name: str, params: List[str], expr: str) -> str
```

**描述**: 生成 Python 参考实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[str]` | |
| `expr` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\multi_lang_verifier.py:93`

---

### `gen_cpp`

```matha
def gen_cpp(func_name: str, params: List[str], expr: str) -> str
```

**描述**: 生成 C++ 参考实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[str]` | |
| `expr` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\multi_lang_verifier.py:101`

---

### `gen_rust`

```matha
def gen_rust(func_name: str, params: List[str], expr: str) -> str
```

**描述**: 生成 Rust 参考实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[str]` | |
| `expr` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\multi_lang_verifier.py:126`

---

### `gen_go`

```matha
def gen_go(func_name: str, params: List[str], expr: str) -> str
```

**描述**: 生成 Go 参考实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[str]` | |
| `expr` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\multi_lang_verifier.py:147`

---

### `gen_java`

```matha
def gen_java(func_name: str, params: List[str], expr: str) -> str
```

**描述**: 生成 Java 参考实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |
| `params` | `List[str]` | |
| `expr` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\multi_lang_verifier.py:170`

---

## `MultiLangCompiler`

```matha
class MultiLangCompiler
```

**描述**: 编译多语言代码到可执行文件。

Source: `src\multi_lang_verifier.py:196`

---

### `__init__`

```matha
def __init__(self, work_dir: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `work_dir` | `str` | |

Source: `src\multi_lang_verifier.py:199`

---

### `compile_cpp`

```matha
def compile_cpp(self, code: str, func_name: str) -> str
```

**描述**: 编译 C++ 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `func_name` | `str` | |

**返回**: `str`

Source: `src\multi_lang_verifier.py:203`

---

### `compile_rust`

```matha
def compile_rust(self, code: str, func_name: str) -> str
```

**描述**: 编译 Rust 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `func_name` | `str` | |

**返回**: `str`

Source: `src\multi_lang_verifier.py:224`

---

### `compile_go`

```matha
def compile_go(self, code: str, func_name: str) -> str
```

**描述**: 编译 Go 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `func_name` | `str` | |

**返回**: `str`

Source: `src\multi_lang_verifier.py:245`

---

### `compile_java`

```matha
def compile_java(self, code: str, func_name: str) -> str
```

**描述**: 编译 Java 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `func_name` | `str` | |

**返回**: `str`

Source: `src\multi_lang_verifier.py:266`

---

### `run_executable`

```matha
def run_executable(self, exe: str, input_args: List[float]) -> Tuple[float, str]
```

**描述**: 运行可执行文件并返回结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exe` | `str` | |
| `input_args` | `List[float]` | |

**返回**: `Tuple[float, str]`

Source: `src\multi_lang_verifier.py:286`

---

## `MultiLangVerifier`

```matha
class MultiLangVerifier
```

**描述**: 多语言交叉验证引擎。

工作流程：
  1. 生成 Matha 函数 + 多语言参考实现
  2. 编译 C++/Rust/Go/Java
  3. 执行所有实现（含 Matha 自身）
  4. 对比结果 + 性能基准
  5. 生成验证报告

Source: `src\multi_lang_verifier.py:310`

---

### `__init__`

```matha
def __init__(self, matha_impl)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matha_impl` | `` | |

Source: `src\multi_lang_verifier.py:322`

---

### `verify`

```matha
def verify(self, func_name: str, params: List[str], expr: str, test_cases: List[Tuple[List[Any], Any]], matha_func) -> MultiLangVerification
```

**描述**: 执行多语言交叉验证。

Args:
    func_name: 函数名
    params: 参数名列表
    expr: Matha 表达式
    test_cases: [(输入参数列表, 期望输出), ...]
    matha_func: Matha 函数实现（可选，用于动态调用）

Returns:
    MultiLangVerification 验证结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `params` | `List[str]` | |
| `expr` | `str` | |
| `test_cases` | `List[Tuple[List[Any], Any]]` | |
| `matha_func` | `` | |

**返回**: `MultiLangVerification`

Source: `src\multi_lang_verifier.py:328`

---

### `_close_enough`

```matha
def _close_enough(self, a: float, b: float, tolerance: float) -> bool
```

**描述**: 判断两个浮点数是否足够接近。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `a` | `float` | |
| `b` | `float` | |
| `tolerance` | `float` | |

**返回**: `bool`

Source: `src\multi_lang_verifier.py:466`

---

### `get_verification`

```matha
def get_verification(self, func_name: str) -> Optional[MultiLangVerification]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |

**返回**: `Optional[MultiLangVerification]`

Source: `src\multi_lang_verifier.py:472`

---

### `generate_report`

```matha
def generate_report(self) -> str
```

**描述**: 生成验证报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\multi_lang_verifier.py:475`

---

## `PerformanceBenchmark`

```matha
class PerformanceBenchmark
```

**描述**: 性能基准测试。

Source: `src\multi_lang_verifier.py:488`

---

### `benchmark_func`

```matha
def benchmark_func(func, args, iterations: int) -> dict
```

**描述**: 基准测试单个函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `` | |
| `args` | `` | |
| `iterations` | `int` | |

**返回**: `dict`

**装饰器**: staticmethod

Source: `src\multi_lang_verifier.py:492`

---

# `multi_paradigm`

```matha

```

**描述**: Matha 多范式引擎 v1.3.0
========================
融合函数式/命令式/逻辑式/符号式编程范式的统一执行引擎。

功能：
  • Functional  — 纯函数式计算（lambda/柯里化/高阶函数）
  • Imperative  — 命令式状态机（变量/循环/分支）
  • Symbolic    — 符号代数（表达式/微积分/简化）
  • Logic       — 逻辑推理（规则/约束/推导）
  • Paradigm    — 范式切换器（根据任务自动选择）

Source: `src\multi_paradigm.py:1`

---

## `Paradigm`

```matha
class Paradigm(str, Enum)
```

Source: `src\multi_paradigm.py:34`

---

## `ParadigmContext`

```matha
class ParadigmContext
```

**描述**: 范式执行上下文。

**装饰器**: dataclass

Source: `src\multi_paradigm.py:44`

---

## `FunctionalEngine`

```matha
class FunctionalEngine
```

**描述**: 纯函数式计算引擎：lambda、柯里化、高阶函数、不可变状态。

Source: `src\multi_paradigm.py:57`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\multi_paradigm.py:60`

---

### `eval`

```matha
def eval(self, expr: Any, env: Dict) -> Any
```

**描述**: 求值表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |
| `env` | `Dict` | |

**返回**: `Any`

Source: `src\multi_paradigm.py:64`

---

### `curry`

```matha
def curry(self, func: Callable, n: int) -> Callable
```

**描述**: 柯里化：将多参函数转为嵌套单参函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `n` | `int` | |

**返回**: `Callable`

Source: `src\multi_paradigm.py:146`

---

### `compose`

```matha
def compose(self, *funcs) -> Callable
```

**描述**: 函数组合：(f ∘ g ∘ h)(x) = f(g(h(x)))

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Callable`

Source: `src\multi_paradigm.py:154`

---

### `fold`

```matha
def fold(self, func: Callable, init: Any, lst: list) -> Any
```

**描述**: 归约：foldl(func, init, [a,b,c]) = func(func(func(init, a), b), c)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `init` | `Any` | |
| `lst` | `list` | |

**返回**: `Any`

Source: `src\multi_paradigm.py:163`

---

### `map`

```matha
def map(self, func: Callable, lst: list) -> list
```

**描述**: 映射：map(func, [a,b,c]) = [func(a), func(b), func(c)]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `lst` | `list` | |

**返回**: `list`

Source: `src\multi_paradigm.py:170`

---

### `filter`

```matha
def filter(self, func: Callable, lst: list) -> list
```

**描述**: 过滤：filter(func, [a,b,c]) = [x for x in lst if func(x)]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |
| `lst` | `list` | |

**返回**: `list`

Source: `src\multi_paradigm.py:174`

---

## `ImperativeEngine`

```matha
class ImperativeEngine
```

**描述**: 命令式状态机：变量赋值、循环、分支、副作用。

Source: `src\multi_paradigm.py:183`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\multi_paradigm.py:186`

---

### `set`

```matha
def set(self, name: str, value: Any)
```

**描述**: 设置变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `value` | `Any` | |

Source: `src\multi_paradigm.py:190`

---

### `get`

```matha
def get(self, name: str) -> Any
```

**描述**: 获取变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\multi_paradigm.py:195`

---

### `eval_stmt`

```matha
def eval_stmt(self, stmt: dict) -> Any
```

**描述**: 执行单条语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `dict` | |

**返回**: `Any`

Source: `src\multi_paradigm.py:199`

---

### `_eval_expr`

```matha
def _eval_expr(self, expr: Any) -> Any
```

**描述**: 表达式求值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Any`

Source: `src\multi_paradigm.py:237`

---

### `get_state`

```matha
def get_state(self) -> Dict[str, Any]
```

**描述**: 获取当前状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\multi_paradigm.py:277`

---

## `LogicEngine`

```matha
class LogicEngine
```

**描述**: 逻辑推理引擎：规则匹配、约束求解、推导。

Source: `src\multi_paradigm.py:286`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\multi_paradigm.py:289`

---

### `add_rule`

```matha
def add_rule(self, name: str, body: Callable, conclusion: Callable)
```

**描述**: 添加推理规则。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `body` | `Callable` | |
| `conclusion` | `Callable` | |

Source: `src\multi_paradigm.py:294`

---

### `add_fact`

```matha
def add_fact(self, name: str, value: Any)
```

**描述**: 添加事实。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `value` | `Any` | |

Source: `src\multi_paradigm.py:299`

---

### `query`

```matha
def query(self, goal: str) -> Optional[Any]
```

**描述**: 查询目标，返回推导结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `goal` | `str` | |

**返回**: `Optional[Any]`

Source: `src\multi_paradigm.py:303`

---

### `unify`

```matha
def unify(self, pattern: dict, facts: dict) -> Optional[dict]
```

**描述**: 合一：将模式与事实匹配。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pattern` | `dict` | |
| `facts` | `dict` | |

**返回**: `Optional[dict]`

Source: `src\multi_paradigm.py:315`

---

## `DataflowEngine`

```matha
class DataflowEngine
```

**描述**: 数据流引擎：节点式计算图。

Source: `src\multi_paradigm.py:332`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\multi_paradigm.py:335`

---

### `add_node`

```matha
def add_node(self, name: str, func: Callable)
```

**描述**: 添加计算节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `Callable` | |

Source: `src\multi_paradigm.py:341`

---

### `add_edge`

```matha
def add_edge(self, from_node: str, to_node: str)
```

**描述**: 添加数据流边。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `from_node` | `str` | |
| `to_node` | `str` | |

Source: `src\multi_paradigm.py:346`

---

### `run`

```matha
def run(self, inputs: Dict[str, Any]) -> Dict[str, Any]
```

**描述**: 运行数据流图。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `inputs` | `Dict[str, Any]` | |

**返回**: `Dict[str, Any]`

Source: `src\multi_paradigm.py:350`

---

## `MultiParadigmEngine`

```matha
class MultiParadigmEngine
```

**描述**: 多范式融合引擎 — 根据任务自动选择并切换执行范式。

策略：
  1. 数值计算 → 函数式（纯函数，无副作用）
  2. 状态管理 → 命令式（变量/循环/分支）
  3. 符号推导 → 符号式（代数/微积分/简化）
  4. 逻辑推理 → 逻辑式（规则/约束/推导）
  5. 管道处理 → 数据流式（节点/边/拓扑）
  6. 混合任务 → 自动组合各范式结果

Source: `src\multi_paradigm.py:397`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\multi_paradigm.py:410`

---

### `compute`

```matha
def compute(self, task: dict) -> dict
```

**描述**: 智能任务分发：根据任务类型自动选择范式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:418`

---

### `_run_functional`

```matha
def _run_functional(self, task: dict) -> dict
```

**描述**: 函数式执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:442`

---

### `_run_imperative`

```matha
def _run_imperative(self, task: dict) -> dict
```

**描述**: 命令式执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:450`

---

### `_run_symbolic`

```matha
def _run_symbolic(self, task: dict) -> dict
```

**描述**: 符号式执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:459`

---

### `_run_logic`

```matha
def _run_logic(self, task: dict) -> dict
```

**描述**: 逻辑式执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:486`

---

### `_run_dataflow`

```matha
def _run_dataflow(self, task: dict) -> dict
```

**描述**: 数据流执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:499`

---

### `_run_mixed`

```matha
def _run_mixed(self, task: dict) -> dict
```

**描述**: 混合执行：组合多种范式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:511`

---

### `switch_paradigm`

```matha
def switch_paradigm(self, paradigm: Paradigm)
```

**描述**: 切换到指定范式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `paradigm` | `Paradigm` | |

Source: `src\multi_paradigm.py:538`

---

### `get_paradigm_history`

```matha
def get_paradigm_history(self) -> List[str]
```

**描述**: 获取范式切换历史。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

Source: `src\multi_paradigm.py:543`

---

## `get_paradigm_engine`

```matha
def get_paradigm_engine() -> MultiParadigmEngine
```

**返回**: `MultiParadigmEngine`

Source: `src\multi_paradigm.py:555`

---

## `paradigm_compute`

```matha
def paradigm_compute(task: dict) -> dict
```

**描述**: 便捷入口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `task` | `dict` | |

**返回**: `dict`

Source: `src\multi_paradigm.py:562`

---

# `net_security`

```matha

```

**描述**: Matha 网络安全引擎 — 威胁检测 · 漏洞扫描 · 病毒清杀 · 防火墙管理

设计原则：
  1. 用数学和逻辑方法建模网络安全问题
  2. 支持威胁签名匹配和风险评分
  3. 可检测、隔离、清杀网络威胁
  4. 生成防御规则并与防火墙联动
  5. 所有操作可审计、可追溯

架构：
  ThreatSignature   — 威胁签名定义（模式 + 风险等级）
  NetworkThreat     — 网络威胁对象（IP/端口/行为/风险分）
  VirusSimulator    — 病毒行为模拟器（用于安全测试）
  NetSecurityEngine — 网络安全引擎主类
  内建函数注册：病毒检测、漏洞扫描、威胁清杀、防火墙联动

用法：
    from src.net_security import NetSecurityEngine

    engine = NetSecurityEngine()

    # 检测威胁
    threats = engine.scan_threats(["192.168.1.100", "10.0.0.50"])

    # 清杀病毒
    result = engine.quarantine("THREAT_001")

    # 生成防火墙规则
    rules = engine.generate_firewall_rules(threats)

Source: `src\net_security.py:1`

---

## `ThreatLevel`

```matha
class ThreatLevel(str, Enum)
```

**描述**: 威胁等级。

Source: `src\net_security.py:50`

---

## `ThreatSignature`

```matha
class ThreatSignature
```

**描述**: 威胁签名 — 匹配模式 + 风险等级 + 描述。

**装饰器**: dataclass

Source: `src\net_security.py:59`

---

## `NetworkThreat`

```matha
class NetworkThreat
```

**描述**: 网络威胁实例。

**装饰器**: dataclass

Source: `src\net_security.py:193`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\net_security.py:209`

---

## `NetSecurityEngine`

```matha
class NetSecurityEngine
```

**描述**: 网络安全引擎 — 威胁检测 · 漏洞扫描 · 病毒清杀 · 防火墙联动。

功能：
  - scan_threats()        扫描网络威胁
  - detect_virus()        检测已知病毒特征
  - quarantine()          隔离威胁源
  - eliminate()           清杀威胁
  - generate_fw_rules()   生成防火墙规则
  - assess_risk()         风险评估
  - get_threat_report()   生成威胁报告

Source: `src\net_security.py:228`

---

### `__init__`

```matha
def __init__(self, signatures: Optional[list[ThreatSignature]])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `signatures` | `Optional[list[ThreatSignature]]` | |

Source: `src\net_security.py:242`

---

### `scan_threats`

```matha
def scan_threats(self, source_ips: list[str], dest_ip: str, dest_port: int) -> list[dict]
```

**描述**: 扫描指定IP的威胁，返回威胁列表。

source_ips: 待扫描的源IP列表
dest_ip: 目标IP
dest_port: 目标端口

返回: 威胁字典列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source_ips` | `list[str]` | |
| `dest_ip` | `str` | |
| `dest_port` | `int` | |

**返回**: `list[dict]`

Source: `src\net_security.py:253`

---

### `detect_virus`

```matha
def detect_virus(self, behavior_pattern: str) -> list[dict]
```

**描述**: 根据行为模式检测潜在病毒。

behavior_pattern: 可疑行为描述（如 "文件加密"、"反弹shell"）

返回: 匹配的威胁列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `behavior_pattern` | `str` | |

**返回**: `list[dict]`

Source: `src\net_security.py:309`

---

### `quarantine`

```matha
def quarantine(self, threat_id: str) -> dict
```

**描述**: 隔离指定威胁。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:334`

---

### `eliminate`

```matha
def eliminate(self, threat_id: str) -> dict
```

**描述**: 清杀指定威胁。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:351`

---

### `eliminate_all`

```matha
def eliminate_all(self, level: Optional[str]) -> dict
```

**描述**: 批量清杀威胁。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `Optional[str]` | |

**返回**: `dict`

Source: `src\net_security.py:369`

---

### `generate_firewall_rules`

```matha
def generate_firewall_rules(self, threats: Optional[list[dict]]) -> list[dict]
```

**描述**: 根据威胁列表生成防火墙规则。

返回: 规则列表，每条包含动作、源IP、目标端口、描述

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threats` | `Optional[list[dict]]` | |

**返回**: `list[dict]`

Source: `src\net_security.py:385`

---

### `get_firewall_rules`

```matha
def get_firewall_rules(self) -> list[dict]
```

**描述**: 获取已生成的防火墙规则列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\net_security.py:416`

---

### `assess_risk`

```matha
def assess_risk(self, ip: str) -> dict
```

**描述**: 评估指定IP的威胁风险。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ip` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:422`

---

### `get_risk_report`

```matha
def get_risk_report(self) -> dict
```

**描述**: 生成整体风险报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\net_security.py:450`

---

### `get_threat_report`

```matha
def get_threat_report(self, threat_id: str) -> dict
```

**描述**: 获取单个威胁的详细信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:478`

---

### `add_signature`

```matha
def add_signature(self, name: str, pattern: str, level: str, category: str, description: str, mitigation: str) -> bool
```

**描述**: 添加自定义威胁签名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `pattern` | `str` | |
| `level` | `str` | |
| `category` | `str` | |
| `description` | `str` | |
| `mitigation` | `str` | |

**返回**: `bool`

Source: `src\net_security.py:499`

---

### `remove_signature`

```matha
def remove_signature(self, name: str) -> bool
```

**描述**: 移除威胁签名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\net_security.py:515`

---

### `list_signatures`

```matha
def list_signatures(self) -> list[dict]
```

**描述**: 列出所有威胁签名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\net_security.py:521`

---

### `threat_count`

```matha
def threat_count(self) -> int
```

**描述**: 返回当前威胁总数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\net_security.py:527`

---

### `eliminated_count`

```matha
def eliminated_count(self) -> int
```

**描述**: 返回已清杀威胁数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\net_security.py:531`

---

### `threat_status`

```matha
def threat_status(self, threat_id: str) -> dict
```

**描述**: 返回威胁当前状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:535`

---

### `_create_threat`

```matha
def _create_threat(self, source_ip: str, dest_ip: str, dest_port: int, name: str, level: ThreatLevel, category: str, description: str, matched_pattern: str, mitigation: str, risk_score: float) -> NetworkThreat
```

**描述**: 创建威胁实例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source_ip` | `str` | |
| `dest_ip` | `str` | |
| `dest_port` | `int` | |
| `name` | `str` | |
| `level` | `ThreatLevel` | |
| `category` | `str` | |
| `description` | `str` | |
| `matched_pattern` | `str` | |
| `mitigation` | `str` | |
| `risk_score` | `float` | |

**返回**: `NetworkThreat`

Source: `src\net_security.py:551`

---

### `_calc_risk`

```matha
def _calc_risk(self, level: ThreatLevel) -> float
```

**描述**: 根据威胁等级计算风险分。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `level` | `ThreatLevel` | |

**返回**: `float`

Source: `src\net_security.py:575`

---

### `_is_suspicious_port`

```matha
def _is_suspicious_port(self, port: int) -> bool
```

**描述**: 检查端口是否为恶意端口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port` | `int` | |

**返回**: `bool`

Source: `src\net_security.py:579`

---

### `_check_ip_reputation`

```matha
def _check_ip_reputation(self, ip: str) -> dict
```

**描述**: 检查IP信誉（模拟）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ip` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:583`

---

### `_is_valid_ip`

```matha
def _is_valid_ip(self, ip: str) -> bool
```

**描述**: 检查是否为有效IPv4地址。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ip` | `str` | |

**返回**: `bool`

Source: `src\net_security.py:598`

---

### `_random_source_port`

```matha
def _random_source_port() -> int
```

**描述**: 生成随机源端口（模拟）。

**返回**: `int`

**装饰器**: staticmethod

Source: `src\net_security.py:609`

---

### `create_virus`

```matha
def create_virus(self, name: str, category: str, level: str, behavior: str, payload: str, source_ip: str, target_ip: str) -> dict
```

**描述**: 创建测试病毒（模拟，用于安全测试）。

参数:
    name: 病毒名称（如 "TestWorm_001"）
    category: 分类（worm/trojan/ransomware/spyware）
    level: 风险等级（low/medium/high/critical）
    behavior: 行为描述（如 "port scan + self replicate"）
    payload: 载荷描述（如 "encrypt files, demand bitcoin"）
    source_ip: 来源IP
    target_ip: 目标IP

返回: 病毒定义字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `category` | `str` | |
| `level` | `str` | |
| `behavior` | `str` | |
| `payload` | `str` | |
| `source_ip` | `str` | |
| `target_ip` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:616`

---

### `create_virus_batch`

```matha
def create_virus_batch(self, count: int, categories: Optional[list]) -> list
```

**描述**: 批量创建测试病毒。

参数:
    count: 创建数量
    categories: 指定分类列表，None 则随机

返回: 创建的病毒列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `count` | `int` | |
| `categories` | `Optional[list]` | |

**返回**: `list`

Source: `src\net_security.py:683`

---

### `analyze_virus`

```matha
def analyze_virus(self, virus_id: str) -> dict
```

**描述**: 分析病毒特征。

返回: 病毒分析报告

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `virus_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:729`

---

### `_analyze_behavior`

```matha
def _analyze_behavior(self, behavior: str) -> dict
```

**描述**: 分析行为特征。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `behavior` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:766`

---

### `patch_vulnerability`

```matha
def patch_vulnerability(self, vuln_id: str, severity: str) -> dict
```

**描述**: 修补漏洞（模拟）。

参数:
    vuln_id: 漏洞ID
    severity: 漏洞严重程度

返回: 修补结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `vuln_id` | `str` | |
| `severity` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:798`

---

### `simulate_spread`

```matha
def simulate_spread(self, virus_id: str, network_size: int) -> dict
```

**描述**: 模拟病毒传播。

参数:
    virus_id: 病毒ID
    network_size: 网络节点数量

返回: 传播模拟结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `virus_id` | `str` | |
| `network_size` | `int` | |

**返回**: `dict`

Source: `src\net_security.py:822`

---

### `neutralize`

```matha
def neutralize(self, threat_id: str, method: str) -> dict
```

**描述**: 高级中和病毒（比清杀更彻底）。

参数:
    threat_id: 威胁ID
    method: 清杀方法（automatic/manual/quarantine_first）

返回: 中和结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threat_id` | `str` | |
| `method` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:853`

---

### `quarantine_all`

```matha
def quarantine_all(self) -> dict
```

**描述**: 隔离所有活跃威胁。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\net_security.py:943`

---

### `get_virus_library`

```matha
def get_virus_library(self) -> list[dict]
```

**描述**: 获取病毒库（所有已创建/已清杀的测试病毒）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\net_security.py:954`

---

### `create_test_scenario`

```matha
def create_test_scenario(self, name: str, virus_count: int, network_size: int) -> dict
```

**描述**: 创建完整测试场景。

参数:
    name: 场景名称
    virus_count: 创建病毒数量
    network_size: 网络节点数

返回: 场景摘要

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `virus_count` | `int` | |
| `network_size` | `int` | |

**返回**: `dict`

Source: `src\net_security.py:971`

---

### `get_quarantine_log`

```matha
def get_quarantine_log(self) -> list[dict]
```

**描述**: 获取隔离操作日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\net_security.py:1021`

---

### `get_elimination_log`

```matha
def get_elimination_log(self) -> list[dict]
```

**描述**: 获取清杀操作日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[dict]`

Source: `src\net_security.py:1025`

---

## `_get_engine`

```matha
def _get_engine() -> NetSecurityEngine
```

**返回**: `NetSecurityEngine`

Source: `src\net_security.py:1037`

---

## `builtin_威胁检测`

```matha
def builtin_威胁检测(source_ips: list) -> list
```

**描述**: 威胁检测(source_ips) → 威胁列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source_ips` | `list` | |

**返回**: `list`

Source: `src\net_security.py:1044`

---

## `builtin_检测病毒`

```matha
def builtin_检测病毒(behavior: str) -> list
```

**描述**: 检测病毒(行为模式) → 匹配签名列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `behavior` | `str` | |

**返回**: `list`

Source: `src\net_security.py:1050`

---

## `builtin_隔离威胁`

```matha
def builtin_隔离威胁(threat_id: str) -> dict
```

**描述**: 隔离威胁(威胁ID) → 操作结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:1056`

---

## `builtin_清杀威胁`

```matha
def builtin_清杀威胁(threat_id: str) -> dict
```

**描述**: 清杀威胁(威胁ID) → 操作结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:1062`

---

## `builtin_清杀全部`

```matha
def builtin_清杀全部(level: str) -> dict
```

**描述**: 清杀全部威胁(级别) → 批量操作结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `level` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:1068`

---

## `builtin_生成防火墙规则`

```matha
def builtin_生成防火墙规则(threats: list) -> list
```

**描述**: 生成防火墙规则(威胁列表) → 规则列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `threats` | `list` | |

**返回**: `list`

Source: `src\net_security.py:1074`

---

## `builtin_风险评估`

```matha
def builtin_风险评估(ip: str) -> dict
```

**描述**: 风险评估(IP地址) → 风险评估结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `ip` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:1080`

---

## `builtin_风险报告`

```matha
def builtin_风险报告() -> dict
```

**描述**: 风险报告() → 整体风险报告。

**返回**: `dict`

Source: `src\net_security.py:1086`

---

## `builtin_威胁报告`

```matha
def builtin_威胁报告(threat_id: str) -> dict
```

**描述**: 威胁报告(威胁ID) → 威胁详情。

| 参数 | 类型 | 说明 |
|------|------|------|
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:1092`

---

## `builtin_添加签名`

```matha
def builtin_添加签名(name: str, pattern: str, level: str, category: str, description: str) -> bool
```

**描述**: 添加签名(名称, 模式, 等级, 分类, 描述) → 是否成功。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `pattern` | `str` | |
| `level` | `str` | |
| `category` | `str` | |
| `description` | `str` | |

**返回**: `bool`

Source: `src\net_security.py:1099`

---

## `builtin_列出签名`

```matha
def builtin_列出签名() -> list
```

**描述**: 列出签名() → 所有威胁签名。

**返回**: `list`

Source: `src\net_security.py:1106`

---

## `builtin_威胁数量`

```matha
def builtin_威胁数量() -> int
```

**描述**: 威胁数量() → 当前威胁总数。

**返回**: `int`

Source: `src\net_security.py:1112`

---

## `builtin_已清杀数量`

```matha
def builtin_已清杀数量() -> int
```

**描述**: 已清杀数量() → 已清杀威胁数。

**返回**: `int`

Source: `src\net_security.py:1118`

---

## `builtin_防火墙规则列表`

```matha
def builtin_防火墙规则列表() -> list
```

**描述**: 防火墙规则列表() → 已生成规则。

**返回**: `list`

Source: `src\net_security.py:1124`

---

## `builtin_威胁状态`

```matha
def builtin_威胁状态(threat_id: str) -> dict
```

**描述**: 威胁状态(威胁ID) → 威胁当前状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `threat_id` | `str` | |

**返回**: `dict`

Source: `src\net_security.py:1130`

---

# `numpy_compat`

```matha

```

**描述**: Matha NumPy 兼容层 - 纯 Python 实现

本模块提供 NumPy 核心功能的纯 Python 实现，无需外部依赖。
支持移动端和平板设备的数学计算需求。

特性：
- 纯 Python 实现，零外部依赖
- 移动端友好的内存管理
- 与 NumPy API 兼容

Source: `src\numpy_compat.py:1`

---

## `ndarray`

```matha
class ndarray
```

**描述**: 多维数组类（NumPy ndarray 兼容实现）

支持：
- 一维到三维数组
- 基本算术运算
- 矩阵运算
- 广播机制

Source: `src\numpy_compat.py:25`

---

### `__init__`

```matha
def __init__(self, data: Union[List, Tuple, 'ndarray'], dtype: type)
```

**描述**: 初始化 ndarray

Args:
    data: 数组数据（嵌套列表或元组）
    dtype: 数据类型（float, int, complex）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `Union[List, Tuple, 'ndarray']` | |
| `dtype` | `type` | |

Source: `src\numpy_compat.py:36`

---

### `_convert_to_array`

```matha
def _convert_to_array(self, data: Union[List, Tuple, 'ndarray']) -> List
```

**描述**: 将输入数据转换为嵌套列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `Union[List, Tuple, 'ndarray']` | |

**返回**: `List`

Source: `src\numpy_compat.py:51`

---

### `_compute_shape`

```matha
def _compute_shape(self, data: List) -> Tuple[int, ...]
```

**描述**: 计算数组形状

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `List` | |

**返回**: `Tuple[int, ...]`

Source: `src\numpy_compat.py:65`

---

### `_cast`

```matha
def _cast(self, value) -> Union[int, float, complex]
```

**描述**: 类型转换

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `` | |

**返回**: `Union[int, float, complex]`

Source: `src\numpy_compat.py:73`

---

### `shape`

```matha
def shape(self) -> Tuple[int, ...]
```

**描述**: 数组形状

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Tuple[int, ...]`

**装饰器**: property

Source: `src\numpy_compat.py:82`

---

### `size`

```matha
def size(self) -> int
```

**描述**: 元素总数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\numpy_compat.py:87`

---

### `ndim`

```matha
def ndim(self) -> int
```

**描述**: 维度数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\numpy_compat.py:94`

---

### `reshape`

```matha
def reshape(self, *new_shape) -> 'ndarray'
```

**描述**: 重塑数组形状

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:98`

---

### `flatten`

```matha
def flatten(self) -> List
```

**描述**: 展平为一维数组

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List`

Source: `src\numpy_compat.py:107`

---

### `_flatten_list`

```matha
def _flatten_list(self, data: List) -> List
```

**描述**: 递归展平列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `List` | |

**返回**: `List`

Source: `src\numpy_compat.py:121`

---

### `__getitem__`

```matha
def __getitem__(self, index)
```

**描述**: 索引访问

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `index` | `` | |

Source: `src\numpy_compat.py:131`

---

### `_nested_get`

```matha
def _nested_get(self, data: List, indices: Tuple) -> any
```

**描述**: 递归获取嵌套列表元素

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `List` | |
| `indices` | `Tuple` | |

**返回**: `any`

Source: `src\numpy_compat.py:141`

---

### `__setitem__`

```matha
def __setitem__(self, index, value)
```

**描述**: 索引赋值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `index` | `` | |
| `value` | `` | |

Source: `src\numpy_compat.py:147`

---

### `_nested_set`

```matha
def _nested_set(self, data: List, indices: Tuple, value)
```

**描述**: 递归设置嵌套列表元素

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `List` | |
| `indices` | `Tuple` | |
| `value` | `` | |

Source: `src\numpy_compat.py:157`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

**描述**: 字符串表示

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\numpy_compat.py:164`

---

### `__str__`

```matha
def __str__(self) -> str
```

**描述**: 打印表示

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\numpy_compat.py:170`

---

### `_format_array`

```matha
def _format_array(self, data: List, depth: int) -> str
```

**描述**: 格式化数组输出

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `List` | |
| `depth` | `int` | |

**返回**: `str`

Source: `src\numpy_compat.py:174`

---

### `__add__`

```matha
def __add__(self, other: Union['ndarray', int, float]) -> 'ndarray'
```

**描述**: 加法

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `Union['ndarray', int, float]` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:195`

---

### `__sub__`

```matha
def __sub__(self, other: Union['ndarray', int, float]) -> 'ndarray'
```

**描述**: 减法

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `Union['ndarray', int, float]` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:201`

---

### `__mul__`

```matha
def __mul__(self, other: Union['ndarray', int, float]) -> 'ndarray'
```

**描述**: 乘法

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `Union['ndarray', int, float]` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:207`

---

### `__truediv__`

```matha
def __truediv__(self, other: Union['ndarray', int, float]) -> 'ndarray'
```

**描述**: 除法

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `Union['ndarray', int, float]` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:213`

---

### `_broadcast_op`

```matha
def _broadcast_op(self, data, scalar, op)
```

**描述**: 广播操作

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `` | |
| `scalar` | `` | |
| `op` | `` | |

Source: `src\numpy_compat.py:219`

---

### `_element_wise_op`

```matha
def _element_wise_op(self, data1, data2, op)
```

**描述**: 逐元素操作

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data1` | `` | |
| `data2` | `` | |
| `op` | `` | |

Source: `src\numpy_compat.py:225`

---

### `abs`

```matha
def abs(self) -> 'ndarray'
```

**描述**: 绝对值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:235`

---

### `sqrt`

```matha
def sqrt(self) -> 'ndarray'
```

**描述**: 平方根（安全处理负数）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:239`

---

### `sin`

```matha
def sin(self) -> 'ndarray'
```

**描述**: 正弦

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:247`

---

### `cos`

```matha
def cos(self) -> 'ndarray'
```

**描述**: 余弦

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:251`

---

### `tan`

```matha
def tan(self) -> 'ndarray'
```

**描述**: 正切

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:255`

---

### `exp`

```matha
def exp(self) -> 'ndarray'
```

**描述**: 指数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:259`

---

### `log`

```matha
def log(self) -> 'ndarray'
```

**描述**: 自然对数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:263`

---

### `dot`

```matha
def dot(self, other: 'ndarray') -> 'ndarray'
```

**描述**: 矩阵点积

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'ndarray'` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:267`

---

### `transpose`

```matha
def transpose(self) -> 'ndarray'
```

**描述**: 转置

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'ndarray'`

Source: `src\numpy_compat.py:282`

---

### `sum`

```matha
def sum(self, axis: Optional[int]) -> Union['ndarray', float]
```

**描述**: 求和

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `axis` | `Optional[int]` | |

**返回**: `Union['ndarray', float]`

Source: `src\numpy_compat.py:289`

---

### `_sum_all`

```matha
def _sum_all(self, data) -> float
```

**描述**: 递归求和

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `` | |

**返回**: `float`

Source: `src\numpy_compat.py:299`

---

### `mean`

```matha
def mean(self) -> float
```

**描述**: 均值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\numpy_compat.py:305`

---

### `std`

```matha
def std(self) -> float
```

**描述**: 标准差

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\numpy_compat.py:309`

---

## `array`

```matha
def array(data: Union[List, Tuple], dtype: type) -> ndarray
```

**描述**: 创建数组

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `Union[List, Tuple]` | |
| `dtype` | `type` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:320`

---

## `zeros`

```matha
def zeros(shape: Tuple[int, ...], dtype: type) -> ndarray
```

**描述**: 创建零数组

| 参数 | 类型 | 说明 |
|------|------|------|
| `shape` | `Tuple[int, ...]` | |
| `dtype` | `type` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:325`

---

## `ones`

```matha
def ones(shape: Tuple[int, ...], dtype: type) -> ndarray
```

**描述**: 创建全一数组

| 参数 | 类型 | 说明 |
|------|------|------|
| `shape` | `Tuple[int, ...]` | |
| `dtype` | `type` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:336`

---

## `eye`

```matha
def eye(n: int, dtype: type) -> ndarray
```

**描述**: 单位矩阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `dtype` | `type` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:347`

---

## `arange`

```matha
def arange(start: float, stop: Optional[float], step: float) -> ndarray
```

**描述**: 生成等差数列

| 参数 | 类型 | 说明 |
|------|------|------|
| `start` | `float` | |
| `stop` | `Optional[float]` | |
| `step` | `float` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:355`

---

## `linspace`

```matha
def linspace(start: float, stop: float, num: int) -> ndarray
```

**描述**: 生成等间隔数列

| 参数 | 类型 | 说明 |
|------|------|------|
| `start` | `float` | |
| `stop` | `float` | |
| `num` | `int` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:368`

---

## `random`

```matha
def random(size: Union[int, Tuple[int, ...]]) -> ndarray
```

**描述**: 随机数组

| 参数 | 类型 | 说明 |
|------|------|------|
| `size` | `Union[int, Tuple[int, ...]]` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:377`

---

## `matrix_multiply`

```matha
def matrix_multiply(A: ndarray, B: ndarray) -> ndarray
```

**描述**: 矩阵乘法

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |
| `B` | `ndarray` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:391`

---

## `matrix_transpose`

```matha
def matrix_transpose(A: ndarray) -> ndarray
```

**描述**: 矩阵转置

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |

**返回**: `ndarray`

Source: `src\numpy_compat.py:396`

---

## `matrix_inverse`

```matha
def matrix_inverse(A: ndarray) -> Optional[ndarray]
```

**描述**: 矩阵求逆（高斯-约当消元法）

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |

**返回**: `Optional[ndarray]`

Source: `src\numpy_compat.py:401`

---

## `matrix_determinant`

```matha
def matrix_determinant(A: ndarray) -> float
```

**描述**: 矩阵行列式（高斯消元法）

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |

**返回**: `float`

Source: `src\numpy_compat.py:442`

---

## `svd_decompose`

```matha
def svd_decompose(A: ndarray) -> Tuple[ndarray, ndarray, ndarray]
```

**描述**: SVD 分解：A = U Σ V^T

使用纯 Python 实现（QR 迭代法）

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |

**返回**: `Tuple[ndarray, ndarray, ndarray]`

Source: `src\numpy_compat.py:475`

---

## `_svd_from_eigenvalues`

```matha
def _svd_from_eigenvalues(A: ndarray) -> Tuple[ndarray, ndarray, ndarray]
```

**描述**: 从特征值分解计算 SVD（对称矩阵）

对于对称矩阵 A，SVD 可以通过特征值分解得到：
- 奇异值 σ_i = |λ_i|（特征值的绝对值）
- U 和 V 由特征向量矩阵决定

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |

**返回**: `Tuple[ndarray, ndarray, ndarray]`

Source: `src\numpy_compat.py:494`

---

## `_svd_general`

```matha
def _svd_general(A: ndarray) -> Tuple[ndarray, ndarray, ndarray]
```

**描述**: 通用矩阵的 SVD 分解（幂迭代法）

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |

**返回**: `Tuple[ndarray, ndarray, ndarray]`

Source: `src\numpy_compat.py:537`

---

## `_eig_power_iteration`

```matha
def _eig_power_iteration(A: ndarray, max_iter: int, tol: float) -> list
```

**描述**: 使用幂迭代法计算特征值和特征向量

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |
| `max_iter` | `int` | |
| `tol` | `float` | |

**返回**: `list`

Source: `src\numpy_compat.py:598`

---

## `_mat_vec_mul`

```matha
def _mat_vec_mul(A: ndarray, v: list) -> list
```

**描述**: 矩阵向量乘法

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |
| `v` | `list` | |

**返回**: `list`

Source: `src\numpy_compat.py:645`

---

## `_dot_product`

```matha
def _dot_product(v1: list, v2: list) -> float
```

**描述**: 向量点积

| 参数 | 类型 | 说明 |
|------|------|------|
| `v1` | `list` | |
| `v2` | `list` | |

**返回**: `float`

Source: `src\numpy_compat.py:658`

---

## `_scale_vec`

```matha
def _scale_vec(v: list, scalar: float) -> list
```

**描述**: 向量缩放

| 参数 | 类型 | 说明 |
|------|------|------|
| `v` | `list` | |
| `scalar` | `float` | |

**返回**: `list`

Source: `src\numpy_compat.py:663`

---

## `_vec_sub`

```matha
def _vec_sub(v1: list, v2: list) -> list
```

**描述**: 向量减法

| 参数 | 类型 | 说明 |
|------|------|------|
| `v1` | `list` | |
| `v2` | `list` | |

**返回**: `list`

Source: `src\numpy_compat.py:668`

---

## `trace`

```matha
def trace(A: ndarray) -> float
```

**描述**: 矩阵迹

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |

**返回**: `float`

Source: `src\numpy_compat.py:673`

---

## `norm`

```matha
def norm(A: ndarray, ord: Optional[str]) -> float
```

**描述**: 矩阵范数

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `ndarray` | |
| `ord` | `Optional[str]` | |

**返回**: `float`

Source: `src\numpy_compat.py:680`

---

# `offline.mobile_api_client`

```matha

```

**描述**: Matha 移动端远程 API 客户端

提供与后端的 HTTP 通信功能：
  - 用户认证
  - 数据同步
  - 冲突检测
  - 文件上传/下载

Source: `src\offline\mobile_api_client.py:1`

---

## `HTTPMethod`

```matha
class HTTPMethod(Enum)
```

**描述**: HTTP 方法

Source: `src\offline\mobile_api_client.py:24`

---

## `SyncStatus`

```matha
class SyncStatus(Enum)
```

**描述**: 同步状态

Source: `src\offline\mobile_api_client.py:33`

---

## `APIResponse`

```matha
class APIResponse
```

**描述**: API 响应

**装饰器**: dataclass

Source: `src\offline\mobile_api_client.py:44`

---

### `success`

```matha
def success(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\offline\mobile_api_client.py:52`

---

### `is_conflict`

```matha
def is_conflict(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

**装饰器**: property

Source: `src\offline\mobile_api_client.py:56`

---

## `SyncRecord`

```matha
class SyncRecord
```

**描述**: 同步记录

**装饰器**: dataclass

Source: `src\offline\mobile_api_client.py:61`

---

## `MathaAPIClient`

```matha
class MathaAPIClient
```

**描述**: Matha 远程 API 客户端

提供与后端的 HTTP 通信：
- 用户认证（JWT Token）
- 数据同步（push/pull）
- 冲突检测
- 文件上传/下载

Source: `src\offline\mobile_api_client.py:75`

---

### `__init__`

```matha
def __init__(self, base_url: str, api_key: Optional[str], timeout: float, max_retries: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `base_url` | `str` | |
| `api_key` | `Optional[str]` | |
| `timeout` | `float` | |
| `max_retries` | `int` | |

Source: `src\offline\mobile_api_client.py:86`

---

### `is_authenticated`

```matha
def is_authenticated(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\offline\mobile_api_client.py:144`

---

### `_build_headers`

```matha
def _build_headers(self) -> Dict[str, str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, str]`

Source: `src\offline\mobile_api_client.py:304`

---

### `on_conflict`

```matha
def on_conflict(self, callback: Callable[[SyncRecord], None]) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable[[SyncRecord], None]` | |

**返回**: `None`

Source: `src\offline\mobile_api_client.py:344`

---

### `on_sync`

```matha
def on_sync(self, callback: Callable[[str, str], None]) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable[[str, str], None]` | |

**返回**: `None`

Source: `src\offline\mobile_api_client.py:347`

---

### `_notify_conflict`

```matha
def _notify_conflict(self, record_type: str, record_id: str, local_data: Dict, remote_data: Dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `record_type` | `str` | |
| `record_id` | `str` | |
| `local_data` | `Dict` | |
| `remote_data` | `Dict` | |

**返回**: `None`

Source: `src\offline\mobile_api_client.py:350`

---

### `_notify_sync`

```matha
def _notify_sync(self, action: str, record_id: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `action` | `str` | |
| `record_id` | `str` | |

**返回**: `None`

Source: `src\offline\mobile_api_client.py:366`

---

### `get_base_url`

```matha
def get_base_url(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\offline\mobile_api_client.py:373`

---

### `set_base_url`

```matha
def set_base_url(self, url: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `url` | `str` | |

**返回**: `None`

Source: `src\offline\mobile_api_client.py:376`

---

### `get_token`

```matha
def get_token(self) -> Optional[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[str]`

Source: `src\offline\mobile_api_client.py:379`

---

### `clear_token`

```matha
def clear_token(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\mobile_api_client.py:382`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\offline\mobile_api_client.py:387`

---

## `get_api_client`

```matha
def get_api_client() -> MathaAPIClient
```

**返回**: `MathaAPIClient`

Source: `src\offline\mobile_api_client.py:394`

---

## `set_api_client`

```matha
def set_api_client(client: MathaAPIClient) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `client` | `MathaAPIClient` | |

**返回**: `None`

Source: `src\offline\mobile_api_client.py:401`

---

# `offline.sqlite_storage`

```matha

```

**描述**: Matha SQLite 本地存储模块

提供 SQLite 本地数据存储功能：
  - 历史记录存储
  - 偏好设置存储
  - 计算结果缓存
  - 离线补全词库

Source: `src\offline\sqlite_storage.py:1`

---

## `HistoryRecord`

```matha
class HistoryRecord
```

**描述**: 历史记录

**装饰器**: dataclass

Source: `src\offline\sqlite_storage.py:25`

---

## `PreferenceRecord`

```matha
class PreferenceRecord
```

**描述**: 偏好设置

**装饰器**: dataclass

Source: `src\offline\sqlite_storage.py:35`

---

## `SQLiteStorage`

```matha
class SQLiteStorage
```

**描述**: SQLite 本地存储

提供持久化数据存储：
- REPL 历史记录
- 用户偏好设置
- 计算结果缓存
- 离线补全词库

Source: `src\offline\sqlite_storage.py:42`

---

### `__init__`

```matha
def __init__(self, db_path: Optional[str])
```

**描述**: 初始化存储

Args:
    db_path: 数据库路径，默认为 ~/.matha/data.db

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `db_path` | `Optional[str]` | |

Source: `src\offline\sqlite_storage.py:53`

---

### `_get_connection`

```matha
def _get_connection(self) -> sqlite3.Connection
```

**描述**: 获取数据库连接

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `sqlite3.Connection`

Source: `src\offline\sqlite_storage.py:67`

---

### `_init_database`

```matha
def _init_database(self) -> None
```

**描述**: 初始化数据库表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\sqlite_storage.py:77`

---

### `save_history`

```matha
def save_history(self, code: str, output: str, is_success: bool, mode: str) -> int
```

**描述**: 保存历史记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `output` | `str` | |
| `is_success` | `bool` | |
| `mode` | `str` | |

**返回**: `int`

Source: `src\offline\sqlite_storage.py:155`

---

### `get_history`

```matha
def get_history(self, limit: int, offset: int) -> List[HistoryRecord]
```

**描述**: 获取历史记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |
| `offset` | `int` | |

**返回**: `List[HistoryRecord]`

Source: `src\offline\sqlite_storage.py:167`

---

### `get_history_count`

```matha
def get_history_count(self) -> int
```

**描述**: 获取历史记录总数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\offline\sqlite_storage.py:177`

---

### `clear_history`

```matha
def clear_history(self) -> int
```

**描述**: 清空历史记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\offline\sqlite_storage.py:184`

---

### `delete_history`

```matha
def delete_history(self, record_id: int) -> bool
```

**描述**: 删除单条历史

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `record_id` | `int` | |

**返回**: `bool`

Source: `src\offline\sqlite_storage.py:192`

---

### `save_preference`

```matha
def save_preference(self, key: str, value: str) -> None
```

**描述**: 保存偏好设置

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `value` | `str` | |

**返回**: `None`

Source: `src\offline\sqlite_storage.py:204`

---

### `get_preference`

```matha
def get_preference(self, key: str, default: str) -> str
```

**描述**: 获取偏好设置

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `default` | `str` | |

**返回**: `str`

Source: `src\offline\sqlite_storage.py:214`

---

### `get_all_preferences`

```matha
def get_all_preferences(self) -> Dict[str, str]
```

**描述**: 获取所有偏好设置

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, str]`

Source: `src\offline\sqlite_storage.py:222`

---

### `delete_preference`

```matha
def delete_preference(self, key: str) -> bool
```

**描述**: 删除偏好设置

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `bool`

Source: `src\offline\sqlite_storage.py:229`

---

### `cache_result`

```matha
def cache_result(self, code: str, output: str, duration_ms: float) -> bool
```

**描述**: 缓存计算结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `output` | `str` | |
| `duration_ms` | `float` | |

**返回**: `bool`

Source: `src\offline\sqlite_storage.py:241`

---

### `get_cached_result`

```matha
def get_cached_result(self, code: str) -> Optional[str]
```

**描述**: 获取缓存的计算结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |

**返回**: `Optional[str]`

Source: `src\offline\sqlite_storage.py:257`

---

### `get_cache_stats`

```matha
def get_cache_stats(self) -> Dict[str, int]
```

**描述**: 获取缓存统计

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, int]`

Source: `src\offline\sqlite_storage.py:270`

---

### `clear_cache`

```matha
def clear_cache(self) -> int
```

**描述**: 清空缓存

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\offline\sqlite_storage.py:277`

---

### `add_word`

```matha
def add_word(self, word: str, category: str) -> None
```

**描述**: 添加补全词

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `word` | `str` | |
| `category` | `str` | |

**返回**: `None`

Source: `src\offline\sqlite_storage.py:289`

---

### `increment_word_usage`

```matha
def increment_word_usage(self, word: str) -> None
```

**描述**: 增加词汇使用次数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `word` | `str` | |

**返回**: `None`

Source: `src\offline\sqlite_storage.py:299`

---

### `get_words`

```matha
def get_words(self, category: Optional[str], limit: int) -> List[str]
```

**描述**: 获取补全词列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `category` | `Optional[str]` | |
| `limit` | `int` | |

**返回**: `List[str]`

Source: `src\offline\sqlite_storage.py:309`

---

### `add_batch_words`

```matha
def add_batch_words(self, words: List[str], category: str) -> int
```

**描述**: 批量添加补全词

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `words` | `List[str]` | |
| `category` | `str` | |

**返回**: `int`

Source: `src\offline\sqlite_storage.py:325`

---

### `enqueue_sync`

```matha
def enqueue_sync(self, action: str, record_type: str, record_id: str, data: Dict[str, Any], priority: int) -> int
```

**描述**: 加入同步队列

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `action` | `str` | |
| `record_type` | `str` | |
| `record_id` | `str` | |
| `data` | `Dict[str, Any]` | |
| `priority` | `int` | |

**返回**: `int`

Source: `src\offline\sqlite_storage.py:340`

---

### `get_pending_sync`

```matha
def get_pending_sync(self, limit: int) -> List[Dict[str, Any]]
```

**描述**: 获取待同步数据

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |

**返回**: `List[Dict[str, Any]]`

Source: `src\offline\sqlite_storage.py:353`

---

### `mark_synced`

```matha
def mark_synced(self, queue_id: int) -> bool
```

**描述**: 标记为已同步

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `queue_id` | `int` | |

**返回**: `bool`

Source: `src\offline\sqlite_storage.py:364`

---

### `mark_failed`

```matha
def mark_failed(self, queue_id: int, error: str) -> bool
```

**描述**: 标记为失败

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `queue_id` | `int` | |
| `error` | `str` | |

**返回**: `bool`

Source: `src\offline\sqlite_storage.py:376`

---

### `get_queue_stats`

```matha
def get_queue_stats(self) -> Dict[str, int]
```

**描述**: 获取队列统计

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, int]`

Source: `src\offline\sqlite_storage.py:388`

---

### `clear_sync_queue`

```matha
def clear_sync_queue(self) -> int
```

**描述**: 清空同步队列

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\offline\sqlite_storage.py:406`

---

### `get_db_size`

```matha
def get_db_size(self) -> float
```

**描述**: 获取数据库大小（MB）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\offline\sqlite_storage.py:418`

---

### `backup`

```matha
def backup(self, backup_path: str) -> bool
```

**描述**: 备份数据库

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `backup_path` | `str` | |

**返回**: `bool`

Source: `src\offline\sqlite_storage.py:427`

---

### `restore`

```matha
def restore(self, backup_path: str) -> bool
```

**描述**: 恢复数据库

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `backup_path` | `str` | |

**返回**: `bool`

Source: `src\offline\sqlite_storage.py:438`

---

### `close`

```matha
def close(self) -> None
```

**描述**: 关闭数据库连接

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\sqlite_storage.py:457`

---

### `__enter__`

```matha
def __enter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\offline\sqlite_storage.py:464`

---

### `__exit__`

```matha
def __exit__(self, exc_type, exc_val, exc_tb)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exc_type` | `` | |
| `exc_val` | `` | |
| `exc_tb` | `` | |

Source: `src\offline\sqlite_storage.py:467`

---

### `__del__`

```matha
def __del__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\offline\sqlite_storage.py:470`

---

## `get_storage`

```matha
def get_storage() -> SQLiteStorage
```

**描述**: 获取全局存储实例（单例）

**返回**: `SQLiteStorage`

Source: `src\offline\sqlite_storage.py:478`

---

# `offline.storage`

```matha

```

**描述**: Matha 离线模式数据存储模块

提供 SQLite 本地数据存储功能：
  - REPL 历史记录持久化
  - 用户偏好设置存储
  - 计算结果缓存
  - 离线补全词库

使用方式：
  from src.offline.storage import OfflineStorage
  storage = OfflineStorage()
  storage.save_history("x = 1 + 2", "3")

Source: `src\offline\storage.py:1`

---

## `HistoryEntry`

```matha
class HistoryEntry
```

**描述**: 历史记录条目

**装饰器**: dataclass

Source: `src\offline\storage.py:30`

---

## `UserPreference`

```matha
class UserPreference
```

**描述**: 用户偏好设置

**装饰器**: dataclass

Source: `src\offline\storage.py:40`

---

## `OfflineStorage`

```matha
class OfflineStorage
```

**描述**: 离线模式数据存储

使用 SQLite 本地数据库存储：
- REPL 历史记录
- 用户偏好设置
- 计算结果缓存
- 离线补全词库

Source: `src\offline\storage.py:47`

---

### `__init__`

```matha
def __init__(self, db_path: Optional[str])
```

**描述**: 初始化离线存储

Args:
    db_path: 数据库文件路径，默认为 ~/.matha/data.db

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `db_path` | `Optional[str]` | |

Source: `src\offline\storage.py:58`

---

### `_get_connection`

```matha
def _get_connection(self) -> sqlite3.Connection
```

**描述**: 获取数据库连接

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `sqlite3.Connection`

Source: `src\offline\storage.py:74`

---

### `_init_database`

```matha
def _init_database(self) -> None
```

**描述**: 初始化数据库表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\storage.py:84`

---

### `save_history`

```matha
def save_history(self, code: str, output: str, is_success: bool) -> int
```

**描述**: 保存历史记录

Args:
    code: 输入代码
    output: 输出结果
    is_success: 是否执行成功

Returns:
    新记录的 ID

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `output` | `str` | |
| `is_success` | `bool` | |

**返回**: `int`

Source: `src\offline\storage.py:144`

---

### `get_history`

```matha
def get_history(self, limit: int, offset: int) -> List[HistoryEntry]
```

**描述**: 获取历史记录

Args:
    limit: 返回数量限制
    offset: 偏移量

Returns:
    历史记录列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |
| `offset` | `int` | |

**返回**: `List[HistoryEntry]`

Source: `src\offline\storage.py:165`

---

### `clear_history`

```matha
def clear_history(self) -> int
```

**描述**: 清空历史记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\offline\storage.py:185`

---

### `get_history_count`

```matha
def get_history_count(self) -> int
```

**描述**: 获取历史记录总数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\offline\storage.py:193`

---

### `save_preference`

```matha
def save_preference(self, key: str, value: str) -> None
```

**描述**: 保存用户偏好设置

Args:
    key: 设置键
    value: 设置值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `value` | `str` | |

**返回**: `None`

Source: `src\offline\storage.py:204`

---

### `get_preference`

```matha
def get_preference(self, key: str, default: str) -> str
```

**描述**: 获取用户偏好设置

Args:
    key: 设置键
    default: 默认值

Returns:
    设置值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `default` | `str` | |

**返回**: `str`

Source: `src\offline\storage.py:220`

---

### `get_all_preferences`

```matha
def get_all_preferences(self) -> Dict[str, str]
```

**描述**: 获取所有偏好设置

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, str]`

Source: `src\offline\storage.py:237`

---

### `cache_result`

```matha
def cache_result(self, code: str, output: str) -> bool
```

**描述**: 缓存计算结果

Args:
    code: 输入代码
    output: 输出结果

Returns:
    是否缓存成功

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `output` | `str` | |

**返回**: `bool`

Source: `src\offline\storage.py:248`

---

### `get_cached_result`

```matha
def get_cached_result(self, code: str) -> Optional[str]
```

**描述**: 获取缓存的计算结果

Args:
    code: 输入代码

Returns:
    缓存的输出，不存在则返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |

**返回**: `Optional[str]`

Source: `src\offline\storage.py:275`

---

### `get_cache_stats`

```matha
def get_cache_stats(self) -> Dict[str, int]
```

**描述**: 获取缓存统计信息

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, int]`

Source: `src\offline\storage.py:294`

---

### `add_completion_word`

```matha
def add_completion_word(self, word: str, category: str) -> None
```

**描述**: 添加补全词

Args:
    word: 词汇
    category: 类别

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `word` | `str` | |
| `category` | `str` | |

**返回**: `None`

Source: `src\offline\storage.py:305`

---

### `increment_word_usage`

```matha
def increment_word_usage(self, word: str) -> None
```

**描述**: 增加词汇使用次数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `word` | `str` | |

**返回**: `None`

Source: `src\offline\storage.py:321`

---

### `get_completion_words`

```matha
def get_completion_words(self, category: Optional[str], limit: int) -> List[str]
```

**描述**: 获取补全词列表

Args:
    category: 按类别筛选
    limit: 返回数量限制

Returns:
    词汇列表（按使用次数排序）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `category` | `Optional[str]` | |
| `limit` | `int` | |

**返回**: `List[str]`

Source: `src\offline\storage.py:331`

---

### `add_batch_words`

```matha
def add_batch_words(self, words: List[str], category: str) -> int
```

**描述**: 批量添加补全词

Args:
    words: 词汇列表
    category: 类别

Returns:
    添加的数量

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `words` | `List[str]` | |
| `category` | `str` | |

**返回**: `int`

Source: `src\offline\storage.py:356`

---

### `get_db_size`

```matha
def get_db_size(self) -> float
```

**描述**: 获取数据库文件大小（MB）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\offline\storage.py:380`

---

### `backup`

```matha
def backup(self, backup_path: str) -> bool
```

**描述**: 备份数据库

Args:
    backup_path: 备份文件路径

Returns:
    是否备份成功

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `backup_path` | `str` | |

**返回**: `bool`

Source: `src\offline\storage.py:387`

---

### `restore`

```matha
def restore(self, backup_path: str) -> bool
```

**描述**: 恢复数据库

Args:
    backup_path: 备份文件路径

Returns:
    是否恢复成功

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `backup_path` | `str` | |

**返回**: `bool`

Source: `src\offline\storage.py:406`

---

### `close`

```matha
def close(self) -> None
```

**描述**: 关闭数据库连接

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\storage.py:427`

---

### `__enter__`

```matha
def __enter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\offline\storage.py:434`

---

### `__exit__`

```matha
def __exit__(self, exc_type, exc_val, exc_tb)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exc_type` | `` | |
| `exc_val` | `` | |
| `exc_tb` | `` | |

Source: `src\offline\storage.py:437`

---

### `__del__`

```matha
def __del__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\offline\storage.py:440`

---

## `get_storage`

```matha
def get_storage() -> OfflineStorage
```

**描述**: 获取全局存储实例（单例）

**返回**: `OfflineStorage`

Source: `src\offline\storage.py:451`

---

# `offline.sync`

```matha

```

**描述**: Matha 离线同步模块

提供离线模式下的数据同步功能：
  - 冲突检测与解决
  - 增量同步
  - 同步日志
  - 同步队列管理

Source: `src\offline\sync.py:1`

---

## `ConflictStrategy`

```matha
class ConflictStrategy(Enum)
```

**描述**: 冲突解决策略

Source: `src\offline\sync.py:24`

---

## `SyncConflict`

```matha
class SyncConflict
```

**描述**: 同步冲突

**装饰器**: dataclass

Source: `src\offline\sync.py:33`

---

## `SyncLog`

```matha
class SyncLog
```

**描述**: 同步日志

**装饰器**: dataclass

Source: `src\offline\sync.py:47`

---

## `SyncConflictResolver`

```matha
class SyncConflictResolver
```

**描述**: 同步冲突解决器

支持的策略：
1. LAST_WRITE_WINS - 最后写入优先（时间戳比较）
2. FIRST_WRITE_WINS - 最先写入优先
3. MERGE - 智能合并（深层合并字典）
4. MANUAL - 手动选择

Source: `src\offline\sync.py:58`

---

### `__init__`

```matha
def __init__(self, default_strategy: ConflictStrategy)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `default_strategy` | `ConflictStrategy` | |

Source: `src\offline\sync.py:69`

---

### `register_callback`

```matha
def register_callback(self, callback: Callable[[SyncConflict], None]) -> None
```

**描述**: 注册冲突解决回调

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable[[SyncConflict], None]` | |

**返回**: `None`

Source: `src\offline\sync.py:75`

---

### `resolve`

```matha
def resolve(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]
```

**描述**: 解决冲突

Args:
    conflict: 冲突对象

Returns:
    解决后的数据，或 None（需要手动处理）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `conflict` | `SyncConflict` | |

**返回**: `Optional[Dict[str, Any]]`

Source: `src\offline\sync.py:79`

---

### `_last_write_wins`

```matha
def _last_write_wins(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]
```

**描述**: 最后写入优先策略

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `conflict` | `SyncConflict` | |

**返回**: `Optional[Dict[str, Any]]`

Source: `src\offline\sync.py:125`

---

### `_first_write_wins`

```matha
def _first_write_wins(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]
```

**描述**: 最先写入优先策略

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `conflict` | `SyncConflict` | |

**返回**: `Optional[Dict[str, Any]]`

Source: `src\offline\sync.py:134`

---

### `_merge`

```matha
def _merge(self, conflict: SyncConflict) -> Optional[Dict[str, Any]]
```

**描述**: 智能合并策略

合并规则：
- 字典：递归合并，本地优先
- 列表：合并去重（基于 ID）
- 标量：本地优先

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `conflict` | `SyncConflict` | |

**返回**: `Optional[Dict[str, Any]]`

Source: `src\offline\sync.py:143`

---

### `_deep_merge`

```matha
def _deep_merge(self, local: Dict[str, Any], remote: Dict[str, Any]) -> Dict[str, Any]
```

**描述**: 深层合并两个字典

规则：
- 嵌套字典：递归合并
- 列表：按 ID 合并去重
- 其他：本地优先

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `local` | `Dict[str, Any]` | |
| `remote` | `Dict[str, Any]` | |

**返回**: `Dict[str, Any]`

Source: `src\offline\sync.py:156`

---

### `_merge_lists`

```matha
def _merge_lists(self, local: List[Any], remote: List[Any]) -> List[Any]
```

**描述**: 合并列表，按 ID 去重

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `local` | `List[Any]` | |
| `remote` | `List[Any]` | |

**返回**: `List[Any]`

Source: `src\offline\sync.py:185`

---

### `manual_resolve`

```matha
def manual_resolve(self, conflict_id: str, resolution: str, resolved_data: Dict[str, Any]) -> None
```

**描述**: 手动解决冲突

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `conflict_id` | `str` | |
| `resolution` | `str` | |
| `resolved_data` | `Dict[str, Any]` | |

**返回**: `None`

Source: `src\offline\sync.py:210`

---

### `_notify_callbacks`

```matha
def _notify_callbacks(self, conflict: SyncConflict) -> None
```

**描述**: 通知所有回调

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `conflict` | `SyncConflict` | |

**返回**: `None`

Source: `src\offline\sync.py:215`

---

### `get_conflict_history`

```matha
def get_conflict_history(self, limit: int) -> List[SyncConflict]
```

**描述**: 获取冲突历史

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |

**返回**: `List[SyncConflict]`

Source: `src\offline\sync.py:223`

---

### `clear_history`

```matha
def clear_history(self) -> None
```

**描述**: 清空冲突历史

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\sync.py:227`

---

### `_apply_resolution`

```matha
def _apply_resolution(self, conflict: SyncConflict, resolution: str) -> Optional[Dict[str, Any]]
```

**描述**: 应用预解决的策略

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `conflict` | `SyncConflict` | |
| `resolution` | `str` | |

**返回**: `Optional[Dict[str, Any]]`

Source: `src\offline\sync.py:232`

---

## `SyncLogger`

```matha
class SyncLogger
```

**描述**: 同步日志记录器

记录所有同步操作的详细信息，用于调试和问题排查

Source: `src\offline\sync.py:243`

---

### `__init__`

```matha
def __init__(self, log_file: Optional[str])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `log_file` | `Optional[str]` | |

Source: `src\offline\sync.py:250`

---

### `log`

```matha
def log(self, action: str, record_type: str, record_id: str, status: str, details: str) -> None
```

**描述**: 记录同步日志

Args:
    action: 操作类型 (push, pull, merge, conflict)
    record_type: 记录类型 (history, preference, cache)
    record_id: 记录 ID
    status: 状态 (pending, synced, conflict, failed)
    details: 详细信息

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `action` | `str` | |
| `record_type` | `str` | |
| `record_id` | `str` | |
| `status` | `str` | |
| `details` | `str` | |

**返回**: `None`

Source: `src\offline\sync.py:255`

---

### `_write_to_file`

```matha
def _write_to_file(self, log: SyncLog) -> None
```

**描述**: 写入日志文件

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `log` | `SyncLog` | |

**返回**: `None`

Source: `src\offline\sync.py:279`

---

### `get_logs`

```matha
def get_logs(self, limit: int, action: Optional[str]) -> List[Dict[str, Any]]
```

**描述**: 获取日志

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |
| `action` | `Optional[str]` | |

**返回**: `List[Dict[str, Any]]`

Source: `src\offline\sync.py:294`

---

### `clear_logs`

```matha
def clear_logs(self) -> None
```

**描述**: 清空日志

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\sync.py:311`

---

## `OfflineSyncManager`

```matha
class OfflineSyncManager
```

**描述**: 离线同步管理器

管理离线模式下的数据同步：
- 同步队列管理
- 冲突检测与解决
- 增量同步
- 同步日志记录

Source: `src\offline\sync.py:320`

---

### `__init__`

```matha
def __init__(self, resolver: Optional[SyncConflictResolver], sync_logger: Optional[SyncLogger], sync_interval: float)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `resolver` | `Optional[SyncConflictResolver]` | |
| `sync_logger` | `Optional[SyncLogger]` | |
| `sync_interval` | `float` | |

Source: `src\offline\sync.py:331`

---

### `register_callback`

```matha
def register_callback(self, callback: Callable[[str, str, str], None]) -> None
```

**描述**: 注册同步回调 (action, record_type, record_id)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `Callable[[str, str, str], None]` | |

**返回**: `None`

Source: `src\offline\sync.py:345`

---

### `enqueue`

```matha
def enqueue(self, action: str, record_type: str, record_id: str, data: Dict[str, Any], priority: int) -> None
```

**描述**: 将操作加入同步队列

Args:
    action: 操作类型 (push, pull)
    record_type: 记录类型
    record_id: 记录 ID
    data: 数据
    priority: 优先级（越高越先处理）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `action` | `str` | |
| `record_type` | `str` | |
| `record_id` | `str` | |
| `data` | `Dict[str, Any]` | |
| `priority` | `int` | |

**返回**: `None`

Source: `src\offline\sync.py:349`

---

### `process_queue`

```matha
def process_queue(self, remote_storage: Any) -> List[Dict[str, Any]]
```

**描述**: 处理同步队列

Args:
    remote_storage: 远程存储接口

Returns:
    处理结果列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `remote_storage` | `Any` | |

**返回**: `List[Dict[str, Any]]`

Source: `src\offline\sync.py:374`

---

### `_process_item`

```matha
def _process_item(self, item: Dict[str, Any], remote_storage: Any) -> Dict[str, Any]
```

**描述**: 处理单个同步项目

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `item` | `Dict[str, Any]` | |
| `remote_storage` | `Any` | |

**返回**: `Dict[str, Any]`

Source: `src\offline\sync.py:415`

---

### `_push_item`

```matha
def _push_item(self, item: Dict[str, Any], remote_storage: Any) -> Dict[str, Any]
```

**描述**: 推送数据到远程

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `item` | `Dict[str, Any]` | |
| `remote_storage` | `Any` | |

**返回**: `Dict[str, Any]`

Source: `src\offline\sync.py:440`

---

### `_pull_item`

```matha
def _pull_item(self, item: Dict[str, Any], remote_storage: Any) -> Dict[str, Any]
```

**描述**: 从远程拉取数据

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `item` | `Dict[str, Any]` | |
| `remote_storage` | `Any` | |

**返回**: `Dict[str, Any]`

Source: `src\offline\sync.py:480`

---

### `_has_conflict`

```matha
def _has_conflict(self, local: Dict[str, Any], remote: Dict[str, Any]) -> bool
```

**描述**: 检测是否有冲突

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `local` | `Dict[str, Any]` | |
| `remote` | `Dict[str, Any]` | |

**返回**: `bool`

Source: `src\offline\sync.py:515`

---

### `_check_network`

```matha
def _check_network(self) -> bool
```

**描述**: 检查网络连接

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\offline\sync.py:534`

---

### `get_queue_size`

```matha
def get_queue_size(self) -> int
```

**描述**: 获取队列大小

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\offline\sync.py:543`

---

### `is_syncing`

```matha
def is_syncing(self) -> bool
```

**描述**: 是否正在同步

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\offline\sync.py:547`

---

### `get_last_sync_time`

```matha
def get_last_sync_time(self) -> float
```

**描述**: 获取最后同步时间

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\offline\sync.py:551`

---

### `get_sync_logs`

```matha
def get_sync_logs(self, limit: int) -> List[Dict[str, Any]]
```

**描述**: 获取同步日志

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |

**返回**: `List[Dict[str, Any]]`

Source: `src\offline\sync.py:555`

---

### `clear_queue`

```matha
def clear_queue(self) -> None
```

**描述**: 清空同步队列

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline\sync.py:559`

---

# `offline_store`

```matha

```

**描述**: Matha 离线存储引擎

提供离线模式下的数据持久化与同步：
  - SQLite 本地存储
  - 变更日志（CRDT 友好）
  - 离线优先架构
  - 冲突自动解决

Source: `src\offline_store.py:1`

---

## `ChangeEntry`

```matha
class ChangeEntry
```

**描述**: 变更日志条目（用于离线同步）

**装饰器**: dataclass

Source: `src\offline_store.py:24`

---

## `OfflineStore`

```matha
class OfflineStore
```

**描述**: 离线存储引擎

Source: `src\offline_store.py:36`

---

### `__init__`

```matha
def __init__(self, db_path: Optional[str])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `db_path` | `Optional[str]` | |

Source: `src\offline_store.py:39`

---

### `_generate_client_id`

```matha
def _generate_client_id(self) -> str
```

**描述**: 生成客户端唯一 ID

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\offline_store.py:45`

---

### `_init_db`

```matha
def _init_db(self) -> None
```

**描述**: 初始化数据库表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline_store.py:53`

---

### `save_project`

```matha
def save_project(self, project_id: str, name: str, data: dict) -> bool
```

**描述**: 保存项目（离线优先）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `project_id` | `str` | |
| `name` | `str` | |
| `data` | `dict` | |

**返回**: `bool`

Source: `src\offline_store.py:101`

---

### `load_project`

```matha
def load_project(self, project_id: str) -> Optional[dict]
```

**描述**: 加载项目

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `project_id` | `str` | |

**返回**: `Optional[dict]`

Source: `src\offline_store.py:119`

---

### `list_projects`

```matha
def list_projects(self) -> List[dict]
```

**描述**: 列出所有项目

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[dict]`

Source: `src\offline_store.py:138`

---

### `delete_project`

```matha
def delete_project(self, project_id: str) -> bool
```

**描述**: 删除项目

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `project_id` | `str` | |

**返回**: `bool`

Source: `src\offline_store.py:155`

---

### `_log_change`

```matha
def _log_change(self, entity_type: str, entity_id: str, action: str, data: dict) -> None
```

**描述**: 记录变更（用于离线同步）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `entity_type` | `str` | |
| `entity_id` | `str` | |
| `action` | `str` | |
| `data` | `dict` | |

**返回**: `None`

Source: `src\offline_store.py:168`

---

### `get_pending_changes`

```matha
def get_pending_changes(self, limit: int) -> List[ChangeEntry]
```

**描述**: 获取待同步的变更

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `limit` | `int` | |

**返回**: `List[ChangeEntry]`

Source: `src\offline_store.py:203`

---

### `mark_synced`

```matha
def mark_synced(self, change_id: str) -> bool
```

**描述**: 标记变更已同步

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `change_id` | `str` | |

**返回**: `bool`

Source: `src\offline_store.py:232`

---

### `get_sync_stats`

```matha
def get_sync_stats(self) -> dict
```

**描述**: 获取同步统计

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\offline_store.py:246`

---

### `resolve_conflict`

```matha
def resolve_conflict(self, local_data: dict, remote_data: dict, local_ts: float, remote_ts: float) -> dict
```

**描述**: 最后写入者获胜冲突解决策略

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `local_data` | `dict` | |
| `remote_data` | `dict` | |
| `local_ts` | `float` | |
| `remote_ts` | `float` | |

**返回**: `dict`

Source: `src\offline_store.py:260`

---

### `close`

```matha
def close(self) -> None
```

**描述**: 关闭存储

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\offline_store.py:276`

---

### `__enter__`

```matha
def __enter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\offline_store.py:282`

---

### `__exit__`

```matha
def __exit__(self, *args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\offline_store.py:285`

---

## `get_offline_store`

```matha
def get_offline_store() -> OfflineStore
```

**描述**: 获取离线存储单例

**返回**: `OfflineStore`

Source: `src\offline_store.py:293`

---

# `optimization.sparse_svd`

```matha

```

**描述**: Matha v4.4 — 稀疏矩阵 SVD 优化模块

本模块提供稀疏矩阵 SVD 分解的优化实现，包括：
  - 稀疏矩阵检测
  - 稀疏 SVD 分解（Lanczos 迭代法）
  - 性能优化与缓存

数学背景：
  对于稀疏矩阵 A（稀疏度 > 90%），使用 Lanczos 迭代法
  可以显著减少计算量，避免遍历零元素。

用法：
  from src.optimization.sparse_svd import SparseSVDOptimizer

  optimizer = SparseSVDOptimizer(threshold=0.9, max_iter=100)

  # 检测稀疏矩阵
  is_sparse = optimizer.is_sparse(matrix)

  # 执行稀疏 SVD
  U, S, Vt = optimizer.svd(matrix)

  # 性能对比
  speedup = optimizer.compare_performance(matrix)

Source: `src\optimization\sparse_svd.py:1`

---

## `SVDResult`

```matha
class SVDResult
```

**描述**: SVD 分解结果。

**装饰器**: dataclass

Source: `src\optimization\sparse_svd.py:39`

---

## `PerformanceStats`

```matha
class PerformanceStats
```

**描述**: 性能统计。

**装饰器**: dataclass

Source: `src\optimization\sparse_svd.py:50`

---

## `SparseSVDOptimizer`

```matha
class SparseSVDOptimizer
```

**描述**: 稀疏矩阵 SVD 优化器。

该优化器提供：
1. 稀疏矩阵检测
2. 稀疏 SVD 分解（Lanczos 迭代法）
3. 性能对比与缓存

用法：
    optimizer = SparseSVDOptimizer(threshold=0.9, max_iter=100)
    result = optimizer.svd(matrix)

Source: `src\optimization\sparse_svd.py:59`

---

### `__init__`

```matha
def __init__(self, threshold: float, max_iter: int, max_singular_values: int, cache_enabled: bool, cache_max_size: int)
```

**描述**: 初始化优化器。

Args:
    threshold: 稀疏度阈值（默认 0.9，即 90% 元素为零）
    max_iter: 最大迭代次数
    max_singular_values: 最大计算的奇异值数量
    cache_enabled: 是否启用缓存
    cache_max_size: 缓存最大条目数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threshold` | `float` | |
| `max_iter` | `int` | |
| `max_singular_values` | `int` | |
| `cache_enabled` | `bool` | |
| `cache_max_size` | `int` | |

Source: `src\optimization\sparse_svd.py:73`

---

### `is_sparse`

```matha
def is_sparse(self, A: 'Matrix') -> bool
```

**描述**: 检测矩阵是否为稀疏矩阵。

Args:
    A: m×n 矩阵

Returns:
    如果是稀疏矩阵返回 True

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `A` | `'Matrix'` | |

**返回**: `bool`

Source: `src\optimization\sparse_svd.py:103`

---

### `_get_sparsity`

```matha
def _get_sparsity(self, A: 'Matrix') -> float
```

**描述**: 计算矩阵稀疏度。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `A` | `'Matrix'` | |

**返回**: `float`

Source: `src\optimization\sparse_svd.py:125`

---

### `_make_cache_key`

```matha
def _make_cache_key(self, A: 'Matrix') -> tuple
```

**描述**: 创建缓存键。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `A` | `'Matrix'` | |

**返回**: `tuple`

Source: `src\optimization\sparse_svd.py:135`

---

### `_evict_cache`

```matha
def _evict_cache(self)
```

**描述**: 缓存淘汰。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\optimization\sparse_svd.py:139`

---

### `svd`

```matha
def svd(self, A: 'Matrix', use_numpy: bool, force_sparse: bool) -> SVDResult
```

**描述**: 执行 SVD 分解，自动选择最优算法。

Args:
    A: m×n 矩阵
    use_numpy: 是否优先使用 NumPy
    force_sparse: 强制使用稀疏算法

Returns:
    SVDResult: 包含 U, S, Vt 和性能统计

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `A` | `'Matrix'` | |
| `use_numpy` | `bool` | |
| `force_sparse` | `bool` | |

**返回**: `SVDResult`

Source: `src\optimization\sparse_svd.py:147`

---

### `compare_performance`

```matha
def compare_performance(self, A: 'Matrix', iterations: int) -> PerformanceStats
```

**描述**: 对比不同算法的性能。

Args:
    A: 测试矩阵
    iterations: 测试迭代次数

Returns:
    PerformanceStats: 性能统计

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `A` | `'Matrix'` | |
| `iterations` | `int` | |

**返回**: `PerformanceStats`

Source: `src\optimization\sparse_svd.py:232`

---

### `batch_svd`

```matha
def batch_svd(self, matrices: List['Matrix'], use_numpy: bool, parallel: bool, n_workers: int) -> List[SVDResult]
```

**描述**: 批量 SVD 分解。

Args:
    matrices: 矩阵列表
    use_numpy: 是否优先使用 NumPy
    parallel: 是否启用并行计算
    n_workers: 工作线程数

Returns:
    SVDResult 列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `matrices` | `List['Matrix']` | |
| `use_numpy` | `bool` | |
| `parallel` | `bool` | |
| `n_workers` | `int` | |

**返回**: `List[SVDResult]`

Source: `src\optimization\sparse_svd.py:282`

---

### `predict_large_matrix_time`

```matha
def predict_large_matrix_time(self, base_size: int, base_time_ms: float, target_size: int, sparsity: float) -> Dict[str, float]
```

**描述**: 预测大规模矩阵的 SVD 耗时。

基于 O(n³) 复杂度模型进行预测。

Args:
    base_size: 基准矩阵规模
    base_time_ms: 基准耗时（ms）
    target_size: 目标矩阵规模
    sparsity: 稀疏度（0-1）

Returns:
    预测耗时字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `base_size` | `int` | |
| `base_time_ms` | `float` | |
| `target_size` | `int` | |
| `sparsity` | `float` | |

**返回**: `Dict[str, float]`

Source: `src\optimization\sparse_svd.py:331`

---

### `get_statistics`

```matha
def get_statistics(self) -> Dict
```

**描述**: 获取优化器统计信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\optimization\sparse_svd.py:379`

---

### `clear_cache`

```matha
def clear_cache(self)
```

**描述**: 清空缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\optimization\sparse_svd.py:391`

---

## `create_optimizer`

```matha
def create_optimizer(threshold: float, max_iter: int, cache_enabled: bool) -> SparseSVDOptimizer
```

**描述**: 创建稀疏 SVD 优化器实例。

Args:
    threshold: 稀疏度阈值
    max_iter: 最大迭代次数
    cache_enabled: 是否启用缓存

Returns:
    SparseSVDOptimizer 实例

| 参数 | 类型 | 说明 |
|------|------|------|
| `threshold` | `float` | |
| `max_iter` | `int` | |
| `cache_enabled` | `bool` | |

**返回**: `SparseSVDOptimizer`

Source: `src\optimization\sparse_svd.py:401`

---

## `quick_svd`

```matha
def quick_svd(A: 'Matrix', threshold: float, max_iter: int) -> Tuple['Matrix', 'Matrix', 'Matrix']
```

**描述**: 快速 SVD 分解（便捷函数）。

Args:
    A: 输入矩阵
    threshold: 稀疏度阈值
    max_iter: 最大迭代次数

Returns:
    (U, S, Vt) 元组

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `'Matrix'` | |
| `threshold` | `float` | |
| `max_iter` | `int` | |

**返回**: `Tuple['Matrix', 'Matrix', 'Matrix']`

Source: `src\optimization\sparse_svd.py:424`

---

# `parser`

```matha

```

**描述**: Matha 递归下降语法分析器。

对应 EBNF（17-完整语法EBNF.md）各章产生式。
每个 parse_* 方法对应一条 EBNF 产生式，方法签名注释标注对应产生式。

规格版本：0.6 草案（M3.2 细化版）

关键消解规则（EBNF §16）：
    - >> 四重语义：链式 / 步进迭代 / 属于判断 / 路径距离（按位置消解）
    - ^ 双语义：中缀次方（前有操作数）/ 前缀开方（前无操作数）
    - { } 双语义：代码块 vs 集合构造（按 { 后内容消解）
    - #N: vs #：：# 后跟数字 → 段号；否则无段号
    - 段内 5 步固定顺序：命令→变量→？公式→字母公式→输出（M3.1/M3.2）
    - >> 链式触发条件：单条命令/单条输出不足以完成任务时才启用（M3.2）

Source: `src\parser.py:1`

---

## `ParseError`

```matha
class ParseError(Exception)
```

**描述**: 语法错误。

Source: `src\parser.py:33`

---

### `__init__`

```matha
def __init__(self, msg: str, token: Token)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |
| `token` | `Token` | |

Source: `src\parser.py:36`

---

## `Parser`

```matha
class Parser
```

**描述**: Matha 递归下降语法分析器。

用法：
    parser = Parser(source_text)
    program = parser.parse()

Source: `src\parser.py:41`

---

### `__init__`

```matha
def __init__(self, source: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

Source: `src\parser.py:49`

---

### `_peek`

```matha
def _peek(self, offset: int) -> Token
```

**描述**: 查看当前 + offset 处的 Token。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `offset` | `int` | |

**返回**: `Token`

Source: `src\parser.py:66`

---

### `_current`

```matha
def _current(self) -> Token
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Token`

Source: `src\parser.py:71`

---

### `_advance`

```matha
def _advance(self) -> Token
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Token`

Source: `src\parser.py:74`

---

### `_check`

```matha
def _check(self, *types) -> bool
```

**描述**: 当前 Token 类型是否在 types 中。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:80`

---

### `_match`

```matha
def _match(self, *types) -> Token | None
```

**描述**: 若当前 Token 匹配则消费并返回，否则返回 None。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Token | None`

Source: `src\parser.py:84`

---

### `_expect`

```matha
def _expect(self, ttype: TokenType, desc: str) -> Token
```

**描述**: 断言当前 Token 为 ttype，消费并返回；否则抛 ParseError。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `ttype` | `TokenType` | |
| `desc` | `str` | |

**返回**: `Token`

Source: `src\parser.py:90`

---

### `_skip_newlines`

```matha
def _skip_newlines(self) -> None
```

**描述**: 跳过连续换行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\parser.py:96`

---

### `_skip_semicolon`

```matha
def _skip_semicolon(self) -> None
```

**描述**: 跳过语句结束的分号（ASCII `;`、全角 `；`）。

注意：全角逗号 `，` 不在此处消费，而是在 _parse_expr 中作为表达式分隔符处理。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\parser.py:101`

---

### `_is_colon`

```matha
def _is_colon(self) -> bool
```

**描述**: 当前是否为冒号（半角 : 或全角 ：）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:114`

---

### `_is_comma`

```matha
def _is_comma(self) -> bool
```

**描述**: 当前是否为逗号（半角 , 或全角 ，）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:118`

---

### `_skip_comma`

```matha
def _skip_comma(self) -> None
```

**描述**: 跳过逗号分隔符（半角 , 或全角 ，）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\parser.py:122`

---

### `_is_read_open`

```matha
def _is_read_open(self) -> bool
```

**描述**: 当前是否为读取开括号（【 或 〔）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:127`

---

### `_is_read_close`

```matha
def _is_read_close(self) -> bool
```

**描述**: 当前是否为读取闭括号（】 或 〔）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:131`

---

### `parse`

```matha
def parse(self) -> ast.Program
```

**描述**: 解析整个程序，返回 AST 根节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.Program`

Source: `src\parser.py:142`

---

### `_parse_top_level`

```matha
def _parse_top_level(self)
```

**描述**: <top_level>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:157`

---

### `_parse_annotation`

```matha
def _parse_annotation(self) -> ast.Annotation
```

**描述**: <annotation> = "*/" , <annot_text> , [ "*" , <expr> ] , "/*" 

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.Annotation`

Source: `src\parser.py:209`

---

### `_parse_command_literal`

```matha
def _parse_command_literal(self) -> ast.CommandLiteral
```

**描述**: <command_literal> = 《文字》 | 【文字】（两种写法等价，M3.1）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.CommandLiteral`

Source: `src\parser.py:221`

---

### `_parse_command_text`

```matha
def _parse_command_text(self) -> str
```

**描述**: <command_text> = <identifier> | <natural_lang_fragment>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\parser.py:235`

---

### `_parse_set_up`

```matha
def _parse_set_up(self, seg_id: int | None) -> ast.SetUp
```

**描述**: <set_up> = @(...) | @:xxx，yyy
<set_up_seg> = @N(...) | @N:xxx，yyy（带段号，M3）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `seg_id` | `int | None` | |

**返回**: `ast.SetUp`

Source: `src\parser.py:242`

---

### `_parse_set_up_item`

```matha
def _parse_set_up_item(self) -> ast.SetUpItem
```

**描述**: <set_up_item> = ( <variable> | <path_expr> ) , [ <annotation> ] , [ "=" , <expr> ]

设定值可能是表达式或路径/文本（如 /data/config、8080、10元、windows.iso）。
当值无法完整解析为表达式时（如含 . 的文件名），回退为文本字符串。
裸标识符值（如 MyApp、x64）视为配置字符串，不当作变量引用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.SetUpItem`

Source: `src\parser.py:291`

---

### `_parse_variable_or_path`

```matha
def _parse_variable_or_path(self)
```

**描述**: <variable> | <path_expr>（路径 a>>b）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:322`

---

### `_is_path_context`

```matha
def _is_path_context(self) -> bool
```

**描述**: 判断 >> 是否为路径/距离语境（绑定/设定左侧 a>>b=...）。

路径语境条件：
  1. >> 后跟标识符或占位符（保留原逻辑）
  2. 标识符后跟 = （区分路径 a>>b=5 与属于判断 a>>b）
  3. 不在控制流条件解析中（_in_control_flow）
  4. 不在 lambda 体内（_in_lambda_body）
  5. 不在函数调用参数中（_in_func_app）
  6. 不处于链式语境（_is_chain_context）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:333`

---

### `_parse_loop_fraction`

```matha
def _parse_loop_fraction(self) -> ast.LoopFraction
```

**描述**: <loop_fraction> = (x/y) 或 （x/y），半/全角等价
支持占位符 ？ 表示待定次数（存储为 -1）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.LoopFraction`

Source: `src\parser.py:368`

---

### `_parse_seg_loop_suffix`

```matha
def _parse_seg_loop_suffix(self) -> ast.SegLoopSuffix
```

**描述**: <seg_loop_suffix> = … , [ <seg_id> ] , <loop_fraction>（单省略号）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.SegLoopSuffix`

Source: `src\parser.py:386`

---

### `_parse_global_loop_suffix`

```matha
def _parse_global_loop_suffix(self) -> ast.GlobalLoopSuffix
```

**描述**: <global_loop_suffix> = …… , <loop_fraction>（双省略号）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.GlobalLoopSuffix`

Source: `src\parser.py:395`

---

### `_parse_output_trail`

```matha
def _parse_output_trail(self) -> ast.OutputTrail
```

**描述**: <output_trail> = <output>
                 , [ <seg_loop_suffix>
                   , [ <subfile_ref> ]            (* 段循环后：子文件，| 分隔多个 *)
                   , [ <global_code_id> ]
                   , [ <global_loop_suffix>
                     , [ <file_ref> ]             (* 全局循环后：文件路径 *)
                     ]
                   ]

完整末行形态（M3.3）：
    #：[输出]…（x/y）【子文件|子文件】<全局编号>……（x/y）【文件/路径】
模板形式（不可编辑代码）允许仅有全局循环而无段级循环：
    #：[输出]……（0/1）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.OutputTrail`

Source: `src\parser.py:401`

---

### `_parse_subfile_ref`

```matha
def _parse_subfile_ref(self) -> list[str]
```

**描述**: <subfile_ref> = ( 【 | 〔 ) , <path_content> , { "|" , <path_content> } , ( 】 | 〕 )

段循环后的子文件引用：下位文件，用于补充/扩充当前段。
多个子文件用 | 分隔：【sub1.matha|sub2.matha】

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\parser.py:439`

---

### `_parse_file_ref`

```matha
def _parse_file_ref(self) -> str
```

**描述**: <file_ref> = ( 【 | 〔 ) , <path_content> , ( 】 | 〕 )

全局循环后的文件路径：当前代码文件被分割/分化成多个文件使用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\parser.py:459`

---

### `_parse_global_id_stmt`

```matha
def _parse_global_id_stmt(self) -> ast.GlobalIdStmt
```

**描述**: <global_id_stmt> = <global_code_id>（独立一行或 #： 前缀的全局编号）
支持 ？ 占位符（模板/schema 形式）和整数编号。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.GlobalIdStmt`

Source: `src\parser.py:471`

---

### `_parse_mech_unit`

```matha
def _parse_mech_unit(self) -> ast.MechUnit
```

**描述**: <mech_unit> = ( <generate> | <generate_seg> ) , ( <code_block> | <mech_stmt> | <mech_body> )

#：{ ... }  → generate + code_block
#1：【命令】 → generate + GenStmt(content=command)
#1：？公式   → generate + GenStmt(content=expr)
#1：[输出]   → generate + GenStmt(content=output_trail)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.MechUnit`

Source: `src\parser.py:484`

---

### `_parse_gen_stmt_content`

```matha
def _parse_gen_stmt_content(self, generate: ast.Generate)
```

**描述**: 解析 gen_stmt / gen_stmt_seg 的内容部分（命令 / 表达式 / 输出追踪）。

<gen_stmt>     = <generate> , ( <command_literal> | <expr> | <output_trail> )
<gen_stmt_seg> = <generate_seg> , ( <command_literal> | <expr> | <output_trail> )

内容后检查 >> 链式（M3.2：单条不够才启用）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `generate` | `ast.Generate` | |

Source: `src\parser.py:504`

---

### `_parse_generate`

```matha
def _parse_generate(self) -> ast.Generate
```

**描述**: <generate> = #： 或 <generate_seg> = #N：

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.Generate`

Source: `src\parser.py:543`

---

### `_parse_code_block`

```matha
def _parse_code_block(self) -> ast.CodeBlock
```

**描述**: <code_block> = { <newline> , { <mech_stmt> , <newline> } , }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.CodeBlock`

Source: `src\parser.py:554`

---

### `_parse_block_body`

```matha
def _parse_block_body(self) -> ast.CodeBlock
```

**描述**: 解析代码块内部语句（不含外层 { }）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.CodeBlock`

Source: `src\parser.py:569`

---

### `_parse_mech_stmt`

```matha
def _parse_mech_stmt(self)
```

**描述**: <mech_stmt> —— 机械语句（分支消解）

<mech_stmt> = <binding> | <set_construct> | <iteration> | <output> | <output_trail>
            | <expr> | <set_up> | <set_up_seg>
            | <gen_stmt> | <gen_stmt_seg>
            | <file_marker> | <global_id_stmt> | <statement> | <chain_stmt>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:582`

---

### `_parse_define_op`

```matha
def _parse_define_op(self) -> ast.AST
```

**描述**: <define_op> = @define_op: <symbol> = <precedence> | <assoc>

例：
    @define_op: ≈ = 5 | left
    @define_op: ≡ = 5 | left
    @define_op: → = 3 | right

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.AST`

Source: `src\parser.py:630`

---

### `_parse_gen_or_file_marker`

```matha
def _parse_gen_or_file_marker(self)
```

**描述**: 区分 #：命令/公式/输出 vs #：【文件】/【路径】

#：【文件】     → FileMarker（结束标记，无段号）
#：【path】     → FileMarker（路径引用，无段号）
#N：【命令】    → GenStmt(command)（有段号）
#N：？公式      → GenStmt(expr)
#N：[输出]      → GenStmt(output_trail)
#N：…1（x/y）  → GenStmt(loop_line)（M3.2 分行）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:668`

---

### `_parse_loop_line`

```matha
def _parse_loop_line(self, generate: ast.Generate)
```

**描述**: M3.2/M3.3 分行循环后缀：#N：…N（x/y）[【子文件|…】][全局编号][……（x/y）][【文件/路径】]

主输出行 [输出] 已在上方独立一行，此处仅解析循环+子文件+路径部分。
生成一个 OutputTrail（expr=None）表示纯循环追踪行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `generate` | `ast.Generate` | |

Source: `src\parser.py:730`

---

### `_parse_read_or_command`

```matha
def _parse_read_or_command(self)
```

**描述**: 【...】独立行：read_block 或 command_literal

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:755`

---

### `_parse_brace_dispatch`

```matha
def _parse_brace_dispatch(self)
```

**描述**: { } 双义消解：代码块 vs 集合构造 vs 字典字面量（EBNF §16）

消解规则（前瞻 { 后第一个 token）：
- NEWLINE → 代码块（EBNF: { <newline> ... }）
- } → 空集合构造
- 逗号或管道 → 集合构造（枚举 {1,2,3} / 理解 {x | cond}）
- 整数/浮点/布尔 → 集合枚举（如 {1, 2, 3}）
- 字符串/标识符 且 下一 token 为 : → 字典字面量（如 {"类型": "程序"}）
- 标识符 且 下一 token 为 | → 集合理解（如 {x | x > 5}）
- 其他 → 代码块（默认规则）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:765`

---

### `_parse_set_construct`

```matha
def _parse_set_construct(self) -> ast.SetConstruct
```

**描述**: <set_construct> = { <var_list> | <cond_list> } 或 { <literal_list> }

枚举形式: {1, 2, 3}（逗号分隔）
理解形式: {x | cond | cond}（管道分隔）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.SetConstruct`

Source: `src\parser.py:810`

---

### `_parse_dict_literal`

```matha
def _parse_dict_literal(self) -> ast.DictLiteral
```

**描述**: <dict_literal> = { <key> : <value> , { , <key> : <value> } }

例：{"类型": "程序"}  →  DictLiteral(keys=["类型"], values=[StringLit("程序")])

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.DictLiteral`

Source: `src\parser.py:841`

---

### `_parse_expr_or_binding`

```matha
def _parse_expr_or_binding(self)
```

**描述**: 表达式 / 绑定消解：variable = expr → binding；否则 → expr

注意：= 在表达式内是关系等于（rel_op），_parse_expr 会将其解析为
BinaryOp("=", left, right)。在语句层，variable = value 应识别为 binding，
故将顶层 BinaryOp("=") 转换为 Binding（variable / path 两种 target）。

重要：若 = 左侧是函数应用（FuncApp），则为等于比较而非赋值。
例：做二元("+")(3)(5) = 8  →  BinaryOp("=", 做二元("+")(3)(5), 8)（等于判断）

Lambda 体内特殊处理：在 lambda 体内（_in_lambda_body=True），= 一律
作为等于比较，不转换为绑定。
例：op = "+" ? left + right : 0  →  BinaryOp("=", op, IfExpr(...))

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:866`

---

### `_make_binding_or_expr`

```matha
def _make_binding_or_expr(self, expr) -> ast.AST
```

**描述**: 将 expr 转换为 binding（如果 applicable），或原样返回。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `ast.AST`

Source: `src\parser.py:909`

---

### `_has_conditional_suffix`

```matha
def _has_conditional_suffix(self, expr) -> bool
```

**描述**: expr 是否是 比较表达式 且后面紧跟三元操作。

用于检测 `3 > 2 ? 100 : 200` 模式：expr 是 BinaryOp(>)，
其后还有 ? then : else。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `bool`

Source: `src\parser.py:923`

---

### `_parse_conditional_binding`

```matha
def _parse_conditional_binding(self, var: ast.Variable, rel_expr: ast.BinaryOp) -> ast.Binding
```

**描述**: 解析 var = <rel_expr> ? <then> : <else> 作为绑定。

rel_expr 是已解析的比较表达式（如 3 > 2）。
需要消费 ? then : else 并构建 IfExpr。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `ast.Variable` | |
| `rel_expr` | `ast.BinaryOp` | |

**返回**: `ast.Binding`

Source: `src\parser.py:941`

---

### `_is_chain_context`

```matha
def _is_chain_context(self) -> bool
```

**描述**: 判断 >> 是否为通用链式语境（M3.2 触发条件）。

链式语境：>> 两侧均为语句且不在循环头/绑定左侧/表达式内部。
M3.2 收紧：仅当单条命令/单条输出不足以完成任务时才启用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:969`

---

### `_parse_chain`

```matha
def _parse_chain(self, first_stmt) -> ast.ChainStmt
```

**描述**: <chain_stmt> = <mech_stmt> , { ">>" , <mech_stmt> }

拍平嵌套：子 mech_stmt（如 #N：段经 _parse_gen_or_file_marker）
可能递归返回 ChainStmt，此处展开为单层扁平链，契合 >> 的
扁平顺序语义（M3.1/M3.2）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `first_stmt` | `` | |

**返回**: `ast.ChainStmt`

Source: `src\parser.py:985`

---

### `_parse_expr`

```matha
def _parse_expr(self)
```

**描述**: <expr> = <or_expr> , [ ";" , <expr> ]* （支持分号分隔的表达式序列）

三元是最低优先级表达式，使 a ? b : c 可出现在绑定值 / lambda 体 /
输出 / 括号内等任意 <expr> 位置。半角 ? / : 对应 lexer 已识别的
OP_QUESTION / OP_COLON（与全角 ？占位符、：段号不冲突）。

let 表达式也在此处支持，使 lambda 体内可使用 let ... in。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1018`

---

### `_parse_or_expr`

```matha
def _parse_or_expr(self)
```

**描述**: <or_expr> = <and_expr> , { "or" , <and_expr> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1097`

---

### `_parse_and_expr`

```matha
def _parse_and_expr(self)
```

**描述**: <and_expr> = <rel_expr> , { "and" , <rel_expr> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1107`

---

### `_parse_rel_expr`

```matha
def _parse_rel_expr(self)
```

**描述**: <rel_expr> = <add_expr> , [ <rel_op> , <add_expr> | <expr> ]

比较运算符（> < >= <=）的右操作数用 _parse_add_expr（标准行为）。
赋值/等于运算符（= ==）的右操作数用 _parse_expr，使绑定值能包含三元表达式。

例：
  `m = 3 > 2 ? 100 : 200` → Binding(m, IfExpr(cond=3>2, then=100, else=200))
  `3 > 2 ? 100 : 200`     → IfExpr(cond=3>2, then=100, else=200)
  `z = 3 > 2`             → Binding(z, BinaryOp('>', 3, 2))

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1117`

---

### `_parse_add_expr`

```matha
def _parse_add_expr(self)
```

**描述**: <add_expr> = <mul_expr> , { ("+" | "-") , <mul_expr> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1153`

---

### `_parse_mul_expr`

```matha
def _parse_mul_expr(self)
```

**描述**: <mul_expr> = <pow_expr> , { ("*" | "/" | "%") , <pow_expr> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1162`

---

### `_parse_pow_expr`

```matha
def _parse_pow_expr(self)
```

**描述**: <pow_expr> = <unary> , [ "^" , <pow_expr> ]（中缀次方，右结合）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1171`

---

### `_parse_unary`

```matha
def _parse_unary(self)
```

**描述**: <unary> = [ "-" | "^" | "++" | "--" ] , <postfix>
^ 双语义消解：前无操作数 → 前缀开方（§16）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1180`

---

### `_parse_postfix`

```matha
def _parse_postfix(self)
```

**描述**: <postfix> = <primary> , { <primary> }（函数应用，左结合）

注意：当 _in_control_flow=True 时（if/while/for 的条件解析中），
跳过 { ... } 作为函数参数的消费，避免条件表达式后的代码块被误判为参数。

Lambda 应用特殊处理：当 expr 是 Lambda 且下一 token 为 ( 时，
不将 ( 消费为参数（否则 lambda 应用会被内层消费），
让外层 _parse_paren_dispatch 处理 lambda 应用。
另外，在 lambda 体内（_in_lambda_body=True），也不消费 ( 作为函数应用，
以便外层能识别 lambda 直接应用 (params) => body(args)。

重要：当 primary 是 Variable 且后续 token 为 KW_IF 时，
应将 primary 与 if 表达式组合（如 a if cond else b）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1199`

---

### `_is_primary_start`

```matha
def _is_primary_start(self) -> bool
```

**描述**: 当前 Token 是否可作为 primary 开头。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\parser.py:1364`

---

### `_parse_primary`

```matha
def _parse_primary(self)
```

**描述**: <primary> = <integer> | <float> | <string> | <bool> | <variable>
| <angle_expr> | <path_expr> | <set_construct> | <read_block>
| <output> | <set_up> | <lambda> | <code_block> | "(" , <expr> , ")"

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1375`

---

### `_parse_paren_dispatch`

```matha
def _parse_paren_dispatch(self)
```

**描述**: ( ... ) 消解：分组 / lambda / 元组（元组暂未实现）

lambda 形式：(params) => body，params 为逗号分隔的参数名（可带 : Type）。
通过「试探-回退」区分：先尝试解析参数列表，若紧随 => 则为 lambda，
否则回退为分组表达式。修复历史 TODO：此前独立 lambda 的 params 永远
为空，导致 lambda 无法作为一等值正确传递参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1451`

---

### `_parse_lambda_param`

```matha
def _parse_lambda_param(self)
```

**描述**: lambda 参数：name 或 name: Type（类型标注可选，仅消费不保留）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1596`

---

### `_parse_output`

```matha
def _parse_output(self) -> ast.Output
```

**描述**: <output> = [ , [ <expr> ] , ]

输出内容可为表达式或自然语言文本（如 [你好，世界]）。
当内容无法解析为完整表达式（含闭括号 ]）时，回退为文本字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.Output`

Source: `src\parser.py:1615`

---

### `_parse_list_literal`

```matha
def _parse_list_literal(self) -> ast.ListLiteral
```

**描述**: <list_literal> = [ <expr> , { , <expr> } ]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.ListLiteral`

Source: `src\parser.py:1640`

---

### `_parse_variable`

```matha
def _parse_variable(self) -> ast.Variable
```

**描述**: <variable> = <placeholder> | <identifier> | <symbol>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.Variable`

Source: `src\parser.py:1652`

---

### `_make_number_lit`

```matha
def _make_number_lit(self, value: str, is_float: bool)
```

**描述**: 从字面量字符串构造数字 AST（分离数值与 CJK 单位）。

支持进制前缀：0b(二进制) / 0t(三进制) / 0x(十六进制)。
进制字面量保留原始形式，此处按前缀转换为十进制 int。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `str` | |
| `is_float` | `bool` | |

Source: `src\parser.py:1667`

---

### `_parse_nl_block`

```matha
def _parse_nl_block(self)
```

**描述**: <nl_block> = 【*/文字/*】<natural_lang>

标注块后的自然语言正文（跳过换行，读取下一行整行作为意图描述）。
正文从原始源码切片，保留空格与标点（英文正文不丢空格）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1701`

---

### `_slice_from`

```matha
def _slice_from(self, start_tok: Token) -> str
```

**描述**: 从原始源码切片：start_tok 起始到行尾（当前 token 为 NEWLINE/EOF）。

用 source_lines[line][col:] 切片，保留 token 间的空格与标点，
避免 lexer 跳过空格导致英文正文丢空格。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `start_tok` | `Token` | |

**返回**: `str`

Source: `src\parser.py:1722`

---

### `_parse_type_expr`

```matha
def _parse_type_expr(self)
```

**描述**: <type_expr>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1739`

---

### `_parse_statement`

```matha
def _parse_statement(self)
```

**描述**: <statement> = let | while | if | for | match | go | expr_or_binding

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:1772`

---

### `_parse_let`

```matha
def _parse_let(self) -> ast.AST
```

**描述**: let [rec] <name> = <expr> [in <expr>]
or: let (name1, name2, ...) = <expr> [in <expr>]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.AST`

Source: `src\parser.py:1794`

---

### `_parse_let_tuple`

```matha
def _parse_let_tuple(self, is_rec: bool) -> ast.AST
```

**描述**: let (name1, name2, ...) = <expr> [in <expr>]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `is_rec` | `bool` | |

**返回**: `ast.AST`

Source: `src\parser.py:1819`

---

### `_parse_while`

```matha
def _parse_while(self) -> ast.WhileStmt
```

**描述**: while <expr> { <block> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.WhileStmt`

Source: `src\parser.py:1835`

---

### `_parse_if`

```matha
def _parse_if(self) -> ast.AST
```

**描述**: if <expr> { <block> } [ 否则 { <block> }]
or: if <expr> then <expr> else <expr>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.AST`

Source: `src\parser.py:1849`

---

### `_parse_if_then_else_expr`

```matha
def _parse_if_then_else_expr(self, cond) -> ast.IfExpr
```

**描述**: 解析 if cond then expr else expr 三元表达式形式。
then/else 分支支持分号分隔的表达式序列，取最后一个表达式的值。
注意：如果 else 分支后还有 else（如 if A then B else C else D），
则只取 then 分支，让外层三元继续。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cond` | `` | |

**返回**: `ast.IfExpr`

Source: `src\parser.py:1882`

---

### `_parse_if_then_else_expr_no_then`

```matha
def _parse_if_then_else_expr_no_then(self, cond) -> ast.IfExpr
```

**描述**: 解析 if cond else expr 三元表达式形式（省略 then）。
注意：如果 else 分支后还有 else（如 if A else B else C），
则回退让外层三元消费 else。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cond` | `` | |

**返回**: `ast.IfExpr`

Source: `src\parser.py:1907`

---

### `_parse_if_expr_impl`

```matha
def _parse_if_expr_impl(self) -> ast.IfExpr
```

**描述**: 内部实现：解析 if 表达式（支持 then/else 关键字形式和 {block} 语句形式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.IfExpr`

Source: `src\parser.py:1930`

---

### `_parse_if_then_else_expr`

```matha
def _parse_if_then_else_expr(self, cond) -> ast.IfExpr
```

**描述**: 解析 if cond then expr else expr 三元表达式形式。
then/else 分支支持分号分隔的表达式序列，取最后一个表达式的值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cond` | `` | |

**返回**: `ast.IfExpr`

Source: `src\parser.py:1967`

---

### `_parse_if_then_else_expr_no_then`

```matha
def _parse_if_then_else_expr_no_then(self, cond) -> ast.IfExpr
```

**描述**: 解析 if cond else expr 三元表达式形式（省略 then）。
注意：如果 else 分支后还有 else（如 if A else B else C），
则只取 then 分支为 cond，让外层三元继续。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cond` | `` | |

**返回**: `ast.IfExpr`

Source: `src\parser.py:1986`

---

### `_parse_semicolon_exprs`

```matha
def _parse_semicolon_exprs(self) -> Any
```

**描述**: 解析分号分隔的表达式序列，返回最后一个表达式的 AST。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\parser.py:2000`

---

### `_parse_if_expr`

```matha
def _parse_if_expr(self) -> ast.IfExpr
```

**描述**: if <expr> { <block> } [ 否则 { <block> } ]
or: if <expr> then <expr> else <expr>

在 lambda 体内 / match 分支内作为表达式使用。
返回 IfExpr，其 then/else_ 可能是 CodeBlock（语句形式）或表达式（三元形式）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.IfExpr`

Source: `src\parser.py:2010`

---

### `_parse_for`

```matha
def _parse_for(self) -> ast.ForStmt
```

**描述**: for <var> in <expr> { <block> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.ForStmt`

Source: `src\parser.py:2019`

---

### `_parse_match_stmt`

```matha
def _parse_match_stmt(self) -> ast.MatchStmt
```

**描述**: <match_stmt> = match <expr> { | <pattern> => <expr> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.MatchStmt`

Source: `src\parser.py:2035`

---

### `_parse_pattern`

```matha
def _parse_pattern(self)
```

**描述**: <pattern> = <literal> | <variable> | <constructor> | "_" | <belongs_pat>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:2063`

---

### `_parse_decl`

```matha
def _parse_decl(self)
```

**描述**: <decl> = <binding> | <func_def> | <type_def> | <statement>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:2087`

---

### `_parse_func_def`

```matha
def _parse_func_def(self) -> ast.FuncDef
```

**描述**: <func_def> = func <id> [annotation] ( <params> ) -> <type> = <lambda>

参数列表：(name: Type, name: Type, ...)，类型标注可选。
lambda 体：(params) => expr

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.FuncDef`

Source: `src\parser.py:2105`

---

### `_parse_typed_param`

```matha
def _parse_typed_param(self) -> tuple[str, Any]
```

**描述**: name: Type（类型标注可选）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `tuple[str, Any]`

Source: `src\parser.py:2175`

---

### `_parse_type_def`

```matha
def _parse_type_def(self)
```

**描述**: <type_def> = <struct_def> | <enum_def> | <alias_def>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:2196`

---

### `_parse_struct_def`

```matha
def _parse_struct_def(self) -> ast.StructDef
```

**描述**: <struct_def> = struct <id> [type_params] [annotation] [=] { <fields> }

= 可选（支持 struct Name { ... } 和 struct Name = { ... } 两种写法）。
fields = { <field_name> : <type_expr> [, | 换行] }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.StructDef`

Source: `src\parser.py:2207`

---

### `_parse_enum_def`

```matha
def _parse_enum_def(self) -> ast.EnumDef
```

**描述**: <enum_def> = enum <id> [type_params] [annotation] [=] { <ctors> }

= 可选。ctors = <id> { | <id> }（管道分隔的构造子）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.EnumDef`

Source: `src\parser.py:2235`

---

### `_parse_alias_def`

```matha
def _parse_alias_def(self) -> ast.AliasDef
```

**描述**: <alias_def> = type <id> [type_params] = <type_expr>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.AliasDef`

Source: `src\parser.py:2259`

---

### `_parse_optional_type_params`

```matha
def _parse_optional_type_params(self) -> list[str]
```

**描述**: <type_params> = [ <typevar> , { , <typevar> } ]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\parser.py:2268`

---

### `_skip_to_rbrace`

```matha
def _skip_to_rbrace(self) -> None
```

**描述**: 跳过到 } 为止（骨架占位）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\parser.py:2279`

---

### `_parse_module_decl`

```matha
def _parse_module_decl(self) -> ast.ModuleDecl
```

**描述**: <module_decl> = module <name> [annotation] [=] { { <decl> } }

= 可选（支持 module Name { ... } 和 module Name = { ... } 两种写法）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.ModuleDecl`

Source: `src\parser.py:2296`

---

### `_parse_import_decl`

```matha
def _parse_import_decl(self) -> ast.ImportDecl
```

**描述**: <import_decl> = use <name> [ { <import_list> } ] [ as <id> ]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ast.ImportDecl`

Source: `src\parser.py:2318`

---

### `_parse_module_name`

```matha
def _parse_module_name(self) -> str
```

**描述**: <module_name> = <identifier> , { . <identifier> }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\parser.py:2333`

---

### `_parse_command_unit`

```matha
def _parse_command_unit(self)
```

**描述**: <command_unit>

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\parser.py:2345`

---

## `parse`

```matha
def parse(source: str) -> ast.Program
```

**描述**: 解析 Matha 源码，返回 AST。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `ast.Program`

Source: `src\parser.py:2358`

---

## `parse_file`

```matha
def parse_file(path: str) -> ast.Program
```

**描述**: 从文件读取并解析。

| 参数 | 类型 | 说明 |
|------|------|------|
| `path` | `str` | |

**返回**: `ast.Program`

Source: `src\parser.py:2363`

---

## `_parse_worker`

```matha
def _parse_worker(source: str) -> ast.Program
```

**描述**: 工作线程执行的单条解析任务。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |

**返回**: `ast.Program`

Source: `src\parser.py:2392`

---

## `parse_batch`

```matha
def parse_batch(sources: list[str], max_workers: int) -> list[ast.Program]
```

**描述**: 批量并行解析 Matha 源码（v2.5 线程池版本）。

参数:
    sources: 待解析的源码列表
    max_workers: 并发线程数（默认 16，受 GIL 限制不建议超过 32）

返回:
    与 sources 顺序对应的 AST 列表（失败的返回空 Program）

| 参数 | 类型 | 说明 |
|------|------|------|
| `sources` | `list[str]` | |
| `max_workers` | `int` | |

**返回**: `list[ast.Program]`

Source: `src\parser.py:2397`

---

## `_get_or_create_pool`

```matha
def _get_or_create_pool(max_workers: int) -> ThreadPoolExecutor
```

**描述**: 获取或创建全局线程池（线程安全）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `max_workers` | `int` | |

**返回**: `ThreadPoolExecutor`

Source: `src\parser.py:2428`

---

## `shutdown_parsers`

```matha
def shutdown_parsers(wait: bool) -> None
```

**描述**: 关闭解析器线程池。

| 参数 | 类型 | 说明 |
|------|------|------|
| `wait` | `bool` | |

**返回**: `None`

Source: `src\parser.py:2440`

---

# `parser_pool`

```matha

```

**描述**: v2.4 ProcessPoolExecutor 解析器重构草案

设计目标：
  1. 绕过 CPython GIL，实现真正的并行意图解析
  2. 保持 v2.3 的 Result 错误传播和结构化异常
  3. 支持批量解析和流式解析两种模式
  4. 可配置工作进程数，适应不同硬件

架构：
  ProcessPoolExecutor
      ├── 主进程：接收用户输入，分发任务
      ├── 工作进程 N：每个进程持有独立的 EnhancedIntentParser 实例
      └── 结果队列：序列化 Result 对象返回主进程

设计约束：
  - MathaError / Result 必须可序列化（pickle），否则跨进程传递失败
  - 恢复策略注册必须在每个工作进程初始化时完成
  - 解析器实例状态（Intent 对象）不能跨进程共享

Source: `src\parser_pool.py:1`

---

## `_worker_init`

```matha
def _worker_init() -> None
```

**描述**: 每个工作进程启动时调用，完成一次性初始化。

关键：恢复策略和标准库必须在工作进程中重新注册，
因为子进程是全新的 Python 解释器实例。

**返回**: `None`

Source: `src\parser_pool.py:45`

---

## `SerializableResult`

```matha
class SerializableResult
```

**描述**: 可 pickle 序列化的 Result 包装。

由于 Result[Ok/Err] 包含闭包/函数引用，无法直接 pickle。
此包装提取可序列化的字段，在接收端重建 Result。

**装饰器**: dataclass

Source: `src\parser_pool.py:71`

---

### `to_result`

```matha
def to_result(self) -> Result
```

**描述**: 重建 Result 对象。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Result`

Source: `src\parser_pool.py:85`

---

### `from_result`

```matha
def from_result(result: Result) -> 'SerializableResult'
```

**描述**: 从 Result 创建可序列化包装。

| 参数 | 类型 | 说明 |
|------|------|------|
| `result` | `Result` | |

**返回**: `'SerializableResult'`

**装饰器**: staticmethod

Source: `src\parser_pool.py:103`

---

## `_worker_parse`

```matha
def _worker_parse(text: str, target_lang: str) -> SerializableResult
```

**描述**: 工作进程执行的单条解析任务。

每个工作进程持有独立的 EnhancedIntentParser 实例，
避免跨进程共享状态。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `SerializableResult`

Source: `src\parser_pool.py:122`

---

## `_worker_execute`

```matha
def _worker_execute(text: str, target_lang: str) -> SerializableResult
```

**描述**: 工作进程执行的解析 + 执行任务。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `SerializableResult`

Source: `src\parser_pool.py:135`

---

## `ProcessPoolIntentParser`

```matha
class ProcessPoolIntentParser
```

**描述**: 基于 ProcessPoolExecutor 的并行意图解析器。

使用方式：
    parser = ProcessPoolIntentParser(max_workers=8)
    # 批量解析
    results = parser.parse_batch(["计算 3 加 5", "反转字符串 abc"])
    # 单条解析
    result = parser.parse("计算 100 以内素数")
    # 解析 + 执行
    result = parser.execute("对数组 [3,1,2] 排序")

Source: `src\parser_pool.py:153`

---

### `__init__`

```matha
def __init__(self, max_workers: int, init_timeout: float)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_workers` | `int` | |
| `init_timeout` | `float` | |

Source: `src\parser_pool.py:166`

---

### `_get_executor`

```matha
def _get_executor(self) -> ProcessPoolExecutor
```

**描述**: 懒初始化进程池（线程安全）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `ProcessPoolExecutor`

Source: `src\parser_pool.py:172`

---

### `parse`

```matha
def parse(self, text: str, target_lang: str) -> Result
```

**描述**: 单条解析（异步提交，同步等待）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Result`

Source: `src\parser_pool.py:182`

---

### `parse_batch`

```matha
def parse_batch(self, texts: list[str], target_lang: str) -> list[Result]
```

**描述**: 批量解析（并发提交，按输入顺序返回）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `texts` | `list[str]` | |
| `target_lang` | `str` | |

**返回**: `list[Result]`

Source: `src\parser_pool.py:193`

---

### `_reorder_results`

```matha
def _reorder_results(self, futures: list, texts: list[str], target_lang: str) -> list[Result]
```

**描述**: 按输入顺序重新排列结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `futures` | `list` | |
| `texts` | `list[str]` | |
| `target_lang` | `str` | |

**返回**: `list[Result]`

Source: `src\parser_pool.py:208`

---

### `execute`

```matha
def execute(self, text: str, target_lang: str) -> Result
```

**描述**: 解析 + 执行（异步提交，同步等待）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Result`

Source: `src\parser_pool.py:225`

---

### `execute_batch`

```matha
def execute_batch(self, texts: list[str], target_lang: str) -> list[Result]
```

**描述**: 批量解析 + 执行。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `texts` | `list[str]` | |
| `target_lang` | `str` | |

**返回**: `list[Result]`

Source: `src\parser_pool.py:236`

---

### `shutdown`

```matha
def shutdown(self, wait: bool) -> None
```

**描述**: 关闭进程池。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `wait` | `bool` | |

**返回**: `None`

Source: `src\parser_pool.py:244`

---

## `HybridIntentParser`

```matha
class HybridIntentParser
```

**描述**: 混合解析器：根据操作类型选择线程池或进程池。

策略：
  - 分类（正则匹配）→ 线程池（GIL 下 regex 释放 GIL）
  - 参数提取（字符串操作）→ 线程池
  - 代码执行（exec）→ 进程池（CPU 密集，绕过 GIL）

Source: `src\parser_pool.py:256`

---

### `__init__`

```matha
def __init__(self, thread_workers: int, process_workers: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `thread_workers` | `int` | |
| `process_workers` | `int` | |

Source: `src\parser_pool.py:265`

---

### `parse`

```matha
def parse(self, text: str, target_lang: str) -> Result
```

**描述**: 解析：线程池执行分类 + 参数提取。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Result`

Source: `src\parser_pool.py:273`

---

### `execute`

```matha
def execute(self, text: str, target_lang: str) -> Result
```

**描述**: 执行：进程池执行代码（绕过 GIL）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `target_lang` | `str` | |

**返回**: `Result`

Source: `src\parser_pool.py:283`

---

### `shutdown`

```matha
def shutdown(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\parser_pool.py:293`

---

## `benchmark_pools`

```matha
def benchmark_pools()
```

**描述**: 对比单进程、线程池、进程池的性能。

Source: `src\parser_pool.py:302`

---

# `perf_opt`

```matha

```

**描述**: Matha v4.0 性能优化层：热点追踪 + 局部变量 + 内存池 + PyPy JIT 适配。

优化策略：
  1. 热点函数追踪：首次执行后编译为本地 Python 函数
  2. 局部变量优化：热点 env 用 list 替代 dict
  3. 对象池：数值对象预分配
  4. 算术内联：简单表达式跳过 AST 遍历
  5. PyPy JIT 适配：__class_getitem__ 兼容

Source: `src\perf_opt.py:1`

---

## `HotFunctionTracker`

```matha
class HotFunctionTracker
```

**描述**: 追踪热点函数，自动编译为本地 Python 函数。

Source: `src\perf_opt.py:26`

---

### `__init__`

```matha
def __init__(self, threshold: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `threshold` | `int` | |

**返回**: `None`

Source: `src\perf_opt.py:29`

---

### `record`

```matha
def record(self, name: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `None`

Source: `src\perf_opt.py:34`

---

### `should_compile`

```matha
def should_compile(self, name: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\perf_opt.py:37`

---

### `compile_and_cache`

```matha
def compile_and_cache(self, name: str, func_def, env_builder: Callable) -> None
```

**描述**: 将 Matha 函数编译为本地 Python 函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func_def` | `` | |
| `env_builder` | `Callable` | |

**返回**: `None`

Source: `src\perf_opt.py:40`

---

### `get_compiled`

```matha
def get_compiled(self, name: str) -> Optional[Callable]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Callable]`

Source: `src\perf_opt.py:48`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\perf_opt.py:52`

---

## `LocalVariableEnv`

```matha
class LocalVariableEnv
```

**描述**: 使用 list 代替 dict 的局部变量环境（加速热点路径）。

Source: `src\perf_opt.py:67`

---

### `__init__`

```matha
def __init__(self, capacity: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `capacity` | `int` | |

**返回**: `None`

Source: `src\perf_opt.py:70`

---

### `_ensure_slot`

```matha
def _ensure_slot(self, name: str) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `int`

Source: `src\perf_opt.py:75`

---

### `set`

```matha
def set(self, name: str, value: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `value` | `Any` | |

**返回**: `None`

Source: `src\perf_opt.py:85`

---

### `get`

```matha
def get(self, name: str) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Any`

Source: `src\perf_opt.py:89`

---

### `contains`

```matha
def contains(self, name: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\perf_opt.py:95`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\perf_opt.py:98`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\perf_opt.py:101`

---

## `NumericPool`

```matha
class NumericPool
```

**描述**: 数值对象池：预分配常用整数值（-128~256）避免重复创建。

Source: `src\perf_opt.py:110`

---

### `__init__`

```matha
def __init__(self, low: int, high: int) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `low` | `int` | |
| `high` | `int` | |

**返回**: `None`

Source: `src\perf_opt.py:113`

---

### `get`

```matha
def get(self, value: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `int` | |

**返回**: `int`

Source: `src\perf_opt.py:118`

---

### `size`

```matha
def size(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\perf_opt.py:124`

---

## `ArithmeticInlineOptimizer`

```matha
class ArithmeticInlineOptimizer
```

**描述**: 对简单算术表达式进行内联优化。

Source: `src\perf_opt.py:132`

---

### `try_inline`

```matha
def try_inline(expr_str: str) -> Optional[float]
```

**描述**: 尝试直接计算表达式字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr_str` | `str` | |

**返回**: `Optional[float]`

**装饰器**: staticmethod

Source: `src\perf_opt.py:143`

---

### `optimize_addition`

```matha
def optimize_addition(a: Any, b: Any) -> Any
```

**描述**: 优化的加法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Any` | |
| `b` | `Any` | |

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\perf_opt.py:155`

---

### `optimize_multiplication`

```matha
def optimize_multiplication(a: Any, b: Any) -> Any
```

**描述**: 优化的乘法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Any` | |
| `b` | `Any` | |

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\perf_opt.py:162`

---

## `PyPyJITAdapter`

```matha
class PyPyJITAdapter
```

**描述**: 为 PyPy JIT 优化解释器入口。

Source: `src\perf_opt.py:173`

---

### `is_pypy`

```matha
def is_pypy() -> bool
```

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\perf_opt.py:177`

---

### `optimize_run`

```matha
def optimize_run(func: Callable) -> Callable
```

**描述**: 装饰器：标记热点函数供 PyPy JIT 优化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `Callable`

**装饰器**: staticmethod

Source: `src\perf_opt.py:181`

---

### `get_stats`

```matha
def get_stats() -> dict
```

**返回**: `dict`

**装饰器**: staticmethod

Source: `src\perf_opt.py:189`

---

## `PerformanceProfiler`

```matha
class PerformanceProfiler
```

**描述**: 解释器性能分析器。

Source: `src\perf_opt.py:201`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\perf_opt.py:204`

---

### `start`

```matha
def start(self, tag: str) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tag` | `str` | |

**返回**: `float`

Source: `src\perf_opt.py:208`

---

### `stop`

```matha
def stop(self, tag: str, start: float) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tag` | `str` | |
| `start` | `float` | |

**返回**: `float`

Source: `src\perf_opt.py:211`

---

### `report`

```matha
def report(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\perf_opt.py:219`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\perf_opt.py:228`

---

## `PersistentCache`

```matha
class PersistentCache
```

**描述**: 跨解释器实例的编译缓存。

Source: `src\perf_opt.py:237`

---

### `__init__`

```matha
def __init__(self, cache_dir: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cache_dir` | `str` | |

**返回**: `None`

Source: `src\perf_opt.py:240`

---

### `_key_path`

```matha
def _key_path(self, key: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `str`

Source: `src\perf_opt.py:247`

---

### `get`

```matha
def get(self, key: str) -> Optional[dict]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `Optional[dict]`

Source: `src\perf_opt.py:251`

---

### `put`

```matha
def put(self, key: str, value: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |
| `value` | `dict` | |

**返回**: `None`

Source: `src\perf_opt.py:264`

---

### `invalidate`

```matha
def invalidate(self, key: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `key` | `str` | |

**返回**: `None`

Source: `src\perf_opt.py:270`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\perf_opt.py:277`

---

### `stats`

```matha
def stats(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

**装饰器**: property

Source: `src\perf_opt.py:285`

---

# `performance_benchmark`

```matha

```

**描述**: Matha 性能基准对比 v2.0
========================
对比 Matha 与 C++/Rust/Go/Java 在相同算法上的性能表现。

测试场景：
  1. 矩阵运算（SVD/求逆/乘法）
  2. 排序算法（快速排序/归并排序）
  3. 数值计算（PI 计算/傅里叶变换）
  4. 字符串处理（搜索/匹配）
  5. 并发性能（线程/进程并行）

报告生成：
  - 基准测试数据
  - 加速比分析
  - 性能瓶颈识别

Source: `src\performance_benchmark.py:1`

---

## `BenchmarkResult`

```matha
class BenchmarkResult
```

**描述**: 单次基准测试结果。

**装饰器**: dataclass

Source: `src\performance_benchmark.py:38`

---

## `PerformanceReport`

```matha
class PerformanceReport
```

**描述**: 性能报告。

**装饰器**: dataclass

Source: `src\performance_benchmark.py:51`

---

### `add_result`

```matha
def add_result(self, result: BenchmarkResult) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `result` | `BenchmarkResult` | |

**返回**: `None`

Source: `src\performance_benchmark.py:56`

---

### `compute_summary`

```matha
def compute_summary(self) -> Dict[str, Dict[str, float]]
```

**描述**: 计算各语言性能汇总。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Dict[str, float]]`

Source: `src\performance_benchmark.py:59`

---

### `generate_markdown`

```matha
def generate_markdown(self) -> str
```

**描述**: 生成 Markdown 报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\performance_benchmark.py:73`

---

## `BenchmarkSuite`

```matha
class BenchmarkSuite
```

**描述**: 基准测试套件。

Source: `src\performance_benchmark.py:112`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\performance_benchmark.py:115`

---

### `run_benchmark`

```matha
def run_benchmark(self, name: str, func, args: tuple, iterations: int, language: str) -> BenchmarkResult
```

**描述**: 运行单次基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `` | |
| `args` | `tuple` | |
| `iterations` | `int` | |
| `language` | `str` | |

**返回**: `BenchmarkResult`

Source: `src\performance_benchmark.py:118`

---

### `add_result`

```matha
def add_result(self, result: BenchmarkResult) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `result` | `BenchmarkResult` | |

**返回**: `None`

Source: `src\performance_benchmark.py:152`

---

### `generate_report`

```matha
def generate_report(self) -> PerformanceReport
```

**描述**: 生成性能报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `PerformanceReport`

Source: `src\performance_benchmark.py:155`

---

## `MultiLangBenchmark`

```matha
class MultiLangBenchmark
```

**描述**: 多语言性能基准对比。

对比 Matha(Python) vs C++ vs Rust vs Go vs Java

Source: `src\performance_benchmark.py:166`

---

### `__init__`

```matha
def __init__(self, work_dir: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `work_dir` | `str` | |

Source: `src\performance_benchmark.py:173`

---

### `benchmark_matrix_svd`

```matha
def benchmark_matrix_svd(self, size: int, iterations: int) -> None
```

**描述**: 矩阵 SVD 基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `size` | `int` | |
| `iterations` | `int` | |

**返回**: `None`

Source: `src\performance_benchmark.py:177`

---

### `benchmark_sort`

```matha
def benchmark_sort(self, n: int, iterations: int) -> None
```

**描述**: 排序基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |
| `iterations` | `int` | |

**返回**: `None`

Source: `src\performance_benchmark.py:212`

---

### `benchmark_parallel_compute`

```matha
def benchmark_parallel_compute(self, n: int, iterations: int) -> None
```

**描述**: 并行计算基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |
| `iterations` | `int` | |

**返回**: `None`

Source: `src\performance_benchmark.py:228`

---

### `_compile_cpp`

```matha
def _compile_cpp(self, code: str, name: str) -> Optional[str]
```

**描述**: 编译 C++ 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |
| `name` | `str` | |

**返回**: `Optional[str]`

Source: `src\performance_benchmark.py:255`

---

### `run_all`

```matha
def run_all(self) -> PerformanceReport
```

**描述**: 运行所有基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `PerformanceReport`

Source: `src\performance_benchmark.py:270`

---

### `generate_report`

```matha
def generate_report(self, output_file: str) -> str
```

**描述**: 生成并保存报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `output_file` | `str` | |

**返回**: `str`

Source: `src\performance_benchmark.py:283`

---

# `pkg_manager`

```matha

```

**描述**: Matha 包管理器 — matha-pkg

提供：
1. 依赖解析（类似 pip/requirements.txt）
2. 版本控制（语义化版本 semver）
3. 包仓库（本地/远程）
4. 环境隔离（虚拟环境）

用法：
  matha-pkg install matha-stdlib
  matha-pkg install matha-stdlib==1.2.3
  matha-pkg install --dev matha-test-utils
  matha-pkg update
  matha-pkg list
  matha-pkg search prime

Source: `src\pkg_manager.py:1`

---

## `Version`

```matha
class Version
```

**描述**: 语义化版本：Major.Minor.Patch

Source: `src\pkg_manager.py:34`

---

### `__init__`

```matha
def __init__(self, major: int, minor: int, patch: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `major` | `int` | |
| `minor` | `int` | |
| `patch` | `int` | |

Source: `src\pkg_manager.py:37`

---

### `parse`

```matha
def parse(cls, version_str: str) -> 'Version'
```

**描述**: 解析版本字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `version_str` | `str` | |

**返回**: `'Version'`

**装饰器**: classmethod

Source: `src\pkg_manager.py:43`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\pkg_manager.py:58`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\pkg_manager.py:61`

---

### `__eq__`

```matha
def __eq__(self, other) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

**返回**: `bool`

Source: `src\pkg_manager.py:64`

---

### `__lt__`

```matha
def __lt__(self, other) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

**返回**: `bool`

Source: `src\pkg_manager.py:69`

---

### `__le__`

```matha
def __le__(self, other) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

**返回**: `bool`

Source: `src\pkg_manager.py:74`

---

### `__gt__`

```matha
def __gt__(self, other) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

**返回**: `bool`

Source: `src\pkg_manager.py:77`

---

### `__ge__`

```matha
def __ge__(self, other) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

**返回**: `bool`

Source: `src\pkg_manager.py:80`

---

### `__hash__`

```matha
def __hash__(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\pkg_manager.py:83`

---

## `PackageMeta`

```matha
class PackageMeta
```

**描述**: 包元数据。

**装饰器**: dataclass

Source: `src\pkg_manager.py:92`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\pkg_manager.py:105`

---

### `from_dict`

```matha
def from_dict(cls, data: dict) -> 'PackageMeta'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `data` | `dict` | |

**返回**: `'PackageMeta'`

**装饰器**: classmethod

Source: `src\pkg_manager.py:120`

---

## `DependencyResolver`

```matha
class DependencyResolver
```

**描述**: 依赖解析器（带版本约束、缓存和冲突解决）。

Source: `src\pkg_manager.py:140`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\pkg_manager.py:153`

---

### `check_constraint`

```matha
def check_constraint(self, version: Version, constraint: str) -> bool
```

**描述**: 检查版本是否满足约束（带缓存）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `version` | `Version` | |
| `constraint` | `str` | |

**返回**: `bool`

Source: `src\pkg_manager.py:157`

---

### `check_constraint`

```matha
def check_constraint(self, version: Version, constraint: str) -> bool
```

**描述**: 检查版本是否满足约束。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `version` | `Version` | |
| `constraint` | `str` | |

**返回**: `bool`

Source: `src\pkg_manager.py:169`

---

### `_check_single`

```matha
def _check_single(self, version: Version, constraint: str) -> bool
```

**描述**: 检查单个约束。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `version` | `Version` | |
| `constraint` | `str` | |

**返回**: `bool`

Source: `src\pkg_manager.py:175`

---

### `_tilde_match`

```matha
def _tilde_match(version: Version, spec: str) -> bool
```

**描述**: ~1.2 匹配 >=1.2.0, <1.3.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `version` | `Version` | |
| `spec` | `str` | |

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\pkg_manager.py:198`

---

### `_caret_match`

```matha
def _caret_match(version: Version, spec: str) -> bool
```

**描述**: ^1.2.3 匹配 >=1.2.3, <2.0.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `version` | `Version` | |
| `spec` | `str` | |

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\pkg_manager.py:204`

---

### `resolve`

```matha
def resolve(self, package: PackageMeta, registry: Dict[str, PackageMeta]) -> List[str]
```

**描述**: 解析依赖树（带缓存和冲突解决）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `package` | `PackageMeta` | |
| `registry` | `Dict[str, PackageMeta]` | |

**返回**: `List[str]`

Source: `src\pkg_manager.py:209`

---

### `_resolve_conflict`

```matha
def _resolve_conflict(self, pkg_name: str, constraint: str, registry: Dict[str, PackageMeta]) -> Optional[PackageMeta]
```

**描述**: 尝试解决版本冲突。

策略：
1. 查找满足所有约束的最新版本
2. 如果找不到，返回 None（依赖调用者处理）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pkg_name` | `str` | |
| `constraint` | `str` | |
| `registry` | `Dict[str, PackageMeta]` | |

**返回**: `Optional[PackageMeta]`

Source: `src\pkg_manager.py:271`

---

### `clear_cache`

```matha
def clear_cache(self)
```

**描述**: 清空缓存。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\pkg_manager.py:286`

---

## `PackageNotFoundError`

```matha
class PackageNotFoundError(Exception)
```

Source: `src\pkg_manager.py:296`

---

## `VersionConflictError`

```matha
class VersionConflictError(Exception)
```

Source: `src\pkg_manager.py:299`

---

## `MathaPackage`

```matha
class MathaPackage
```

**描述**: Matha 包管理器。

Source: `src\pkg_manager.py:302`

---

### `__init__`

```matha
def __init__(self, root_dir: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `root_dir` | `str` | |

Source: `src\pkg_manager.py:305`

---

### `_load_registry`

```matha
def _load_registry(self)
```

**描述**: 加载包注册表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\pkg_manager.py:314`

---

### `_scan_stdlib`

```matha
def _scan_stdlib(self, stdlib_dir: Path)
```

**描述**: 扫描标准库目录。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stdlib_dir` | `Path` | |

Source: `src\pkg_manager.py:335`

---

### `_scan_core_modules`

```matha
def _scan_core_modules(self, src_dir: Path)
```

**描述**: 扫描核心模块。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `src_dir` | `Path` | |

Source: `src\pkg_manager.py:349`

---

### `install`

```matha
def install(self, spec: str, dev: bool) -> List[str]
```

**描述**: 安装包。

Args:
    spec: 包规格，格式: name[==version]
    dev: 是否安装为开发依赖

Returns:
    解析后的依赖列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `str` | |
| `dev` | `bool` | |

**返回**: `List[str]`

Source: `src\pkg_manager.py:367`

---

### `list_packages`

```matha
def list_packages(self) -> List[Tuple[str, Version]]
```

**描述**: 列出已安装的包。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Tuple[str, Version]]`

Source: `src\pkg_manager.py:415`

---

### `search`

```matha
def search(self, query: str) -> List[PackageMeta]
```

**描述**: 搜索包。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `query` | `str` | |

**返回**: `List[PackageMeta]`

Source: `src\pkg_manager.py:419`

---

### `show`

```matha
def show(self, name: str)
```

**描述**: 显示包信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

Source: `src\pkg_manager.py:430`

---

# `pkg_manager_v2`

```matha

```

**描述**: Matha 包管理器增强版 — v2.0

完善功能：
  1. Lockfile 支持（锁定精确版本）
  2. 远程包安装（HTTP/HTTPS）
  3. 包发布（pack + publish）
  4. 依赖树可视化
  5. 环境隔离（虚拟环境管理）
  6. 包搜索（远程仓库）

用法：
  from src.pkg_manager_v2 import MathaPackageManager
  mgr = MathaPackageManager()
  mgr.install("matha-stdlib")
  mgr.lock()           # 生成 lockfile
  mgr.pack("my-pkg")   # 打包
  mgr.publish("my-pkg") # 发布

Source: `src\pkg_manager_v2.py:1`

---

## `LockEntry`

```matha
class LockEntry
```

**描述**: Lockfile 条目。

**装饰器**: dataclass

Source: `src\pkg_manager_v2.py:46`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\pkg_manager_v2.py:54`

---

### `from_dict`

```matha
def from_dict(cls, data: Dict) -> 'LockEntry'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `data` | `Dict` | |

**返回**: `'LockEntry'`

**装饰器**: classmethod

Source: `src\pkg_manager_v2.py:64`

---

## `Lockfile`

```matha
class Lockfile
```

**描述**: Lockfile 管理器。

Source: `src\pkg_manager_v2.py:74`

---

### `__init__`

```matha
def __init__(self, lockfile_path: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lockfile_path` | `str` | |

Source: `src\pkg_manager_v2.py:77`

---

### `load`

```matha
def load(self) -> None
```

**描述**: 加载 lockfile。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\pkg_manager_v2.py:81`

---

### `save`

```matha
def save(self) -> None
```

**描述**: 保存 lockfile。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\pkg_manager_v2.py:90`

---

### `add`

```matha
def add(self, entry: LockEntry) -> None
```

**描述**: 添加锁条目。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `entry` | `LockEntry` | |

**返回**: `None`

Source: `src\pkg_manager_v2.py:100`

---

### `get`

```matha
def get(self, name: str) -> Optional[LockEntry]
```

**描述**: 获取锁条目。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[LockEntry]`

Source: `src\pkg_manager_v2.py:104`

---

### `remove`

```matha
def remove(self, name: str) -> None
```

**描述**: 移除锁条目。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `None`

Source: `src\pkg_manager_v2.py:108`

---

### `update`

```matha
def update(self, name: str, version: Version) -> bool
```

**描述**: 更新版本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `version` | `Version` | |

**返回**: `bool`

Source: `src\pkg_manager_v2.py:112`

---

### `packages`

```matha
def packages(self) -> Dict[str, LockEntry]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, LockEntry]`

**装饰器**: property

Source: `src\pkg_manager_v2.py:120`

---

## `RemotePackageClient`

```matha
class RemotePackageClient
```

**描述**: 远程包仓库客户端。

Source: `src\pkg_manager_v2.py:128`

---

### `__init__`

```matha
def __init__(self, base_url: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `base_url` | `str` | |

Source: `src\pkg_manager_v2.py:131`

---

### `search`

```matha
def search(self, query: str) -> List[Dict]
```

**描述**: 搜索远程包。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `query` | `str` | |

**返回**: `List[Dict]`

Source: `src\pkg_manager_v2.py:134`

---

### `get_metadata`

```matha
def get_metadata(self, name: str, version: Optional[str]) -> Optional[Dict]
```

**描述**: 获取包元数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `version` | `Optional[str]` | |

**返回**: `Optional[Dict]`

Source: `src\pkg_manager_v2.py:144`

---

### `download`

```matha
def download(self, name: str, version: str, dest_dir: Path) -> Path
```

**描述**: 下载并解压包。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `version` | `str` | |
| `dest_dir` | `Path` | |

**返回**: `Path`

Source: `src\pkg_manager_v2.py:157`

---

## `PackageDownloadError`

```matha
class PackageDownloadError(Exception)
```

Source: `src\pkg_manager_v2.py:175`

---

## `MathaPackageManager`

```matha
class MathaPackageManager(MathaPackage)
```

**描述**: 增强版 Matha 包管理器。

新增功能：
  - Lockfile 管理
  - 远程包安装/发布
  - 依赖树可视化
  - 环境隔离

Source: `src\pkg_manager_v2.py:183`

---

### `__init__`

```matha
def __init__(self, root_dir: str, remote_url: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `root_dir` | `str` | |
| `remote_url` | `str` | |

Source: `src\pkg_manager_v2.py:194`

---

### `lock`

```matha
def lock(self) -> str
```

**描述**: 生成/更新 lockfile。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\pkg_manager_v2.py:206`

---

### `_compute_checksum`

```matha
def _compute_checksum(self, pkg: PackageMeta) -> str
```

**描述**: 计算包的校验和。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pkg` | `PackageMeta` | |

**返回**: `str`

Source: `src\pkg_manager_v2.py:225`

---

### `check_lockfile`

```matha
def check_lockfile(self) -> Dict
```

**描述**: 检查 lockfile 一致性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\pkg_manager_v2.py:230`

---

### `install_remote`

```matha
def install_remote(self, spec: str) -> List[str]
```

**描述**: 从远程仓库安装包。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `str` | |

**返回**: `List[str]`

Source: `src\pkg_manager_v2.py:249`

---

### `search_remote`

```matha
def search_remote(self, query: str) -> List[Dict]
```

**描述**: 在远程仓库搜索包。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `query` | `str` | |

**返回**: `List[Dict]`

Source: `src\pkg_manager_v2.py:290`

---

### `pack`

```matha
def pack(self, pkg_name: str, version: Optional[str]) -> str
```

**描述**: 打包发布包。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pkg_name` | `str` | |
| `version` | `Optional[str]` | |

**返回**: `str`

Source: `src\pkg_manager_v2.py:300`

---

### `publish`

```matha
def publish(self, pkg_name: str, version: Optional[str]) -> bool
```

**描述**: 发布包到远程仓库。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pkg_name` | `str` | |
| `version` | `Optional[str]` | |

**返回**: `bool`

Source: `src\pkg_manager_v2.py:341`

---

### `create_env`

```matha
def create_env(self, env_name: str) -> Path
```

**描述**: 创建隔离环境。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `env_name` | `str` | |

**返回**: `Path`

Source: `src\pkg_manager_v2.py:355`

---

### `list_envs`

```matha
def list_envs(self) -> List[Dict]
```

**描述**: 列出所有环境。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Dict]`

Source: `src\pkg_manager_v2.py:373`

---

### `show_tree`

```matha
def show_tree(self, pkg_name: str, depth: int) -> str
```

**描述**: 显示依赖树。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pkg_name` | `str` | |
| `depth` | `int` | |

**返回**: `str`

Source: `src\pkg_manager_v2.py:386`

---

### `install`

```matha
def install(self, spec: str, dev: bool) -> List[str]
```

**描述**: 安装（集成 lockfile）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `str` | |
| `dev` | `bool` | |

**返回**: `List[str]`

Source: `src\pkg_manager_v2.py:417`

---

### `list_packages`

```matha
def list_packages(self) -> List[Tuple[str, Version]]
```

**描述**: 列出（显示 lockfile 状态）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Tuple[str, Version]]`

Source: `src\pkg_manager_v2.py:438`

---

# `profiler`

```matha

```

**描述**: Matha 性能 Profiler — 火焰图 + 调用统计

解决 Matha 性能瓶颈可观测性不足的问题。

功能：
  1. 函数级调用计时
  2. 调用次数统计
  3. 火焰图数据生成（Chrome/Edge 格式）
  4. 性能报告生成（Markdown/JSON）
  5. 与 JIT/Memoization 集成

用法：
  from src.profiler import MathaProfiler, profile

  # 方式1: 装饰器
  @profile
  def fib(n):
      if n <= 1: return n
      return fib(n-1) + fib(n-2)

  # 方式2: 上下文管理器
  with MathaProfiler() as prof:
      result = fib(30)

  # 查看报告
  print(prof.report())
  prof.save_flamegraph("fib_flamegraph.html")

Source: `src\profiler.py:1`

---

## `CallStats`

```matha
class CallStats
```

**描述**: 单次函数调用的统计。

**装饰器**: dataclass

Source: `src\profiler.py:50`

---

### `to_dict`

```matha
def to_dict(self) -> Dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict`

Source: `src\profiler.py:61`

---

## `MathaProfiler`

```matha
class MathaProfiler
```

**描述**: Matha 性能分析器。

提供：
  1. cProfile 集成（底层）
  2. 调用树统计
  3. 火焰图生成
  4. Markdown/JSON 报告

Source: `src\profiler.py:78`

---

### `__init__`

```matha
def __init__(self, sort_by: str, top_n: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sort_by` | `str` | |
| `top_n` | `int` | |

Source: `src\profiler.py:89`

---

### `__enter__`

```matha
def __enter__(self) -> 'MathaProfiler'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'MathaProfiler'`

Source: `src\profiler.py:98`

---

### `__exit__`

```matha
def __exit__(self, *args) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\profiler.py:102`

---

### `start`

```matha
def start(self) -> None
```

**描述**: 启动 profiling。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\profiler.py:105`

---

### `stop`

```matha
def stop(self) -> None
```

**描述**: 停止 profiling。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\profiler.py:112`

---

### `_parse_stats`

```matha
def _parse_stats(self) -> None
```

**描述**: 解析 cProfile 统计结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\profiler.py:120`

---

### `report`

```matha
def report(self, format: str) -> str
```

**描述**: 生成性能报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `format` | `str` | |

**返回**: `str`

Source: `src\profiler.py:142`

---

### `_report_markdown`

```matha
def _report_markdown(self, total_time: float) -> str
```

**描述**: 生成 Markdown 格式报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `total_time` | `float` | |

**返回**: `str`

Source: `src\profiler.py:156`

---

### `_report_json`

```matha
def _report_json(self, total_time: float) -> str
```

**描述**: 生成 JSON 格式报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `total_time` | `float` | |

**返回**: `str`

Source: `src\profiler.py:195`

---

### `save_flamegraph`

```matha
def save_flamegraph(self, filepath: str) -> str
```

**描述**: 生成 Chrome/Edge 兼容的火焰图 HTML 文件。

Args:
    filepath: 输出文件路径

Returns:
    生成的文件路径

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `filepath` | `str` | |

**返回**: `str`

Source: `src\profiler.py:211`

---

### `get_call_tree`

```matha
def get_call_tree(self) -> Dict[str, CallStats]
```

**描述**: 获取调用树。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, CallStats]`

Source: `src\profiler.py:327`

---

### `reset`

```matha
def reset(self) -> None
```

**描述**: 重置 profiler。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\profiler.py:331`

---

## `profile`

```matha
def profile(func: Optional[Callable]) -> Callable
```

**描述**: 函数级 Profiler 装饰器。

用法:
    @profile
    def fib(n):
        if n <= 1: return n
        return fib(n-1) + fib(n-2)

    result = fib(30)
    print(fib.__profile_report__)  # 查看报告

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Optional[Callable]` | |

**返回**: `Callable`

Source: `src\profiler.py:346`

---

## `get_profile_report`

```matha
def get_profile_report(func_name: str) -> str
```

**描述**: 获取指定函数的性能报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `func_name` | `str` | |

**返回**: `str`

Source: `src\profiler.py:379`

---

## `all_reports`

```matha
def all_reports() -> Dict[str, str]
```

**描述**: 获取所有已 profiled 函数的报告。

**返回**: `Dict[str, str]`

Source: `src\profiler.py:389`

---

## `fib`

```matha
def fib(n: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `int`

**装饰器**: profile(name='fibonacci')

Source: `src\profiler.py:409`

---

## `fib_raw`

```matha
def fib_raw(n: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `int`

Source: `src\profiler.py:429`

---

# `recovery_strategy_cow`

```matha

```

**描述**: v2.5 Copy-on-Write 策略缓存 — 无锁读操作

设计目标：
  消除 RecoveryStrategy.try_recover() 中的读锁竞争。

核心思想：
  - 策略注册（写操作）：创建新快照 → 原子替换引用（锁保护）
  - 策略读取（读操作）：直接读取快照引用（完全无锁）
  - 垃圾回收：旧快照由 GC 自动回收

性能对比：
  | 方案 | 读操作 | 写操作 | 适用场景 |
  |---|---|---|---|
  | 单一 RLock（v2.3） | 串行 | 串行 | 低并发 |
  | 读写锁分离（v2.4） | 并行（需锁） | 串行 | 中并发 |
  | Copy-on-Write（v2.5） | 无锁 | 锁+拷贝 | 高并发 |

内存开销：
  - 每次注册策略：深拷贝整个字典（~1KB × 策略数）
  - 策略注册频率低（模块导入时一次性），可接受
  - 旧快照由 GC 回收，无内存泄漏

Source: `src\recovery_strategy_cow.py:1`

---

## `CopyOnWriteRecoveryStrategy`

```matha
class CopyOnWriteRecoveryStrategy
```

**描述**: Copy-on-Write 版本的恢复策略注册表（v2.5）。

锁层次结构：
  _suggestion_lock (Level 0): 最细粒度，仅保护 suggestions 列表
  _snapshot_lock   (Level 1): 保护快照引用的原子替换

无锁读操作：
  try_recover() 完全不持有 _snapshot_lock，
  直接读取 self._snapshot 的引用（原子操作，CPU 指令级）。

写操作策略：
  register() 持有 _snapshot_lock，
  创建新快照（深拷贝）→ 替换引用 → 释放锁。

Source: `src\recovery_strategy_cow.py:38`

---

### `register`

```matha
def register(cls, stage: ErrorStage)
```

**描述**: 装饰器：注册恢复策略（v2.6 按需深拷贝优化）。

写操作流程：
  1. 获取写锁（阻塞其他写操作）
  2. 浅拷贝 dict（共享引用）+ 仅深拷贝目标 stage 的 list
  3. 在副本上注册新策略
  4. 原子替换快照引用
  5. 释放写锁

v2.6 优化：
  - 旧版：copy.deepcopy(snapshot) 深拷贝整个 dict，开销 0.023ms/次
  - 新版：dict() + list() 仅深拷贝被修改的 stage，开销 ~0.003ms/次（9x 加速）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `stage` | `ErrorStage` | |

**装饰器**: classmethod

Source: `src\recovery_strategy_cow.py:66`

---

### `try_recover`

```matha
def try_recover(cls, error: MathaError) -> Optional[MathaError]
```

**描述**: 尝试所有注册的恢复策略（无锁读 + 建议锁写）。

读操作流程：
  1. 读取快照引用（原子操作，无锁）
  2. 在锁外执行策略函数（不阻塞其他读操作）
  3. 若需要修改 error.suggestions，获取建议锁

关键优化：
  - 步骤 1 是纯内存读取，无锁竞争
  - 步骤 2 完全在锁外执行，10000 线程可完全并行
  - 步骤 3 仅在最坏情况下（策略返回非 None）才加锁

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `error` | `MathaError` | |

**返回**: `Optional[MathaError]`

**装饰器**: classmethod

Source: `src\recovery_strategy_cow.py:97`

---

### `get_strategy_count`

```matha
def get_strategy_count(cls, stage: ErrorStage) -> int
```

**描述**: 获取指定阶段的策略数量（无锁读取）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `stage` | `ErrorStage` | |

**返回**: `int`

**装饰器**: classmethod

Source: `src\recovery_strategy_cow.py:133`

---

### `clear`

```matha
def clear(cls) -> None
```

**描述**: 清空所有策略（写锁保护，仅用于测试）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\recovery_strategy_cow.py:138`

---

### `get_snapshot_size`

```matha
def get_snapshot_size(cls) -> int
```

**描述**: 获取当前快照的策略总数（无锁读取）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `int`

**装饰器**: classmethod

Source: `src\recovery_strategy_cow.py:144`

---

## `benchmark_cow_vs_rwlock`

```matha
def benchmark_cow_vs_rwlock()
```

**描述**: 对比 Copy-on-Write（v2.6 按需深拷贝）和读写锁的性能。

Source: `src\recovery_strategy_cow.py:153`

---

## `benchmark_register_cost`

```matha
def benchmark_register_cost()
```

**描述**: 测量 v2.6 按需深拷贝 vs 旧版全量深拷贝的注册开销。

Source: `src\recovery_strategy_cow.py:205`

---

# `recovery_strategy_v24`

```matha

```

**描述**: v2.4 P1 优化：RecoveryStrategy 读写锁分离

设计目标：
  将单一 RLock 拆分为独立的读锁和写锁，
  使高频的 try_recover() 调用不再阻塞其他读操作。

锁结构设计：
  ┌─────────────────────────────────────────────┐
  │           RecoveryStrategy                   │
  │                                              │
  │  _read_lock: RLock    ← 保护 _strategies 读取  │
  │  _write_lock: Lock      ← 保护 _strategies 写入  │
  │  _suggestion_lock: Lock ← 保护 error.suggestions 修改 │
  │                                              │
  │  register()   → _write_lock (低频)            │
  │  try_recover() → _read_lock (高频) → 执行策略 → _suggestion_lock │
  └─────────────────────────────────────────────┘

性能对比（估算）：
  1000 线程并发 try_recover():
    - 当前 RLock:  ~480ms（所有线程串行获取同一把锁）
    - 读写分离:     ~120ms（读操作并行，仅写操作串行）
    - 理论加速:     ~4x

Source: `src\recovery_strategy_v24.py:1`

---

## `RWRecoveryStrategy`

```matha
class RWRecoveryStrategy
```

**描述**: 读写锁分离的错误恢复策略注册表。

相比 v2.3 的单一 RLock，此实现：
  1. 读操作（try_recover）完全并行，不互相阻塞
  2. 写操作（register）串行，但频率极低
  3. 策略执行在锁外进行，锁持有时间最短化

Source: `src\recovery_strategy_v24.py:43`

---

### `register`

```matha
def register(cls, stage: ErrorStage)
```

**描述**: 装饰器：注册恢复策略（写锁保护）。

注意：装饰器在模块导入时执行，属于低频操作，
使用写锁完全可接受。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `stage` | `ErrorStage` | |

**装饰器**: classmethod

Source: `src\recovery_strategy_v24.py:66`

---

### `try_recover`

```matha
def try_recover(cls, error: MathaError) -> Optional[MathaError]
```

**描述**: 尝试所有注册的恢复策略（读锁 + 副本遍历 + 锁外执行）。

锁使用策略：
  1. 获取读锁 → 复制策略列表 → 释放读锁（快）
  2. 在锁外执行策略函数（慢，不持有锁）
  3. 若需要修改 error.suggestions，获取建议锁（快）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `error` | `MathaError` | |

**返回**: `Optional[MathaError]`

**装饰器**: classmethod

Source: `src\recovery_strategy_v24.py:83`

---

### `get_registered_stages`

```matha
def get_registered_stages(cls) -> list[ErrorStage]
```

**描述**: 获取已注册策略的所有阶段（用于调试）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `list[ErrorStage]`

**装饰器**: classmethod

Source: `src\recovery_strategy_v24.py:114`

---

### `get_strategy_count`

```matha
def get_strategy_count(cls, stage: ErrorStage) -> int
```

**描述**: 获取指定阶段的策略数量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `stage` | `ErrorStage` | |

**返回**: `int`

**装饰器**: classmethod

Source: `src\recovery_strategy_v24.py:120`

---

### `clear`

```matha
def clear(cls) -> None
```

**描述**: 清空所有策略（用于测试）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\recovery_strategy_v24.py:126`

---

## `benchmark_rwlock_vs_rlock`

```matha
def benchmark_rwlock_vs_rlock()
```

**描述**: 对比单一 RLock 和读写锁分离的性能。

Source: `src\recovery_strategy_v24.py:163`

---

# `release_notes_v23`

```matha

```

**描述**: v2.3 Release Notes — 完整版

Matha 自成长引擎 v2.3.0
发布日期: 2025-07-26
主题: 结构化异常处理 + 并发安全优化

Source: `src\release_notes_v23.py:1`

---

# `repl`

```matha

```

**描述**: Matha REPL — 交互式开发环境 — v2.2

支持：
  1. 表达式求值（Matha 源码）
  2. 自然语言意图解析
  3. 结果自然语言解释
  4. 历史记录与变量持久化
  5. 多语言输出切换

Source: `src\repl.py:1`

---

## `REPLState`

```matha
class REPLState
```

**描述**: REPL 运行时状态。

**装饰器**: dataclass

Source: `src\repl.py:40`

---

## `MathaREPL`

```matha
class MathaREPL
```

**描述**: Matha 交互式 REPL。

Source: `src\repl.py:51`

---

### `__init__`

```matha
def __init__(self, debug: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `debug` | `bool` | |

Source: `src\repl.py:62`

---

### `_setup_readline`

```matha
def _setup_readline(self) -> None
```

**描述**: 设置 readline 自动补全

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl.py:76`

---

### `run`

```matha
def run(self) -> None
```

**描述**: 运行 REPL 主循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl.py:88`

---

### `_print_banner`

```matha
def _print_banner(self) -> None
```

**描述**: 打印欢迎信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl.py:113`

---

### `_read_input`

```matha
def _read_input(self) -> str
```

**描述**: 读取用户输入，支持多行输入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\repl.py:123`

---

### `_needs_continuation`

```matha
def _needs_continuation(self, text: str) -> bool
```

**描述**: 检查输入是否需要继续多行

规则：
- 未闭合的括号/方括号/花括号
- 未完成的冒号语句（def, if, for, while）
- 连续的缩进行

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `bool`

Source: `src\repl.py:157`

---

### `_process_line`

```matha
def _process_line(self, line: str) -> None
```

**描述**: 处理输入行（支持多行）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl.py:190`

---

### `_process_matha_expr`

```matha
def _process_matha_expr(self, line: str) -> None
```

**描述**: 处理 Matha 表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl.py:238`

---

### `_process_natural_language`

```matha
def _process_natural_language(self, line: str) -> None
```

**描述**: 处理自然语言输入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl.py:260`

---

### `_parse_intent`

```matha
def _parse_intent(self, line: str) -> None
```

**描述**: 解析意图并显示详细信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl.py:287`

---

### `_explain_intent`

```matha
def _explain_intent(self, line: str) -> None
```

**描述**: 解释意图（简版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl.py:300`

---

### `_set_mode`

```matha
def _set_mode(self, mode: str) -> None
```

**描述**: 切换模式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\repl.py:307`

---

### `_store_result`

```matha
def _store_result(self, outputs: list) -> None
```

**描述**: 将输出存储到变量 _1, _2, ...

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `outputs` | `list` | |

**返回**: `None`

Source: `src\repl.py:317`

---

### `_print_help`

```matha
def _print_help(self) -> None
```

**描述**: 打印帮助信息。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl.py:323`

---

### `_print_history`

```matha
def _print_history(self) -> None
```

**描述**: 打印历史输入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl.py:341`

---

### `_print_summary`

```matha
def _print_summary(self) -> None
```

**描述**: 打印会话摘要。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl.py:349`

---

## `run_repl`

```matha
def run_repl(debug: bool) -> None
```

**描述**: 启动 REPL。

| 参数 | 类型 | 说明 |
|------|------|------|
| `debug` | `bool` | |

**返回**: `None`

Source: `src\repl.py:364`

---

## `main`

```matha
def main(argv) -> int
```

**描述**: CLI 入口。支持：matha（REPL）、matha eval <expr>、matha run <file>。

| 参数 | 类型 | 说明 |
|------|------|------|
| `argv` | `` | |

**返回**: `int`

Source: `src\repl.py:370`

---

## `_cmd_eval`

```matha
def _cmd_eval(args) -> int
```

**描述**: 计算单行表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\repl.py:414`

---

## `_cmd_run`

```matha
def _cmd_run(args) -> int
```

**描述**: 运行 .matha 文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `args` | `` | |

**返回**: `int`

Source: `src\repl.py:422`

---

# `repl_completion`

```matha

```

**描述**: Matha REPL 自动补全模块

提供 Tab 键自动补全功能：
  - 变量名补全
  - 函数名补全
  - 关键字补全
  - 历史记录导航（↑↓ 键）
  - 命令补全

使用方式：
  from src.repl_completion import REPLCompleter
  completer = REPLCompleter(state)
  readline.set_completer(completer.complete)
  readline.parse_and_bind("tab: complete")

Source: `src\repl_completion.py:1`

---

## `REPLCompleter`

```matha
class REPLCompleter
```

**描述**: REPL 自动补全器

Source: `src\repl_completion.py:23`

---

### `__init__`

```matha
def __init__(self, state)
```

**描述**: 初始化补全器

Args:
    state: REPLState 对象，包含 variables 和 history

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `state` | `` | |

Source: `src\repl_completion.py:68`

---

### `get_all_completions`

```matha
def get_all_completions(self) -> List[str]
```

**描述**: 获取所有可用的补全项

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

Source: `src\repl_completion.py:80`

---

### `complete`

```matha
def complete(self, text: str, state: int) -> Optional[str]
```

**描述**: 补全函数（供 readline 使用）

Args:
    text: 当前输入的文本
    state: 补全结果索引

Returns:
    第 state 个补全结果，或 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `state` | `int` | |

**返回**: `Optional[str]`

Source: `src\repl_completion.py:103`

---

### `_get_completions`

```matha
def _get_completions(self, text: str) -> List[str]
```

**描述**: 获取匹配的补全项列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `List[str]`

Source: `src\repl_completion.py:122`

---

### `get_hint`

```matha
def get_hint(self, text: str) -> str
```

**描述**: 获取补全提示

Args:
    text: 当前输入的文本

Returns:
    补全提示字符串

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_completion.py:153`

---

### `get_context_hint`

```matha
def get_context_hint(self, text: str) -> str
```

**描述**: 上下文感知补全提示

根据当前输入位置提供智能建议：
- 函数调用后提示参数
- 属性访问后提示可用方法
- 运算符后提示操作数

Args:
    text: 当前输入的文本

Returns:
    上下文提示字符串

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_completion.py:177`

---

### `_get_function_hints`

```matha
def _get_function_hints(self, func_name: str) -> List[str]
```

**描述**: 获取函数参数提示

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |

**返回**: `List[str]`

Source: `src\repl_completion.py:218`

---

### `_get_attribute_hints`

```matha
def _get_attribute_hints(self, prefix: str, suffix: str) -> List[str]
```

**描述**: 获取属性提示

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `prefix` | `str` | |
| `suffix` | `str` | |

**返回**: `List[str]`

Source: `src\repl_completion.py:235`

---

## `REPLHistoryManager`

```matha
class REPLHistoryManager
```

**描述**: REPL 历史记录管理器

Source: `src\repl_completion.py:249`

---

### `__init__`

```matha
def __init__(self, max_history: int)
```

**描述**: 初始化历史管理器

Args:
    max_history: 最大历史记录数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_history` | `int` | |

Source: `src\repl_completion.py:252`

---

### `add`

```matha
def add(self, entry: str) -> None
```

**描述**: 添加历史记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `entry` | `str` | |

**返回**: `None`

Source: `src\repl_completion.py:263`

---

### `previous`

```matha
def previous(self) -> Optional[str]
```

**描述**: 获取上一条历史记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[str]`

Source: `src\repl_completion.py:271`

---

### `next`

```matha
def next(self) -> Optional[str]
```

**描述**: 获取下一条历史记录

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[str]`

Source: `src\repl_completion.py:278`

---

### `current`

```matha
def current(self) -> Optional[str]
```

**描述**: 获取当前历史项

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[str]`

Source: `src\repl_completion.py:288`

---

### `__len__`

```matha
def __len__(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\repl_completion.py:294`

---

### `__iter__`

```matha
def __iter__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\repl_completion.py:297`

---

## `TerminalColors`

```matha
class TerminalColors
```

**描述**: 终端颜色支持

Source: `src\repl_completion.py:305`

---

### `colored`

```matha
def colored(cls, text: str, color: str) -> str
```

**描述**: 生成带颜色的文本

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `text` | `str` | |
| `color` | `str` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\repl_completion.py:334`

---

### `keyword`

```matha
def keyword(cls, text: str) -> str
```

**描述**: 关键字颜色（蓝色）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `text` | `str` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\repl_completion.py:339`

---

### `builtin`

```matha
def builtin(cls, text: str) -> str
```

**描述**: 内置函数颜色（绿色）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `text` | `str` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\repl_completion.py:344`

---

### `variable`

```matha
def variable(cls, text: str) -> str
```

**描述**: 变量颜色（黄色）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `text` | `str` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\repl_completion.py:349`

---

### `command`

```matha
def command(cls, text: str) -> str
```

**描述**: 命令颜色（红色）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `text` | `str` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\repl_completion.py:354`

---

### `hint`

```matha
def hint(cls, text: str) -> str
```

**描述**: 提示颜色（灰色）

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `text` | `str` | |

**返回**: `str`

**装饰器**: classmethod

Source: `src\repl_completion.py:359`

---

## `setup_readline_completion`

```matha
def setup_readline_completion(repl_state, use_colors: bool)
```

**描述**: 设置 readline 补全

Args:
    repl_state: REPLState 对象
    use_colors: 是否启用颜色

使用示例：
    from src.repl_completion import setup_readline_completion
    setup_readline_completion(repl_state)

| 参数 | 类型 | 说明 |
|------|------|------|
| `repl_state` | `` | |
| `use_colors` | `bool` | |

Source: `src\repl_completion.py:368`

---

## `demo_completion`

```matha
def demo_completion()
```

**描述**: 演示自动补全功能

Source: `src\repl_completion.py:398`

---

# `repl_highlight`

```matha

```

**描述**: Matha REPL 语法高亮模块

提供终端语法高亮功能：
  - 关键词着色（蓝色）
  - 字符串着色（绿色）
  - 注释着色（灰色）
  - 数字着色（黄色）
  - 函数调用着色（青色）
  - 运算符着色（红色）

使用方式：
  from src.repl_highlight import highlight_syntax
  highlighted = highlight_syntax("x = 3.14  # 设置值")

Source: `src\repl_highlight.py:1`

---

## `SyntaxHighlighter`

```matha
class SyntaxHighlighter
```

**描述**: 语法高亮器

Source: `src\repl_highlight.py:21`

---

### `__init__`

```matha
def __init__(self, enabled: bool)
```

**描述**: 初始化语法高亮器

Args:
    enabled: 是否启用高亮

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `enabled` | `bool` | |

Source: `src\repl_highlight.py:71`

---

### `_check_color_support`

```matha
def _check_color_support(self) -> bool
```

**描述**: 检查终端是否支持颜色

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\repl_highlight.py:81`

---

### `highlight`

```matha
def highlight(self, text: str) -> str
```

**描述**: 对输入文本进行语法高亮

Args:
    text: 原始文本

Returns:
    高亮后的文本

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_highlight.py:93`

---

### `_highlight_comments`

```matha
def _highlight_comments(self, text: str) -> str
```

**描述**: 高亮注释

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_highlight.py:129`

---

### `_highlight_strings`

```matha
def _highlight_strings(self, text: str) -> str
```

**描述**: 高亮字符串

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_highlight.py:139`

---

### `_highlight_numbers`

```matha
def _highlight_numbers(self, text: str) -> str
```

**描述**: 高亮数字

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_highlight.py:166`

---

### `_highlight_keywords`

```matha
def _highlight_keywords(self, text: str) -> str
```

**描述**: 高亮关键字

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_highlight.py:175`

---

### `_highlight_functions`

```matha
def _highlight_functions(self, text: str) -> str
```

**描述**: 高亮函数调用

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_highlight.py:185`

---

### `_highlight_operators`

```matha
def _highlight_operators(self, text: str) -> str
```

**描述**: 高亮运算符

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `str`

Source: `src\repl_highlight.py:196`

---

## `get_highlighter`

```matha
def get_highlighter() -> SyntaxHighlighter
```

**描述**: 获取全局语法高亮器实例

**返回**: `SyntaxHighlighter`

Source: `src\repl_highlight.py:213`

---

## `highlight_syntax`

```matha
def highlight_syntax(text: str, enabled: bool) -> str
```

**描述**: 快速高亮函数

Args:
    text: 原始文本
    enabled: 是否启用高亮

Returns:
    高亮后的文本

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |
| `enabled` | `bool` | |

**返回**: `str`

Source: `src\repl_highlight.py:221`

---

## `demo_highlight`

```matha
def demo_highlight()
```

**描述**: 演示语法高亮

Source: `src\repl_highlight.py:236`

---

# `repl_todo`

```matha

```

**描述**: Matha REPL 自动补全 - 剩余待办事项检查

当前完成度：90%
待完成项：10%

Source: `src\repl_todo.py:1`

---

## `check_repl_integration`

```matha
def check_repl_integration()
```

**描述**: 检查 REPL 集成状态

Source: `src\repl_todo.py:81`

---

# `repl_v23`

```matha

```

**描述**: v2.3 异常处理集成到 REPL — 增强版

改动点：
  1. REPL 使用 EnhancedIntentParser 替代基础 IntentParser
  2. 自然语言模式下显示结构化错误报告（含恢复建议）
  3. 错误计数区分 WARNING/ERROR
  4. 提供 "recover" 命令尝试自动恢复

Source: `src\repl_v23.py:1`

---

## `REPLState`

```matha
class REPLState
```

**描述**: REPL 运行时状态（v2.3 增强，线程安全）。

**装饰器**: dataclass

Source: `src\repl_v23.py:46`

---

### `append_error`

```matha
def append_error(self, error: MathaError) -> None
```

**描述**: 线程安全地追加错误到日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `error` | `MathaError` | |

**返回**: `None`

Source: `src\repl_v23.py:59`

---

### `get_error_log`

```matha
def get_error_log(self) -> list[MathaError]
```

**描述**: 线程安全地获取错误日志副本。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[MathaError]`

Source: `src\repl_v23.py:64`

---

## `MathaREPL`

```matha
class MathaREPL
```

**描述**: Matha 交互式 REPL v2.3 — 集成结构化异常处理。

Source: `src\repl_v23.py:70`

---

### `__init__`

```matha
def __init__(self, debug: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `debug` | `bool` | |

Source: `src\repl_v23.py:81`

---

### `run`

```matha
def run(self) -> None
```

**描述**: 运行 REPL 主循环。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl_v23.py:90`

---

### `_print_banner`

```matha
def _print_banner(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl_v23.py:115`

---

### `_read_input`

```matha
def _read_input(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\repl_v23.py:124`

---

### `_process_line`

```matha
def _process_line(self, line: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl_v23.py:129`

---

### `_process_matha_expr`

```matha
def _process_matha_expr(self, line: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl_v23.py:165`

---

### `_process_natural_language`

```matha
def _process_natural_language(self, line: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl_v23.py:180`

---

### `_parse_intent`

```matha
def _parse_intent(self, line: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl_v23.py:224`

---

### `_explain_intent`

```matha
def _explain_intent(self, line: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

**返回**: `None`

Source: `src\repl_v23.py:250`

---

### `_recover_last_error`

```matha
def _recover_last_error(self) -> None
```

**描述**: 尝试恢复最近一次错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl_v23.py:254`

---

### `_handle_matha_error`

```matha
def _handle_matha_error(self, error: MathaError, source: str) -> None
```

**描述**: 处理 MathaError，显示友好报告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `error` | `MathaError` | |
| `source` | `str` | |

**返回**: `None`

Source: `src\repl_v23.py:269`

---

### `_set_mode`

```matha
def _set_mode(self, mode: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mode` | `str` | |

**返回**: `None`

Source: `src\repl_v23.py:285`

---

### `_store_result`

```matha
def _store_result(self, outputs: list) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `outputs` | `list` | |

**返回**: `None`

Source: `src\repl_v23.py:293`

---

### `_print_help`

```matha
def _print_help(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl_v23.py:297`

---

### `_print_history`

```matha
def _print_history(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl_v23.py:316`

---

### `_print_errors`

```matha
def _print_errors(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl_v23.py:323`

---

### `_print_summary`

```matha
def _print_summary(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\repl_v23.py:335`

---

## `run_repl`

```matha
def run_repl(debug: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `debug` | `bool` | |

**返回**: `None`

Source: `src\repl_v23.py:348`

---

# `result`

```matha

```

**描述**: Matha Result 类型与异常处理 — v2.2

提供 Rust 风格的 Result<T, E> 和 Option<T> 类型，
支持函数式错误传播（? 运算符语义）。

Source: `src\result.py:1`

---

## `ResultState`

```matha
class ResultState(Enum)
```

**描述**: Result 状态。

Source: `src\result.py:22`

---

## `Ok`

```matha
class Ok
```

**描述**: 成功结果。

**装饰器**: dataclass

Source: `src\result.py:29`

---

### `is_ok`

```matha
def is_ok(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:35`

---

### `is_err`

```matha
def is_err(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:38`

---

### `unwrap`

```matha
def unwrap(self) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `T`

Source: `src\result.py:41`

---

### `expect`

```matha
def expect(self, msg: str) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `T`

Source: `src\result.py:44`

---

### `unwrap_or`

```matha
def unwrap_or(self, default: T) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `default` | `T` | |

**返回**: `T`

Source: `src\result.py:47`

---

### `map`

```matha
def map(self, fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:50`

---

### `map_err`

```matha
def map_err(self, _fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:53`

---

### `and_then`

```matha
def and_then(self, fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:56`

---

### `or_else`

```matha
def or_else(self, _fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:59`

---

### `unwrap_or_else`

```matha
def unwrap_or_else(self, fn) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `T`

Source: `src\result.py:62`

---

### `err`

```matha
def err(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\result.py:65`

---

### `ok`

```matha
def ok(self) -> Optional[T]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[T]`

Source: `src\result.py:68`

---

### `as_ref`

```matha
def as_ref(self) -> 'Ok'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'Ok'`

Source: `src\result.py:71`

---

### `iter`

```matha
def iter(self) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\result.py:74`

---

### `label`

```matha
def label(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\result.py:77`

---

## `Err`

```matha
class Err
```

**描述**: 错误结果。

**装饰器**: dataclass

Source: `src\result.py:82`

---

### `is_ok`

```matha
def is_ok(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:89`

---

### `is_err`

```matha
def is_err(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:92`

---

### `unwrap`

```matha
def unwrap(self) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `T`

Source: `src\result.py:95`

---

### `expect`

```matha
def expect(self, msg: str) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `T`

Source: `src\result.py:98`

---

### `unwrap_or`

```matha
def unwrap_or(self, default: T) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `default` | `T` | |

**返回**: `T`

Source: `src\result.py:101`

---

### `map`

```matha
def map(self, _fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:104`

---

### `map_err`

```matha
def map_err(self, fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:107`

---

### `and_then`

```matha
def and_then(self, _fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:110`

---

### `or_else`

```matha
def or_else(self, fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:113`

---

### `unwrap_or_else`

```matha
def unwrap_or_else(self, fn) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `T`

Source: `src\result.py:116`

---

### `err`

```matha
def err(self) -> Optional[E]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional[E]`

Source: `src\result.py:119`

---

### `ok`

```matha
def ok(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\result.py:122`

---

### `as_ref`

```matha
def as_ref(self) -> 'Err'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'Err'`

Source: `src\result.py:125`

---

### `iter`

```matha
def iter(self) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\result.py:128`

---

### `label`

```matha
def label(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\result.py:131`

---

### `context`

```matha
def context(self, msg: str) -> 'Err'
```

**描述**: 添加错误上下文。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `'Err'`

Source: `src\result.py:134`

---

## `Some`

```matha
class Some
```

**描述**: 有值。

**装饰器**: dataclass

Source: `src\result.py:145`

---

### `is_some`

```matha
def is_some(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:149`

---

### `is_none`

```matha
def is_none(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:152`

---

### `unwrap`

```matha
def unwrap(self) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `T`

Source: `src\result.py:155`

---

### `unwrap_or`

```matha
def unwrap_or(self, default: T) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `default` | `T` | |

**返回**: `T`

Source: `src\result.py:158`

---

### `unwrap_or_else`

```matha
def unwrap_or_else(self, fn) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `T`

Source: `src\result.py:161`

---

### `map`

```matha
def map(self, fn) -> 'Option'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `'Option'`

Source: `src\result.py:164`

---

### `and_then`

```matha
def and_then(self, fn) -> 'Option'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `'Option'`

Source: `src\result.py:167`

---

### `filter`

```matha
def filter(self, pred) -> 'Option'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `pred` | `` | |

**返回**: `'Option'`

Source: `src\result.py:170`

---

### `ok_or`

```matha
def ok_or(self, err: E) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `err` | `E` | |

**返回**: `'Result'`

Source: `src\result.py:173`

---

### `ok_or_else`

```matha
def ok_or_else(self, err_fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `err_fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:176`

---

### `iter`

```matha
def iter(self) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\result.py:179`

---

### `label`

```matha
def label(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\result.py:182`

---

## `None_`

```matha
class None_
```

**描述**: 无值。

**装饰器**: dataclass

Source: `src\result.py:187`

---

### `is_some`

```matha
def is_some(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:190`

---

### `is_none`

```matha
def is_none(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\result.py:193`

---

### `unwrap`

```matha
def unwrap(self) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `T`

Source: `src\result.py:196`

---

### `expect`

```matha
def expect(self, msg: str) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `T`

Source: `src\result.py:199`

---

### `unwrap_or`

```matha
def unwrap_or(self, default: T) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `default` | `T` | |

**返回**: `T`

Source: `src\result.py:202`

---

### `unwrap_or_else`

```matha
def unwrap_or_else(self, fn) -> T
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `` | |

**返回**: `T`

Source: `src\result.py:205`

---

### `map`

```matha
def map(self, _fn) -> 'Option'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_fn` | `` | |

**返回**: `'Option'`

Source: `src\result.py:208`

---

### `and_then`

```matha
def and_then(self, _fn) -> 'Option'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_fn` | `` | |

**返回**: `'Option'`

Source: `src\result.py:211`

---

### `filter`

```matha
def filter(self, _pred) -> 'Option'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_pred` | `` | |

**返回**: `'Option'`

Source: `src\result.py:214`

---

### `ok_or`

```matha
def ok_or(self, err: E) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `err` | `E` | |

**返回**: `'Result'`

Source: `src\result.py:217`

---

### `ok_or_else`

```matha
def ok_or_else(self, err_fn) -> 'Result'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `err_fn` | `` | |

**返回**: `'Result'`

Source: `src\result.py:220`

---

### `iter`

```matha
def iter(self) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Any`

Source: `src\result.py:223`

---

### `label`

```matha
def label(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\result.py:226`

---

## `MathaResultError`

```matha
class MathaResultError(Exception)
```

**描述**: Matha Result 类型错误。

Source: `src\result.py:237`

---

### `__init__`

```matha
def __init__(self, message: str, trace: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `trace` | `str` | |

Source: `src\result.py:239`

---

## `MathaOptionError`

```matha
class MathaOptionError(MathaResultError)
```

**描述**: Option 类型错误。

Source: `src\result.py:245`

---

## `MathaTypeError`

```matha
class MathaTypeError(MathaResultError)
```

**描述**: 类型转换错误。

Source: `src\result.py:250`

---

## `ok`

```matha
def ok(value: T) -> Result
```

**描述**: 创建成功结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `value` | `T` | |

**返回**: `Result`

Source: `src\result.py:259`

---

## `err`

```matha
def err(error: E) -> Result
```

**描述**: 创建错误结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `error` | `E` | |

**返回**: `Result`

Source: `src\result.py:264`

---

## `some`

```matha
def some(value: T) -> Option
```

**描述**: 创建 Some。

| 参数 | 类型 | 说明 |
|------|------|------|
| `value` | `T` | |

**返回**: `Option`

Source: `src\result.py:269`

---

## `none`

```matha
def none() -> Option
```

**描述**: 创建 None。

**返回**: `Option`

Source: `src\result.py:274`

---

## `result`

```matha
def result(fn, *args, **kwargs) -> Result
```

**描述**: 执行函数并捕获异常，返回 Result。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

**返回**: `Result`

Source: `src\result.py:279`

---

## `try_unwrap`

```matha
def try_unwrap(result: Result) -> Any
```

**描述**: 安全解包 Result。

| 参数 | 类型 | 说明 |
|------|------|------|
| `result` | `Result` | |

**返回**: `Any`

Source: `src\result.py:287`

---

## `try_unwrap_or`

```matha
def try_unwrap_or(result: Result, default: Any) -> Any
```

**描述**: 安全解包 Result，失败时返回默认值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `result` | `Result` | |
| `default` | `Any` | |

**返回**: `Any`

Source: `src\result.py:295`

---

## `register_result_builtins`

```matha
def register_result_builtins(builtins: dict) -> None
```

**描述**: 注册 Result/Option 类型到 Matha 内建函数表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\result.py:306`

---

# `selfupgrade`

```matha

```

**描述**: Matha 自我升级子系统：探针 + 沙箱 + 升级。

为解释器提供运行时内省（探针）、隔离试运行（沙箱）与安全合并
（升级）三件套，使 Matha 程序可在运行时加载新源码、先在沙箱中
验证、再决定是否提交到本体——实现「自我升级」而不会因坏代码
污染已注册的函数/变量。

设计要点：
  - Probe：只读内省，返回普通 dict/list，供 Matha 与 Python 两侧使用。
  - Sandbox：克隆本体解释器的 env/funcs/constructors/builtins，在其上
    运行新源码；本体不受影响。提供 diff()/commit()/rollback()。
  - upgrade(parent, source, verify)：沙箱试运行 →（可选）校验 → 通过
    则 commit，失败/校验不过则 rollback，返回 UpgradeResult。

状态化内建（探针_状态 / 试运行 / 升级 等）由 Interpreter._install_self_builtins
注册并绑定到各自解释器实例；沙箱克隆后会重新绑定到沙箱自身解释器，
故在沙箱内部调用 升级(...) 会在沙箱内部再开一层沙箱，层间隔离。

Source: `src\selfupgrade.py:1`

---

## `Probe`

```matha
class Probe
```

**描述**: 对解释器运行时状态的只读内省视图。

所有方法返回普通容器（dict/list/bool），不返回解释器内部对象的
可变引用，便于跨语言边界（Matha 侧只能消费 dict/list/标量）。

Source: `src\selfupgrade.py:31`

---

### `__init__`

```matha
def __init__(self, interp: Interpreter)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `interp` | `Interpreter` | |

Source: `src\selfupgrade.py:38`

---

### `state`

```matha
def state(self) -> dict
```

**描述**: 综合状态快照：变量/函数/内建/构造子名 + 输出/追踪/深度计数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\selfupgrade.py:41`

---

### `env`

```matha
def env(self) -> dict
```

**描述**: 环境变量名 → 值 的浅快照。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\selfupgrade.py:54`

---

### `func_names`

```matha
def func_names(self) -> list
```

**描述**: 已注册函数名列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list`

Source: `src\selfupgrade.py:58`

---

### `builtin_names`

```matha
def builtin_names(self) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list`

Source: `src\selfupgrade.py:62`

---

### `has`

```matha
def has(self, name: str) -> bool
```

**描述**: name 是否已定义（变量/函数/内建/构造子任一）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `bool`

Source: `src\selfupgrade.py:65`

---

### `func_info`

```matha
def func_info(self, name: str) -> Optional[dict]
```

**描述**: 单个函数的参数信息；未定义返回 None。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[dict]`

Source: `src\selfupgrade.py:71`

---

## `Sandbox`

```matha
class Sandbox
```

**描述**: 隔离执行环境：克隆本体解释器状态，代码运行不影响本体。

典型流程：
    sb = interp.sandbox()
    outs, trace, err = sb.run(new_source)
    if err is None and ok(outs): sb.commit()
    else: sb.rollback()

合并策略（commit）：env/funcs/constructors 以 update 合并到本体；
builtins 不合并（升级不改 Python 内建）。一次性：commit 或
rollback 后沙箱即作废，不可再用。

Source: `src\selfupgrade.py:91`

---

### `__init__`

```matha
def __init__(self, parent: Interpreter)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `parent` | `Interpreter` | |

Source: `src\selfupgrade.py:105`

---

### `run`

```matha
def run(self, source: str) -> tuple[list, list[str], Optional[str]]
```

**描述**: 在沙箱中解析并运行源码；返回 (outputs, trace, error)。

error 非 None 表示解析或运行时出错（已捕获，不抛出）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `tuple[list, list[str], Optional[str]]`

Source: `src\selfupgrade.py:126`

---

### `call`

```matha
def call(self, name: str, *args)
```

**描述**: 在沙箱解释器上调用函数/内建。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

Source: `src\selfupgrade.py:141`

---

### `diff`

```matha
def diff(self) -> dict
```

**描述**: 沙箱相对初始快照的变更摘要。

返回：
    {
      "新函数": [...],      # 新增名
      "改函数": [...],      # 覆写名（FuncDef 对象变了）
      "新变量": {名: 值},
      "改变量": {名: 新值},
      "新构造子": [...],
    }

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\selfupgrade.py:148`

---

### `commit`

```matha
def commit(self) -> dict
```

**描述**: 将沙箱变更合并回本体解释器；返回 diff。一次性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\selfupgrade.py:180`

---

### `rollback`

```matha
def rollback(self) -> None
```

**描述**: 放弃沙箱变更。一次性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\selfupgrade.py:190`

---

### `_check`

```matha
def _check(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\selfupgrade.py:194`

---

## `UpgradeResult`

```matha
class UpgradeResult
```

**描述**: 升级流程的统一结果。

**装饰器**: dataclass

Source: `src\selfupgrade.py:204`

---

### `as_dict`

```matha
def as_dict(self) -> dict
```

**描述**: 转为普通 dict（供 Matha 侧消费）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\selfupgrade.py:212`

---

## `upgrade`

```matha
def upgrade(parent: Interpreter, source: str, verify: Optional[Callable[[Sandbox], bool]]) -> UpgradeResult
```

**描述**: 加载 Matha 源码到沙箱试运行，通过后合并到本体解释器。

流程：
  1. 创建沙箱（克隆本体状态）
  2. 在沙箱中解析 + 运行源码
  3. 运行出错 → rollback，返回失败结果（本体未被触碰）
  4. verify 给定时调用 verify(sandbox)；返回假或抛错 → rollback
  5. 通过 → commit，返回成功结果（含变更 diff）

Args:
    parent: 本体解释器（升级目标）。
    source: 待加载的 Matha 源码（通常含 func 定义）。
    verify: 可选校验回调，接收沙箱，返回 bool。

Returns:
    UpgradeResult

| 参数 | 类型 | 说明 |
|------|------|------|
| `parent` | `Interpreter` | |
| `source` | `str` | |
| `verify` | `Optional[Callable[[Sandbox], bool]]` | |

**返回**: `UpgradeResult`

Source: `src\selfupgrade.py:226`

---

# `semantic`

```matha

```

**描述**: Matha 语义分析器。

基于 parser 产出的 AST，执行以下语义检查（对应 M3.1/M3.2 规则）：

1. 变量赋值与作用域
   - @ 设定声明变量（@N:变量=值 / @(a|b|c)）
   - Binding 赋值（变量 = 表达式）
   - Variable 引用解析（未定义变量检测）

2. 命令链 >> 语义
   - 链式数据流追踪（前一环节输出 → 后一环节输入）
   - 链类型兼容性（command→command / output→output / command→output）
   - M3.2 触发条件：单条命令/单条输出不足以完成任务时才启用

3. 段内 5 步固定顺序（M3.1/M3.2）
   - 命令→变量→？公式→字母公式→输出
   - 顺序违规检测

4. 公式分层（M3.2）
   - ？公式 = 简化抽象（占位符结构）
   - 字母公式 = 精确化（变量替换占位符）
   - 结构对应检查

5. 资源读取能力（M3.2）
   - 命令/输出识别 URL/文件/目录/端口

6. 循环后缀校验
   - 段级循环 …N(x/y)：N 匹配段号，x ≤ y
   - 全局循环 ……(x/y)：x ≤ y
   - 全局编号一致性

Source: `src\semantic.py:1`

---

## `_build_builtin_symtab`

```matha
def _build_builtin_symtab() -> 'SymbolTable'
```

**描述**: 构建包含所有内建符号的符号表（模块级缓存，仅调用一次）。

**返回**: `'SymbolTable'`

Source: `src\semantic.py:48`

---

## `SemanticError`

```matha
class SemanticError
```

**描述**: 语义错误/警告条目。

Source: `src\semantic.py:103`

---

### `__init__`

```matha
def __init__(self, msg: str, severity: str, line: int, node: Any)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |
| `severity` | `str` | |
| `line` | `int` | |
| `node` | `Any` | |

Source: `src\semantic.py:106`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\semantic.py:112`

---

## `SemanticAnalyzer`

```matha
class SemanticAnalyzer
```

**描述**: Matha 语义分析器（visitor 模式）。

用法：
    parser = Parser(source)
    program = parser.parse()
    analyzer = SemanticAnalyzer()
    analyzer.analyze(program)
    for err in analyzer.errors:
        print(err)

Source: `src\semantic.py:118`

---

### `__init__`

```matha
def __init__(self, verbose: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `verbose` | `bool` | |

Source: `src\semantic.py:130`

---

### `_enable_logging`

```matha
def _enable_logging(self) -> None
```

**描述**: 配置 logger 输出到 stderr（仅 verbose 模式调用）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\semantic.py:254`

---

### `_log`

```matha
def _log(self, msg: str) -> None
```

**描述**: 输出调试日志。仅在 verbose 模式下实际输出，否则静默。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |

**返回**: `None`

Source: `src\semantic.py:262`

---

### `analyze`

```matha
def analyze(self, program: ast.Program) -> list[SemanticError]
```

**描述**: 分析整个程序，返回错误/警告列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `ast.Program` | |

**返回**: `list[SemanticError]`

Source: `src\semantic.py:271`

---

### `_visit`

```matha
def _visit(self, node: Any) -> Any
```

**描述**: 根据节点类型分发到对应的 visit_* 方法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `Any` | |

**返回**: `Any`

Source: `src\semantic.py:280`

---

### `_visit_generic`

```matha
def _visit_generic(self, node: Any) -> Any
```

**描述**: 默认 visitor：遍历 dataclass 字段中的 AST 节点。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `Any` | |

**返回**: `Any`

Source: `src\semantic.py:288`

---

### `_error`

```matha
def _error(self, msg: str, node: Any, severity: str) -> None
```

**描述**: 记录语义错误。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `msg` | `str` | |
| `node` | `Any` | |
| `severity` | `str` | |

**返回**: `None`

Source: `src\semantic.py:300`

---

### `_visit_Program`

```matha
def _visit_Program(self, node: ast.Program) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Program` | |

**返回**: `None`

Source: `src\semantic.py:309`

---

### `_visit_ModuleDecl`

```matha
def _visit_ModuleDecl(self, node: ast.ModuleDecl) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ModuleDecl` | |

**返回**: `None`

Source: `src\semantic.py:317`

---

### `_visit_ImportDecl`

```matha
def _visit_ImportDecl(self, node: ast.ImportDecl) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ImportDecl` | |

**返回**: `None`

Source: `src\semantic.py:330`

---

### `_visit_StructDef`

```matha
def _visit_StructDef(self, node: ast.StructDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.StructDef` | |

**返回**: `None`

Source: `src\semantic.py:341`

---

### `_visit_EnumDef`

```matha
def _visit_EnumDef(self, node: ast.EnumDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.EnumDef` | |

**返回**: `None`

Source: `src\semantic.py:349`

---

### `_visit_AliasDef`

```matha
def _visit_AliasDef(self, node: ast.AliasDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.AliasDef` | |

**返回**: `None`

Source: `src\semantic.py:357`

---

### `_visit_FuncDef`

```matha
def _visit_FuncDef(self, node: ast.FuncDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FuncDef` | |

**返回**: `None`

Source: `src\semantic.py:361`

---

### `_visit_MechUnit`

```matha
def _visit_MechUnit(self, node: ast.MechUnit) -> None
```

**描述**: 机械单元：进入段作用域，追踪 5 步顺序。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.MechUnit` | |

**返回**: `None`

Source: `src\semantic.py:376`

---

### `_visit_CodeBlock`

```matha
def _visit_CodeBlock(self, node: ast.CodeBlock) -> None
```

**描述**: 代码块：逐条分析语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.CodeBlock` | |

**返回**: `None`

Source: `src\semantic.py:393`

---

### `_visit_GenStmt`

```matha
def _visit_GenStmt(self, node: ast.GenStmt) -> None
```

**描述**: gen_stmt / gen_stmt_seg：分类步骤类型并记录到段追踪器。

当段号变化时（#1 → #2），重置段追踪器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.GenStmt` | |

**返回**: `None`

Source: `src\semantic.py:400`

---

### `_classify_gen_stmt_content`

```matha
def _classify_gen_stmt_content(self, content: Any) -> Optional[str]
```

**描述**: 将 GenStmt 内容分类为 5 步之一。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `content` | `Any` | |

**返回**: `Optional[str]`

Source: `src\semantic.py:431`

---

### `_expr_contains_placeholder`

```matha
def _expr_contains_placeholder(self, expr: Any) -> bool
```

**描述**: 递归检查表达式是否含 ？ 占位符变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `bool`

Source: `src\semantic.py:446`

---

### `_check_formula_layering`

```matha
def _check_formula_layering(self, tracker: SegmentTracker, letter_formula: Any) -> None
```

**描述**: 检查字母公式与？公式的结构对应。

M3.2 语义：？公式 = 简化抽象；字母公式 = 精确化。
若段内有？公式，检查字母公式的运算符结构与？公式一致。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `tracker` | `SegmentTracker` | |
| `letter_formula` | `Any` | |

**返回**: `None`

Source: `src\semantic.py:463`

---

### `_extract_op_structure`

```matha
def _extract_op_structure(self, expr: Any) -> list[str]
```

**描述**: 提取表达式的运算符结构（忽略操作数，只保留运算符序列）。

例：？+？=？ → ["+", "="]
    a+b=c   → ["+", "="]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `list[str]`

Source: `src\semantic.py:485`

---

### `_collect_ops`

```matha
def _collect_ops(self, expr: Any, ops: list[str]) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |
| `ops` | `list[str]` | |

**返回**: `None`

Source: `src\semantic.py:495`

---

### `_visit_SetUp`

```matha
def _visit_SetUp(self, node: ast.SetUp) -> None
```

**描述**: @ 设定：声明变量 + 赋值检查。

@N: 形式包含段号，需检测段号变化并重置追踪器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.SetUp` | |

**返回**: `None`

Source: `src\semantic.py:508`

---

### `_visit_set_up_item`

```matha
def _visit_set_up_item(self, item: ast.SetUpItem) -> None
```

**描述**: 处理单个设定项：声明变量、检查赋值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `item` | `ast.SetUpItem` | |

**返回**: `None`

Source: `src\semantic.py:519`

---

### `_visit_Binding`

```matha
def _visit_Binding(self, node: ast.Binding) -> None
```

**描述**: 绑定：target = value。

语义：
- 若 target 是 Variable 且未定义 → 声明新变量
- 若 target 是 Variable 且已定义 → 重新赋值（警告）
- value 中的变量引用需解析

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Binding` | |

**返回**: `None`

Source: `src\semantic.py:556`

---

### `_visit_Variable`

```matha
def _visit_Variable(self, node: ast.Variable) -> None
```

**描述**: 变量引用解析：检查是否已定义。

在输出上下文 [...] 内，未定义变量降级为警告（可能是文本标签而非变量引用）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Variable` | |

**返回**: `None`

Source: `src\semantic.py:587`

---

### `_visit_ChainStmt`

```matha
def _visit_ChainStmt(self, node: ast.ChainStmt) -> None
```

**描述**: 命令链 >> 的语义分析。

M3.2 规则：
- 触发条件：单条命令/单条输出不足以完成任务时才启用
- 链类型兼容：command→command / output→output / command→output
- 数据流：前一环节的输出可作为后一环节的输入

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ChainStmt` | |

**返回**: `None`

Source: `src\semantic.py:603`

---

### `_check_chain_trigger_condition`

```matha
def _check_chain_trigger_condition(self, node: ast.ChainStmt) -> None
```

**描述**: M3.2 触发条件检查：单条命令/单条输出是否已足够。

简化判断：若所有环节都是相同类型的简单命令（无参数依赖），提示警告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ChainStmt` | |

**返回**: `None`

Source: `src\semantic.py:648`

---

### `_is_simple_command`

```matha
def _is_simple_command(self, stmt: Any) -> bool
```

**描述**: 判断链式环节是否为简单命令（无变量依赖）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `Any` | |

**返回**: `bool`

Source: `src\semantic.py:667`

---

### `_has_chain_data_dependency`

```matha
def _has_chain_data_dependency(self, node: ast.ChainStmt) -> bool
```

**描述**: 检查链中是否有数据依赖（后一环节引用前一环节的输出）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ChainStmt` | |

**返回**: `bool`

Source: `src\semantic.py:676`

---

### `_classify_chain_link`

```matha
def _classify_chain_link(self, stmt: Any) -> Optional[str]
```

**描述**: 将链式环节分类为 "command" / "output" / "expr" / "setup"。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `Any` | |

**返回**: `Optional[str]`

Source: `src\semantic.py:684`

---

### `_unwrap_chain_link_content`

```matha
def _unwrap_chain_link_content(self, stmt: Any) -> Any
```

**描述**: 解包链式环节，提取实际内容。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `Any` | |

**返回**: `Any`

Source: `src\semantic.py:697`

---

### `_check_chain_compatibility`

```matha
def _check_chain_compatibility(self, prev: str, curr: str) -> bool
```

**描述**: 检查链式相邻环节的类型兼容性。

兼容组合：
    command → command   （命令流水线）
    command → output    （命令结果→输出）
    output  → output    （多输出链式）
    output  → command   （输出→后续命令）
    setup   → command   （设定→命令）
    setup   → output    （设定→输出）
    expr    → expr      （表达式链式）
    expr    → output    （表达式→输出）
不兼容：
    output → setup      （输出后不能设定变量）
    command → setup     （命令后不能设定变量）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `prev` | `str` | |
| `curr` | `str` | |

**返回**: `bool`

Source: `src\semantic.py:703`

---

### `_extract_output_vars`

```matha
def _extract_output_vars(self, stmt: Any) -> list[str]
```

**描述**: 从链式环节中提取输出变量名（用于数据流追踪）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `Any` | |

**返回**: `list[str]`

Source: `src\semantic.py:725`

---

### `_collect_var_names`

```matha
def _collect_var_names(self, expr: Any, names: list[str]) -> None
```

**描述**: 递归收集表达式中的变量名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |
| `names` | `list[str]` | |

**返回**: `None`

Source: `src\semantic.py:743`

---

### `_visit_OutputTrail`

```matha
def _visit_OutputTrail(self, node: ast.OutputTrail) -> None
```

**描述**: 输出追踪：检查循环后缀 + 子文件/文件路径引用 + 资源读取 + 变量引用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.OutputTrail` | |

**返回**: `None`

Source: `src\semantic.py:758`

---

### `_check_seg_loop`

```matha
def _check_seg_loop(self, seg_loop: ast.SegLoopSuffix) -> None
```

**描述**: 段级循环后缀校验：…N(x/y)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `seg_loop` | `ast.SegLoopSuffix` | |

**返回**: `None`

Source: `src\semantic.py:802`

---

### `_check_global_loop`

```matha
def _check_global_loop(self, global_loop: ast.GlobalLoopSuffix) -> None
```

**描述**: 全局循环后缀校验：……(x/y)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `global_loop` | `ast.GlobalLoopSuffix` | |

**返回**: `None`

Source: `src\semantic.py:825`

---

### `_check_subfiles`

```matha
def _check_subfiles(self, subfiles: list[str]) -> None
```

**描述**: M3.3 子文件引用校验：段循环后的下位文件（补充/扩充当前段）。

多个子文件用 | 分隔。骨架阶段仅识别资源类型，不强制存在性校验。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `subfiles` | `list[str]` | |

**返回**: `None`

Source: `src\semantic.py:837`

---

### `_check_file_ref`

```matha
def _check_file_ref(self, file_ref: str) -> None
```

**描述**: M3.3 文件路径引用校验：全局循环后的文件分割标记。

语义：当前代码文件被分割/分化成多个文件使用。
骨架阶段仅识别资源类型，不强制存在性校验。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `file_ref` | `str` | |

**返回**: `None`

Source: `src\semantic.py:854`

---

### `_check_resource_reading`

```matha
def _check_resource_reading(self, node: ast.OutputTrail) -> None
```

**描述**: M3.2 资源读取检查：输出内容若为资源路径，识别并验证。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.OutputTrail` | |

**返回**: `None`

Source: `src\semantic.py:870`

---

### `_extract_text`

```matha
def _extract_text(self, expr: Any) -> Optional[str]
```

**描述**: 从表达式中提取纯文本（用于资源识别）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Optional[str]`

Source: `src\semantic.py:881`

---

### `_visit_CommandLiteral`

```matha
def _visit_CommandLiteral(self, node: ast.CommandLiteral) -> None
```

**描述**: 命令字面量：识别资源读取能力（M3.2）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.CommandLiteral` | |

**返回**: `None`

Source: `src\semantic.py:893`

---

### `_visit_ReadBlock`

```matha
def _visit_ReadBlock(self, node: ast.ReadBlock) -> None
```

**描述**: 读取块：识别资源类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.ReadBlock` | |

**返回**: `None`

Source: `src\semantic.py:902`

---

### `_visit_NLBlock`

```matha
def _visit_NLBlock(self, node: ast.NLBlock) -> None
```

**描述**: 自然语言意图块：记录意图，不检查正文变量（正文是自然语言文本）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.NLBlock` | |

**返回**: `None`

Source: `src\semantic.py:919`

---

### `_visit_FileMarker`

```matha
def _visit_FileMarker(self, node: ast.FileMarker) -> None
```

**描述**: 文件标记：#：【文件】或 #：【路径】。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FileMarker` | |

**返回**: `None`

Source: `src\semantic.py:927`

---

### `_visit_GlobalIdStmt`

```matha
def _visit_GlobalIdStmt(self, node: ast.GlobalIdStmt) -> None
```

**描述**: 全局编号语句：跨文件绑定。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.GlobalIdStmt` | |

**返回**: `None`

Source: `src\semantic.py:938`

---

### `_visit_DictLiteral`

```matha
def _visit_DictLiteral(self, node: ast.DictLiteral) -> None
```

**描述**: 字典字面量：keys 为字段名（非变量引用），只检查 values。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.DictLiteral` | |

**返回**: `None`

Source: `src\semantic.py:954`

---

### `_visit_BinaryOp`

```matha
def _visit_BinaryOp(self, node: ast.BinaryOp) -> None
```

**描述**: 二元运算的语义分析。

特殊处理 `=` 运算符（公式赋值）：
- 左侧变量为引用（必须已定义）
- 右侧若为简单 Variable，视为公式输出（定义新变量）
例：端口+路径=连接 → 连接 被定义

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.BinaryOp` | |

**返回**: `None`

Source: `src\semantic.py:962`

---

### `_visit_UnaryOp`

```matha
def _visit_UnaryOp(self, node: ast.UnaryOp) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.UnaryOp` | |

**返回**: `None`

Source: `src\semantic.py:986`

---

### `_visit_Output`

```matha
def _visit_Output(self, node: ast.Output) -> None
```

**描述**: 输出 [...]：设置输出上下文标志，变量未定义降级为警告。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Output` | |

**返回**: `None`

Source: `src\semantic.py:989`

---

### `_visit_AngleExpr`

```matha
def _visit_AngleExpr(self, node: ast.AngleExpr) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.AngleExpr` | |

**返回**: `None`

Source: `src\semantic.py:997`

---

### `_visit_FuncApp`

```matha
def _visit_FuncApp(self, node: ast.FuncApp) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FuncApp` | |

**返回**: `None`

Source: `src\semantic.py:1000`

---

### `_visit_Lambda`

```matha
def _visit_Lambda(self, node: ast.Lambda) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Lambda` | |

**返回**: `None`

Source: `src\semantic.py:1004`

---

### `_visit_LetBinding`

```matha
def _visit_LetBinding(self, node: ast.LetBinding) -> None
```

**描述**: let x = val [in body] — 局部绑定：在符号表中声明变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.LetBinding` | |

**返回**: `None`

Source: `src\semantic.py:1013`

---

### `_visit_LetTupleBinding`

```matha
def _visit_LetTupleBinding(self, node: ast.LetTupleBinding) -> None
```

**描述**: let (a, b, ...) = val [in body] — 元组解构绑定。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.LetTupleBinding` | |

**返回**: `None`

Source: `src\semantic.py:1020`

---

### `_visit_IfExpr`

```matha
def _visit_IfExpr(self, node: ast.IfExpr) -> None
```

**描述**: 三元表达式 cond ? then : else — 解析三个子表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.IfExpr` | |

**返回**: `None`

Source: `src\semantic.py:1028`

---

### `_visit_PathExpr`

```matha
def _visit_PathExpr(self, node: ast.PathExpr) -> None
```

**描述**: 路径表达式 a.b（属性访问）或 a >> b（路径/距离）。

跨模块引用：模块名.成员 → 跳过成员解析（成员由导入模块定义）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.PathExpr` | |

**返回**: `None`

Source: `src\semantic.py:1035`

---

### `_visit_IfStmt`

```matha
def _visit_IfStmt(self, node: ast.IfStmt) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.IfStmt` | |

**返回**: `None`

Source: `src\semantic.py:1051`

---

### `_visit_MatchStmt`

```matha
def _visit_MatchStmt(self, node: ast.MatchStmt) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.MatchStmt` | |

**返回**: `None`

Source: `src\semantic.py:1057`

---

### `_visit_LoopStep`

```matha
def _visit_LoopStep(self, node: ast.LoopStep) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.LoopStep` | |

**返回**: `None`

Source: `src\semantic.py:1063`

---

### `_visit_GoStmt`

```matha
def _visit_GoStmt(self, node: ast.GoStmt) -> None
```

**描述**: go 启动并发任务/协程。

- go <任务名>：裸标识符视为任务名引用（类似命令名），不强制变量解析；
  若恰好已定义为函数/任务则视为正常引用。
- go <函数调用> / go <lambda>：正常解析（检查函数已定义、解析实参）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.GoStmt` | |

**返回**: `None`

Source: `src\semantic.py:1071`

---

### `_visit_IntegerLit`

```matha
def _visit_IntegerLit(self, node: ast.IntegerLit) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.IntegerLit` | |

**返回**: `None`

Source: `src\semantic.py:1088`

---

### `_visit_FloatLit`

```matha
def _visit_FloatLit(self, node: ast.FloatLit) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.FloatLit` | |

**返回**: `None`

Source: `src\semantic.py:1091`

---

### `_visit_StringLit`

```matha
def _visit_StringLit(self, node: ast.StringLit) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.StringLit` | |

**返回**: `None`

Source: `src\semantic.py:1094`

---

### `_visit_BoolLit`

```matha
def _visit_BoolLit(self, node: ast.BoolLit) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.BoolLit` | |

**返回**: `None`

Source: `src\semantic.py:1097`

---

### `_visit_Annotation`

```matha
def _visit_Annotation(self, node: ast.Annotation) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node` | `ast.Annotation` | |

**返回**: `None`

Source: `src\semantic.py:1100`

---

## `analyze_ast`

```matha
def analyze_ast(program: ast.Program, verbose: bool) -> list[SemanticError]
```

**描述**: 分析已解析的 AST，返回语义错误/警告列表。

适用于已调用 parse() 得到 program 的场景，避免重复解析。

| 参数 | 类型 | 说明 |
|------|------|------|
| `program` | `ast.Program` | |
| `verbose` | `bool` | |

**返回**: `list[SemanticError]`

Source: `src\semantic.py:1109`

---

## `analyze`

```matha
def analyze(program: ast.Program, verbose: bool) -> list[SemanticError]
```

**描述**: 分析 AST，返回语义错误/警告列表。

verbose=True 时输出详细调试日志（子文件引用、文件路径、循环后缀等）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `program` | `ast.Program` | |
| `verbose` | `bool` | |

**返回**: `list[SemanticError]`

Source: `src\semantic.py:1118`

---

## `analyze_source`

```matha
def analyze_source(source: str, verbose: bool) -> tuple[ast.Program, list[SemanticError]]
```

**描述**: 从源码分析，返回 (AST, 错误列表)。

verbose=True 时输出详细调试日志，便于排查子文件引用等潜在问题。

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `verbose` | `bool` | |

**返回**: `tuple[ast.Program, list[SemanticError]]`

Source: `src\semantic.py:1127`

---

# `stdlib.algebra`

```matha

```

**描述**: Matha v4.2 — 代数运算标准库

提供代数核心功能：
  - 多项式运算：加减乘除、因式分解
  - 方程求解：一元一次、一元二次
  - 因式分解：质因数、多项式因式
  - 代数化简：合并同类项、展开公式

数学表达：
  所有函数遵循代数结构定义，确保数学严谨性。

用法：
  from src.stdlib.algebra import (
      solve_quadratic, factor_polynomial,
      simplify_expression, expand_expression,
      gcd_poly, lcm_poly,
  )

Source: `src\stdlib\algebra.py:1`

---

## `Polynomial`

```matha
class Polynomial
```

**描述**: 多项式：a_n·x^n + ... + a_1·x + a_0

**装饰器**: dataclass

Source: `src\stdlib\algebra.py:31`

---

### `degree`

```matha
def degree(self) -> int
```

**描述**: 获取多项式次数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\stdlib\algebra.py:35`

---

### `evaluate`

```matha
def evaluate(self, x: float) -> float
```

**描述**: 求值：P(x)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\algebra.py:42`

---

### `__str__`

```matha
def __str__(self) -> str
```

**描述**: 转为可读字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\stdlib\algebra.py:49`

---

### `__add__`

```matha
def __add__(self, other: 'Polynomial') -> 'Polynomial'
```

**描述**: 多项式加法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'Polynomial'` | |

**返回**: `'Polynomial'`

Source: `src\stdlib\algebra.py:63`

---

### `__mul__`

```matha
def __mul__(self, other: 'Polynomial') -> 'Polynomial'
```

**描述**: 多项式乘法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'Polynomial'` | |

**返回**: `'Polynomial'`

Source: `src\stdlib\algebra.py:73`

---

## `solve_linear`

```matha
def solve_linear(a: float, b: float) -> Optional[float]
```

**描述**: 求解一元一次方程：ax + b = 0

数学定义：
  solve_linear: ℝ × ℝ → ℝ ∪ {None}
  solve_linear(a, b) = -b/a  if a ≠ 0, else None

Args:
    a: x 的系数
    b: 常数项

Returns:
    解 x，无解时返回 None

Examples:
    >>> solve_linear(2, -4)
    2.0
    >>> solve_linear(0, 5)
    None

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `float` | |
| `b` | `float` | |

**返回**: `Optional[float]`

**示例**:
```
>>> solve_linear(2, -4)
```

Source: `src\stdlib\algebra.py:86`

---

## `solve_quadratic`

```matha
def solve_quadratic(a: float, b: float, c: float) -> List[complex]
```

**描述**: 求解一元二次方程：ax² + bx + c = 0

数学定义：
  x = (-b ± √(b²-4ac)) / (2a)

Args:
    a, b, c: 方程系数

Returns:
    解列表（可能含复数）

Examples:
    >>> solve_quadratic(1, -3, 2)
    [2.0, 1.0]
    >>> solve_quadratic(1, 2, 5)
    [(-1+2j), (-1-2j)]

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `float` | |
| `b` | `float` | |
| `c` | `float` | |

**返回**: `List[complex]`

**示例**:
```
>>> solve_quadratic(1, -3, 2)
```

Source: `src\stdlib\algebra.py:112`

---

## `solve_system_linear`

```matha
def solve_system_linear(eqs: List[Tuple[float, float, float]]) -> Optional[Tuple[float, float]]
```

**描述**: 求解二元一次方程组：
  a₁x + b₁y = c₁
  a₂x + b₂y = c₂

Args:
    eqs: 方程系数列表 [(a1, b1, c1), (a2, b2, c2)]

Returns:
    (x, y) 或 None（无解/无穷多解）

| 参数 | 类型 | 说明 |
|------|------|------|
| `eqs` | `List[Tuple[float, float, float]]` | |

**返回**: `Optional[Tuple[float, float]]`

Source: `src\stdlib\algebra.py:150`

---

## `factor_integer`

```matha
def factor_integer(n: int) -> Dict[int, int]
```

**描述**: 整数因式分解：n = p1^a1 × p2^a2 × ... × pk^ak

返回 {素数: 指数} 字典。

Examples:
    >>> factor_integer(60)
    {2: 2, 3: 1, 5: 1}

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `Dict[int, int]`

**示例**:
```
>>> factor_integer(60)
```

Source: `src\stdlib\algebra.py:181`

---

## `factor_polynomial_simple`

```matha
def factor_polynomial_simple(coeffs: List[float]) -> str
```

**描述**: 简单多项式因式分解（仅处理二次多项式）。

对于 ax² + bx + c，尝试因式分解为 (px + q)(rx + s)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `coeffs` | `List[float]` | |

**返回**: `str`

Source: `src\stdlib\algebra.py:209`

---

## `simplify_expression`

```matha
def simplify_expression(expr: str) -> str
```

**描述**: 简化表达式（基础版）。

支持：
- 合并同类项
- 消除零项
- 简化常数和

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

Source: `src\stdlib\algebra.py:237`

---

## `expand_expression`

```matha
def expand_expression(expr: str) -> str
```

**描述**: 展开表达式（基础版）。

支持 (a+b)(c+d) 形式展开。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

Source: `src\stdlib\algebra.py:265`

---

## `poly_add`

```matha
def poly_add(p1: List[float], p2: List[float]) -> List[float]
```

**描述**: 多项式加法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `List[float]` | |
| `p2` | `List[float]` | |

**返回**: `List[float]`

Source: `src\stdlib\algebra.py:294`

---

## `poly_mul`

```matha
def poly_mul(p1: List[float], p2: List[float]) -> List[float]
```

**描述**: 多项式乘法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `List[float]` | |
| `p2` | `List[float]` | |

**返回**: `List[float]`

Source: `src\stdlib\algebra.py:305`

---

## `poly_div`

```matha
def poly_div(p1: List[float], p2: List[float]) -> Tuple[List[float], List[float]]
```

**描述**: 多项式除法：p1 = p2 × q + r

Returns:
    (quotient, remainder)

| 参数 | 类型 | 说明 |
|------|------|------|
| `p1` | `List[float]` | |
| `p2` | `List[float]` | |

**返回**: `Tuple[List[float], List[float]]`

Source: `src\stdlib\algebra.py:314`

---

## `poly_derivative`

```matha
def poly_derivative(p: List[float]) -> List[float]
```

**描述**: 多项式求导。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `List[float]` | |

**返回**: `List[float]`

Source: `src\stdlib\algebra.py:355`

---

## `poly_integral`

```matha
def poly_integral(p: List[float], c: float) -> List[float]
```

**描述**: 多项式积分（不定积分）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `List[float]` | |
| `c` | `float` | |

**返回**: `List[float]`

Source: `src\stdlib\algebra.py:362`

---

# `stdlib.arithmetic`

```matha

```

**描述**: Matha v4.2 — 算术运算标准库

提供数学计算核心功能：
  - 基本运算：加减乘除、幂、开方
  - 取整运算：向上/向下取整、四舍五入
  - 数学常数：π, e, φ 等
  - 高级函数：最大公约数、最小公倍数、阶乘、素数判断

数学表达：
  所有函数都遵循集合论定义，确保数学严谨性。

用法：
  from src.stdlib.arithmetic import (
      add, subtract, multiply, divide,
      power, sqrt, abs, round, floor, ceil,
      gcd, lcm, factorial, is_prime,
      PI, E, PHI,
  )

Source: `src\stdlib\arithmetic.py:1`

---

## `MathConstants`

```matha
class MathConstants
```

**描述**: 数学常数集合。

Source: `src\stdlib\arithmetic.py:31`

---

### `list_all`

```matha
def list_all(cls) -> dict
```

**描述**: 列出所有常数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `dict`

**装饰器**: classmethod

Source: `src\stdlib\arithmetic.py:48`

---

## `add`

```matha
def add(a: Union[int, float], b: Union[int, float]) -> Union[int, float]
```

**描述**: 加法运算：a + b

数学定义：
  add: ℝ × ℝ → ℝ
  add(a, b) = a + b

Args:
    a: 被加数
    b: 加数

Returns:
    和

Examples:
    >>> add(3, 5)
    8
    >>> add(2.5, 3.7)
    6.2

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Union[int, float]` | |
| `b` | `Union[int, float]` | |

**返回**: `Union[int, float]`

**示例**:
```
>>> add(3, 5)
```

Source: `src\stdlib\arithmetic.py:67`

---

## `subtract`

```matha
def subtract(a: Union[int, float], b: Union[int, float]) -> Union[int, float]
```

**描述**: 减法运算：a - b

数学定义：
  subtract: ℝ × ℝ → ℝ
  subtract(a, b) = a - b

Args:
    a: 被减数
    b: 减数

Returns:
    差

Examples:
    >>> subtract(10, 3)
    7

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Union[int, float]` | |
| `b` | `Union[int, float]` | |

**返回**: `Union[int, float]`

**示例**:
```
>>> subtract(10, 3)
```

Source: `src\stdlib\arithmetic.py:91`

---

## `multiply`

```matha
def multiply(a: Union[int, float], b: Union[int, float]) -> Union[int, float]
```

**描述**: 乘法运算：a × b

数学定义：
  multiply: ℝ × ℝ → ℝ
  multiply(a, b) = a × b

Args:
    a: 乘数
    b: 被乘数

Returns:
    积

Examples:
    >>> multiply(3, 5)
    15

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Union[int, float]` | |
| `b` | `Union[int, float]` | |

**返回**: `Union[int, float]`

**示例**:
```
>>> multiply(3, 5)
```

Source: `src\stdlib\arithmetic.py:113`

---

## `divide`

```matha
def divide(a: Union[int, float], b: Union[int, float]) -> Optional[float]
```

**描述**: 除法运算：a ÷ b

数学定义：
  divide: ℝ × (ℝ \ {0}) → ℝ
  divide(a, b) = a / b

Args:
    a: 被除数
    b: 除数（不能为 0）

Returns:
    商，除数为 0 时返回 None

Examples:
    >>> divide(10, 3)
    3.333...
    >>> divide(10, 0)
    None

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Union[int, float]` | |
| `b` | `Union[int, float]` | |

**返回**: `Optional[float]`

**示例**:
```
>>> divide(10, 3)
```

Source: `src\stdlib\arithmetic.py:135`

---

## `power`

```matha
def power(base: Union[int, float], exp: Union[int, float]) -> Union[int, float]
```

**描述**: 幂运算：base^exp

数学定义：
  power: ℝ × ℤ → ℝ
  power(base, exp) = base^exp

Args:
    base: 底数
    exp: 指数

Returns:
    幂

Examples:
    >>> power(2, 3)
    8
    >>> power(9, 0.5)
    3.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `base` | `Union[int, float]` | |
| `exp` | `Union[int, float]` | |

**返回**: `Union[int, float]`

**示例**:
```
>>> power(2, 3)
```

Source: `src\stdlib\arithmetic.py:161`

---

## `sqrt`

```matha
def sqrt(x: Union[int, float]) -> Optional[float]
```

**描述**: 平方根运算：√x

数学定义：
  sqrt: ℝ⁺ → ℝ
  sqrt(x) = x^(1/2)

Args:
    x: 被开方数（必须 ≥ 0）

Returns:
    平方根，x < 0 时返回 None

Examples:
    >>> sqrt(16)
    4.0
    >>> sqrt(-1)
    None

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Union[int, float]` | |

**返回**: `Optional[float]`

**示例**:
```
>>> sqrt(16)
```

Source: `src\stdlib\arithmetic.py:185`

---

## `abs_value`

```matha
def abs_value(x: Union[int, float]) -> Union[int, float]
```

**描述**: 绝对值运算：|x|

数学定义：
  abs: ℝ → ℝ⁺
  abs(x) = |x|

Args:
    x: 输入值

Returns:
    绝对值

Examples:
    >>> abs_value(-5)
    5
    >>> abs_value(3)
    3

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Union[int, float]` | |

**返回**: `Union[int, float]`

**示例**:
```
>>> abs_value(-5)
```

Source: `src\stdlib\arithmetic.py:210`

---

## `floor`

```matha
def floor(x: Union[int, float]) -> int
```

**描述**: 向下取整：⌊x⌋

数学定义：
  floor: ℝ → ℤ
  floor(x) = max{n ∈ ℤ | n ≤ x}

Args:
    x: 输入值

Returns:
    向下取整结果

Examples:
    >>> floor(3.7)
    3
    >>> floor(-2.3)
    -3

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Union[int, float]` | |

**返回**: `int`

**示例**:
```
>>> floor(3.7)
```

Source: `src\stdlib\arithmetic.py:237`

---

## `ceil`

```matha
def ceil(x: Union[int, float]) -> int
```

**描述**: 向上取整：⌈x⌉

数学定义：
  ceil: ℝ → ℤ
  ceil(x) = min{n ∈ ℤ | n ≥ x}

Args:
    x: 输入值

Returns:
    向上取整结果

Examples:
    >>> ceil(3.2)
    4
    >>> ceil(-2.7)
    -2

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Union[int, float]` | |

**返回**: `int`

**示例**:
```
>>> ceil(3.2)
```

Source: `src\stdlib\arithmetic.py:260`

---

## `round_value`

```matha
def round_value(x: Union[int, float], ndigits: int) -> Union[int, float]
```

**描述**: 四舍五入：round(x, ndigits)

数学定义：
  round: ℝ × ℕ → ℝ
  round(x, n) = ⌊x × 10^n + 0.5⌋ / 10^n

Args:
    x: 输入值
    ndigits: 小数位数（默认 0）

Returns:
    四舍五入结果

Examples:
    >>> round_value(3.7)
    4
    >>> round_value(3.14159, 2)
    3.14

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Union[int, float]` | |
| `ndigits` | `int` | |

**返回**: `Union[int, float]`

**示例**:
```
>>> round_value(3.7)
```

Source: `src\stdlib\arithmetic.py:283`

---

## `trunc`

```matha
def trunc(x: Union[int, float]) -> Union[int, float]
```

**描述**: 截断取整：向零取整

数学定义：
  trunc: ℝ → ℤ
  trunc(x) = sign(x) × ⌊|x|⌋

Args:
    x: 输入值

Returns:
    截断结果

Examples:
    >>> trunc(3.7)
    3
    >>> trunc(-2.3)
    -2

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Union[int, float]` | |

**返回**: `Union[int, float]`

**示例**:
```
>>> trunc(3.7)
```

Source: `src\stdlib\arithmetic.py:307`

---

## `gcd`

```matha
def gcd(a: int, b: int) -> int
```

**描述**: 最大公约数：gcd(a, b)

数学定义：
  gcd: ℤ × ℤ → ℕ
  gcd(a, b) = max{n ∈ ℕ | n|a ∧ n|b}

Args:
    a: 第一个整数
    b: 第二个整数

Returns:
    最大公约数

Examples:
    >>> gcd(12, 18)
    6
    >>> gcd(7, 13)
    1

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `int` | |
| `b` | `int` | |

**返回**: `int`

**示例**:
```
>>> gcd(12, 18)
```

Source: `src\stdlib\arithmetic.py:334`

---

## `lcm`

```matha
def lcm(a: int, b: int) -> int
```

**描述**: 最小公倍数：lcm(a, b)

数学定义：
  lcm: ℤ × ℤ → ℕ
  lcm(a, b) = min{n ∈ ℕ | a|n ∧ b|n}

Args:
    a: 第一个整数
    b: 第二个整数

Returns:
    最小公倍数

Examples:
    >>> lcm(4, 6)
    12
    >>> lcm(3, 5)
    15

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `int` | |
| `b` | `int` | |

**返回**: `int`

**示例**:
```
>>> lcm(4, 6)
```

Source: `src\stdlib\arithmetic.py:358`

---

## `factorial`

```matha
def factorial(n: int) -> int
```

**描述**: 阶乘：n!

数学定义：
  factorial: ℕ → ℕ
  factorial(n) = n! = ∏_{i=1}^{n} i
  factorial(0) = 1

Args:
    n: 非负整数

Returns:
    n 的阶乘

Examples:
    >>> factorial(5)
    120
    >>> factorial(0)
    1

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `int`

**示例**:
```
>>> factorial(5)
```

Source: `src\stdlib\arithmetic.py:384`

---

## `is_prime`

```matha
def is_prime(n: int) -> bool
```

**描述**: 素数判断：is_prime(n)

数学定义：
  is_prime: ℕ → {True, False}
  is_prime(n) = True ⟺ n > 1 ∧ ∀d∈ℕ, d|n → d=1 ∨ d=n

Args:
    n: 待判断的整数

Returns:
    是否为素数

Examples:
    >>> is_prime(7)
    True
    >>> is_prime(10)
    False

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `bool`

**示例**:
```
>>> is_prime(7)
```

Source: `src\stdlib\arithmetic.py:410`

---

## `prime_factors`

```matha
def prime_factors(n: int) -> List[int]
```

**描述**: 素因数分解：n = p1^a1 × p2^a2 × ... × pk^ak

数学定义：
  prime_factors: ℕ → ℤ*
  prime_factors(n) = [p1, p2, ..., pk] 其中 pi 是 n 的素因数

Args:
    n: 待分解的正整数

Returns:
    素因数列表（含重复）

Examples:
    >>> prime_factors(12)
    [2, 2, 3]
    >>> prime_factors(60)
    [2, 2, 3, 5]

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `List[int]`

**示例**:
```
>>> prime_factors(12)
```

Source: `src\stdlib\arithmetic.py:442`

---

## `sieve_of_eratosthenes`

```matha
def sieve_of_eratosthenes(limit: int) -> List[int]
```

**描述**: 埃拉托斯特尼筛法：找出 2 到 limit 之间的所有素数

数学定义：
  sieve: ℕ → ℤ*
  sieve(n) = {p ∈ ℤ | 2 ≤ p ≤ n ∧ is_prime(p)}

Args:
    limit: 上界

Returns:
    素数列表

Examples:
    >>> sieve_of_eratosthenes(20)
    [2, 3, 5, 7, 11, 13, 17, 19]

| 参数 | 类型 | 说明 |
|------|------|------|
| `limit` | `int` | |

**返回**: `List[int]`

**示例**:
```
>>> sieve_of_eratosthenes(20)
```

Source: `src\stdlib\arithmetic.py:477`

---

## `sin`

```matha
def sin(rad: float) -> float
```

**描述**: 正弦函数：sin(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `rad` | `float` | |

**返回**: `float`

Source: `src\stdlib\arithmetic.py:513`

---

## `cos`

```matha
def cos(rad: float) -> float
```

**描述**: 余弦函数：cos(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `rad` | `float` | |

**返回**: `float`

Source: `src\stdlib\arithmetic.py:518`

---

## `tan`

```matha
def tan(rad: float) -> float
```

**描述**: 正切函数：tan(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `rad` | `float` | |

**返回**: `float`

Source: `src\stdlib\arithmetic.py:523`

---

## `asin`

```matha
def asin(x: float) -> Optional[float]
```

**描述**: 反正弦函数：arcsin(x)，x ∈ [-1, 1]

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `Optional[float]`

Source: `src\stdlib\arithmetic.py:528`

---

## `acos`

```matha
def acos(x: float) -> Optional[float]
```

**描述**: 反余弦函数：arccos(x)，x ∈ [-1, 1]

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `Optional[float]`

Source: `src\stdlib\arithmetic.py:535`

---

## `atan`

```matha
def atan(x: float) -> float
```

**描述**: 反正切函数：arctan(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\arithmetic.py:542`

---

## `atan2`

```matha
def atan2(y: float, x: float) -> float
```

**描述**: 二参数反正切：arctan(y/x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `y` | `float` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\arithmetic.py:547`

---

## `log`

```matha
def log(x: float, base: float) -> Optional[float]
```

**描述**: 对数函数：log_base(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `base` | `float` | |

**返回**: `Optional[float]`

Source: `src\stdlib\arithmetic.py:556`

---

## `log2`

```matha
def log2(x: float) -> Optional[float]
```

**描述**: 以 2 为底的对数：log2(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `Optional[float]`

Source: `src\stdlib\arithmetic.py:563`

---

## `log10`

```matha
def log10(x: float) -> Optional[float]
```

**描述**: 以 10 为底的对数：log10(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |

**返回**: `Optional[float]`

Source: `src\stdlib\arithmetic.py:570`

---

## `combination`

```matha
def combination(n: int, k: int) -> Optional[int]
```

**描述**: 组合数：C(n, k) = n! / (k! × (n-k)!)

数学定义：
  C: ℕ × ℕ → ℕ
  C(n, k) = n! / (k! × (n-k)!)

Args:
    n: 总数
    k: 选取数

Returns:
    组合数，k > n 时返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `k` | `int` | |

**返回**: `Optional[int]`

Source: `src\stdlib\arithmetic.py:581`

---

## `permutation`

```matha
def permutation(n: int, k: int) -> Optional[int]
```

**描述**: 排列数：P(n, k) = n! / (n-k)!

数学定义：
  P: ℕ × ℕ → ℕ
  P(n, k) = n! / (n-k)!

Args:
    n: 总数
    k: 选取数

Returns:
    排列数，k > n 时返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |
| `k` | `int` | |

**返回**: `Optional[int]`

Source: `src\stdlib\arithmetic.py:601`

---

# `stdlib.calculus`

```matha

```

**描述**: Matha v4.2 — 微积分运算标准库

提供微积分核心功能：
  - 极限计算
  - 导数（数值/符号）
  - 积分（数值/符号）
  - 泰勒展开
  - 级数求和

数学表达：
  所有函数遵循微积分定义，确保数学严谨性。

用法：
  from src.stdlib.calculus import (
      derivative, integral, limit,
      taylor_series, infinite_sum,
      newton_method,
  )

Source: `src\stdlib\calculus.py:1`

---

## `derivative`

```matha
def derivative(f: Callable[[float], float], x: float, h: float) -> float
```

**描述**: 数值求导：f'(x) ≈ [f(x+h) - f(x-h)] / (2h)

数学定义：
  f'(x) = lim(h→0) [f(x+h) - f(x-h)] / (2h)

Args:
    f: 函数
    x: 求导点
    h: 步长（默认 1e-8）

Returns:
    导数值

Examples:
    >>> derivative(lambda x: x**2, 3)
    6.0
    >>> derivative(math.sin, math.pi/2)
    0.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `x` | `float` | |
| `h` | `float` | |

**返回**: `float`

**示例**:
```
>>> derivative(lambda x: x**2, 3)
```

Source: `src\stdlib\calculus.py:31`

---

## `second_derivative`

```matha
def second_derivative(f: Callable[[float], float], x: float, h: float) -> float
```

**描述**: 数值求二阶导数：f''(x) ≈ [f(x+h) - 2f(x) + f(x-h)] / h²

Examples:
    >>> second_derivative(lambda x: x**2, 3)
    2.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `x` | `float` | |
| `h` | `float` | |

**返回**: `float`

**示例**:
```
>>> second_derivative(lambda x: x**2, 3)
```

Source: `src\stdlib\calculus.py:55`

---

## `integral`

```matha
def integral(f: Callable[[float], float], a: float, b: float, n: int) -> float
```

**描述**: 数值积分（辛普森法则）：∫[a,b] f(x)dx

数学定义：
  ∫[a,b] f(x)dx ≈ (h/3)[f(x₀) + 4f(x₁) + 2f(x₂) + ... + f(xₙ)]

Args:
    f: 被积函数
    a: 下限
    b: 上限
    n: 分段数（偶数）

Returns:
    积分值

Examples:
    >>> integral(lambda x: x**2, 0, 1)
    0.3333...
    >>> integral(math.sin, 0, math.pi)
    2.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `a` | `float` | |
| `b` | `float` | |
| `n` | `int` | |

**返回**: `float`

**示例**:
```
>>> integral(lambda x: x**2, 0, 1)
```

Source: `src\stdlib\calculus.py:70`

---

## `numerical_integral_trapezoid`

```matha
def numerical_integral_trapezoid(f: Callable[[float], float], a: float, b: float, n: int) -> float
```

**描述**: 数值积分（梯形法则）：∫[a,b] f(x)dx

更简单但精度略低。

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `a` | `float` | |
| `b` | `float` | |
| `n` | `int` | |

**返回**: `float`

Source: `src\stdlib\calculus.py:104`

---

## `limit_forward`

```matha
def limit_forward(f: Callable[[float], float], x: float, direction: float) -> float
```

**描述**: 前向极限：lim(h→0+) f(x+h)

Args:
    f: 函数
    x: 趋近点
    direction: 趋近方向（1 为正，-1 为负）

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `x` | `float` | |
| `direction` | `float` | |

**返回**: `float`

Source: `src\stdlib\calculus.py:121`

---

## `limit_two_sided`

```matha
def limit_two_sided(f: Callable[[float], float], x: float, h: float) -> float
```

**描述**: 双侧极限：lim(x→x₀) f(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `x` | `float` | |
| `h` | `float` | |

**返回**: `float`

Source: `src\stdlib\calculus.py:133`

---

## `taylor_series`

```matha
def taylor_series(f: Callable[[float], float], x0: float, x: float, n: int) -> float
```

**描述**: 泰勒级数展开：f(x) ≈ Σ f^(k)(x₀) / k! × (x-x₀)^k

Args:
    f: 函数
    x0: 展开点
    x: 求值点
    n: 展开项数

Returns:
    泰勒近似值

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `x0` | `float` | |
| `x` | `float` | |
| `n` | `int` | |

**返回**: `float`

Source: `src\stdlib\calculus.py:148`

---

## `_numerical_derivative`

```matha
def _numerical_derivative(f: Callable[[float], float], x: float, n: int, h: float) -> float
```

**描述**: 数值计算 n 阶导数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `x` | `float` | |
| `n` | `int` | |
| `h` | `float` | |

**返回**: `float`

Source: `src\stdlib\calculus.py:173`

---

## `infinite_sum`

```matha
def infinite_sum(term_fn: Callable[[int], float], max_terms: int, tolerance: float) -> Tuple[float, int]
```

**描述**: 无穷级数求和：S = Σ term(n) for n in [0, ∞)

当连续项变化小于 tolerance 时停止。

Returns:
    (sum, terms_used)

| 参数 | 类型 | 说明 |
|------|------|------|
| `term_fn` | `Callable[[int], float]` | |
| `max_terms` | `int` | |
| `tolerance` | `float` | |

**返回**: `Tuple[float, int]`

Source: `src\stdlib\calculus.py:190`

---

## `newton_method`

```matha
def newton_method(f: Callable[[float], float], x0: float, tolerance: float, max_iter: int) -> Tuple[float, int]
```

**描述**: 牛顿法求根：f(x) = 0

迭代公式：x_{n+1} = x_n - f(x_n) / f'(x_n)

Args:
    f: 函数
    x0: 初始猜测
    tolerance: 收敛精度
    max_iter: 最大迭代次数

Returns:
    (root, iterations)

| 参数 | 类型 | 说明 |
|------|------|------|
| `f` | `Callable[[float], float]` | |
| `x0` | `float` | |
| `tolerance` | `float` | |
| `max_iter` | `int` | |

**返回**: `Tuple[float, int]`

Source: `src\stdlib\calculus.py:216`

---

## `gamma`

```matha
def gamma(n: float) -> float
```

**描述**: Gamma 函数：Γ(n) = (n-1)!

使用 Lanczos 近似。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `float` | |

**返回**: `float`

Source: `src\stdlib\calculus.py:250`

---

## `beta`

```matha
def beta(a: float, b: float) -> float
```

**描述**: Beta 函数：B(a,b) = Γ(a)Γ(b) / Γ(a+b)

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `float` | |
| `b` | `float` | |

**返回**: `float`

Source: `src\stdlib\calculus.py:278`

---

# `stdlib.calculus_symbolic`

```matha

```

**描述**: Matha v4.4 — 符号微积分标准库（SymPy 集成版）

提供符号微积分核心功能：
  - 符号求导：d/dx(f(x))
  - 符号积分：∫f(x)dx
  - 泰勒展开：f(x) ≈ Σ f^(n)(a)/n! * (x-a)^n
  - 极限计算：lim(x→a) f(x)
  - 级数求和：Σ a_n
  - 微分方程求解

数学表达：
  所有函数遵循微积分定义，确保数学严谨性。
  使用 SymPy 进行符号计算，支持任意精度。

用法：
  from src.stdlib.calculus_symbolic import (
      symbolic_derivative,
      symbolic_integral,
      taylor_series,
      limit,
      infinite_sum,
      solve_ode,
  )

Source: `src\stdlib\calculus_symbolic.py:1`

---

## `_DummySymbol`

```matha
class _DummySymbol
```

Source: `src\stdlib\calculus_symbolic.py:54`

---

## `symbolic_derivative`

```matha
def symbolic_derivative(expr: str, var: str) -> str
```

**描述**: 符号求导：f'(x) = d/dx(f(x))

数学定义：
  f'(x) = lim(h→0) [f(x+h) - f(x)] / h

Args:
    expr: 数学表达式字符串，如 "x**2 + 3*x + 1"
    var: 求导变量，默认为 "x"

Returns:
    导数表达式字符串

Examples:
    >>> symbolic_derivative("x**2", "x")
    '2*x'
    >>> symbolic_derivative("sin(x)", "x")
    'cos(x)'
    >>> symbolic_derivative("exp(x)*cos(x)", "x")
    'exp(x)*cos(x) - exp(x)*sin(x)'

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `var` | `str` | |

**返回**: `str`

**示例**:
```
>>> symbolic_derivative("x**2", "x")
```

Source: `src\stdlib\calculus_symbolic.py:80`

---

## `symbolic_partial_derivative`

```matha
def symbolic_partial_derivative(expr: str, var: str) -> str
```

**描述**: 符号偏导数：∂f/∂x

Examples:
    >>> symbolic_partial_derivative("x**2 + y**2", "x")
    '2*x'

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `var` | `str` | |

**返回**: `str`

**示例**:
```
>>> symbolic_partial_derivative("x**2 + y**2", "x")
```

Source: `src\stdlib\calculus_symbolic.py:114`

---

## `symbolic_integral`

```matha
def symbolic_integral(expr: str, var: str) -> str
```

**描述**: 符号积分：∫f(x)dx

数学定义：
  ∫f(x)dx = F(x) + C，其中 F'(x) = f(x)

Args:
    expr: 数学表达式字符串
    var: 积分变量，默认为 "x"

Returns:
    积分结果表达式字符串

Examples:
    >>> symbolic_integral("x**2", "x")
    'x**3/3'
    >>> symbolic_integral("sin(x)", "x")
    '-cos(x)'
    >>> symbolic_integral("1/x", "x")
    'log(x)'

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `var` | `str` | |

**返回**: `str`

**示例**:
```
>>> symbolic_integral("x**2", "x")
```

Source: `src\stdlib\calculus_symbolic.py:138`

---

## `definite_integral`

```matha
def definite_integral(expr: str, var: str, lower: float, upper: float) -> float
```

**描述**: 定积分：∫[lower, upper] f(x)dx

数学定义：
  ∫[a,b] f(x)dx = F(b) - F(a)

Args:
    expr: 数学表达式字符串
    var: 积分变量
    lower: 下界
    upper: 上界

Returns:
    定积分数值结果

Examples:
    >>> definite_integral("x**2", "x", 0, 1)
    0.3333333333333333
    >>> definite_integral("sin(x)", "x", 0, 3.141592653589793)
    2.0

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `var` | `str` | |
| `lower` | `float` | |
| `upper` | `float` | |

**返回**: `float`

**示例**:
```
>>> definite_integral("x**2", "x", 0, 1)
```

Source: `src\stdlib\calculus_symbolic.py:179`

---

## `taylor_series`

```matha
def taylor_series(expr: str, var: str, point: float, order: int) -> str
```

**描述**: 泰勒展开：f(x) ≈ Σ f^(n)(a)/n! * (x-a)^n

数学定义：
  f(x) = f(a) + f'(a)(x-a) + f''(a)/2!(x-a)^2 + ...

Args:
    expr: 数学表达式字符串
    var: 变量名
    point: 展开点，默认为 0（麦克劳林级数）
    order: 展开阶数

Returns:
    泰勒展开表达式字符串

Examples:
    >>> taylor_series("exp(x)", "x", 0, 5)
    'x**5/120 + x**4/24 + x**3/6 + x**2/2 + x + 1'
    >>> taylor_series("sin(x)", "x", 0, 5)
    'x**5/120 - x**3/6 + x'

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `var` | `str` | |
| `point` | `float` | |
| `order` | `int` | |

**返回**: `str`

**示例**:
```
>>> taylor_series("exp(x)", "x", 0, 5)
```

Source: `src\stdlib\calculus_symbolic.py:217`

---

## `limit`

```matha
def limit(expr: str, var: str, point: float, direction: str) -> float
```

**描述**: 极限计算：lim(x→point) f(x)

数学定义：
  lim(x→a) f(x) = L，当 x 趋近于 a 时 f(x) 趋近于 L

Args:
    expr: 数学表达式字符串
    var: 变量名
    point: 趋近点
    direction: 方向（"+" 右极限，"-" 左极限）

Returns:
    极限值

Examples:
    >>> limit("sin(x)/x", "x", 0)
    1.0
    >>> limit("1/x", "x", 0, "+")
    inf

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `var` | `str` | |
| `point` | `float` | |
| `direction` | `str` | |

**返回**: `float`

**示例**:
```
>>> limit("sin(x)/x", "x", 0)
```

Source: `src\stdlib\calculus_symbolic.py:255`

---

## `infinite_sum`

```matha
def infinite_sum(expr: str, var: str, lower: int, upper: Optional[int]) -> Union[str, float]
```

**描述**: 级数求和：Σ f(n)

数学定义：
  Σ[n=lower, upper] f(n)

Args:
    expr: 通项公式字符串
    var: 求和变量
    lower: 下界
    upper: 上界（None 表示无穷级数）

Returns:
    求和结果

Examples:
    >>> infinite_sum("1/n**2", "n", 1, None)
    1.6449340668482264  # π²/6
    >>> infinite_sum("x**n/factorial(n)", "n", 0, None)
    'exp(x)'

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `var` | `str` | |
| `lower` | `int` | |
| `upper` | `Optional[int]` | |

**返回**: `Union[str, float]`

**示例**:
```
>>> infinite_sum("1/n**2", "n", 1, None)
```

Source: `src\stdlib\calculus_symbolic.py:296`

---

## `solve_ode`

```matha
def solve_ode(ode: str, func_name: str, var: str) -> str
```

**描述**: 常微分方程求解

数学定义：
  求解 dy/dx = f(x, y) 的通解或特解

Args:
    ode: 微分方程字符串，如 "y'(x) = x*y(x)"
    func_name: 函数名
    var: 自变量名

Returns:
    解表达式字符串

Examples:
    >>> solve_ode("y'(x) = x*y(x)", "y", "x")
    'C1*exp(x**2/2)'

| 参数 | 类型 | 说明 |
|------|------|------|
| `ode` | `str` | |
| `func_name` | `str` | |
| `var` | `str` | |

**返回**: `str`

**示例**:
```
>>> solve_ode("y'(x) = x*y(x)", "y", "x")
```

Source: `src\stdlib\calculus_symbolic.py:337`

---

## `sympy_parse`

```matha
def sympy_parse(expr: str, namespace: Optional[dict]) -> any
```

**描述**: 解析数学表达式为 SymPy 对象

支持的运算符：
  +  -  *  /  **  (优先级与 Python 一致)
  ^  表示幂运算（会被替换为 **）

Args:
    expr: 数学表达式字符串
    namespace: 可选的命名空间字典

Returns:
    SymPy 表达式对象

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |
| `namespace` | `Optional[dict]` | |

**返回**: `any`

Source: `src\stdlib\calculus_symbolic.py:382`

---

## `latex_format`

```matha
def latex_format(expr: str) -> str
```

**描述**: 将表达式转换为 LaTeX 格式。

Examples:
    >>> latex_format("x**2 + 2*x + 1")
    'x^{2} + 2 x + 1'

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

**示例**:
```
>>> latex_format("x**2 + 2*x + 1")
```

Source: `src\stdlib\calculus_symbolic.py:427`

---

## `pretty_format`

```matha
def pretty_format(expr: str) -> str
```

**描述**: 将表达式转换为美观的文本格式。

Examples:
    >>> pretty_format("x**2 + 2*x + 1")
    'x² + 2x + 1'

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

**示例**:
```
>>> pretty_format("x**2 + 2*x + 1")
```

Source: `src\stdlib\calculus_symbolic.py:445`

---

## `batch_derivative`

```matha
def batch_derivative(expressions: List[str], var: str) -> List[str]
```

**描述**: 批量求导

Args:
    expressions: 表达式列表
    var: 变量名

Returns:
    导数表达式列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `expressions` | `List[str]` | |
| `var` | `str` | |

**返回**: `List[str]`

Source: `src\stdlib\calculus_symbolic.py:467`

---

## `batch_integral`

```matha
def batch_integral(expressions: List[str], var: str) -> List[str]
```

**描述**: 批量积分

Args:
    expressions: 表达式列表
    var: 变量名

Returns:
    积分表达式列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `expressions` | `List[str]` | |
| `var` | `str` | |

**返回**: `List[str]`

Source: `src\stdlib\calculus_symbolic.py:481`

---

## `batch_taylor`

```matha
def batch_taylor(expressions: List[str], var: str, point: float, order: int) -> List[str]
```

**描述**: 批量泰勒展开

Args:
    expressions: 表达式列表
    var: 变量名
    point: 展开点
    order: 阶数

Returns:
    泰勒展开表达式列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `expressions` | `List[str]` | |
| `var` | `str` | |
| `point` | `float` | |
| `order` | `int` | |

**返回**: `List[str]`

Source: `src\stdlib\calculus_symbolic.py:495`

---

## `SymbolicConstants`

```matha
class SymbolicConstants
```

**描述**: 符号数学常量。

Source: `src\stdlib\calculus_symbolic.py:515`

---

### `list_all`

```matha
def list_all(cls) -> dict
```

**描述**: 列出所有常量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `dict`

**装饰器**: classmethod

Source: `src\stdlib\calculus_symbolic.py:525`

---

# `stdlib.core`

```matha

```

**描述**: Matha 标准库 Core — v2.2

提供基础类型：Int, String, Bool, Array 的完整实现。
所有类型均支持 Matha 内建调用约定（函数式柯里化兼容）。

Source: `src\stdlib\core.py:1`

---

## `register_core_builtins`

```matha
def register_core_builtins(builtins: dict) -> None
```

**描述**: 将 Core 标准库注册到 Matha 内建函数表。

参数:
    builtins: 内建函数 dict，由 _build_domain_builtins 传入。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\stdlib\core.py:17`

---

## `_int_to_roman`

```matha
def _int_to_roman(n: int) -> str
```

**描述**: 整数转罗马数字。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `str`

Source: `src\stdlib\core.py:125`

---

## `_roman_to_int`

```matha
def _roman_to_int(s: str) -> int
```

**描述**: 罗马数字转整数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `int`

Source: `src\stdlib\core.py:138`

---

## `_int_factors`

```matha
def _int_factors(n: int) -> list
```

**描述**: 返回 n 的所有正因数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `list`

Source: `src\stdlib\core.py:151`

---

## `_int_is_prime`

```matha
def _int_is_prime(n: int) -> bool
```

**描述**: 判断 n 是否为素数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `bool`

Source: `src\stdlib\core.py:157`

---

## `_str_pad_left`

```matha
def _str_pad_left(s: Any, width: int, char: str) -> str
```

**描述**: 左填充字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `Any` | |
| `width` | `int` | |
| `char` | `str` | |

**返回**: `str`

Source: `src\stdlib\core.py:174`

---

## `_str_pad_right`

```matha
def _str_pad_right(s: Any, width: int, char: str) -> str
```

**描述**: 右填充字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `Any` | |
| `width` | `int` | |
| `char` | `str` | |

**返回**: `str`

Source: `src\stdlib\core.py:182`

---

## `_str_to_bool`

```matha
def _str_to_bool(s: str) -> bool
```

**描述**: 字符串转布尔。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `bool`

Source: `src\stdlib\core.py:190`

---

## `_array_reduce`

```matha
def _array_reduce(arr: list, fn, initial: Any) -> Any
```

**描述**: 数组归约。

| 参数 | 类型 | 说明 |
|------|------|------|
| `arr` | `list` | |
| `fn` | `` | |
| `initial` | `Any` | |

**返回**: `Any`

Source: `src\stdlib\core.py:195`

---

## `_array_flatten`

```matha
def _array_flatten(arr: list, depth: int) -> list
```

**描述**: 扁平化嵌套数组。

| 参数 | 类型 | 说明 |
|------|------|------|
| `arr` | `list` | |
| `depth` | `int` | |

**返回**: `list`

Source: `src\stdlib\core.py:206`

---

## `MathaType`

```matha
class MathaType
```

**描述**: Matha 运行时类型封装。

Source: `src\stdlib\core.py:221`

---

### `to_int`

```matha
def to_int(x: Any) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Any` | |

**返回**: `int`

**装饰器**: staticmethod

Source: `src\stdlib\core.py:225`

---

### `to_float`

```matha
def to_float(x: Any) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Any` | |

**返回**: `float`

**装饰器**: staticmethod

Source: `src\stdlib\core.py:235`

---

### `to_str`

```matha
def to_str(x: Any) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Any` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\stdlib\core.py:245`

---

### `to_bool`

```matha
def to_bool(x: Any) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Any` | |

**返回**: `bool`

**装饰器**: staticmethod

Source: `src\stdlib\core.py:253`

---

### `to_array`

```matha
def to_array(x: Any) -> list
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `Any` | |

**返回**: `list`

**装饰器**: staticmethod

Source: `src\stdlib\core.py:263`

---

### `coalesce`

```matha
def coalesce(*args) -> Any
```

**描述**: 返回第一个非 None 值。

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\stdlib\core.py:271`

---

# `stdlib.linear_algebra`

```matha

```

**描述**: Matha v4.4 — 矩阵运算标准库

提供线性代数核心功能：
  - 矩阵创建与操作
  - 矩阵乘法、转置、逆矩阵
  - 行列式、迹、秩
  - 特征值与特征向量
  - SVD 分解
  - 矩阵范数

数学表达：
  所有函数遵循线性代数定义，确保数学严谨性。

用法：
  from src.stdlib.linear_algebra import (
      Matrix,
      matrix_multiply,
      matrix_transpose,
      matrix_inverse,
      matrix_determinant,
      matrix_eigenvalues,
      svd_decompose,
  )

Source: `src\stdlib\linear_algebra.py:1`

---

## `_make_cache_key`

```matha
def _make_cache_key(A: 'Matrix') -> tuple
```

**描述**: 创建缓存键。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `'Matrix'` | |

**返回**: `tuple`

Source: `src\stdlib\linear_algebra.py:52`

---

## `_evict_cache`

```matha
def _evict_cache(cache: dict, max_size: int)
```

**描述**: 缓存淘汰：当缓存过大时，移除最早插入的条目。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cache` | `dict` | |
| `max_size` | `int` | |

Source: `src\stdlib\linear_algebra.py:57`

---

## `Matrix`

```matha
class Matrix
```

**描述**: 矩阵类：支持基本线性代数运算。

数学表示：
  A = [a_ij] ∈ R^(m×n)

用法：
  mat = Matrix([[1, 2], [3, 4]])
  result = mat.multiply(Matrix([[5, 6], [7, 8]]))

**装饰器**: dataclass

Source: `src\stdlib\linear_algebra.py:68`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 初始化矩阵属性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\stdlib\linear_algebra.py:84`

---

### `zeros`

```matha
def zeros(cls, rows: int, cols: int) -> 'Matrix'
```

**描述**: 创建零矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `rows` | `int` | |
| `cols` | `int` | |

**返回**: `'Matrix'`

**装饰器**: classmethod

Source: `src\stdlib\linear_algebra.py:95`

---

### `ones`

```matha
def ones(cls, rows: int, cols: int) -> 'Matrix'
```

**描述**: 创建全一矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `rows` | `int` | |
| `cols` | `int` | |

**返回**: `'Matrix'`

**装饰器**: classmethod

Source: `src\stdlib\linear_algebra.py:100`

---

### `identity`

```matha
def identity(cls, n: int) -> 'Matrix'
```

**描述**: 创建单位矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `n` | `int` | |

**返回**: `'Matrix'`

**装饰器**: classmethod

Source: `src\stdlib\linear_algebra.py:105`

---

### `random`

```matha
def random(cls, rows: int, cols: int, scale: float) -> 'Matrix'
```

**描述**: 创建随机矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `rows` | `int` | |
| `cols` | `int` | |
| `scale` | `float` | |

**返回**: `'Matrix'`

**装饰器**: classmethod

Source: `src\stdlib\linear_algebra.py:111`

---

### `rows`

```matha
def rows(self) -> int
```

**描述**: 返回行数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\stdlib\linear_algebra.py:118`

---

### `cols`

```matha
def cols(self) -> int
```

**描述**: 返回列数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\stdlib\linear_algebra.py:123`

---

### `shape`

```matha
def shape(self) -> Tuple[int, int]
```

**描述**: 返回矩阵形状。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Tuple[int, int]`

**装饰器**: property

Source: `src\stdlib\linear_algebra.py:128`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

**描述**: 返回矩阵的字符串表示。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\stdlib\linear_algebra.py:132`

---

### `__str__`

```matha
def __str__(self) -> str
```

**描述**: 返回矩阵的可读字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\stdlib\linear_algebra.py:136`

---

### `__add__`

```matha
def __add__(self, other: 'Matrix') -> 'Matrix'
```

**描述**: 矩阵加法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'Matrix'` | |

**返回**: `'Matrix'`

Source: `src\stdlib\linear_algebra.py:143`

---

### `__sub__`

```matha
def __sub__(self, other: 'Matrix') -> 'Matrix'
```

**描述**: 矩阵减法。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'Matrix'` | |

**返回**: `'Matrix'`

Source: `src\stdlib\linear_algebra.py:150`

---

### `__mul__`

```matha
def __mul__(self, other: Union['Matrix', float]) -> 'Matrix'
```

**描述**: 矩阵乘法或数乘。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `Union['Matrix', float]` | |

**返回**: `'Matrix'`

Source: `src\stdlib\linear_algebra.py:157`

---

### `__rmul__`

```matha
def __rmul__(self, other: float) -> 'Matrix'
```

**描述**: 标量左乘。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `float` | |

**返回**: `'Matrix'`

Source: `src\stdlib\linear_algebra.py:167`

---

### `__eq__`

```matha
def __eq__(self, other: object) -> bool
```

**描述**: 矩阵相等比较。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `object` | |

**返回**: `bool`

Source: `src\stdlib\linear_algebra.py:171`

---

## `matrix_multiply`

```matha
def matrix_multiply(A: Matrix, B: Matrix) -> Matrix
```

**描述**: 矩阵乘法：C = A × B

数学定义：
  c_ij = Σ_k a_ik * b_kj

Args:
    A: m×n 矩阵
    B: n×p 矩阵

Returns:
    C: m×p 矩阵

Raises:
    ValueError: 维度不匹配

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `B` | `Matrix` | |

**返回**: `Matrix`

Source: `src\stdlib\linear_algebra.py:185`

---

## `matrix_transpose`

```matha
def matrix_transpose(A: Matrix) -> Matrix
```

**描述**: 矩阵转置：A^T

数学定义：
  (A^T)_ij = A_ji

Args:
    A: m×n 矩阵

Returns:
    A^T: n×m 矩阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `Matrix`

Source: `src\stdlib\linear_algebra.py:219`

---

## `matrix_scale`

```matha
def matrix_scale(A: Matrix, scalar: float) -> Matrix
```

**描述**: 矩阵数乘：c × A

Args:
    A: 矩阵
    scalar: 标量

Returns:
    c × A

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `scalar` | `float` | |

**返回**: `Matrix`

Source: `src\stdlib\linear_algebra.py:236`

---

## `matrix_add`

```matha
def matrix_add(A: Matrix, B: Matrix) -> Matrix
```

**描述**: 矩阵加法：A + B

Args:
    A: m×n 矩阵
    B: m×n 矩阵

Returns:
    A + B

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `B` | `Matrix` | |

**返回**: `Matrix`

Source: `src\stdlib\linear_algebra.py:251`

---

## `matrix_subtract`

```matha
def matrix_subtract(A: Matrix, B: Matrix) -> Matrix
```

**描述**: 矩阵减法：A - B

Args:
    A: m×n 矩阵
    B: m×n 矩阵

Returns:
    A - B

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `B` | `Matrix` | |

**返回**: `Matrix`

Source: `src\stdlib\linear_algebra.py:265`

---

## `matrix_determinant`

```matha
def matrix_determinant(A: Matrix) -> float
```

**描述**: 矩阵行列式：det(A)

数学定义：
  det(A) = Σ (-1)^(i+j) * a_ij * M_ij

Args:
    A: n×n 方阵

Returns:
    行列式值

Raises:
    ValueError: 非方阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `float`

Source: `src\stdlib\linear_algebra.py:283`

---

## `matrix_trace`

```matha
def matrix_trace(A: Matrix) -> float
```

**描述**: 矩阵迹：tr(A) = Σ a_ii

Args:
    A: n×n 方阵

Returns:
    迹

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `float`

Source: `src\stdlib\linear_algebra.py:349`

---

## `matrix_rank`

```matha
def matrix_rank(A: Matrix) -> int
```

**描述**: 矩阵秩：rank(A)

数学定义：
  rank(A) = 最大线性无关行（列）数

Args:
    A: m×n 矩阵

Returns:
    秩

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `int`

Source: `src\stdlib\linear_algebra.py:365`

---

## `matrix_norm`

```matha
def matrix_norm(A: Matrix, ord: Optional[str]) -> float
```

**描述**: 矩阵范数

支持的范数：
  - 'fro': Frobenius 范数
  - 'inf': 无穷范数（行和最大值）
  - '1': 1-范数（列和最大值）
  - None: Frobenius 范数（默认）

Args:
    A: m×n 矩阵
    ord: 范数类型

Returns:
    范数值

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `ord` | `Optional[str]` | |

**返回**: `float`

Source: `src\stdlib\linear_algebra.py:412`

---

## `matrix_inverse`

```matha
def matrix_inverse(A: Matrix) -> Optional[Matrix]
```

**描述**: 矩阵求逆：A^(-1)

数学定义：
  A × A^(-1) = I

Args:
    A: n×n 可逆方阵

Returns:
    A^(-1)，不可逆时返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `Optional[Matrix]`

Source: `src\stdlib\linear_algebra.py:447`

---

## `matrix_eigenvalues`

```matha
def matrix_eigenvalues(A: Matrix) -> List[complex]
```

**描述**: 矩阵特征值：det(A - λI) = 0

数学定义：
  A × v = λ × v

Args:
    A: n×n 方阵

Returns:
    特征值列表

Note:
    使用 QR 算法近似计算，适用于中小型矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `List[complex]`

Source: `src\stdlib\linear_algebra.py:515`

---

## `_qr_eigenvalues`

```matha
def _qr_eigenvalues(A: Matrix) -> List[complex]
```

**描述**: QR 算法求特征值（简化版）。

使用 Iterated QR 分解逼近特征值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `List[complex]`

Source: `src\stdlib\linear_algebra.py:574`

---

## `_qr_decomposition`

```matha
def _qr_decomposition(A: List[List[float]]) -> Tuple[List[List[float]], List[List[float]]]
```

**描述**: QR 分解：A = QR

使用 Gram-Schmidt 正交化。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `List[List[float]]` | |

**返回**: `Tuple[List[List[float]], List[List[float]]]`

Source: `src\stdlib\linear_algebra.py:597`

---

## `svd_decompose`

```matha
def svd_decompose(A: Matrix) -> Tuple[Matrix, Matrix, Matrix]
```

**描述**: SVD 分解：A = U Σ V^T

数学定义：
  A = U × Σ × V^T

Args:
    A: m×n 矩阵

Returns:
    U: m×m 左奇异向量矩阵
    S: m×n 奇异值矩阵（对角线为奇异值）
    Vt: n×n V 的转置矩阵

Note:
    优先使用 NumPy 实现（性能更好），回退到纯 Python 实现。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `Tuple[Matrix, Matrix, Matrix]`

Source: `src\stdlib\linear_algebra.py:638`

---

## `_is_sparse_matrix`

```matha
def _is_sparse_matrix(A: Matrix, threshold: float) -> bool
```

**描述**: 检测矩阵是否为稀疏矩阵。

Args:
    A: m×n 矩阵
    threshold: 稀疏度阈值（默认 0.9，即 90% 元素为零）

Returns:
    如果是稀疏矩阵返回 True

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `threshold` | `float` | |

**返回**: `bool`

Source: `src\stdlib\linear_algebra.py:696`

---

## `svd_decompose_sparse`

```matha
def svd_decompose_sparse(A: Matrix, max_iter: int) -> Tuple[Matrix, Matrix, Matrix]
```

**描述**: 稀疏矩阵 SVD 分解（基于迭代法）。

使用 Lanczos 迭代或幂迭代法，适用于稀疏矩阵。

Args:
    A: m×n 稀疏矩阵
    max_iter: 最大迭代次数

Returns:
    U: m×m 左奇异向量矩阵
    S: m×n 奇异值矩阵
    Vt: n×n V 的转置矩阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `max_iter` | `int` | |

**返回**: `Tuple[Matrix, Matrix, Matrix]`

Source: `src\stdlib\linear_algebra.py:713`

---

## `_svd_from_eigenvalues`

```matha
def _svd_from_eigenvalues(A: Matrix) -> Tuple[Matrix, Matrix, Matrix]
```

**描述**: 从特征值分解推导 SVD（仅适用于对称矩阵）。

对于对称矩阵 A：
- 奇异值 σ_i = |λ_i|（特征值的绝对值）
- U 和 Vt 由特征向量矩阵决定

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `Tuple[Matrix, Matrix, Matrix]`

Source: `src\stdlib\linear_algebra.py:812`

---

## `_eigenvectors_with_values`

```matha
def _eigenvectors_with_values(A: Matrix) -> list
```

**描述**: 使用幂迭代法计算特征值和特征向量。

Returns:
    list of (eigenvalue, eigenvector) tuples

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `list`

Source: `src\stdlib\linear_algebra.py:851`

---

## `_svd_power_iteration`

```matha
def _svd_power_iteration(A: Matrix) -> Tuple[Matrix, Matrix, Matrix]
```

**描述**: 使用幂迭代法近似 SVD（简化版）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `Tuple[Matrix, Matrix, Matrix]`

Source: `src\stdlib\linear_algebra.py:900`

---

## `lu_decompose`

```matha
def lu_decompose(A: Matrix) -> Tuple[Matrix, Matrix]
```

**描述**: LU 分解：A = L × U

Args:
    A: n×n 方阵

Returns:
    L: 下三角矩阵
    U: 上三角矩阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `Tuple[Matrix, Matrix]`

Source: `src\stdlib\linear_algebra.py:951`

---

## `cholesky_decompose`

```matha
def cholesky_decompose(A: Matrix) -> Optional[Matrix]
```

**描述**: Cholesky 分解：A = L × L^T

Args:
    A: n×n 对称正定矩阵

Returns:
    L: 下三角矩阵

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |

**返回**: `Optional[Matrix]`

Source: `src\stdlib\linear_algebra.py:991`

---

## `solve_linear_system`

```matha
def solve_linear_system(A: Matrix, b: List[float]) -> Optional[List[float]]
```

**描述**: 求解线性方程组 Ax = b

使用高斯消元法。

Args:
    A: n×n 系数矩阵
    b: n×1 常数向量

Returns:
    x: n×1 解向量

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `Matrix` | |
| `b` | `List[float]` | |

**返回**: `Optional[List[float]]`

Source: `src\stdlib\linear_algebra.py:1027`

---

# `stdlib.logic`

```matha

```

**描述**: Matha v4.2 — 逻辑与证明标准库

提供数理逻辑核心功能：
  - 命题逻辑：与、或、非、蕴含、等价
  - 谓词逻辑：全称量词、存在量词
  - 集合运算：并、交、差、补
  - 证明辅助：真值表、归谬法

数学表达：
  所有函数遵循集合论与数理逻辑定义。

用法：
  from src.stdlib.logic import (
      AND, OR, NOT, IMPLIES, IFF,
      truth_table, prove_by_contradiction,
      set_union, set_intersection, set_difference,
  )

Source: `src\stdlib\logic.py:1`

---

## `AND`

```matha
def AND(p: bool, q: bool) -> bool
```

**描述**: 逻辑与：p ∧ q

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |
| `q` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:30`

---

## `OR`

```matha
def OR(p: bool, q: bool) -> bool
```

**描述**: 逻辑或：p ∨ q

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |
| `q` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:35`

---

## `NOT`

```matha
def NOT(p: bool) -> bool
```

**描述**: 逻辑非：¬p

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:40`

---

## `IMPLIES`

```matha
def IMPLIES(p: bool, q: bool) -> bool
```

**描述**: 逻辑蕴含：p → q（等价于 ¬p ∨ q）

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |
| `q` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:45`

---

## `IFF`

```matha
def IFF(p: bool, q: bool) -> bool
```

**描述**: 逻辑等价：p ↔ q

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |
| `q` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:50`

---

## `XOR`

```matha
def XOR(p: bool, q: bool) -> bool
```

**描述**: 异或：p ⊕ q

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |
| `q` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:55`

---

## `NAND`

```matha
def NAND(p: bool, q: bool) -> bool
```

**描述**: 与非：¬(p ∧ q)

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |
| `q` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:60`

---

## `NOR`

```matha
def NOR(p: bool, q: bool) -> bool
```

**描述**: 或非：¬(p ∨ q)

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | `bool` | |
| `q` | `bool` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:65`

---

## `truth_table`

```matha
def truth_table(num_vars: int, expr_fn: callable) -> List[dict]
```

**描述**: 生成真值表。

Args:
    num_vars: 变量数量
    expr_fn: 接受 bool 列表，返回 bool 的表达式函数

Returns:
    真值表行列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_vars` | `int` | |
| `expr_fn` | `callable` | |

**返回**: `List[dict]`

Source: `src\stdlib\logic.py:74`

---

## `print_truth_table`

```matha
def print_truth_table(num_vars: int, expr_fn: callable, var_names: List[str])
```

**描述**: 打印可读的真值表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_vars` | `int` | |
| `expr_fn` | `callable` | |
| `var_names` | `List[str]` | |

Source: `src\stdlib\logic.py:99`

---

## `set_union`

```matha
def set_union(a: Set, b: Set) -> Set
```

**描述**: 集合并：A ∪ B

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Set` | |
| `b` | `Set` | |

**返回**: `Set`

Source: `src\stdlib\logic.py:121`

---

## `set_intersection`

```matha
def set_intersection(a: Set, b: Set) -> Set
```

**描述**: 集合交：A ∩ B

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Set` | |
| `b` | `Set` | |

**返回**: `Set`

Source: `src\stdlib\logic.py:126`

---

## `set_difference`

```matha
def set_difference(a: Set, b: Set) -> Set
```

**描述**: 集合差：A \ B

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Set` | |
| `b` | `Set` | |

**返回**: `Set`

Source: `src\stdlib\logic.py:131`

---

## `set_complement`

```matha
def set_complement(a: Set, universe: Set) -> Set
```

**描述**: 集合补：A' = U \ A

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Set` | |
| `universe` | `Set` | |

**返回**: `Set`

Source: `src\stdlib\logic.py:136`

---

## `set_symmetric_difference`

```matha
def set_symmetric_difference(a: Set, b: Set) -> Set
```

**描述**: 对称差：A △ B = (A \ B) ∪ (B \ A)

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Set` | |
| `b` | `Set` | |

**返回**: `Set`

Source: `src\stdlib\logic.py:141`

---

## `is_subset`

```matha
def is_subset(a: Set, b: Set) -> bool
```

**描述**: 子集判断：A ⊆ B

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Set` | |
| `b` | `Set` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:146`

---

## `is_proper_subset`

```matha
def is_proper_subset(a: Set, b: Set) -> bool
```

**描述**: 真子集判断：A ⊂ B

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `Set` | |
| `b` | `Set` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:151`

---

## `prove_by_contradiction`

```matha
def prove_by_contradiction(assumption_fn: callable, contradiction_fn: callable) -> bool
```

**描述**: 归谬法证明辅助。

如果假设 P 导致矛盾，则 P 为假。

Args:
    assumption_fn: 假设函数，返回假设下的结果
    contradiction_fn: 矛盾检测函数

Returns:
    True 如果检测到矛盾（证明假设不成立）

| 参数 | 类型 | 说明 |
|------|------|------|
| `assumption_fn` | `callable` | |
| `contradiction_fn` | `callable` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:160`

---

## `check_tautology`

```matha
def check_tautology(num_vars: int, expr_fn: callable) -> bool
```

**描述**: 检查是否为永真式（重言式）。

Args:
    num_vars: 变量数
    expr_fn: 表达式函数

Returns:
    True 如果所有赋值下表达式均为 True

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_vars` | `int` | |
| `expr_fn` | `callable` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:185`

---

## `check_contradiction`

```matha
def check_contradiction(num_vars: int, expr_fn: callable) -> bool
```

**描述**: 检查是否为矛盾式。

Returns:
    True 如果所有赋值下表达式均为 False

| 参数 | 类型 | 说明 |
|------|------|------|
| `num_vars` | `int` | |
| `expr_fn` | `callable` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:202`

---

## `Predicate`

```matha
class Predicate
```

**描述**: 谓词：定义在有限域上的命题函数。

**装饰器**: dataclass

Source: `src\stdlib\logic.py:220`

---

### `evaluate`

```matha
def evaluate(self, element) -> bool
```

**描述**: 求值：P(element)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `element` | `` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:226`

---

### `exists`

```matha
def exists(self) -> bool
```

**描述**: 存在量词：∃x, P(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:230`

---

### `forall`

```matha
def forall(self) -> bool
```

**描述**: 全称量词：∀x, P(x)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\stdlib\logic.py:234`

---

### `count`

```matha
def count(self) -> int
```

**描述**: 满足谓词的元素个数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\stdlib\logic.py:238`

---

## `InferenceEngine`

```matha
class InferenceEngine
```

**描述**: 简单推理引擎，支持基本推理规则。

支持规则：
- Modus Ponens: P → Q, P ⊢ Q
- Modus Tollens: P → Q, ¬Q ⊢ ¬P
- Hypothetical Syllogism: P → Q, Q → R ⊢ P → R
- Disjunctive Syllogism: P ∨ Q, ¬P ⊢ Q

Source: `src\stdlib\logic.py:247`

---

### `modus_ponens`

```matha
def modus_ponens(p_implies_q: bool, p: bool) -> Optional[bool]
```

**描述**: Modus Ponens: (P → Q) ∧ P ⊢ Q

Returns:
    Q 的值，如果前提不一致返回 None

| 参数 | 类型 | 说明 |
|------|------|------|
| `p_implies_q` | `bool` | |
| `p` | `bool` | |

**返回**: `Optional[bool]`

**装饰器**: staticmethod

Source: `src\stdlib\logic.py:259`

---

### `modus_tollens`

```matha
def modus_tollens(p_implies_q: bool, not_q: bool) -> Optional[bool]
```

**描述**: Modus Tollens: (P → Q) ∧ ¬Q ⊢ ¬P

| 参数 | 类型 | 说明 |
|------|------|------|
| `p_implies_q` | `bool` | |
| `not_q` | `bool` | |

**返回**: `Optional[bool]`

**装饰器**: staticmethod

Source: `src\stdlib\logic.py:271`

---

### `hypothetical_syllogism`

```matha
def hypothetical_syllogism(p_implies_q: bool, q_implies_r: bool) -> Optional[bool]
```

**描述**: Hypothetical Syllogism: (P → Q) ∧ (Q → R) ⊢ (P → R)

| 参数 | 类型 | 说明 |
|------|------|------|
| `p_implies_q` | `bool` | |
| `q_implies_r` | `bool` | |

**返回**: `Optional[bool]`

**装饰器**: staticmethod

Source: `src\stdlib\logic.py:280`

---

### `disjunctive_syllogism`

```matha
def disjunctive_syllogism(p_or_q: bool, not_p: bool) -> Optional[bool]
```

**描述**: Disjunctive Syllogism: (P ∨ Q) ∧ ¬P ⊢ Q

| 参数 | 类型 | 说明 |
|------|------|------|
| `p_or_q` | `bool` | |
| `not_p` | `bool` | |

**返回**: `Optional[bool]`

**装饰器**: staticmethod

Source: `src\stdlib\logic.py:289`

---

# `stdlib.probability_stats`

```matha

```

**描述**: Matha v4.4 — 概率统计模块示例

提供概率论与数理统计核心功能：
  - 概率分布：正态分布、二项分布、泊松分布、指数分布
  - 假设检验：Z 检验、t 检验、卡方检验
  - 回归分析：线性回归、多项式回归
  - 统计量：均值、方差、标准差、相关系数

数学表达：
  所有函数遵循概率论与数理统计定义。

用法：
  from src.stdlib.probability_stats import (
      NormalDistribution,
      z_test, t_test, chi_square_test,
      linear_regression,
      correlation,
  )

Source: `src\stdlib\probability_stats.py:1`

---

## `Distribution`

```matha
class Distribution(Enum)
```

**描述**: 概率分布类型。

Source: `src\stdlib\probability_stats.py:32`

---

## `NormalDistribution`

```matha
class NormalDistribution
```

**描述**: 正态分布：N(μ, σ²)

概率密度函数：
  f(x) = (1/σ√(2π)) × exp(-(x-μ)²/(2σ²))

用法：
  dist = NormalDistribution(mu=0, sigma=1)
  pdf = dist.pdf(1.0)       # 概率密度
  cdf = dist.cdf(1.0)       # 累积分布
  ppf = dist.ppf(0.95)      # 分位数

**装饰器**: dataclass

Source: `src\stdlib\probability_stats.py:42`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 验证参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\stdlib\probability_stats.py:59`

---

### `pdf`

```matha
def pdf(self, x: float) -> float
```

**描述**: 概率密度函数：P(X = x)

数学定义：
  f(x) = (1/σ√(2π)) × exp(-(x-μ)²/(2σ²))

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:64`

---

### `cdf`

```matha
def cdf(self, x: float) -> float
```

**描述**: 累积分布函数：P(X ≤ x)

使用误差函数近似计算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:74`

---

### `ppf`

```matha
def ppf(self, p: float) -> float
```

**描述**: 分位数函数：F^(-1)(p)

找到 x 使得 P(X ≤ x) = p

Args:
    p: 概率值 (0, 1)

Returns:
    分位数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `p` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:83`

---

### `mean`

```matha
def mean(self) -> float
```

**描述**: 期望：E[X] = μ

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:112`

---

### `variance`

```matha
def variance(self) -> float
```

**描述**: 方差：Var(X) = σ²

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:116`

---

### `std`

```matha
def std(self) -> float
```

**描述**: 标准差：σ

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:120`

---

## `BinomialDistribution`

```matha
class BinomialDistribution
```

**描述**: 二项分布：B(n, p)

概率质量函数：
  P(X = k) = C(n,k) × p^k × (1-p)^(n-k)

用法：
  dist = BinomialDistribution(n=10, p=0.5)
  pmf = dist.pmf(5)      # P(X = 5)
  cdf = dist.cdf(5)       # P(X ≤ 5)

**装饰器**: dataclass

Source: `src\stdlib\probability_stats.py:126`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 验证参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\stdlib\probability_stats.py:142`

---

### `pmf`

```matha
def pmf(self, k: int) -> float
```

**描述**: 概率质量函数：P(X = k)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `k` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:149`

---

### `cdf`

```matha
def cdf(self, k: int) -> float
```

**描述**: 累积分布函数：P(X ≤ k)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `k` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:157`

---

### `mean`

```matha
def mean(self) -> float
```

**描述**: 期望：E[X] = np

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:163`

---

### `variance`

```matha
def variance(self) -> float
```

**描述**: 方差：Var(X) = np(1-p)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:167`

---

## `PoissonDistribution`

```matha
class PoissonDistribution
```

**描述**: 泊松分布：Poisson(λ)

概率质量函数：
  P(X = k) = (λ^k × e^(-λ)) / k!

用法：
  dist = PoissonDistribution(lambda_=3.0)
  pmf = dist.pmf(2)      # P(X = 2)
  cdf = dist.cdf(2)       # P(X ≤ 2)

**装饰器**: dataclass

Source: `src\stdlib\probability_stats.py:177`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 验证参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\stdlib\probability_stats.py:192`

---

### `pmf`

```matha
def pmf(self, k: int) -> float
```

**描述**: 概率质量函数：P(X = k)

数学定义：
  P(X = k) = (λ^k × e^(-λ)) / k!

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `k` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:197`

---

### `cdf`

```matha
def cdf(self, k: int) -> float
```

**描述**: 累积分布函数：P(X ≤ k)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `k` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:208`

---

### `mean`

```matha
def mean(self) -> float
```

**描述**: 期望：E[X] = λ

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:214`

---

### `variance`

```matha
def variance(self) -> float
```

**描述**: 方差：Var(X) = λ

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:218`

---

### `std`

```matha
def std(self) -> float
```

**描述**: 标准差：σ = √λ

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:222`

---

## `ExponentialDistribution`

```matha
class ExponentialDistribution
```

**描述**: 指数分布：Exp(λ)

概率密度函数：
  f(x) = λ × e^(-λx), x ≥ 0

用法：
  dist = ExponentialDistribution(lambda_=1.0)
  pdf = dist.pdf(1.0)       # 概率密度
  cdf = dist.cdf(1.0)       # 累积分布

**装饰器**: dataclass

Source: `src\stdlib\probability_stats.py:232`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 验证参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\stdlib\probability_stats.py:247`

---

### `pdf`

```matha
def pdf(self, x: float) -> float
```

**描述**: 概率密度函数：f(x) = λ × e^(-λx), x ≥ 0

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:252`

---

### `cdf`

```matha
def cdf(self, x: float) -> float
```

**描述**: 累积分布函数：F(x) = 1 - e^(-λx), x ≥ 0

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:260`

---

### `ppf`

```matha
def ppf(self, p: float) -> float
```

**描述**: 分位数函数：F^(-1)(p) = -ln(1-p) / λ

Args:
    p: 概率值 (0, 1)

Returns:
    分位数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `p` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:268`

---

### `mean`

```matha
def mean(self) -> float
```

**描述**: 期望：E[X] = 1/λ

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:282`

---

### `variance`

```matha
def variance(self) -> float
```

**描述**: 方差：Var(X) = 1/λ²

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:286`

---

### `std`

```matha
def std(self) -> float
```

**描述**: 标准差：σ = 1/λ

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:290`

---

## `UniformDistribution`

```matha
class UniformDistribution
```

**描述**: 连续均匀分布：U(a, b)

概率密度函数：
  f(x) = 1/(b-a), a ≤ x ≤ b

用法：
  dist = UniformDistribution(a=0, b=1)
  pdf = dist.pdf(0.5)       # 概率密度
  cdf = dist.cdf(0.5)       # 累积分布

**装饰器**: dataclass

Source: `src\stdlib\probability_stats.py:300`

---

### `__post_init__`

```matha
def __post_init__(self)
```

**描述**: 验证参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\stdlib\probability_stats.py:316`

---

### `pdf`

```matha
def pdf(self, x: float) -> float
```

**描述**: 概率密度函数：f(x) = 1/(b-a), a ≤ x ≤ b

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:321`

---

### `cdf`

```matha
def cdf(self, x: float) -> float
```

**描述**: 累积分布函数：F(x) = (x-a)/(b-a), a ≤ x ≤ b

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:329`

---

### `ppf`

```matha
def ppf(self, p: float) -> float
```

**描述**: 分位数函数：F^(-1)(p) = a + p(b-a)

Args:
    p: 概率值 [0, 1]

Returns:
    分位数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `p` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:339`

---

### `mean`

```matha
def mean(self) -> float
```

**描述**: 期望：E[X] = (a+b)/2

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:353`

---

### `variance`

```matha
def variance(self) -> float
```

**描述**: 方差：Var(X) = (b-a)²/12

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:357`

---

### `std`

```matha
def std(self) -> float
```

**描述**: 标准差：σ = (b-a)/√12

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:361`

---

### `sample`

```matha
def sample(self, n: int) -> List[float]
```

**描述**: 生成随机样本

Args:
    n: 样本数量

Returns:
    样本列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |

**返回**: `List[float]`

Source: `src\stdlib\probability_stats.py:365`

---

## `mean`

```matha
def mean(data: List[float]) -> float
```

**描述**: 样本均值：x̄ = (1/n)Σx_i

Args:
    data: 样本数据

Returns:
    均值

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `List[float]` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:383`

---

## `variance`

```matha
def variance(data: List[float], ddof: int) -> float
```

**描述**: 样本方差：s² = Σ(x_i - x̄)² / (n - ddof)

Args:
    data: 样本数据
    ddof: 自由度修正（默认 1，样本方差）

Returns:
    方差

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `List[float]` | |
| `ddof` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:398`

---

## `std`

```matha
def std(data: List[float], ddof: int) -> float
```

**描述**: 样本标准差：s = √s²

Args:
    data: 样本数据
    ddof: 自由度修正

Returns:
    标准差

| 参数 | 类型 | 说明 |
|------|------|------|
| `data` | `List[float]` | |
| `ddof` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:415`

---

## `correlation`

```matha
def correlation(x: List[float], y: List[float]) -> float
```

**描述**: 皮尔逊相关系数：r = cov(X,Y) / (σ_X × σ_Y)

数学定义：
  r = Σ(x_i - x̄)(y_i - ȳ) / √(Σ(x_i - x̄)² × Σ(y_i - ȳ)²)

Args:
    x: 变量 X 的数据
    y: 变量 Y 的数据

Returns:
    相关系数 (-1 到 1)

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `List[float]` | |
| `y` | `List[float]` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:429`

---

## `z_test`

```matha
def z_test(sample: List[float], population_mean: float, population_std: Optional[float], sample_std: Optional[float]) -> Tuple[float, float]
```

**描述**: Z 检验：检验样本均值与总体均值的差异

数学定义：
  Z = (x̄ - μ) / (σ/√n)

当总体标准差已知时使用。

Args:
    sample: 样本数据
    population_mean: 总体均值 μ
    population_std: 总体标准差 σ（可选）
    sample_std: 样本标准差 s（当 population_std 未知时使用）

Returns:
    (z_statistic, p_value)

Examples:
    # 检验样本均值是否等于 100
    sample = [98, 102, 100, 99, 101]
    z, p = z_test(sample, population_mean=100, population_std=5)

| 参数 | 类型 | 说明 |
|------|------|------|
| `sample` | `List[float]` | |
| `population_mean` | `float` | |
| `population_std` | `Optional[float]` | |
| `sample_std` | `Optional[float]` | |

**返回**: `Tuple[float, float]`

Source: `src\stdlib\probability_stats.py:461`

---

## `t_test`

```matha
def t_test(sample: List[float], population_mean: float) -> Tuple[float, float]
```

**描述**: t 检验：检验样本均值与假设均值的差异

数学定义：
  t = (x̄ - μ) / (s/√n)

当总体标准差未知时使用。

Args:
    sample: 样本数据
    population_mean: 假设的总体均值（默认 0）

Returns:
    (t_statistic, p_value)

| 参数 | 类型 | 说明 |
|------|------|------|
| `sample` | `List[float]` | |
| `population_mean` | `float` | |

**返回**: `Tuple[float, float]`

Source: `src\stdlib\probability_stats.py:509`

---

## `chi_square_test`

```matha
def chi_square_test(observed: List[int], expected: Optional[List[float]]) -> Tuple[float, float]
```

**描述**: 卡方检验：检验观测频数与期望频数的差异

数学定义：
  χ² = Σ(O_i - E_i)² / E_i

Args:
    observed: 观测频数
    expected: 期望频数（可选，默认等概率）

Returns:
    (chi_square_statistic, p_value)

| 参数 | 类型 | 说明 |
|------|------|------|
| `observed` | `List[int]` | |
| `expected` | `Optional[List[float]]` | |

**返回**: `Tuple[float, float]`

Source: `src\stdlib\probability_stats.py:542`

---

## `_chi_square_cdf`

```matha
def _chi_square_cdf(x: float, k: int) -> float
```

**描述**: 卡方分布累积分布函数（近似）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `float` | |
| `k` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:580`

---

## `_lower_incomplete_gamma`

```matha
def _lower_incomplete_gamma(a: float, x: float) -> float
```

**描述**: 下不完全伽马函数（连分数展开）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `float` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:599`

---

## `_upper_incomplete_gamma`

```matha
def _upper_incomplete_gamma(a: float, x: float) -> float
```

**描述**: 上不完全伽马函数（连分数展开）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `a` | `float` | |
| `x` | `float` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:624`

---

## `RegressionResult`

```matha
class RegressionResult
```

**描述**: 回归分析结果。

**装饰器**: dataclass

Source: `src\stdlib\probability_stats.py:661`

---

## `linear_regression`

```matha
def linear_regression(x: List[float], y: List[float]) -> RegressionResult
```

**描述**: 一元线性回归：y = β₀ + β₁x + ε

数学定义：
  β₁ = Σ(x_i - x̄)(y_i - ȳ) / Σ(x_i - x̄)²
  β₀ = ȳ - β₁x̄

Args:
    x: 自变量数据
    y: 因变量数据

Returns:
    回归结果

Examples:
    x = [1, 2, 3, 4, 5]
    y = [2, 4, 5, 4, 5]
    result = linear_regression(x, y)
    print(f"y = {result.coefficients[1]:.4f}x + {result.coefficients[0]:.4f}")
    print(f"R² = {result.r_squared:.4f}")

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `List[float]` | |
| `y` | `List[float]` | |

**返回**: `RegressionResult`

Source: `src\stdlib\probability_stats.py:670`

---

## `polynomial_regression`

```matha
def polynomial_regression(x: List[float], y: List[float], degree: int) -> RegressionResult
```

**描述**: 多项式回归：y = β₀ + β₁x + β₂x² + ... + β_dx^d

数学定义：
  通过最小二乘法求解回归系数

Args:
    x: 自变量数据
    y: 因变量数据
    degree: 多项式次数

Returns:
    回归结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `x` | `List[float]` | |
| `y` | `List[float]` | |
| `degree` | `int` | |

**返回**: `RegressionResult`

Source: `src\stdlib\probability_stats.py:733`

---

## `_matrix_transpose_multiply`

```matha
def _matrix_transpose_multiply(A: List[List[float]]) -> List[List[float]]
```

**描述**: 矩阵转置乘矩阵。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `List[List[float]]` | |

**返回**: `List[List[float]]`

Source: `src\stdlib\probability_stats.py:789`

---

## `_matrix_vector_multiply_transpose`

```matha
def _matrix_vector_multiply_transpose(A: List[List[float]], v: List[float]) -> List[float]
```

**描述**: 矩阵转置乘向量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `List[List[float]]` | |
| `v` | `List[float]` | |

**返回**: `List[float]`

Source: `src\stdlib\probability_stats.py:800`

---

## `_solve_linear_system`

```matha
def _solve_linear_system(A: List[List[float]], b: List[float]) -> List[float]
```

**描述**: 高斯消元求解线性方程组。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `List[List[float]]` | |
| `b` | `List[float]` | |

**返回**: `List[float]`

Source: `src\stdlib\probability_stats.py:810`

---

## `_matrix_vector_multiply`

```matha
def _matrix_vector_multiply(A: List[List[float]], v: List[float], row_idx: int) -> float
```

**描述**: 矩阵乘向量（指定行）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `A` | `List[List[float]]` | |
| `v` | `List[float]` | |
| `row_idx` | `int` | |

**返回**: `float`

Source: `src\stdlib\probability_stats.py:840`

---

# `stdlib_extend`

```matha

```

**描述**: Matha 标准库补全：re/hashlib/sqlite3/pathlib/asyncio/csv/logging等。

Source: `src\stdlib_extend.py:1`

---

## `_register_standard_library`

```matha
def _register_standard_library(builtins: dict) -> None
```

**描述**: 将标准库函数注册为 Matha 内建。

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\stdlib_extend.py:20`

---

## `register_all`

```matha
def register_all(builtins: dict) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `builtins` | `dict` | |

**返回**: `None`

Source: `src\stdlib_extend.py:142`

---

# `symbol_codegen`

```matha

```

**描述**: Matha 代码生成引擎 v1.3.0
===========================
将 Matha 符号表达式和 AST 转换为多种目标语言的执行代码。

目标语言：
  • Python  — 直接执行
  • JavaScript — 浏览器/Node.js
  • C/C++  — 高性能计算
  • Matha   — 自举回 Matha 语法

功能：
  • codegen_python   — 生成 Python 代码
  • codegen_javascript — 生成 JS 代码
  • codegen_c        — 生成 C 代码
  • codegen_matha    — 生成 Matha 代码
  • generate_driver  — 生成硬件驱动代码
  • generate_library — 生成可导入的库文件

Source: `src\symbol_codegen.py:1`

---

## `CodeGenerator`

```matha
class CodeGenerator
```

**描述**: 代码生成基类。

Source: `src\symbol_codegen.py:35`

---

### `__init__`

```matha
def __init__(self, lang: str, indent: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `lang` | `str` | |
| `indent` | `str` | |

Source: `src\symbol_codegen.py:38`

---

### `emit`

```matha
def emit(self, line: str)
```

**描述**: 输出一行代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `line` | `str` | |

Source: `src\symbol_codegen.py:44`

---

### `block`

```matha
def block(self, body_fn)
```

**描述**: 代码块缩进。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `body_fn` | `` | |

Source: `src\symbol_codegen.py:48`

---

### `get_code`

```matha
def get_code(self) -> str
```

**描述**: 获取生成的代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\symbol_codegen.py:54`

---

## `PythonGenerator`

```matha
class PythonGenerator(CodeGenerator)
```

**描述**: 生成 Python 代码。

Source: `src\symbol_codegen.py:63`

---

### `__init__`

```matha
def __init__(self, func_name: str, imports: List[str])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `imports` | `List[str]` | |

Source: `src\symbol_codegen.py:66`

---

### `_emit_header`

```matha
def _emit_header(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:72`

---

### `_emit_body`

```matha
def _emit_body(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:77`

---

### `add_expression`

```matha
def add_expression(self, expr_str: str, var_name: str)
```

**描述**: 添加表达式求值（在函数体内emit，插入到return之前）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `var_name` | `str` | |

Source: `src\symbol_codegen.py:82`

---

### `_expr_to_python`

```matha
def _expr_to_python(self, expr: str) -> str
```

**描述**: 表达式转 Python。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:90`

---

## `JavaScriptGenerator`

```matha
class JavaScriptGenerator(CodeGenerator)
```

**描述**: 生成 JavaScript 代码。

Source: `src\symbol_codegen.py:108`

---

### `__init__`

```matha
def __init__(self, func_name: str, as_module: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `as_module` | `bool` | |

Source: `src\symbol_codegen.py:111`

---

### `_emit_header`

```matha
def _emit_header(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:117`

---

### `_emit_body`

```matha
def _emit_body(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:128`

---

### `add_expression`

```matha
def add_expression(self, expr_str: str, var_name: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `var_name` | `str` | |

Source: `src\symbol_codegen.py:133`

---

### `_expr_to_javascript`

```matha
def _expr_to_javascript(self, expr: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:137`

---

## `CGenerator`

```matha
class CGenerator(CodeGenerator)
```

**描述**: 生成 C 代码。

Source: `src\symbol_codegen.py:154`

---

### `__init__`

```matha
def __init__(self, func_name: str, return_type: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `return_type` | `str` | |

Source: `src\symbol_codegen.py:157`

---

### `_emit_header`

```matha
def _emit_header(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:163`

---

### `_emit_body`

```matha
def _emit_body(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:173`

---

### `add_expression`

```matha
def add_expression(self, expr_str: str, var_name: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `var_name` | `str` | |

Source: `src\symbol_codegen.py:177`

---

### `_expr_to_c`

```matha
def _expr_to_c(self, expr: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:181`

---

## `MathaGenerator`

```matha
class MathaGenerator(CodeGenerator)
```

**描述**: 生成 Matha 代码。

Source: `src\symbol_codegen.py:199`

---

### `__init__`

```matha
def __init__(self, func_name: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |

Source: `src\symbol_codegen.py:202`

---

### `_emit_header`

```matha
def _emit_header(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:207`

---

### `_emit_body`

```matha
def _emit_body(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:213`

---

### `add_expression`

```matha
def add_expression(self, expr_str: str, var_name: str)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_str` | `str` | |
| `var_name` | `str` | |

Source: `src\symbol_codegen.py:217`

---

## `DriverSpec`

```matha
class DriverSpec
```

**描述**: 硬件驱动规格。

**装饰器**: dataclass

Source: `src\symbol_codegen.py:226`

---

## `DriverGenerator`

```matha
class DriverGenerator
```

**描述**: 硬件驱动代码生成器。

Source: `src\symbol_codegen.py:236`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:245`

---

### `generate`

```matha
def generate(self, spec: DriverSpec) -> str
```

**描述**: 根据规格生成驱动代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\symbol_codegen.py:248`

---

### `generate_library`

```matha
def generate_library(self, functions: List[Dict[str, str]], lang: str) -> str
```

**描述**: 生成可导入库文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `functions` | `List[Dict[str, str]]` | |
| `lang` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:257`

---

## `MathaCodeGen`

```matha
class MathaCodeGen
```

**描述**: 统一代码生成器 — 支持多语言目标代码生成。

用法：
    cg = MathaCodeGen()
    cg.python("x^2 + 3*x", func_name="f")
    cg.javascript("sin(x) + cos(x)")
    cg.c("sqrt(x)", return_type="double")
    cg.matha("x + y")

Source: `src\symbol_codegen.py:275`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbol_codegen.py:287`

---

### `python`

```matha
def python(self, expr: str, func_name: str, imports: List[str]) -> str
```

**描述**: 生成 Python 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |
| `imports` | `List[str]` | |

**返回**: `str`

Source: `src\symbol_codegen.py:290`

---

### `javascript`

```matha
def javascript(self, expr: str, func_name: str, as_module: bool) -> str
```

**描述**: 生成 JavaScript 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |
| `as_module` | `bool` | |

**返回**: `str`

Source: `src\symbol_codegen.py:297`

---

### `c`

```matha
def c(self, expr: str, func_name: str, return_type: str) -> str
```

**描述**: 生成 C 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |
| `return_type` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:304`

---

### `matha`

```matha
def matha(self, expr: str, func_name: str) -> str
```

**描述**: 生成 Matha 代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `func_name` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:311`

---

### `generate_driver`

```matha
def generate_driver(self, spec: DriverSpec) -> str
```

**描述**: 生成硬件驱动代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `spec` | `DriverSpec` | |

**返回**: `str`

Source: `src\symbol_codegen.py:317`

---

### `generate_library`

```matha
def generate_library(self, functions: List[Dict[str, str]], lang: str) -> str
```

**描述**: 生成可导入库。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `functions` | `List[Dict[str, str]]` | |
| `lang` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:321`

---

## `get_codegen`

```matha
def get_codegen() -> MathaCodeGen
```

**返回**: `MathaCodeGen`

Source: `src\symbol_codegen.py:333`

---

## `codegen_python`

```matha
def codegen_python(expr: str, **kwargs) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:340`

---

## `codegen_javascript`

```matha
def codegen_javascript(expr: str, **kwargs) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:344`

---

## `codegen_c`

```matha
def codegen_c(expr: str, **kwargs) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `str` | |

**返回**: `str`

Source: `src\symbol_codegen.py:348`

---

# `symbolic`

```matha

```

**描述**: Matha 符号引擎 v1.3.0
========================
统一的符号表达式系统，支持 AST 解析、代数运算、微积分、自动简化。

功能：
  • Symbol       — 符号变量
  • Expr         — 表达式基类（Add/Mul/Pow/Func/Num/Var）
  • Parser       — 符号表达式解析器
  • simplify/diff/integrate/evaluate — 核心运算
  • ASTNode      — AST 节点（跨平台代码生成用）
  • LISP/Scheme  — 函数式语言互操作层

Source: `src\symbolic.py:1`

---

## `ASTNodeType`

```matha
class ASTNodeType(str, Enum)
```

Source: `src\symbolic.py:31`

---

## `ASTNode`

```matha
class ASTNode
```

**描述**: AST 节点：统一表示所有表达式和操作。

**装饰器**: dataclass

Source: `src\symbolic.py:54`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\symbolic.py:62`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\symbolic.py:71`

---

### `__repr__`

```matha
def __repr__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:101`

---

## `Expr`

```matha
class Expr
```

**描述**: 符号表达式基类 — 代数运算的核心抽象。

Source: `src\symbolic.py:109`

---

### `__add__`

```matha
def __add__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:113`

---

### `__radd__`

```matha
def __radd__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:114`

---

### `__sub__`

```matha
def __sub__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:115`

---

### `__rsub__`

```matha
def __rsub__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:116`

---

### `__mul__`

```matha
def __mul__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:117`

---

### `__rmul__`

```matha
def __rmul__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:118`

---

### `__truediv__`

```matha
def __truediv__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:119`

---

### `__rtruediv__`

```matha
def __rtruediv__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:120`

---

### `__pow__`

```matha
def __pow__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:121`

---

### `__rpow__`

```matha
def __rpow__(self, other)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

Source: `src\symbolic.py:122`

---

### `__neg__`

```matha
def __neg__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:123`

---

### `simplify`

```matha
def simplify(self) -> 'Expr'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'Expr'`

Source: `src\symbolic.py:125`

---

### `diff`

```matha
def diff(self, var: str) -> 'Expr'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `str` | |

**返回**: `'Expr'`

Source: `src\symbolic.py:126`

---

### `evaluate`

```matha
def evaluate(self, bindings: Dict[str, float]) -> float
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `Dict[str, float]` | |

**返回**: `float`

Source: `src\symbolic.py:127`

---

### `substitute`

```matha
def substitute(self, var: str, value: 'Expr') -> 'Expr'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `str` | |
| `value` | `'Expr'` | |

**返回**: `'Expr'`

Source: `src\symbolic.py:128`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:130`

---

### `__repr__`

```matha
def __repr__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:131`

---

## `Num`

```matha
class Num(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:135`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:137`

---

### `evaluate`

```matha
def evaluate(self, _bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `_bindings` | `` | |

Source: `src\symbolic.py:138`

---

### `substitute`

```matha
def substitute(self, *a)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:139`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:140`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:141`

---

## `Var`

```matha
class Var(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:144`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:146`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:150`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:152`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:154`

---

## `Neg`

```matha
class Neg(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:157`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:159`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:163`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:164`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:165`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:166`

---

## `Add`

```matha
class Add(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:169`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:172`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:182`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:183`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:185`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:186`

---

## `Sub`

```matha
class Sub(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:189`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:192`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:197`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:198`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:200`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:201`

---

## `Mul`

```matha
class Mul(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:204`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:207`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:225`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:226`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:228`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:231`

---

## `Div`

```matha
class Div(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:234`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:237`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:245`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:249`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:251`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:258`

---

## `Pow`

```matha
class Pow(Expr)
```

**装饰器**: dataclass(frozen=True)

Source: `src\symbolic.py:261`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:264`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:271`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:273`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:275`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:291`

---

## `Log`

```matha
class Log(Expr)
```

Source: `src\symbolic.py:295`

---

### `__init__`

```matha
def __init__(self, expr)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

Source: `src\symbolic.py:296`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:297`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:298`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:299`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:300`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:301`

---

## `FuncCall`

```matha
class FuncCall(Expr)
```

Source: `src\symbolic.py:304`

---

### `__init__`

```matha
def __init__(self, name, args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `` | |
| `args` | `` | |

Source: `src\symbolic.py:305`

---

### `simplify`

```matha
def simplify(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:306`

---

### `evaluate`

```matha
def evaluate(self, bindings)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `bindings` | `` | |

Source: `src\symbolic.py:308`

---

### `substitute`

```matha
def substitute(self, var, value)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |
| `value` | `` | |

Source: `src\symbolic.py:323`

---

### `diff`

```matha
def diff(self, var)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `var` | `` | |

Source: `src\symbolic.py:325`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:331`

---

## `to_expr`

```matha
def to_expr(obj) -> Expr
```

**描述**: 将 Python 值转换为 Expr 对象。

| 参数 | 类型 | 说明 |
|------|------|------|
| `obj` | `` | |

**返回**: `Expr`

Source: `src\symbolic.py:340`

---

## `symbolic`

```matha
def symbolic(var: str) -> Var
```

**描述**: 快捷创建符号变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `var` | `str` | |

**返回**: `Var`

Source: `src\symbolic.py:351`

---

## `SymbolicParser`

```matha
class SymbolicParser
```

**描述**: 解析数学表达式字符串 → Expr AST。

支持: x^2 + 3*x - 5, sin(x), sqrt(x), log(x, 10), 3!

Source: `src\symbolic.py:364`

---

### `parse`

```matha
def parse(self, text: str) -> Expr
```

**描述**: 解析表达式字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Expr`

Source: `src\symbolic.py:377`

---

### `_parse_expr`

```matha
def _parse_expr(self, text: str) -> Expr
```

**描述**: 解析加减表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Expr`

Source: `src\symbolic.py:386`

---

### `_parse_term`

```matha
def _parse_term(self, text: str) -> Expr
```

**描述**: 解析乘除表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Expr`

Source: `src\symbolic.py:403`

---

### `_parse_power`

```matha
def _parse_power(self, text: str) -> Expr
```

**描述**: 解析幂运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Expr`

Source: `src\symbolic.py:419`

---

### `_parse_primary`

```matha
def _parse_primary(self, text: str) -> Expr
```

**描述**: 解析基本单元：数字、变量、函数调用、括号表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |

**返回**: `Expr`

Source: `src\symbolic.py:441`

---

### `_split_top_level`

```matha
def _split_top_level(self, text: str, separators: List[str]) -> List[str]
```

**描述**: 在括号外按分隔符分割。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `separators` | `List[str]` | |

**返回**: `List[str]`

Source: `src\symbolic.py:490`

---

### `_split_with_ops`

```matha
def _split_with_ops(self, text: str, separators: List[str]) -> List[tuple]
```

**描述**: 在括号外按分隔符分割，同时记录每个分隔符。
返回 [(part0, op0), (part1, op1), ...]，part0 的 op 为 None。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `text` | `str` | |
| `separators` | `List[str]` | |

**返回**: `List[tuple]`

Source: `src\symbolic.py:517`

---

## `symbol_expr`

```matha
def symbol_expr(text: str) -> Expr
```

**描述**: 解析符号表达式字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `Expr`

Source: `src\symbolic.py:556`

---

## `simplify_expr`

```matha
def simplify_expr(expr: Expr) -> Expr
```

**描述**: 简化表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `Expr` | |

**返回**: `Expr`

Source: `src\symbolic.py:560`

---

## `diff_expr`

```matha
def diff_expr(expr: Expr, var: str) -> Expr
```

**描述**: 对变量求导。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `Expr` | |
| `var` | `str` | |

**返回**: `Expr`

Source: `src\symbolic.py:564`

---

## `eval_expr`

```matha
def eval_expr(expr: Expr, **bindings) -> float
```

**描述**: 数值求值。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `Expr` | |

**返回**: `float`

Source: `src\symbolic.py:568`

---

## `ast_to_dict`

```matha
def ast_to_dict(expr: Expr) -> dict
```

**描述**: 表达式转 AST 字典（用于代码生成）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `Expr` | |

**返回**: `dict`

Source: `src\symbolic.py:572`

---

## `_expr_to_ast`

```matha
def _expr_to_ast(expr: Expr) -> ASTNode
```

**描述**: Expr → ASTNode 转换。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `Expr` | |

**返回**: `ASTNode`

Source: `src\symbolic.py:576`

---

## `LISPExpr`

```matha
class LISPExpr
```

**描述**: LISP/Scheme 风格表达式，支持函数式编程互操作。

Source: `src\symbolic.py:604`

---

### `parse`

```matha
def parse(s: str)
```

**描述**: 解析 LISP S-表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**装饰器**: staticmethod

Source: `src\symbolic.py:608`

---

### `_tokenize`

```matha
def _tokenize(s: str) -> List[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `List[str]`

**装饰器**: staticmethod

Source: `src\symbolic.py:614`

---

### `_parse_tokens`

```matha
def _parse_tokens(tokens, pos)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `tokens` | `` | |
| `pos` | `` | |

**装饰器**: staticmethod

Source: `src\symbolic.py:652`

---

### `eval`

```matha
def eval(expr, env: dict) -> Any
```

**描述**: 求值 LISP 表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expr` | `` | |
| `env` | `dict` | |

**返回**: `Any`

**装饰器**: staticmethod

Source: `src\symbolic.py:680`

---

### `__init__`

```matha
def __init__(self, *args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:721`

---

### `__call__`

```matha
def __call__(self, *args)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:723`

---

### `__str__`

```matha
def __str__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbolic.py:726`

---

## `symbolic_calc`

```matha
def symbolic_calc(expression: str, **bindings) -> dict
```

**描述**: 统一符号计算接口。

| 参数 | 类型 | 说明 |
|------|------|------|
| `expression` | `str` | |

**返回**: `dict`

Source: `src\symbolic.py:736`

---

# `symbols`

```matha

```

**描述**: 符号表与作用域管理。

为语义分析器提供变量/函数/类型的定义追踪与引用解析。
作用域层级：全局 → 模块 → 代码块 → 段。

Source: `src\symbols.py:1`

---

## `Symbol`

```matha
class Symbol
```

**描述**: 符号表条目。

**装饰器**: dataclass

Source: `src\symbols.py:13`

---

## `Scope`

```matha
class Scope
```

**描述**: 作用域。

**装饰器**: dataclass

Source: `src\symbols.py:25`

---

### `define`

```matha
def define(self, sym: Symbol) -> bool
```

**描述**: 在当前作用域定义符号。若已存在同名符号则返回 False。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sym` | `Symbol` | |

**返回**: `bool`

Source: `src\symbols.py:33`

---

### `resolve`

```matha
def resolve(self, name: str) -> Optional[Symbol]
```

**描述**: 从当前作用域向上查找符号。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Symbol]`

Source: `src\symbols.py:40`

---

### `resolve_local`

```matha
def resolve_local(self, name: str) -> Optional[Symbol]
```

**描述**: 仅在当前作用域查找（不向上）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Symbol]`

Source: `src\symbols.py:48`

---

### `child`

```matha
def child(self, name: str) -> 'Scope'
```

**描述**: 创建并挂载子作用域。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `'Scope'`

Source: `src\symbols.py:52`

---

## `SymbolTable`

```matha
class SymbolTable
```

**描述**: 符号表：管理作用域栈。

Source: `src\symbols.py:59`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\symbols.py:62`

---

### `push`

```matha
def push(self, name: str) -> Scope
```

**描述**: 进入新作用域。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Scope`

Source: `src\symbols.py:67`

---

### `pop`

```matha
def pop(self) -> Scope
```

**描述**: 退出当前作用域，返回父作用域。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Scope`

Source: `src\symbols.py:74`

---

### `define`

```matha
def define(self, name: str, kind: str, decl: Any, line: int, type_info: Any, is_placeholder: bool) -> bool
```

**描述**: 在当前作用域定义符号。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `kind` | `str` | |
| `decl` | `Any` | |
| `line` | `int` | |
| `type_info` | `Any` | |
| `is_placeholder` | `bool` | |

**返回**: `bool`

Source: `src\symbols.py:81`

---

### `resolve`

```matha
def resolve(self, name: str) -> Optional[Symbol]
```

**描述**: 解析符号引用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Symbol]`

Source: `src\symbols.py:90`

---

### `resolve_local`

```matha
def resolve_local(self, name: str) -> Optional[Symbol]
```

**描述**: 仅在当前作用域解析（不向上查找）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Symbol]`

Source: `src\symbols.py:94`

---

## `SegmentTracker`

```matha
class SegmentTracker
```

**描述**: 追踪单个段内的 5 步执行顺序。

M3.1/M3.2 规则：命令→变量→？公式→字母公式→输出，顺序不可调换，可省略。

**装饰器**: dataclass

Source: `src\symbols.py:123`

---

### `record_step`

```matha
def record_step(self, step: str, node: Any) -> Optional[str]
```

**描述**: 记录一个步骤，返回错误信息（若有顺序违规）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `step` | `str` | |
| `node` | `Any` | |

**返回**: `Optional[str]`

Source: `src\symbols.py:133`

---

### `get_step`

```matha
def get_step(self, step: str) -> Optional[Any]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `step` | `str` | |

**返回**: `Optional[Any]`

Source: `src\symbols.py:149`

---

## `detect_resource_type`

```matha
def detect_resource_type(text: str) -> str
```

**描述**: 识别命令/输出内容中的资源类型（M3.2 一等能力）。

判断规则（按优先级）：
    1. 包含 :// → URL
    2. 以 / 或盘符开头（如 /data、d:\）→ 文件/目录
    3. 相对路径含分隔符（如 ./data、data/file）→ 文件
    4. 匹配 host:port 模式 → 端口
    5. 含字母扩展名的相对文件名（如 config_loader.matha、sub.py）→ 文件
    6. 其他 → 纯文本

| 参数 | 类型 | 说明 |
|------|------|------|
| `text` | `str` | |

**返回**: `str`

Source: `src\symbols.py:165`

---

# `symtab_exporter`

```matha

```

**描述**: Matha 符号表导出器：导出所有内建函数签名，供其它语言调用。

让其它编程语言能查询 Matha 的完整函数库（数学/工程/各学科），
知道每个函数的名称、参数数、所属领域，从而正确调用。

导出格式：
  - JSON：通用格式，任何语言可解析
  - Python dict：Python 直接可用
  - TypeScript .d.ts：类型声明
  - Markdown 表格：人类可读文档

Source: `src\symtab_exporter.py:1`

---

## `_get_arity`

```matha
def _get_arity(fn) -> int
```

**描述**: 推断函数的参数数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `fn` | `` | |

**返回**: `int`

Source: `src\symtab_exporter.py:21`

---

## `_classify_function`

```matha
def _classify_function(name: str) -> tuple[str, str]
```

**描述**: 根据函数名推断所属领域和分类。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |

**返回**: `tuple[str, str]`

Source: `src\symtab_exporter.py:32`

---

## `export_symtab`

```matha
def export_symtab(interp: Interpreter) -> list[dict]
```

**描述**: 导出 Matha 的完整符号表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `Interpreter` | |

**返回**: `list[dict]`

Source: `src\symtab_exporter.py:102`

---

## `export_symtab_json`

```matha
def export_symtab_json(interp: Interpreter, indent: int) -> str
```

**描述**: 导出符号表为 JSON。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `Interpreter` | |
| `indent` | `int` | |

**返回**: `str`

Source: `src\symtab_exporter.py:135`

---

## `export_symtab_d_ts`

```matha
def export_symtab_d_ts(interp: Interpreter) -> str
```

**描述**: 导出符号表为 TypeScript .d.ts 类型声明。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `Interpreter` | |

**返回**: `str`

Source: `src\symtab_exporter.py:140`

---

## `export_symtab_markdown`

```matha
def export_symtab_markdown(interp: Interpreter) -> str
```

**描述**: 导出符号表为 Markdown 表格。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `Interpreter` | |

**返回**: `str`

Source: `src\symtab_exporter.py:158`

---

## `export_symtab_python`

```matha
def export_symtab_python(interp: Interpreter) -> str
```

**描述**: 导出符号表为 Python 可导入模块字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interp` | `Interpreter` | |

**返回**: `str`

Source: `src\symtab_exporter.py:174`

---

# `template_convert`

```matha

```

**描述**: 新旧 Matha 模板互转工具

旧模板特征：#N：{ ... } 有段号，多段分散
新模板特征：#：{ ... } 无段号，整合为一代码块

转换规则：
  旧 → 新：
    1. 去掉段号 #N： → #：
    2. 合并多个段为一个代码块
    3. 命令段用 >> 链式连接
    4. 可选添加 【*/说明/*】 和 @：【内容】
  新 → 旧：
    1. 添加段号 #：→ #1：, #2：, ...
    2. 拆分代码块为多个段
    3. 去掉 【*/说明/*】 和 @：【内容】（可选）

Source: `src\template_convert.py:1`

---

## `old_to_new`

```matha
def old_to_new(src: str, title: str, code_id: str) -> str
```

**描述**: 旧模板 → 新模板

将 #N：{ ... } 形式的多段代码合并为 #：{ ... } 单代码块。
段号被去除，语句被整合，输出行保留 …（x/y）【子文件】 后缀。
code_id：可选代码编号前缀（如 "？" 或 "1"），放在 #： 之前。

| 参数 | 类型 | 说明 |
|------|------|------|
| `src` | `str` | |
| `title` | `str` | |
| `code_id` | `str` | |

**返回**: `str`

Source: `src\template_convert.py:22`

---

## `new_to_old`

```matha
def new_to_old(src: str) -> str
```

**描述**: 新模板 → 旧模板

将 [？|N]#：{ ... } 单代码块拆分为 #：{ } #2：{ } ... 多段代码。
绑定语句和其后的输出行放在同一段中（保持变量作用域）。
段号自动递增，@：和【*/.../*】被去除。
代码编号前缀（？/N）被去除（旧模板无此概念）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `src` | `str` | |

**返回**: `str`

Source: `src\template_convert.py:81`

---

## `convert`

```matha
def convert(src: str, direction: str) -> Tuple[str, str]
```

**描述**: 自动检测并转换模板格式

direction: "auto" | "old_to_new" | "new_to_old"
返回: (转换结果, 实际使用的方向)

| 参数 | 类型 | 说明 |
|------|------|------|
| `src` | `str` | |
| `direction` | `str` | |

**返回**: `Tuple[str, str]`

Source: `src\template_convert.py:149`

---

# `tokens`

```matha

```

**描述**: Token 类型定义。

对应 EBNF §2 词法层（02-词法结构.md）。
所有 Matha Unicode 符号（？【】〔〕《》#：………，等）均按字面终结符处理。

Source: `src\tokens.py:1`

---

## `TokenType`

```matha
class TokenType(Enum)
```

**描述**: Matha Token 类型。

命名约定：
    - OP_*   运算符
    - PUNCT_* 标点/括号
    - KW_*   保留关键字
    - LIT_*  字面量
    - MATHA_* Matha 专属 Unicode 符号

Source: `src\tokens.py:10`

---

## `Token`

```matha
class Token
```

**描述**: 词法 Token。

Source: `src\tokens.py:121`

---

### `__init__`

```matha
def __init__(self, type: TokenType, value: str, line: int, col: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type` | `TokenType` | |
| `value` | `str` | |
| `line` | `int` | |
| `col` | `int` | |

Source: `src\tokens.py:126`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\tokens.py:132`

---

### `__eq__`

```matha
def __eq__(self, other) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `` | |

**返回**: `bool`

Source: `src\tokens.py:135`

---

# `tools`

```matha

```

**描述**: Matha 工程工具：格式化器、Linter、测试框架 stub。

Source: `src\tools.py:1`

---

## `FormatConfig`

```matha
class FormatConfig
```

**装饰器**: dataclass

Source: `src\tools.py:16`

---

## `MathaFormatter`

```matha
class MathaFormatter
```

**描述**: Matha 代码格式化器。

Source: `src\tools.py:22`

---

### `__init__`

```matha
def __init__(self, config: FormatConfig) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `config` | `FormatConfig` | |

**返回**: `None`

Source: `src\tools.py:25`

---

### `format`

```matha
def format(self, source: str) -> str
```

**描述**: 格式化 Matha 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\tools.py:28`

---

### `check_format`

```matha
def check_format(self, source: str) -> list[str]
```

**描述**: 检查代码格式问题。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list[str]`

Source: `src\tools.py:55`

---

## `LintIssue`

```matha
class LintIssue
```

**装饰器**: dataclass

Source: `src\tools.py:72`

---

## `MathaLinter`

```matha
class MathaLinter
```

**描述**: Matha 代码检查器。

Source: `src\tools.py:80`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools.py:91`

---

### `lint`

```matha
def lint(self, source: str) -> list[LintIssue]
```

**描述**: 对源码执行 lint 检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `list[LintIssue]`

Source: `src\tools.py:97`

---

### `get_summary`

```matha
def get_summary(self, issues: list[LintIssue]) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `issues` | `list[LintIssue]` | |

**返回**: `str`

Source: `src\tools.py:111`

---

## `MathaTestCase`

```matha
class MathaTestCase
```

**描述**: Matha 测试用例基类。

Source: `src\tools.py:121`

---

### `__init__`

```matha
def __init__(self, name: str) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `None`

Source: `src\tools.py:124`

---

### `assert_equal`

```matha
def assert_equal(self, actual: Any, expected: Any, msg: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `actual` | `Any` | |
| `expected` | `Any` | |
| `msg` | `str` | |

**返回**: `bool`

Source: `src\tools.py:128`

---

### `assert_true`

```matha
def assert_true(self, value: Any, msg: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |
| `msg` | `str` | |

**返回**: `bool`

Source: `src\tools.py:135`

---

### `assert_raises`

```matha
def assert_raises(self, exc_type: type, fn: callable, *args, **kwargs) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `exc_type` | `type` | |
| `fn` | `callable` | |

**返回**: `bool`

Source: `src\tools.py:138`

---

### `run_all`

```matha
def run_all(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\tools.py:150`

---

## `test`

```matha
def test(name: str, fn: callable) -> MathaTestCase
```

**描述**: 创建并运行测试用例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `fn` | `callable` | |

**返回**: `MathaTestCase`

Source: `src\tools.py:156`

---

## `run_tests`

```matha
def run_tests(test_cases: list[MathaTestCase]) -> dict
```

**描述**: 运行所有测试用例。

| 参数 | 类型 | 说明 |
|------|------|------|
| `test_cases` | `list[MathaTestCase]` | |

**返回**: `dict`

Source: `src\tools.py:164`

---

## `MathaProfiler`

```matha
class MathaProfiler
```

**描述**: Matha 性能分析器。

Source: `src\tools.py:178`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools.py:181`

---

### `enable`

```matha
def enable(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools.py:185`

---

### `disable`

```matha
def disable(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools.py:188`

---

### `profile_call`

```matha
def profile_call(self, name: str, fn: callable, *args, **kwargs) -> Any
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `fn` | `callable` | |

**返回**: `Any`

Source: `src\tools.py:191`

---

### `report`

```matha
def report(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\tools.py:202`

---

## `MathaREPL`

```matha
class MathaREPL
```

**描述**: Matha REPL 交互环境。

Source: `src\tools.py:215`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools.py:218`

---

### `run`

```matha
def run(self, code: str) -> Any
```

**描述**: 执行单行代码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `code` | `str` | |

**返回**: `Any`

Source: `src\tools.py:222`

---

### `history`

```matha
def history(self, n: int) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `n` | `int` | |

**返回**: `list[str]`

Source: `src\tools.py:231`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools.py:234`

---

# `tools.doc_generator`

```matha

```

**描述**: Matha 文档生成器

从 Matha 源代码自动提取文档信息，生成 API 参考文档。

支持格式：
  - Markdown
  - HTML
  - JSON（用于静态站点生成器）

使用方式：
  from src.tools.doc_generator import MathaDocGenerator
  generator = MathaDocGenerator(['src/stdlib'])
  generator.generate_markdown('docs/api.md')

Source: `src\tools\doc_generator.py:1`

---

## `FunctionDoc`

```matha
class FunctionDoc
```

**描述**: 函数文档信息

**装饰器**: dataclass

Source: `src\tools\doc_generator.py:31`

---

## `ClassDoc`

```matha
class ClassDoc
```

**描述**: 类文档信息

**装饰器**: dataclass

Source: `src\tools\doc_generator.py:44`

---

## `ModuleDoc`

```matha
class ModuleDoc
```

**描述**: 模块文档信息

**装饰器**: dataclass

Source: `src\tools\doc_generator.py:56`

---

## `MathaDocGenerator`

```matha
class MathaDocGenerator
```

**描述**: Matha 文档生成器

从源代码提取文档信息，支持多种输出格式。

Source: `src\tools\doc_generator.py:66`

---

### `__init__`

```matha
def __init__(self, source_dirs: List[str], exclude_patterns: Optional[List[str]])
```

**描述**: 初始化文档生成器

Args:
    source_dirs: 源代码目录列表
    exclude_patterns: 排除的文件模式列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source_dirs` | `List[str]` | |
| `exclude_patterns` | `Optional[List[str]]` | |

Source: `src\tools\doc_generator.py:84`

---

### `discover_modules`

```matha
def discover_modules(self) -> List[Path]
```

**描述**: 发现所有 Python 模块文件

Returns:
    模块文件路径列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Path]`

Source: `src\tools\doc_generator.py:96`

---

### `extract_docstring`

```matha
def extract_docstring(self, obj: Any) -> str
```

**描述**: 提取对象的 docstring

Args:
    obj: 对象（函数、类、模块）

Returns:
    docstring 内容

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `obj` | `Any` | |

**返回**: `str`

Source: `src\tools\doc_generator.py:117`

---

### `parse_docstring`

```matha
def parse_docstring(self, docstring: str) -> Dict[str, Any]
```

**描述**: 解析 docstring，提取结构化信息

Args:
    docstring: 原始 docstring

Returns:
    解析后的字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `docstring` | `str` | |

**返回**: `Dict[str, Any]`

Source: `src\tools\doc_generator.py:130`

---

### `inspect_function`

```matha
def inspect_function(self, func) -> Optional[FunctionDoc]
```

**描述**: 检查函数对象，提取文档信息

Args:
    func: 函数对象

Returns:
    FunctionDoc 对象

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `` | |

**返回**: `Optional[FunctionDoc]`

Source: `src\tools\doc_generator.py:226`

---

### `_format_type_annotation`

```matha
def _format_type_annotation(self, annotation) -> str
```

**描述**: 格式化类型注解

支持：
- 简单类型：int, str, float
- 泛型类型：List[int], Dict[str, int]
- 可选类型：Optional[int]
- 联合类型：int | str
- 复杂类型：Tuple[int, str], Callable[[int], str]

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `annotation` | `` | |

**返回**: `str`

Source: `src\tools\doc_generator.py:304`

---

### `inspect_class`

```matha
def inspect_class(self, cls) -> Optional[ClassDoc]
```

**描述**: 检查类对象，提取文档信息

Args:
    cls: 类对象

Returns:
    ClassDoc 对象

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `cls` | `` | |

**返回**: `Optional[ClassDoc]`

Source: `src\tools\doc_generator.py:341`

---

### `process_module`

```matha
def process_module(self, module_path: Path) -> Optional[ModuleDoc]
```

**描述**: 处理单个模块文件

Args:
    module_path: 模块文件路径

Returns:
    ModuleDoc 对象

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `module_path` | `Path` | |

**返回**: `Optional[ModuleDoc]`

Source: `src\tools\doc_generator.py:397`

---

### `generate`

```matha
def generate(self) -> Dict[str, ModuleDoc]
```

**描述**: 生成所有模块文档

Returns:
    模块文档字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, ModuleDoc]`

Source: `src\tools\doc_generator.py:477`

---

### `generate_markdown`

```matha
def generate_markdown(self, output_path: str) -> str
```

**描述**: 生成 Markdown 格式文档

Args:
    output_path: 输出文件路径

Returns:
    生成的 Markdown 内容

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `output_path` | `str` | |

**返回**: `str`

Source: `src\tools\doc_generator.py:494`

---

### `generate_html`

```matha
def generate_html(self, output_path: str) -> str
```

**描述**: 生成 HTML 格式文档

Args:
    output_path: 输出文件路径

Returns:
    生成的 HTML 内容

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `output_path` | `str` | |

**返回**: `str`

Source: `src\tools\doc_generator.py:596`

---

### `generate_json`

```matha
def generate_json(self, output_path: str) -> str
```

**描述**: 生成 JSON 格式文档

Args:
    output_path: 输出文件路径

Returns:
    生成的 JSON 内容

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `output_path` | `str` | |

**返回**: `str`

Source: `src\tools\doc_generator.py:655`

---

# `tools.flame_graph`

```matha

```

**描述**: 性能分析器火焰图可视化组件

提供交互式火焰图（Flame Graph）渲染，
支持展开/收起调用栈、悬停查看详情、时间缩放。

依赖：
  pip install matplotlib seaborn

使用：
  from src.tools.flame_graph import FlameGraphRenderer
  renderer = FlameGraphRenderer()
  renderer.render(profiler_data, output_path="docs/flame_graph.html")

Source: `src\tools\flame_graph.py:1`

---

## `CallFrame`

```matha
class CallFrame
```

**描述**: 调用栈帧。

**装饰器**: dataclass

Source: `src\tools\flame_graph.py:29`

---

### `to_dict`

```matha
def to_dict(self) -> dict
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict`

Source: `src\tools\flame_graph.py:39`

---

## `FlameGraphData`

```matha
class FlameGraphData
```

**描述**: 火焰图数据。

**装饰器**: dataclass

Source: `src\tools\flame_graph.py:52`

---

## `_get_color`

```matha
def _get_color(depth: int, palette: str) -> str
```

**描述**: 根据深度获取颜色。

| 参数 | 类型 | 说明 |
|------|------|------|
| `depth` | `int` | |
| `palette` | `str` | |

**返回**: `str`

Source: `src\tools\flame_graph.py:84`

---

## `FlameGraphBuilder`

```matha
class FlameGraphBuilder
```

**描述**: 从调用栈数据构建火焰图。

Source: `src\tools\flame_graph.py:94`

---

### `__init__`

```matha
def __init__(self, palette: str, max_depth: int)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `palette` | `str` | |
| `max_depth` | `int` | |

Source: `src\tools\flame_graph.py:97`

---

### `build`

```matha
def build(self, call_stack: List[Tuple[str, float]], total_duration: float) -> FlameGraphData
```

**描述**: 从线性调用栈构建火焰图树。

Args:
    call_stack: [(函数名, 持续时间ms), ...] 深度优先遍历序列
    total_duration: 总执行时间（ms）

Returns:
    FlameGraphData

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `call_stack` | `List[Tuple[str, float]]` | |
| `total_duration` | `float` | |

**返回**: `FlameGraphData`

Source: `src\tools\flame_graph.py:101`

---

### `_layout`

```matha
def _layout(self, frame: CallFrame, start: float, end: float)
```

**描述**: 递归设置火焰图布局（x 坐标和宽度百分比）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `CallFrame` | |
| `start` | `float` | |
| `end` | `float` | |

Source: `src\tools\flame_graph.py:132`

---

### `_flatten`

```matha
def _flatten(self, frame: CallFrame) -> List[CallFrame]
```

**描述**: 将树展平为列表（用于渲染）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `CallFrame` | |

**返回**: `List[CallFrame]`

Source: `src\tools\flame_graph.py:150`

---

## `FlameGraphRenderer`

```matha
class FlameGraphRenderer
```

**描述**: 将火焰图数据渲染为交互式 HTML。

Source: `src\tools\flame_graph.py:162`

---

### `render`

```matha
def render(self, data: FlameGraphData, output_path: str) -> str
```

**描述**: 渲染火焰图为 HTML 文件。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `FlameGraphData` | |
| `output_path` | `str` | |

**返回**: `str`

Source: `src\tools\flame_graph.py:435`

---

### `render_to_string`

```matha
def render_to_string(self, data: FlameGraphData) -> str
```

**描述**: 渲染为 HTML 字符串。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `data` | `FlameGraphData` | |

**返回**: `str`

Source: `src\tools\flame_graph.py:451`

---

### `_build_html`

```matha
def _build_html(title: str, total_ms: float, frame_count: int, gen_time: str, data_json: str, palette_json: str) -> str
```

**描述**: 构建 HTML 内容（避免 .format() 与 JS 花括号冲突）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `title` | `str` | |
| `total_ms` | `float` | |
| `frame_count` | `int` | |
| `gen_time` | `str` | |
| `data_json` | `str` | |
| `palette_json` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\tools\flame_graph.py:464`

---

## `build_flame_data_from_profiler`

```matha
def build_flame_data_from_profiler(profiler_data: dict) -> FlameGraphData
```

**描述**: 从 MathaProfiler 的统计数据结构构建火焰图数据。

| 参数 | 类型 | 说明 |
|------|------|------|
| `profiler_data` | `dict` | |

**返回**: `FlameGraphData`

Source: `src\tools\flame_graph.py:481`

---

## `build_flame_data_from_trace`

```matha
def build_flame_data_from_trace(trace: List[dict]) -> FlameGraphData
```

**描述**: 从原始调用跟踪列表构建火焰图数据。

trace 格式：
  [{"name": "func_a", "start": 0.0, "duration": 0.05},
   {"name": "func_b", "start": 0.01, "duration": 0.02},
   ...]

| 参数 | 类型 | 说明 |
|------|------|------|
| `trace` | `List[dict]` | |

**返回**: `FlameGraphData`

Source: `src\tools\flame_graph.py:495`

---

## `gen_call_stack`

```matha
def gen_call_stack(depth, remaining)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `depth` | `` | |
| `remaining` | `` | |

Source: `src\tools\flame_graph.py:524`

---

# `tools.perf_profiler`

```matha

```

**描述**: Matha 性能分析器模块

提供性能分析功能：
  - 函数执行时间追踪
  - JIT 编译效果对比
  - 内存使用分析
  - HTML/JSON 报告生成
  - 性能回归测试

使用方式：
  from src.tools.perf_profiler import MathaProfiler
  profiler = MathaProfiler()
  profiler.start()
  # 执行代码...
  profiler.stop()
  profiler.generate_report('docs/performance_report.html')

Source: `src\tools\perf_profiler.py:1`

---

## `ProfileResult`

```matha
class ProfileResult
```

**描述**: 性能分析结果

**装饰器**: dataclass

Source: `src\tools\perf_profiler.py:38`

---

## `PerformanceReport`

```matha
class PerformanceReport
```

**描述**: 性能报告

**装饰器**: dataclass

Source: `src\tools\perf_profiler.py:51`

---

## `MathaProfiler`

```matha
class MathaProfiler
```

**描述**: Matha 性能分析器

Source: `src\tools\perf_profiler.py:60`

---

### `__init__`

```matha
def __init__(self, enable_memory: bool, enable_jit: bool)
```

**描述**: 初始化性能分析器

Args:
    enable_memory: 是否启用内存分析
    enable_jit: 是否启用 JIT 效果对比

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `enable_memory` | `bool` | |
| `enable_jit` | `bool` | |

Source: `src\tools\perf_profiler.py:63`

---

### `start`

```matha
def start(self) -> None
```

**描述**: 开始性能分析

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools\perf_profiler.py:82`

---

### `stop`

```matha
def stop(self) -> None
```

**描述**: 停止性能分析

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools\perf_profiler.py:89`

---

### `profile`

```matha
def profile(self, func: Callable) -> Callable
```

**描述**: 装饰器：分析函数性能

Args:
    func: 要分析的函数

Returns:
    包装后的函数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func` | `Callable` | |

**返回**: `Callable`

Source: `src\tools\perf_profiler.py:96`

---

### `benchmark`

```matha
def benchmark(self, name: str, func: Callable, *args, **kwargs) -> Dict[str, float]
```

**描述**: 运行基准测试

Args:
    name: 测试名称
    func: 要测试的函数
    *args: 位置参数
    iterations: 迭代次数
    **kwargs: 关键字参数

Returns:
    基准测试结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `Callable` | |

**返回**: `Dict[str, float]`

Source: `src\tools\perf_profiler.py:127`

---

### `compare_jit`

```matha
def compare_jit(self, name: str, func: Callable, *args, **kwargs) -> Dict[str, float]
```

**描述**: 比较 JIT 编译效果

Args:
    name: 函数名称
    func: 函数
    *args: 位置参数
    **kwargs: 关键字参数

Returns:
    JIT 效果对比结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `func` | `Callable` | |

**返回**: `Dict[str, float]`

Source: `src\tools\perf_profiler.py:165`

---

### `get_memory_usage`

```matha
def get_memory_usage(self) -> Dict[str, float]
```

**描述**: 获取当前内存使用情况

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, float]`

Source: `src\tools\perf_profiler.py:210`

---

### `generate_html_report`

```matha
def generate_html_report(self, output_path: str) -> str
```

**描述**: 生成 HTML 格式性能报告

Args:
    output_path: 输出文件路径

Returns:
    HTML 内容

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `output_path` | `str` | |

**返回**: `str`

Source: `src\tools\perf_profiler.py:221`

---

### `generate_json_report`

```matha
def generate_json_report(self, output_path: str) -> str
```

**描述**: 生成 JSON 格式性能报告

Args:
    output_path: 输出文件路径

Returns:
    JSON 内容

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `output_path` | `str` | |

**返回**: `str`

Source: `src\tools\perf_profiler.py:302`

---

### `_generate_summary`

```matha
def _generate_summary(self) -> Dict[str, Any]
```

**描述**: 生成执行摘要

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\tools\perf_profiler.py:335`

---

### `_generate_results_html`

```matha
def _generate_results_html(self) -> str
```

**描述**: 生成结果 HTML 表格

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\tools\perf_profiler.py:357`

---

### `_generate_benchmarks_html`

```matha
def _generate_benchmarks_html(self) -> str
```

**描述**: 生成基准测试 HTML

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\tools\perf_profiler.py:392`

---

### `_generate_jit_html`

```matha
def _generate_jit_html(self) -> str
```

**描述**: 生成 JIT 效果 HTML 表格

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\tools\perf_profiler.py:422`

---

### `run_performance_test`

```matha
def run_performance_test(self, test_name: str, func: Callable, *args, **kwargs) -> bool
```

**描述**: 运行性能回归测试

Args:
    test_name: 测试名称
    func: 测试函数
    *args: 位置参数
    **kwargs: 关键字参数

Returns:
    测试是否通过（性能在阈值内）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `test_name` | `str` | |
| `func` | `Callable` | |

**返回**: `bool`

Source: `src\tools\perf_profiler.py:438`

---

### `clear_results`

```matha
def clear_results(self) -> None
```

**描述**: 清除分析结果

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools\perf_profiler.py:463`

---

### `print_summary`

```matha
def print_summary(self) -> None
```

**描述**: 打印性能摘要

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\tools\perf_profiler.py:469`

---

## `profile`

```matha
def profile(func: Callable) -> Callable
```

**描述**: 简化的 profile 装饰器

| 参数 | 类型 | 说明 |
|------|------|------|
| `func` | `Callable` | |

**返回**: `Callable`

Source: `src\tools\perf_profiler.py:489`

---

## `benchmark`

```matha
def benchmark(name: str, func: Callable, *args, **kwargs) -> Dict[str, float]
```

**描述**: 简化的 benchmark 函数

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `func` | `Callable` | |

**返回**: `Dict[str, float]`

Source: `src\tools\perf_profiler.py:495`

---

## `sample_function`

```matha
def sample_function(n: int) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `n` | `int` | |

**返回**: `int`

Source: `src\tools\perf_profiler.py:513`

---

# `transpiler`

```matha

```

**描述**: Matha 多语言转译器：把 Matha 程序转译为其它编程语言源码。

让其它语言能直接运行 Matha 编写的程序/应用/系统。

支持目标语言：
  - Python：转译为可执行的 Python 脚本
  - JavaScript：转译为可运行的 JS 脚本
  - JSON IR：转译为中间表示（任何语言可解析后执行）

转译策略：
  - func 定义 → 目标语言函数定义
  - 绑定（Binding）→ 变量赋值
  - 表达式 → 目标语言等价表达式
  - 输出（Output）→ print / console.log
  - if/match/loop → 目标语言控制流
  - Matha 内建函数 → 目标语言等价函数映射

符号映射表（Matha → 目标语言）：
  Matha 内建     → Python              → JavaScript
  ─────────────────────────────────────────────────
  sin/cos/tan    → math.sin/cos/tan    → Math.sin/cos/tan
  sqrt           → math.sqrt           → Math.sqrt
  ln             → math.log            → Math.log
  log10          → math.log10          → Math.log10
  abs            → abs                 → Math.abs
  max/min        → max/min             → Math.max/min
  len            → len                 → .length
  ord/chr        → ord/chr             → .charCodeAt(0)/String.fromCharCode
  + - * / % ^    → + - * / % **        → + - * / % **
  真/假          → True/False          → true/false

Source: `src\transpiler.py:1`

---

## `TranspileError`

```matha
class TranspileError(Exception)
```

**描述**: 转译错误。

Source: `src\transpiler.py:42`

---

## `PythonTranspiler`

```matha
class PythonTranspiler
```

**描述**: 把 Matha AST 转译为 Python 源码。

Source: `src\transpiler.py:80`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\transpiler.py:83`

---

### `transpile`

```matha
def transpile(self, source: str) -> str
```

**描述**: 转译 Matha 源码为 Python 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\transpiler.py:88`

---

### `_pad`

```matha
def _pad(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\transpiler.py:101`

---

### `_transpile_decl`

```matha
def _transpile_decl(self, decl) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `` | |

**返回**: `None`

Source: `src\transpiler.py:104`

---

### `_transpile_func`

```matha
def _transpile_func(self, fn: ast.FuncDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `ast.FuncDef` | |

**返回**: `None`

Source: `src\transpiler.py:122`

---

### `_transpile_stmt`

```matha
def _transpile_stmt(self, stmt) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `` | |

**返回**: `None`

Source: `src\transpiler.py:142`

---

### `_transpile_expr`

```matha
def _transpile_expr(self, expr) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `str`

Source: `src\transpiler.py:194`

---

### `_map_op`

```matha
def _map_op(op: str) -> str
```

**描述**: Matha 运算符 → Python 运算符。

| 参数 | 类型 | 说明 |
|------|------|------|
| `op` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\transpiler.py:257`

---

## `JavaScriptTranspiler`

```matha
class JavaScriptTranspiler
```

**描述**: 把 Matha AST 转译为 JavaScript 源码。

Source: `src\transpiler.py:272`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\transpiler.py:275`

---

### `transpile`

```matha
def transpile(self, source: str) -> str
```

**描述**: 转译 Matha 源码为 JavaScript 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\transpiler.py:279`

---

### `_pad`

```matha
def _pad(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\transpiler.py:291`

---

### `_transpile_decl`

```matha
def _transpile_decl(self, decl) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `` | |

**返回**: `None`

Source: `src\transpiler.py:294`

---

### `_transpile_func`

```matha
def _transpile_func(self, fn: ast.FuncDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `ast.FuncDef` | |

**返回**: `None`

Source: `src\transpiler.py:309`

---

### `_transpile_stmt`

```matha
def _transpile_stmt(self, stmt) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `` | |

**返回**: `None`

Source: `src\transpiler.py:328`

---

### `_transpile_expr`

```matha
def _transpile_expr(self, expr) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `str`

Source: `src\transpiler.py:375`

---

### `_map_op`

```matha
def _map_op(op: str) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `op` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\transpiler.py:432`

---

## `json_repr`

```matha
def json_repr(s: str) -> str
```

**描述**: 把字符串转为 JSON 字符串表示（JS 兼容）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `s` | `str` | |

**返回**: `str`

Source: `src\transpiler.py:442`

---

## `transpile`

```matha
def transpile(source: str, target: str) -> str
```

**描述**: 把 Matha 源码转译为目标语言源码。

Args:
    source: Matha 源码
    target: "python" | "javascript" | "typescript" | "json"

Returns:
    目标语言源码字符串

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `target` | `str` | |

**返回**: `str`

Source: `src\transpiler.py:452`

---

# `transpiler_ts`

```matha

```

**描述**: TypeScript 转译器 — 将 Matha AST 转译为 TypeScript 源码

TypeScript 是 Python 的超集，额外支持类型注解和接口定义。

支持：
  - 函数定义（带类型注解）
  - 变量声明（let/const + 类型推断）
  - 表达式转译（数学函数、运算符、条件表达式）
  - 控制流（if/else、while）
  - 类型映射（float → number, bool → boolean, string → string）

Source: `src\transpiler_ts.py:1`

---

## `TranspileError`

```matha
class TranspileError(Exception)
```

**描述**: 转译错误。

Source: `src\transpiler_ts.py:21`

---

## `TypeScriptTranspiler`

```matha
class TypeScriptTranspiler
```

**描述**: 把 Matha AST 转译为 TypeScript 源码。

Source: `src\transpiler_ts.py:56`

---

### `__init__`

```matha
def __init__(self, add_types: bool)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `add_types` | `bool` | |

Source: `src\transpiler_ts.py:59`

---

### `transpile`

```matha
def transpile(self, source: str) -> str
```

**描述**: 转译 Matha 源码为 TypeScript 源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `str`

Source: `src\transpiler_ts.py:65`

---

### `_pad`

```matha
def _pad(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\transpiler_ts.py:82`

---

### `_transpile_decl`

```matha
def _transpile_decl(self, decl) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `` | |

**返回**: `None`

Source: `src\transpiler_ts.py:85`

---

### `_transpile_func`

```matha
def _transpile_func(self, fn: ast.FuncDef) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `fn` | `ast.FuncDef` | |

**返回**: `None`

Source: `src\transpiler_ts.py:104`

---

### `_transpile_stmt`

```matha
def _transpile_stmt(self, stmt) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `` | |

**返回**: `None`

Source: `src\transpiler_ts.py:136`

---

### `_transpile_expr`

```matha
def _transpile_expr(self, expr) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `str`

Source: `src\transpiler_ts.py:186`

---

### `_infer_type`

```matha
def _infer_type(self, expr) -> str | None
```

**描述**: 推断表达式的 TypeScript 类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `` | |

**返回**: `str | None`

Source: `src\transpiler_ts.py:251`

---

### `_infer_param_type`

```matha
def _infer_param_type(self, param) -> str
```

**描述**: 推断参数类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `param` | `` | |

**返回**: `str`

Source: `src\transpiler_ts.py:282`

---

### `_map_op`

```matha
def _map_op(op: str) -> str
```

**描述**: Matha 运算符 → TypeScript 运算符。

| 参数 | 类型 | 说明 |
|------|------|------|
| `op` | `str` | |

**返回**: `str`

**装饰器**: staticmethod

Source: `src\transpiler_ts.py:289`

---

## `transpile`

```matha
def transpile(source: str, target: str) -> str
```

**描述**: 把 Matha 源码转译为目标语言源码。

Args:
    source: Matha 源码
    target: "python" | "javascript" | "typescript" | "json"

Returns:
    目标语言源码字符串

| 参数 | 类型 | 说明 |
|------|------|------|
| `source` | `str` | |
| `target` | `str` | |

**返回**: `str`

Source: `src\transpiler_ts.py:304`

---

# `tree_sitter_backends`

```matha

```

**描述**: matha-auth tree-sitter 后端（无需外部依赖的内联 AST 解析器）

提供 Rust/Go/JavaScript/C 的内联正则解析器，作为 tree-sitter C 扩展的降级方案。

Source: `src\tree_sitter_backends.py:1`

---

## `ASTNode`

```matha
class ASTNode
```

**描述**: AST 节点

**装饰器**: dataclass

Source: `src\tree_sitter_backends.py:17`

---

### `to_dict`

```matha
def to_dict(self) -> Dict[str, Any]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\tree_sitter_backends.py:24`

---

### `__len__`

```matha
def __len__(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\tree_sitter_backends.py:32`

---

### `__bool__`

```matha
def __bool__(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\tree_sitter_backends.py:35`

---

## `RustParser`

```matha
class RustParser
```

**描述**: Rust 内联 AST 解析器（正则实现）

Source: `src\tree_sitter_backends.py:41`

---

### `parse`

```matha
def parse(self, source: str) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `ASTNode`

Source: `src\tree_sitter_backends.py:44`

---

## `GoParser`

```matha
class GoParser
```

**描述**: Go 内联 AST 解析器（正则实现）

Source: `src\tree_sitter_backends.py:77`

---

### `parse`

```matha
def parse(self, source: str) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `ASTNode`

Source: `src\tree_sitter_backends.py:80`

---

## `JSParser`

```matha
class JSParser
```

**描述**: JavaScript 内联 AST 解析器（正则实现）

Source: `src\tree_sitter_backends.py:106`

---

### `parse`

```matha
def parse(self, source: str) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `ASTNode`

Source: `src\tree_sitter_backends.py:109`

---

## `CParser`

```matha
class CParser
```

**描述**: C 内联 AST 解析器（正则实现）

Source: `src\tree_sitter_backends.py:148`

---

### `parse`

```matha
def parse(self, source: str) -> ASTNode
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `ASTNode`

Source: `src\tree_sitter_backends.py:151`

---

## `get_parser`

```matha
def get_parser(language: str) -> Any
```

**描述**: 根据语言返回对应的解析器实例

| 参数 | 类型 | 说明 |
|------|------|------|
| `language` | `str` | |

**返回**: `Any`

Source: `src\tree_sitter_backends.py:176`

---

## `is_cext_available`

```matha
def is_cext_available() -> bool
```

**描述**: 检查 C 扩展是否可用（当前始终为 False，使用内联解析器）

**返回**: `bool`

Source: `src\tree_sitter_backends.py:190`

---

## `parse_source`

```matha
def parse_source(language: str, source: str) -> ASTNode
```

**描述**: 便捷函数：解析指定语言的源码

| 参数 | 类型 | 说明 |
|------|------|------|
| `language` | `str` | |
| `source` | `str` | |

**返回**: `ASTNode`

Source: `src\tree_sitter_backends.py:195`

---

# `tree_sitter_cext`

```matha

```

**描述**: Tree-Sitter C 扩展模块

将 Rust/Go/JS/C 的树形解析器实现为 Python C 扩展模块，
替代内联 Python 解析器，提升 5-10x 解析性能。

构建:
  python setup.py build_ext --inplace
  或
  pip install -e .

依赖:
  tree-sitter>=0.23.0
  tree-sitter-rust>=0.21.0
  tree-sitter-go>=0.23.0
  tree-sitter-javascript>=0.21.0
  tree-sitter-c>=0.21.0

Source: `src\tree_sitter_cext.py:1`

---

## `_try_load_tree_sitter`

```matha
def _try_load_tree_sitter() -> bool
```

**描述**: 尝试加载 tree-sitter C 扩展。

**返回**: `bool`

Source: `src\tree_sitter_cext.py:35`

---

## `CST_RustParser`

```matha
class CST_RustParser
```

**描述**: 基于 tree-sitter 的 Rust 解析器（C 扩展）。

Source: `src\tree_sitter_cext.py:61`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\tree_sitter_cext.py:64`

---

### `parse`

```matha
def parse(self, source: str) -> 'CSTNode'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `'CSTNode'`

Source: `src\tree_sitter_cext.py:72`

---

## `CST_GoParser`

```matha
class CST_GoParser
```

**描述**: 基于 tree-sitter 的 Go 解析器（C 扩展）。

Source: `src\tree_sitter_cext.py:78`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\tree_sitter_cext.py:81`

---

### `parse`

```matha
def parse(self, source: str) -> 'CSTNode'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `'CSTNode'`

Source: `src\tree_sitter_cext.py:85`

---

## `CST_JSParser`

```matha
class CST_JSParser
```

**描述**: 基于 tree-sitter 的 JavaScript 解析器（C 扩展）。

Source: `src\tree_sitter_cext.py:91`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\tree_sitter_cext.py:94`

---

### `parse`

```matha
def parse(self, source: str) -> 'CSTNode'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `'CSTNode'`

Source: `src\tree_sitter_cext.py:98`

---

## `CST_CParser`

```matha
class CST_CParser
```

**描述**: 基于 tree-sitter 的 C 解析器（C 扩展）。

Source: `src\tree_sitter_cext.py:104`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\tree_sitter_cext.py:107`

---

### `parse`

```matha
def parse(self, source: str) -> 'CSTNode'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `'CSTNode'`

Source: `src\tree_sitter_cext.py:111`

---

## `_cst_from_tree`

```matha
def _cst_from_tree(tree) -> 'CSTNode'
```

**描述**: 将 tree-sitter Tree 转换为统一 CSTNode。

| 参数 | 类型 | 说明 |
|------|------|------|
| `tree` | `` | |

**返回**: `'CSTNode'`

Source: `src\tree_sitter_cext.py:119`

---

## `_extract_fields`

```matha
def _extract_fields(node) -> dict
```

**描述**: 从 tree-sitter 节点提取命名字段。

| 参数 | 类型 | 说明 |
|------|------|------|
| `node` | `` | |

**返回**: `dict`

Source: `src\tree_sitter_cext.py:137`

---

## `CSTNode`

```matha
class CSTNode
```

**描述**: tree-sitter CST 节点（与 ASTNode 接口兼容）。

Source: `src\tree_sitter_cext.py:147`

---

### `__init__`

```matha
def __init__(self, type: str, value: str, children: list, fields: dict)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type` | `str` | |
| `value` | `str` | |
| `children` | `list` | |
| `fields` | `dict` | |

Source: `src\tree_sitter_cext.py:150`

---

### `child`

```matha
def child(self, type_filter: Optional[str]) -> Optional['CSTNode']
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_filter` | `Optional[str]` | |

**返回**: `Optional['CSTNode']`

Source: `src\tree_sitter_cext.py:156`

---

### `children_by`

```matha
def children_by(self, type_filter: str) -> list['CSTNode']
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_filter` | `str` | |

**返回**: `list['CSTNode']`

Source: `src\tree_sitter_cext.py:164`

---

### `leaf_text`

```matha
def leaf_text(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\tree_sitter_cext.py:167`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\tree_sitter_cext.py:172`

---

## `get_cst_parser`

```matha
def get_cst_parser(language: str)
```

**描述**: 根据语言返回对应的 CST 解析器。

| 参数 | 类型 | 说明 |
|------|------|------|
| `language` | `str` | |

Source: `src\tree_sitter_cext.py:178`

---

# `type_system`

```matha

```

**描述**: Matha 静态类型推断系统。

支持：
  - 基本类型推断：Int, Float, String, Bool, List[T], Dict[K,V]
  - 泛型类型：List<T>, Dict<K,V>, Option<T>
  - 函数类型推断：(A, B) -> C
  - 类型检查：编译期/运行期双重校验
  - 类型注解语法：x: Int = 1, func f(x: Float) -> String

使用方式：
  from src.type_system import TypeInferencer, TypeEnv
  inferencer = TypeInferencer()
  errors = inferencer.infer(program)

Source: `src\type_system.py:1`

---

## `TypeKind`

```matha
class TypeKind(Enum)
```

**描述**: 类型分类。

Source: `src\type_system.py:28`

---

## `Type`

```matha
class Type
```

**描述**: 类型表示。

**装饰器**: dataclass(frozen=True)

Source: `src\type_system.py:39`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\type_system.py:45`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\type_system.py:50`

---

### `list_of`

```matha
def list_of(element_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `element_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system.py:62`

---

### `dict_of`

```matha
def dict_of(key_type: 'Type', value_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `key_type` | `'Type'` | |
| `value_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system.py:66`

---

### `tuple_of`

```matha
def tuple_of(*types) -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system.py:70`

---

### `function`

```matha
def function(params: tuple['Type', ...], return_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `params` | `tuple['Type', ...]` | |
| `return_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system.py:74`

---

### `option`

```matha
def option(element_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `element_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system.py:78`

---

### `is_numeric`

```matha
def is_numeric(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\type_system.py:81`

---

### `is_container`

```matha
def is_container(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\type_system.py:84`

---

### `element_type`

```matha
def element_type(self) -> Optional['Type']
```

**描述**: 获取泛型第一个参数（用于 List）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional['Type']`

Source: `src\type_system.py:87`

---

### `value_type`

```matha
def value_type(self) -> Optional['Type']
```

**描述**: 获取 Dict 的值类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional['Type']`

Source: `src\type_system.py:91`

---

## `TypeEnv`

```matha
class TypeEnv
```

**描述**: 类型环境：变量名 → 类型，函数名 → 函数类型。

**装饰器**: dataclass

Source: `src\type_system.py:110`

---

### `define_var`

```matha
def define_var(self, name: str, typ: Type) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `typ` | `Type` | |

**返回**: `None`

Source: `src\type_system.py:116`

---

### `define_func`

```matha
def define_func(self, name: str, typ: Type) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `typ` | `Type` | |

**返回**: `None`

Source: `src\type_system.py:119`

---

### `get_var`

```matha
def get_var(self, name: str) -> Optional[Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Type]`

Source: `src\type_system.py:122`

---

### `get_func`

```matha
def get_func(self, name: str) -> Optional[Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Type]`

Source: `src\type_system.py:125`

---

### `merge`

```matha
def merge(self, other: 'TypeEnv') -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `'TypeEnv'` | |

**返回**: `None`

Source: `src\type_system.py:128`

---

### `copy`

```matha
def copy(self) -> 'TypeEnv'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'TypeEnv'`

Source: `src\type_system.py:133`

---

## `TypeError`

```matha
class TypeError
```

**描述**: 类型错误。

**装饰器**: dataclass

Source: `src\type_system.py:146`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\type_system.py:153`

---

## `TypeInferencer`

```matha
class TypeInferencer
```

**描述**: Matha 静态类型推断器。

对 AST 执行类型推断，返回类型错误列表。

Source: `src\type_system.py:162`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\type_system.py:168`

---

### `_init_builtin_types`

```matha
def _init_builtin_types(self) -> None
```

**描述**: 初始化内建函数类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\type_system.py:174`

---

### `infer`

```matha
def infer(self, program: Any) -> list[TypeError]
```

**描述**: 对程序 AST 执行类型推断。

Args:
    program: ast.Program 或其他 AST 节点

Returns:
    类型错误列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `list[TypeError]`

Source: `src\type_system.py:215`

---

### `_infer_decl`

```matha
def _infer_decl(self, decl: Any) -> None
```

**描述**: 推断声明的类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `Any` | |

**返回**: `None`

Source: `src\type_system.py:238`

---

### `_infer_binding`

```matha
def _infer_binding(self, binding: Any) -> None
```

**描述**: 推断变量绑定类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `binding` | `Any` | |

**返回**: `None`

Source: `src\type_system.py:254`

---

### `_infer_func_def`

```matha
def _infer_func_def(self, func_def: Any) -> None
```

**描述**: 推断函数定义类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_def` | `Any` | |

**返回**: `None`

Source: `src\type_system.py:263`

---

### `_infer_expr`

```matha
def _infer_expr(self, expr: Any) -> Type
```

**描述**: 推断表达式类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Type`

Source: `src\type_system.py:292`

---

### `_infer_binary_op`

```matha
def _infer_binary_op(self, expr: Any) -> Type
```

**描述**: 推断二元运算符类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Type`

Source: `src\type_system.py:391`

---

### `_infer_func_app`

```matha
def _infer_func_app(self, expr: Any) -> Type
```

**描述**: 推断函数调用类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Type`

Source: `src\type_system.py:430`

---

### `_infer_stmt`

```matha
def _infer_stmt(self, stmt: Any) -> None
```

**描述**: 推断语句。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `Any` | |

**返回**: `None`

Source: `src\type_system.py:455`

---

### `_get_var_name`

```matha
def _get_var_name(self, target: Any) -> Optional[str]
```

**描述**: 获取变量名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `target` | `Any` | |

**返回**: `Optional[str]`

Source: `src\type_system.py:473`

---

### `_type_compatible`

```matha
def _type_compatible(self, source: Type, target: Type) -> bool
```

**描述**: 检查类型兼容性。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `Type` | |
| `target` | `Type` | |

**返回**: `bool`

Source: `src\type_system.py:479`

---

### `_infer_type_expr`

```matha
def _infer_type_expr(self, type_expr: Any) -> Type
```

**描述**: 推断类型表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_expr` | `Any` | |

**返回**: `Type`

Source: `src\type_system.py:487`

---

## `TypeChecker`

```matha
class TypeChecker
```

**描述**: 运行期类型检查器。

Source: `src\type_system.py:507`

---

### `check`

```matha
def check(cls, value: Any, expected_type_name: str, var_name: str) -> bool
```

**描述**: 运行期类型检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `value` | `Any` | |
| `expected_type_name` | `str` | |
| `var_name` | `str` | |

**返回**: `bool`

**装饰器**: classmethod

Source: `src\type_system.py:520`

---

### `check_numeric`

```matha
def check_numeric(cls, value: Any, var_name: str) -> bool
```

**描述**: 检查数值类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `value` | `Any` | |
| `var_name` | `str` | |

**返回**: `bool`

**装饰器**: classmethod

Source: `src\type_system.py:534`

---

### `coerce`

```matha
def coerce(cls, value: Any, target_type: str) -> Any
```

**描述**: 类型转换。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `value` | `Any` | |
| `target_type` | `str` | |

**返回**: `Any`

**装饰器**: classmethod

Source: `src\type_system.py:541`

---

## `GenericType`

```matha
class GenericType
```

**描述**: 泛型类型支持。

Source: `src\type_system.py:559`

---

### `bind`

```matha
def bind(cls, type_var: str, concrete_type: Type) -> None
```

**描述**: 绑定泛型参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `type_var` | `str` | |
| `concrete_type` | `Type` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\type_system.py:566`

---

### `resolve`

```matha
def resolve(cls, type_expr: Type) -> Type
```

**描述**: 解析泛型类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `type_expr` | `Type` | |

**返回**: `Type`

**装饰器**: classmethod

Source: `src\type_system.py:571`

---

### `infer_generic`

```matha
def infer_generic(cls, func_name: str, arg_types: tuple[Type, ...]) -> dict[str, Type]
```

**描述**: 从函数调用推断泛型参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `func_name` | `str` | |
| `arg_types` | `tuple[Type, ...]` | |

**返回**: `dict[str, Type]`

**装饰器**: classmethod

Source: `src\type_system.py:579`

---

# `type_system_v2`

```matha

```

**描述**: Matha 类型系统增强 v2.0
========================
新增类型系统能力：
  1. 依赖类型（Dependent Types）- 值约束类型
  2. 子类型系统（Subtyping）- 继承关系
  3. 精炼类型（Refinement Types）- 谓词约束
  4. 泛型约束（Generic Constraints）- where 子句
  5. 类型别名（Type Aliases）- 可读性增强
  6. 枚举类型（Enum）- 有限值集合

与 Rust/Lean 的对比：
  - Rust: Trait 系统 + 泛型约束
  - Lean: 依赖类型 + 命题即类型
  - Matha: 精炼类型 + 子类型 + 依赖类型（简化版）

Source: `src\type_system_v2.py:1`

---

## `TypeKind`

```matha
class TypeKind(Enum)
```

**描述**: 类型分类。

Source: `src\type_system_v2.py:31`

---

## `Type`

```matha
class Type
```

**描述**: 类型表示。

**装饰器**: dataclass(frozen=True)

Source: `src\type_system_v2.py:50`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\type_system_v2.py:58`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\type_system_v2.py:65`

---

### `list_of`

```matha
def list_of(element_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `element_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:78`

---

### `dict_of`

```matha
def dict_of(key_type: 'Type', value_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `key_type` | `'Type'` | |
| `value_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:82`

---

### `tuple_of`

```matha
def tuple_of(*types) -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:86`

---

### `function`

```matha
def function(params: Tuple['Type', ...], return_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `params` | `Tuple['Type', ...]` | |
| `return_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:90`

---

### `option`

```matha
def option(element_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `element_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:94`

---

### `union`

```matha
def union(*types) -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:98`

---

### `refinement`

```matha
def refinement(name: str, predicate: str) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `predicate` | `str` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:102`

---

### `dependent`

```matha
def dependent(param_name: str, param_type: 'Type', body_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `param_name` | `str` | |
| `param_type` | `'Type'` | |
| `body_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:106`

---

### `subtype`

```matha
def subtype(name: str, supertype: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `supertype` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:111`

---

### `enum`

```matha
def enum(name: str, members: List[str]) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `members` | `List[str]` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:115`

---

### `alias`

```matha
def alias(name: str, underlying: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `str` | |
| `underlying` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\type_system_v2.py:119`

---

## `SubtypeRegistry`

```matha
class SubtypeRegistry
```

**描述**: 子类型关系注册表。

支持声明：
  Dog <: Animal
  Animal <: LivingBeing
 正方形 <: 矩形 <: 四边形

Source: `src\type_system_v2.py:137`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\type_system_v2.py:147`

---

### `add_subtype`

```matha
def add_subtype(self, subtype: str, supertype: str) -> None
```

**描述**: 添加子类型关系。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `subtype` | `str` | |
| `supertype` | `str` | |

**返回**: `None`

Source: `src\type_system_v2.py:151`

---

### `is_subtype_of`

```matha
def is_subtype_of(self, subtype: str, supertype: str) -> bool
```

**描述**: 检查 subtype 是否是 supertype 的子类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `subtype` | `str` | |
| `supertype` | `str` | |

**返回**: `bool`

Source: `src\type_system_v2.py:158`

---

### `get_supertypes`

```matha
def get_supertypes(self, subtype: str) -> Set[str]
```

**描述**: 获取所有父类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `subtype` | `str` | |

**返回**: `Set[str]`

Source: `src\type_system_v2.py:175`

---

### `get_hierarchy`

```matha
def get_hierarchy(self, type_name: str) -> List[str]
```

**描述**: 获取类型层次链。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_name` | `str` | |

**返回**: `List[str]`

Source: `src\type_system_v2.py:187`

---

## `RefinementChecker`

```matha
class RefinementChecker
```

**描述**: 精炼类型谓词检查器。

支持谓词：
  x > 0       (正数)
  x >= min && x <= max  (范围约束)
  len(s) > 0  (非空字符串)
  n % 2 == 0  (偶数)

Source: `src\type_system_v2.py:201`

---

### `check`

```matha
def check(self, value: Any, predicate: str) -> bool
```

**描述**: 检查值是否满足谓词。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |
| `predicate` | `str` | |

**返回**: `bool`

Source: `src\type_system_v2.py:220`

---

## `TypeConstraint`

```matha
class TypeConstraint
```

**描述**: 类型约束。

**装饰器**: dataclass

Source: `src\type_system_v2.py:238`

---

## `TypeError`

```matha
class TypeError
```

**描述**: 类型错误。

**装饰器**: dataclass

Source: `src\type_system_v2.py:246`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\type_system_v2.py:253`

---

## `EnhancedTypeInferencer`

```matha
class EnhancedTypeInferencer
```

**描述**: 增强型类型推断器 v2.0。

新增能力：
  1. 依赖类型推断
  2. 子类型检查
  3. 精炼类型验证
  4. 泛型约束检查
  5. 类型别名解析

Source: `src\type_system_v2.py:258`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\type_system_v2.py:270`

---

### `_init_builtin_types`

```matha
def _init_builtin_types(self) -> None
```

**描述**: 初始化内建类型和函数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\type_system_v2.py:279`

---

### `define_alias`

```matha
def define_alias(self, name: str, type_expr: Type) -> None
```

**描述**: 定义类型别名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `type_expr` | `Type` | |

**返回**: `None`

Source: `src\type_system_v2.py:293`

---

### `define_enum`

```matha
def define_enum(self, name: str, members: List[str]) -> None
```

**描述**: 定义枚举类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `members` | `List[str]` | |

**返回**: `None`

Source: `src\type_system_v2.py:297`

---

### `add_subtype`

```matha
def add_subtype(self, subtype: str, supertype: str) -> None
```

**描述**: 添加子类型关系。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `subtype` | `str` | |
| `supertype` | `str` | |

**返回**: `None`

Source: `src\type_system_v2.py:301`

---

### `infer`

```matha
def infer(self, expr: str, context: Dict[str, Type]) -> Type
```

**描述**: 推断表达式类型。

支持：
  - 基本类型: 1 -> Int, 1.5 -> Float, "hello" -> String
  - 函数调用: sin(1.5) -> Float
  - 泛型: [1, 2, 3] -> List[Int]
  - 精炼类型: {x: Int | x > 0}

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `context` | `Dict[str, Type]` | |

**返回**: `Type`

Source: `src\type_system_v2.py:305`

---

### `check_subtype`

```matha
def check_subtype(self, subtype: Type, supertype: Type) -> bool
```

**描述**: 检查子类型关系。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `subtype` | `Type` | |
| `supertype` | `Type` | |

**返回**: `bool`

Source: `src\type_system_v2.py:377`

---

### `check_refinement`

```matha
def check_refinement(self, value: Any, predicate: str) -> bool
```

**描述**: 检查精炼类型谓词。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |
| `predicate` | `str` | |

**返回**: `bool`

Source: `src\type_system_v2.py:385`

---

### `infer_program`

```matha
def infer_program(self, program: str) -> List[Type]
```

**描述**: 推断整个程序的类型列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `str` | |

**返回**: `List[Type]`

Source: `src\type_system_v2.py:389`

---

## `TypeChecker`

```matha
class TypeChecker
```

**描述**: 类型检查器（编译期 + 运行期）。

Source: `src\type_system_v2.py:404`

---

### `__init__`

```matha
def __init__(self, inferencer: EnhancedTypeInferencer)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `inferencer` | `EnhancedTypeInferencer` | |

Source: `src\type_system_v2.py:407`

---

### `check`

```matha
def check(self, expr: str, expected: Type, context: Dict[str, Type]) -> bool
```

**描述**: 检查表达式类型是否与预期匹配。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `str` | |
| `expected` | `Type` | |
| `context` | `Dict[str, Type]` | |

**返回**: `bool`

Source: `src\type_system_v2.py:411`

---

### `check_call`

```matha
def check_call(self, func_name: str, args: List[Any], expected_sig: Type) -> bool
```

**描述**: 检查函数调用签名。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `args` | `List[Any]` | |
| `expected_sig` | `Type` | |

**返回**: `bool`

Source: `src\type_system_v2.py:422`

---

# `typesystem_v2`

```matha

```

**描述**: Matha 增强类型系统：约束求解 + 模式匹配 + 运行时检查。

升级内容：
  1. 约束求解器：从函数调用推导泛型参数
  2. 模式匹配类型推断：match arm 类型传播
  3. 类型约束：where T: Numeric / T: Comparable
  4. 运行时类型检查：可选的严格模式
  5. 类型缓存：跨调用复用推断结果

Source: `src\typesystem_v2.py:1`

---

## `TypeKind`

```matha
class TypeKind(Enum)
```

Source: `src\typesystem_v2.py:23`

---

## `Type`

```matha
class Type
```

**装饰器**: dataclass(frozen=True)

Source: `src\typesystem_v2.py:35`

---

### `__str__`

```matha
def __str__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\typesystem_v2.py:41`

---

### `int_type`

```matha
def int_type() -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:50`

---

### `float_type`

```matha
def float_type() -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:54`

---

### `string_type`

```matha
def string_type() -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:58`

---

### `bool_type`

```matha
def bool_type() -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:62`

---

### `void_type`

```matha
def void_type() -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:66`

---

### `any_type`

```matha
def any_type() -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:70`

---

### `unknown_type`

```matha
def unknown_type() -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:74`

---

### `list_of`

```matha
def list_of(element: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `element` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:78`

---

### `dict_of`

```matha
def dict_of(key: 'Type', value: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `key` | `'Type'` | |
| `value` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:82`

---

### `tuple_of`

```matha
def tuple_of(*types) -> 'Type'
```

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:86`

---

### `option_of`

```matha
def option_of(element: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `element` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:90`

---

### `function`

```matha
def function(params: tuple['Type', ...], ret: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `params` | `tuple['Type', ...]` | |
| `ret` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: staticmethod

Source: `src\typesystem_v2.py:94`

---

### `with_constraints`

```matha
def with_constraints(self, *constraints) -> 'Type'
```

**描述**: 为类型添加约束。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'Type'`

Source: `src\typesystem_v2.py:97`

---

### `is_numeric`

```matha
def is_numeric(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:101`

---

### `is_container`

```matha
def is_container(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:104`

---

### `element_type`

```matha
def element_type(self) -> Optional['Type']
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional['Type']`

Source: `src\typesystem_v2.py:107`

---

### `has_constraint`

```matha
def has_constraint(self, constraint: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `constraint` | `str` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:110`

---

### `__eq__`

```matha
def __eq__(self, other: object) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `object` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:113`

---

## `Constraint`

```matha
class Constraint
```

**描述**: 类型约束。

**装饰器**: dataclass

Source: `src\typesystem_v2.py:139`

---

### `solve`

```matha
def solve(self) -> bool
```

**描述**: 检查约束是否可满足。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:145`

---

### `_is_subtype`

```matha
def _is_subtype(self, sub: Type, super_type: Type) -> bool
```

**描述**: 检查 sub 是否为 super_type 的子类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sub` | `Type` | |
| `super_type` | `Type` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:154`

---

## `ConstraintSolver`

```matha
class ConstraintSolver
```

**描述**: 约束求解器。

Source: `src\typesystem_v2.py:166`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\typesystem_v2.py:169`

---

### `add`

```matha
def add(self, constraint: Constraint) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `constraint` | `Constraint` | |

**返回**: `None`

Source: `src\typesystem_v2.py:173`

---

### `solve`

```matha
def solve(self) -> list[str]
```

**描述**: 求解所有约束，返回错误列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\typesystem_v2.py:176`

---

### `infer_from_call`

```matha
def infer_from_call(self, func_type: Type, arg_types: list[Type]) -> dict[str, Type]
```

**描述**: 从函数调用推断泛型参数。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_type` | `Type` | |
| `arg_types` | `list[Type]` | |

**返回**: `dict[str, Type]`

Source: `src\typesystem_v2.py:184`

---

## `PatternMatchInferencer`

```matha
class PatternMatchInferencer
```

**描述**: 模式匹配类型推断。

Source: `src\typesystem_v2.py:214`

---

### `infer`

```matha
def infer(self, scrutinee_type: Type, patterns: list[tuple[Type, Type]]) -> list[Type]
```

**描述**: 对 match 表达式推断每个分支的类型。

Args:
    scrutinee_type: 被匹配表达式的类型
    patterns: [(模式类型, 分支表达式类型), ...]

Returns:
    各分支的类型列表

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `scrutinee_type` | `Type` | |
| `patterns` | `list[tuple[Type, Type]]` | |

**返回**: `list[Type]`

Source: `src\typesystem_v2.py:217`

---

### `_compatible`

```matha
def _compatible(self, t1: Type, t2: Type) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `t1` | `Type` | |
| `t2` | `Type` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:238`

---

### `_common_type`

```matha
def _common_type(self, types: list[Type]) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `types` | `list[Type]` | |

**返回**: `Type`

Source: `src\typesystem_v2.py:243`

---

## `TypeConstraint`

```matha
class TypeConstraint
```

**描述**: 类型约束定义。

Source: `src\typesystem_v2.py:261`

---

### `satisfies`

```matha
def satisfies(cls, type_name: str, constraint: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `type_name` | `str` | |
| `constraint` | `str` | |

**返回**: `bool`

**装饰器**: classmethod

Source: `src\typesystem_v2.py:277`

---

## `StrictTypeChecker`

```matha
class StrictTypeChecker
```

**描述**: 运行时严格类型检查。

Source: `src\typesystem_v2.py:286`

---

### `__init__`

```matha
def __init__(self, strict: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `strict` | `bool` | |

**返回**: `None`

Source: `src\typesystem_v2.py:298`

---

### `check`

```matha
def check(self, value: Any, expected: Type, var_name: str) -> bool
```

**描述**: 运行时类型检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |
| `expected` | `Type` | |
| `var_name` | `str` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:302`

---

### `get_errors`

```matha
def get_errors(self) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\typesystem_v2.py:319`

---

### `clear`

```matha
def clear(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\typesystem_v2.py:322`

---

## `TypeEnv`

```matha
class TypeEnv
```

**描述**: 增强类型环境。

**装饰器**: dataclass

Source: `src\typesystem_v2.py:331`

---

### `define_var`

```matha
def define_var(self, name: str, typ: Type) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `typ` | `Type` | |

**返回**: `None`

Source: `src\typesystem_v2.py:338`

---

### `define_func`

```matha
def define_func(self, name: str, typ: Type) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `typ` | `Type` | |

**返回**: `None`

Source: `src\typesystem_v2.py:341`

---

### `set_constraint`

```matha
def set_constraint(self, name: str, constraints: list[str]) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |
| `constraints` | `list[str]` | |

**返回**: `None`

Source: `src\typesystem_v2.py:344`

---

### `get_var`

```matha
def get_var(self, name: str) -> Optional[Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Type]`

Source: `src\typesystem_v2.py:347`

---

### `get_func`

```matha
def get_func(self, name: str) -> Optional[Type]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `name` | `str` | |

**返回**: `Optional[Type]`

Source: `src\typesystem_v2.py:350`

---

### `resolve_generic`

```matha
def resolve_generic(self, typ: Type) -> Type
```

**描述**: 解析泛型类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `typ` | `Type` | |

**返回**: `Type`

Source: `src\typesystem_v2.py:353`

---

### `copy`

```matha
def copy(self) -> 'TypeEnv'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'TypeEnv'`

Source: `src\typesystem_v2.py:363`

---

## `EnhancedTypeInferencer`

```matha
class EnhancedTypeInferencer
```

**描述**: 增强类型推断器：约束求解 + 模式匹配 + 运行时检查。

Source: `src\typesystem_v2.py:376`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\typesystem_v2.py:379`

---

### `infer`

```matha
def infer(self, program: Any) -> list[str]
```

**描述**: 对程序执行完整类型推断。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `program` | `Any` | |

**返回**: `list[str]`

Source: `src\typesystem_v2.py:386`

---

### `_init_builtins`

```matha
def _init_builtins(self) -> None
```

**描述**: 初始化内建函数类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\typesystem_v2.py:402`

---

### `_infer_decl`

```matha
def _infer_decl(self, decl: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `decl` | `Any` | |

**返回**: `None`

Source: `src\typesystem_v2.py:427`

---

### `_infer_binding`

```matha
def _infer_binding(self, binding: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `binding` | `Any` | |

**返回**: `None`

Source: `src\typesystem_v2.py:438`

---

### `_infer_func_def`

```matha
def _infer_func_def(self, func_def: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_def` | `Any` | |

**返回**: `None`

Source: `src\typesystem_v2.py:444`

---

### `_infer_expr`

```matha
def _infer_expr(self, expr: Any) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Type`

Source: `src\typesystem_v2.py:471`

---

### `_infer_binary`

```matha
def _infer_binary(self, expr: Any) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Type`

Source: `src\typesystem_v2.py:551`

---

### `_infer_func_app`

```matha
def _infer_func_app(self, expr: Any) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |

**返回**: `Type`

Source: `src\typesystem_v2.py:575`

---

### `_infer_stmt`

```matha
def _infer_stmt(self, stmt: Any) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `stmt` | `Any` | |

**返回**: `None`

Source: `src\typesystem_v2.py:598`

---

### `_infer_type_expr`

```matha
def _infer_type_expr(self, type_expr: Any) -> Type
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `type_expr` | `Any` | |

**返回**: `Type`

Source: `src\typesystem_v2.py:609`

---

### `_compatible`

```matha
def _compatible(self, source: Type, target: Type) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `Type` | |
| `target` | `Type` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:625`

---

### `check_runtime`

```matha
def check_runtime(self, value: Any, expected: Type, var_name: str) -> bool
```

**描述**: 运行时类型检查。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `value` | `Any` | |
| `expected` | `Type` | |
| `var_name` | `str` | |

**返回**: `bool`

Source: `src\typesystem_v2.py:634`

---

### `get_errors`

```matha
def get_errors(self) -> list[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\typesystem_v2.py:638`

---

# `typesystem_v2_fixed`

```matha

```

**描述**: Matha 类型系统 v2 - 约束求解器补全

修复 typesystem_v2.py 中的 NotImplemented，实现完整的约束求解。

Source: `src\typesystem_v2_fixed.py:1`

---

## `Type`

```matha
class Type
```

**描述**: 类型表示。

**装饰器**: dataclass

Source: `src\typesystem_v2_fixed.py:17`

---

### `int_type`

```matha
def int_type(cls) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:25`

---

### `float_type`

```matha
def float_type(cls) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:29`

---

### `string_type`

```matha
def string_type(cls) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:33`

---

### `bool_type`

```matha
def bool_type(cls) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:37`

---

### `void_type`

```matha
def void_type(cls) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:41`

---

### `any_type`

```matha
def any_type(cls) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:45`

---

### `unknown_type`

```matha
def unknown_type(cls) -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:49`

---

### `list_type`

```matha
def list_type(cls, elem: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `elem` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:53`

---

### `dict_type`

```matha
def dict_type(cls, key: 'Type', value: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `key` | `'Type'` | |
| `value` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:57`

---

### `func_type`

```matha
def func_type(cls, params: list['Type'], return_type: 'Type') -> 'Type'
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `params` | `list['Type']` | |
| `return_type` | `'Type'` | |

**返回**: `'Type'`

**装饰器**: classmethod

Source: `src\typesystem_v2_fixed.py:61`

---

### `with_constraints`

```matha
def with_constraints(self, *constraints) -> 'Type'
```

**描述**: 添加约束。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `'Type'`

Source: `src\typesystem_v2_fixed.py:64`

---

### `is_numeric`

```matha
def is_numeric(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\typesystem_v2_fixed.py:73`

---

### `is_container`

```matha
def is_container(self) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\typesystem_v2_fixed.py:76`

---

### `element_type`

```matha
def element_type(self) -> Optional['Type']
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Optional['Type']`

Source: `src\typesystem_v2_fixed.py:79`

---

### `has_constraint`

```matha
def has_constraint(self, constraint: str) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `constraint` | `str` | |

**返回**: `bool`

Source: `src\typesystem_v2_fixed.py:82`

---

### `__eq__`

```matha
def __eq__(self, other: object) -> bool
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `other` | `object` | |

**返回**: `bool`

Source: `src\typesystem_v2_fixed.py:85`

---

### `__hash__`

```matha
def __hash__(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

Source: `src\typesystem_v2_fixed.py:91`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\typesystem_v2_fixed.py:94`

---

## `Constraint`

```matha
class Constraint
```

**描述**: 类型约束。

**装饰器**: dataclass

Source: `src\typesystem_v2_fixed.py:119`

---

### `solve`

```matha
def solve(self) -> bool
```

**描述**: 检查约束是否可满足。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `bool`

Source: `src\typesystem_v2_fixed.py:125`

---

### `_is_subtype`

```matha
def _is_subtype(self, sub: Type, super_type: Type) -> bool
```

**描述**: 检查 sub 是否为 super_type 的子类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sub` | `Type` | |
| `super_type` | `Type` | |

**返回**: `bool`

Source: `src\typesystem_v2_fixed.py:135`

---

### `__repr__`

```matha
def __repr__(self) -> str
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\typesystem_v2_fixed.py:150`

---

## `ConstraintSolver`

```matha
class ConstraintSolver
```

**描述**: 约束求解器。

Source: `src\typesystem_v2_fixed.py:154`

---

### `__init__`

```matha
def __init__(self) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\typesystem_v2_fixed.py:157`

---

### `add`

```matha
def add(self, constraint: Constraint) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `constraint` | `Constraint` | |

**返回**: `None`

Source: `src\typesystem_v2_fixed.py:161`

---

### `add_eq`

```matha
def add_eq(self, left: Type, right: Type) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `left` | `Type` | |
| `right` | `Type` | |

**返回**: `None`

Source: `src\typesystem_v2_fixed.py:164`

---

### `add_subtype`

```matha
def add_subtype(self, sub: Type, super_type: Type) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sub` | `Type` | |
| `super_type` | `Type` | |

**返回**: `None`

Source: `src\typesystem_v2_fixed.py:167`

---

### `solve`

```matha
def solve(self) -> list[str]
```

**描述**: 求解所有约束，返回错误列表。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `list[str]`

Source: `src\typesystem_v2_fixed.py:170`

---

### `solve_and_unify`

```matha
def solve_and_unify(self) -> dict[str, Type]
```

**描述**: 求解并 unify 类型变量。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `dict[str, Type]`

Source: `src\typesystem_v2_fixed.py:178`

---

### `check_type`

```matha
def check_type(self, expr_type: Type, expected_type: Type) -> bool
```

**描述**: 检查表达式类型是否匹配预期类型。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr_type` | `Type` | |
| `expected_type` | `Type` | |

**返回**: `bool`

Source: `src\typesystem_v2_fixed.py:192`

---

# `visual_editor.node_executor`

```matha

```

**描述**: Matha 可视化编程器 - 节点执行引擎

支持拓扑排序执行节点图：
  - 检测循环依赖
  - 拓扑排序计算执行顺序
  - 增量执行（只执行变更节点）
  - 错误处理和恢复
  - 执行监控和日志

Source: `src\visual_editor\node_executor.py:1`

---

## `ExecutionStatus`

```matha
class ExecutionStatus(Enum)
```

**描述**: 执行状态

Source: `src\visual_editor\node_executor.py:23`

---

## `ExecutionError`

```matha
class ExecutionError(Exception)
```

**描述**: 执行错误

Source: `src\visual_editor\node_executor.py:32`

---

### `__init__`

```matha
def __init__(self, message: str, node_id: Optional[str], port_name: Optional[str])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `message` | `str` | |
| `node_id` | `Optional[str]` | |
| `port_name` | `Optional[str]` | |

Source: `src\visual_editor\node_executor.py:34`

---

## `NodeExecutionResult`

```matha
class NodeExecutionResult
```

**描述**: 节点执行结果

Source: `src\visual_editor\node_executor.py:40`

---

### `__init__`

```matha
def __init__(self, node_id: str, node_type: str, status: ExecutionStatus, output: Optional[Dict[str, Any]], error: Optional[str], duration_ms: float)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_id` | `str` | |
| `node_type` | `str` | |
| `status` | `ExecutionStatus` | |
| `output` | `Optional[Dict[str, Any]]` | |
| `error` | `Optional[str]` | |
| `duration_ms` | `float` | |

Source: `src\visual_editor\node_executor.py:42`

---

### `to_dict`

```matha
def to_dict(self) -> Dict[str, Any]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_executor.py:59`

---

## `GraphMetrics`

```matha
class GraphMetrics
```

**描述**: 图指标统计

Source: `src\visual_editor\node_executor.py:71`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\visual_editor\node_executor.py:73`

---

### `to_dict`

```matha
def to_dict(self) -> Dict[str, Any]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_executor.py:81`

---

## `NodeExecutor`

```matha
class NodeExecutor
```

**描述**: 节点执行器

负责：
1. 图结构验证（检测循环依赖）
2. 拓扑排序计算执行顺序
3. 执行节点并传递数据
4. 错误处理和恢复

Source: `src\visual_editor\node_executor.py:92`

---

### `__init__`

```matha
def __init__(self)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

Source: `src\visual_editor\node_executor.py:103`

---

### `add_node`

```matha
def add_node(self, node_id: str, node_data: Dict[str, Any]) -> None
```

**描述**: 添加节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_id` | `str` | |
| `node_data` | `Dict[str, Any]` | |

**返回**: `None`

Source: `src\visual_editor\node_executor.py:113`

---

### `add_connection`

```matha
def add_connection(self, from_node: str, from_port: str, to_node: str, to_port: str) -> None
```

**描述**: 添加连线

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `from_node` | `str` | |
| `from_port` | `str` | |
| `to_node` | `str` | |
| `to_port` | `str` | |

**返回**: `None`

Source: `src\visual_editor\node_executor.py:119`

---

### `remove_node`

```matha
def remove_node(self, node_id: str) -> bool
```

**描述**: 移除节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_id` | `str` | |

**返回**: `bool`

Source: `src\visual_editor\node_executor.py:131`

---

### `remove_connection`

```matha
def remove_connection(self, from_node: str, from_port: str, to_node: str, to_port: str) -> bool
```

**描述**: 移除连线

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `from_node` | `str` | |
| `from_port` | `str` | |
| `to_node` | `str` | |
| `to_port` | `str` | |

**返回**: `bool`

Source: `src\visual_editor\node_executor.py:144`

---

### `clear`

```matha
def clear(self) -> None
```

**描述**: 清空图

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\visual_editor\node_executor.py:154`

---

### `validate_graph`

```matha
def validate_graph(self) -> Tuple[bool, Optional[str]]
```

**描述**: 验证图结构

Returns:
    (是否有效, 错误信息)

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Tuple[bool, Optional[str]]`

Source: `src\visual_editor\node_executor.py:162`

---

### `_detect_cycle`

```matha
def _detect_cycle(self) -> Tuple[bool, List[str]]
```

**描述**: 检测图中是否有循环

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Tuple[bool, List[str]]`

Source: `src\visual_editor\node_executor.py:200`

---

### `compute_execution_order`

```matha
def compute_execution_order(self) -> List[str]
```

**描述**: 计算拓扑排序执行顺序

使用 Kahn 算法：
1. 计算每个节点的入度
2. 将入度为 0 的节点加入队列
3. 依次处理队列中的节点，减少相邻节点的入度
4. 重复直到队列为空

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

Source: `src\visual_editor\node_executor.py:236`

---

### `execute`

```matha
def execute(self, node_executor: Optional[Dict[str, Any]], context: Optional[Dict[str, Any]]) -> Dict[str, Any]
```

**描述**: 执行整个节点图

Args:
    node_executor: 节点执行函数字典 {node_type: func}
    context: 执行上下文（共享数据）

Returns:
    执行结果摘要

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_executor` | `Optional[Dict[str, Any]]` | |
| `context` | `Optional[Dict[str, Any]]` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_executor.py:280`

---

### `execute_incremental`

```matha
def execute_incremental(self, changed_node_ids: Set[str], node_executor: Optional[Dict[str, Any]], context: Optional[Dict[str, Any]]) -> Dict[str, Any]
```

**描述**: 增量执行（只执行变更节点及其依赖）

Args:
    changed_node_ids: 发生变更的节点 ID 集合
    node_executor: 节点执行函数字典
    context: 执行上下文

Returns:
    执行结果摘要

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `changed_node_ids` | `Set[str]` | |
| `node_executor` | `Optional[Dict[str, Any]]` | |
| `context` | `Optional[Dict[str, Any]]` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_executor.py:407`

---

### `_get_affected_nodes`

```matha
def _get_affected_nodes(self, changed_node_ids: Set[str]) -> Set[str]
```

**描述**: 获取受影响的节点（变更节点及其下游依赖）

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `changed_node_ids` | `Set[str]` | |

**返回**: `Set[str]`

Source: `src\visual_editor\node_executor.py:442`

---

### `_collect_inputs`

```matha
def _collect_inputs(self, node_id: str, node_outputs: Dict[str, Dict]) -> Dict[str, Any]
```

**描述**: 收集节点的输入值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_id` | `str` | |
| `node_outputs` | `Dict[str, Dict]` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_executor.py:459`

---

### `_default_execute`

```matha
def _default_execute(self, node_data: Dict, inputs: Dict, context: Dict) -> Dict
```

**描述**: 默认节点执行函数

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_data` | `Dict` | |
| `inputs` | `Dict` | |
| `context` | `Dict` | |

**返回**: `Dict`

Source: `src\visual_editor\node_executor.py:471`

---

### `_execute_math_node`

```matha
def _execute_math_node(self, node_type: str, inputs: Dict) -> Dict
```

**描述**: 执行数学运算节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_type` | `str` | |
| `inputs` | `Dict` | |

**返回**: `Dict`

Source: `src\visual_editor\node_executor.py:490`

---

### `_execute_logic_node`

```matha
def _execute_logic_node(self, node_type: str, inputs: Dict) -> Dict
```

**描述**: 执行逻辑运算节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_type` | `str` | |
| `inputs` | `Dict` | |

**返回**: `Dict`

Source: `src\visual_editor\node_executor.py:544`

---

### `_execute_stats_node`

```matha
def _execute_stats_node(self, node_type: str, inputs: Dict) -> Dict
```

**描述**: 执行统计节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_type` | `str` | |
| `inputs` | `Dict` | |

**返回**: `Dict`

Source: `src\visual_editor\node_executor.py:574`

---

### `_execute_matrix_node`

```matha
def _execute_matrix_node(self, node_type: str, inputs: Dict) -> Dict
```

**描述**: 执行矩阵运算节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_type` | `str` | |
| `inputs` | `Dict` | |

**返回**: `Dict`

Source: `src\visual_editor\node_executor.py:602`

---

### `_execute_special_node`

```matha
def _execute_special_node(self, node_type: str, node_data: Dict, inputs: Dict) -> Dict
```

**描述**: 执行特殊节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_type` | `str` | |
| `node_data` | `Dict` | |
| `inputs` | `Dict` | |

**返回**: `Dict`

Source: `src\visual_editor\node_executor.py:631`

---

### `on_node_execute`

```matha
def on_node_execute(self, callback) -> None
```

**描述**: 注册节点执行回调

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `` | |

**返回**: `None`

Source: `src\visual_editor\node_executor.py:660`

---

### `on_execution_complete`

```matha
def on_execution_complete(self, callback) -> None
```

**描述**: 注册执行完成回调

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `` | |

**返回**: `None`

Source: `src\visual_editor\node_executor.py:664`

---

### `on_error`

```matha
def on_error(self, callback) -> None
```

**描述**: 注册错误回调

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `callback` | `` | |

**返回**: `None`

Source: `src\visual_editor\node_executor.py:668`

---

### `nodes`

```matha
def nodes(self) -> Dict[str, Any]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

**装饰器**: property

Source: `src\visual_editor\node_executor.py:674`

---

### `connections`

```matha
def connections(self) -> List[Dict[str, str]]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[Dict[str, str]]`

**装饰器**: property

Source: `src\visual_editor\node_executor.py:678`

---

### `execution_order`

```matha
def execution_order(self) -> List[str]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[str]`

**装饰器**: property

Source: `src\visual_editor\node_executor.py:682`

---

### `metrics`

```matha
def metrics(self) -> GraphMetrics
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `GraphMetrics`

**装饰器**: property

Source: `src\visual_editor\node_executor.py:686`

---

### `execution_history`

```matha
def execution_history(self) -> List[NodeExecutionResult]
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `List[NodeExecutionResult]`

**装饰器**: property

Source: `src\visual_editor\node_executor.py:690`

---

### `to_dict`

```matha
def to_dict(self) -> Dict[str, Any]
```

**描述**: 序列化为字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_executor.py:693`

---

### `from_dict`

```matha
def from_dict(cls, data: Dict[str, Any]) -> 'NodeExecutor'
```

**描述**: 从字典反序列化

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `data` | `Dict[str, Any]` | |

**返回**: `'NodeExecutor'`

**装饰器**: classmethod

Source: `src\visual_editor\node_executor.py:703`

---

## `get_executor`

```matha
def get_executor() -> NodeExecutor
```

**描述**: 获取全局执行器实例

**返回**: `NodeExecutor`

Source: `src\visual_editor\node_executor.py:716`

---

# `visual_editor.node_types`

```matha

```

**描述**: Matha 可视化编程器 - 节点类型定义

定义各类计算节点的类型、输入输出端口、执行逻辑：
  - 数学运算节点
  - 逻辑判断节点
  - 变量节点
  - 输入输出节点
  - 控制流节点

Source: `src\visual_editor\node_types.py:1`

---

## `NodeType`

```matha
class NodeType(Enum)
```

**描述**: 节点类型枚举

Source: `src\visual_editor\node_types.py:22`

---

## `PortDefinition`

```matha
class PortDefinition
```

**描述**: 端口定义

**装饰器**: dataclass

Source: `src\visual_editor\node_types.py:99`

---

## `NodeDefinition`

```matha
class NodeDefinition
```

**描述**: 节点定义

**装饰器**: dataclass

Source: `src\visual_editor\node_types.py:109`

---

### `input_count`

```matha
def input_count(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\visual_editor\node_types.py:124`

---

### `output_count`

```matha
def output_count(self) -> int
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `int`

**装饰器**: property

Source: `src\visual_editor\node_types.py:128`

---

## `NodeRegistry`

```matha
class NodeRegistry
```

**描述**: 节点注册表

Source: `src\visual_editor\node_types.py:132`

---

### `register`

```matha
def register(cls, definition: NodeDefinition) -> None
```

**描述**: 注册节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `definition` | `NodeDefinition` | |

**返回**: `None`

**装饰器**: classmethod

Source: `src\visual_editor\node_types.py:138`

---

### `get`

```matha
def get(cls, node_type: str) -> Optional[NodeDefinition]
```

**描述**: 获取节点定义

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `node_type` | `str` | |

**返回**: `Optional[NodeDefinition]`

**装饰器**: classmethod

Source: `src\visual_editor\node_types.py:144`

---

### `get_all`

```matha
def get_all(cls) -> Dict[str, NodeDefinition]
```

**描述**: 获取所有节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |

**返回**: `Dict[str, NodeDefinition]`

**装饰器**: classmethod

Source: `src\visual_editor\node_types.py:149`

---

### `get_by_category`

```matha
def get_by_category(cls, category: str) -> List[NodeDefinition]
```

**描述**: 按类别获取节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `category` | `str` | |

**返回**: `List[NodeDefinition]`

**装饰器**: classmethod

Source: `src\visual_editor\node_types.py:154`

---

### `search`

```matha
def search(cls, keyword: str) -> List[NodeDefinition]
```

**描述**: 搜索节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `keyword` | `str` | |

**返回**: `List[NodeDefinition]`

**装饰器**: classmethod

Source: `src\visual_editor\node_types.py:159`

---

## `Node`

```matha
class Node
```

**描述**: 节点实例

Source: `src\visual_editor\node_types.py:168`

---

### `__init__`

```matha
def __init__(self, node_type: NodeType, position: tuple, config: Optional[Dict[str, Any]])
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `node_type` | `NodeType` | |
| `position` | `tuple` | |
| `config` | `Optional[Dict[str, Any]]` | |

Source: `src\visual_editor\node_types.py:171`

---

### `_init_ports`

```matha
def _init_ports(self) -> None
```

**描述**: 初始化端口值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `None`

Source: `src\visual_editor\node_types.py:194`

---

### `set_input`

```matha
def set_input(self, port_name: str, value: Any) -> None
```

**描述**: 设置输入值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port_name` | `str` | |
| `value` | `Any` | |

**返回**: `None`

Source: `src\visual_editor\node_types.py:202`

---

### `get_input`

```matha
def get_input(self, port_name: str) -> Any
```

**描述**: 获取输入值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port_name` | `str` | |

**返回**: `Any`

Source: `src\visual_editor\node_types.py:207`

---

### `set_output`

```matha
def set_output(self, port_name: str, value: Any) -> None
```

**描述**: 设置输出值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port_name` | `str` | |
| `value` | `Any` | |

**返回**: `None`

Source: `src\visual_editor\node_types.py:211`

---

### `get_output`

```matha
def get_output(self, port_name: str) -> Any
```

**描述**: 获取输出值

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `port_name` | `str` | |

**返回**: `Any`

Source: `src\visual_editor\node_types.py:216`

---

### `execute`

```matha
def execute(self) -> Dict[str, Any]
```

**描述**: 执行节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_types.py:220`

---

### `to_dict`

```matha
def to_dict(self) -> Dict[str, Any]
```

**描述**: 序列化为字典

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `Dict[str, Any]`

Source: `src\visual_editor\node_types.py:233`

---

### `from_dict`

```matha
def from_dict(cls, data: Dict[str, Any]) -> 'Node'
```

**描述**: 从字典反序列化

| 参数 | 类型 | 说明 |
|------|------|------|
| `cls` | `` | |
| `data` | `Dict[str, Any]` | |

**返回**: `'Node'`

**装饰器**: classmethod

Source: `src\visual_editor\node_types.py:247`

---

## `_create_math_node`

```matha
def _create_math_node(node_type: NodeType, label: str, category: str, **kwargs) -> NodeDefinition
```

**描述**: 创建数学运算节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `node_type` | `NodeType` | |
| `label` | `str` | |
| `category` | `str` | |

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:261`

---

## `_create_logic_node`

```matha
def _create_logic_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition
```

**描述**: 创建逻辑运算节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `node_type` | `NodeType` | |
| `label` | `str` | |

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:304`

---

## `_create_constant_node`

```matha
def _create_constant_node(node_type: NodeType, label: str, value: Any, **kwargs) -> NodeDefinition
```

**描述**: 创建常量节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `node_type` | `NodeType` | |
| `label` | `str` | |
| `value` | `Any` | |

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:319`

---

## `_create_variable_node`

```matha
def _create_variable_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition
```

**描述**: 创建变量节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `node_type` | `NodeType` | |
| `label` | `str` | |

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:331`

---

## `_create_input_node`

```matha
def _create_input_node(**kwargs) -> NodeDefinition
```

**描述**: 创建输入节点

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:343`

---

## `_create_output_node`

```matha
def _create_output_node(**kwargs) -> NodeDefinition
```

**描述**: 创建输出节点

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:355`

---

## `_create_if_node`

```matha
def _create_if_node(**kwargs) -> NodeDefinition
```

**描述**: 创建条件分支节点

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:367`

---

## `_create_sequence_node`

```matha
def _create_sequence_node(**kwargs) -> NodeDefinition
```

**描述**: 创建序列节点

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:383`

---

## `_create_stats_node`

```matha
def _create_stats_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition
```

**描述**: 创建统计节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `node_type` | `NodeType` | |
| `label` | `str` | |

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:399`

---

## `_create_matrix_node`

```matha
def _create_matrix_node(node_type: NodeType, label: str, **kwargs) -> NodeDefinition
```

**描述**: 创建矩阵运算节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `node_type` | `NodeType` | |
| `label` | `str` | |

**返回**: `NodeDefinition`

Source: `src\visual_editor\node_types.py:421`

---

## `register_all_nodes`

```matha
def register_all_nodes() -> None
```

**描述**: 注册所有节点

**返回**: `None`

Source: `src\visual_editor\node_types.py:452`

---

# `vm`

```matha

```

**描述**: MathaVM：MIR 级原生解释器

与现有 Interpreter（AST 级）的区别：
  - Interpreter: AST → Python 递归求值（慢，~75x 慢于 Python 原生）
  - MathaVM:     MIR → 栈式执行（快，~1-2x 快于 Interpreter）

设计目标：
  1. 直接执行 MIR 指令，无需 Python AST 递归
  2. 与 Interpreter 交叉验证（相同输入 → 相同输出）
  3. 支持所有 MIR 指令类型
  4. 性能基准测试

Source: `src\vm.py:1`

---

## `configure_vm_logging`

```matha
def configure_vm_logging(level: int) -> None
```

**描述**: 配置 VM 日志。

| 参数 | 类型 | 说明 |
|------|------|------|
| `level` | `int` | |

**返回**: `None`

Source: `src\vm.py:30`

---

## `VMResult`

```matha
class VMResult(Enum)
```

**描述**: VM 执行结果。

Source: `src\vm.py:45`

---

## `VMFrame`

```matha
class VMFrame
```

**描述**: 执行帧（函数调用栈）。

**装饰器**: dataclass

Source: `src\vm.py:58`

---

## `VMState`

```matha
class VMState
```

**描述**: VM 全局状态。

**装饰器**: dataclass

Source: `src\vm.py:70`

---

## `MathaVM`

```matha
class MathaVM
```

**描述**: Matha VM：直接执行 MIR 指令的原生解释器。

与 Interpreter 的对比：
┌─────────────────┬──────────────────────┬─────────────────────┐
│     特性        │   Interpreter        │     MathaVM         │
├─────────────────┼──────────────────────┼─────────────────────┤
│ 执行级别         │ AST 递归求值          │ MIR 指令执行         │
│ 性能            │ 慢（~75x）           │ 快（~1-2x）         │
│ 内存占用         │ 高（递归栈）          │ 低（迭代执行）       │
│ 调试能力         │ 好（Python 反射）     │ 中（trace 日志）     │
│ 交叉验证         │ 基准                 │ 对比目标             │
└─────────────────┴──────────────────────┴─────────────────────┘

Source: `src\vm.py:85`

---

### `__init__`

```matha
def __init__(self, max_iterations: int, debug: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `max_iterations` | `int` | |
| `debug` | `bool` | |

**返回**: `None`

Source: `src\vm.py:114`

---

### `run`

```matha
def run(self, mir_program: Any) -> tuple[list, list[str]]
```

**描述**: 运行 MIR 程序，返回 (outputs, trace)。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `mir_program` | `Any` | |

**返回**: `tuple[list, list[str]]`

Source: `src\vm.py:122`

---

### `run_func`

```matha
def run_func(self, func_name: str, args: Optional[list[float]]) -> float
```

**描述**: 运行指定函数，返回结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `func_name` | `str` | |
| `args` | `Optional[list[float]]` | |

**返回**: `float`

Source: `src\vm.py:144`

---

### `_execute_frame`

```matha
def _execute_frame(self, frame: VMFrame) -> None
```

**描述**: 执行单个帧。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |

**返回**: `None`

Source: `src\vm.py:166`

---

### `_execute_instruction`

```matha
def _execute_instruction(self, frame: VMFrame, instr: Any, pc: int) -> VMResult
```

**描述**: 执行单条指令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `instr` | `Any` | |
| `pc` | `int` | |

**返回**: `VMResult`

Source: `src\vm.py:200`

---

### `_execute_arith`

```matha
def _execute_arith(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult
```

**描述**: 执行算术运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `instr` | `Any` | |
| `operands` | `list` | |
| `result_var` | `str` | |

**返回**: `VMResult`

Source: `src\vm.py:267`

---

### `_execute_compare`

```matha
def _execute_compare(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult
```

**描述**: 执行比较运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `instr` | `Any` | |
| `operands` | `list` | |
| `result_var` | `str` | |

**返回**: `VMResult`

Source: `src\vm.py:300`

---

### `_execute_logical`

```matha
def _execute_logical(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult
```

**描述**: 执行逻辑运算。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `instr` | `Any` | |
| `operands` | `list` | |
| `result_var` | `str` | |

**返回**: `VMResult`

Source: `src\vm.py:330`

---

### `_execute_call`

```matha
def _execute_call(self, frame: VMFrame, instr: Any, operands: list, result_var: str, metadata: dict) -> VMResult
```

**描述**: 执行函数调用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `instr` | `Any` | |
| `operands` | `list` | |
| `result_var` | `str` | |
| `metadata` | `dict` | |

**返回**: `VMResult`

Source: `src\vm.py:354`

---

### `_execute_function_call`

```matha
def _execute_function_call(self, caller_frame: VMFrame, func_name: str, func: Any, args: list[float], result_var: str) -> VMResult
```

**描述**: 执行用户函数调用。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `caller_frame` | `VMFrame` | |
| `func_name` | `str` | |
| `func` | `Any` | |
| `args` | `list[float]` | |
| `result_var` | `str` | |

**返回**: `VMResult`

Source: `src\vm.py:395`

---

### `_execute_store`

```matha
def _execute_store(self, frame: VMFrame, instr: Any, operands: list) -> VMResult
```

**描述**: 执行 store 指令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `instr` | `Any` | |
| `operands` | `list` | |

**返回**: `VMResult`

Source: `src\vm.py:424`

---

### `_execute_load`

```matha
def _execute_load(self, frame: VMFrame, instr: Any, operands: list, result_var: str) -> VMResult
```

**描述**: 执行 load 指令。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `instr` | `Any` | |
| `operands` | `list` | |
| `result_var` | `str` | |

**返回**: `VMResult`

Source: `src\vm.py:432`

---

### `_resolve_operand`

```matha
def _resolve_operand(self, frame: VMFrame, op: Any) -> float
```

**描述**: 解析操作数（变量或常量）。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `frame` | `VMFrame` | |
| `op` | `Any` | |

**返回**: `float`

Source: `src\vm.py:442`

---

### `_eval_expr`

```matha
def _eval_expr(self, expr: Any, frame: VMFrame) -> float
```

**描述**: 递归求值表达式。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `expr` | `Any` | |
| `frame` | `VMFrame` | |

**返回**: `float`

Source: `src\vm.py:455`

---

## `CrossVerifier`

```matha
class CrossVerifier
```

**描述**: 交叉验证器：对比 Interpreter (AST) 和 MathaVM (MIR) 的执行结果。

工作流程：
1. 解析 Matha 源码为 AST
2. 用 Interpreter 执行 AST
3. 将 AST 编译为 MIR
4. 用 MathaVM 执行 MIR
5. 对比两者的输出和 trace

Source: `src\vm.py:482`

---

### `__init__`

```matha
def __init__(self, verbose: bool) -> None
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `verbose` | `bool` | |

**返回**: `None`

Source: `src\vm.py:494`

---

### `verify`

```matha
def verify(self, source: str) -> dict
```

**描述**: 验证源码在两个解释器中产生相同结果。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `source` | `str` | |

**返回**: `dict`

Source: `src\vm.py:498`

---

### `batch_verify`

```matha
def batch_verify(self, sources: list[str]) -> dict
```

**描述**: 批量验证多个源码。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |
| `sources` | `list[str]` | |

**返回**: `dict`

Source: `src\vm.py:553`

---

### `get_summary`

```matha
def get_summary(self) -> str
```

**描述**: 获取验证摘要。

| 参数 | 类型 | 说明 |
|------|------|------|
| `self` | `` | |

**返回**: `str`

Source: `src\vm.py:573`

---

## `benchmark`

```matha
def benchmark(interpreter_source: str, iterations: int) -> dict
```

**描述**: 性能基准测试。

| 参数 | 类型 | 说明 |
|------|------|------|
| `interpreter_source` | `str` | |
| `iterations` | `int` | |

**返回**: `dict`

Source: `src\vm.py:584`

---
