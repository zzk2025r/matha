import React from 'react';
import ReactDOM from 'react-dom/client';
import MathaAdminApp from './admin/index';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MathaAdminApp />
  </React.StrictMode>
);
